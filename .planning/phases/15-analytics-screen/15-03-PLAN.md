---
phase: 15-analytics-screen
plan: 03
type: execute
wave: 3
depends_on: ["15-01", "15-02"]
files_modified:
  - MyHomeApp/Features/Analytics/DeltaChip.swift
  - MyHomeApp/Features/Analytics/DeltaDrillDownSheet.swift
  - MyHomeApp/Features/Analytics/AnalyticsView.swift
  - MyHome.xcodeproj/project.pbxproj
autonomous: false
requirements: [ANL-05, ANL-06, ANL-07]
user_setup: []
must_haves:
  truths:
    - "The delta chip uses inverted color: green (DesignTokens.positive) when spend DECREASED vs prior period, coral (DesignTokens.negative) when it INCREASED (ANL-05)"
    - "Tapping the delta chip presents DeltaDrillDownSheet showing per-category current vs prior spend, sorted by absolute delta (ANL-06)"
    - "A final xcodebuild clean build succeeds with all new Features/Analytics + Support files registered in project.pbxproj (success criterion 7)"
    - "The full test suite passes, including AnalyticsAggregatorTests.testMidnightISTBucketBoundary (success criterion 6 / ANL-07)"
  artifacts:
    - path: "MyHomeApp/Features/Analytics/DeltaChip.swift"
      provides: "ANL-05 inverted-color delta chip with onTap"
      contains: "struct DeltaChip"
    - path: "MyHomeApp/Features/Analytics/DeltaDrillDownSheet.swift"
      provides: "ANL-06 per-category current-vs-prior drill-down sheet"
      contains: "struct DeltaDrillDownSheet"
  key_links:
    - from: "MyHomeApp/Features/Analytics/DeltaChip.swift"
      to: "DesignTokens.positive / DesignTokens.negative"
      via: "inverted: delta > 0 ? negative : positive (ANL-05, Pitfall 6)"
      pattern: "delta > 0 \\? DesignTokens.negative : DesignTokens.positive"
    - from: "MyHomeApp/Features/Analytics/AnalyticsView.swift"
      to: "DeltaDrillDownSheet"
      via: ".sheet(isPresented: $showDeltaDrillDown) on chip tap (ANL-06)"
      pattern: "showDeltaDrillDown"
    - from: "MyHomeApp/Features/Analytics/DeltaDrillDownSheet.swift"
      to: "SpendSummary.priorCategorySpend"
      via: "keyed lookup for per-category prior totals"
      pattern: "priorCategorySpend"
---

<objective>
Close Phase 15: add the period-over-period delta chip with the inverted color convention (ANL-05),
its tap-driven drill-down sheet showing which categories drove the change (ANL-06), wire both into
AnalyticsView, then run the phase-closing full clean-build + test gate (success criteria 6 & 7) and a
human-verify checkpoint on the iPhone 17 simulator.

Purpose: this is the phase-completing plan. It delivers the last two visible features and proves the
whole Phase 15 stack (aggregator + screen + nav + chips) compiles clean and passes tests end-to-end,
then gets human eyes on the visuals before the phase is marked done. Autonomous is false because of the
human-verify checkpoint.

Output: DeltaChip.swift + DeltaDrillDownSheet.swift (new, pbxproj-registered under G_ANL), AnalyticsView
wiring, a green full suite, and a human-approved visual pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-analytics-screen/15-RESEARCH.md
@.planning/phases/15-analytics-screen/15-CONTEXT.md
@.planning/phases/15-analytics-screen/15-01-SUMMARY.md
@.planning/phases/15-analytics-screen/15-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: DeltaChip with inverted color + DeltaDrillDownSheet, wired into AnalyticsView (ANL-05/06)</name>
  <files>MyHomeApp/Features/Analytics/DeltaChip.swift, MyHomeApp/Features/Analytics/DeltaDrillDownSheet.swift, MyHomeApp/Features/Analytics/AnalyticsView.swift, MyHome.xcodeproj/project.pbxproj</files>
  <read_first>
    - 15-RESEARCH.md lines 382-442 (DeltaChip full design: inverted chipColor, arrow.up/down, pctLabel zero-guard; DeltaDrillDownSheet rationale + .sheet wiring + priorCategorySpend keyed lookup, sorted by absolute delta)
    - 15-RESEARCH.md lines 578-585 (Pitfall 6 color inversion; Pitfall 7 NSDecimalNumber Decimal-division — pctLabel must keep delta/prior in Decimal before .doubleValue)
    - MyHomeApp/Support/AnalyticsAggregator.swift (SpendSummary: delta, deltaFraction, priorTotalSpend, categoryBreakdown, priorCategorySpend keyed by PersistentIdentifier)
    - MyHomeApp/DesignSystem/DesignTokens.swift lines 23-24 (positive #34E29B green, negative #FF6B6B coral) + NeuSurface
    - MyHomeApp/Support/Decimal+INR.swift (formattedINRWhole for sheet rows)
    - MyHomeApp/Features/Analytics/AnalyticsView.swift (self — headline card from 15-02 where the chip mounts)
    - any existing .sheet site, e.g. MyHomeApp/Features/Expenses/EditExpenseView.swift, for the app's sheet presentation convention
    - MyHome.xcodeproj/project.pbxproj — G_ANL group children (created in 15-02), PBXFileReference/PBXBuildFile sections, app Sources phase line 933
  </read_first>
  <action>
    Create `DeltaChip.swift` (`import SwiftUI`): `struct DeltaChip: View` taking `let delta: Decimal`,
    `let priorTotal: Decimal`, `let onTap: () -> Void`. INVERTED color semantics (ANL-05, Pitfall 6):
    `isPositive = delta > 0` means spent MORE → `chipColor = delta > 0 ? DesignTokens.negative : DesignTokens.positive`
    (green when delta <= 0 = spent less; coral when delta > 0 = spent more). Add an inline comment citing ANL-05 / the
    inversion. Arrow is `arrow.up` when delta > 0 else `arrow.down`. The chip is a `Button(action: onTap)` with an HStack
    of the SF arrow + a `pctLabel` (`.monospacedDigit()`), `.foregroundStyle(chipColor)`, `chipColor.opacity(0.15)`
    background, `.clipShape(Capsule())`, `.buttonStyle(.plain)`, plus an `.accessibilityLabel` describing the direction +
    percent. `pctLabel`: `guard priorTotal > 0 else { return "—" }`, then
    `abs(NSDecimalNumber(decimal: delta / priorTotal).doubleValue) * 100` formatted `"%.0f%%"` — keep the division in
    Decimal until the NSDecimalNumber conversion (Pitfall 7: never `Double(delta)/Double(priorTotal)`).
    Create `DeltaDrillDownSheet.swift` (`import SwiftUI / SwiftData`): `struct DeltaDrillDownSheet: View` taking
    `let summary: SpendSummary`. In a NavigationStack-wrapped List/ScrollView on `DesignTokens.bgCanvas`, render one row
    per category present in `summary.categoryBreakdown`, each showing: category name, current spend
    (`item.spentDecimal.formattedINRWhole()`), prior spend looked up from `summary.priorCategorySpend[item.id] ?? .zero`,
    and a per-category `DeltaChip(delta: current - prior, priorTotal: prior) { }` (non-interactive inner chip is fine —
    pass an empty closure). Sort rows DESCENDING by absolute per-category delta (biggest movers first, 15-RESEARCH line 442).
    Add a "Done"/dismiss affordance. Style rows with `.neuSurface(.recessed)` or list-row tokens consistent with the app.
    Wire into `AnalyticsView`: add `@State private var showDeltaDrillDown = false`; in the headline card mount
    `DeltaChip(delta: summary.delta, priorTotal: summary.priorTotalSpend) { showDeltaDrillDown = true }`; add
    `.sheet(isPresented: $showDeltaDrillDown) { DeltaDrillDownSheet(summary: summary) }`.
    Register both new files: 4 pbxproj edits each — `FANL05 /* DeltaChip.swift */` and `FANL06 /* DeltaDrillDownSheet.swift */`
    added to the G_ANL group children, PBXFileReference + PBXBuildFile entries, and `AANL05`/`AANL06` build files added to
    the APP target Sources phase (line 933). No synchronized groups — manual registration required.
  </action>
  <verify>
    <automated>cd "/Users/reo/My Projects/my-home" && xcodebuild build -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `xcodebuild build -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17'` succeeds (both files registered + compile).
    - `grep -c 'delta > 0 ? DesignTokens.negative : DesignTokens.positive' MyHomeApp/Features/Analytics/DeltaChip.swift` returns 1 (inverted color, ANL-05 / Pitfall 6).
    - `grep -c 'Double(delta)' MyHomeApp/Features/Analytics/DeltaChip.swift` returns 0 (Pitfall 7: Decimal division preserved).
    - `grep -c 'priorCategorySpend' MyHomeApp/Features/Analytics/DeltaDrillDownSheet.swift` returns >= 1 (per-category prior lookup, ANL-06).
    - `grep -c 'showDeltaDrillDown' MyHomeApp/Features/Analytics/AnalyticsView.swift` returns >= 2 (@State + sheet/chip wiring).
    - `grep -c 'FANL05' MyHome.xcodeproj/project.pbxproj` returns >= 2 and `grep -c 'FANL06' MyHome.xcodeproj/project.pbxproj` returns >= 2.
  </acceptance_criteria>
  <done>DeltaChip (inverted color) + DeltaDrillDownSheet exist, registered under G_ANL, wired into AnalyticsView with chip tap presenting the sheet; builds clean.</done>
</task>

<task type="auto">
  <name>Task 2: Phase-closing full clean-build + full test gate (success criteria 6 & 7 / ANL-07)</name>
  <files>MyHome.xcodeproj/project.pbxproj</files>
  <read_first>
    - 15-CONTEXT.md lines 51-52 (success criteria 6 & 7: testMidnightISTBucketBoundary passes; xcodebuild clean build succeeds after all Features/Analytics + Support files registered)
    - 15-RESEARCH.md lines 654-660 (full suite command + quick run command for AnalyticsAggregatorTests)
  </read_first>
  <action>
    Run the phase-closing gate. First a full `xcodebuild clean build` for the MyHome scheme on the iPhone 17 simulator to
    prove ALL new Features/Analytics files (AnalyticsView, AnalyticsTrendChart, AnalyticsCategoryBars, DeltaChip,
    DeltaDrillDownSheet) and the Support file (AnalyticsAggregator) are registered in project.pbxproj and compile from a
    clean state (success criterion 7). Then run the full test suite to confirm no regressions and that
    AnalyticsAggregatorTests.testMidnightISTBucketBoundary passes (success criterion 6 / ANL-07). If either gate fails,
    fix the missing pbxproj registration or the failing test before proceeding — do NOT advance to the human checkpoint
    on a red gate. This task modifies project.pbxproj only if a registration gap is discovered during the gate.
  </action>
  <verify>
    <automated>cd "/Users/reo/My Projects/my-home" && xcodebuild clean build test -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `xcodebuild clean build test -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17'` ends with "** TEST SUCCEEDED **" (clean build + full suite green — success criteria 6 & 7).
    - The test output shows `testMidnightISTBucketBoundary` among the passing tests (ANL-07 exit criterion).
    - `grep -c 'AnalyticsAggregator.swift' MyHome.xcodeproj/project.pbxproj` >= 2 AND each of FANL02..FANL06 appears >= 2 times (all six new files registered).
  </acceptance_criteria>
  <done>Full clean build + full test suite green on iPhone 17, including testMidnightISTBucketBoundary; all six new files confirmed registered — success criteria 6 & 7 met.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human visual verification of Analytics screen on iPhone 17 simulator</name>
  <what-built>
    The complete Analytics screen: push entry from Overview (ANL-01), Week/Month/Year segmented range control (ANL-02),
    AreaMark spending-trend chart with IST bucketing and no future Year zero-bars (ANL-03), by-category bars sorted
    descending in the neumorphic palette (ANL-04), an inverted-color period-over-period delta chip (ANL-05), and a
    tap-driven per-category drill-down sheet (ANL-06) — all in the v1.2 neumorphic design language.
  </what-built>
  <how-to-verify>
    1. Run the app on the iPhone 17 simulator (Xcode 26.5, scheme MyHome). To get realistic data, launch with the DEBUG
       `-seedSampleData` launch argument (the seeder exists in MyHomeApp.swift) so trend/category/delta have content.
    2. On the Overview screen, find the Analytics entry (the "See analytics" CTA on the "Over Time" section header) and
       tap it once. CONFIRM: the Analytics screen slides in via a navigation push (back button appears); the tab bar is
       unchanged and Analytics is NOT a new tab (success criterion 1 / ANL-01).
    3. Toggle Week → Month → Year on the segmented control. CONFIRM: the headline total, trend chart, category bars, and
       delta chip all update together with no stale/frozen content (success criterion 2 / ANL-02). On Year, CONFIRM the
       trend shows only months up to and including the current month — no empty future months (success criterion 3 / ANL-03).
    4. CONFIRM the category bars list every category for the range, ordered largest spend first, using the correct
       per-category neumorphic colors (success criterion 4 / ANL-04).
    5. CONFIRM the delta chip color is inverted: green when you spent LESS than the prior period, coral when you spent MORE
       (ANL-05). Tap the delta chip. CONFIRM a drill-down sheet appears listing categories with current vs prior spend,
       biggest movers first (ANL-06).
    6. CONFIRM the headline number uses solid bold default-design numerals (not thin rounded), surfaces are neumorphic
       (.neuSurface), and money reads as rounded word amounts where expected — matching Phases 13–14 design language.
  </how-to-verify>
  <resume-signal>Type "approved" if all six checks pass, or describe what looks wrong (e.g., delta color not inverted, Year shows future months, range switch stale) and it will be fixed before the phase is marked complete.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | DeltaChip/DeltaDrillDownSheet render already-aggregated SpendSummary fields; the drill-down sheet uses the same in-process `.sheet` convention as the rest of the app (EditExpenseView, EditBudgetSheet). No new external input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-07 | Repudiation / UX-correctness | Delta chip color semantics | mitigate | Inversion (`delta > 0 ? negative : positive`) is asserted by a grep gate + verified by the human-verify checkpoint, preventing a silently-wrong "green = spent more" reading that would mislead the user (ANL-05). |
| T-15-08 | Information Disclosure | DeltaDrillDownSheet per-category rows | mitigate | Sheet reads SpendSummary.categoryBreakdown + priorCategorySpend, both self-transfer-excluded upstream (15-01); no raw transfer rows surface. |
| T-15-09 | Tampering | Final pbxproj registration of all 6 new files | mitigate | The Task-2 clean-build+test gate fails if any of AnalyticsAggregator + FANL02..FANL06 registration is missing — success criterion 7 enforced, not assumed. |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | No package installs; first-party Swift/SwiftUI/Charts/SwiftData only. |
</threat_model>

<verification>
- DeltaChip uses inverted color (green = spent less, coral = spent more) — grep gate + human-verify (ANL-05).
- Delta chip tap presents per-category drill-down sorted by absolute delta (ANL-06).
- Full clean build + full test suite green on iPhone 17, including testMidnightISTBucketBoundary (success criteria 6 & 7 / ANL-07).
- Human confirms the live screen against all seven phase success criteria.
</verification>

<success_criteria>
- Delta chips inverted color + tap drill-down working (success criterion 5 / ANL-05, ANL-06).
- testMidnightISTBucketBoundary passes in the full suite (success criterion 6 / ANL-07).
- xcodebuild clean build succeeds with all Features/Analytics + Support files registered (success criterion 7).
- Human-verify checkpoint approved on the iPhone 17 simulator.
</success_criteria>

<output>
Create `.planning/phases/15-analytics-screen/15-03-SUMMARY.md` when done
</output>
