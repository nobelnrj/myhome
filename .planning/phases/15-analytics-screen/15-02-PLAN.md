---
phase: 15-analytics-screen
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - MyHomeApp/Features/Analytics/AnalyticsView.swift
  - MyHomeApp/Features/Analytics/AnalyticsTrendChart.swift
  - MyHomeApp/Features/Analytics/AnalyticsCategoryBars.swift
  - MyHomeApp/Features/Overview/OverviewView.swift
  - MyHome.xcodeproj/project.pbxproj
autonomous: true
requirements: [ANL-01, ANL-02, ANL-03, ANL-04]
user_setup: []
must_haves:
  truths:
    - "A single tap on an Analytics entry point on Overview pushes AnalyticsView via .navigationDestination (slide-in); it is NOT a tab and the tab bar is unchanged (ANL-01)"
    - "AnalyticsView owns @State selectedRange (week/month/year) via a segmented Picker; changing it re-runs AnalyticsAggregator.summarize at the top of body so headline, trend chart, and category bars update with no stale data (ANL-02)"
    - "The trend chart is an AreaMark + LineMark over pre-aggregated SpendBucket values (Pitfall A — never raw @Query into Chart); Year shows only months up to current month (ANL-03)"
    - "The by-category breakdown shows all categories for the range, sorted descending by spend, with the neumorphic category palette (ANL-04)"
  artifacts:
    - path: "MyHomeApp/Features/Analytics/AnalyticsView.swift"
      provides: "ANL-01/02 root screen: @Query all expenses, range picker, summarize in body, headline"
      contains: "struct AnalyticsView"
      min_lines: 70
    - path: "MyHomeApp/Features/Analytics/AnalyticsTrendChart.swift"
      provides: "ANL-03 AreaMark + LineMark trend chart over [SpendBucket]"
      contains: "AreaMark"
    - path: "MyHomeApp/Features/Analytics/AnalyticsCategoryBars.swift"
      provides: "ANL-04 by-category breakdown over [CategorySpendItem] sorted descending"
      contains: "CategorySpendItem"
  key_links:
    - from: "MyHomeApp/Features/Overview/OverviewView.swift"
      to: "AnalyticsView"
      via: ".navigationDestination(isPresented: $navigateToAnalytics) push (ANL-01)"
      pattern: "navigateToAnalytics"
    - from: "MyHomeApp/Features/Analytics/AnalyticsView.swift"
      to: "AnalyticsAggregator.summarize"
      via: "computed at top of body, re-runs on selectedRange change (ANL-02)"
      pattern: "AnalyticsAggregator\\.summarize"
    - from: "MyHomeApp/Features/Analytics/AnalyticsTrendChart.swift"
      to: "SpendBucket"
      via: "pre-aggregated buckets passed in, never raw expenses (Pitfall A)"
      pattern: "AreaMark"
---

<objective>
Build the visible Analytics screen and its push entry point from Overview, against the
`SpendSummary` contract delivered by 15-01. AnalyticsView owns the Week/Month/Year range, computes
`SpendSummary` once at the top of `body`, and renders a headline + AreaMark trend chart (ANL-03) +
by-category breakdown (ANL-04). Navigation mirrors the existing `navigateToAssets` push (ANL-01).

Purpose: this plan turns the tested data layer into the screen the user actually sees, in the v1.2
neumorphic design language (DesignTokens, .neuSurface, CategoryStyle.color, solid .semibold/.bold
.default numerals, rounded word amounts). Delta chips + drill-down are deferred to 15-03 so this plan
stays at 3 tasks within budget.

Output: three new `Features/Analytics/` files (pbxproj-registered under a new G_ANL group inside
G120 Features), plus an Analytics entry point wired into OverviewMonthContent.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-analytics-screen/15-RESEARCH.md
@.planning/phases/15-analytics-screen/15-CONTEXT.md
@.planning/phases/15-analytics-screen/15-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: AnalyticsView + AnalyticsTrendChart, registered in new G_ANL group (ANL-01/02/03)</name>
  <files>MyHomeApp/Features/Analytics/AnalyticsView.swift, MyHomeApp/Features/Analytics/AnalyticsTrendChart.swift, MyHome.xcodeproj/project.pbxproj</files>
  <read_first>
    - 15-RESEARCH.md lines 446-481 (no-stale-data rule: selectedRange @State in AnalyticsView, summarize at top of body, @Query spans 2 years; Picker(.segmented) pattern reusing SpendRange.label)
    - 15-RESEARCH.md lines 247-335 (Pitfall A pre-aggregate outside Chart DSL; AreaMark + LineMark + catmullRom gradient snippet; year-range filter; Decimal-only axis labels)
    - MyHomeApp/Features/Overview/SpendOverTimeChart.swift (analog: Picker(.segmented) over SpendRange, Pitfall A guard aggregating before Chart {}, axis label formatting via Decimal.formattedINRCompact, @State selectedRange)
    - MyHomeApp/Support/AnalyticsAggregator.swift (from 15-01: summarize signature + SpendSummary fields trendBuckets/totalSpend/categoryBreakdown/delta)
    - MyHomeApp/Support/SpendOverTimeAggregator.swift lines 6-39 (SpendRange .week/.month/.year + .label; SpendBucket date/spent/spentDecimal/dateLabel)
    - MyHomeApp/DesignSystem/DesignTokens.swift lines 20-50 (accent, positive, negative, bgCanvas, label tiers, cat*) and MyHomeApp/DesignSystem/NeuSurface.swift (.neuSurface(.raised/.floating/.recessed))
    - MyHomeApp/Support/Decimal+INR.swift (formattedINRWhole/formattedINRWords/formattedINRCompact)
    - MyHome.xcodeproj/project.pbxproj lines 485-499 (G120 Features children — add G_ANL here) + PBXFileReference / PBXBuildFile sections + app Sources phase at line 933
  </read_first>
  <action>
    Create the `Features/Analytics/` group and two files.
    `AnalyticsView.swift` (`import SwiftUI / SwiftData / Charts`): `struct AnalyticsView: View` with an init taking
    `expenses: [Expense]` and `categories: [Category]` (passed from Overview — do NOT pass a pre-computed
    SpendSummary; the screen controls its own range per 15-RESEARCH line 378). Hold `@State private var
    selectedRange: SpendRange = .month`. At the TOP of `body`, compute
    `let summary = AnalyticsAggregator.summarize(expenses: expenses, categories: categories, range: selectedRange)`
    (NOT inside a nested LazyVStack/Group — 15-RESEARCH line 459, no-stale-data rule). Layout in a ScrollView on
    `DesignTokens.bgCanvas` with `.scrollContentBackground(.hidden)`:
    (1) a segmented `Picker("Range", selection: $selectedRange)` with `.pickerStyle(.segmented)` tagging
    `SpendRange.week/.month/.year` via their `.label` (reuse the existing enum — no new range type);
    (2) a headline card `.neuSurface(.floating)` showing `summary.totalSpend.formattedINRWords()` as the hero number
    in solid `.font(.system(size: 34, weight: .bold))` `.default` design + `.monospacedDigit()` (NOT thin ultraLight-rounded
    per the v1.2 hero-font rule) with a "SPENT" / range caption in `DesignTokens.label2`;
    (3) `AnalyticsTrendChart(buckets: summary.trendBuckets)` inside `.neuSurface(.raised)`;
    (4) a placeholder VStack where the category bars (Task 2) and delta chip (15-03) will mount — leave a clearly
    commented insertion point. Set `.navigationTitle("Analytics")`.
    `AnalyticsTrendChart.swift` (`import SwiftUI / Charts`): `struct AnalyticsTrendChart: View` taking
    `let buckets: [SpendBucket]`. The buckets are ALREADY pre-aggregated (Pitfall A) — render
    `Chart(buckets) { point in AreaMark(x: .value("Date", point.date), y: .value("Spend", point.spent))` with a
    vertical `LinearGradient` from `DesignTokens.accent.opacity(0.35)` to `.opacity(0.0)`, plus a `LineMark` over the
    same x/y in `DesignTokens.accent`, `.lineStyle(StrokeStyle(lineWidth: 2))`, `.interpolationMethod(.catmullRom)` }.
    Axis labels format via `Decimal(d).formattedINRCompact()` for the y value (Decimal lossy-from-Double acceptable for
    AXIS LABELS ONLY — Pitfall B). Empty/zero-only buckets render a calm empty state, not a crash. Give the chart a
    fixed `.frame(height: 200)`.
    Register BOTH files with the 4 pbxproj edits each, AND create a new `G_ANL /* Analytics */` PBXGroup (path = Analytics,
    sourceTree "<group>") containing `FANL02 /* AnalyticsView.swift */` and `FANL03 /* AnalyticsTrendChart.swift */`,
    and add `G_ANL` to the G120 Features group children (lines ~485-499). Add `AANL02`/`AANL03` build files to the APP
    target Sources phase (line 933). The project has NO synchronized groups — the G_ANL group `path = Analytics` MUST
    match the actual `Features/Analytics/` directory or the compiler will not find the files (Pitfall 5).
  </action>
  <verify>
    <automated>cd "/Users/reo/My Projects/my-home" && xcodebuild build -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `xcodebuild build -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17'` succeeds with zero "cannot find type 'AnalyticsView'/'AnalyticsTrendChart'" errors.
    - `grep -c 'G_ANL' MyHome.xcodeproj/project.pbxproj` returns >= 2 (group block + child entry under G120).
    - `grep -c 'AreaMark' MyHomeApp/Features/Analytics/AnalyticsTrendChart.swift` returns >= 1 (ANL-03).
    - `grep -c 'AnalyticsAggregator.summarize' MyHomeApp/Features/Analytics/AnalyticsView.swift` returns >= 1.
    - `grep -c '.pickerStyle(.segmented)' MyHomeApp/Features/Analytics/AnalyticsView.swift` returns 1 (ANL-02 range control).
    - `grep -c 'weight: .light, design: .rounded' MyHomeApp/Features/Analytics/AnalyticsView.swift` returns 0 (hero uses solid .bold .default, not thin rounded).
  </acceptance_criteria>
  <done>AnalyticsView + AnalyticsTrendChart build clean, registered under G_ANL; range picker drives summarize at top of body; AreaMark trend renders pre-aggregated buckets.</done>
</task>

<task type="auto">
  <name>Task 2: AnalyticsCategoryBars + mount into AnalyticsView (ANL-04)</name>
  <files>MyHomeApp/Features/Analytics/AnalyticsCategoryBars.swift, MyHomeApp/Features/Analytics/AnalyticsView.swift, MyHome.xcodeproj/project.pbxproj</files>
  <read_first>
    - MyHomeApp/Features/Overview/SpendByCategoryChart.swift (track-backed horizontal bar row pattern + CategorySpendItem usage — copy-and-adapt target; the established "bar breakdown" the design language matches)
    - 15-RESEARCH.md lines 309-324 (track-backed rows vs native BarMark — either satisfies ANL-04; track-backed preferred for design fit; native BarMark snippet provided if chosen)
    - MyHomeApp/Features/Shared/CategoryStyle.swift line 49 (color(for:)) and DesignTokens cat* palette
    - MyHomeApp/Support/AnalyticsAggregator.swift (SpendSummary.categoryBreakdown already sorted descending by spend, each item carries .color + .spentDecimal)
    - MyHomeApp/Features/Analytics/AnalyticsView.swift (self — the commented insertion point from Task 1)
  </read_first>
  <action>
    Create `AnalyticsCategoryBars.swift` (`import SwiftUI`): `struct AnalyticsCategoryBars: View` taking
    `let items: [CategorySpendItem]` (already sorted descending by spend from the aggregator). Render a VStack of
    track-backed rows mirroring `SpendByCategoryChart`: each row shows the category name (`DesignTokens.label`), the
    amount via `item.spentDecimal.formattedINRWhole()` (`DesignTokens.label2`, `.monospacedDigit()`), and a horizontal
    track (`DesignTokens.fillRecessed2`-style recessed bar) with a fill capsule whose width is proportional to
    `item.spent / maxSpent` and whose color is `item.color` (the per-category neumorphic palette). Guard `maxSpent > 0`.
    Show "all categories for the range" — do not truncate to top-N (ANL-04 success criterion 4). Empty state when
    `items` is empty: a calm "No spend this range" row. If native `BarMark` is chosen instead, use a horizontal
    `Chart(items) { BarMark(x: .value("Spend", $0.spent), y: .value("Category", $0.name)) .foregroundStyle($0.color.gradient) .cornerRadius(4) }`
    fed by the pre-aggregated items (Pitfall A) — either approach is acceptable.
    Mount it in `AnalyticsView` at the Task-1 insertion point: `AnalyticsCategoryBars(items: summary.categoryBreakdown)`
    inside a `.neuSurface(.raised)` section with a "By Category" section label.
    Register the new file: 4 pbxproj edits — `FANL04 /* AnalyticsCategoryBars.swift */` added to the G_ANL group children,
    `AANL04` build file added to the app Sources phase, plus PBXFileReference + PBXBuildFile entries.
  </action>
  <verify>
    <automated>cd "/Users/reo/My Projects/my-home" && xcodebuild build -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `xcodebuild build -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17'` succeeds (AnalyticsCategoryBars registered + compiles).
    - `grep -c 'CategorySpendItem' MyHomeApp/Features/Analytics/AnalyticsCategoryBars.swift` returns >= 1 (reuses existing type — no redefinition).
    - `grep -c 'struct CategorySpendItem' MyHomeApp/Features/Analytics/AnalyticsCategoryBars.swift` returns 0 (does NOT redefine the type).
    - `grep -c 'AnalyticsCategoryBars(items: summary.categoryBreakdown)' MyHomeApp/Features/Analytics/AnalyticsView.swift` returns 1 (mounted).
    - `grep -c 'FANL04' MyHome.xcodeproj/project.pbxproj` returns >= 2.
  </acceptance_criteria>
  <done>By-category breakdown renders all categories sorted descending with the neumorphic palette, mounted in AnalyticsView; builds clean.</done>
</task>

<task type="auto">
  <name>Task 3: Wire push navigation from Overview to Analytics (ANL-01)</name>
  <files>MyHomeApp/Features/Overview/OverviewView.swift</files>
  <read_first>
    - MyHomeApp/Features/Overview/OverviewView.swift lines 82-110 (OverviewMonthContent: @Query categories, @Query allGlobalExpenses, @State navigateToAssets), line 204 (sectionHeader CTA pattern: `sectionHeader("Net Worth", action: ("See holdings", { navigateToAssets = true }))`), lines 235-237 (SpendOverTimeChart "Over Time" section), line 281 (`.navigationDestination(isPresented: $navigateToAssets) { AssetsListView() }`)
    - 15-RESEARCH.md lines 339-378 (navigation entry point: add @State navigateToAnalytics, .navigationDestination passing allGlobalExpenses + categories, tap target options — section header CTA or dedicated card; .navigationDestination MUST live in OverviewMonthContent, the NavigationStack content, not OverviewView)
  </read_first>
  <action>
    In `OverviewMonthContent` add `@State private var navigateToAnalytics = false` alongside `navigateToAssets`.
    Add a tap target on Overview to open Analytics — the minimal, low-risk option: extend the existing "Over Time"
    section header (around line 235) with a `("See analytics", { navigateToAnalytics = true })` action using the same
    `sectionHeader(_:action:)` helper already used for "Net Worth". (A richer dedicated card is acceptable but optional;
    do not remove or restyle the existing SpendOverTimeChart — it stays on Overview per Phase 14 D-05.)
    Add `.navigationDestination(isPresented: $navigateToAnalytics) { AnalyticsView(expenses: allGlobalExpenses,
    categories: categories) }` alongside the existing `navigateToAssets` destination (line ~281). Pass `allGlobalExpenses`
    (the unfiltered 2-year-wide query) so the aggregator can compute prior-period deltas (Pitfall 2). Do NOT add a new
    tab or alter the tab-bar layout (ANL-01). The NavigationStack owned by OverviewView provides the slide-in push automatically.
  </action>
  <verify>
    <automated>cd "/Users/reo/My Projects/my-home" && xcodebuild build -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `xcodebuild build -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17'` succeeds.
    - `grep -c 'navigateToAnalytics' MyHomeApp/Features/Overview/OverviewView.swift` returns >= 3 (@State + destination binding + tap setter).
    - `grep -c 'AnalyticsView(expenses: allGlobalExpenses, categories: categories)' MyHomeApp/Features/Overview/OverviewView.swift` returns 1 (correct 2-year-wide query passed — Pitfall 2).
    - `grep -c 'navigationDestination(isPresented: $navigateToAnalytics)' MyHomeApp/Features/Overview/OverviewView.swift` returns 1 (push, not tab — ANL-01).
  </acceptance_criteria>
  <done>A tap on Overview pushes AnalyticsView via .navigationDestination with the wide expense query; tab bar unchanged; builds clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Analytics navigation reuses the in-process `.navigationDestination` push mechanism (same as `navigateToAssets`); no new URL scheme, IPC, or external input. AnalyticsView reads already-persisted SwiftData via @Query/passed arrays. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-04 | Information Disclosure | Analytics headline + category bars | mitigate | All figures derive from SpendSummary (self-transfer-excluded in 15-01); no raw transfer rows surface. |
| T-15-05 | Denial of Service | Raw @Query array entering Chart DSL | mitigate | Pitfall A: charts receive only pre-aggregated [SpendBucket]/[CategorySpendItem]; AnalyticsView never passes raw expenses into Chart {}, avoiding O(n) Chart re-layout on large datasets. |
| T-15-06 | Tampering | pbxproj G_ANL group registration | mitigate | Build gate fails if G_ANL group path or the 4 per-file edits are missing — registration verified, not assumed (Pitfall 5). |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | No package installs; first-party Swift/SwiftUI/Charts/SwiftData only. |
</threat_model>

<verification>
- AnalyticsView, AnalyticsTrendChart, AnalyticsCategoryBars build clean under G_ANL (pbxproj registration proven).
- Range picker drives AnalyticsAggregator.summarize at the top of body (no stale data — ANL-02).
- AreaMark trend renders pre-aggregated buckets (ANL-03); category bars show all categories sorted descending (ANL-04).
- Overview pushes AnalyticsView via .navigationDestination; tab bar unchanged (ANL-01).
</verification>

<success_criteria>
- Single tap on Overview opens Analytics via push, not a tab (success criterion 1 / ANL-01).
- Week/Month/Year switch updates headline + trend chart + category bars with no stale data (success criterion 2 / ANL-02).
- Trend chart buckets IST-correctly; Year shows only months up to current (success criterion 3 / ANL-03).
- By-category bars show all categories sorted descending with neumorphic palette (success criterion 4 / ANL-04).
</success_criteria>

<output>
Create `.planning/phases/15-analytics-screen/15-02-SUMMARY.md` when done
</output>
