---
phase: 16-ai-insight-card
plan: 05
type: execute
wave: 4
depends_on: ["16-04"]
files_modified: []
autonomous: false
requirements: [AI-01, AI-05]

must_haves:
  truths:
    - "On a physical A17 Pro+ device with Apple Intelligence on, a coherent on-device insight appears at the bottom of Analytics with no network call (AI-01)"
    - "Insight reveals character-by-character; Reduce Motion shows full text instantly with no orb (SC-3)"
    - "Card is silently absent (no shell/gap/spinner) on an unavailable device (D-01/SC-2)"
  artifacts: []
  key_links: []
---

<objective>
End-of-phase human verification of the behaviors that the simulator cannot prove.
The iPhone 17 simulator runs the unit suite (SC-5) but cannot execute Apple
Intelligence generation, so the live insight (AI-01), the typewriter/Reduce-Motion
reveal (SC-3), and the silent-absence behavior on an ineligible device (SC-2) must
be confirmed by a human on real hardware.

Purpose: SC-1/SC-2/SC-3 are visual/temporal/device-gated and not automatable.

Output: Confirmed on-device behavior or a logged gap list for `/gsd-plan-phase --gaps`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/16-ai-insight-card/16-VALIDATION.md
@.planning/phases/16-ai-insight-card/16-04-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: On-device verification of live insight, typewriter, and silent absence</name>
  <files>none (verification-only)</files>
  <read_first>
    - .planning/phases/16-ai-insight-card/16-VALIDATION.md (Manual-Only Verifications table)
    - .planning/phases/16-ai-insight-card/16-04-SUMMARY.md (what shipped)
    - .planning/ROADMAP.md §"Phase 16" Success Criteria 1, 2, 3
  </read_first>
  <what-built>
    The `AIInsightCard` on the Analytics screen: on-device FoundationModels
    generation, streaming typewriter reveal with breathing orb, Reduce-Motion instant
    text, numeric-integrity verifier + fallback, and the silent-absence path on
    unavailable devices. The full unit suite (4 availability branches + 2 error cases
    + verifier) and clean build are already GREEN on the iPhone 17 simulator (Plan 04).
  </what-built>
  <action>
    Human-verify on real hardware (simulator cannot run Apple Intelligence). Execute
    the numbered steps in <how-to-verify>, then report sign-off or failing step
    numbers. No code is written in this task.
  </action>
  <how-to-verify>
    On a physical A17 Pro+ device with Apple Intelligence enabled (Settings → Apple
    Intelligence & Siri):
    1. Build/run MyHome on the device. Open Overview → tap into Analytics.
    2. Confirm an AI Insight card appears BELOW the category bars and produces a
       coherent natural-language observation about the current range's spending
       (SC-1). It should sit on a raised neumorphic surface with a violet edge-glow +
       sparkles label.
    3. Watch the text reveal character-by-character; confirm a breathing orb shows
       while generating (SC-3).
    4. Switch Week → Month → Year rapidly; confirm the insight regenerates per range
       and never shows a stale/other-range figure (D-07/D-08).
    5. Cross-check every rupee/%/delta in the text against the headline + category
       figures on the same screen — they must match exactly (AI-04). Note any mismatch.
    6. Confirm no network activity (airplane mode still generates) — AI-01 on-device guarantee.
    7. Enable Settings → Accessibility → Motion → Reduce Motion; reopen Analytics;
       confirm the full text appears instantly with NO orb (SC-3).
    8. On an ineligible device or simulator (or with Apple Intelligence disabled),
       open Analytics; confirm the screen ends cleanly after the category bars with
       NO shell card, blank gap, error text, or stuck spinner (D-01/SC-2).
  </how-to-verify>
  <verify>
    <human-check>All 8 steps pass on a physical A17 Pro+ device (and the unavailable-path step on an ineligible device/sim).</human-check>
  </verify>
  <acceptance_criteria>
    - Live coherent on-device insight visible (SC-1); confirmed no network needed (AI-01)
    - Typewriter reveal + orb while generating; Reduce Motion → instant full text, no orb (SC-3)
    - Every figure in the insight matches an on-screen pre-computed value (AI-04)
    - Card silently absent on the unavailable path — no shell/gap/spinner (SC-2)
    - Any deviation logged with the failing step number for `/gsd-plan-phase --gaps`
  </acceptance_criteria>
  <done>Human sign-off recorded, or a gap list with failing step numbers captured.</done>
  <resume-signal>Type "approved" if all steps pass, or list the failing step numbers + what you saw.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| on-device model → real user | First time the feature is exercised on genuine Apple Intelligence hardware rather than mocks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Information Disclosure | live generation | mitigate | Human confirms generation works offline (airplane-mode step) — proves the on-device / no-network guarantee in reality, not just by code review. |
| T-16-02 | Tampering | live insight figures | mitigate | Human cross-checks every figure against on-screen pre-computed values — real-world confirmation of the InsightVerifier (AI-04). |
| T-16-SC | Tampering | npm/pip/cargo installs | accept | No external package installs in this phase. |
</threat_model>

<verification>
- Human sign-off on all 8 steps, OR a logged gap list with failing step numbers.
</verification>

<success_criteria>
- SC-1, SC-2, SC-3, and AI-04 confirmed on real hardware (the simulator-impossible behaviors).
</success_criteria>

<output>
Create `.planning/phases/16-ai-insight-card/16-05-SUMMARY.md` (record sign-off or gaps) when done.
</output>
