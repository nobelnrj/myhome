---
phase: 16-ai-insight-card
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - MyHomeApp/DesignSystem/DesignTokens.swift
  - MyHomeApp/Support/InsightService.swift
  - MyHomeApp/Support/InsightVerifier.swift
  - MyHomeApp/Features/Analytics/AIInsightCard.swift
  - MyHomeTests/InsightServiceTests.swift
  - MyHomeTests/InsightVerifierTests.swift
  - MyHome.xcodeproj/project.pbxproj
autonomous: true
requirements: [AI-02, AI-03, AI-04, AI-05]

must_haves:
  truths:
    - "All 5 new files compile against IPHONEOS_DEPLOYMENT_TARGET=17.0 with FoundationModels behind @available(iOS 26, *)"
    - "SpendInsight @Generable struct supports memberwise init SpendInsight(observation:suggestion:) (Open Question 1 resolved)"
    - "InsightServiceTests and InsightVerifierTests compile and execute (assertions may be RED pending Plans 02/03)"
    - "Violet AI accent tokens exist in DesignTokens, scoped for AI use only; canary #FFD60A unchanged"
  artifacts:
    - path: "MyHomeApp/Support/InsightService.swift"
      provides: "InsightGenerating protocol, SpendInsight @Generable, InsightPromptBuilder skeleton, isInsightAvailable helper signature"
      contains: "protocol InsightGenerating"
    - path: "MyHomeApp/Support/InsightVerifier.swift"
      provides: "InsightVerifier + InsightFallbackBuilder stub signatures"
      contains: "enum InsightVerifier"
    - path: "MyHomeApp/Features/Analytics/AIInsightCard.swift"
      provides: "AIInsightCard view stub gated @available(iOS 26, *)"
      contains: "struct AIInsightCard"
    - path: "MyHomeApp/DesignSystem/DesignTokens.swift"
      provides: "aiViolet* tokens"
      contains: "aiViolet"
    - path: "MyHomeTests/InsightServiceTests.swift"
      provides: "Availability + error test scaffolds + MockInsightService"
      contains: "MockInsightService"
    - path: "MyHomeTests/InsightVerifierTests.swift"
      provides: "Number-integrity + fallback test scaffolds"
      contains: "InsightVerifierTests"
  key_links:
    - from: "MyHome.xcodeproj/project.pbxproj"
      to: "InsightService.swift, InsightVerifier.swift, AIInsightCard.swift, InsightServiceTests.swift, InsightVerifierTests.swift"
      via: "PBXBuildFile + PBXFileReference + PBXGroup + PBXSourcesBuildPhase (4 edits each)"
      pattern: "InsightService.swift in Sources"
---

<objective>
Lay the testable foundation for the AI Insight card: define the contracts (the
`InsightGenerating` protocol seam, the `@Generable SpendInsight` struct, the
`isInsightAvailable` availability helper signature, the `InsightVerifier` /
`InsightFallbackBuilder` / `InsightPromptBuilder` API surfaces as stubs), add the
AI-only violet accent tokens to `DesignTokens` (D-04), register all five new files
in `project.pbxproj`, and write the RED test scaffolds that Plans 02 and 03 turn
GREEN.

This plan resolves RESEARCH Open Question 1 (does `@Generable` preserve a
memberwise init on `SpendInsight`?) via a build-check before any code depends on
that init for the fallback path.

Purpose: Establish all interface contracts up front so Plans 02 (verifier), 03
(service) and 04 (view) implement against fixed signatures with zero scavenger
hunts, and so the pbxproj 4-edit footgun is paid once in a single foundation pass.

Output: 5 new files registered + compiling; 2 test files compiling and running
(RED expected and acceptable for this wave); violet tokens added.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-ai-insight-card/16-CONTEXT.md
@.planning/phases/16-ai-insight-card/16-RESEARCH.md
@.planning/phases/16-ai-insight-card/16-VALIDATION.md
@MyHomeApp/Support/AnalyticsAggregator.swift
@MyHomeApp/DesignSystem/DesignTokens.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add AI-only violet accent tokens to DesignTokens</name>
  <read_first>
    - MyHomeApp/DesignSystem/DesignTokens.swift (the `Color(hex:)` init, the existing
      accent block lines ~21-23, and the `neonGlow(_:radius:intensity:)` modifier at
      line ~120 — the card in Plan 04 reuses this exact modifier)
    - .planning/phases/16-ai-insight-card/16-RESEARCH.md Pattern 7 (violet hex values)
  </read_first>
  <action>
    Add three AI-scoped color tokens to DesignTokens (D-04): `aiVioletTop` =
    `#C4A6FF`, `aiVioletBottom` = `#7C5CFF`, `aiVioletGlow` = `#8B5CF6`. Place them
    in their own clearly commented "AI Insight accent (Phase 16 — localized; not a
    general accent)" section so it is unmistakable these are NOT a new app accent.
    Do NOT touch `accent`/`accentSoft`/`accentOnYellow` (canary `#FFD60A` stays the
    primary accent everywhere else per D-04). Use the existing `Color(hex:)` init.
  </action>
  <verify>
    <automated>cd "/Users/reo/My Projects/my-home" && grep -c 'aiViolet' MyHomeApp/DesignSystem/DesignTokens.swift</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'aiVioletTop' MyHomeApp/DesignSystem/DesignTokens.swift` returns `1`
    - `grep -c 'aiVioletBottom' MyHomeApp/DesignSystem/DesignTokens.swift` returns `1`
    - `grep -c 'aiVioletGlow' MyHomeApp/DesignSystem/DesignTokens.swift` returns `1`
    - the `accent = Color(hex: "#FFD60A")` line is byte-identical to before (canary untouched)
  </acceptance_criteria>
  <done>Three violet AI tokens defined; canary accent untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Define contracts + register 5 files in pbxproj + @Generable memberwise-init build-check</name>
  <read_first>
    - MyHomeApp/Support/AnalyticsAggregator.swift (SpendSummary fields: totalSpend,
      priorTotalSpend, delta, deltaFraction, trendBuckets, categoryBreakdown:[CategorySpendItem],
      priorCategorySpend:[PersistentIdentifier:Decimal], range:SpendRange)
    - MyHomeApp/Features/Overview/SpendByCategoryChart.swift (CategorySpendItem:
      `id: PersistentIdentifier`, `name: String`, `spentDecimal: Decimal`)
    - MyHomeApp/Support/SpendOverTimeAggregator.swift (SpendRange: .week/.month/.year)
    - MyHome.xcodeproj/project.pbxproj — study the G_ANL group (~line 516), the G140
      Support group (~line 704), the G200 MyHomeTests group (~line 753), and the
      app + test sources build phases (~lines 1089 / 1171), using FANL01/AANL01
      (AnalyticsAggregator) and FANL07/AANL07 (AnalyticsAggregatorTests) as the exact
      4-edit template
    - .planning/phases/16-ai-insight-card/16-RESEARCH.md Patterns 1/2/3/5/6, Pitfall 7
      (pbxproj 4-edit pattern), Open Question 1
  </read_first>
  <action>
    Create the following as contracts/stubs (no real logic yet; Plans 02-04 fill them).
    Every FoundationModels-touching declaration carries `@available(iOS 26, *)`.

    `MyHomeApp/Support/InsightService.swift`:
    - `@Generable SpendInsight` struct with `var observation: String` (@Guide: terse
      one-or-two-sentence observation, use ONLY given figures) and `var suggestion: String?`
      (@Guide: optional soft data-grounded nudge, never budget-policing) per D-05.
      CRITICAL build-check (Open Question 1): add a compile-only reference that
      constructs `SpendInsight(observation: "x", suggestion: nil)` to PROVE the
      memberwise init survives the `@Generable` macro. If it does NOT compile,
      introduce a separate plain `FallbackInsight { let observation: String; let suggestion: String? }`
      and record the divergence in the SUMMARY (Plan 02 + 04 depend on this answer).
    - `protocol InsightGenerating: Sendable { func generate(for summary: SpendSummary) async throws -> SpendInsight }`
    - `enum InsightPromptBuilder` with `static let systemInstructions: String` and
      `static func buildPrompt(for summary: SpendSummary) -> String` returning `""` (stub).
    - file-scope `func isInsightAvailable(_ availability: SystemLanguageModel.Availability) -> Bool`
      returning `false` (stub) — the unit-testable seam for the 4 availability branches (AI-02).

    `MyHomeApp/Support/InsightVerifier.swift`:
    - `enum InsightVerifier` with `struct Result { let observation: String; let suggestion: String?; let passed: Bool }`
      and `static func verify(_ insight: SpendInsight, against summary: SpendSummary) -> Result`
      returning `Result(observation: insight.observation, suggestion: insight.suggestion, passed: true)` (stub).
    - `enum InsightFallbackBuilder` with `static func build(for summary: SpendSummary) -> SpendInsight` (stub placeholder).

    `MyHomeApp/Features/Analytics/AIInsightCard.swift`:
    - `struct AIInsightCard: View { let summary: SpendSummary; var body: some View { EmptyView() } }` (stub; Plan 04 fills it).

    Then register all THREE app files (InsightService.swift + InsightVerifier.swift
    into the G140 Support group; AIInsightCard.swift into the G_ANL Analytics group)
    AND the two test files (created in Task 3 — register them now) using the 4-edit
    pbxproj pattern each: PBXBuildFile, PBXFileReference, PBXGroup children,
    PBXSourcesBuildPhase. Use unique stable IDs in the existing style. Test files go
    into the G200 MyHomeTests group + the test target sources build phase.
  </action>
  <verify>
    <automated>cd "/Users/reo/My Projects/my-home" && xcodebuild build -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' -quiet 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `xcodebuild build -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17'` SUCCEEDS (proves memberwise init + all gating compile under deployment target 17.0)
    - pbxproj contains all five: `InsightService.swift in Sources`, `InsightVerifier.swift in Sources`, `AIInsightCard.swift in Sources`, `InsightServiceTests.swift in Sources`, `InsightVerifierTests.swift in Sources`
    - `grep -c 'protocol InsightGenerating' MyHomeApp/Support/InsightService.swift` returns `1`
    - `grep -c '@Generable' MyHomeApp/Support/InsightService.swift` >= `1`
    - SUMMARY records the Open-Question-1 result (memberwise init preserved: yes/no)
  </acceptance_criteria>
  <done>5 files exist, compile, and are registered; SpendInsight memberwise init confirmed (or FallbackInsight adopted + recorded).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write RED test scaffolds (MockInsightService + all 8 behavior cases)</name>
  <read_first>
    - MyHomeTests/AnalyticsAggregatorTests.swift (Swift Testing style: `import Testing`,
      `@Test`, `#expect`; how SpendSummary fixtures are built)
    - MyHomeApp/Support/AnalyticsAggregator.swift (SpendSummary construction for fixtures)
    - .planning/phases/16-ai-insight-card/16-VALIDATION.md Per-Task Verification Map
      (the 8 cases) + Pitfall 9 (gate FoundationModels test methods with `@available(iOS 26, *)`
      per `@Test`, not at struct level)
    - .planning/phases/16-ai-insight-card/16-RESEARCH.md "Mock Session for Tests" + Open Question 3
  </read_first>
  <behavior>
    - AI-02: isInsightAvailable(.available) == true
    - AI-02: isInsightAvailable(.unavailable(.deviceNotEligible)) == false
    - AI-02: isInsightAvailable(.unavailable(.appleIntelligenceNotEnabled)) == false
    - AI-02: isInsightAvailable(.unavailable(.modelNotReady)) == false
    - AI-03: MockInsightService failing with a guardrail-style error routes to fallback (no throw escapes)
    - AI-03: MockInsightService failing with an exceededContextWindowSize-style error routes to fallback
    - AI-04: InsightVerifier rejects a model-invented number (not in facts) → passed == false, returns fallback text
    - AI-04: InsightVerifier passes when every number is a fact value → passed == true, observation unchanged
  </behavior>
  <action>
    Create `MyHomeTests/InsightServiceTests.swift` and `MyHomeTests/InsightVerifierTests.swift`
    (Swift Testing). Add `MockInsightService: InsightGenerating` (configurable
    `Result<SpendInsight, Error>`, `callCount`, `try await Task.yield()` so
    cancellation propagates) in InsightServiceTests. Write `@Test` functions for all
    8 behaviors above, each gated `@available(iOS 26, *)`. Build a shared `SpendSummary`
    fixture helper. For the error cases (Open Question 3): if
    `LanguageModelSession.GenerationError.Context` is NOT publicly constructible, have
    the mock throw a local error type and the production catch match
    `as? LanguageModelSession.GenerationError` — write the test against the fallback
    OUTCOME, not the concrete error case. These tests are EXPECTED RED now (stubs
    return placeholders); Plans 02 and 03 turn them GREEN.
  </action>
  <verify>
    <automated>cd "/Users/reo/My Projects/my-home" && xcodebuild test -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:MyHomeTests/InsightServiceTests -only-testing:MyHomeTests/InsightVerifierTests -quiet 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - both test files COMPILE and the test bundle RUNS (exit may be non-zero — RED assertions are acceptable in Wave 1)
    - `grep -c 'MockInsightService' MyHomeTests/InsightServiceTests.swift` >= `1`
    - InsightServiceTests contains exactly the 4 availability `@Test`s + 2 error `@Test`s
    - InsightVerifierTests contains the reject + pass `@Test`s
    - SUMMARY lists which of the 8 are RED (expected) vs accidentally GREEN
  </acceptance_criteria>
  <done>8 behavior tests exist, compile, and run; RED status documented for Plans 02/03 handoff.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user finance data → on-device model prompt | `SpendSummary` Decimal facts + category names are serialised into an on-device `LanguageModelSession` prompt; nothing crosses a network boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Information Disclosure | InsightService.swift (prompt construction surface) | mitigate | Contract is on-device `LanguageModelSession` only; no `URLSession`/network entitlement introduced. Verified end-to-end in Plan 03/04; this plan only defines stubs. |
| T-16-02 | Tampering | SpendInsight / InsightVerifier contract | mitigate | `InsightVerifier.verify` is the integrity gate defined here; rejects model-invented numbers (AI-04). Stub returns passed=true; Plan 02 implements real rejection. |
| T-16-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this phase — zero new external dependencies (all first-party Apple frameworks already in toolchain). RESEARCH "Package Legitimacy Audit" = N/A. |
</threat_model>

<verification>
- `xcodebuild build -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17'` succeeds.
- `grep IPHONEOS_DEPLOYMENT_TARGET MyHome.xcodeproj/project.pbxproj` returns `17.0` (unchanged).
- All 5 new files appear in pbxproj sources build phases.
- Test bundle compiles and runs (RED allowed this wave).
</verification>

<success_criteria>
- Contracts fixed for Plans 02/03/04; violet tokens added; pbxproj footgun paid once.
- Open Question 1 (memberwise init) answered and recorded.
- Test scaffolds RED and ready to be turned GREEN.
</success_criteria>

<output>
Create `.planning/phases/16-ai-insight-card/16-01-SUMMARY.md` when done.
</output>
