---
phase: 11.1-sip-automation-and-nps-nav-auto-refresh
plan: 03
type: execute
wave: 2
depends_on:
  - 11.1-01
files_modified:
  - MyHomeApp/Support/NPSNavService.swift
  - MyHomeApp/Features/Assets/NPSSchemePickerView.swift
  - MyHomeTests/NPSNavServiceTests.swift
autonomous: true
requirements:
  - D-01
  - ASSET-03
  - ASSET-04
  - ASSET-09
must_haves:
  truths:
    - "NPS NAV refreshes best-effort once per IST day on demand, rendering the cached value first and never blocking"
    - "A failed or malformed npsnav.in response silently leaves the last cached NAV intact (no crash)"
    - "Each linked NPS holding's Asset.currentNAV + navAsOfDate update from the bulk fetch"
    - "The NPS scheme picker lets the user search and select a scheme code per strategy holding"
  artifacts:
    - path: "MyHomeApp/Support/NPSNavService.swift"
      provides: "@MainActor @Observable NPS NAV service cloned from AMFINavService: IST daily gate, silent-fail, npsnav.in latest-min parser, scheme-code -> currentNAV update loop"
      contains: "final class NPSNavService"
      exports: ["refreshIfNeeded", "forceRefresh"]
    - path: "MyHomeApp/Features/Assets/NPSSchemePickerView.swift"
      provides: "Searchable NPS scheme picker mirroring AMFISchemePickerView; binds selectedSchemeCode"
      contains: "struct NPSSchemePickerView"
    - path: "MyHomeTests/NPSNavServiceTests.swift"
      provides: "Unit coverage for latest-min parser (Number nav -> Decimal) and IST shouldFetch gate"
      contains: "NPSNavServiceTests"
  key_links:
    - from: "MyHomeApp/Support/NPSNavService.swift"
      to: "Asset.npsSchemeCode"
      via: "fetch assets, match npsSchemeCode, set currentNAV + navAsOfDate, save"
      pattern: "npsSchemeCode"
    - from: "MyHomeApp/Features/Assets/NPSSchemePickerView.swift"
      to: "NPSNavService.schemeList"
      via: "filtered list bound to picker"
      pattern: "npsNavService"
---

<objective>
Add NPS NAV auto-refresh — the capability Phase 11 deferred (ASSET-04). Clone AMFINavService into NPSNavService (swap URL to npsnav.in `/api/latest-min`, swap the semicolon-text parser for an array-of-pairs JSON parser, swap `amfiSchemeCode` for `npsSchemeCode`), and mirror AMFISchemePickerView into a searchable NPSSchemePickerView so each of the user's 3 strategy holdings (E/C/G) links to its own scheme code.

Purpose: NPS holdings get the same best-effort/cached/never-blocks/manual-override/staleness posture as MF (ASSET-03 reused, ASSET-09 staleness, ASSET-04 reversed for NPS). This service is also consumed by the SIP accrual engine's NPS path and by EditAssetView's NPS section (11.1-04).
Output: NPSNavService.swift, NPSSchemePickerView.swift, filled-in NPSNavServiceTests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11.1-sip-automation-and-nps-nav-auto-refresh/11.1-CONTEXT.md
@.planning/phases/11.1-sip-automation-and-nps-nav-auto-refresh/11.1-RESEARCH.md
@.planning/phases/11.1-sip-automation-and-nps-nav-auto-refresh/11.1-PATTERNS.md
@MyHomeApp/Support/AMFINavService.swift
@MyHomeApp/Features/Assets/AMFISchemePickerView.swift
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: NPSNavService — clone AMFINavService with npsnav.in latest-min parser</name>
  <files>MyHomeApp/Support/NPSNavService.swift, MyHomeTests/NPSNavServiceTests.swift</files>
  <read_first>
    - MyHomeApp/Support/AMFINavService.swift (ENTIRE file — the verbatim clone target: AMFIScheme struct, shouldFetch IST gate ~71-76, refreshIfNeeded ~84-96, forceRefresh ~99-106, performFetch ~110-151, silent-fail catch)
    - .planning/phases/11.1-sip-automation-and-nps-nav-auto-refresh/11.1-PATTERNS.md (NPSNavService section: class decl, NPSEntry Decodable, parseNPSLatest, NPSScheme struct, what-to-swap list)
    - .planning/phases/11.1-sip-automation-and-nps-nav-auto-refresh/11.1-RESEARCH.md (Pattern 1 NPSNavService; Pitfall 1 JSON Number; Open Question 1 defensive metadata parse; External API Reference latest-min shape)
    - MyHomeTests/NPSNavServiceTests.swift (Wave-0 scaffold from 11.1-01); MyHomeTests/AMFINavServiceTests.swift (test analog); MyHomeTests/Fixtures (committed npsnav.in latest-min sample)
  </read_first>
  <behavior>
    - shouldFetch(lastFetchDate:referenceDate:) is false when lastFetchDate is at/after today's IST start-of-day, true otherwise (verbatim from AMFINavService IST gate)
    - parseNPSLatest decodes `[["SM001003", 49.5429], ...]` (nav is a JSON Number) into [NPSScheme] with nav as a Decimal via String(format:"%.4f",) — never Decimal(exactly:)
    - A malformed/empty body yields [] (no throw); performFetch catch is silent (cached NAV preserved)
    - performFetch updates Asset.currentNAV + navAsOfDate for every Asset whose npsSchemeCode matches a fetched code, then saves
  </behavior>
  <action>
    Create `MyHomeApp/Support/NPSNavService.swift` by cloning AMFINavService verbatim (PATTERNS NPSNavService section). Swap exactly: class name -> `NPSNavService`; scheme struct -> `NPSScheme` (code:String, name:String, nav:Decimal, id:String{code}); URL constant -> `URL(string: "https://npsnav.in/api/latest-min")!`; lastFetchKey -> `"npsNavLastFetchDate"`; the parser body; and the Asset field read from `amfiSchemeCode` -> `npsSchemeCode`. Keep IST gate, isFetching re-entrancy guard, Task-wrap, defer, silent-fail catch, currentNAV/navAsOfDate update loop, and context.save() identical.
    Implement `parseNPSLatest(_ data:Data) -> [NPSScheme]` using a custom `NPSEntry: Decodable` reading an unkeyed container (decode String code, then Double nav) and converting `Decimal(string: String(format: "%.4f", entry.nav)) ?? skip` (Pitfall 1). Parse DEFENSIVELY for both a bare top-level array and an object-with-data wrapper (Open Question 1); set navAsOfDate to today IST start-of-day when no per-entry date exists (latest-min has none).
    Fill NPSNavServiceTests: parse the committed latest-min fixture into >0 schemes with non-zero Decimal NAVs; assert a fabricated Number nav 49.5429 round-trips to Decimal(string:"49.5429"); assert shouldFetch is false when lastFetch==today IST start and true when lastFetch==yesterday; assert an empty/garbage Data parses to [].
  </action>
  <verify>
    <automated>cd "/Users/reo/My Projects/my-home" && xcodebuild test -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:MyHomeTests/NPSNavServiceTests 2>&1 | tail -15 | grep -q "TEST SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - `NPSNavService.swift` contains `final class NPSNavService`, `https://npsnav.in/api/latest-min`, `"npsNavLastFetchDate"`, and reads `npsSchemeCode`
    - A test asserts parseNPSLatest on the fixture returns at least one NPSScheme with a non-zero Decimal nav
    - A test asserts shouldFetch returns false for a same-IST-day lastFetch and true for the prior day
    - A test asserts garbage Data yields an empty array (silent-fail, no throw)
    - `-only-testing:MyHomeTests/NPSNavServiceTests` reports TEST SUCCEEDED
  </acceptance_criteria>
  <done>NPSNavService refreshes NPS NAVs best-effort with the AMFI IST/silent-fail posture and a Number-nav JSON parser, unit-tested offline.</done>
</task>

<task type="auto">
  <name>Task 2: NPSSchemePickerView — mirror AMFISchemePickerView</name>
  <files>MyHomeApp/Features/Assets/NPSSchemePickerView.swift</files>
  <read_first>
    - MyHomeApp/Features/Assets/AMFISchemePickerView.swift (ENTIRE file — verbatim mirror: @Binding selectedSchemeCode, query @State, filteredSchemes, 4-state content switcher ~48-57, loadedState rows ~86-128, .searchable)
    - .planning/phases/11.1-sip-automation-and-nps-nav-auto-refresh/11.1-PATTERNS.md (NPSSchemePickerView section: struct decl, filteredSchemes adds code search, loadedState rows show name + code, T-11-10 plain Text, 44pt touch target)
    - MyHomeApp/Support/NPSNavService.swift (Task 1 output — schemeList + isFetching + NPSScheme)
  </read_first>
  <action>
    Create `MyHomeApp/Features/Assets/NPSSchemePickerView.swift` mirroring AMFISchemePickerView verbatim (PATTERNS NPSSchemePickerView section). Swap: service type -> `var npsNavService: NPSNavService`; scheme type -> `[NPSScheme]`; filteredSchemes filters on `name` OR `code` (NPS users search by PFM/asset-class and by code). Keep the 4-state content switcher (isFetching / empty / loaded), the List+ForEach rows displaying `scheme.name` (plain Text — T-11-10) over `scheme.code` (caption), the checkmark on selection, `.frame(minHeight: 44)`, `.listStyle(.insetGrouped)`, and `.searchable(text:$query, placement:.navigationBarDrawer(displayMode:.always))`. `@Binding var selectedSchemeCode: String?`; set it + dismiss on tap. NPSScheme.name may be empty until resolved from `/api/detailed` — display the code prominently so an empty name is still selectable (Open Question 2: picker shows both Tier I/II as they appear in the scheme list).
    Add a SwiftUI preview if the analog has one. Do NOT modify EditAssetView here (that is 11.1-04).
  </action>
  <verify>
    <automated>cd "/Users/reo/My Projects/my-home" && grep -q "struct NPSSchemePickerView" MyHomeApp/Features/Assets/NPSSchemePickerView.swift && grep -q "selectedSchemeCode" MyHomeApp/Features/Assets/NPSSchemePickerView.swift && grep -q "\.searchable" MyHomeApp/Features/Assets/NPSSchemePickerView.swift && xcodebuild build -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' 2>&1 | tail -5 | grep -q "BUILD SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - `NPSSchemePickerView.swift` contains `struct NPSSchemePickerView: View`, `@Binding var selectedSchemeCode: String?`, and `var npsNavService: NPSNavService`
    - filteredSchemes filters on both `name` and `code`
    - Rows use `Text(scheme.name)` / `Text(scheme.code)` (plain Text, no AttributedString markdown — T-11-10) and `.frame(minHeight: 44)`
    - `xcodebuild build ...` reports BUILD SUCCEEDED
  </acceptance_criteria>
  <done>A searchable NPS scheme picker mirrors the AMFI picker, binds a selected scheme code, and builds clean on iPhone 17 sim.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| NPSNavService ← npsnav.in | Untrusted single-maintainer JSON crosses into stored NAV values |
| User → NPSSchemePickerView | User-typed search query filters the scheme list |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-113-01 | Tampering | Malformed npsnav.in latest-min body | mitigate | parseNPSLatest returns [] on decode failure; performFetch catch silent-fail preserves cached NAV (test: garbage -> []) |
| T-113-02 | Tampering | NAV precision loss via Double | mitigate | Number nav converted via `Decimal(string: String(format:"%.4f",))`, never Decimal(exactly:) (Pitfall 1) |
| T-113-03 | Information Disclosure | Plain-HTTP to npsnav.in | mitigate | Hard-coded `https://` URL; iOS ATS blocks HTTP by default (T-11-05 posture) |
| T-113-04 | Tampering | Markdown/HTML injection via scheme name in picker | mitigate | Scheme name rendered with plain `Text()` only (T-11-10), never AttributedString(markdown:) |
| T-113-05 | Denial of Service | npsnav.in single-maintainer outage | accept | Silent-fail + staleness badge + manual NAV override is the designed posture (Pitfall 7); no retry/alternative-endpoint chasing |
| T-111-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase (zero new SPM deps) |
</threat_model>

<verification>
- `xcodebuild test ... -only-testing:MyHomeTests/NPSNavServiceTests` green on iPhone 17 sim.
- `xcodebuild build -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17'` succeeds.
- NPSNavService is NOT yet injected into RootView/EditAssetView (that is 11.1-04).
</verification>

<success_criteria>
- NPSNavService clones the AMFI best-effort/cached/silent-fail/IST posture for NPS NAVs (ASSET-03 reused, ASSET-04 reversed, ASSET-09 staleness).
- NPSSchemePickerView mirrors the AMFI picker with name+code search.
- Number-nav precision handled via String intermediary; parser unit-tested offline.
</success_criteria>

<output>
Create `.planning/phases/11.1-sip-automation-and-nps-nav-auto-refresh/11.1-03-SUMMARY.md` when done.
</output>
