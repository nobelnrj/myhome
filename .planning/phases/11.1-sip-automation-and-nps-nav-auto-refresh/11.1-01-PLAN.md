---
phase: 11.1-sip-automation-and-nps-nav-auto-refresh
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - MyHomeApp/Persistence/Schema/SchemaV8.swift
  - MyHomeApp/Persistence/Schema/MigrationPlan.swift
  - MyHomeApp/Persistence/Models/Asset.swift
  - MyHomeApp/Persistence/Models/Account.swift
  - MyHomeApp/Persistence/Models/Category.swift
  - MyHomeApp/Persistence/Models/Expense.swift
  - MyHomeApp/Persistence/Models/NetWorthSnapshot.swift
  - MyHomeApp/Persistence/Models/Note.swift
  - MyHomeApp/Persistence/Models/NoteBlock.swift
  - MyHomeApp/Persistence/Models/SIP.swift
  - MyHomeApp/Persistence/Models/SIPAmountChange.swift
  - MyHomeApp/Persistence/Models/Contribution.swift
  - MyHome.xcodeproj/project.pbxproj
  - MyHomeTests/SchemaV8MigrationTests.swift
  - MyHomeTests/NPSNavServiceTests.swift
  - MyHomeTests/SIPAccrualServiceTests.swift
  - MyHomeTests/ReconcileTests.swift
  - MyHomeTests/Fixtures
autonomous: true
requirements:
  - D-08
  - ASSET-08
must_haves:
  truths:
    - "App launches and migrates a real SchemaV7 store to SchemaV8 without crashing"
    - "Existing Asset rows gain npsSchemeCode = nil after migration"
    - "SIP, SIPAmountChange, and Contribution can be inserted, saved, and queried without error"
    - "Note and Asset save/query still work after the typealias flip (no STAB-08 crash)"
  artifacts:
    - path: "MyHomeApp/Persistence/Schema/SchemaV8.swift"
      provides: "SchemaV8 versioned schema: V7 models copied verbatim + npsSchemeCode + SIP/SIPAmountChange/Contribution @Models"
      contains: "enum SchemaV8"
    - path: "MyHomeApp/Persistence/Models/SIP.swift"
      provides: "typealias SIP = SchemaV8.SIP"
      contains: "typealias SIP = SchemaV8.SIP"
    - path: "MyHomeTests/SchemaV8MigrationTests.swift"
      provides: "BLOCKING V7->V8 additive migration fixture test"
      contains: "SchemaV8MigrationTests"
    - path: "MyHomeTests/NPSNavServiceTests.swift"
      provides: "Wave-0 scaffold for NPS parser + IST gate (per 11.1-03)"
    - path: "MyHomeTests/SIPAccrualServiceTests.swift"
      provides: "Wave-0 scaffold for accrual math (per 11.1-02)"
    - path: "MyHomeTests/ReconcileTests.swift"
      provides: "Wave-0 scaffold for reconcile overwrite (per 11.1-05)"
  key_links:
    - from: "MyHomeApp/Persistence/Schema/MigrationPlan.swift"
      to: "SchemaV8"
      via: "schemas array append + v7ToV8 .custom stage"
      pattern: "SchemaV8\\.self"
    - from: "MyHomeApp/Persistence/Models/*.swift"
      to: "SchemaV8"
      via: "all 10 typealiases flipped to SchemaV8 in one commit (STAB-08)"
      pattern: "typealias \\w+ = SchemaV8\\."
---

<objective>
Lay the SchemaV8 foundation that every other plan in this phase builds on: one additive migration adding `Asset.npsSchemeCode` plus three new @Models (SIP, SIPAmountChange, Contribution), all 10 typealiases flipped to V8 atomically, all 10 new .swift files registered in project.pbxproj, and the Wave-0 test scaffolds (with committed offline fixtures) that downstream plans fill in.

Purpose: SchemaV8 is the hard dependency for the NPS service, SIP accrual engine, and all SIP UI. A partial typealias flip (STAB-08) or a missing pbxproj ref crashes the app or silently drops a file from the build. This plan gets the schema + build wiring right once.
Output: SchemaV8.swift, updated MigrationPlan.swift, 3 new typealias files + 7 flipped, pbxproj entries for all new files, SchemaV8MigrationTests (BLOCKING), and empty-but-compiling NPSNavServiceTests / SIPAccrualServiceTests / ReconcileTests scaffolds with fixtures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11.1-sip-automation-and-nps-nav-auto-refresh/11.1-CONTEXT.md
@.planning/phases/11.1-sip-automation-and-nps-nav-auto-refresh/11.1-RESEARCH.md
@.planning/phases/11.1-sip-automation-and-nps-nav-auto-refresh/11.1-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SchemaV8 versioned schema + 3 new @Models</name>
  <files>MyHomeApp/Persistence/Schema/SchemaV8.swift</files>
  <read_first>
    - MyHomeApp/Persistence/Schema/SchemaV7.swift (copy ALL models verbatim into V8; CloudKit rules lines 1-21; Asset @Model ~249-269; Expense.accountID bare-UUID pattern ~113-115)
    - .planning/phases/11.1-sip-automation-and-nps-nav-auto-refresh/11.1-PATTERNS.md (SchemaV8.swift section: enum header, models[] list, field shapes for SIP/SIPAmountChange/Contribution)
    - .planning/phases/11.1-sip-automation-and-nps-nav-auto-refresh/11.1-RESEARCH.md (Pattern 6 SchemaV8 Migration; proposed model fields)
  </read_first>
  <behavior>
    - SchemaV8.versionIdentifier == Schema.Version(8, 0, 0)
    - SchemaV8.models lists all 7 V7 models + SIP + SIPAmountChange + Contribution (10 total)
    - A SIP instance defaults: dayOfMonth=1, amount=Decimal(0), isActive=true, lastAccruedDate=nil, npsAllocationE/C/G=0
    - SchemaV8.Asset has npsSchemeCode: String? defaulting nil, in addition to every V7 Asset field
  </behavior>
  <action>
    Create `enum SchemaV8: VersionedSchema` with `static let versionIdentifier = Schema.Version(8, 0, 0)`. Copy every V7 model verbatim into the V8 enum (Expense, Category, Note, NoteBlock, Account, Asset, NetWorthSnapshot) — do NOT reference SchemaV7 types; nest fresh copies under SchemaV8. Append `var npsSchemeCode: String? = nil` to SchemaV8.Asset after its last existing field (D-08, parallels existing `amfiSchemeCode`).
    Add three new `@Model final class` types per PATTERNS.md SchemaV8 section, all obeying SchemaV7's CloudKit rules (every property defaulted or optional; NO `@Attribute(.unique)`; `Decimal` for money not Double; UTC `Date`; `UUID` id; NO stored enums):
    - `SIP`: id:UUID, assetID:UUID (bare UUID, NOT @Relationship), dayOfMonth:Int=1, amount:Decimal=Decimal(0), startDate:Date, isActive:Bool=true (D-04), lastAccruedDate:Date?=nil, reminderNotificationID:String?=nil, createdAt:Date, npsAllocationE/C/G:Int=0 (D-01), npsAssetE/C/G:UUID?=nil.
    - `SIPAmountChange`: id:UUID, sipID:UUID (bare UUID), effectiveFrom:Date, amount:Decimal=Decimal(0), createdAt:Date (D-07).
    - `Contribution`: id:UUID, assetID:UUID, sipID:UUID, date:Date, amount:Decimal=Decimal(0), navUsed:Decimal=Decimal(0), navDate:Date, unitsAdded:Decimal=Decimal(0), isEstimate:Bool=true (D-03), createdAt:Date.
    `models[]` must list all 10 in the same shape as SchemaV7.models. Add `models` and `versionIdentifier` exactly as V7 declares them.
    Do NOT flip any typealias in this task (Task 3 does the atomic flip).
  </action>
  <verify>
    <automated>cd "/Users/reo/My Projects/my-home" && grep -q "enum SchemaV8" MyHomeApp/Persistence/Schema/SchemaV8.swift && grep -q "npsSchemeCode" MyHomeApp/Persistence/Schema/SchemaV8.swift && grep -c "@Model" MyHomeApp/Persistence/Schema/SchemaV8.swift</automated>
  </verify>
  <acceptance_criteria>
    - `SchemaV8.swift` contains `enum SchemaV8: VersionedSchema` and `Schema.Version(8, 0, 0)`
    - `SchemaV8.swift` defines `final class SIP`, `final class SIPAmountChange`, `final class Contribution` each annotated `@Model`
    - `SchemaV8.Asset` contains `var npsSchemeCode: String?`
    - No `@Attribute(.unique)` anywhere in the file; no `@Relationship` on SIP/Contribution back-refs
    - `models` array literal lists exactly 10 `.self` entries
  </acceptance_criteria>
  <done>SchemaV8.swift compiles as a standalone versioned schema with all V7 models copied verbatim, npsSchemeCode appended to Asset, and SIP/SIPAmountChange/Contribution defined per CloudKit rules.</done>
</task>

<task type="auto">
  <name>Task 2: Append v7ToV8 migration stage + register all 10 new files in project.pbxproj</name>
  <files>MyHomeApp/Persistence/Schema/MigrationPlan.swift, MyHome.xcodeproj/project.pbxproj</files>
  <read_first>
    - MyHomeApp/Persistence/Schema/MigrationPlan.swift (schemas array line ~11; stages array line ~15; v6ToV7 .custom stage lines ~116-121 — the exact analog to copy)
    - .planning/phases/11.1-sip-automation-and-nps-nav-auto-refresh/11.1-PATTERNS.md (MigrationPlan.swift section; Critical Cross-Cutting Warning 2 = 10 files x 4 pbxproj edits)
    - .planning/phases/11.1-sip-automation-and-nps-nav-auto-refresh/11.1-RESEARCH.md (Pitfall 4 xcodeproj 4-edit rule)
    - Memory [[xcodeproj-explicit-file-refs]]
  </read_first>
  <action>
    In MigrationPlan.swift: append `SchemaV8.self` to the `schemas` array (after `SchemaV7.self`); append `v7ToV8` to the `stages` array (after `v6ToV7`); add `static let v7ToV8 = MigrationStage.custom(fromVersion: SchemaV7.self, toVersion: SchemaV8.self, willMigrate: nil, didMigrate: nil)` mirroring the existing v6ToV7 stage. Never remove any earlier schema/stage. didMigrate is nil because the migration is purely additive (npsSchemeCode defaults nil; SIP/SIPAmountChange/Contribution are new empty tables — no backfill).
    In MyHome.xcodeproj/project.pbxproj: register all 10 NEW .swift files this phase introduces with the 4-edit rule each (PBXFileReference, PBXBuildFile, PBXSourcesBuildPhase app target, PBXSourcesBuildPhase test target where @testable). The 10 new files: SchemaV8.swift, SIP.swift, SIPAmountChange.swift, Contribution.swift (app target), and the 6 created in later plans — NPSNavService.swift, SIPAccrualService.swift, NPSSchemePickerView.swift, SIPSetupView.swift, ContributionLogView.swift, ReconcileView.swift (app target). Also register the 4 NEW test files in the MyHomeTests target: SchemaV8MigrationTests.swift, NPSNavServiceTests.swift, SIPAccrualServiceTests.swift, ReconcileTests.swift. Mirror the exact entry format of an existing Asset.swift / AMFINavService.swift / SchemaV7MigrationTests.swift reference already in the file (copy a known-good entry, swap name + generate fresh 24-hex UUIDs). Registering all files here once prevents the post-merge build-gate silent-omission failure across all downstream plans. Per-plan creator map (the 14 entries must match files actually created across plans 01-05, no more, no less): 01 creates SchemaV8.swift/SIP.swift/SIPAmountChange.swift/Contribution.swift + the 4 test scaffolds; 02 creates SIPAccrualService.swift; 03 creates NPSNavService.swift/NPSSchemePickerView.swift; 04 creates SIPSetupView.swift; 05 creates ContributionLogView.swift/ReconcileView.swift and fills ReconcileTests.swift. (RootView.swift, EditAssetView.swift, AssetDetailView.swift, MigrationPlan.swift, NotificationActions.swift, and the 7 flipped model files are MODIFIED not created — already in pbxproj, do NOT add new refs for them.)
  </action>
  <verify>
    <automated>cd "/Users/reo/My Projects/my-home" && grep -q "SchemaV8.self" MyHomeApp/Persistence/Schema/MigrationPlan.swift && grep -q "v7ToV8" MyHomeApp/Persistence/Schema/MigrationPlan.swift && for f in SchemaV8 SIP SIPAmountChange Contribution NPSNavService SIPAccrualService NPSSchemePickerView SIPSetupView ContributionLogView ReconcileView SchemaV8MigrationTests NPSNavServiceTests SIPAccrualServiceTests ReconcileTests; do grep -q "$f.swift" MyHome.xcodeproj/project.pbxproj || { echo "MISSING pbxproj ref: $f.swift"; exit 1; }; done && echo "all pbxproj refs present"</automated>
  </verify>
  <acceptance_criteria>
    - `MigrationPlan.swift` schemas array ends with `SchemaV8.self`; stages array ends with `v7ToV8`
    - `v7ToV8` declared as `MigrationStage.custom(fromVersion: SchemaV7.self, toVersion: SchemaV8.self, willMigrate: nil, didMigrate: nil)`
    - All 14 new .swift filenames (10 source + 4 test) appear in project.pbxproj
    - The grep loop above prints `all pbxproj refs present`
  </acceptance_criteria>
  <done>MigrationPlan chains V7->V8 additively, and every new file this phase will create is already registered in the build target so no later plan hits a silent compile-omission.</done>
</task>

<task type="auto">
  <name>Task 3: Atomic typealias flip (all 10) + 3 new typealias files</name>
  <files>MyHomeApp/Persistence/Models/Asset.swift, MyHomeApp/Persistence/Models/Account.swift, MyHomeApp/Persistence/Models/Category.swift, MyHomeApp/Persistence/Models/Expense.swift, MyHomeApp/Persistence/Models/NetWorthSnapshot.swift, MyHomeApp/Persistence/Models/Note.swift, MyHomeApp/Persistence/Models/NoteBlock.swift, MyHomeApp/Persistence/Models/SIP.swift, MyHomeApp/Persistence/Models/SIPAmountChange.swift, MyHomeApp/Persistence/Models/Contribution.swift</files>
  <read_first>
    - MyHomeApp/Persistence/Models/Asset.swift (typealias pattern, line 17: `typealias Asset = SchemaV7.Asset`)
    - .planning/phases/11.1-sip-automation-and-nps-nav-auto-refresh/11.1-PATTERNS.md (Typealias Files section; Critical Cross-Cutting Warning 1 = STAB-08)
    - Memory [[schema-version-mutation-footgun]] (STAB-08: flip ALL typealiases together or note save/query crashes)
  </read_first>
  <action>
    Flip ALL 7 existing typealiases from `SchemaV7.X` to `SchemaV8.X` in the SAME commit (STAB-08), at the exact known lines: Asset.swift:17, Account.swift:16, Category.swift:18, Expense.swift:21, NetWorthSnapshot.swift:16, Note.swift:26, NoteBlock.swift:23. Create 3 NEW typealias files mirroring Asset.swift's structure (import SwiftData + doc comment + one typealias): SIP.swift (`typealias SIP = SchemaV8.SIP`), SIPAmountChange.swift (`typealias SIPAmountChange = SchemaV8.SIPAmountChange`), Contribution.swift (`typealias Contribution = SchemaV8.Contribution`). After this task, grep must show ZERO `SchemaV7` references remaining in Persistence/Models/. This is the STAB-08 atomic flip — a partial flip crashes note/asset save/query at runtime.
  </action>
  <verify>
    <automated>cd "/Users/reo/My Projects/my-home" && test "$(grep -rl 'SchemaV7' MyHomeApp/Persistence/Models/ | wc -l | tr -d ' ')" = "0" && test "$(grep -rl 'typealias.*= SchemaV8' MyHomeApp/Persistence/Models/ | wc -l | tr -d ' ')" = "10" && echo "10 typealiases on V8, 0 on V7"</automated>
  </verify>
  <acceptance_criteria>
    - Zero files under `MyHomeApp/Persistence/Models/` contain the string `SchemaV7`
    - Exactly 10 files under `MyHomeApp/Persistence/Models/` contain `typealias <Name> = SchemaV8.`
    - SIP.swift, SIPAmountChange.swift, Contribution.swift exist and each contain `import SwiftData`
  </acceptance_criteria>
  <done>All 10 model typealiases resolve to SchemaV8 in one commit; no SchemaV7 reference survives in the Models directory.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: SchemaV8 migration fixture test (BLOCKING) + Wave-0 test scaffolds + fixtures</name>
  <files>MyHomeTests/SchemaV8MigrationTests.swift, MyHomeTests/NPSNavServiceTests.swift, MyHomeTests/SIPAccrualServiceTests.swift, MyHomeTests/ReconcileTests.swift, MyHomeTests/Fixtures</files>
  <read_first>
    - MyHomeTests/SchemaV7MigrationTests.swift (the exact analog — copy its V6->V7 fixture-store-open structure for V7->V8)
    - MyHomeTests/Fixtures (existing fixture-store layout; how prior migration tests load a real store)
    - .planning/phases/11.1-sip-automation-and-nps-nav-auto-refresh/11.1-VALIDATION.md (Per-Task Verification Map; Wave 0 Requirements: fixtures for npsnav.in historical, mfapi.in historical, NAVAll.txt)
    - .planning/phases/11.1-sip-automation-and-nps-nav-auto-refresh/11.1-RESEARCH.md (External API Reference for exact JSON fixture shapes; Validation Architecture test map)
  </read_first>
  <behavior>
    - Opening a committed SchemaV7 fixture store under the SchemaV8 container migrates without throwing
    - After migration, existing Asset rows have npsSchemeCode == nil
    - After migration, a freshly inserted Note and Asset save and re-query without error (STAB-08 guard)
    - SIP, SIPAmountChange, Contribution can be inserted into the migrated store and queried back
  </behavior>
  <action>
    Write `SchemaV8MigrationTests` (Swift Testing `import Testing`) mirroring SchemaV7MigrationTests: open a committed V7 fixture store with the full AppMigrationPlan, assert no throw, assert existing Asset.npsSchemeCode == nil, assert a round-trip insert+query of Note, Asset, SIP, and Contribution succeeds (proves STAB-08 typealias flip is consistent). This test is BLOCKING — it must pass before any Wave-2 plan starts.
    Commit offline JSON/text fixtures under MyHomeTests/Fixtures (or Resources) for downstream parser tests: an npsnav.in `/api/latest-min` array-of-pairs sample, an npsnav.in `/api/historical/{code}` reverse-chron `DD-MM-YYYY` Number-nav sample, an mfapi.in `/mf/{code}` reverse-chron `DD-MM-YYYY` String-nav sample. Use the exact shapes in RESEARCH.md External API Reference.
    Create COMPILING-but-skeletal test files NPSNavServiceTests.swift (per 11.1-03), SIPAccrualServiceTests.swift (per 11.1-02), ReconcileTests.swift (per 11.1-05): each an empty `@Suite struct` with a single `@Test func placeholder() {}` and a header comment listing the behaviors that plan will fill in (from VALIDATION.md Per-Task Verification Map). These scaffolds exist so downstream plans add tests, not files (their pbxproj refs were registered in Task 2).
  </action>
  <verify>
    <automated>cd "/Users/reo/My Projects/my-home" && xcodebuild test -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:MyHomeTests/SchemaV8MigrationTests 2>&1 | tail -20 | grep -q "TEST SUCCEEDED\|** TEST SUCCEEDED **"</automated>
  </verify>
  <acceptance_criteria>
    - `xcodebuild test ... -only-testing:MyHomeTests/SchemaV8MigrationTests` reports TEST SUCCEEDED
    - NPSNavServiceTests.swift, SIPAccrualServiceTests.swift, ReconcileTests.swift compile (full suite builds)
    - Fixture files for npsnav.in latest-min, npsnav.in historical, and mfapi.in history exist under MyHomeTests
    - Migration test asserts `npsSchemeCode == nil` on a pre-existing Asset and round-trips SIP + Contribution
  </acceptance_criteria>
  <done>The BLOCKING V7->V8 migration fixture test passes on iPhone 17 sim, offline parser fixtures are committed, and the three downstream test scaffolds compile so Wave-2/3 plans only add test methods.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SwiftData store ← migration | A real on-disk V7 store crosses into V8; corruption or partial typealias flip risks data loss / crash |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-111-01 | Tampering | V7→V8 migration of existing financial store | mitigate | Purely additive `.custom(willMigrate:nil,didMigrate:nil)`; BLOCKING fixture test (Task 4) proves no data loss before phase proceeds |
| T-111-02 | Denial of Service | Partial typealias flip (STAB-08) | mitigate | Task 3 flips all 10 in one commit; verify gate asserts zero SchemaV7 refs remain; migration test round-trips Note+Asset+SIP |
| T-111-03 | Tampering | New .swift silently absent from build (xcodeproj) | mitigate | Task 2 registers all 14 files with the 4-edit rule; verify gate greps every filename in pbxproj |
| T-111-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs this phase (zero new SPM deps per RESEARCH Package Legitimacy Audit: N/A); nothing to verify |
</threat_model>

<verification>
- `xcodebuild build -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17'` succeeds (all new files compile and are in the target).
- `xcodebuild test -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:MyHomeTests/SchemaV8MigrationTests` succeeds.
- `grep -rl SchemaV7 MyHomeApp/Persistence/Models/` returns nothing.
</verification>

<success_criteria>
- SchemaV8 migration runs additively on a real V7 store (BLOCKING test green).
- All 10 typealiases on SchemaV8 in one commit; 3 new model files created.
- All 14 new files registered in project.pbxproj.
- Wave-0 fixtures + 3 test scaffolds committed and compiling.
</success_criteria>

<output>
Create `.planning/phases/11.1-sip-automation-and-nps-nav-auto-refresh/11.1-01-SUMMARY.md` when done.
</output>
