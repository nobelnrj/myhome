---
phase: 11.1-sip-automation-and-nps-nav-auto-refresh
plan: 05
type: execute
wave: 4
depends_on:
  - 11.1-02
  - 11.1-03
  - 11.1-04
files_modified:
  - MyHomeApp/Features/Assets/ReconcileView.swift
  - MyHomeApp/Features/Assets/ContributionLogView.swift
  - MyHomeApp/Features/Assets/AssetDetailView.swift
  - MyHomeApp/Support/NotificationActions.swift
  - MyHomeApp/RootView.swift
  - MyHomeTests/ReconcileTests.swift
autonomous: true
requirements:
  - D-05
  - D-06
must_haves:
  truths:
    - "A user can open a confirm-actual-units screen, type the authoritative total units from their statement, and overwrite the holding's running estimate"
    - "Reconciling overwrites Asset.units but never deletes prior Contribution rows — estimates stay as history"
    - "A user can view a per-holding timeline of contributions that marks each as estimate vs reconciled"
    - "Tapping the monthly SIP reconcile notification deep-links into the reconcile screen for that SIP's holding"
  artifacts:
    - path: "MyHomeApp/Features/Assets/ReconcileView.swift"
      provides: "Whole-holding total-units overwrite screen (D-05): authoritative units input, validated, overwrites Asset.units, retains Contribution history"
      contains: "struct ReconcileView"
    - path: "MyHomeApp/Features/Assets/ContributionLogView.swift"
      provides: "Read-only per-holding contribution timeline with estimate-vs-reconciled markers"
      contains: "struct ContributionLogView"
    - path: "MyHomeApp/Support/NotificationActions.swift"
      provides: "kReconcileCategoryID + reconcile-tap routing to kOpenReconcileNotification (D-06)"
      contains: "kReconcileCategoryID"
    - path: "MyHomeTests/ReconcileTests.swift"
      provides: "Unit coverage: total-units overwrite updates Asset.units, Contribution history preserved, input validation rejects non-numeric/negative"
      contains: "ReconcileTests"
  key_links:
    - from: "MyHomeApp/Support/NotificationActions.swift"
      to: "RootView (kOpenReconcileNotification observer)"
      via: "didReceive tap with categoryIdentifier == kReconcileCategoryID posts kOpenReconcileNotification with sipID"
      pattern: "kReconcileCategoryID|kOpenReconcileNotification"
    - from: "MyHomeApp/RootView.swift"
      to: "ReconcileView"
      via: ".onReceive(kOpenReconcileNotification) resolves SIP->Asset and presents ReconcileView sheet"
      pattern: "kOpenReconcileNotification"
    - from: "MyHomeApp/Features/Assets/ReconcileView.swift"
      to: "Asset.units + Contribution"
      via: "asset.units = reconciledUnits; Contribution rows untouched; explicit context.save()"
      pattern: "asset\\.units ="
---

<objective>
Close the reconcile loop (D-05, D-06): build the whole-holding total-units overwrite screen (`ReconcileView`), the estimate-vs-reconciled contribution timeline (`ContributionLogView`), wire both into `AssetDetailView`, and make the monthly SIP reconcile notification (scheduled by 11.1-02) actually open the reconcile screen by defining `kReconcileCategoryID` + a reconcile deep-link channel and presenting `ReconcileView` from `RootView`. Fill in the Wave-0 `ReconcileTests` scaffold with real assertions.

Purpose: 11.1-02 schedules a `sip-reconcile-{uuid}` notification with `categoryIdentifier = kReconcileCategoryID`, and 11.1-01 pre-registered ReconcileView.swift + ContributionLogView.swift in pbxproj and seeded the ReconcileTests scaffold — but no plan created these files or handled the tap. Without this plan the reminder tap is a dead end (D-06 unmet) and the reconcile overwrite (D-05) does not exist.
Output: new ReconcileView.swift + ContributionLogView.swift, modified AssetDetailView.swift + NotificationActions.swift + RootView.swift, filled-in ReconcileTests.swift.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11.1-sip-automation-and-nps-nav-auto-refresh/11.1-CONTEXT.md
@.planning/phases/11.1-sip-automation-and-nps-nav-auto-refresh/11.1-RESEARCH.md
@.planning/phases/11.1-sip-automation-and-nps-nav-auto-refresh/11.1-PATTERNS.md
@MyHomeApp/Features/Assets/EditAssetView.swift
@MyHomeApp/Support/NotificationActions.swift
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ReconcileView (total-units overwrite, D-05) + ReconcileTests</name>
  <files>MyHomeApp/Features/Assets/ReconcileView.swift, MyHomeTests/ReconcileTests.swift</files>
  <read_first>
    - MyHomeApp/Features/Assets/EditAssetView.swift (save/dismiss pattern saveAsset ~222-265; isValid ~45-50; toolbar Cancel/Save ~178-190; @Environment(\.modelContext) + @Environment(\.dismiss); units field input handling)
    - .planning/phases/11.1-sip-automation-and-nps-nav-auto-refresh/11.1-PATTERNS.md (ReconcileView section ~516-547: reconcile() save pattern = asset.units = reconciledUnits, Contributions NOT deleted, explicit save; toolbar verbatim; 44pt touch target)
    - MyHomeApp/Persistence/Schema/SchemaV8.swift (Asset.units type = Decimal; Contribution fields — from 11.1-01)
    - MyHomeTests/ReconcileTests.swift (Wave-0 scaffold from 11.1-01 Task 4 to fill in)
    - .planning/phases/11.1-sip-automation-and-nps-nav-auto-refresh/11.1-CONTEXT.md (D-05 whole-holding total-units overwrite; prior estimates retained as Contribution history)
  </read_first>
  <behavior>
    - reconciledUnits parses from the text field as a Decimal; a non-numeric or empty string yields nil (Save disabled)
    - isValid is true only when the parsed reconciledUnits is non-nil and > 0
    - reconcile() sets asset.units to the parsed reconciledUnits (whole-holding overwrite — D-05), calls explicit context.save(), and dismisses
    - reconcile() does NOT delete or mutate any Contribution row — prior estimates remain queryable as history (D-05)
    - A negative or zero entry never reaches asset.units (guard + disabled Save)
  </behavior>
  <action>
    Create `MyHomeApp/Features/Assets/ReconcileView.swift` mirroring EditAssetView's NavigationStack-in-sheet + Form + validated-save shape (PATTERNS ReconcileView section). Take `var asset: Asset`; `@Environment(\.modelContext) private var context`; `@Environment(\.dismiss) private var dismiss`. State: `@State private var unitsText: String = ""` (prefill with the current `asset.units` formatted, so the user edits the authoritative figure).
    Add a computed `reconciledUnits: Decimal?` = `Decimal(string: unitsText.trimmingCharacters(in: .whitespaces))` returning nil for empty/non-numeric input (input-validation mitigation — never force-unwrap, never crash on garbage). Add `isValid: Bool` = `(reconciledUnits ?? 0) > 0`.
    Form: a section explaining "Enter the total units shown on your latest statement — this overwrites the running estimate" (plain Text — T-11-10), a single decimal `TextField` bound to `$unitsText` with `.keyboardType(.decimalPad)` and `.frame(minHeight: 44)`. Optionally show the current estimated `asset.units` for comparison (plain Text).
    Add `reconcile()` mirroring EditAssetView.saveAsset: `guard let units = reconciledUnits, units > 0 else { return }`; `asset.units = units` (D-05 whole-holding overwrite); do NOT touch Contribution rows (estimates stay as history — D-05); `try context.save()` explicit (CR-01); `assertionFailure` on catch (mirror EditAssetView line ~262); `dismiss()`.
    Toolbar Cancel / "Confirm Units" copied verbatim from EditAssetView, Save disabled when `!isValid`.
    Extract the input parse + range check into a `static func parseReconciledUnits(_ text: String) -> Decimal?` (returns the Decimal only when non-nil and > 0, else nil) so it is unit-testable without the view.
    Fill `MyHomeTests/ReconcileTests.swift` (Swift Testing `import Testing`) against an in-memory SchemaV8 container: (1) insert an Asset with units estimate + 2 Contribution rows, call the reconcile mutation (set asset.units to an authoritative figure, save), assert `asset.units` equals the new figure AND both Contribution rows still exist (history preserved — D-05); (2) assert `ReconcileView.parseReconciledUnits` returns nil for "", "abc", "-5", "0" and a non-nil Decimal for "123.4567".
  </action>
  <verify>
    <automated>cd "/Users/reo/My Projects/my-home" && grep -q "struct ReconcileView" MyHomeApp/Features/Assets/ReconcileView.swift && grep -q "asset.units =" MyHomeApp/Features/Assets/ReconcileView.swift && grep -q "parseReconciledUnits" MyHomeApp/Features/Assets/ReconcileView.swift && xcodebuild test -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:MyHomeTests/ReconcileTests 2>&1 | tail -15 | grep -q "TEST SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - `ReconcileView.swift` contains `struct ReconcileView: View`, sets `asset.units` from the parsed input, and never deletes a Contribution
    - `parseReconciledUnits` returns nil for "", "abc", "-5", "0" and a Decimal for "123.4567" (asserted in tests)
    - A test asserts that after reconcile the two pre-existing Contribution rows are still present (history retained — D-05)
    - A test asserts asset.units equals the authoritative figure after reconcile
    - `-only-testing:MyHomeTests/ReconcileTests` reports TEST SUCCEEDED
  </acceptance_criteria>
  <done>The reconcile screen overwrites the holding's total units from a validated statement figure, preserves estimate history, and the overwrite + input validation are unit-tested.</done>
</task>

<task type="auto">
  <name>Task 2: ContributionLogView (estimate-vs-reconciled timeline) + AssetDetailView entry</name>
  <files>MyHomeApp/Features/Assets/ContributionLogView.swift, MyHomeApp/Features/Assets/AssetDetailView.swift</files>
  <read_first>
    - .planning/phases/11.1-sip-automation-and-nps-nav-auto-refresh/11.1-PATTERNS.md (ContributionLogView section ~477-512: read-only @Query filtered by assetID via init predicate, sort by date reverse, row = date/amount/navUsed/unitsAdded + isEstimate badge; 44pt touch target)
    - MyHomeApp/Features/Assets/AssetDetailView.swift (ENTIRE file — where holding detail rows/sections live; how it navigates to EditAssetView; how it reads the asset)
    - MyHomeApp/Features/Assets/EditAssetView.swift (Form/List + plain Text(...) display convention — T-11-10)
    - MyHomeApp/Persistence/Schema/SchemaV8.swift (Contribution fields: date, amount, navUsed, navDate, unitsAdded, isEstimate — from 11.1-01)
  </read_first>
  <action>
    Create `MyHomeApp/Features/Assets/ContributionLogView.swift` (PATTERNS ContributionLogView section). Take `var asset: Asset`; in `init(asset:)` build a predicate `@Query` filtered by `assetID == asset.id`, sorted `\Contribution.date` order `.reverse` (mirror the PATTERNS init-predicate approach). Body: a `List`/`ForEach(contributions)` whose row shows `Text(contribution.date, ...)`, the amount and `unitsAdded` (plain Text — T-11-10), the NAV used, and an estimate-vs-reconciled marker driven by `contribution.isEstimate` (e.g. an "Estimate" badge / SF Symbol when `isEstimate == true`, "Reconciled"/checkmark when false). `.navigationTitle("Contributions")`, `.navigationBarTitleDisplayMode(.inline)`, `.frame(minHeight: 44)` on rows. Read-only — no edit/delete. Empty state via `ContentUnavailableView` when no contributions.
    In AssetDetailView: add a "Contributions" NavigationLink to `ContributionLogView(asset: asset)` and a "Reconcile units" NavigationLink (or sheet button) to `ReconcileView(asset: asset)` so both screens are reachable from the holding detail (the same surface EditAssetView is reached from). Mirror the existing navigation/section style already in AssetDetailView. Do NOT alter unrelated detail sections.
  </action>
  <verify>
    <automated>cd "/Users/reo/My Projects/my-home" && grep -q "struct ContributionLogView" MyHomeApp/Features/Assets/ContributionLogView.swift && grep -q "isEstimate" MyHomeApp/Features/Assets/ContributionLogView.swift && grep -q "ContributionLogView" MyHomeApp/Features/Assets/AssetDetailView.swift && grep -q "ReconcileView" MyHomeApp/Features/Assets/AssetDetailView.swift && xcodebuild build -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' 2>&1 | tail -5 | grep -q "BUILD SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - `ContributionLogView.swift` contains `struct ContributionLogView: View`, a predicate `@Query` filtered by the asset's id, and an `isEstimate`-driven badge
    - Rows display contribution fields via plain `Text(...)` (T-11-10) and use `.frame(minHeight: 44)`
    - AssetDetailView links to both `ContributionLogView` and `ReconcileView` for the holding
    - `xcodebuild build ...` reports BUILD SUCCEEDED
  </acceptance_criteria>
  <done>A read-only contribution timeline clearly marks estimates vs reconciled entries and is reachable, alongside the reconcile screen, from the holding detail.</done>
</task>

<task type="auto">
  <name>Task 3: kReconcileCategoryID + reconcile deep-link tap routing (D-06)</name>
  <files>MyHomeApp/Support/NotificationActions.swift, MyHomeApp/RootView.swift</files>
  <read_first>
    - MyHomeApp/Support/NotificationActions.swift (ENTIRE file: kReminderCategoryID ~9; registerReminderNotificationCategory + setNotificationCategories ~68-86; didReceive default-tap branch ~159-181; handleDeepLink + kOpenNoteNotification ~298-324 — the exact analog to mirror)
    - MyHomeApp/RootView.swift (kOpenNoteNotification .onReceive observer ~104-111; selectedTab + deepLink state ~34-35; .environment chain ~140 — the analog for a reconcile sheet observer)
    - .planning/phases/11.1-sip-automation-and-nps-nav-auto-refresh/11.1-RESEARCH.md (Pattern 5 Options ~398-410: Option 1 new category + Option 2 new Notification.Name; 11.1-02 already stamps kReconcileCategoryID + userInfo["sipID"])
    - MyHomeApp/Persistence/Schema/SchemaV8.swift (SIP.assetID — to resolve sipID -> Asset for presentation)
  </read_first>
  <action>
    In NotificationActions.swift: define `let kReconcileCategoryID = "com.myhome.sip.reconcile"` (the exact string 11.1-02 stamps on the reconcile notification) and `let kOpenReconcileNotification = Notification.Name("com.myhome.openReconcile")` next to `kOpenNoteNotification`. Register a second `UNNotificationCategory(identifier: kReconcileCategoryID, actions: [], intentIdentifiers: [], options: [])` and include it in the SAME `setNotificationCategories([...])` call inside `registerReminderNotificationCategory` (append the reconcile category to the existing array — do NOT make a second setNotificationCategories call, which would overwrite the reminder category). In `didReceive`, before the existing `default` note deep-link, branch on `response.notification.request.content.categoryIdentifier == kReconcileCategoryID`: read `userInfo["sipID"] as? String` -> UUID, and post `kOpenReconcileNotification` with `["sipID": sipID]` (mirror handleDeepLink's NotificationCenter.default.post; guard the UUID parse, never force-unwrap untrusted userInfo).
    In RootView.swift: add `@State private var deepLinkReconcileSIPID: UUID? = nil`. Add an `.onReceive(NotificationCenter.default.publisher(for: kOpenReconcileNotification))` mirroring the kOpenNoteNotification observer (~104-111): set `deepLinkReconcileSIPID` from `userInfo["sipID"]`. Present `ReconcileView` via `.sheet(item:)` (wrap the SIP-resolved Asset, or fetch the SIP by id from the modelContext then its Asset by `assetID`) — resolve sipID -> SIP -> Asset and show `ReconcileView(asset:)` in a NavigationStack sheet; clear the state on dismiss. Reuse the existing `@Environment(\.modelContext)` already present in RootView for the fetch. Do NOT remove or reorder the existing kOpenNoteNotification observer or the scenePhase chain (owned by 11.1-04).
  </action>
  <verify>
    <automated>cd "/Users/reo/My Projects/my-home" && grep -q 'kReconcileCategoryID = "com.myhome.sip.reconcile"' MyHomeApp/Support/NotificationActions.swift && grep -q "kOpenReconcileNotification" MyHomeApp/Support/NotificationActions.swift && grep -q "kOpenReconcileNotification" MyHomeApp/RootView.swift && grep -c "setNotificationCategories" MyHomeApp/Support/NotificationActions.swift | grep -q "^1$" && xcodebuild build -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' 2>&1 | tail -5 | grep -q "BUILD SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - NotificationActions.swift defines `kReconcileCategoryID = "com.myhome.sip.reconcile"` and `kOpenReconcileNotification`
    - The reconcile category is added inside the single existing `setNotificationCategories([...])` call (grep count of `setNotificationCategories` stays at 1 — reminder category not clobbered)
    - `didReceive` posts `kOpenReconcileNotification` with the parsed `sipID` when the tapped notification's category is `kReconcileCategoryID`
    - RootView observes `kOpenReconcileNotification` and presents `ReconcileView` for the resolved holding
    - `xcodebuild build ...` reports BUILD SUCCEEDED
  </acceptance_criteria>
  <done>Tapping the monthly SIP reconcile notification deep-links into ReconcileView for that SIP's holding, with a dedicated category that does not disturb the existing note reminder category.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → ReconcileView | User-typed total-units string crosses into an authoritative overwrite of Asset.units (drives net-worth math) |
| Notification userInfo → app | `userInfo["sipID"]` from a delivered notification is parsed and used to resolve a holding to present |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115-01 | Tampering | Non-numeric / negative / empty units input | mitigate | `parseReconciledUnits` returns nil for non-numeric/empty/<=0 (tested); Save disabled and `reconcile()` guards `units > 0` before touching `asset.units` — no crash, no corruption |
| T-115-02 | Tampering | Reconcile silently destroys estimate history | mitigate | `reconcile()` only sets `asset.units`; Contribution rows are never deleted/mutated (D-05; test asserts both rows survive) |
| T-115-03 | Tampering | Malformed/spoofed notification userInfo (missing/garbage sipID) | mitigate | `userInfo["sipID"]` parsed via `UUID(uuidString:)` with guard; nil → no post, no presentation; never force-unwrapped |
| T-115-04 | Denial of Service | Second setNotificationCategories call clobbers reminder category | mitigate | Reconcile category appended to the single existing `setNotificationCategories([...])` array (verify gate asserts exactly one call) |
| T-115-05 | Repudiation | Markdown/HTML in contribution/scheme display | mitigate | All contribution + units display via plain `Text()` (T-11-10), never AttributedString(markdown:) |
| T-111-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase (zero new SPM deps; RESEARCH Package Legitimacy Audit N/A) |
</threat_model>

<verification>
- `xcodebuild build -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17'` succeeds (ReconcileView, ContributionLogView, AssetDetailView, NotificationActions, RootView all compile).
- `xcodebuild test ... -only-testing:MyHomeTests/ReconcileTests` green on iPhone 17 sim.
- Manual (post-merge, human): schedule a near-term reconcile reminder, tap the banner → ReconcileView opens for the holding; enter total units → estimate overwritten, Contribution log still shows prior estimates marked as estimates.
</verification>

<success_criteria>
- D-05: whole-holding total-units overwrite implemented + tested (asset.units overwritten, Contribution history retained).
- D-06: monthly reconcile notification tap deep-links into the reconcile screen via kReconcileCategoryID + kOpenReconcileNotification.
- Estimate-vs-reconciled contribution timeline reachable from the holding detail.
</success_criteria>

<output>
Create `.planning/phases/11.1-sip-automation-and-nps-nav-auto-refresh/11.1-05-SUMMARY.md` when done.
</output>
</content>
</invoke>
