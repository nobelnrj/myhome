---
phase: 11.1-sip-automation-and-nps-nav-auto-refresh
plan: 02
type: execute
wave: 2
depends_on:
  - 11.1-01
files_modified:
  - MyHomeApp/Support/SIPAccrualService.swift
  - MyHomeTests/SIPAccrualServiceTests.swift
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-07
must_haves:
  truths:
    - "An active SIP auto-accrues one Contribution per elapsed installment date since lastAccruedDate"
    - "Each installment is priced at the NAV published on that exact date, falling back to nearest prior published NAV on holidays/weekends"
    - "An NPS SIP splits its amount across the 3 strategy holdings by allocation %, summing exactly to the total"
    - "Accrual uses the SIP amount in effect at each historical date, not the current amount"
    - "A SIP with isActive == false produces no accruals"
    - "lastAccruedDate advances only after a successful save (safe re-entry on failure)"
  artifacts:
    - path: "MyHomeApp/Support/SIPAccrualService.swift"
      provides: "@MainActor @Observable accrual engine: elapsed-date enumeration, historical NAV fetch, NPS split, Contribution write, reconcile-reminder scheduling"
      contains: "final class SIPAccrualService"
      exports: ["accrueIfNeeded"]
    - path: "MyHomeTests/SIPAccrualServiceTests.swift"
      provides: "Unit coverage for date math, NPS split, amount-history, nearest-prior NAV, isActive guard, MF/NPS parsers"
      contains: "SIPAccrualServiceTests"
  key_links:
    - from: "MyHomeApp/Support/SIPAccrualService.swift"
      to: "api.mfapi.in / npsnav.in historical endpoints"
      via: "URLSession fetch inside Task, session-cached per fund"
      pattern: "api.mfapi.in/mf|npsnav.in/api/historical"
    - from: "MyHomeApp/Support/SIPAccrualService.swift"
      to: "Contribution + Asset.units"
      via: "context.insert(Contribution) then asset.units += unitsAdded, save, then advance lastAccruedDate"
      pattern: "Contribution\\(|\\.units \\+="
---

<objective>
Build the SIP accrual engine: a `@MainActor @Observable final class SIPAccrualService` cloned in shape from AMFINavService that, on each invocation, finds every elapsed installment date for every active SIP, prices it at the exact-date historical NAV (mfapi.in for MF, npsnav.in history for NPS, nearest-prior fallback), splits NPS amounts across the 3 strategy holdings by allocation %, writes Contribution rows, bumps Asset.units, advances lastAccruedDate after save, and schedules the monthly reconcile reminder.

Purpose: This is the heart of the phase — the testable financial math behind D-01 (split), D-02 (exact-date NAV), D-03 (assume-it-ran), D-04 (isActive), D-07 (amount history). It is pure-function-heavy and unit-tested without the simulator UI.
Output: SIPAccrualService.swift + filled-in SIPAccrualServiceTests covering the VALIDATION.md test map.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11.1-sip-automation-and-nps-nav-auto-refresh/11.1-CONTEXT.md
@.planning/phases/11.1-sip-automation-and-nps-nav-auto-refresh/11.1-RESEARCH.md
@.planning/phases/11.1-sip-automation-and-nps-nav-auto-refresh/11.1-PATTERNS.md
@MyHomeApp/Support/AMFINavService.swift
@MyHomeApp/Support/NetWorthSnapshotService.swift
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Historical NAV fetch + parse + nearest-prior lookup (pure functions)</name>
  <files>MyHomeApp/Support/SIPAccrualService.swift, MyHomeTests/SIPAccrualServiceTests.swift</files>
  <read_first>
    - MyHomeApp/Support/AMFINavService.swift (class skeleton; silent-fail catch; Decimal-from-String parsing lines ~192-193; navDateFormatter)
    - .planning/phases/11.1-sip-automation-and-nps-nav-auto-refresh/11.1-RESEARCH.md (Pattern 2 Historical NAV Lookup; Pitfall 1 JSON Number vs String; Pitfall 2 DD-MM-YYYY vs DD-MMM-YYYY; External API Reference)
    - .planning/phases/11.1-sip-automation-and-nps-nav-auto-refresh/11.1-PATTERNS.md (SIPAccrualService section; historyDateFormatter; Shared Patterns Decimal Money + Silent-Fail)
    - MyHomeTests/SIPAccrualServiceTests.swift (Wave-0 scaffold from 11.1-01 to fill in); MyHomeTests/Fixtures (committed npsnav.in + mfapi.in samples)
  </read_first>
  <behavior>
    - mfapi.in `data` entries (`"nav"` is a String like "102.88080") parse to Decimal; npsnav.in history (`nav` is a Number like 49.5429) parse to Decimal via String(format:"%.4f",) intermediary
    - History date strings `"10-06-2026"` (dd-MM-yyyy) parse with the dedicated historyDateFormatter; an NAVAll-style `"10-Jun-2026"` returns nil through that formatter
    - navEntry(for:in:) on a reverse-chron array returns the first entry whose date <= target (nearest-prior); returns the prior-Friday entry for a Saturday target; returns nil when target precedes the oldest entry
  </behavior>
  <action>
    Create `MyHomeApp/Support/SIPAccrualService.swift` with `@MainActor @Observable final class SIPAccrualService { var modelContext: ModelContext?; private(set) var isAccruing = false }` cloning the AMFINavService skeleton (RESEARCH Pattern; PATTERNS SIPAccrualService section).
    Add a static `historyDateFormatter` (dateFormat `"dd-MM-yyyy"`, locale `en_US_POSIX`, timeZone `Asia/Kolkata`) — SEPARATE from any AMFI `"dd-MMM-yyyy"` formatter (Pitfall 2; never share).
    Add MF parser: decode mfapi.in `https://api.mfapi.in/mf/{amfiSchemeCode}` into `{meta, data:[{date,nav:String}], status}`; map to `[(date: Date, nav: Decimal)]` via `Decimal(string:)` on the String nav, dropping unparseable rows.
    Add NPS parser: decode npsnav.in `https://npsnav.in/api/historical/{npsSchemeCode}` array of `{date, nav: Double}`; map nav via `Decimal(string: String(format: "%.4f", nav))` (Pitfall 1 — never `Decimal(exactly:)`).
    Add `func navEntry(for targetDate: Date, in history: [(date: Date, nav: Decimal)]) -> (date: Date, nav: Decimal)?` returning `history.first { $0.date <= targetDate }` (arrays are reverse-chron; nearest-prior fallback per D-02).
    Add a session cache `[String: [(Date, Decimal)]]` keyed by scheme code so each fund's full history is fetched once per accrual pass (Pitfall 8). Fetches run inside `Task` with silent-fail catch.
    Fill SIPAccrualServiceTests with: MF String-nav parse, NPS Number-nav parse, dd-MM-yyyy parses / dd-MMM-yyyy returns nil, nearest-prior fallback (exact hit, weekend->prior Friday, before-oldest->nil) — loading the committed fixtures (offline, deterministic).
  </action>
  <verify>
    <automated>cd "/Users/reo/My Projects/my-home" && xcodebuild test -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:MyHomeTests/SIPAccrualServiceTests 2>&1 | tail -15 | grep -q "TEST SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - `SIPAccrualService.swift` contains `final class SIPAccrualService` and a static `historyDateFormatter` with `"dd-MM-yyyy"`
    - SIPAccrualServiceTests asserts mfapi String-nav and npsnav Number-nav both parse to a non-zero Decimal
    - A test asserts `navEntry(for:in:)` returns the nearest-prior entry for a weekend target and nil before the oldest entry
    - `-only-testing:MyHomeTests/SIPAccrualServiceTests` reports TEST SUCCEEDED
  </acceptance_criteria>
  <done>Historical NAV fetch/parse for both sources and the nearest-prior lookup are implemented and unit-tested offline against committed fixtures.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Elapsed-date enumeration + amount-history resolution + NPS split + units math</name>
  <files>MyHomeApp/Support/SIPAccrualService.swift, MyHomeTests/SIPAccrualServiceTests.swift</files>
  <read_first>
    - .planning/phases/11.1-sip-automation-and-nps-nav-auto-refresh/11.1-RESEARCH.md (Pattern 3 IST date math + day-of-month clamping; Pattern 4 allocation split + Decimal rounding; Security: guard nav>0)
    - .planning/phases/11.1-sip-automation-and-nps-nav-auto-refresh/11.1-PATTERNS.md (Shared Patterns: IST gate, Decimal Money)
    - MyHomeApp/Support/AccountBalance.swift (Decimal money discipline — memory [[account-balance-sign-convention]])
    - MyHomeApp/Support/SIPAccrualService.swift (Task 1 output to extend)
  </read_first>
  <behavior>
    - elapsedInstallmentDates(dayOfMonth:31, in Feb) yields Feb 28/29 (clamped, never skipped)
    - lastAccruedDate == nil accrues from startDate forward; an already-accrued date is never re-emitted
    - effectiveAmount(at: date) returns the amount from the latest SIPAmountChange with effectiveFrom <= date (D-07); past dates keep the prior amount across stacked changes
    - splitAmount(total, E50/C30/G20) returns three Decimals summing EXACTLY to total; the rounding remainder lands on the largest slice (D-01)
    - units = amount / nav rounded down to scale 4; guarded so nav <= 0 skips the date
  </behavior>
  <action>
    Extend SIPAccrualService with pure helpers (RESEARCH Patterns 3 + 4):
    - `elapsedInstallmentDates(dayOfMonth:startDate:lastAccruedDate:today:calendar:) -> [Date]` walking month-by-month with an IST `.gregorian` calendar, using `Calendar.date(bySetting:.day,value:dayOfMonth,of:)` clamping (Feb 31 -> Feb 28/29), lower bound exclusive on lastAccruedDate (or startDate when nil), upper bound inclusive on today IST start-of-day.
    - `effectiveAmount(forDate:sip:changes:) -> Decimal` = amount of the latest SIPAmountChange whose effectiveFrom <= date, falling back to SIP.amount when no change precedes the date (D-07 next-installment-only; backdating explicitly out of scope).
    - `splitAmount(_:eP:cP:gP:) -> (e:Decimal,c:Decimal,g:Decimal)` using NSDecimalNumberHandler `.down` scale 2 on E and C, assigning the residual `total - (e+c)` to the largest-allocation slice so the three sum exactly to total (D-01 remainder-paisa rule). For MF SIPs (single holding, 100%) this degenerates to the whole amount on one holding.
    - `units(amount:nav:) -> Decimal?` using NSDecimalNumberHandler `.down` scale 4, returning nil when `nav <= 0` (security: never divide by zero/negative).
    Add tests: dayOfMonth=31 across Feb (leap + non-leap), lastAccruedDate=nil from startDate, no-double-accrual, effectiveAmount across two stacked changes, split E50/C30/G20 sums to total with remainder on E, split allocation not summing to 100 is rejected upstream (assert the validation helper used by UI), units rounds down to 4dp, nav<=0 -> nil.
  </action>
  <verify>
    <automated>cd "/Users/reo/My Projects/my-home" && xcodebuild test -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:MyHomeTests/SIPAccrualServiceTests 2>&1 | tail -15 | grep -q "TEST SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - A test asserts `elapsedInstallmentDates` with dayOfMonth 31 produces a Feb 28 (or 29 in a leap year) date and never skips the month
    - A test asserts `effectiveAmount` returns the older amount for a date before a change and the newer for a date after
    - A test asserts `splitAmount(Decimal(10000), 50,30,20)` returns three Decimals summing to exactly 10000 with the remainder on the largest slice
    - A test asserts `units(amount:nav:)` returns nil when nav is 0
    - `-only-testing:MyHomeTests/SIPAccrualServiceTests` reports TEST SUCCEEDED
  </acceptance_criteria>
  <done>Date enumeration, amount-history resolution, NPS allocation split, and units math are implemented as tested pure functions honoring D-01/D-02/D-07.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: accrueIfNeeded orchestration + Contribution write + monthly reconcile reminder</name>
  <files>MyHomeApp/Support/SIPAccrualService.swift, MyHomeTests/SIPAccrualServiceTests.swift</files>
  <read_first>
    - MyHomeApp/Support/AMFINavService.swift (refreshIfNeeded entry + performFetch defer/save structure)
    - MyHomeApp/Support/NotificationScheduler.swift (maxSafeMonthlyDay = 28; UNUserNotificationCenter usage)
    - .planning/phases/11.1-sip-automation-and-nps-nav-auto-refresh/11.1-RESEARCH.md (Pattern 5 reconcile reminder = direct UNUserNotificationCenter, identifier "sip-reconcile-{uuid}", kReconcileCategoryID; Pitfall 5 lastAccruedDate-after-save; Pitfall 6 day clamp; Open Question 3 isAccruing guard)
    - .planning/phases/11.1-sip-automation-and-nps-nav-auto-refresh/11.1-PATTERNS.md (SIPAccrualService scheduleReconcileReminder; Shared Patterns Task-wrap + defer + explicit save)
  </read_first>
  <behavior>
    - accrueIfNeeded() is a no-op when isAccruing is already true or modelContext is nil (re-entrancy guard)
    - For each active SIP: a Contribution row is inserted per elapsed date with isEstimate=true, amount=effectiveAmount, navUsed/navDate from history, unitsAdded computed; Asset.units increases by unitsAdded
    - An NPS SIP writes 3 Contribution rows per installment (one per strategy Asset) using each strategy holding's own NAV; an MF SIP writes 1
    - A SIP with isActive==false yields zero Contribution rows
    - lastAccruedDate advances to the last processed date ONLY after context.save() succeeds
    - After accrual, a monthly reconcile reminder is scheduled with identifier "sip-reconcile-{sip.id}" on day min(dayOfMonth+3, 28); deactivating a SIP cancels it
  </behavior>
  <action>
    Add `func accrueIfNeeded()` mirroring AMFINavService.refreshIfNeeded (guard context + !isAccruing; set isAccruing; `Task { await performAccrual(context:) }`). Implement `performAccrual` with `defer { isAccruing = false }`, IST `todayIST` start-of-day, silent-fail catch.
    Per active SIP (skip isActive==false — D-04): fetch its Asset; enumerate elapsed dates (Task 2); for each date resolve effectiveAmount (Task 2), fetch the holding's historical NAV via the session cache (Task 1) and `navEntry(for:)`; for NPS (npsAllocation set) split the amount across npsAssetE/C/G at EACH strategy holding's own historical NAV and insert 3 Contributions (D-01); for MF insert 1; set Contribution fields (date, amount, navUsed, navDate, unitsAdded, isEstimate=true, assetID, sipID); bump each target Asset.units += unitsAdded. Call `try context.save()` (explicit — CR-01) and ONLY THEN set `sip.lastAccruedDate = lastProcessedDate` and save again — never before (Pitfall 5; failure leaves cursor for safe re-entry). Guard nav>0 before dividing (skip+log otherwise).
    Add `scheduleReconcileReminder(for:)` using direct UNUserNotificationCenter (NOT NotificationScheduler.makeRequest — different domain): UNCalendarNotificationTrigger repeats:true with day=min(sip.dayOfMonth+3, NotificationScheduler.maxSafeMonthlyDay) hour=9; content.categoryIdentifier=kReconcileCategoryID; content.userInfo=["sipID": sip.id.uuidString]; identifier "sip-reconcile-{sip.id.uuidString}"; store on sip.reminderNotificationID. Add `cancelReconcileReminder(for:)` removing that identifier.
    Add tests against an in-memory SchemaV8 container: isActive=false -> 0 contributions; one MF SIP with a 2-month gap -> 2 contributions, Asset.units increased, lastAccruedDate advanced; one NPS SIP -> 3 contributions per installment summing amount; a save-failure path leaves lastAccruedDate unchanged (inject a context error or assert the order).
  </action>
  <verify>
    <automated>cd "/Users/reo/My Projects/my-home" && xcodebuild test -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:MyHomeTests/SIPAccrualServiceTests 2>&1 | tail -15 | grep -q "TEST SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - `SIPAccrualService.swift` exports `func accrueIfNeeded()` and contains `"sip-reconcile-"` and `kReconcileCategoryID`
    - A test asserts an isActive==false SIP produces zero Contribution rows
    - A test asserts an NPS SIP produces 3 Contributions per installment whose amounts sum to the installment amount
    - A test asserts lastAccruedDate is advanced only after a successful save (unchanged on a forced save failure)
    - `-only-testing:MyHomeTests/SIPAccrualServiceTests` reports TEST SUCCEEDED
  </acceptance_criteria>
  <done>accrueIfNeeded accrues estimated Contributions per elapsed date (NPS-split / MF-single), advances the cursor only after save, and schedules a monthly reconcile reminder — all unit-tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SIPAccrualService ← npsnav.in / mfapi.in | Untrusted single-maintainer JSON crosses into financial unit math |
| SIPAccrualService → SwiftData | Computed estimates written as Contribution rows + Asset.units mutation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-112-01 | Tampering | Malformed/oversized history JSON | mitigate | Decode failures drop the row / silent-fail catch; never fatalError; parser tests cover bad rows |
| T-112-02 | Tampering | Zero/negative NAV used as divisor | mitigate | `units(amount:nav:)` guards `nav > 0`, returns nil, skips the date (test asserts nil at nav 0) |
| T-112-03 | Tampering | Allocation not summing to 100 / negative % | mitigate | Validation helper rejects (sum==100, each >=0); split residual forced to sum-exactly-total |
| T-112-04 | Denial of Service | Many accrued months on first launch | mitigate | One monthly reconcile notification per SIP (not per installment); accrual is a silent background Task; history fetched once per fund/session |
| T-112-05 | Information Disclosure | Plain-HTTP to NAV endpoints | mitigate | Hard-coded `https://` URLs; ATS blocks HTTP by default (same T-11-05 posture) |
| T-112-06 | Tampering | Cursor advances before save (lost installments) | mitigate | lastAccruedDate advanced only after `try context.save()` succeeds (Pitfall 5; test) |
| T-111-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase (zero new SPM deps; RESEARCH audit N/A) |
</threat_model>

<verification>
- `xcodebuild test ... -only-testing:MyHomeTests/SIPAccrualServiceTests` green on iPhone 17 sim.
- `xcodebuild build -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17'` succeeds.
- SIPAccrualService is NOT yet wired into RootView (that is 11.1-04) — this plan ships and tests the engine in isolation.
</verification>

<success_criteria>
- Accrual engine implements D-01 (split), D-02 (exact-date + nearest-prior NAV), D-03 (assume-it-ran), D-04 (isActive guard), D-07 (amount history) with passing unit tests.
- Monthly reconcile reminder scheduling/cancellation implemented via direct UNUserNotificationCenter.
- lastAccruedDate advances only after save.
</success_criteria>

<output>
Create `.planning/phases/11.1-sip-automation-and-nps-nav-auto-refresh/11.1-02-SUMMARY.md` when done.
</output>
