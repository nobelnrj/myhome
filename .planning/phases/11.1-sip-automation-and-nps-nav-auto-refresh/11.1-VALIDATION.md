---
phase: 11.1
slug: sip-automation-and-nps-nav-auto-refresh
status: approved
nyquist_compliant: true
wave_0_complete: true
created: 2026-06-12
---

# Phase 11.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Swift Testing / XCTest (existing MyHome test target) |
| **Config file** | none — uses existing MyHome.xcodeproj scheme `MyHome` |
| **Quick run command** | `xcodebuild test -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:<TargetTests/Suite>` |
| **Full suite command** | `xcodebuild test -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17'` |
| **Estimated runtime** | ~60–120 seconds (simulator boot + suite) |

---

## Sampling Rate

- **After every task commit:** Run the quick (suite-scoped) test command for the touched area.
- **After every plan wave:** Run the full suite command.
- **Before `/gsd-verify-work`:** Full suite must be green and the app must build/run on iPhone 17 sim.
- **Max feedback latency:** ~120 seconds.

---

## Per-Task Verification Map

> Populated by the planner per task. Core testable behaviors this phase must cover:

| Area | Requirement / Decision | Test Type | Notes |
|------|------------------------|-----------|-------|
| NPS NAV history parse (`DD-MM-YYYY`, JSON Number) | D-02 | unit | Distinct from NAVAll.txt `DD-MMM-YYYY` — guard the format pitfall |
| MF historical NAV parse (`DD-MM-YYYY`, JSON String) | D-02 | unit | nav is String here vs Number on npsnav.in |
| Exact-date NAV lookup + nearest-prior fallback | D-02 | unit | holiday/weekend/not-yet-published → nearest prior published NAV + capture date used |
| SIP elapsed-installment date computation (IST) | D-03 | unit | day-of-month > month length (31 in Feb) clamping; weekend/holiday resolution |
| units = amount ÷ NAV (Decimal) | D-01 | unit | no Double drift; rounding/remainder policy |
| NPS allocation split across E/C/G (sum-to-100, remainder paisa) | D-01 | unit | deterministic remainder assignment to largest slice |
| Amount-change history applies from next installment only | D-07 | unit | past installments keep prior amount; stacked changes |
| Whole-holding total-units reconcile overwrite | D-05 | unit | prior estimates retained as history (11.1-05 ReconcileTests) |
| `isActive` toggle stops future accrual | D-04 | unit | no accrual past toggle-off |
| SchemaV8 additive migration (V7→V8) | D-08 | integration | open store, query, save Note/Asset/SIP/Contribution without crash (STAB-08) |

---

## Wave 0 Requirements

- [x] Test suite/target scaffolding for SIP accrual + NAV-history parsing (pure-function units, no UI). (11.1-01 Task 4)
- [x] Sample npsnav.in historical, mfapi.in historical, and NAVAll.txt fixtures committed for offline deterministic parser tests. (11.1-01 Task 4)

*Pure model/service math (accrual, split, date resolution, parsing) is unit-testable without the simulator; isolate it from UI for fast feedback.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Daily NPS NAV refresh on `scenePhase .active` | D-02 / ASSET-03 | depends on real app lifecycle + network | Launch on iPhone 17 sim, confirm NPS holdings show refreshed NAV + staleness badge; airplane-mode confirms silent-fail + cached value |
| Monthly reconcile reminder fires + opens confirm screen | D-06 | local notification delivery | Schedule near-term reminder, confirm notification + deep-link to reconcile screen |
| NPS scheme picker search (PFM + asset class) | D-01 | interactive UI | Search and link E/C/G scheme codes on a holding |

---

## Validation Sign-Off

- [x] All accrual/parse/split/date model logic has automated unit verification
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers fixture + test-target setup
- [x] No watch-mode flags
- [x] Feedback latency < 120s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** approved
