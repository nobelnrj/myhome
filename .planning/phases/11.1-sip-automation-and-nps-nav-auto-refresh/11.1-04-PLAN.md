---
phase: 11.1-sip-automation-and-nps-nav-auto-refresh
plan: 04
type: execute
wave: 3
depends_on:
  - 11.1-02
  - 11.1-03
files_modified:
  - MyHomeApp/RootView.swift
  - MyHomeApp/Features/Assets/EditAssetView.swift
  - MyHomeApp/Features/Assets/SIPSetupView.swift
  - MyHomeTests/SIPSetupValidationTests.swift
autonomous: true
requirements:
  - D-01
  - D-04
  - D-07
  - ASSET-03
  - ASSET-09
must_haves:
  truths:
    - "On app foreground, NPS NAV refreshes and SIP accrual runs alongside the existing AMFI/net-worth chain"
    - "An NPS holding can be linked to a scheme code via the picker from EditAssetView"
    - "A user can configure a SIP on a holding (amount, day-of-month, isActive, and NPS allocation % for NPS)"
    - "An NPS SIP save is rejected unless allocation %s sum to 100"
    - "Toggling a SIP isActive off stops future accrual and cancels its reconcile reminder"
  artifacts:
    - path: "MyHomeApp/Features/Assets/SIPSetupView.swift"
      provides: "SIP definition form: amount, dayOfMonth, isActive, NPS allocation E/C/G + strategy-holding links; create/edit + save"
      contains: "struct SIPSetupView"
    - path: "MyHomeApp/Features/Assets/EditAssetView.swift"
      provides: "NPS Scheme section (npsSchemeCode via NPSSchemePickerView) + SIP entry NavigationLink"
      contains: "npsSchemeCode"
    - path: "MyHomeApp/RootView.swift"
      provides: "npsNavService + sipAccrualService @State, modelContext injection, scenePhase .active calls, environment injection"
      contains: "sipAccrualService"
  key_links:
    - from: "MyHomeApp/RootView.swift"
      to: "NPSNavService + SIPAccrualService"
      via: "scenePhase .active: refreshIfNeeded() + accrueIfNeeded()"
      pattern: "npsNavService.refreshIfNeeded|sipAccrualService.accrueIfNeeded"
    - from: "MyHomeApp/Features/Assets/EditAssetView.swift"
      to: "NPSSchemePickerView"
      via: "NavigationLink bound to $npsSchemeCode for nps class"
      pattern: "NPSSchemePickerView"
    - from: "MyHomeApp/Features/Assets/SIPSetupView.swift"
      to: "SIP + SIPAmountChange"
      via: "explicit context.save() on SIP create/edit; amount change appends SIPAmountChange"
      pattern: "SIPAmountChange"
---

<objective>
Wire the two new services into the app lifecycle and give the user the entry points: RootView gains npsNavService + sipAccrualService (@State, modelContext, scenePhase .active calls, environment injection mirroring amfiNavService); EditAssetView gains an NPS Scheme section (picker -> npsSchemeCode) and a SIP entry; and SIPSetupView lets the user define/edit a SIP (amount, day-of-month, isActive, and for NPS the E/C/G allocation % + strategy-holding links) with validation and amount-change history.

Purpose: This connects the engine (11.1-02) and NPS service (11.1-03) to real user actions and the foreground chain. Without it, accrual and NPS refresh never run and SIPs can't be created.
Output: modified RootView.swift + EditAssetView.swift, new SIPSetupView.swift, SIPSetupValidationTests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11.1-sip-automation-and-nps-nav-auto-refresh/11.1-CONTEXT.md
@.planning/phases/11.1-sip-automation-and-nps-nav-auto-refresh/11.1-RESEARCH.md
@.planning/phases/11.1-sip-automation-and-nps-nav-auto-refresh/11.1-PATTERNS.md
@MyHomeApp/Features/Assets/EditAssetView.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire NPSNavService + SIPAccrualService into RootView scenePhase chain</name>
  <files>MyHomeApp/RootView.swift</files>
  <read_first>
    - MyHomeApp/RootView.swift (lines 41-49 service @State; ~95-101 modelContext injection; ~123-130 scenePhase .active chain; ~140 environment injection — the exact amfiNavService pattern to mirror)
    - .planning/phases/11.1-sip-automation-and-nps-nav-auto-refresh/11.1-PATTERNS.md (RootView.swift section: all four injection points spelled out)
    - MyHomeApp/Support/NPSNavService.swift, MyHomeApp/Support/SIPAccrualService.swift (the types being wired)
  </read_first>
  <action>
    Mirror the existing amfiNavService wiring in all four RootView sites (PATTERNS RootView section):
    1. `@State private var npsNavService = NPSNavService()` and `@State private var sipAccrualService = SIPAccrualService()` after the existing amfiNavService/netWorthSnapshotService declarations.
    2. In onAppear, `npsNavService.modelContext = modelContext` and `sipAccrualService.modelContext = modelContext` after the existing assignments.
    3. In `onChange(of: scenePhase)` `.active` branch, add `npsNavService.refreshIfNeeded()` then `sipAccrualService.accrueIfNeeded()` after `amfiNavService.refreshIfNeeded()`, and place BOTH strictly BEFORE the existing `netWorthSnapshotService.upsertIfNeeded()` call. Ordering is mandatory (D-02): `sipAccrualService.accrueIfNeeded()` MUST appear on a source line ABOVE `netWorthSnapshotService.upsertIfNeeded()` so the same-cycle net-worth snapshot includes the just-accrued units. Final order in the `.active` block: routineResetService.resetIfNeeded() -> amfiNavService.refreshIfNeeded() -> npsNavService.refreshIfNeeded() -> sipAccrualService.accrueIfNeeded() -> netWorthSnapshotService.upsertIfNeeded().
    4. Add `.environment(npsNavService)` next to `.environment(amfiNavService)` (NPSSchemePickerView and EditAssetView read it from the environment). SIPAccrualService does not need environment injection (only RootView drives it).
    Do not change any existing service ordering or remove anything.
  </action>
  <verify>
    <automated>cd "/Users/reo/My Projects/my-home" && grep -q "npsNavService.refreshIfNeeded" MyHomeApp/RootView.swift && grep -q "sipAccrualService.accrueIfNeeded" MyHomeApp/RootView.swift && grep -q ".environment(npsNavService)" MyHomeApp/RootView.swift && test "$(grep -n 'sipAccrualService.accrueIfNeeded' MyHomeApp/RootView.swift | head -1 | cut -d: -f1)" -lt "$(grep -n 'netWorthSnapshotService.upsertIfNeeded' MyHomeApp/RootView.swift | head -1 | cut -d: -f1)" && xcodebuild build -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' 2>&1 | tail -5 | grep -q "BUILD SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - RootView declares `npsNavService` and `sipAccrualService` as `@State` and assigns each a `modelContext`
    - scenePhase `.active` calls `npsNavService.refreshIfNeeded()` and `sipAccrualService.accrueIfNeeded()`; the `sipAccrualService.accrueIfNeeded()` line is strictly ABOVE `netWorthSnapshotService.upsertIfNeeded()` (asserted by the line-number grep in <verify>)
    - `.environment(npsNavService)` present
    - `xcodebuild build ...` reports BUILD SUCCEEDED
  </acceptance_criteria>
  <done>Both new services initialize, receive the modelContext, run on every foreground in the right order, and NPSNavService is available via the environment.</done>
</task>

<task type="auto">
  <name>Task 2: EditAssetView — NPS Scheme section + SIP entry</name>
  <files>MyHomeApp/Features/Assets/EditAssetView.swift</files>
  <read_first>
    - MyHomeApp/Features/Assets/EditAssetView.swift (ENTIRE file: @Environment(AMFINavService.self) line 18; Section 2 MF Scheme conditional ~80-105; saveAsset target assignments ~249-256; @State form fields ~22-28)
    - .planning/phases/11.1-sip-automation-and-nps-nav-auto-refresh/11.1-PATTERNS.md (EditAssetView section: NPS Scheme section mirrors Section 2; SIP section edit-mode-only; saveAsset npsSchemeCode assignment)
    - MyHomeApp/Features/Assets/NPSSchemePickerView.swift (the destination), MyHomeApp/Features/Assets/SIPSetupView.swift (Task 3 — link target)
  </read_first>
  <action>
    Add `@Environment(NPSNavService.self) private var npsNavService` (mirrors the AMFINavService environment read) and `@State private var npsSchemeCode: String? = nil`. In edit mode, initialize npsSchemeCode from the asset.
    Add an NPS Scheme Section conditional on `assetClassRaw == "nps"`, mirroring the existing MF Scheme Section 2: a NavigationLink to `NPSSchemePickerView(selectedSchemeCode: $npsSchemeCode, npsNavService: npsNavService)` showing the selected code or "Tap to choose a scheme" (plain Text — T-11-10; `.frame(minHeight: 44)`).
    Add a SIP Section shown only when editing an existing asset (`asset != nil`, since a SIP needs a persisted Asset.id): a NavigationLink "Configure SIP" to `SIPSetupView(asset: asset!)`.
    In saveAsset, mirror the existing amfiSchemeCode assignment: `target.npsSchemeCode = assetClassRaw == "nps" ? npsSchemeCode : nil` (clears the code if the class is switched away from nps).
  </action>
  <verify>
    <automated>cd "/Users/reo/My Projects/my-home" && grep -q "NPSSchemePickerView" MyHomeApp/Features/Assets/EditAssetView.swift && grep -q "npsSchemeCode = assetClassRaw == \"nps\"" MyHomeApp/Features/Assets/EditAssetView.swift && grep -q "SIPSetupView" MyHomeApp/Features/Assets/EditAssetView.swift && xcodebuild build -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' 2>&1 | tail -5 | grep -q "BUILD SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - EditAssetView reads `@Environment(NPSNavService.self)` and has `@State ... npsSchemeCode`
    - An `assetClassRaw == "nps"` section links to `NPSSchemePickerView` bound to `$npsSchemeCode`
    - saveAsset sets `target.npsSchemeCode` to the code for nps class and nil otherwise
    - A `Configure SIP` NavigationLink to `SIPSetupView` appears in edit mode
    - `xcodebuild build ...` reports BUILD SUCCEEDED
  </acceptance_criteria>
  <done>EditAssetView links NPS holdings to a scheme code via the picker and persists npsSchemeCode, and exposes SIP setup for existing holdings.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: SIPSetupView — SIP create/edit form with validation + amount-change history</name>
  <files>MyHomeApp/Features/Assets/SIPSetupView.swift, MyHomeTests/SIPSetupValidationTests.swift</files>
  <read_first>
    - MyHomeApp/Features/Assets/EditAssetView.swift (save pattern saveAsset ~222-265; isValid ~45-50; toolbar Cancel/Save ~178-190; @Query/@State usage)
    - .planning/phases/11.1-sip-automation-and-nps-nav-auto-refresh/11.1-PATTERNS.md (SIPSetupView section: struct decl, isValid sum-to-100, saveSIP target pattern, toolbar verbatim, 44pt)
    - MyHomeApp/Persistence/Schema/SchemaV8.swift (SIP + SIPAmountChange field shapes — from 11.1-01)
    - .planning/phases/11.1-sip-automation-and-nps-nav-auto-refresh/11.1-CONTEXT.md (D-01 allocation sum-to-100, D-04 isActive, D-07 next-installment-only amount change)
  </read_first>
  <behavior>
    - isValid is true only when amount > 0 and dayOfMonth in 1...31 and (asset is not nps OR npsAllocationE+C+G == 100)
    - Saving a new SIP inserts a SIP with assetID = asset.id and an initial SIPAmountChange(effectiveFrom: startDate, amount:)
    - Editing an existing SIP's amount appends a NEW SIPAmountChange with effectiveFrom = `SIPSetupView.nextInstallmentDate(dayOfMonth:after:calendar:)` (the next installment strictly after today, D-07), leaving prior changes intact
    - `nextInstallmentDate(dayOfMonth:after:calendar:)` is a static pure func: given a dayOfMonth and a reference date, it returns the first installment date on dayOfMonth that is strictly after the reference, clamping dayOfMonth to month length (e.g. 31 -> Feb 28/29) using an IST `.gregorian` calendar (analogous to `elapsedInstallmentDates` in 11.1-02)
    - Toggling isActive off on save flags the SIP inactive (the engine then accrues nothing; reminder cancellation handled by the engine on next pass)
  </behavior>
  <action>
    Create `MyHomeApp/Features/Assets/SIPSetupView.swift` mirroring EditAssetView's NavigationStack-in-sheet + Form + validation + save (PATTERNS SIPSetupView section). Take `var asset: Asset`; `@Environment(\.modelContext)`, `@Environment(\.dismiss)`. Load the existing active SIP for this asset via a predicate `@Query` filtered by assetID (or fetch in init) — edit if present, else create.
    Form fields: amount (Decimal), dayOfMonth (1...31 stepper/picker), isActive (Toggle — D-04). For `asset.assetClassRaw == "nps"`: three allocation % fields E/C/G (Int) plus three strategy-holding pickers (npsAssetE/C/G choosing among the user's nps Assets) — D-01. Show a live "must total 100%" hint.
    `isValid` per the behavior block. `saveSIP()` mirrors saveAsset: create-or-fetch SIP, set assetID/dayOfMonth/amount/isActive/startDate and (for nps) npsAllocationE/C/G + npsAssetE/C/G; on CREATE append an initial SIPAmountChange(effectiveFrom: startDate, amount:); on EDIT where amount changed append a SIPAmountChange(effectiveFrom: next installment date computed from dayOfMonth, amount:) — never rewrite past changes (D-07). Keep SIP.amount in sync with the latest change for convenience. `try context.save()` explicit (CR-01); `assertionFailure` on catch (mirror EditAssetView). Toolbar Cancel/Save verbatim; `.frame(minHeight: 44)` on interactive rows.
    Extract the next-installment computation into a static pure func `static func nextInstallmentDate(dayOfMonth: Int, after reference: Date, calendar: Calendar) -> Date` (IST `.gregorian` calendar, clamp dayOfMonth to month length, return the first matching day strictly after `reference`) — analogous to `elapsedInstallmentDates` in 11.1-02; the EDIT amount-change path sets `effectiveFrom` from this func. Add SIPSetupValidationTests: extract the sum-to-100 + amount>0 + dayOfMonth-range validation into a static pure func and test it (valid E50/C30/G20; reject E50/C30/G10; reject amount 0; reject dayOfMonth 0 and 32; MF SIP valid regardless of allocation). Also test `nextInstallmentDate` DIRECTLY: for a reference of e.g. 2026-06-12 with dayOfMonth 5 it returns 2026-07-05; with dayOfMonth 20 it returns 2026-06-20; with dayOfMonth 31 in a Feb reference it clamps to Feb 28/29.
  </action>
  <verify>
    <automated>cd "/Users/reo/My Projects/my-home" && grep -q "struct SIPSetupView" MyHomeApp/Features/Assets/SIPSetupView.swift && grep -q "SIPAmountChange" MyHomeApp/Features/Assets/SIPSetupView.swift && grep -q "func nextInstallmentDate" MyHomeApp/Features/Assets/SIPSetupView.swift && xcodebuild test -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:MyHomeTests/SIPSetupValidationTests 2>&1 | tail -15 | grep -q "TEST SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - `SIPSetupView.swift` contains `struct SIPSetupView: View`, a `Toggle` for isActive, and appends `SIPAmountChange`
    - A test asserts allocation E50/C30/G20 validates and E50/C30/G10 is rejected for an nps asset
    - A test asserts amount 0 and dayOfMonth 0/32 are rejected; an MF SIP validates regardless of allocation
    - A test calls `SIPSetupView.nextInstallmentDate(dayOfMonth:after:calendar:)` directly and asserts the next strictly-after date (incl. Feb clamp); editing amount appends a new SIPAmountChange with effectiveFrom from that helper
    - `-only-testing:MyHomeTests/SIPSetupValidationTests` reports TEST SUCCEEDED
  </acceptance_criteria>
  <done>Users can create/edit a SIP with validated amount/day/allocation, isActive toggle, and next-installment-only amount-change history, with validation unit-tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → SIPSetupView | User-entered amount, dayOfMonth, allocation % cross into persisted SIP definitions that drive money math |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-114-01 | Tampering | SIP amount/day/allocation out of range | mitigate | isValid + tested static validator: amount>0, dayOfMonth 1...31, nps allocation sums to 100, each >=0 (Save disabled otherwise) |
| T-114-02 | Tampering | Past estimates rewritten on amount change | mitigate | Amount change appends a NEW SIPAmountChange effective next-installment; prior records never mutated (D-07; test) |
| T-114-03 | Repudiation | Markdown/HTML in displayed scheme code/name | mitigate | All scheme code/name display via plain `Text()` (T-11-10) |
| T-111-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase (zero new SPM deps) |
</threat_model>

<verification>
- `xcodebuild build -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17'` succeeds.
- `xcodebuild test ... -only-testing:MyHomeTests/SIPSetupValidationTests` green.
- Manual (post-merge, human): on iPhone 17 sim, foreground app -> NPS NAV updates with staleness badge; link an NPS holding via picker; configure a SIP and confirm allocation-sum validation blocks Save.
</verification>

<success_criteria>
- NPS NAV refresh + SIP accrual run on foreground in the correct order.
- NPS holdings link to scheme codes; SIPs configurable with validated allocation (D-01), isActive (D-04), and amount-change history (D-07).
</success_criteria>

<output>
Create `.planning/phases/11.1-sip-automation-and-nps-nav-auto-refresh/11.1-04-SUMMARY.md` when done.
</output>
