---
phase: 19-auto-sync-sync-ux-multipeer-bootstrap
plan: 05
type: execute
wave: 5
depends_on: ["19-02", "19-03", "19-04"]
files_modified:
  - .planning/phases/19-auto-sync-sync-ux-multipeer-bootstrap/screens/
autonomous: false
requirements: [SYNC-04, SYNC-05]

must_haves:
  truths:
    - "Full test suite green in one run: transport, coordinator, presentation, bootstrap, AND DarkBitIdentityTests (dark byte-identity survived the whole phase)"
    - "Regression gates hold: encrypted MCSession (.required), both Bonjour entries + usage description present, Google OAuth scheme/BGTask id/UIBackgroundModes untouched"
    - "Simulator review set exists: Sync surface + bootstrap sheet in light AND dark, self-reviewed against the neumorphic bar"
    - "Two-device sign-off: change on A appears on B automatically; Sync now works; a fresh install bootstraps a full copy; last-synced/status UI reads truthfully"
  artifacts:
    - path: ".planning/phases/19-auto-sync-sync-ux-multipeer-bootstrap/screens/"
      provides: "Light+dark review screenshots of the Sync surface and bootstrap sheet"
  key_links:
    - from: "phase gate"
      to: "ROADMAP Phase 19 success criteria 1-5"
      via: "each criterion mapped to an automated gate or a numbered human-check step"
      pattern: "SYNC-0[45]"
---

<objective>
Phase gate for Phase 19: one automated regression sweep proving every plan's gates
still hold together (tests, plist, encryption, OAuth/BGTask byte-identity, dark
tokens), a simulator screenshot review set of the new sync UI, and the single
end-of-phase human verification on two physical phones — the only part of SYNC-04/05
that genuinely needs hardware (real MC discovery over home Wi-Fi).

Purpose: ROADMAP success criteria 1–5 signed off as TRUE, not assumed from unit
tests.

Output: green sweep, review screenshots committed, human approval recorded in the
SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-auto-sync-sync-ux-multipeer-bootstrap/19-02-SUMMARY.md
@.planning/phases/19-auto-sync-sync-ux-multipeer-bootstrap/19-03-SUMMARY.md
@.planning/phases/19-auto-sync-sync-ux-multipeer-bootstrap/19-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated phase sweep + simulator review set, then end-of-phase human sign-off</name>
  <read_first>
    - .planning/phases/19-auto-sync-sync-ux-multipeer-bootstrap/19-01-PLAN.md (must_haves — the gates being re-proven)
    - MyHomeApp/Info.plist (final state after Plans 01–04)
  </read_first>
  <action>
    1. Full suite, single run: `xcodebuild test -scheme MyHome -destination
       'platform=iOS Simulator,name=iPhone 17'` — zero failures. Explicitly confirm
       SyncTransportTests, SyncCoordinatorTests, SyncStatusPresentationTests,
       BootstrapAdvisorTests, and DarkBitIdentityTests all executed and passed
       (list them from the log in the SUMMARY).
    2. Regression grep/plist gates (code-filtered — never bare grep -c on files with
       comment prose):
       - `grep -v '^\s*//' MyHomeApp/Sync/MultipeerSyncTransport.swift | grep -c 'encryptionPreference: .required'` ≥ 1
       - PlistBuddy prints NSLocalNetworkUsageDescription and both `_myhome-sync._tcp`
         and `_myhome-sync._udp` under NSBonjourServices
       - `grep -c 'com.googleusercontent.apps' MyHomeApp/Info.plist` == 1;
         BGTaskSchedulerPermittedIdentifiers still contains
         com.reojacob.myhome.emailrefresh; UIBackgroundModes unchanged
       - Coordinator layering: `grep -v '^\s*//' MyHomeApp/Sync/SyncCoordinator.swift
         | grep -c 'import MultipeerConnectivity'` == 0 (seam intact)
    3. Simulator review set → `.planning/phases/19-auto-sync-sync-ux-multipeer-bootstrap/screens/`:
       build, install, launch with `-seedSampleData -startTab 4`; screenshot the
       Sync screen light+dark (sync-light.png / sync-dark.png). Then relaunch on an
       ERASED store WITHOUT -seedSampleData to capture the bootstrap sheet
       (bootstrap-light.png / bootstrap-dark.png). Self-review each against the
       neumorphic bar (soft-white matte light, neon-window dark) and fix-and-recapture
       obvious misses (UI files only — tokens untouchable).
    4. Note in the SUMMARY that simulator MC discovery is unreliable
       (simulator-to-simulator Bonjour is not a supported MC path) — the human check
       below is the authoritative SYNC-04 proof; the simulator loop only reviews UI.
  </action>
  <verify>
    <automated>cd "/Users/reo/My Projects/my-home" && ls .planning/phases/19-auto-sync-sync-ux-multipeer-bootstrap/screens/ | grep -c '\.png' && xcodebuild test -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' -quiet 2>&1 | tail -5</automated>
    <human-check>
      End-of-phase two-phone verification (both phones on home Wi-Fi, app installed
      on A with real data; B available for a fresh-install pass):
      1. AUTO-SYNC (criterion 1): open MyHome on both phones, foregrounded. Sync
         screen on each shows "Connected to <other phone's name>". Add an expense
         on A → within ~10s it appears on B with correct amount/category, no taps.
         Edit a note on B → text updates on A.
      2. SYNC NOW (criterion 2): background the app on B, reopen it, go to
         Settings → Sync → tap "Sync Now" → status cycles connecting/syncing and
         last-synced updates to "Just now" on both.
      3. BOOTSTRAP (criterion 3): delete the app on B, reinstall, launch — the
         "Set up from your other phone" sheet appears; with A open, it connects,
         copies, and shows "Done — N added…". B now shows A's expenses, notes,
         accounts, and assets; spot-check one Decimal amount digit-for-digit.
      4. SURFACE (criterion 4): Sync screen on both phones shows a truthful
         last-synced relative time, current status, connected peer name, and last
         merge summary; Settings row shows the glanceable last-synced text.
      5. NO SILENT LOSS (criterion 5): with B temporarily offline (Wi-Fi off),
         edit the SAME note on both phones (B edited LAST); rejoin Wi-Fi, let it
         sync → B's newer text wins on both; nothing else disappeared.
      6. LIFECYCLE: background the app on A → B's Sync screen drops to "Looking
         for your other phone…" shortly after; foreground A → reconnects by itself.
      7. THEME: flip Appearance Light/Dark on the Sync screen — neumorphic in both;
         dark looks exactly like the rest of the dark app.
      Reply "approved" or describe issues (which step, which phone, what you saw).
    </human-check>
  </verify>
  <acceptance_criteria>
    - Full suite green in a single run; all four new sync test suites + DarkBitIdentityTests listed as executed
    - All regression gates from step 2 pass
    - screens/ contains ≥4 png files (sync + bootstrap, light + dark), self-reviewed
    - Human sign-off received for steps 1–7 (or issues fed back into gap closure)
  </acceptance_criteria>
  <done>Phase 19 success criteria 1–5 verified end-to-end on real hardware; evidence committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Verification-only plan; re-proves the mitigations shipped in Plans 01–04 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-14 | Regression | phase-wide gates | mitigate | Single-run sweep re-asserts .required encryption, plist integrity, OAuth/BGTask byte-identity, seam layering, and dark token identity before sign-off |
</threat_model>

<verification>
- Automated sweep green; screenshots present; human check approved (the two-device steps are the authoritative SYNC-04/05 evidence).
</verification>

<success_criteria>
- All five ROADMAP Phase 19 success criteria observed TRUE on hardware; SYNC-04 and SYNC-05 closable.
</success_criteria>

<output>
Create `.planning/phases/19-auto-sync-sync-ux-multipeer-bootstrap/19-05-SUMMARY.md` when done.
</output>
