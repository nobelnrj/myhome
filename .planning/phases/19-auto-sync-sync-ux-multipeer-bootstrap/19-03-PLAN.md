---
phase: 19-auto-sync-sync-ux-multipeer-bootstrap
plan: 03
type: execute
wave: 3
depends_on: ["19-02"]
files_modified:
  - MyHomeApp/Features/Settings/SyncStatusView.swift
  - MyHomeApp/Features/Settings/SettingsView.swift
  - MyHomeTests/SyncStatusPresentationTests.swift
  - MyHome.xcodeproj/project.pbxproj
autonomous: true
requirements: [SYNC-05]

must_haves:
  truths:
    - "Settings has a Sync entry whose destination shows current status (Idle / Looking for your other phone / Syncing / error message) and the connected peer's display name"
    - "Last-synced time is shown as a relative string ('2 minutes ago'); before any sync it reads 'Never synced'"
    - "A 'Sync Now' button triggers SyncCoordinator.syncNow() and the status line visibly changes (connecting/syncing feedback)"
    - "Last merge results (added/updated/removed counts) are visible after a sync — the surface is trustworthy, not a black box"
    - "Surface is styled with DesignTokens + NeuSurface in light AND dark; dark rendering stays byte-identical (DarkBitIdentityTests green — zero token definitions touched)"
  artifacts:
    - path: "MyHomeApp/Features/Settings/SyncStatusView.swift"
      provides: "Neumorphic sync surface + pure SyncStatusPresentation mapping (status → label/token/symbol, date → relative text)"
      contains: "SyncStatusPresentation"
    - path: "MyHomeTests/SyncStatusPresentationTests.swift"
      provides: "Unit tests for the pure presentation mapping (all SyncStatus cases + nil/near/old dates)"
      contains: "SyncStatusPresentationTests"
  key_links:
    - from: "MyHomeApp/Features/Settings/SyncStatusView.swift"
      to: "SyncCoordinator"
      via: "@Environment(SyncCoordinator.self) — reads coordinator.statusStore, calls syncNow()"
      pattern: "Environment\\(SyncCoordinator.self\\)"
    - from: "MyHomeApp/Features/Settings/SettingsView.swift"
      to: "SyncStatusView"
      via: "NavigationLink row in the Data section"
      pattern: "SyncStatusView"
---

<objective>
Ship the sync surface (SYNC-05 UI half): a neumorphic status screen reachable from
Settings showing current sync status, connected peer name, relative last-synced time,
last merge stats, and a "Sync Now" affordance — driven entirely by the Plan-02
SyncStatusStore. All display logic lives in a pure, unit-tested
`SyncStatusPresentation` mapper; the view is thin.

Purpose: ROADMAP criterion 4 — "the sync surface shows last-synced time and current
status with a clear affordance" — and the visibility mitigation promised for T-19-02
(unexpected peer names are user-visible).

Output: SyncStatusView live in Settings, presentation tests green, dark byte-identity
preserved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-auto-sync-sync-ux-multipeer-bootstrap/19-02-SUMMARY.md
@MyHomeApp/Sync/SyncStatusStore.swift
@MyHomeApp/DesignSystem/DesignTokens.swift
@MyHomeApp/DesignSystem/NeuSurface.swift
@MyHomeApp/Features/Settings/SettingsView.swift
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SyncStatusPresentation — pure status/date mapping + tests</name>
  <read_first>
    - MyHomeApp/Sync/SyncStatusStore.swift (SyncStatus cases + lastSyncedAt/lastMergeStats — the exact inputs)
    - MyHomeApp/DesignSystem/DesignTokens.swift (semantic tokens: positive/negative/orange/label2 — reference by NAME only, never redefine or edit any token)
  </read_first>
  <behavior>
    - label(for: .idle) == "Up to date" when lastSyncedAt != nil, "Not synced yet" when nil; .connecting → "Looking for your other phone…"; .syncing → "Syncing…"; .error(m) → m (verbatim, already human-readable from Plan 02)
    - symbol/token mapping: .idle → checkmark + DesignTokens.positive; .connecting → dotradiowaves symbol + label2; .syncing → arrow-triangle-2 + accentText; .error → exclamation + negative (assert the mapping function returns the expected token identity, not raw colors)
    - relativeLastSynced(nil) == "Never synced"; a date 90 seconds ago produces a string containing "minute"; a date < 60s ago produces "Just now" (explicit branch — RelativeDateTimeFormatter's "in 0 seconds" weirdness avoided)
    - mergeSummary(MergeStats(inserted:3, updated:2, deleted:1, skipped:4, adopted:0)) == "3 added · 2 updated · 1 removed"; all-zero stats → "Nothing new — already in sync"
  </behavior>
  <action>
    Create `MyHomeApp/Features/Settings/SyncStatusView.swift` starting with the pure
    part (the view body is Task 2 — a placeholder `EmptyView` body is fine for this
    task if committed separately, but both tasks may land in one file edit):

    `enum SyncStatusPresentation` (no SwiftUI state, fully deterministic, all inputs
    passed in — testable without rendering):
    - `static func label(for status: SyncStatus, lastSyncedAt: Date?) -> String`
    - `static func systemImage(for status: SyncStatus) -> String`
    - `static func tint(for status: SyncStatus) -> Color` (returns EXISTING
      DesignTokens members — this file defines zero colors)
    - `static func relativeLastSynced(_ date: Date?, now: Date = Date()) -> String`
      (nil → "Never synced"; <60s → "Just now"; else RelativeDateTimeFormatter
      with .named style, en locale inherited)
    - `static func mergeSummary(_ stats: MergeStats?) -> String?` (nil → nil; zero
      activity → "Nothing new — already in sync"; else "N added · N updated ·
      N removed", omitting zero components, always ≥1 component)

    Create `MyHomeTests/SyncStatusPresentationTests.swift` (Swift Testing) covering
    every behavior bullet.

    Register SyncStatusView.swift (app target) and SyncStatusPresentationTests.swift
    (test target) in project.pbxproj — 4 edits each (PBXBuildFile, PBXFileReference,
    PBXGroup children under Features/Settings and MyHomeTests, correct
    PBXSourcesBuildPhase).
  </action>
  <verify>
    <automated>cd "/Users/reo/My Projects/my-home" && xcodebuild test -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:MyHomeTests/SyncStatusPresentationTests -quiet 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - All SyncStatusPresentationTests PASS
    - `grep -v '^\s*//' MyHomeApp/Features/Settings/SyncStatusView.swift | grep -c 'Color.adaptive\|Color(hex'` returns 0 (no new color definitions — tokens only)
  </acceptance_criteria>
  <done>Every string/symbol/tint the surface shows is a pure tested function of SyncStatusStore state.</done>
</task>

<task type="auto">
  <name>Task 2: SyncStatusView neumorphic surface + Settings entry row</name>
  <read_first>
    - MyHomeApp/DesignSystem/NeuSurface.swift (`.neuSurface(.raised)` API + states — the styling primitive)
    - MyHomeApp/Features/Settings/SettingsView.swift (Data Section ~line 190: AccountsListView/AssetsListView NavigationLink row pattern to copy; environment access idiom)
    - MyHomeApp/RootView.swift (how @Observable services are consumed via @Environment)
  </read_first>
  <action>
    In `MyHomeApp/Features/Settings/SyncStatusView.swift`, build the view:

    `struct SyncStatusView: View` with `@Environment(SyncCoordinator.self) private
    var coordinator`. Read `coordinator.statusStore` (all fields are @Observable —
    live updates for free). Layout (ScrollView on DesignTokens.bgCanvas, matching
    the app's neumorphic screens):

    1. **Status card** (`.neuSurface(.raised)`): status symbol in the presentation
       tint + label(for:lastSyncedAt:), the connected peer name beneath it when
       non-nil ("Connected to <name>" — the T-19-02 visibility mitigation; show
       "No phone nearby" in idle/connecting with no peer), and the relative
       last-synced line (`relativeLastSynced`) in DesignTokens.label2. Add a
       `TimelineView(.periodic(from: .now, by: 30))` (or a 30s timer) around the
       relative-time text so "Just now" ages without user interaction.
    2. **Last sync results row**: `mergeSummary(statusStore.lastMergeStats)` when
       non-nil, small type, label2.
    3. **"Sync Now" button**: prominent, accent-styled like existing primary actions
       (accent fill + accentOnYellow text), `isInteractive: true` neuSurface press
       feedback, calls `coordinator.syncNow()`. Disabled (label3) while status ==
       .syncing.
    4. Footer caption (label3): "Syncing works when both phones have MyHome open on
       the same Wi-Fi." — sets the foreground-only expectation; if status is
       .error and the message mentions Local Network, the caption appends the
       Settings → Privacy → Local Network hint.

    Navigation title "Sync". Uses ONLY existing DesignTokens members and NeuSurface —
    zero token/dark-branch edits (DarkBitIdentityTests is the tripwire).

    In `MyHomeApp/Features/Settings/SettingsView.swift`, Data section: add a
    `NavigationLink(destination: SyncStatusView())` row ABOVE the export/import
    snapshot rows, styled like the Accounts/Assets rows (icon
    `arrow.triangle.2.circlepath`, title "Sync"), with a trailing secondary text
    showing `SyncStatusPresentation.relativeLastSynced(...)` so last-synced is
    glanceable without drilling in. SettingsView gains `@Environment(SyncCoordinator.self)`.

    Self-verify visually (repo loop): build, install on iPhone 17 simulator, launch
    with `-seedSampleData -startTab 4`, screenshot Settings + the Sync screen in
    BOTH light and dark; confirm neumorphic styling, legible tokens, no clipped
    layout. Fix and recapture if off.
  </action>
  <verify>
    <automated>cd "/Users/reo/My Projects/my-home" && grep -c 'SyncStatusView' MyHomeApp/Features/Settings/SettingsView.swift && xcodebuild test -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' -quiet 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - Settings → Sync row present with glanceable last-synced text; destination renders status card, peer name slot, merge summary, Sync Now button, footer
    - Sync Now disabled while syncing; button calls coordinator.syncNow()
    - Light + dark screenshots captured and self-reviewed
    - FULL suite green including DarkBitIdentityTests (no token drift)
  </acceptance_criteria>
  <done>The trustworthy sync surface exists: status, peer, last-synced, results, and a clear always-works affordance.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Read-only presentation over SyncStatusStore; syncNow() is an existing Plan-02 entry point |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-09 | Spoofing (visibility) | connected peer display | mitigate | Surfaces the connected peer's display name prominently — the compensating control promised in T-19-02/T-19-08 for the unauthenticated household link |
| T-19-10 | Information Disclosure | sync surface contents | accept | Shows only status, peer name, timestamps, and counts — no record data; screen sits behind the existing Face ID gate like the rest of Settings |
| T-19-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs — SwiftUI/Foundation only. |
</threat_model>

<verification>
- SyncStatusPresentationTests green; full suite green including DarkBitIdentityTests.
- Light + dark simulator screenshots of Settings and the Sync screen captured and reviewed.
- No new Color definitions; only existing DesignTokens referenced.
</verification>

<success_criteria>
- SYNC-05 (surface half) + ROADMAP criterion 4: last-synced time, live status, peer visibility, merge results, and a manual "Sync Now" affordance — neumorphic in both themes with dark byte-identity intact.
</success_criteria>

<output>
Create `.planning/phases/19-auto-sync-sync-ux-multipeer-bootstrap/19-03-SUMMARY.md` when done.
</output>
