---
phase: 19-auto-sync-sync-ux-multipeer-bootstrap
plan: 04
type: execute
wave: 4
depends_on: ["19-02", "19-03"]
files_modified:
  - MyHomeApp/Sync/BootstrapAdvisor.swift
  - MyHomeApp/Features/Settings/SyncBootstrapView.swift
  - MyHomeApp/Features/Settings/SyncStatusView.swift
  - MyHomeApp/RootView.swift
  - MyHomeTests/BootstrapAdvisorTests.swift
  - MyHome.xcodeproj/project.pbxproj
autonomous: true
requirements: [SYNC-05]

must_haves:
  truths:
    - "A fresh (effectively empty) install is offered 'Set up from your other phone' on first launch; completing it leaves this phone holding a full copy of the other phone's data"
    - "Running bootstrap against a NON-empty store MERGES — it never wipes or clobbers; a newer local edit survives the bootstrap exchange (engine LWW, proven in a test)"
    - "The prompt appears at most until answered: 'Set up later' or completion sets a persisted flag and it never auto-appears again"
    - "Bootstrap remains reachable manually from the Sync surface for users who skipped the prompt"
    - "The bootstrap sheet shows live progress (looking → connected to <peer> → syncing → 'Imported N records from <peer>') driven by SyncStatusStore — never a dead spinner"
  artifacts:
    - path: "MyHomeApp/Sync/BootstrapAdvisor.swift"
      provides: "Pure store-emptiness check (user-entered entities only, seeded categories ignored) + shouldOfferBootstrap gate over the persisted flag"
      contains: "enum BootstrapAdvisor"
    - path: "MyHomeApp/Features/Settings/SyncBootstrapView.swift"
      provides: "Bootstrap sheet: explainer, live status states, completion summary, Set up later"
      contains: "SyncBootstrapView"
    - path: "MyHomeTests/BootstrapAdvisorTests.swift"
      provides: "Emptiness/gate tests + bootstrap-merges-not-clobbers loopback test"
      contains: "BootstrapAdvisorTests"
  key_links:
    - from: "MyHomeApp/RootView.swift"
      to: "BootstrapAdvisor.shouldOfferBootstrap"
      via: "onAppear check → .sheet presenting SyncBootstrapView on a fresh install"
      pattern: "shouldOfferBootstrap"
    - from: "MyHomeApp/Features/Settings/SyncBootstrapView.swift"
      to: "SyncCoordinator / SyncStatusStore"
      via: "@Environment coordinator — start()+syncNow() drive the exchange; statusStore.lastMergeStats signals completion"
      pattern: "lastMergeStats"
---

<objective>
Ship the SYNC-05 bootstrap flow: a first-run "Set up from your other phone" sheet
that seeds a fresh install with a full copy of the other phone's data using the
EXISTING snapshot exchange (the Plan-02 coordinator already pushes full snapshots on
connect — bootstrap is a guided window onto that, not a second sync path). A
non-empty store going through bootstrap merges via the Phase-18 engine, so local
data is never wiped or silently lost.

Purpose: ROADMAP criterion 3 — a first-time bootstrap seeds the second install —
with the never-clobber guarantee explicit and tested.

Output: BootstrapAdvisor + SyncBootstrapView, first-run presentation from RootView,
manual entry from the Sync surface, tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-auto-sync-sync-ux-multipeer-bootstrap/19-02-SUMMARY.md
@.planning/phases/19-auto-sync-sync-ux-multipeer-bootstrap/19-03-SUMMARY.md
@MyHomeApp/Sync/SyncCoordinator.swift
@MyHomeApp/Sync/SyncStatusStore.swift
@MyHomeApp/RootView.swift
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: BootstrapAdvisor — emptiness detection + offer gate + never-clobber proof</name>
  <read_first>
    - MyHomeApp/Persistence/Schema/SchemaV10.swift (entity list — which types indicate USER data vs seeded data)
    - MyHomeTests/SyncCoordinatorTests.swift (FakeSyncTransport pair + in-memory container fixtures to REUSE for the loopback test)
  </read_first>
  <behavior>
    - isStoreEffectivelyEmpty == true for a brand-new store AND for a store containing only seeded Categories (both phones seed the same 14 at first launch — categories are NOT user data)
    - isStoreEffectivelyEmpty == false if ANY Expense, Note, Account, Asset, SIP, or NetWorthSnapshot exists (one test per type is overkill — parameterize over inserts)
    - shouldOfferBootstrap == false when the UserDefaults flag "sync.bootstrapResolved" is set, regardless of emptiness; == false when store is non-empty; == true only for (empty store, flag unset)
    - Never-clobber (SYNC-05 core): store B non-empty with a note edited NEWER than A's copy; run the full coordinator exchange over a linked FakeSyncTransport pair (the bootstrap path IS this exchange) → B gains A's records it lacked AND B's newer edit text survives; B's pre-existing rows are never deleted (row counts only grow or stay equal — no tombstones in play)
  </behavior>
  <action>
    Create `MyHomeApp/Sync/BootstrapAdvisor.swift`:

    `@MainActor enum BootstrapAdvisor`:
    - `static let resolvedFlagKey = "sync.bootstrapResolved"`
    - `static func isStoreEffectivelyEmpty(context: ModelContext) -> Bool`: fetchCount
      (use `context.fetchCount(FetchDescriptor<T>())` — cheap, no row materialization)
      for Expense, Note, Account, Asset, SIP, NetWorthSnapshot; all zero → true.
      Categories, routine templates, and DeletionLog are deliberately EXCLUDED
      (seeded/derived, not user-entered).
    - `static func shouldOfferBootstrap(context: ModelContext,
      defaults: UserDefaults = .standard) -> Bool`: `!defaults.bool(forKey:
      resolvedFlagKey) && isStoreEffectivelyEmpty(context:)`.
    - `static func markResolved(defaults: UserDefaults = .standard)`.
    - DEBUG suppression hook: `static var isSuppressedByLaunchArguments: Bool` —
      true when ProcessInfo arguments contain `-seedSampleData` or
      `-suppressBootstrapPrompt` (keeps the simulator screenshot loop and future
      UI-verification launches from being blocked by the sheet). Compiled in
      DEBUG only; false in release.

    Create `MyHomeTests/BootstrapAdvisorTests.swift` (Swift Testing, @MainActor,
    `@Suite(.serialized)` — it touches the shared UserDefaults flag; inject a
    dedicated `UserDefaults(suiteName: "bootstrap-tests")` and clean it per test).
    Implement every behavior bullet; the never-clobber test reuses FakeSyncTransport
    + two coordinators + two in-memory SchemaV10 containers exactly as
    SyncCoordinatorTests does (same file's fixtures may be extracted to a shared
    test helper if needed — keep it in the test target).

    Register BootstrapAdvisor.swift (app target), SyncBootstrapView.swift (Task 2's
    file — register now, app target), and BootstrapAdvisorTests.swift (test target)
    in project.pbxproj — 4 edits each.
  </action>
  <verify>
    <automated>cd "/Users/reo/My Projects/my-home" && xcodebuild test -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:MyHomeTests/BootstrapAdvisorTests -quiet 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - All BootstrapAdvisorTests PASS, including the never-clobber loopback test
    - Emptiness check uses fetchCount and ignores Category/DeletionLog
    - No new sync path exists — grep BootstrapAdvisor.swift for `mergeData|exportData` returns 0 (advisor only advises; the coordinator syncs)
  </acceptance_criteria>
  <done>Fresh-install detection, one-shot offer gating, and the merge-not-clobber guarantee are pinned by tests.</done>
</task>

<task type="auto">
  <name>Task 2: SyncBootstrapView sheet + first-run presentation + manual entry from Sync surface</name>
  <read_first>
    - MyHomeApp/Features/Settings/SyncStatusView.swift (Plan 03 — SyncStatusPresentation helpers + neumorphic layout idiom to match; the manual-entry row lands here)
    - MyHomeApp/RootView.swift (existing @State sheet-presentation patterns, e.g. deepLinkReconcileSIPID; onAppear ordering)
  </read_first>
  <action>
    Create `MyHomeApp/Features/Settings/SyncBootstrapView.swift`:

    `struct SyncBootstrapView: View` with `@Environment(SyncCoordinator.self)`,
    `@Environment(\.dismiss)`, and an `onResolved: () -> Void` callback. Neumorphic
    sheet (bgCanvas + neuSurface cards, DesignTokens only — no token edits):

    - Header: phone/arrow imagery (SF Symbols), title "Set up from your other
      phone", body copy: "Open MyHome on your other phone and keep both on the
      same Wi-Fi. Everything — expenses, notes, accounts, assets — will be copied
      over. Anything already on this phone is kept and merged, never deleted."
      (the never-clobber promise is user-facing copy, matching actual behavior).
    - Live progress section driven by `coordinator.statusStore` via the Plan-03
      `SyncStatusPresentation` mappers: connecting → "Looking for your other
      phone…", connected → "Connected to <peer>", syncing → "Copying data…".
    - Completion state: when `statusStore.lastMergeStats` transitions from the
      value captured at sheet-appear (`onAppear` snapshot it; compare with
      `.onChange`) to a NEW value, show "Done — <mergeSummary>" + "Imported from
      <peerName>" and a prominent "Start using MyHome" button that calls
      `BootstrapAdvisor.markResolved()`, `onResolved()`, and dismisses.
    - On appear: `coordinator.start()` (idempotent) then `coordinator.syncNow()`
      — forces fresh discovery + a snapshotRequest so the other phone pushes its
      snapshot even if a connection already existed. No bespoke transport calls.
    - Error state: statusStore.status == .error shows the message + a "Try Again"
      button (calls syncNow() again).
    - "Set up later" toolbar/button: `BootstrapAdvisor.markResolved()`,
      `onResolved()`, dismiss — the prompt never nags again (manual path remains).
    - The sheet is `.interactiveDismissDisabled(false)`; treat swipe-dismiss as
      "Set up later" via `.onDisappear` ONLY if not completed — set the flag there
      too so a swipe doesn't re-trigger the prompt next launch.

    In `MyHomeApp/RootView.swift`:
    - `@State private var showBootstrapSheet = false`; in an `.onAppear` (after the
      existing setup), guard `!BootstrapAdvisor.isSuppressedByLaunchArguments` (DEBUG)
      and `BootstrapAdvisor.shouldOfferBootstrap(context: modelContext)` →
      `showBootstrapSheet = true`. Present via `.sheet(isPresented:
      $showBootstrapSheet) { SyncBootstrapView(onResolved: { showBootstrapSheet =
      false }) }` alongside the existing sheets. The Face ID lock overlay must stay
      ABOVE the sheet logic — do not reorder existing modifiers.

    In `MyHomeApp/Features/Settings/SyncStatusView.swift`:
    - Below the Sync Now button add a quiet secondary row/button "Set up from your
      other phone…" presenting the same `SyncBootstrapView` in a sheet — the manual
      entry for users who skipped (works on non-empty stores too; it merges).

    Self-verify (simulator loop): 1) erase simulator or delete app → launch WITHOUT
    -seedSampleData → bootstrap sheet appears on first run; screenshot light+dark.
    2) "Set up later" → relaunch → sheet does NOT reappear. 3) launch with
    -seedSampleData → sheet suppressed. 4) Sync screen shows the manual entry row.
  </action>
  <verify>
    <automated>cd "/Users/reo/My Projects/my-home" && grep -c 'SyncBootstrapView' MyHomeApp/RootView.swift MyHomeApp/Features/Settings/SyncStatusView.swift && xcodebuild test -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' -quiet 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - First-run empty store → sheet auto-presents; resolved flag (any path: complete, Set up later, swipe-dismiss) prevents re-presentation across relaunches
    - Sheet drives the exchange ONLY through coordinator.start()/syncNow() and reads statusStore — zero direct transport/engine calls
    - Manual entry row present on the Sync surface
    - FULL suite green including DarkBitIdentityTests (tokens untouched); light+dark screenshots captured
  </acceptance_criteria>
  <done>A fresh phone can be seeded from the other phone in one guided flow; existing data always merges and is never clobbered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bootstrap merge → empty/non-empty store | A full remote snapshot lands on a phone whose owner explicitly asked for it (or merges into existing data) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-11 | Tampering / data loss | bootstrap into non-empty store | mitigate | Bootstrap has NO wipe/replace path — it reuses the merge exchange; never-clobber loopback test pins newer-local-edit survival; user copy states "kept and merged, never deleted" |
| T-19-12 | Spoofing | seeding from an unexpected peer | mitigate (partial) / accept | Sheet displays the connected peer's name before/while copying (user can Set up later if unexpected); underlying link remains the accepted T-19-02 household-trust model |
| T-19-13 | Denial of Service | prompt nagging / blocking first run | mitigate | One-shot resolved flag covers complete/later/swipe; DEBUG launch-arg suppression keeps verification loops unblocked |
| T-19-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs. |
</threat_model>

<verification>
- BootstrapAdvisorTests green (emptiness, gate, never-clobber); full suite green incl. DarkBitIdentityTests.
- Simulator first-run loop: sheet appears on empty store, respects the resolved flag, suppressed under -seedSampleData.
- No second sync path: bootstrap files call only coordinator APIs.
</verification>

<success_criteria>
- SYNC-05 (bootstrap half) + ROADMAP criteria 3 and 5: fresh install seedable from the other phone via the snapshot path; non-empty stores merge; local edits never silently lost.
</success_criteria>

<output>
Create `.planning/phases/19-auto-sync-sync-ux-multipeer-bootstrap/19-04-SUMMARY.md` when done.
</output>
