---
phase: 01-foundation-manual-expense-spine
plan: 04
type: execute
wave: 4
depends_on: [01-03]
files_modified:
  - MyHomeTests/Resources/MyHomeV1Seed.store
  - MyHomeTests/MigrationTests.swift
autonomous: false
requirements: [FND-05]

must_haves:
  truths:
    - "A bundled v1 SQLite store (MyHomeV1Seed.store) exists in the test target resources"
    - "The migration-load test opens the bundled v1 store under AppMigrationPlan and reads at least one Expense"
    - "Full unit + migration suite is green"
  artifacts:
    - path: "MyHomeTests/Resources/MyHomeV1Seed.store"
      provides: "Pre-seeded v1 store for the migration-load test"
    - path: "MyHomeTests/MigrationTests.swift"
      provides: "Migration-load test that loads the bundled store and asserts data survives"
      contains: "v1StoreMigratesCleanly"
  key_links:
    - from: "MigrationTests.v1StoreMigratesCleanly"
      to: "AppMigrationPlan + SchemaV1"
      via: "ModelContainer(for:migrationPlan:configurations:) over a temp copy of the bundled store"
      pattern: "AppMigrationPlan"
---

<objective>
Close the last FND-05 verification gap: create the bundled v1 seed store and turn the plan-01 migration-test stub green. This proves the VersionedSchema + SchemaMigrationPlan path actually loads a real on-disk v1 store (not just compiles), which is the success criterion the rest of the migration story depends on.

This plan is deferred to last because the seed store requires a working app (plan 03) to generate a real store file. Per RESEARCH Pattern 9 the migration is a load-and-survive test (stages are empty in v1), but it exercises the full container init path so Phase 2's first real migration cannot fail silently.

Purpose: Verify the durable migration path with a real store, not a stub.
Output: A bundled seed store + a green migration-load test + a green full suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-manual-expense-spine/01-RESEARCH.md
@.planning/phases/01-foundation-manual-expense-spine/01-VALIDATION.md
@.planning/phases/01-foundation-manual-expense-spine/01-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>Plan 03 produced a working app that writes a real SwiftData v1 store. The migration-load test needs that exact on-disk store as a bundled fixture.</what-built>
  <how-to-verify>
    Generate and capture the v1 seed store (RESEARCH Pattern 9 "How to create the seed store"):
    1. Run the app once in the iPhone 16 simulator and add exactly one expense (amount 100, note "Seed").
    2. Locate the store at the App Group container path (or the Application Support fallback if App Group did not provision - see plan 02 SUMMARY for the actual path used).
    3. Copy MyHome.store plus its -shm and -wal sidecar files.
    4. Create MyHomeTests/Resources/ and place the copied store there renamed to MyHomeV1Seed.store (keep the matching -shm/-wal sidecars alongside).
    5. Add MyHomeV1Seed.store (and sidecars) to the MyHomeTests target's Copy Bundle Resources build phase.
    This step is human-action because locating the simulator container store file is a manual filesystem operation with no reliable CLI in this project.
  </how-to-verify>
  <resume-signal>Type "seeded" once MyHomeV1Seed.store is in MyHomeTests/Resources and added to the test target's bundle resources, or describe any blocker (e.g. store path not found).</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Implement the migration-load test against the bundled v1 store</name>
  <files>MyHomeTests/MigrationTests.swift, MyHomeTests/Resources/MyHomeV1Seed.store</files>
  <read_first>
    - MyHomeTests/MigrationTests.swift (the red stub from plan 01)
    - .planning/phases/01-foundation-manual-expense-spine/01-RESEARCH.md (Pattern 9 migration-load test; assumption A5 Bundle accessor in Swift Testing)
    - .planning/phases/01-foundation-manual-expense-spine/01-VALIDATION.md (Wave 0 Requirements - seed store)
    - MyHomeApp/Persistence/Schema/MigrationPlan.swift and SchemaV1.swift (from plan 02)
  </read_first>
  <action>
    Replace the v1StoreMigratesCleanly stub in MigrationTests.swift with the real test per RESEARCH Pattern 9: locate the bundled MyHomeV1Seed.store via the test bundle (Bundle accessor; if Bundle(for:) does not resolve in the Swift Testing context per assumption A5, use Bundle.module or the test bundle identifier - whichever resolves), copy it to a unique temp URL (with defer cleanup), open it with ModelContainer(for: Schema(versionedSchema: SchemaV1.self), migrationPlan: AppMigrationPlan.self, configurations: [ModelConfiguration(schema:url:)]), then fetch FetchDescriptor of Expense and #expect the result is non-empty (the seed has one row). If the bundled store is genuinely missing at runtime, the test must FAIL (use #expect / Issue.record that fails) rather than silently pass - this is the FND-05 gate.
  </action>
  <verify>
    <automated>xcodebuild test -scheme MyHome -only-testing:MyHomeTests/MigrationTests/v1StoreMigratesCleanly -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | tail -8 | grep -q "TEST SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - MyHomeTests/Resources/MyHomeV1Seed.store exists and is in the test target bundle resources
    - MigrationTests.swift loads the bundled store via AppMigrationPlan and asserts a non-empty Expense fetch
    - v1StoreMigratesCleanly passes (was a red stub from plan 01)
    - Full suite green: xcodebuild test -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 16' reports TEST SUCCEEDED
  </acceptance_criteria>
  <done>The bundled v1 store loads under AppMigrationPlan and the migration-load test is green; the entire Phase 1 test suite passes (FND-05 verified).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bundled store fixture -> test container | A checked-in store file is loaded by the migration path; it is test-only and contains synthetic seed data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-08 | Information Disclosure | MyHomeV1Seed.store checked into repo | accept | Seed store contains only synthetic data (amount 100, note "Seed") - no real financial or personal data. Verified at seed-creation checkpoint. |
| T-01-09 | Denial of Service (silent data loss) | migration path not exercised against a real store | mitigate | The load-and-survive test exercises the full ModelContainer + AppMigrationPlan init against a real on-disk v1 store, so Phase 2's first additive migration cannot fail silently (FND-05, Pitfall 7). |
| T-01-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable - zero third-party packages (RESEARCH Package Legitimacy Audit). |
</threat_model>

<verification>
- xcodebuild test -scheme MyHome -only-testing:MyHomeTests/MigrationTests/v1StoreMigratesCleanly -destination 'platform=iOS Simulator,name=iPhone 16' -> TEST SUCCEEDED
- Full suite: xcodebuild test -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 16' -> TEST SUCCEEDED (all model, format, and migration tests green)
</verification>

<success_criteria>
- A bundled v1 seed store exists and loads cleanly under AppMigrationPlan (FND-05)
- The migration-load test is green; the full Phase 1 suite is green
</success_criteria>

<output>
Create `.planning/phases/01-foundation-manual-expense-spine/01-04-SUMMARY.md` when done.
</output>
