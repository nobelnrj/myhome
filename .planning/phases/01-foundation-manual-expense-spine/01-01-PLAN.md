---
phase: 01-foundation-manual-expense-spine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - MyHome.xcodeproj/project.pbxproj
  - MyHomeApp/MyHomeApp.swift
  - MyHomeApp/RootView.swift
  - MyHomeApp/Resources/PrivacyInfo.xcprivacy
  - MyHomeApp/Resources/Assets.xcassets
  - MyHomeApp/MyHome.entitlements
  - MyHomeTests/ExpenseModelTests.swift
  - MyHomeTests/MigrationTests.swift
  - README.md
autonomous: false
requirements: [FND-01, FND-02, FND-04, FND-06]
user_setup:
  - service: apple-developer
    why: "Xcode signing for the bundle ID and App Group entitlement on the free Apple ID"
    dashboard_config:
      - task: "Confirm Xcode 26 installed and an iOS 17+ simulator (iPhone 16) is available"
        location: "Xcode > Settings > Components"

must_haves:
  truths:
    - "Xcode project builds and launches on an iOS 17 simulator with bundle ID com.reojacob.myhome"
    - "PrivacyInfo.xcprivacy declares NSPrivacyTracking false and required-reason API codes CA92.1 + C617.1"
    - "Test target runs Swift Testing @Test functions and the failing stubs are red (not absent)"
  artifacts:
    - path: "MyHomeApp/Resources/PrivacyInfo.xcprivacy"
      provides: "Required-reason API privacy manifest"
      contains: "NSPrivacyTracking"
    - path: "MyHomeTests/ExpenseModelTests.swift"
      provides: "Failing test stubs for model/CRUD/formatting (Nyquist Wave 0)"
      contains: "@Test"
    - path: "MyHomeTests/MigrationTests.swift"
      provides: "Failing migration-load test stub (Nyquist Wave 0)"
      contains: "v1StoreMigratesCleanly"
  key_links:
    - from: "MyHomeApp.xcodeproj build settings"
      to: "iOS 17.0 deployment + Swift 6.2 + SWIFT_STRICT_CONCURRENCY complete"
      via: "project.pbxproj IPHONEOS_DEPLOYMENT_TARGET / SWIFT_VERSION"
      pattern: "IPHONEOS_DEPLOYMENT_TARGET = 17"
---

<objective>
Bootstrap the greenfield Xcode project and lock every one-way-door identifier on day one, then scaffold the Swift Testing target with failing test stubs so all downstream model/CRUD/formatting work has a red bar to turn green (Nyquist Wave 0).

This is the foundation commit: bundle/CloudKit/App-Group IDs (D-09), iOS 17.0 minimum, Swift 6.2 with complete strict concurrency, the PrivacyInfo.xcprivacy manifest (FND-04), and the test infrastructure (FND-06). NO @Model, NO schema, NO UI in this plan — those land in plan 02 and 03 so schema decisions never share a commit with bootstrap or UI work.

Purpose: De-risk the irreversible decisions and give the rest of the phase a test harness.
Output: A buildable, launchable shell app; a privacy manifest; and red test stubs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-manual-expense-spine/01-CONTEXT.md
@.planning/phases/01-foundation-manual-expense-spine/01-RESEARCH.md
@.planning/phases/01-foundation-manual-expense-spine/01-VALIDATION.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap Xcode project with locked identifiers and strict concurrency</name>
  <files>MyHome.xcodeproj/project.pbxproj, MyHomeApp/MyHomeApp.swift, MyHomeApp/RootView.swift, MyHomeApp/MyHome.entitlements, README.md</files>
  <read_first>
    - .planning/phases/01-foundation-manual-expense-spine/01-CONTEXT.md (D-09 immutable IDs; D-08 forward-compat note)
    - .planning/phases/01-foundation-manual-expense-spine/01-RESEARCH.md (§Recommended Project Structure; Open Questions 1 + 2 — App Group fallback, strict concurrency)
    - .planning/research/PITFALLS.md (Pitfall 5 mixing @Observable, Pitfall 18 concurrency, Pitfall 13 App Group)
  </read_first>
  <action>
    Create an iOS App Xcode project named MyHome with app target MyHomeApp, SwiftUI lifecycle, SwiftData enabled (no Core Data). Set these build settings in project.pbxproj (one-way-door per D-09): PRODUCT_BUNDLE_IDENTIFIER = com.reojacob.myhome; IPHONEOS_DEPLOYMENT_TARGET = 17.0; SWIFT_VERSION = 6.0 (Swift 6.2 toolchain); SWIFT_STRICT_CONCURRENCY = complete (per RESEARCH Open Question 2 — set from day one, not later). Add an App Group entitlement group.com.reojacob.myhome in MyHome.entitlements and reference the CloudKit container iCloud.com.reojacob.myhome as a comment/entitlement placeholder ONLY (CloudKit stays off in v1). Create MyHomeApp.swift as the @main App stub rendering RootView, and RootView.swift as a NavigationStack placeholder (real list arrives in plan 03). Create the folder layout from RESEARCH §Recommended Project Structure: MyHomeApp/Features/Expenses/, MyHomeApp/Persistence/{Schema,Models}/, MyHomeApp/Resources/ (empty dirs are fine — populated later). Add a README.md recording the project-wide rules: @Observable only (never @StateObject/@ObservedObject/@Published per Pitfall 5), Decimal money, UTC dates, no @Attribute(.unique), and the App Group fallback note (if the App Group entitlement will not provision on the free account, the ModelContainer in plan 02 falls back to .applicationSupportDirectory with a documented migration TODO — do NOT change the bundle/group IDs themselves). Do not add the .modelContainer modifier yet — plan 02 owns the container factory.
  </action>
  <verify>
    <automated>xcodebuild build -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | tail -5 | grep -q "BUILD SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - `xcodebuild build -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 16'` reports BUILD SUCCEEDED
    - project.pbxproj contains `PRODUCT_BUNDLE_IDENTIFIER = com.reojacob.myhome`
    - project.pbxproj contains `IPHONEOS_DEPLOYMENT_TARGET = 17.0`
    - project.pbxproj contains `SWIFT_STRICT_CONCURRENCY = complete`
    - MyHome.entitlements contains `group.com.reojacob.myhome`
    - `grep -rn "@StateObject\|@ObservedObject\|@Published" MyHomeApp/` returns zero results
  </acceptance_criteria>
  <done>App builds and launches a placeholder NavigationStack on the iPhone 16 simulator; all three immutable IDs and strict-concurrency setting are committed.</done>
</task>

<task type="auto">
  <name>Task 2: Add PrivacyInfo.xcprivacy and the Swift Testing target with failing stubs (Nyquist Wave 0)</name>
  <files>MyHomeApp/Resources/PrivacyInfo.xcprivacy, MyHomeTests/ExpenseModelTests.swift, MyHomeTests/MigrationTests.swift</files>
  <read_first>
    - .planning/phases/01-foundation-manual-expense-spine/01-RESEARCH.md (§PrivacyInfo.xcprivacy plist structure; Pattern 7 in-memory fixture; Pattern 8 reflection test; Pattern 9 migration test; §Wave 0 Gaps)
    - .planning/phases/01-foundation-manual-expense-spine/01-VALIDATION.md (§Per-Task Verification Map; §Wave 0 Requirements)
    - .planning/research/PITFALLS.md (Pitfall 4 privacy manifest, Pitfall 16 test container)
  </read_first>
  <action>
    Create MyHomeApp/Resources/PrivacyInfo.xcprivacy (the plist from RESEARCH §PrivacyInfo.xcprivacy plist structure): NSPrivacyTracking false, empty NSPrivacyTrackingDomains and NSPrivacyCollectedDataTypes, NSPrivacyAccessedAPITypes with NSPrivacyAccessedAPICategoryUserDefaults reason CA92.1 and NSPrivacyAccessedAPICategoryFileTimestamp reason C617.1. Add it to the MyHomeApp target's Copy Bundle Resources phase (FND-04). Create the MyHomeTests target configured for Swift Testing (no XCTest base class — @Test functions only; FND-06). Write MyHomeTests/ExpenseModelTests.swift with a @MainActor struct containing FAILING stub @Test functions named exactly: expenseCRUD, expenseUpdate, currencyFormatting, expensePropertiesAreCloudKitReady — each body uses #expect(Bool(false), "stub — implemented in plan 02") (or references the not-yet-existing Expense type guarded so the file still compiles; prefer a compiling red stub using #expect(Bool(false))). Write MyHomeTests/MigrationTests.swift with a @MainActor struct containing a FAILING stub @Test function named exactly v1StoreMigratesCleanly using Issue.record("seed store not yet created — plan 04") so it is red until plan 04. These stubs MUST be red, not absent — they are the Nyquist sampling baseline. Do NOT implement real assertions referencing Expense yet (that type arrives in plan 02); keep stubs compiling.
  </action>
  <verify>
    <automated>grep -l "NSPrivacyTracking" MyHomeApp/Resources/PrivacyInfo.xcprivacy && grep -c "@Test" MyHomeTests/ExpenseModelTests.swift</automated>
  </verify>
  <acceptance_criteria>
    - `grep -l "NSPrivacyTracking" MyHomeApp/Resources/PrivacyInfo.xcprivacy` prints the file path
    - PrivacyInfo.xcprivacy contains `CA92.1` and `C617.1`
    - `grep -c "@Test" MyHomeTests/ExpenseModelTests.swift` returns 4
    - MigrationTests.swift contains a @Test function named `v1StoreMigratesCleanly`
    - `xcodebuild test -scheme MyHome -only-testing:MyHomeTests -destination 'platform=iOS Simulator,name=iPhone 16'` compiles and runs (tests RED is expected and correct at this stage)
  </acceptance_criteria>
  <done>Privacy manifest is in the target; test target runs Swift Testing with 5 red stub tests serving as the Nyquist baseline for plans 02–04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → app store URL | SwiftData store file location decided here (App Group container vs Application Support fallback) — affects data-at-rest scope |
| App Store review → app binary | Privacy manifest correctness is a ship gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | SwiftData store file | accept | Store inherits iOS Data Protection (Complete class default); App Group container is sandboxed to this app. No PII beyond local expense amounts in Phase 1. Do not downgrade protection class. |
| T-01-02 | Denial of Service (ship blocker) | PrivacyInfo.xcprivacy | mitigate | Add manifest day one with NSPrivacyTracking false + CA92.1/C617.1 (FND-04); verified via grep gate. Missing manifest → ITMS-91053 rejection. |
| T-01-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable — Phase 1 installs zero third-party packages (RESEARCH §Package Legitimacy Audit). All deps are Apple frameworks. No install tasks, no legitimacy checkpoint required. |
</threat_model>

<verification>
- `xcodebuild build -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 16'` → BUILD SUCCEEDED
- `xcodebuild test -scheme MyHome -only-testing:MyHomeTests -destination 'platform=iOS Simulator,name=iPhone 16'` runs (red expected)
- Identifiers grep-confirmed in project.pbxproj and entitlements
</verification>

<success_criteria>
- Project builds and launches on iOS 17 simulator with the three locked identifiers (D-09) and strict concurrency complete
- PrivacyInfo.xcprivacy present in target with correct keys (FND-04)
- Swift Testing target runs with 5 red stub tests (FND-06 infrastructure; Nyquist Wave 0 baseline)
- Zero @StateObject/@ObservedObject/@Published in the codebase (Pitfall 5)
</success_criteria>

<output>
Create `.planning/phases/01-foundation-manual-expense-spine/01-01-SUMMARY.md` when done.
</output>
