---
phase: 01-foundation-manual-expense-spine
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - MyHomeApp/Persistence/Models/Expense.swift
  - MyHomeApp/Persistence/Schema/SchemaV1.swift
  - MyHomeApp/Persistence/Schema/MigrationPlan.swift
  - MyHomeApp/Persistence/ModelContainer+App.swift
  - MyHomeApp/MyHomeApp.swift
  - MyHomeApp/Support/Decimal+INR.swift
  - MyHomeApp/Support/Date+Display.swift
  - MyHomeTests/ExpenseModelTests.swift
autonomous: false
requirements: [FND-03, FND-05, FND-06, FND-07]

must_haves:
  truths:
    - "Expense @Model exists with all properties optional or defaulted, no @Attribute(.unique), Decimal money, UTC Date"
    - "VersionedSchema v1 + SchemaMigrationPlan scaffolded; ModelContainer loads it from the App Group URL (or documented fallback)"
    - "Decimal renders as en-IN lakh grouping (₹1,00,000.00); negative renders as -₹500.00"
    - "Reflection test, CRUD test, update test, and currency test pass (turn the plan-01 stubs green)"
  artifacts:
    - path: "MyHomeApp/Persistence/Schema/SchemaV1.swift"
      provides: "VersionedSchema v1 with nested Expense @Model"
      contains: "VersionedSchema"
    - path: "MyHomeApp/Persistence/Schema/MigrationPlan.swift"
      provides: "SchemaMigrationPlan with empty stages"
      contains: "SchemaMigrationPlan"
    - path: "MyHomeApp/Persistence/ModelContainer+App.swift"
      provides: "appContainer() factory wiring App Group store URL + migration plan"
      contains: "appContainer"
    - path: "MyHomeApp/Support/Decimal+INR.swift"
      provides: "en-IN currency formatting"
      contains: "en_IN"
  key_links:
    - from: "MyHomeApp.swift"
      to: "ModelContainer.appContainer()"
      via: ".modelContainer modifier"
      pattern: "modelContainer"
    - from: "Expense @Model"
      to: "SchemaV1.models + ModelContainer schema"
      via: "Schema(versionedSchema: SchemaV1.self)"
      pattern: "versionedSchema"
---

<objective>
Build the durable data spine: the Expense @Model (CloudKit-ready per the 8 rules), VersionedSchema v1 + SchemaMigrationPlan scaffolding, the ModelContainer factory wired to the App Group store URL, and en-IN currency / UTC date formatting helpers. Then turn the plan-01 model/CRUD/formatting test stubs green.

This is the load-bearing schema commit. The 8 CloudKit-readiness rules (FND-03) are non-negotiable even though v1 is local-only. The VersionedSchema scaffold (FND-05) means Phase 2's Category addition is a lightweight additive migration, never a rewrite (D-08). NO UI in this plan — screens land in plan 03.

Purpose: A correct, forward-compatible schema is the asset the whole app is staked on.
Output: Expense model, schema versioning, container factory, formatting helpers, and 4 green tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation-manual-expense-spine/01-CONTEXT.md
@.planning/phases/01-foundation-manual-expense-spine/01-RESEARCH.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@.planning/phases/01-foundation-manual-expense-spine/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expense @Model inside VersionedSchema v1 + MigrationPlan + ModelContainer factory</name>
  <files>MyHomeApp/Persistence/Schema/SchemaV1.swift, MyHomeApp/Persistence/Schema/MigrationPlan.swift, MyHomeApp/Persistence/Models/Expense.swift, MyHomeApp/Persistence/ModelContainer+App.swift, MyHomeApp/MyHomeApp.swift, MyHomeTests/ExpenseModelTests.swift</files>
  <read_first>
    - MyHomeTests/ExpenseModelTests.swift (the red stubs from plan 01 to turn green)
    - .planning/phases/01-foundation-manual-expense-spine/01-RESEARCH.md (Pattern 1 @Model, Pattern 2 VersionedSchema + MigrationPlan, Pattern 3 ModelContainer injection, Open Question 3 typealias)
    - .planning/phases/01-foundation-manual-expense-spine/01-CONTEXT.md (D-05 field list, D-06 no merchant field, D-08 forward-compat)
    - .planning/research/ARCHITECTURE.md (8 CloudKit-readiness @Model rules)
    - .planning/research/PITFALLS.md (Pitfall 1, 2, 7, 13, 17, 18)
  </read_first>
  <behavior>
    - expenseCRUD: insert an Expense(amount: Decimal(500), note: "Lunch") into an in-memory ModelContext, save, fetch → count == 1, currencyCode == "INR", amount == Decimal(500); delete → fetch empty
    - expenseUpdate: insert, mutate amount to Decimal(750) and note, save, re-fetch → amount == Decimal(750) and updatedAt changed
    - expensePropertiesAreCloudKitReady: Mirror over an Expense(amount: Decimal(0)) — every stored property is optional or has a non-nil default; schema entity for "Expense" has empty uniquenessConstraints
  </behavior>
  <action>
    Create SchemaV1.swift: enum SchemaV1: VersionedSchema with static versionIdentifier = Schema.Version(1, 0, 0) and a nested @Model final class Expense carrying exactly the D-05 fields, all optional-or-defaulted: id: UUID = UUID(); amount: Decimal = Decimal(0); currencyCode: String = "INR"; date: Date = Date() (UTC timestamp per D-02); note: String? = nil (D-05/D-06 — no merchant field); createdAt: Date = Date(); updatedAt: Date = Date(). NO @Attribute(.unique) anywhere (Pitfall 2). Init signature: (id: UUID = UUID(), amount: Decimal, currencyCode: String = "INR", date: Date = Date(), note: String? = nil) setting createdAt/updatedAt = Date(). models returns [SchemaV1.Expense.self]. Create Expense.swift with `typealias Expense = SchemaV1.Expense` (RESEARCH Open Question 3) so views/tests use bare Expense. Create MigrationPlan.swift: enum AppMigrationPlan: SchemaMigrationPlan with schemas = [SchemaV1.self] and stages = [] (empty — single version per D-08; Phase 2 appends SchemaV2 + a stage). Create ModelContainer+App.swift: static func appContainer() throws -> ModelContainer using Schema(versionedSchema: SchemaV1.self), ModelConfiguration with the App Group store URL (containerURL(forSecurityApplicationGroupIdentifier: "group.com.reojacob.myhome")) appending "MyHome.store" and cloudKitDatabase: .none; init via ModelContainer(for: schema, migrationPlan: AppMigrationPlan.self, configurations: [config]). If the App Group container URL is nil at runtime (free-account provisioning fail per RESEARCH Open Question 1), fall back to .applicationSupportDirectory with a `// TODO: migrate to App Group URL when paid account active` comment — never change the group ID string. Wire .modelContainer into MyHomeApp.swift via the static container pattern (Pattern 3). Then implement the expenseCRUD, expenseUpdate, and expensePropertiesAreCloudKitReady tests in ExpenseModelTests.swift per the behavior block, using an in-memory ModelContainer(for: Expense.self, configurations: ModelConfiguration(isStoredInMemoryOnly: true)) per test (FND-06, Pitfall 16). If the uniquenessConstraints API path is not accessible, fall back to a grep-style assertion documented in RESEARCH Pattern 8 note.
  </action>
  <verify>
    <automated>xcodebuild test -scheme MyHome -only-testing:MyHomeTests/ExpenseModelTests/expenseCRUD -only-testing:MyHomeTests/ExpenseModelTests/expenseUpdate -only-testing:MyHomeTests/ExpenseModelTests/expensePropertiesAreCloudKitReady -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | tail -8 | grep -q "TEST SUCCEEDED\|** TEST SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - SchemaV1.swift contains `VersionedSchema` and `Schema.Version(1, 0, 0)`
    - `grep -rn "@Attribute(.unique)" MyHomeApp/` returns zero results
    - `grep -rn ": Double" MyHomeApp/Persistence/` returns zero results (Pitfall 17 — money is Decimal)
    - MigrationPlan.swift contains `SchemaMigrationPlan` with `stages` returning `[]`
    - ModelContainer+App.swift contains `group.com.reojacob.myhome` and `migrationPlan: AppMigrationPlan.self`
    - MyHomeApp.swift contains `.modelContainer`
    - expenseCRUD, expenseUpdate, expensePropertiesAreCloudKitReady all pass (were red stubs from plan 01)
  </acceptance_criteria>
  <done>Expense model + versioned schema + container factory exist and are CloudKit-ready; 3 model tests pass; app still builds and launches with the container injected.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: en-IN currency formatting + UTC date display helpers</name>
  <files>MyHomeApp/Support/Decimal+INR.swift, MyHomeApp/Support/Date+Display.swift, MyHomeTests/ExpenseModelTests.swift</files>
  <read_first>
    - MyHomeTests/ExpenseModelTests.swift (the currencyFormatting red stub)
    - .planning/phases/01-foundation-manual-expense-spine/01-RESEARCH.md (Pattern 5 en-IN formatting, §Code Examples en-IN + UTC display; assumption A1)
    - .planning/phases/01-foundation-manual-expense-spine/01-CONTEXT.md (D-04 paise-optional display, D-02 UTC storage)
    - .planning/research/PITFALLS.md (Pitfall 17 locale formatting)
  </read_first>
  <behavior>
    - currencyFormatting: Decimal(100000).formattedINR() contains "1,00,000" (lakh grouping) and has prefix "₹"
    - negative: Decimal(-500).formattedINR() begins with "-₹" (sign before symbol, automatic from Decimal sign)
  </behavior>
  <action>
    Create Support/Decimal+INR.swift: extension Decimal with func formattedINR() -> String returning self.formatted(.currency(code: "INR").locale(Locale(identifier: "en_IN"))) (Pattern 5 — FormatStyle, not NumberFormatter, not hand-rolled ₹ per Pitfall 17). Create Support/Date+Display.swift: extension Date with formattedForExpenseList() (DateFormatter .medium date / .short time, locale .current, timeZone .current) and formattedForDatePickerRow() ("Today, h:mm a" when Calendar.current.isDateInToday, else medium+short) per RESEARCH §Code Examples UTC storage + local display (D-02). Implement the currencyFormatting test per the behavior block (assert lakh grouping substring "1,00,000" and "₹" prefix; add a negative-amount assertion that -₹ prefixes). Note: if the live en_IN output differs from the assumed format (RESEARCH A1), assert on the lakh-grouping substring "1,00,000" which is the load-bearing FND-07 behavior rather than the exact symbol placement.
  </action>
  <verify>
    <automated>xcodebuild test -scheme MyHome -only-testing:MyHomeTests/ExpenseModelTests/currencyFormatting -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | tail -8 | grep -q "TEST SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - Decimal+INR.swift contains `en_IN` and `.currency(code: "INR")`
    - Date+Display.swift defines `formattedForExpenseList` and `formattedForDatePickerRow`
    - currencyFormatting test passes asserting lakh grouping substring "1,00,000" and "₹" prefix (FND-07)
    - `grep -rn "String(format:" MyHomeApp/Support/Decimal+INR.swift` returns zero results (no hand-rolled formatting, Pitfall 17)
  </acceptance_criteria>
  <done>Currency renders en-IN lakh grouping and dates have UTC-store/local-display helpers; the currencyFormatting test passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → SwiftData store | Store URL (App Group container) holds local financial data at rest |
| schema → CloudKit (future) | Schema shape decided now must mirror cleanly when CloudKit flips on later |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Tampering | Expense.amount Decimal parse (future input) | accept (this plan) | Range/parse validation lands in plan 03 at the keypad input boundary; the model stores Decimal with no constraint. Documented here, mitigated in plan 03. |
| T-01-04 | Information Disclosure | SwiftData store at App Group URL | accept | iOS Data Protection Complete class by default; container sandboxed. Do not downgrade. No network surface in Phase 1. |
| T-01-05 | Denial of Service (data loss) | VersionedSchema migration path | mitigate | Scaffold VersionedSchema v1 + SchemaMigrationPlan from day one (FND-05, Pitfall 7) so Phase 2's additive Category migration cannot silently wipe the store. |
| T-01-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable — zero third-party packages (RESEARCH §Package Legitimacy Audit); all Apple frameworks. |
</threat_model>

<verification>
- `xcodebuild test -scheme MyHome -only-testing:MyHomeTests/ExpenseModelTests -destination 'platform=iOS Simulator,name=iPhone 16'` → all 4 model/format tests green
- `grep -rn "@Attribute(.unique)" MyHomeApp/` → zero
- `grep -rn ": Double" MyHomeApp/Persistence/` → zero
- App still builds with the container injected
</verification>

<success_criteria>
- Expense @Model satisfies all 8 CloudKit-readiness rules (FND-03), verified by the reflection test
- VersionedSchema v1 + SchemaMigrationPlan scaffolded and loaded by the container (FND-05)
- en-IN lakh-grouping formatting works (FND-07)
- In-memory ModelContainer fixtures drive all tests (FND-06); 4 stubs from plan 01 are now green
</success_criteria>

<output>
Create `.planning/phases/01-foundation-manual-expense-spine/01-02-SUMMARY.md` when done.
</output>
