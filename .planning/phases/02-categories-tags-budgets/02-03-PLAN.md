---
phase: 02-categories-tags-budgets
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - MyHomeApp/Features/Expenses/CategoryPickerView.swift
  - MyHomeApp/Features/Expenses/AddExpenseView.swift
  - MyHomeApp/Features/Expenses/EditExpenseView.swift
autonomous: false
requirements: [EXP-06]
must_haves:
  truths:
    - "When adding an expense, the user can optionally pick one category from the seeded list (off the ≤3-tap fast path) — D2-12"
    - "When editing an expense, the category row pre-fills the current category and can be changed or cleared"
    - "Leaving an expense uncategorized stays valid and adds no taps to the core add flow (EXP-01 preserved)"
  artifacts:
    - path: "MyHomeApp/Features/Expenses/CategoryPickerView.swift"
      provides: "Reusable sheet picker binding selectedCategory: Category?, with None + Clear affordances"
      contains: "struct CategoryPickerView"
    - path: "MyHomeApp/Features/Expenses/AddExpenseView.swift"
      provides: "Optional Category row in Section 2 + category wiring on save"
      contains: "selectedCategory"
    - path: "MyHomeApp/Features/Expenses/EditExpenseView.swift"
      provides: "Pre-filled Category row + isDirty includes category + category applied on save"
      contains: "selectedCategory"
  key_links:
    - from: "AddExpenseView.saveExpense"
      to: "expense.categories"
      via: "selectedCategory wiring on save"
      pattern: "expense\\.categories"
    - from: "CategoryPickerView"
      to: "Category @Query"
      via: "@Query(sort: \\.sortOrder)"
      pattern: "@Query"
---

<objective>
Add the optional category picker to the existing Add/Edit expense sheets (D2-12), realizing the deferred "amount → category → save" intent without regressing the ≤4-tap add target. Create a reusable `CategoryPickerView` sheet (None + Clear affordances) consumed by both sheets.

Purpose: EXP-06 — let the user attach one category to an expense. The picker is optional and lives in Section 2, off the ≤3-tap critical path, so uncategorized entry stays frictionless (D2-12 / EXP-01). This plan is independent of the Budgets surface and touches only the Expenses feature files.
Output: CategoryPickerView.swift (new), updated AddExpenseView.swift + EditExpenseView.swift.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-categories-tags-budgets/02-PATTERNS.md
@.planning/phases/02-categories-tags-budgets/02-UI-SPEC.md
@.planning/phases/02-categories-tags-budgets/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CategoryPickerView reusable sheet</name>
  <files>MyHomeApp/Features/Expenses/CategoryPickerView.swift</files>
  <read_first>
    - MyHomeApp/Features/Expenses/EditExpenseView.swift (the NavigationStack-in-sheet + toolbar analog, lines 58–89)
    - MyHomeApp/Features/Expenses/ExpenseRow.swift (the HStack row layout analog)
    - .planning/phases/02-categories-tags-budgets/02-PATTERNS.md § "CategoryPickerView.swift" (full row + toolbar code)
    - .planning/phases/02-categories-tags-budgets/02-UI-SPEC.md § "Screen 5 (New): CategoryPickerView" + § Copywriting Contract (None / Clear / "Category" copy) + § Accessibility (icons .accessibilityHidden(true))
  </read_first>
  <behavior>
    - Tapping a category row sets the binding to that category and dismisses.
    - Tapping the "None" row (top of list) sets the binding to nil and dismisses.
    - The "Clear" toolbar button appears only when a category is currently selected and sets the binding to nil on tap.
    - The currently-selected category row shows a `checkmark` in `.accentColor`.
  </behavior>
  <action>
    Create `CategoryPickerView.swift` (`import SwiftUI` + `import SwiftData`). Struct: `@Binding var selectedCategory: Category?`, `@Environment(\.dismiss) private var dismiss`, `@Query(sort: \Category.sortOrder) private var categories: [Category]`. Body is `NavigationStack { List { ... } }` with `.listStyle(.insetGrouped)`, `.navigationTitle("Category")`, `.navigationBarTitleDisplayMode(.inline)`. First row is a "None" `Button` (`circle.slash` icon, `.secondary`, 24pt frame; checkmark when `selectedCategory == nil`) that sets nil + dismisses. Then `ForEach(categories)` rows: SF Symbol (`category.symbolName`, 16pt/24pt frame, `.secondary`, `.accessibilityHidden(true)`), `Text(category.name ?? "")` (`.body`, `.primary`), Spacer, checkmark (`.accentColor`) when `selectedCategory?.persistentModelID == category.persistentModelID`; tap sets `selectedCategory = category` + dismiss. Each row `.frame(minHeight: 44)`, `.buttonStyle(.plain)`. Toolbar: `.cancellationAction` "Cancel" (dismiss); `.destructiveAction` "Clear" (`.systemRed` tint) only when `selectedCategory != nil` → sets nil + dismiss. Use plain `Text` for the name (never AttributedString(markdown:) — T-02-07).
  </action>
  <verify>
    <automated>xcodebuild build -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - CategoryPickerView.swift declares `struct CategoryPickerView` with `@Binding var selectedCategory: Category?` and `@Query(sort: \Category.sortOrder)`.
    - Body contains a NavigationStack, a top "None" row, a ForEach over categories with a selection checkmark, and a conditional "Clear" toolbar button.
    - `xcodebuild build` exits 0.
  </acceptance_criteria>
  <done>CategoryPickerView compiles, lists seeded categories sorted by sortOrder, and supports select / None / Clear with a selection checkmark.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Category row wiring into Add + Edit expense sheets</name>
  <files>MyHomeApp/Features/Expenses/AddExpenseView.swift, MyHomeApp/Features/Expenses/EditExpenseView.swift</files>
  <read_first>
    - MyHomeApp/Features/Expenses/AddExpenseView.swift (the file being modified — @State block, optionalSection, saveExpense)
    - MyHomeApp/Features/Expenses/EditExpenseView.swift (the file being modified — @State, initializeFields, optionalSection, isDirty, saveExpense)
    - .planning/phases/02-categories-tags-budgets/02-PATTERNS.md § "AddExpenseView.swift update" + § "EditExpenseView.swift update" (exact @State additions, Category row layout, save wiring, isDirty extension, line numbers)
    - .planning/phases/02-categories-tags-budgets/02-UI-SPEC.md § "Screen 1 (Modified)" + § "Screen 2 (Modified)" (Category row position: below Date, above Note; "None"/chevron styling; ≤4-tap note)
  </read_first>
  <behavior>
    - Existing ExpenseModelTests + add/edit behavior stay green (no regression to the core flow).
    - Saving an Add with a selected category persists `expense.categories = [cat]`; saving with None persists `[]`.
    - Editing pre-fills the row from `expense.categories.first`; changing/clearing the category and saving persists the new value; isDirty becomes true when only the category changed.
  </behavior>
  <action>
    AddExpenseView.swift: add `@State private var selectedCategory: Category? = nil` and `@State private var showCategoryPicker: Bool = false`. In the optional/Section-2 area, add a "Category" Button row BELOW the Date row and ABOVE the Note field, matching the existing Date-row style (label `.primary`, value = symbol + name `.secondary` or "None", trailing `chevron.right`, `frame(minHeight: 44)`, `.secondarySystemBackground`, `RoundedRectangle(cornerRadius: 12)`); tapping sets `showCategoryPicker = true`; attach `.sheet(isPresented: $showCategoryPicker) { CategoryPickerView(selectedCategory: $selectedCategory) }`. In `saveExpense()`, after `context.insert(expense)`, set `expense.categories = selectedCategory.map { [$0] } ?? []` BEFORE `try context.save()`. Do not move the category row onto the keypad/critical path — EXP-01 ≤4-tap must hold.

    EditExpenseView.swift: add the same `@State` pair. In `initializeFields()` set `selectedCategory = expense.categories.first`. Add the identical Category row to its optionalSection (same position/styling) with the same `.sheet`. Extend `isDirty` to also compare `selectedCategory?.persistentModelID != expense.categories.first?.persistentModelID`. In `saveExpense()` set `expense.categories = selectedCategory.map { [$0] } ?? []` and `expense.updatedAt = Date()` before `try context.save()` (CR-01).
  </action>
  <verify>
    <automated>xcodebuild test -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing MyHomeTests/ExpenseCategoryTests -only-testing MyHomeTests/ExpenseModelTests 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Both AddExpenseView.swift and EditExpenseView.swift declare `selectedCategory` and `showCategoryPicker` @State and present `CategoryPickerView` via `.sheet`.
    - AddExpenseView.saveExpense sets `expense.categories` from `selectedCategory` before save; EditExpenseView does the same plus `updatedAt = Date()`.
    - EditExpenseView `isDirty` includes a category comparison.
    - `xcodebuild test ... -only-testing MyHomeTests/ExpenseCategoryTests -only-testing MyHomeTests/ExpenseModelTests` exits 0.
  </acceptance_criteria>
  <done>Add and Edit sheets present an optional Category row; selection persists to expense.categories; uncategorized stays valid; the ≤4-tap add path is unchanged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify category picker + fast-path preservation</name>
  <action>Pause for the developer to run the app on the iPhone 17 simulator and confirm the category picker behavior and fast-path tap count below. No code changes — this is a human verification gate.</action>
  <what-built>The optional category picker on the Add/Edit expense sheets and the reusable CategoryPickerView sheet (None / Clear / select). This is the only UI-interaction surface in this plan that needs a visual + tap-count confirmation (manual-only per VALIDATION.md: "Category picker stays off the ≤3-tap fast path").</what-built>
  <how-to-verify>
    1. Build & run on the iPhone 17 simulator (Xcode 26.5, scheme MyHome).
    2. Add fast-path check: tap `+`, type an amount on the keypad, tap "Save Expense" — confirm this is still 3 taps and the expense saves uncategorized (no category step forced). EXP-01 / D2-12.
    3. Tap `+` again, type an amount, scroll to Section 2, tap the "Category" row → pick "Groceries" → confirm the row shows the Groceries icon + name, save, and the expense persists with that category.
    4. Edit that expense → confirm the Category row pre-fills "Groceries" → tap it → tap "Clear" → confirm it returns to "None" → save.
    5. Confirm the picker lists all 14 seeded categories sorted (Groceries first … Misc last) with a checkmark on the selected one.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issue (extra taps, wrong order, missing Clear).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user text → category name display | Category names rendered in picker rows; only display, no execution |
| (no network / no auth) | Local-only app |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Spoofing/Tampering | category name rendered in picker | mitigate | Plain `Text(category.name ?? "")` — never `AttributedString(markdown:)` (consistent with Phase 1 T-01-06) |
| T-02-08 | Tampering | category write on expense save | mitigate | Explicit `try context.save()` (CR-01) after setting `expense.categories`; nil-safe `.map { [$0] } ?? []` |
| T-02-SC | Tampering | package installs | n/a | No installs — first-party SwiftUI/SwiftData only |

No HIGH threats. Local-only UI wiring; category-name display sanitized via plain Text.
</threat_model>

<verification>
Full suite green: `xcodebuild test -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' 2>&1 | tail -30`. Human checkpoint confirms fast-path preservation and picker behavior.
</verification>

<success_criteria>
- User can attach one category to an expense via an optional picker (EXP-06).
- Add/Edit sheets pre-fill, change, and clear the category; uncategorized stays valid.
- The ≤4-tap add path is unchanged (human-verified).
</success_criteria>

<output>
Create `.planning/phases/02-categories-tags-budgets/02-03-SUMMARY.md` when done.
</output>
