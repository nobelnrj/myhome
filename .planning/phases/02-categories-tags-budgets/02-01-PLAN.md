---
phase: 02-categories-tags-budgets
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - MyHomeApp/Persistence/Schema/SchemaV2.swift
  - MyHomeApp/Persistence/Schema/MigrationPlan.swift
  - MyHomeApp/Persistence/Models/Expense.swift
  - MyHomeApp/Persistence/Models/Category.swift
  - MyHomeApp/Persistence/ModelContainer+App.swift
  - MyHomeTests/CategorySeedTests.swift
  - MyHomeTests/CategoryCRUDTests.swift
  - MyHomeTests/ExpenseCategoryTests.swift
  - MyHomeTests/BudgetModelTests.swift
  - MyHomeTests/MigrationTests.swift
autonomous: true
requirements: [EXP-04, EXP-05, EXP-06, EXP-07]
must_haves:
  truths:
    - "App ships with 14 India-tuned predefined categories on first launch"
    - "Seeding is idempotent — relaunch does not duplicate categories"
    - "An expense can carry a category, and deleting a category nullifies that link (expense becomes uncategorized)"
    - "A category can store a recurring monthlyBudget as Decimal"
    - "An existing v1 store opens cleanly and migrates forward to SchemaV2 with expense data intact"
  artifacts:
    - path: "MyHomeApp/Persistence/Schema/SchemaV2.swift"
      provides: "SchemaV2 VersionedSchema with Category @Model + Expense with to-many categories relationship"
      contains: "enum SchemaV2"
    - path: "MyHomeApp/Persistence/Models/Category.swift"
      provides: "typealias Category = SchemaV2.Category"
      contains: "typealias Category"
    - path: "MyHomeApp/Persistence/ModelContainer+App.swift"
      provides: "Schema(versionedSchema: SchemaV2.self) wiring + idempotent seedCategoriesIfNeeded hook"
      contains: "seedCategoriesIfNeeded"
  key_links:
    - from: "MyHomeApp/Persistence/Models/Expense.swift"
      to: "SchemaV2.Expense"
      via: "typealias flip"
      pattern: "typealias Expense = SchemaV2\\.Expense"
    - from: "MyHomeApp/Persistence/Schema/MigrationPlan.swift"
      to: "SchemaV2"
      via: "MigrationStage.custom V1→V2"
      pattern: "MigrationStage\\.custom"
    - from: "MyHomeApp/Persistence/ModelContainer+App.swift"
      to: "seedCategoriesIfNeeded"
      via: "post-container-init call"
      pattern: "seedCategoriesIfNeeded\\(context:"
---

<objective>
Establish the SchemaV2 data foundation for Phase 2: add the `Category` `@Model`, an additive optional to-many `Expense ↔ Category` relationship, the `monthlyBudget: Decimal?` field, the V1→V2 migration stage, idempotent first-launch category seeding, and the model-level test suite that every later plan builds on.

Purpose: Everything in Phase 2 (budget calc, pickers, the Budgets surface) depends on the `Category` type, the `Expense.categories` relationship, and the typealias flips compiling. This plan is the keystone — it ships the schema additively (never mutating SchemaV1) so the existing on-disk v1 store survives, per D-08 / D2-02.
Output: SchemaV2.swift, Category typealias, updated MigrationPlan/Expense/ModelContainer, four new test files, updated MigrationTests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-categories-tags-budgets/02-RESEARCH.md
@.planning/phases/02-categories-tags-budgets/02-PATTERNS.md
@.planning/phases/02-categories-tags-budgets/02-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SchemaV2 + Category @Model + additive Expense relationship + migration wiring</name>
  <files>MyHomeApp/Persistence/Schema/SchemaV2.swift, MyHomeApp/Persistence/Schema/MigrationPlan.swift, MyHomeApp/Persistence/Models/Expense.swift, MyHomeApp/Persistence/Models/Category.swift, MyHomeTests/MigrationTests.swift</files>
  <read_first>
    - MyHomeApp/Persistence/Schema/SchemaV1.swift (the immutable analog — copy its enum shell + @Model field/init shape verbatim; NEVER edit it)
    - MyHomeApp/Persistence/Schema/MigrationPlan.swift (the file being modified; current empty-stages shape)
    - MyHomeApp/Persistence/Models/Expense.swift (the typealias being flipped)
    - MyHomeTests/MigrationTests.swift (the migration test being updated)
    - .planning/phases/02-categories-tags-budgets/02-PATTERNS.md § "SchemaV2.swift", § "MigrationPlan.swift", § "Expense.swift typealias flip", § "Category.swift new typealias", § "MigrationTests.swift update" (exact code + line numbers)
    - .planning/phases/02-categories-tags-budgets/02-RESEARCH.md § Pattern 1, § Pitfall P2-01, § Pitfall P2-04 (why MigrationStage.custom, not .lightweight)
  </read_first>
  <behavior>
    - MigrationTests/v1StoreMigratesCleanly: bundled MyHomeV1Seed.store opens under AppMigrationPlan and the seeded Expense(amount: 100, note: "Seed") is readable post-migration with amount/note/currencyCode intact.
    - The Expense entity in SchemaV2 still satisfies CloudKit-readiness (all attributes optional/defaulted, no uniqueness constraints) — existing ExpenseModelTests stays green.
  </behavior>
  <action>
    Create `MyHomeApp/Persistence/Schema/SchemaV2.swift`: `enum SchemaV2: VersionedSchema` with `versionIdentifier = Schema.Version(2, 0, 0)` and `models = [SchemaV2.Expense.self, SchemaV2.Category.self]`. Declare `@Model final class Category` with: `var id: UUID = UUID()`, `var name: String? = nil`, `var symbolName: String? = nil`, `var sortOrder: Int = 0`, `var monthlyBudget: Decimal? = nil`, `var currencyCode: String = "INR"`, `var createdAt: Date = Date()`, and the inverse relationship `@Relationship(deleteRule: .nullify, inverse: \SchemaV2.Expense.categories) var expenses: [SchemaV2.Expense] = []`. Add `init(name: String, symbolName: String?, sortOrder: Int = 0)` assigning id/name/symbolName/sortOrder and `createdAt = Date()`. Declare `@Model final class Expense` as a copy of SchemaV1.Expense's seven fields (id, amount, currencyCode, date, note, createdAt, updatedAt — all optional/defaulted, money as Decimal, no `@Attribute(.unique)`) PLUS the new `@Relationship(deleteRule: .nullify, inverse: \SchemaV2.Category.expenses) var categories: [SchemaV2.Category] = []`. Both relationship sides MUST declare `inverse:` explicitly (CloudKit rule 4). Use Array, never Set (SwiftData requirement). Copy the SchemaV1.Expense init shape verbatim.

    Update `MigrationPlan.swift`: change `schemas` to `[SchemaV1.self, SchemaV2.self]` (append — never remove SchemaV1). Set `stages` to `[v1ToV2]` and add `static let v1ToV2 = MigrationStage.custom(fromVersion: SchemaV1.self, toVersion: SchemaV2.self, willMigrate: nil, didMigrate: nil)`. Use `.custom` with nil closures (NOT `.lightweight`) to sidestep the iOS 17.0–17.3 SchemaMigrationPlan bug FB13812722 per RESEARCH Pitfall P2-04; keep the explanatory comment.

    Flip `Models/Expense.swift`: change the single line to `typealias Expense = SchemaV2.Expense`. No other change.

    Create `Models/Category.swift`: copy the Expense.swift docstring pattern, declare `typealias Category = SchemaV2.Category`.

    Update `MigrationTests.swift`: the ModelContainer target schema must be SchemaV2 so the plan migrates V1→V2. Change the `Schema(versionedSchema:)` used for the ModelContainer to `SchemaV2.self` while the bundled store on disk remains the v1 store — the AppMigrationPlan reference and `FetchDescriptor<Expense>` are unchanged (typealias already resolves to SchemaV2.Expense). Keep all existing #expect assertions on amount/note/currencyCode.

    Do NOT touch ModelContainer+App.swift in this task (Task 2 owns it). Do NOT add `@Attribute(.unique)` anywhere (Pitfall P2-06).
  </action>
  <verify>
    <automated>xcodebuild test -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing MyHomeTests/MigrationTests -only-testing MyHomeTests/ExpenseModelTests 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - SchemaV2.swift declares `enum SchemaV2: VersionedSchema` with `Category` @Model (monthlyBudget: Decimal?) and `Expense` @Model with `var categories: [SchemaV2.Category]`.
    - SchemaV1.swift is byte-identical to its pre-Phase-2 state (no edits).
    - MigrationPlan.swift `schemas` returns `[SchemaV1.self, SchemaV2.self]` and `stages` returns a single `MigrationStage.custom` V1→V2 stage with nil closures.
    - Models/Expense.swift contains `typealias Expense = SchemaV2.Expense`; Models/Category.swift contains `typealias Category = SchemaV2.Category`.
    - `xcodebuild test ... -only-testing MyHomeTests/MigrationTests -only-testing MyHomeTests/ExpenseModelTests` exits 0.
  </acceptance_criteria>
  <done>SchemaV2 compiles, typealiases flip cleanly, and the bundled v1 store migrates forward to SchemaV2 with seed data intact (MigrationTests + ExpenseModelTests green).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Idempotent category seeding + model-level test suite</name>
  <files>MyHomeApp/Persistence/ModelContainer+App.swift, MyHomeTests/CategorySeedTests.swift, MyHomeTests/CategoryCRUDTests.swift, MyHomeTests/ExpenseCategoryTests.swift, MyHomeTests/BudgetModelTests.swift</files>
  <read_first>
    - MyHomeApp/Persistence/ModelContainer+App.swift (the file being modified — Schema init line + container init + return)
    - MyHomeTests/ExpenseModelTests.swift (the analog for makeContainer + @MainActor @Test patterns — copy makeContainer exactly, add Category.self to the model list)
    - .planning/phases/02-categories-tags-budgets/02-PATTERNS.md § "ModelContainer+App.swift update", § "CategorySeedTests.swift", § "CategoryCRUDTests.swift", § "BudgetCalculatorTests.swift" (test code + the seedCategoriesIfNeeded body with all 14 entries)
    - .planning/phases/02-categories-tags-budgets/02-UI-SPEC.md § "Category Taxonomy Reference" (the 14 exact name/symbol/sortOrder rows — source of truth for the seed)
    - .planning/phases/02-categories-tags-budgets/02-RESEARCH.md § Pattern 2, § Pitfall P2-03 (idempotent empty-check seeding)
  </read_first>
  <behavior>
    - CategorySeedTests/seedsOnEmptyStore: calling seedCategoriesIfNeeded on an empty store inserts exactly 14 categories.
    - CategorySeedTests/seedIsIdempotent: calling seedCategoriesIfNeeded twice leaves exactly 14 categories (no duplicates).
    - CategoryCRUDTests/deleteNullifiesExpenseLink: deleting a Category that an Expense references leaves that expense's `categories` empty (nullify, D2-04).
    - ExpenseCategoryTests: an expense can have a category assigned (`expense.categories = [cat]`) and cleared (`expense.categories = []`) and both states persist.
    - BudgetModelTests: a Category's `monthlyBudget` Decimal value stores and round-trips through a fetch unchanged; nil monthlyBudget round-trips as nil.
  </behavior>
  <action>
    Update `ModelContainer+App.swift`: change the Schema init to `Schema(versionedSchema: SchemaV2.self)`. After `ModelContainer(...)` is created and before returning, call `try seedCategoriesIfNeeded(context: container.mainContext)`. Add `seedCategoriesIfNeeded(context:)` as an `internal` (NOT private — must be callable from tests) `@MainActor` file-level function: fetch `FetchDescriptor<Category>()` with `fetchLimit = 1`, `guard existing.isEmpty else { return }`, then build the 14 predefined categories from the exact name/symbol/sortOrder table in the UI-SPEC Category Taxonomy Reference (Groceries/cart/0 … Misc/tray/13), `context.insert` each, and `try context.save()` (CR-01 explicit save). Keep the comment that this is idempotent and runs once.

    Create `MyHomeTests/CategorySeedTests.swift`: `@MainActor struct CategorySeedTests` with a `makeContainer()` copied from ExpenseModelTests but listing BOTH `Expense.self, Category.self`. Add `@Test` cases `seedsOnEmptyStore` (expect 14 after one call) and `seedIsIdempotent` (call twice, expect 14). Call `seedCategoriesIfNeeded(context:)` directly.

    Create `MyHomeTests/CategoryCRUDTests.swift` (same @MainActor + makeContainer with Category.self): `@Test` for add (insert + save + fetch count), rename (mutate name + save + refetch), delete-nullify (`deleteNullifiesExpenseLink` per PATTERNS — insert cat + expense linked, delete cat, expect `fetched.first?.categories.isEmpty == true`), and uniqueness-by-fetch (insert "Food", fetch by lowercased name predicate, expect one match — demonstrates the lookup-before-insert path, no @Attribute(.unique)).

    Create `MyHomeTests/ExpenseCategoryTests.swift` (same harness): `@Test` assign-category (set `expense.categories = [cat]`, save, refetch, expect first category id matches) and clear-category (set `= []`, save, refetch, expect empty).

    Create `MyHomeTests/BudgetModelTests.swift` (same harness): `@Test` budget store/retrieve (set `category.monthlyBudget = Decimal(15000)`, save, refetch, expect `== Decimal(15000)` and `type(of:) == Decimal.self`) and nil-budget round-trip (expect nil after refetch).

    All new test files: `import Testing / SwiftData / Foundation / @testable import MyHome`, `@MainActor` on the struct.
  </action>
  <verify>
    <automated>xcodebuild test -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing MyHomeTests/CategorySeedTests -only-testing MyHomeTests/CategoryCRUDTests -only-testing MyHomeTests/ExpenseCategoryTests -only-testing MyHomeTests/BudgetModelTests 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - ModelContainer+App.swift uses `Schema(versionedSchema: SchemaV2.self)` and calls `try seedCategoriesIfNeeded(context: container.mainContext)` before returning.
    - `seedCategoriesIfNeeded` is `internal` (callable from tests), gates on `FetchDescriptor<Category>` `fetchLimit = 1` empty-check, inserts exactly 14 categories matching the UI-SPEC taxonomy table, and calls `try context.save()`.
    - `xcodebuild test ... -only-testing MyHomeTests/CategorySeedTests -only-testing MyHomeTests/CategoryCRUDTests -only-testing MyHomeTests/ExpenseCategoryTests -only-testing MyHomeTests/BudgetModelTests` exits 0.
    - No `@Attribute(.unique)` anywhere in SchemaV2.
  </acceptance_criteria>
  <done>Seeding is idempotent (14 categories, no duplicates), category delete nullifies expense links, expense category assign/clear and budget storage all verified green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| on-disk v1 store → SchemaV2 migration | Existing user expense data crosses the migration; primary integrity risk |
| (no network / no auth / no IPC) | 100% local single-household app, no backend, CloudKit not enabled this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering (data loss) | V1→V2 migration | mitigate | Additive-only SchemaV2 (never mutate SchemaV1, P2-01); MigrationStage.custom nil-closures avoids FB13812722 (P2-04); MigrationTests opens the bundled v1 store and asserts seed data intact |
| T-02-02 | Tampering | seedCategoriesIfNeeded cold-launch / reinstall | mitigate | Idempotent FetchDescriptor(fetchLimit:1) empty-check before insert (P2-03) — relaunch never duplicates |
| T-02-03 | Tampering | Category.monthlyBudget Decimal storage | accept (UI guard later) | Stored as Decimal (never Double, Pitfall 17); range guard `abs < 1e9` is enforced at the EditBudgetSheet input boundary in plan 04, not at the model |
| T-02-SC | Tampering | npm/pip/cargo installs | n/a | No package installs this phase — all first-party Apple frameworks (RESEARCH Package Legitimacy Audit: not applicable) |

No HIGH threats. Local-only app: no injection/SSRF/authz/session surface. Primary risk is migration data loss, mitigated by additive-only SchemaV2 + the bundled-store MigrationTests gate.
</threat_model>

<verification>
Full suite green at plan close: `xcodebuild test -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' 2>&1 | tail -30`. All Phase 1 tests (ExpenseModelTests, MigrationTests) remain green under SchemaV2.
</verification>

<success_criteria>
- 14 predefined categories seed on first launch; relaunch does not duplicate (EXP-04).
- Category add/rename/delete works at the model layer; delete nullifies expense links (EXP-05).
- Expense carries an optional to-many category relationship; assign and clear both persist (EXP-06).
- Category stores a `monthlyBudget: Decimal?` that round-trips (EXP-07).
- Bundled v1 store migrates forward to SchemaV2 with all expense data intact.
</success_criteria>

<output>
Create `.planning/phases/02-categories-tags-budgets/02-01-SUMMARY.md` when done.
</output>
