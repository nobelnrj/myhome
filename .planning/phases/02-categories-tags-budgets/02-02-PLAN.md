---
phase: 02-categories-tags-budgets
plan: 02
type: tdd
wave: 2
depends_on: ["02-01"]
files_modified:
  - MyHomeApp/Support/BudgetCalculator.swift
  - MyHomeTests/BudgetCalculatorTests.swift
autonomous: true
requirements: [EXP-08, EXP-09]
must_haves:
  truths:
    - "Budget progress reports ₹-remaining, % used, and a color threshold that shifts at 80% and 100% (D2-09)"
    - "Month spend is aggregated per category from a fetched expense array (in-memory, no #Predicate aggregation)"
    - "Uncategorized spend is summed into its own bucket and excluded from per-category budget math (D2-08)"
    - "Month boundaries are computed in the user's timezone so UTC-stored expense dates fall in the correct month (P2-05)"
  artifacts:
    - path: "MyHomeApp/Support/BudgetCalculator.swift"
      provides: "BudgetProgressData value type + BudgetColor enum + BudgetCalculator pure static aggregation"
      contains: "struct BudgetProgressData"
    - path: "MyHomeTests/BudgetCalculatorTests.swift"
      provides: "Pure-function tests for thresholds, aggregation, uncategorized bucket"
      contains: "BudgetCalculatorTests"
  key_links:
    - from: "BudgetProgressData.colorThreshold"
      to: "BudgetColor"
      via: "fractionUsed threshold mapping"
      pattern: "colorThreshold"
    - from: "BudgetCalculator.monthlySpend"
      to: "PersistentIdentifier keyed totals"
      via: "in-memory reduce over expense.categories.first"
      pattern: "monthlySpend"
---

<objective>
Build the pure budget-math layer: `BudgetProgressData` (remaining / fractionUsed / colorThreshold), the `BudgetColor` enum, and `BudgetCalculator` static methods (`monthlySpend` per-category aggregation, `monthBoundaries` timezone-correct date math, and an uncategorized-bucket sum). All pure value types — no SwiftData, no SwiftUI — so they are unit-tested with no ModelContainer.

Purpose: EXP-08 (progress + color thresholds) and EXP-09 (month-grouped aggregation, uncategorized bucket) need correct, tested math BEFORE any view consumes it. Isolating this as a pure-function TDD plan means the Budgets surface (plan 04) builds on verified arithmetic.
Output: BudgetCalculator.swift, BudgetCalculatorTests.swift.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-categories-tags-budgets/02-RESEARCH.md
@.planning/phases/02-categories-tags-budgets/02-PATTERNS.md
@.planning/phases/02-categories-tags-budgets/02-01-SUMMARY.md
</context>

<feature>
  <name>BudgetCalculator + BudgetProgressData pure budget math</name>
  <files>MyHomeApp/Support/BudgetCalculator.swift, MyHomeTests/BudgetCalculatorTests.swift</files>
  <read_first>
    - MyHomeApp/Support/Decimal+INR.swift (the pure-utility analog — `import Foundation` only, no SwiftUI/SwiftData)
    - MyHomeTests/ExpenseModelTests.swift § currencyFormatting (the pure-function test pattern requiring no ModelContainer; § makeContainer for the aggregation tests that DO need PersistentIdentifier)
    - .planning/phases/02-categories-tags-budgets/02-PATTERNS.md § "BudgetCalculator.swift", § "BudgetCalculatorTests.swift" (BudgetProgressData shape, monthlySpend/monthBoundaries signatures)
    - .planning/phases/02-categories-tags-budgets/02-RESEARCH.md § Pattern 3, § "Budget progress computation" code example, § Pitfall P2-05 (timezone month boundaries)
    - .planning/phases/02-categories-tags-budgets/02-UI-SPEC.md § Color (D2-09 thresholds: <0.80 normal, 0.80–0.99 warning, ≥1.00 over)
  </read_first>
  <behavior>
    - colorThresholdNormal: budget 1000, spent 700 → fractionUsed 0.7, colorThreshold == .normal, remaining == 300.
    - colorThresholdWarning: budget 1000, spent 850 → fractionUsed 0.85, colorThreshold == .warning.
    - colorThresholdOverBudget: budget 1000, spent 1200 → fractionUsed 1.2, colorThreshold == .overBudget, remaining == -200.
    - exactly80: budget 1000, spent 800 → fractionUsed 0.8, colorThreshold == .warning (boundary is inclusive at .warning).
    - exactly100: budget 1000, spent 1000 → colorThreshold == .overBudget (boundary inclusive at .over).
    - noBudget: budget nil → fractionUsed nil, remaining nil, colorThreshold == .normal.
    - zeroBudgetGuard: budget 0 → fractionUsed nil (no divide-by-zero).
    - monthlyAggregation: expenses across two categories sum into per-category PersistentIdentifier totals; refund (negative amount) reduces its category's total.
    - uncategorizedBucket: an expense with empty `categories` is summed into the uncategorized total and is NOT present in any per-category key.
    - monthBoundaries: for a given year/month DateComponents, start is first instant of the month and end is last second, computed with `Calendar.current` + `timeZone = .current`.
  </behavior>
  <implementation>
    RED → write BudgetCalculatorTests.swift first; each #expect must fail before the type exists. GREEN → implement BudgetCalculator.swift to pass. REFACTOR only if needed.

    BudgetCalculator.swift (`import Foundation` ONLY — no SwiftUI, no SwiftData):
    - `enum BudgetColor { case normal, warning, overBudget }`.
    - `struct BudgetProgressData` with `let category: Category`, `let spent: Decimal`, `let budget: Decimal?`; computed `remaining: Decimal?` (`budget - spent`, nil if no budget), `fractionUsed: Double?` (`Double(truncating: (spent / b) as NSDecimalNumber)`, nil when budget nil or `b <= 0`), `colorThreshold: BudgetColor` (nil fraction → .normal; `>= 1.0` → .overBudget; `>= 0.8` → .warning; else .normal).
    - `struct BudgetCalculator` with `static func monthlySpend(for expenses: [Expense], categories: [Category]) -> [PersistentIdentifier: Decimal]` (iterate, take `expense.categories.first`, accumulate `totals[key, default: .zero] += expense.amount`; expenses with empty categories are skipped here), `static func uncategorizedSpend(for expenses: [Expense]) -> Decimal` (sum of expenses whose `categories.isEmpty`), and `static func monthBoundaries(for month: DateComponents) -> (start: Date, end: Date)?` using `var cal = Calendar.current; cal.timeZone = .current` then `cal.date(from: month)` and `cal.date(byAdding: DateComponents(month: 1, second: -1), to: start)` per P2-05.

    Since BudgetProgressData holds a `Category` `@Model`, the threshold/remaining/fraction tests instantiate a real `Category` via an in-memory ModelContainer (copy makeContainer from ExpenseModelTests, list Expense.self + Category.self) — these are still fast. The monthlyAggregation/uncategorized tests need the container for PersistentIdentifier. Do NOT add a tuple-based secondary init unless the @Model instantiation proves problematic at runtime.
  </implementation>
</feature>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure in-memory arithmetic over already-fetched local data; no network/auth/IPC |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering | Decimal→Double fractionUsed conversion | accept | Double used only for the visual bar fraction (never for money math); remaining/spent stay Decimal. No precision risk to stored amounts. |
| T-02-05 | Tampering | divide-by-zero on zero/negative budget | mitigate | `fractionUsed` guards `b > 0`, returns nil otherwise (zeroBudgetGuard test) |
| T-02-06 | Tampering | UTC vs local month boundary misclassification | mitigate | monthBoundaries computes with `Calendar.current` + `timeZone = .current` per Pitfall P2-05; verified by monthBoundaries test |
| T-02-SC | Tampering | package installs | n/a | No installs — first-party Foundation only |

No HIGH threats. Pure-function module on local data; primary risks are arithmetic edge cases, all covered by tests.
</threat_model>

<verification>
`xcodebuild test -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing MyHomeTests/BudgetCalculatorTests 2>&1 | tail -25` exits 0. Full suite green: `xcodebuild test -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' 2>&1 | tail -30`.
</verification>

<success_criteria>
- Color thresholds shift correctly at the 0.80 and 1.00 boundaries (EXP-08).
- Per-category month aggregation and the uncategorized bucket are correct, refunds reduce spend (EXP-09, D2-08).
- Month boundaries are timezone-correct (P2-05).
- BudgetCalculator.swift imports Foundation only (no SwiftUI/SwiftData).
</success_criteria>

<output>
Create `.planning/phases/02-categories-tags-budgets/02-02-SUMMARY.md` when done.
</output>
