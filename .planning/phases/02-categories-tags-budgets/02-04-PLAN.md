---
phase: 02-categories-tags-budgets
plan: 04
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - MyHomeApp/Features/Budgets/BudgetProgressView.swift
  - MyHomeApp/Features/Budgets/BudgetCategoryCard.swift
  - MyHomeApp/Features/Budgets/EditBudgetSheet.swift
  - MyHomeApp/Support/Date+Display.swift
autonomous: true
requirements: [EXP-07, EXP-08]
must_haves:
  truths:
    - "A budget card shows the category, its month spend, a ₹-remaining + % bar, and the bar shifts color at 80% (warning) and 100% (over) — D2-09"
    - "Color is never the sole signal — every threshold pairs the bar with ₹-remaining and % text"
    - "A category with no budget shows 'No budget set' and an Edit affordance"
    - "The user can set a positive monthly budget via the reused DecimalKeypad and remove it with confirmation (EXP-07)"
    - "A month label formats as 'May 2026' for the pager"
  artifacts:
    - path: "MyHomeApp/Features/Budgets/BudgetProgressView.swift"
      provides: "Pure progress bar mapping BudgetColor → fill, capped at 100% width, with No-budget branch"
      contains: "struct BudgetProgressView"
    - path: "MyHomeApp/Features/Budgets/BudgetCategoryCard.swift"
      provides: "Card: icon + name + spent + edit pencil + progress + remaining/%"
      contains: "struct BudgetCategoryCard"
    - path: "MyHomeApp/Features/Budgets/EditBudgetSheet.swift"
      provides: "Set/clear monthlyBudget via DecimalKeypad with validation guard"
      contains: "struct EditBudgetSheet"
    - path: "MyHomeApp/Support/Date+Display.swift"
      provides: "formattedAsMonthYear() -> String"
      contains: "formattedAsMonthYear"
  key_links:
    - from: "BudgetProgressView.fillColor"
      to: "BudgetColor"
      via: "colorThreshold → SwiftUI Color mapping"
      pattern: "case \\.overBudget"
    - from: "EditBudgetSheet.saveBudget"
      to: "category.monthlyBudget"
      via: "validated Decimal assign + explicit save"
      pattern: "monthlyBudget ="
    - from: "BudgetCategoryCard"
      to: "EditBudgetSheet"
      via: ".sheet on Edit pencil"
      pattern: "EditBudgetSheet"
---

<objective>
Build the budget visualization and editing leaf components that the Budgets surface composes: `BudgetProgressView` (color-threshold bar), `BudgetCategoryCard` (full card row), `EditBudgetSheet` (set/clear monthly budget), and the `formattedAsMonthYear()` date helper for the month pager.

Purpose: EXP-08 (₹-remaining + % bar, color shift at 80%/100%, color never sole signal — D2-09) and EXP-07 (set monthly budget). These are self-contained display/edit components consuming the verified `BudgetProgressData`/`BudgetColor` from plan 02. Isolating them from the BudgetsView container (plan 05) keeps each plan small and lets the card/sheet/bar be built and previewed independently.
Output: BudgetProgressView.swift, BudgetCategoryCard.swift, EditBudgetSheet.swift (new), updated Date+Display.swift.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-categories-tags-budgets/02-PATTERNS.md
@.planning/phases/02-categories-tags-budgets/02-UI-SPEC.md
@.planning/phases/02-categories-tags-budgets/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: BudgetProgressView + formattedAsMonthYear date helper</name>
  <files>MyHomeApp/Features/Budgets/BudgetProgressView.swift, MyHomeApp/Support/Date+Display.swift</files>
  <read_first>
    - MyHomeApp/Features/Expenses/ExpenseRow.swift (the pure-display-component analog — let property, no @Query)
    - MyHomeApp/Support/Date+Display.swift (the file being modified — append inside the existing `extension Date`, do not create a second extension)
    - MyHomeApp/Support/Decimal+INR.swift (formattedINR for the remaining value)
    - .planning/phases/02-categories-tags-budgets/02-PATTERNS.md § "BudgetProgressView.swift" + § "Date+Display.swift update" (GeometryReader bar code, formattedAsMonthYear body)
    - .planning/phases/02-categories-tags-budgets/02-UI-SPEC.md § "Screen 3 → BudgetCategoryCard layout Row 2/3", § Color (BudgetColor → Color mapping, 8pt track), § Accessibility (bar .accessibilityElement(children: .ignore))
    - .planning/phases/02-categories-tags-budgets/02-02-SUMMARY.md (BudgetProgressData/BudgetColor API as built)
  </read_first>
  <action>
    Create `BudgetProgressView.swift` (`import SwiftUI`): `struct BudgetProgressView { let data: BudgetProgressData }` — pure value, no @Bindable/@Query. Private `fillColor: Color` mapping `data.colorThreshold`: `.normal → Color.accentColor`, `.warning → Color(.systemOrange)`, `.overBudget → Color(.systemRed)`. Body: if `data.budget == nil`, render `Text("No budget set")` (`.subheadline`, `.tertiary`). Otherwise a `VStack(alignment: .leading, spacing: 8)` containing: (a) the bar — `GeometryReader` + `ZStack(alignment: .leading)` with a track `RoundedRectangle(cornerRadius: 4).fill(Color(.secondarySystemBackground)).frame(height: 8)` and, when `data.fractionUsed != nil`, a fill `RoundedRectangle(cornerRadius: 4).fill(fillColor).frame(width: min(CGFloat(fraction), 1.0) * geo.size.width, height: 8).animation(.easeInOut(duration: 0.3), value: fraction)`; wrap with `.frame(height: 8)` and `.accessibilityElement(children: .ignore)`; (b) an `HStack` with the remaining/over text and the "X% used" text. Remaining text: when `remaining >= 0` show "₹{remaining.formattedINR()} remaining" (color per threshold: normal → `.secondary`, warning → `.systemOrange`, over → `.systemRed`), when `remaining < 0` show "₹{abs(remaining).formattedINR()} over budget" (`.systemRed`). Percent text: "100%+" when `fractionUsed >= 1.0`, else "\(Int(fractionUsed*100))% used" (`.subheadline`, `.secondary`). Pair color with text always (D2-09). Cap bar width at full track (never overflow).

    Update `Date+Display.swift`: append a `formattedAsMonthYear() -> String` method INSIDE the existing `extension Date` — `DateFormatter` with `dateFormat = "MMMM yyyy"`, `locale = .current`, `timeZone = .current`, return `string(from: self)`.
  </action>
  <verify>
    <automated>xcodebuild build -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - BudgetProgressView.swift maps all three BudgetColor cases and caps the fill at `min(fraction, 1.0) * width`.
    - The "No budget set" branch renders when `data.budget == nil`; the bar branch always pairs with ₹-remaining + % text.
    - Date+Display.swift contains `formattedAsMonthYear()` inside the single existing `extension Date` block.
    - `xcodebuild build` exits 0.
  </acceptance_criteria>
  <done>Progress bar renders color-shifted fill capped at 100% with paired text; no-budget branch present; month-year formatter added.</done>
</task>

<task type="auto">
  <name>Task 2: BudgetCategoryCard + EditBudgetSheet</name>
  <files>MyHomeApp/Features/Budgets/BudgetCategoryCard.swift, MyHomeApp/Features/Budgets/EditBudgetSheet.swift</files>
  <read_first>
    - MyHomeApp/Features/Expenses/ExpenseRow.swift (card HStack + accessibilityElement(children:.combine) analog)
    - MyHomeApp/Features/Expenses/EditExpenseView.swift (the EditBudgetSheet structural analog — @Bindable, amountSection with DecimalKeypadView, toolbar, confirmationDialog, save guard, lines 14–264)
    - MyHomeApp/Features/Expenses/DecimalKeypadView.swift (reused for budget entry — no system keyboard, Pitfall 6)
    - .planning/phases/02-categories-tags-budgets/02-PATTERNS.md § "BudgetCategoryCard.swift" + § "EditBudgetSheet.swift" + § Shared Patterns (CR-01 save, amount validation guard, confirmationDialog)
    - .planning/phases/02-categories-tags-budgets/02-UI-SPEC.md § "Screen 3 BudgetCategoryCard layout", § "Screen 7 EditBudgetSheet", § Copywriting Contract (Set Budget / Save Budget / Remove Budget / "for [Category]" copy), § Accessibility ("Edit budget for ..." label)
  </read_first>
  <action>
    Create `BudgetCategoryCard.swift` (`import SwiftUI`): `struct BudgetCategoryCard { let progressData: BudgetProgressData; @State private var showEditBudget = false }`. Body: `VStack(alignment: .leading, spacing: 8)` — Row 1 `HStack`: category SF Symbol (`progressData.category.symbolName`, 28×28 frame, `.body`, `.secondary`, `.accessibilityHidden(true)`), name (`.body`, lineLimit 1), Spacer, spent `Text(progressData.spent.formattedINR())` (`.subheadline`, `.secondary`), Edit pencil Button (`pencil`, 44pt touch target, `.secondary`, `.accessibilityLabel("Edit budget for \(progressData.category.name ?? "category")")`) → `showEditBudget = true`. Row 2: `BudgetProgressView(data: progressData)`. Card chrome: `.padding(16)`, `.secondarySystemBackground`, `RoundedRectangle(cornerRadius: 12)`, `.shadow(color: .black.opacity(0.04), radius: 2, y: 1)`, `.accessibilityElement(children: .combine)`, `.sheet(isPresented: $showEditBudget) { EditBudgetSheet(category: progressData.category) }`.

    Create `EditBudgetSheet.swift` (`import SwiftUI` + `import SwiftData`): `struct EditBudgetSheet { @Bindable var category: Category; @Environment(\.modelContext) private var context; @Environment(\.dismiss) private var dismiss; @State amountString/amountShakeOffset/amountIsError/showRemoveConfirmation }`. Body `NavigationStack` with `.navigationTitle("Set Budget")` inline; show the category name as "for \(category.name ?? "")" (`.subheadline`, `.secondary`) below the title. amountSection: large centered `displayAmount` (`.largeTitle`, bold) + `DecimalKeypadView(displayString: $amountString)` — NO sign toggle (budgets positive only); pre-fill from existing `category.monthlyBudget`. Toolbar: `.cancellationAction` "Cancel" (dismiss, no save); `.confirmationAction` "Save Budget" (`.accentColor`, disabled until parsed amount > 0). `removeBudgetSection`: full-width "Remove Budget" `.role(.destructive)` `.tint(.systemRed)` button rendered ONLY when `category.monthlyBudget != nil` → sets `showRemoveConfirmation = true`. `.confirmationDialog("Remove Budget?", ...)` with "Remove Budget" destructive (sets `category.monthlyBudget = nil`, `try? context.save()`, dismiss) and "Cancel". `saveBudget()`: guard parsed `amount > 0` (else shake) and `abs(amount) < Decimal(1_000_000_000)` (else shake), set `category.monthlyBudget = amount`, `try context.save()` (CR-01, assertionFailure on catch), dismiss.
  </action>
  <verify>
    <automated>xcodebuild test -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing MyHomeTests/BudgetModelTests -only-testing MyHomeTests/BudgetCalculatorTests 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - BudgetCategoryCard.swift renders icon + name + spent + Edit pencil + BudgetProgressView and presents EditBudgetSheet via `.sheet`.
    - EditBudgetSheet.swift uses `@Bindable var category: Category`, reuses DecimalKeypadView (no sign toggle), guards `amount > 0 && abs(amount) < 1_000_000_000`, sets `category.monthlyBudget` with explicit `try context.save()`, and offers a confirmed "Remove Budget" only when a budget exists.
    - `xcodebuild test ... -only-testing MyHomeTests/BudgetModelTests -only-testing MyHomeTests/BudgetCalculatorTests` exits 0.
  </acceptance_criteria>
  <done>Budget card composes the bar + spent + edit affordance; EditBudgetSheet sets a validated positive budget and removes it with confirmation, persisting via explicit save.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user keypad input → category.monthlyBudget | Untrusted numeric input crosses into stored Decimal |
| category name → card / sheet display | Display only, no execution |
| (no network / no auth) | Local-only app |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09 | Tampering | budget amount overflow / non-positive | mitigate | `saveBudget` guards `amount > 0` and `abs(amount) < Decimal(1_000_000_000)` (same guard as Phase 1 expense amount, V5 input validation); Decimal not Double |
| T-02-10 | Tampering | budget write / remove persistence | mitigate | Explicit `try context.save()` (CR-01) on set and remove; remove gated behind confirmationDialog to prevent accidental loss |
| T-02-11 | Spoofing/Tampering | category name in card/sheet | mitigate | Plain `Text` rendering — never AttributedString(markdown:) |
| T-02-SC | Tampering | package installs | n/a | No installs — first-party SwiftUI/SwiftData; reuses existing DecimalKeypadView |

No HIGH threats. Local-only; budget input validated at the entry boundary with the established Decimal range guard.
</threat_model>

<verification>
Full suite green: `xcodebuild test -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' 2>&1 | tail -30`. (Visual color-threshold rendering is human-verified in plan 05's checkpoint, where cards appear on the live Budgets surface.)
</verification>

<success_criteria>
- Budget progress bar shows ₹-remaining + % and shifts color at 80%/100%, color paired with text (EXP-08, D2-09).
- User can set a positive monthly budget and remove it with confirmation (EXP-07).
- Month-year label helper exists for the pager.
</success_criteria>

<output>
Create `.planning/phases/02-categories-tags-budgets/02-04-SUMMARY.md` when done.
</output>
