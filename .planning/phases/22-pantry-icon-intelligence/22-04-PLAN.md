---
phase: 22-pantry-icon-intelligence
plan: 04
type: execute
wave: 4
depends_on: ["22-01", "22-02", "22-03"]
files_modified:
  - MyHomeTests/PantryIconFixture.swift
  - MyHomeTests/PantryIconEvalTests.swift
  - MyHomeTests/PantryIconStructuralTests.swift
  - MyHomeApp/Features/Kitchen/PantryIconGalleryView.swift
  - MyHomeApp/RootView.swift
  - MyHome.xcodeproj/project.pbxproj
autonomous: false
requirements: [ICON-01, ICON-02, ICON-03]
must_haves:
  truths:
    - "Every one of the 17 category tiles is confirmed to actually render on the simulator — no empty tile, in light and dark."
    - "The names that motivated the phase (kitchen tissue, fabric softener, dish scrubber, toilet cleaner, aluminium foil, paper napkins) resolve to meaningful categories rather than the neutral bag."
    - "Every name the old keyword table got right still gets the same answer — 100% on the non-regression set."
    - "The model accuracy suite is runnable on demand but cannot redden a routine full-suite run."
    - "The full test suite passes with -parallel-testing-enabled NO."
  artifacts:
    - path: "MyHomeTests/PantryIconFixture.swift"
      provides: "Committed household-weighted name→expected-category reference dataset"
      contains: "motivating"
    - path: "MyHomeTests/PantryIconEvalTests.swift"
      provides: "Opt-in model accuracy suite (>=90% overall, 100% non-regression)"
      contains: "PANTRY_ICON_EVAL"
    - path: "MyHomeTests/PantryIconStructuralTests.swift"
      provides: "Always-on deterministic gates: symbol totality, graceful degradation, no leakage"
      contains: "PantryCategory.allCases"
    - path: "MyHomeApp/Features/Kitchen/PantryIconGalleryView.swift"
      provides: "DEBUG -iconGallery screen rendering all 17 tiles for screenshot verification"
      contains: "PantryCategory.allCases"
  key_links:
    - from: "MyHomeApp/RootView.swift"
      to: "PantryIconGalleryView"
      via: "DEBUG-only -iconGallery launch argument, mirroring the -startTab hook"
      pattern: "iconGallery"
---

<objective>
Prove the phase actually works, along the exact dimensions AI-SPEC §5.2 names — and confirm on a real simulator that all 17 symbols render, because no test can prove an SF Symbol exists.

The 20-03 empty-tile bug survived a passing unit test. This plan is the answer to that: the deterministic guarantees (symbol totality, graceful degradation, no leakage) are always-on blocking tests, the non-deterministic model accuracy is an explicitly-invoked suite so a single flaky classification cannot redden an unrelated run, and the one thing neither can cover — does this string draw pixels — is a screenshot.

Purpose: close ICON-01, ICON-02 and ICON-03 with evidence.
Output: a committed fixture, two test suites, a DEBUG tile gallery, and light+dark screenshot evidence in the phase directory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-pantry-icon-intelligence/22-AI-SPEC.md
@.planning/phases/22-pantry-icon-intelligence/22-01-SUMMARY.md
@.planning/phases/22-pantry-icon-intelligence/22-02-SUMMARY.md
@.planning/phases/22-pantry-icon-intelligence/22-03-SUMMARY.md

@MyHomeApp/RootView.swift
@MyHomeApp/Features/Kitchen/PantryCategory.swift
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reference fixture + always-on structural gates + opt-in accuracy suite</name>
  <files>MyHomeTests/PantryIconFixture.swift, MyHomeTests/PantryIconStructuralTests.swift, MyHomeTests/PantryIconEvalTests.swift, MyHome.xcodeproj/project.pbxproj</files>
  <behavior>
    Structural (always on, deterministic, blocking):
    - Every `PantryCategory` case yields a non-empty, whitespace-free symbol and a colour — the mapping is total.
    - A resolver with a nil classifier answers every fixture name from the keyword table or the neutral tile, never an empty symbol and never a crash.
    - A resolver whose classifier always throws leaves every fixture name on its keyword answer and writes nothing to the cache.
    - Every name in the non-regression set resolves, through the keyword path alone, to the SAME symbol it produced before Phase 22.
    - No-leakage: `PantryItem` has no icon-bearing property, and the sync snapshot round-trip carries no icon data.

    Eval (opt-in, non-deterministic, non-blocking):
    - Overall exact-category accuracy across the fixture is >= 90%.
    - Accuracy on the non-regression subset is 100%.
  </behavior>
  <action>
Create `MyHomeTests/PantryIconFixture.swift` holding `enum PantryIconFixture` with `static let cases: [(name: String, expected: PantryCategory)]`, split into three clearly-MARKed groups so failures are legible.

MOTIVATING (the names that caused this phase — AI-SPEC §5.1): kitchen tissue → paperDisposable, fabric softener → cleaning, dish scrubber → cleaning, toilet cleaner → cleaning, aluminium foil → paperDisposable, paper napkins → paperDisposable.

NON-REGRESSION (must stay exactly right — the old table handled these): milk → dairy, eggs → eggs, filter coffee → brew, cooking oil → oilFat, sona masoori rice → grainStaple, atta → grainStaple, toor dal → grainStaple, onions → produce, sugar → spice, dishwash liquid → cleaning. Expose this group separately as `static let nonRegression` since it carries a stricter threshold.

AMBIGUOUS / ADVERSARIAL, with the accepted answer recorded per AI-SPEC §5.1: ghee → oilFat (NOT dairy), curd → dairy, coconut oil → oilFat (reads as produce but is a fat), green tea → brew, bournvita → beverage (documented choice: a malt drink mix is prepared as a beverage; brew is reserved for tea/coffee leaves and grounds), baby wipes → personalCare (documented choice: intended use is personal care, not general paper disposables), "" → other, "zqx" → other. Weight the fixture toward this household's Indian-staple vocabulary rather than generic US grocery terms — add at least six more such names (e.g. rava, poha, jaggery, curry leaves, coconut, tamarind) with their accepted categories.

Put the two documented tie-break rationales (bournvita, baby wipes) in comments in the file, so a future disagreement is a decision to revisit rather than a bug to chase.

Create `MyHomeTests/PantryIconStructuralTests.swift` for the deterministic gates listed above. These run on every invocation and MUST be green. Drive the degradation tests through a resolver built with a nil classifier and with a `FakePantryIconClassifier` set to throw, over an isolated `UserDefaults` suite. For the no-leakage test, assert reflectively that `PantryItem` exposes no property whose name contains "symbol", "icon" or "color", and grep-equivalent assert that the kitchen sync DTOs carry no such key — mirror however 20-02's round-trip tests reach the DTOs rather than inventing a new access path.

Create `MyHomeTests/PantryIconEvalTests.swift` for the accuracy suite. Gate the whole suite on an environment variable — `.enabled(if: ProcessInfo.processInfo.environment["PANTRY_ICON_EVAL"] == "1")` — so a routine run skips it entirely and a single flaky classification can never redden an unrelated suite (AI-SPEC §5.2). Inside, use the real `FoundationModelsPantryIconClassifier` when available, skip with a clear message when not, classify every fixture name, and assert the two thresholds. On failure, print the full name → expected → actual table so the miss is diagnosable without a rerun.

pbxproj: four manual edits per new test file into the MyHomeTests target; no synchronized groups exist.
  </action>
  <verify>
    <automated>xcodebuild test -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' -parallel-testing-enabled NO -only-testing:MyHomeTests/PantryIconStructuralTests 2>&1 | tail -20</automated>
  </verify>
  <done>Structural suite green; the eval suite compiles and reports as skipped without `PANTRY_ICON_EVAL=1`, and its opt-in invocation is recorded in the SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 2: DEBUG icon gallery, screenshot all 17 tiles, replace any symbol that renders empty</name>
  <files>MyHomeApp/Features/Kitchen/PantryIconGalleryView.swift, MyHomeApp/Features/Kitchen/PantryCategory.swift, MyHomeApp/RootView.swift, MyHome.xcodeproj/project.pbxproj</files>
  <action>
Create `MyHomeApp/Features/Kitchen/PantryIconGalleryView.swift`, wrapped entirely in `#if DEBUG`. It renders a scrolling grid over `PantryCategory.allCases`, each entry showing the same `IconTile(symbol:color:size:38,cornerRadius:11)` the rows use, with the category name and the raw symbol string underneath. Rendering the literal symbol string next to the tile is what makes an empty tile unambiguous in a screenshot — you can see which name failed, not just that something is blank. Background `DesignTokens.bgCanvas` so both themes look like the real surface.

Wire it into `RootView.swift` behind a `#if DEBUG` check for the `-iconGallery` launch argument, mirroring the existing `-startTab` hook style exactly — when present, show `PantryIconGalleryView` instead of the normal root content. simctl cannot navigate the app, so a launch argument is the only way to reach a deterministic screenshot of all 17 tiles.

Then run the verify loop: build, install, and launch on the iPhone 17 simulator with `-iconGallery`, once in light appearance and once in dark, capturing both via `xcrun simctl io booted screenshot`. Save them as `.planning/phases/22-pantry-icon-intelligence/22-04-light-icon-gallery.png` and `22-04-dark-icon-gallery.png`, matching the naming used by the 20-03/20-04 evidence in the phase directories.

READ the screenshots yourself before handing them over. For any tile that is blank, the symbol name does not exist — SwiftUI draws nothing and raises no error, which is exactly the 20-03 failure. Replace that entry in `PantryCategory.presentation` with a symbol you can confirm renders, re-run the loop, and re-capture. Prefer a long-established generic symbol over a clever specific one; a correct-looking tile beats a perfect-sounding name that draws nothing. Record every substitution in the SUMMARY with the rejected name, so the next person does not retry it.

Do not modify anything outside the presentation table's symbol strings — colours, categories, keyword rules and the resolver all stay as shipped by 22-01 through 22-03.
  </action>
  <verify>
    <automated>xcodebuild test -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' -parallel-testing-enabled NO 2>&1 | tail -30</automated>
  </verify>
  <done>Both gallery screenshots exist in the phase directory, all 17 tiles show a visible glyph in both themes, any substituted symbol is recorded in the SUMMARY, and the FULL test suite passes with parallel testing disabled.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — tiles render and unseen names get sensible icons</name>
  <what-built>
The on-device classifier now picks each pantry item's icon from a closed 17-category set, with the keyword table as the offline fallback and a device-local cache. All 17 tiles have been screenshotted in light and dark, the structural gates are green, and the full suite passes with parallel testing disabled.
  </what-built>
  <how-to-verify>
1. Open `.planning/phases/22-pantry-icon-intelligence/22-04-light-icon-gallery.png` and `22-04-dark-icon-gallery.png`. Confirm all 17 tiles show a glyph — no blank squares — and that each glyph is a defensible fit for its category name.
2. On the simulator or your phone, open Kitchen → Pantry and add these four items: "kitchen tissue", "fabric softener", "dish scrubber", "aluminium foil". Each should draw a neutral bag tile immediately, then upgrade in place within a few seconds to a meaningful icon. Confirm there is no spinner in the tile and no row jump when it changes.
3. Confirm the existing staples (Milk, Eggs, Filter coffee, Sona Masoori rice, Onions) look exactly as they did before this phase.
4. Settings → Apple Intelligence off (or on a device without it): relaunch, open Pantry, and confirm rows still draw instantly with the keyword icons and nothing is blank or crashing.
5. Confirm the Kitchen surface still looks right in both light and dark.
  </how-to-verify>
  <action>Present the two gallery screenshots and the verification steps above to the user, then pause. Do not proceed to the SUMMARY until the user responds. If the user names a wrong-looking tile, substitute that symbol in `PantryCategory.presentation`, re-run the Task 2 screenshot loop, and re-present.</action>
  <verify>
    <human-check>User confirms all 17 tiles render, the four unseen names upgrade to meaningful icons in place, the existing staples are unchanged, and the Apple-Intelligence-off path still draws instantly.</human-check>
  </verify>
  <done>User replies "approved", or every issue they named has been fixed and re-verified.</done>
  <resume-signal>Type "approved", or name the tiles/items that look wrong</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DEBUG launch argument → root view | A build-time-gated hook swaps the app's root content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-10 | Elevation of privilege | `-iconGallery` hook | mitigate | Entire view and its RootView call site are inside `#if DEBUG`, so the hook does not exist in a Release build — same containment as the shipped `-startTab` and `-seedSampleData` hooks. |
| T-22-11 | Information disclosure | eval suite prompts | accept | The fixture is a static list of generic grocery nouns committed to the repo; it carries no household data and runs fully on-device. |
| T-22-12 | Tampering | symbol substitution during screenshot pass | mitigate | Substitutions are confined to `PantryCategory.presentation` string literals, re-verified by re-screenshot, and each rejected name is recorded in the SUMMARY. |
| T-22-SC | Tampering | dependencies | accept | Zero new packages across the whole phase. |
</threat_model>

<verification>
- `xcodebuild test -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' -parallel-testing-enabled NO` — full suite green. Parallel testing MUST stay disabled for the authoritative run: parallel clones turn a single fatal error into hundreds of bogus 0.000s failures in unrelated suites.
- Opt-in eval, recorded in the SUMMARY with its accuracy figures: the same command with `PANTRY_ICON_EVAL=1` and `-only-testing:MyHomeTests/PantryIconEvalTests`.
- Both gallery screenshots present in the phase directory and visually clean.
- `git diff --stat` across the whole phase shows no change under `MyHomeApp/Models/`, no `Schema*` file, no `Sync*` file, and no `DesignTokens.swift` change.
</verification>

<success_criteria>
- ICON-01: unseen names resolve to meaningful categories; fixture accuracy >= 90% overall and 100% on the non-regression set.
- ICON-02: every category maps to a symbol confirmed to render in light and dark; a fake symbol is unrepresentable by construction.
- ICON-03: no icon state on `PantryItem`, none in the sync snapshot, SchemaV11 unchanged; model-unavailable degrades instantly to the keyword table.
- Full suite green with parallel testing disabled; human verification approved.
</success_criteria>

<output>
Create `.planning/phases/22-pantry-icon-intelligence/22-04-SUMMARY.md` when done. Include the measured eval accuracy figures, every symbol substitution with the rejected name, and both screenshot paths.
</output>
