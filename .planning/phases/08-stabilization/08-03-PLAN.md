---
phase: 08-stabilization
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - MyHomeTests/CategoryCRUDTests.swift
  - MyHomeApp/Persistence/Schema/SchemaV5.swift
autonomous: false
requirements: [STAB-03]
user_setup: []
must_haves:
  truths:
    - "Adding a new custom category lands at the bottom of the list at max(existing.sortOrder)+1, not the top (success criterion #3, D-05)"
    - "A regression test locks in the max+1 insertion behavior so it cannot silently regress (D-08)"
    - "The Category.init sortOrder:Int=0 default is documented as a latent footgun (defensive comment)"
    - "The live-app symptom is reproduced/confirmed before assuming a code change is needed (researcher Open Question 1)"
  artifacts:
    - path: "MyHomeTests/CategoryCRUDTests.swift"
      provides: "STAB-03 regression test asserting new category sortOrder == 14"
      contains: "STAB-03"
    - path: "MyHomeApp/Persistence/Schema/SchemaV5.swift"
      provides: "Defensive comment on Category.init sortOrder default"
      contains: "sortOrder"
  key_links:
    - from: "ManageCategoriesView.addCategory"
      to: "Category(sortOrder: max+1)"
      via: "already-correct insertion (no production change)"
      pattern: "max\\(\\) \\+ 1|max\\(\\)"
---

<objective>
STAB-03: New custom categories must append at the bottom of the list using `max(existing.sortOrder)+1`, not the top (success criterion #3, D-05).

Research finding (HIGH/MEDIUM confidence): there is NO broken production add path. `ManageCategoriesView.addCategory` (line ~191-193) already computes `(all.map(\.sortOrder).max() ?? 999) + 1` correctly, and `seedCategoriesIfNeeded` seeds 0–13 with explicit orders. The only latent footgun is `Category.init(name:symbolName:sortOrder:)` defaulting `sortOrder` to `0` (SchemaV5.swift:55) — any future caller omitting the argument would sort to the top. The reported user symptom may be stale live-store data, not an active code bug.

Per D-05, this plan does NOT invent a code change to an already-correct path. Scope = (a) a regression test that locks in max+1 (seed 0–13 → assert new == 14), (b) a defensive comment on the Category init default, (c) a manual live-app reproduction checkpoint to determine whether the symptom is a real bug or stale data.

Purpose: Guarantee correct insertion order and prevent silent regression (success criterion #3).
Output: Regression test + defensive comment + a human verdict on the live symptom.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-stabilization/08-CONTEXT.md
@.planning/phases/08-stabilization/08-RESEARCH.md
@.planning/phases/08-stabilization/08-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Regression test — new category lands at max(sortOrder)+1</name>
  <files>MyHomeTests/CategoryCRUDTests.swift</files>
  <read_first>
    - MyHomeTests/CategoryCRUDTests.swift (existing suite — `makeContainer()` in-memory helper ~15-18, the `addCategory` insert test ~21-32; note the suite's local `Cat` typealias usage)
    - MyHomeApp/Features/Budgets/ManageCategoriesView.swift lines ~175-203 (`addCategory` — the `(all.map(\.sortOrder).max() ?? 999) + 1` logic the test reproduces)
    - MyHomeApp/Persistence/ModelContainer+App.swift `seedCategoriesIfNeeded` (~89 — confirms seed orders 0–13)
  </read_first>
  <behavior>
    - Seed 14 categories with sortOrder 0..13 into an in-memory container; save.
    - Apply the same insertion logic as ManageCategoriesView.addCategory: `nextSortOrder = (all.map(\.sortOrder).max() ?? 999) + 1`; insert a "Custom" category with that order; save.
    - Fetch sorted ascending by sortOrder; expect `.last?.name == "Custom"` and `.last?.sortOrder == 14`.
  </behavior>
  <action>
    Add one `@Test` named `newCategoryAppendsAtBottom` (description tagged "STAB-03") to the existing `CategoryCRUDTests` struct (no new file). Use the suite's existing `makeContainer()` helper and `Cat` typealias. Seed 0..<14, then reproduce the `max(sortOrder)+1` insertion exactly as `ManageCategoriesView.addCategory` does, and assert with `#expect(sorted.last?.name == "Custom", "STAB-03")` and `#expect(sorted.last?.sortOrder == 14, "STAB-03")`. This test passes against current correct code (it is a lock-in / anti-regression test, not a RED→GREEN bug test — the bug is data, not code).
  </action>
  <verify>
    <automated>xcodebuild test -project MyHome.xcodeproj -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:MyHomeTests/CategoryCRUDTests 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "STAB-03" MyHomeTests/CategoryCRUDTests.swift` returns >= 1.
    - Test present: `grep -c "newCategoryAppendsAtBottom" MyHomeTests/CategoryCRUDTests.swift` returns 1.
    - Test PASSES (the insertion logic is already correct); full CategoryCRUDTests suite green.
    - Commit `test(08-03): lock in max+1 category insertion order`.
  </acceptance_criteria>
  <done>Regression test added asserting new category sortOrder == 14; CategoryCRUDTests suite green.</done>
</task>

<task type="auto">
  <name>Task 2: Defensive comment on Category.init sortOrder default</name>
  <files>MyHomeApp/Persistence/Schema/SchemaV5.swift</files>
  <read_first>
    - MyHomeApp/Persistence/Schema/SchemaV5.swift lines ~55-62 (`init(name: String, symbolName: String?, sortOrder: Int = 0)` — the current Category model used via `typealias Category = SchemaV5.Category`)
    - MyHomeApp/Features/Budgets/ManageCategoriesView.swift line ~191-193 (the correct caller, referenced by the comment)
  </read_first>
  <action>
    Add a one-line defensive comment immediately above the `init(name:symbolName:sortOrder:)` declaration in `SchemaV5.Category` warning that `sortOrder` defaults to `0` and any caller that omits it will sort to the TOP of the `@Query(sort: \Category.sortOrder)` list — callers MUST pass `max(existing.sortOrder)+1` (see ManageCategoriesView.addCategory). Do NOT change the default value or the init body — changing the default could break the seed path and SchemaV5 identity; this is a comment-only change.
  </action>
  <verify>
    <automated>grep -n "sortOrder" MyHomeApp/Persistence/Schema/SchemaV5.swift | grep -i "default\|top\|max" || grep -B1 "init(name: String, symbolName: String?, sortOrder: Int = 0)" MyHomeApp/Persistence/Schema/SchemaV5.swift</automated>
  </verify>
  <acceptance_criteria>
    - A comment containing the warning about the `sortOrder` default appears on the line immediately above the Category `init` in SchemaV5.swift.
    - `init(name: String, symbolName: String?, sortOrder: Int = 0)` is unchanged (default value not modified).
    - Commit `docs(08-03): warn on Category.init sortOrder default footgun`.
  </acceptance_criteria>
  <done>Defensive comment added; init signature unchanged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Confirm live-app category insertion symptom (STAB-03)</name>
  <what-built>
    A regression test locking in `max(sortOrder)+1` insertion and a defensive comment on the Category init default. Research found no broken production add path, so the reported "new category appears at top" symptom may be stale live-store data rather than a code bug (researcher Open Question 1).
  </what-built>
  <how-to-verify>
    1. Build and run the app on the iPhone 17 simulator (Xcode 26.5, scheme MyHome).
    2. Go to Manage Categories.
    3. Add a brand-new custom category (e.g. "Verify08").
    4. Observe where it lands in the list.
    Expected: the new category appears at the BOTTOM of the list (success criterion #3).
    If it appears at the TOP or middle: the symptom is reproducible with current code — report it. That means a non-grepped add path exists OR the displayed list mixes in stale data with sortOrder 0; capture which categories sit above the new one and their apparent order so a follow-up can locate the offending path or schedule a data backfill.
  </how-to-verify>
  <resume-signal>Type "bottom — confirmed correct" if it appends at the bottom, or describe exactly where it landed and which categories preceded it.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | STAB-03 is internal ordering correctness; no untrusted input boundary involved |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06 | Tampering (data integrity) | Category.sortOrder default = 0 footgun | mitigate | Defensive comment + regression test lock the correct max+1 contract; no production path currently broken |
</threat_model>

<verification>
- Full suite green: `xcodebuild test -project MyHome.xcodeproj -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' 2>&1 | tail -30`
- Manual checkpoint (Task 3) determines whether any further code/data work is warranted; if symptom is reproducible, surface as a follow-up (not silently closed).
</verification>

<success_criteria>
- Regression test asserts new category sortOrder == max+1 (14 after seeding 0–13).
- Defensive comment present on Category init; default unchanged.
- Live-app symptom confirmed bottom (or reported with detail if reproducible).
</success_criteria>

<output>
Create `.planning/phases/08-stabilization/08-03-SUMMARY.md` when done. Record the Task 3 human verdict (bottom-confirmed vs. reproducible-symptom) in the summary.
</output>
