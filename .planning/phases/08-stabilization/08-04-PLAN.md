---
phase: 08-stabilization
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - MyHomeApp/Features/Notes/RoutineResetService.swift
  - MyHomeApp/RootView.swift
autonomous: true
requirements: [STAB-04]
user_setup: []
must_haves:
  truths:
    - "RoutineResetService skeleton exists as a @MainActor @Observable final class (success criterion #4, D-06)"
    - "The full call path RootView scenePhase .active → RoutineResetService.resetIfNeeded() is wired (D-07)"
    - "resetIfNeeded() logs a would-reset message using IST startOfToday and performs NO model writes this phase (D-06)"
    - "The service is ready to accept the lastCheckedDate field from Phase 9 — the IST date seam is in place (success criterion #4)"
  artifacts:
    - path: "MyHomeApp/Features/Notes/RoutineResetService.swift"
      provides: "Logged scaffold service mirroring LockController"
      contains: "RoutineResetService"
      min_lines: 12
    - path: "MyHomeApp/RootView.swift"
      provides: "@State ownership + onChange wiring"
      contains: "routineResetService"
  key_links:
    - from: "RootView.onChange(of: scenePhase)"
      to: "routineResetService.resetIfNeeded()"
      via: "synchronous call on .active (no Task wrap)"
      pattern: "routineResetService\\.resetIfNeeded\\(\\)"
---

<objective>
STAB-04 (scaffold only): Ship a `RoutineResetService` skeleton wired to `RootView.onChange(of: scenePhase)` on `.active`, ready to accept the `NoteBlock.lastCheckedDate` field that Phase 9 will add (success criterion #4). This de-risks the wiring now so Phase 9 only fills the body.

Per D-06 this is a LOGGED scaffold, not a bare no-op: it establishes the full call path and logs a "would reset" message computing IST `startOfToday`, but performs NO model writes this phase (the schema field does not exist yet). Per D-07 it wires alongside the existing `LockController.scenePhaseChanged` / `gmailSyncController.scenePhaseChanged` observers, follows the `@MainActor @Observable final class` pattern (cloned structurally from LockController), and is called SYNCHRONOUSLY (no `Task` wrap) because `resetIfNeeded()` is synchronous.

IST seam: `Calendar(identifier: .gregorian)` with `.timeZone = TimeZone(identifier: "Asia/Kolkata")!`, then `startOfDay(for: Date())`.

Purpose: Establish the routine-reset call path before SchemaV6 (success criterion #4).
Output: New `RoutineResetService.swift` + RootView wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-stabilization/08-CONTEXT.md
@.planning/phases/08-stabilization/08-RESEARCH.md
@.planning/phases/08-stabilization/08-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create RoutineResetService.swift scaffold</name>
  <files>MyHomeApp/Features/Notes/RoutineResetService.swift</files>
  <read_first>
    - MyHomeApp/Security/LockController.swift lines ~32-34 (the `@MainActor @Observable final class` declaration) and ~87-108 (the scenePhase hook style) — structural analog to clone
    - 08-PATTERNS.md "RoutineResetService.swift" section (the exact scaffold body, IST seam, and Phase 9 placeholder comments)
  </read_first>
  <action>
    Create a new file `MyHomeApp/Features/Notes/RoutineResetService.swift` declaring `@MainActor @Observable final class RoutineResetService` (mirroring LockController structurally). Add a single synchronous method `func resetIfNeeded()` that: builds `var cal = Calendar(identifier: .gregorian)`, sets `cal.timeZone = TimeZone(identifier: "Asia/Kolkata")!` (IST — household timezone), computes `let todayIST = cal.startOfDay(for: Date())`, and `print`s a "would reset" line including `todayIST` and noting it is a Phase 8 no-op scaffold. Include doc comments stating: no model writes this phase; Phase 9 fills the body once `NoteBlock.lastCheckedDate` (SchemaV6) lands; ownership is via `@State` in RootView. `import Foundation` only — no SwiftData/SwiftUI needed for the scaffold. Do NOT write to any model context. (STAB-04, D-06)
  </action>
  <verify>
    <automated>xcodebuild build -project MyHome.xcodeproj -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f MyHomeApp/Features/Notes/RoutineResetService.swift`.
    - Source assertions: `grep -c "@MainActor" RoutineResetService.swift` >= 1, `grep -c "@Observable" RoutineResetService.swift` >= 1, `grep -c "final class RoutineResetService" RoutineResetService.swift` == 1, `grep -c "func resetIfNeeded" RoutineResetService.swift` == 1.
    - IST seam present: `grep -c "Asia/Kolkata" RoutineResetService.swift` >= 1 and `grep -c "startOfDay" RoutineResetService.swift` >= 1.
    - No model writes: the file contains no `context.save`, `ctx.save`, `.insert(`, or `.delete(` calls.
    - Project builds.
  </acceptance_criteria>
  <done>RoutineResetService.swift created as a logged, write-free @MainActor @Observable scaffold with the IST date seam; project builds.</done>
</task>

<task type="auto">
  <name>Task 2: Wire RoutineResetService into RootView scenePhase</name>
  <files>MyHomeApp/RootView.swift</files>
  <read_first>
    - MyHomeApp/RootView.swift line ~40 (`@State private var lockController = LockController()` — the ownership analog to slot beside) and lines ~106-114 (the `.onChange(of: scenePhase)` block hosting `lockController.scenePhaseChanged` and `gmailSyncController.scenePhaseChanged`)
    - MyHomeApp/Features/Notes/RoutineResetService.swift (created in Task 1 — the type being instantiated)
  </read_first>
  <action>
    In RootView, add `@State private var routineResetService = RoutineResetService()` alongside the existing `lockController` declaration (use `@State`, never `@StateObject` — matches the existing @Observable ownership idiom). Inside the existing `.onChange(of: scenePhase) { _, newPhase in ... }` closure, after the `gmailSyncController.scenePhaseChanged(newPhase)` line, add a synchronous call gated on `.active`: `if newPhase == .active { routineResetService.resetIfNeeded() }`. Do NOT wrap it in `Task` — `resetIfNeeded()` is synchronous and both `onChange` and the service are `@MainActor` (D-07). Leave the existing lock/gmail/auth handlers unchanged.
  </action>
  <verify>
    <automated>xcodebuild build -project MyHome.xcodeproj -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "@State private var routineResetService = RoutineResetService()" MyHomeApp/RootView.swift` == 1.
    - Source assertion: `grep -c "routineResetService.resetIfNeeded()" MyHomeApp/RootView.swift` == 1, and it sits inside the `.onChange(of: scenePhase)` block gated by `newPhase == .active`.
    - Not Task-wrapped: the `resetIfNeeded()` call is a direct synchronous call (no `Task {` enclosing it).
    - Existing `lockController.scenePhaseChanged` / `gmailSyncController.scenePhaseChanged` calls remain present and unchanged.
    - Project builds.
  </acceptance_criteria>
  <done>RootView owns routineResetService via @State and calls resetIfNeeded() synchronously on scenePhase .active; project builds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | STAB-04 scaffold performs no model writes and processes no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07 | Information Disclosure | RoutineResetService log line | mitigate | The "would reset" log prints only the IST date, no note content — preserves existing T-03-16 (no note body in logs) posture |
| T-08-08 | Tampering | scenePhase-triggered reset | accept | Scaffold performs no writes this phase; reset logic and its data-integrity considerations are deferred to Phase 9 |
</threat_model>

<verification>
- Project builds: `xcodebuild build -project MyHome.xcodeproj -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' 2>&1 | tail -15`
- Manual (success criterion #4): foreground the app → confirm the "[RoutineResetService] resetIfNeeded ... No-op" log line is emitted and no model writes occur.
</verification>

<success_criteria>
- RoutineResetService exists as @MainActor @Observable final class with a synchronous, write-free resetIfNeeded() and the IST date seam.
- RootView owns it via @State and calls it on scenePhase .active without a Task wrapper.
- Project builds; existing scenePhase handlers intact.
</success_criteria>

<output>
Create `.planning/phases/08-stabilization/08-04-SUMMARY.md` when done.
</output>
