---
phase: 11-asset-tracker
plan: 03
type: execute
wave: 3
depends_on: ["11-01", "11-02"]
files_modified:
  - MyHomeApp/Features/Assets/AssetsListView.swift
  - MyHomeApp/Features/Assets/EditAssetView.swift
  - MyHomeApp/Features/Assets/AssetDetailView.swift
  - MyHomeApp/Features/Assets/AMFISchemePickerView.swift
  - MyHomeApp/Features/Assets/StalenessView.swift
  - MyHomeApp/Features/Settings/SettingsView.swift
  - MyHomeTests/AssetGainLossTests.swift
  - MyHomeTests/StalenessBadgeTests.swift
autonomous: false
requirements: [ASSET-01, ASSET-02, ASSET-04, ASSET-06, ASSET-09]
must_haves:
  truths:
    - "User can add, edit, and delete a holding (MF / stock / NPS) under Settings > Assets"
    - "MF holdings link to an AMFI scheme via a searchable picker that stores amfiSchemeCode"
    - "Stock and NPS holdings are valued by manual current-price entry"
    - "Each holding shows current value, gain/loss (absolute + %), and a staleness badge when the NAV is older than 1 calendar day (IST)"
    - "Zero-cost-basis holdings show '—' for the percentage without crashing"
  artifacts:
    - path: "MyHomeApp/Features/Assets/AssetsListView.swift"
      provides: "Holdings CRUD list with swipe-delete + pull-to-refresh"
      min_lines: 60
    - path: "MyHomeApp/Features/Assets/EditAssetView.swift"
      provides: "Add/edit sheet with class picker, AMFI scheme row, units/cost/NAV fields, validation"
      min_lines: 80
    - path: "MyHomeApp/Features/Assets/AMFISchemePickerView.swift"
      provides: "Searchable AMFI scheme picker (4 states) writing amfiSchemeCode"
      min_lines: 40
    - path: "MyHomeApp/Features/Assets/StalenessView.swift"
      provides: "Calendar-day IST staleness badge"
      contains: "Asia/Kolkata"
  key_links:
    - from: "MyHomeApp/Features/Settings/SettingsView.swift"
      to: "AssetsListView"
      via: "NavigationLink in Data section"
      pattern: "AssetsListView"
    - from: "MyHomeApp/Features/Assets/EditAssetView.swift"
      to: "AMFISchemePickerView"
      via: "scheme row navigation, binds amfiSchemeCode"
      pattern: "AMFISchemePickerView"
    - from: "MyHomeApp/Features/Assets/AssetsListView.swift"
      to: "amfiNavService.forceRefresh()"
      via: ".refreshable pull-to-refresh"
      pattern: "forceRefresh"
---

<objective>
Build the Holdings CRUD UI under Settings > Assets: `AssetsListView` (list + swipe-delete + pull-to-refresh), `EditAssetView` (add/edit sheet with class picker, conditional MF scheme row, units/cost/NAV fields, validation guards), `AMFISchemePickerView` (searchable picker with 4 states writing `amfiSchemeCode`), `AssetDetailView` (value + gain/loss + staleness), and the reusable `StalenessView` badge. Add the Settings entry row.

Purpose: This is the user-facing holdings management surface (D-05). It mirrors the Accounts CRUD module file-for-file. Gain/loss math and staleness are pure-logic candidates and ship with unit tests.
Output: 5 new Asset feature views, the Settings row, and two test suites (gain/loss, staleness). A human-verify checkpoint confirms the live CRUD + picker flow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-asset-tracker/11-CONTEXT.md
@.planning/phases/11-asset-tracker/11-RESEARCH.md
@.planning/phases/11-asset-tracker/11-PATTERNS.md
@.planning/phases/11-asset-tracker/11-UI-SPEC.md
@.planning/phases/11-asset-tracker/11-01-SUMMARY.md
@.planning/phases/11-asset-tracker/11-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: StalenessView badge + gain/loss logic + tests</name>
  <files>MyHomeApp/Features/Assets/StalenessView.swift, MyHomeTests/StalenessBadgeTests.swift, MyHomeTests/AssetGainLossTests.swift</files>
  <read_first>
    - .planning/phases/11-asset-tracker/11-PATTERNS.md (StalenessView.swift section — exact target; AssetDetailView gain/loss computed props at lines ~636-645)
    - .planning/phases/11-asset-tracker/11-UI-SPEC.md (Surface 7 Staleness Badge; Surface 5 gain/loss color + zero-cost-basis rule; Copywriting Contract for "Stale" / "as of" / gain labels)
    - .planning/phases/11-asset-tracker/11-RESEARCH.md (Per-Holding Gain/Loss formula; ASSET-09 staleness D-10)
    - MyHomeApp/Persistence/Models/Asset.swift (units, costBasisPerUnit, currentNAV, navAsOfDate)
    - MyHomeApp/Support/Decimal+INR.swift (formattedINR / formattedINRWhole — confirm exact function names)
  </read_first>
  <behavior>
    Staleness (ASSET-09, D-10): given navAsOfDate, compute days from navAsOfDate to startOfTodayIST (Calendar Asia/Kolkata); stale when diff > 1
    - navAsOfDate == today → not stale
    - navAsOfDate == yesterday (diff 1) → not stale
    - navAsOfDate == 2 days ago (diff 2) → stale
    - navAsOfDate == nil → not stale (badge hidden; "price not set" shown elsewhere)
    Gain/loss (ASSET-06): totalCost = (units ?? 0) × (costBasisPerUnit ?? 0); currentValue = (units ?? 0) × (currentNAV ?? 0); absoluteGain = currentValue − totalCost; percentGain = totalCost > 0 ? (absoluteGain / totalCost) × 100 : nil
    - positive gain returns positive absolute + positive %
    - negative gain returns negative absolute + negative %
    - zero/nil cost basis returns nil % (UI shows "—"), absolute gain still computed
  </behavior>
  <action>
    Create `Features/Assets/StalenessView.swift`: a `struct StalenessView: View` taking `let navAsOfDate: Date?`, computing `isStale` via `Calendar(identifier: .gregorian)` with `timeZone = TimeZone(identifier: "Asia/Kolkata")!`, `startOfDay(for: Date())`, and `dateComponents([.day], from: navAsOfDate, to: startOfTodayIST).day ?? 0 > 1` (D-10). When stale: render `Image(systemName: "clock.badge.exclamationmark")` + `Text("Stale")`, font `.caption`, `Color(.systemOrange)`, with `.accessibilityLabel("Price is stale. Last updated \(formattedDate).")`. When fresh or nil: `EmptyView`.
    Put the gain/loss math where it is unit-testable: add a small pure helper (e.g. `AssetValuation` enum/struct with static funcs `currentValue(_:)`, `totalCost(_:)`, `absoluteGain(_:)`, `percentGain(_:) -> Decimal?`) inside StalenessView.swift OR a sibling file, taking an Asset (or units/cost/nav). Reuse this from AssetDetailView in Task 3 so the math is not duplicated. All Decimal.
    Write StalenessBadgeTests and AssetGainLossTests covering every behavior case above. Use a fixed `Date` injection (compute the IST boundary explicitly in the test) so the staleness test is deterministic.
  </action>
  <verify>
    <automated>cd "/Users/reo/My Projects/my-home" && xcodebuild test -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:MyHomeTests/StalenessBadgeTests -only-testing:MyHomeTests/AssetGainLossTests 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - StalenessView.swift uses `TimeZone(identifier: "Asia/Kolkata")` and the threshold `> 1` (grep confirms both)
    - StalenessBadgeTests asserts: today→not stale, yesterday→not stale, 2-days-ago→stale, nil→not stale
    - AssetGainLossTests asserts: positive gain, negative gain, and zero-cost-basis returns nil % (no divide-by-zero crash) with absolute gain still present
    - percentGain returns `Decimal?` and is nil when totalCost <= 0
    - Both test suites green on iPhone 17 simulator
  </acceptance_criteria>
  <done>StalenessView renders only when stale per the IST calendar-day rule; gain/loss helper handles positive/negative/zero-cost cases — tests green.</done>
</task>

<task type="auto">
  <name>Task 2: AssetsListView + EditAssetView + AMFISchemePickerView + Settings row</name>
  <files>MyHomeApp/Features/Assets/AssetsListView.swift, MyHomeApp/Features/Assets/EditAssetView.swift, MyHomeApp/Features/Assets/AMFISchemePickerView.swift, MyHomeApp/Features/Settings/SettingsView.swift</files>
  <read_first>
    - MyHomeApp/Features/Settings/AccountsListView.swift (CRUD list: @Query, ContentUnavailableView empty state, row layout, swipeActions, confirmationDialog, toolbar +, sheet, delete-with-save — mirror exactly)
    - MyHomeApp/Features/Settings/EditAccountView.swift (sheet structure nil=create/non-nil=edit, segmented Picker, DatePicker as-of, validation isValid + inline error, onAppear pre-populate, saveAccount idiom)
    - MyHomeApp/Features/Settings/SettingsView.swift (lines ~186-200 — the Data section and the existing Accounts NavigationLink + rowLabel helper)
    - .planning/phases/11-asset-tracker/11-PATTERNS.md (AssetsListView / EditAssetView / AMFISchemePickerView sections — exact target code incl. holdingRow, validation guards, saveAsset, picker states)
    - .planning/phases/11-asset-tracker/11-UI-SPEC.md (Surfaces 2, 3, 4; asset class icons + colors; Copywriting Contract; Interaction Contracts — pull-to-refresh, scheme search)
    - MyHomeApp/Features/Shared/IconTile.swift (the IconTile signature)
    - MyHomeApp/Support/AMFINavService.swift (schemeList, isFetching, forceRefresh — produced by Plan 02)
  </read_first>
  <action>
    Create `AssetsListView`: `@Query(sort: \Asset.createdAt, order: .reverse)`, reach `amfiNavService` from the environment (injected by RootView in Plan 02). Empty state `ContentUnavailableView("No Holdings Yet", systemImage: "chart.bar", description: Text("Tap + to add your first holding."))`. One flat `List(.insetGrouped)` of `holdingRow(_:)` showing IconTile (symbol+color per class — mutual_fund→"chart.bar.fill"/systemBlue, stock→"chart.line.uptrend.xyaxis"/systemGreen, nps→"umbrella.fill"/systemOrange), name (.body), "class · N units" (.subheadline .secondary), trailing current value `(units×currentNAV).formattedINRWhole()` or "—", and inline `StalenessView(navAsOfDate:)`. `.swipeActions` destructive Delete → `.confirmationDialog("Delete Holding?", ...)` with "Delete Holding" / "Cancel" and the exact message from the copy contract; delete via `context.delete` + `try context.save()`. Toolbar `+` → `.sheet { EditAssetView(asset: nil) }`. `.refreshable { amfiNavService.forceRefresh() }` (D-06). NavigationLink each row → `AssetDetailView(asset:)` (Task 3).
    Create `EditAssetView(asset: Asset?)`: NavigationStack-in-sheet, title "New Holding"/"Edit Holding". Sections per UI-SPEC: Identity (name TextField + segmented class Picker mutual_fund/stock/nps); MF scheme row (conditional — only when class == mutual_fund — navigates to AMFISchemePickerView, binds `amfiSchemeCode`, shows scheme name or "Tap to choose a scheme"); Units & Cost (decimalPad fields + display-only Total cost); Current NAV/Price (label "Current NAV (auto-fetched)" for MF else "Current price"; editable; DatePicker "As of" .date; MF caption "NAV auto-updates daily from AMFI"); Danger Zone Delete (edit mode). Validation `isValid`: non-empty name + `units > 0` + `abs(units) < 1_000_000` + `abs(costBasisPerUnit) < 1_000_000_000` (security guards T-09-05). saveAsset: nil=insert new Asset else edit existing; set fields; `amfiSchemeCode = assetClassRaw == "mutual_fund" ? amfiSchemeCode : nil`; `try context.save()`; dismiss. onAppear pre-populates from `asset`. Render names as plain `Text` (T-09-06 — never AttributedString markdown).
    Create `AMFISchemePickerView(selectedSchemeCode: Binding<String?>, amfiNavService: AMFINavService)`: 4 states — (A) loaded: `List` of `amfiNavService.schemeList` filtered by `query` via `.searchable`, each row scheme name (.body 2 lines) + code (.caption .secondary), selected row tinted `.accentColor.opacity(0.12)` + checkmark, tap sets binding + pops; (B) empty: `ContentUnavailableView("No Schemes Loaded", systemImage: "arrow.down.circle", description: Text("Scheme data hasn't been fetched yet."))` + `Button("Fetch Now")` (.tint accentColor) calling `forceRefresh()`; (C) fetching: `ProgressView("Loading schemes…")`; (D) failed: re-show B with "Couldn't load schemes. Check your connection and try again." + "Try Again". Title "Choose Scheme" inline.
    Modify `SettingsView`: add `NavigationLink(destination: AssetsListView())` in the Data section adjacent to the Accounts row, label "Assets", symbol "chart.bar", color `Color(.systemPurple)` via the existing rowLabel helper.
  </action>
  <verify>
    <automated>cd "/Users/reo/My Projects/my-home" && xcodebuild build -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - AssetsListView has `.refreshable { amfiNavService.forceRefresh() }` and a `.confirmationDialog` with title "Delete Holding?"; delete calls `context.delete` + `context.save()`
    - EditAssetView isValid enforces `abs(units) < 1_000_000` and `abs(costBasisPerUnit) < 1_000_000_000` (grep confirms both bounds)
    - EditAssetView saveAsset sets `amfiSchemeCode` to nil for non-mutual_fund classes (grep shows the conditional)
    - AMFISchemePickerView is `.searchable` and writes the bound `selectedSchemeCode` on tap; "Fetch Now" calls `forceRefresh()`
    - SettingsView contains `NavigationLink(destination: AssetsListView())` with label "Assets"
    - Asset/scheme names rendered via plain `Text(...)` — `grep -c 'AttributedString(markdown' MyHomeApp/Features/Assets/*.swift` returns 0
    - Project builds on iPhone 17 simulator
  </acceptance_criteria>
  <done>Holdings CRUD list, edit sheet, searchable AMFI picker, and Settings entry build and follow the Accounts pattern with validation guards and pull-to-refresh.</done>
</task>

<task type="auto">
  <name>Task 3: AssetDetailView (value + gain/loss + staleness)</name>
  <files>MyHomeApp/Features/Assets/AssetDetailView.swift</files>
  <read_first>
    - MyHomeApp/Features/Settings/AccountDetailView.swift (header balance card pattern, detail List rows, toolbar Edit → sheet)
    - .planning/phases/11-asset-tracker/11-PATTERNS.md (AssetDetailView.swift section — computed props currentValue/totalCost/absoluteGain/percentGain; header card)
    - .planning/phases/11-asset-tracker/11-UI-SPEC.md (Surface 5 Asset Detail — header card, detail rows table, gain/loss color rules, "as of" label format, zero-cost-basis "—")
    - MyHomeApp/Features/Assets/StalenessView.swift + the gain/loss helper from Task 1 (reuse — do not duplicate math)
    - MyHomeApp/Support/Decimal+INR.swift (formattedINR / formattedINRWhole)
  </read_first>
  <action>
    Create `AssetDetailView(asset: Asset)`. Header card (`cardStyle()`, mirrors AccountDetailView.balanceCard): asset-class label (.subheadline .secondary), current value `(units×currentNAV).formattedINRWhole()` in `.largeTitle.weight(.semibold)` (or "—" .secondary when no NAV), holding name (.body .secondary), and inline `StalenessView(navAsOfDate:)` + "as of \(navAsOfDate.formatted(.dateTime.day().month(.abbreviated).year()))" or "price not set". Detail List (.insetGrouped): rows Units, Cost per unit, Total cost, Current NAV/Price, Gain/Loss (absolute `.formattedINRWhole()` in green/red/primary per sign + `(+X.XX%)`/`(−X.XX%)` or "—" when percentGain nil), and AMFI Scheme Code (only for mutual_fund). Reuse the gain/loss helper from Task 1 (AssetValuation) — do not re-derive. Toolbar "Edit" → `.sheet { EditAssetView(asset: asset) }`. Gain text always carries explicit "+"/"−" prefix (accessibility — color is not the only signal). Render all text via plain `Text`.
  </action>
  <verify>
    <automated>cd "/Users/reo/My Projects/my-home" && xcodebuild build -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - AssetDetailView reuses the Task 1 gain/loss helper (grep shows the helper type referenced, not a re-implemented totalCost/percentGain)
    - Gain/loss row shows "—" for percentage when percentGain is nil (zero cost basis) and never divides by zero
    - Gain text includes an explicit "+"/"−" prefix; color is green/red/primary
    - AMFI Scheme Code row appears only when assetClassRaw == "mutual_fund"
    - Header shows `StalenessView` + an "as of" date label (or "price not set" when navAsOfDate is nil)
    - Project builds on iPhone 17 simulator
  </acceptance_criteria>
  <done>AssetDetailView shows current value, gain/loss with sign + color, scheme code (MF only), and staleness — reusing the tested gain/loss helper.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Holdings CRUD under Settings > Assets: add/edit/delete holdings for MF/stock/NPS, the searchable AMFI scheme picker, per-holding value + gain/loss + staleness on the detail view, and pull-to-refresh NAV fetch.</what-built>
  <how-to-verify>
    1. Build & run on the iPhone 17 simulator (scheme MyHome). Settings → Assets.
    2. Tap +. Add a Mutual Fund: enter a name, tap the Scheme row → if "No Schemes Loaded", tap "Fetch Now" (requires network); after schemes load, search a fund name, tap one (verify checkmark + it returns showing the scheme name). Enter units + cost per unit. Confirm "NAV auto-updates daily from AMFI" caption shows. Save.
    3. Confirm the holding appears in the list with a current value (or "—" if no NAV yet).
    4. Pull-to-refresh the list — the iOS spinner appears and dismisses (no error popup even if offline, per D-07).
    5. Tap the holding → AssetDetailView. Verify current value, units, cost, gain/loss (absolute + % with +/− and green/red), and AMFI Scheme Code row. If the NAV date is >1 day old, a "Stale" orange badge shows.
    6. Add a Stock holding: confirm NO scheme picker row, label reads "Current price", enter price manually + as-of date. Save and confirm value.
    7. Edit a holding (change units) and Save — value updates. Swipe-delete a holding → "Delete Holding?" dialog → confirm it is removed.
    8. Try saving a holding with empty name or units = 0 — Save is disabled / inline error shows.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues (wrong copy, layout, picker behavior, gain/loss sign, staleness).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User form input → Asset @Model | Units / cost / NAV entered by the user; unbounded values corrupt aggregation |
| AMFI scheme name → UI text | External scheme names rendered in picker/detail; must be inert |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-09 | Tampering | Unreasonably large units/cost entry (e.g. 1e18) | mitigate | `abs(units) < 1_000_000` + `abs(costBasisPerUnit) < 1_000_000_000` guards in EditAssetView.isValid/saveAsset (mirrors T-09-05); grep gate asserts both bounds |
| T-11-10 | Tampering | XSS-style markdown injection via scheme/asset name | mitigate | Render all names with plain `Text(...)` — never `Text(AttributedString(markdown:))` (T-09-06); grep gate asserts 0 markdown renders |
| T-11-11 | DoS | Zero/nil cost-basis divide-by-zero in % gain | mitigate | percentGain returns nil when totalCost <= 0; UI shows "—"; AssetGainLossTests covers the case |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs — zero new SPM dependencies |
</threat_model>

<verification>
- StalenessBadgeTests + AssetGainLossTests green.
- Project builds on iPhone 17 simulator.
- Human-verify checkpoint approved (CRUD + picker + detail + pull-to-refresh).
- Validation bounds present; no markdown text rendering.
</verification>

<success_criteria>
- User can add/edit/delete MF/stock/NPS holdings under Settings > Assets (ASSET-01, ASSET-02).
- MF holdings link to an AMFI scheme via searchable picker storing amfiSchemeCode (D-01/D-02); stocks/NPS use manual price entry (ASSET-04).
- Per-holding gain/loss (absolute + %) with zero-cost-basis handled (ASSET-06).
- Staleness badge + as-of date on holdings older than 1 IST calendar day (ASSET-09).
</success_criteria>

<output>
Create `.planning/phases/11-asset-tracker/11-03-SUMMARY.md` when done.
</output>
