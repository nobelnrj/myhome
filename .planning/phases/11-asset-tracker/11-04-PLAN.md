---
phase: 11-asset-tracker
plan: 04
type: execute
wave: 4
depends_on: ["11-02", "11-03"]
files_modified:
  - MyHomeApp/Features/Assets/NetWorthCard.swift
  - MyHomeApp/Features/Assets/NetWorthTrendChart.swift
  - MyHomeApp/Features/Overview/OverviewView.swift
  - MyHomeTests/AllocationSegmentTests.swift
autonomous: false
requirements: [ASSET-05, ASSET-07, ASSET-08]
must_haves:
  truths:
    - "The Overview screen shows a net-worth card with the total and a 4-class allocation donut"
    - "The card is tappable and navigates to the holdings list"
    - "The donut never crashes when cash is negative (cash slice clamped to 0; true total shown in center)"
    - "A net-worth trend chart renders from NetWorthSnapshot history"
    - "When there are no assets and cash is 0, the net-worth section is suppressed entirely"
  artifacts:
    - path: "MyHomeApp/Features/Assets/NetWorthCard.swift"
      provides: "Overview card: total + allocation donut + legend, tappable to holdings"
      min_lines: 50
    - path: "MyHomeApp/Features/Assets/NetWorthTrendChart.swift"
      provides: "AreaMark+LineMark trend over NetWorthSnapshot"
      min_lines: 30
  key_links:
    - from: "MyHomeApp/Features/Overview/OverviewView.swift"
      to: "NetWorthCard"
      via: "inserted in OverviewMonthContent scroll, suppressed when no data"
      pattern: "NetWorthCard"
    - from: "MyHomeApp/Features/Assets/NetWorthCard.swift"
      to: "DonutChart"
      via: "4 DonutSegments with cash clamped max(cashValue,0)"
      pattern: "DonutChart"
    - from: "MyHomeApp/Features/Assets/NetWorthCard.swift"
      to: "AssetsListView"
      via: "card tap navigation"
      pattern: "AssetsListView"
---

<objective>
Surface the net worth on the Overview screen (D-04): a tappable `NetWorthCard` showing the total, a 4-class allocation donut (reusing `DonutChart`), a per-class legend, and a `NetWorthTrendChart` (AreaMark+LineMark over `NetWorthSnapshot`). Insert it into `OverviewView` after the spend hero, suppressed when there is no data. Tapping navigates to the holdings list (the same `AssetsListView` from Plan 03).

Purpose: Completes the user-visible net-worth picture (ASSET-05/07/08). Reuses the existing DonutChart and SpendOverTimeChart patterns with the mandatory Decimal→Double boundary handling and the negative-cash clamp.
Output: NetWorthCard, NetWorthTrendChart, the OverviewView insertion, the allocation-segment test suite, and a human-verify checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-asset-tracker/11-CONTEXT.md
@.planning/phases/11-asset-tracker/11-RESEARCH.md
@.planning/phases/11-asset-tracker/11-PATTERNS.md
@.planning/phases/11-asset-tracker/11-UI-SPEC.md
@.planning/phases/11-asset-tracker/11-02-SUMMARY.md
@.planning/phases/11-asset-tracker/11-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Allocation segment builder + NetWorthCard + NetWorthTrendChart</name>
  <files>MyHomeApp/Features/Assets/NetWorthCard.swift, MyHomeApp/Features/Assets/NetWorthTrendChart.swift, MyHomeTests/AllocationSegmentTests.swift</files>
  <read_first>
    - MyHomeApp/Features/Shared/DonutChart.swift (DonutSegment signature: id/label/value:Double/color; center overlay closure; size param)
    - MyHomeApp/Features/Overview/SpendOverTimeChart.swift (AreaMark+LineMark structure, Pitfall A/B — aggregate/convert OUTSIDE the Chart DSL; chartYAxis/chartXAxis; empty state)
    - MyHomeApp/Features/Overview/OverviewView.swift (WhereItsGoingCard donut+legend HStack layout; sectionHeader; cardStyle; the double(_:) Decimal->Double helper)
    - .planning/phases/11-asset-tracker/11-PATTERNS.md (NetWorthCard.swift + NetWorthTrendChart.swift sections — exact DonutSegment build with max(.,0) clamp + NSDecimalNumber conversion)
    - .planning/phases/11-asset-tracker/11-UI-SPEC.md (Surface 1 card layout; Surface 6 trend chart; donut segment colors MF=blue/stock=green/nps=orange/cash=teal; negative-net display rule)
    - MyHomeApp/Support/NetWorthCalculator.swift (the breakdown(assets:accounts:expenses:) producing mf/stock/nps/cash + total — from Plan 02)
    - MyHomeApp/Support/Decimal+INR.swift (formattedINRWhole / formattedINRCompact)
  </read_first>
  <behavior>
    Allocation segments (ASSET-07, D-11):
    - given mf/stock/nps/cash sub-totals, builds 4 DonutSegments with values converted to Double via NSDecimalNumber
    - the cash segment value is max(cashValue, 0) (negative cash clamped to 0 — prevents SectorMark crash)
    - positive sub-totals map to their values; a zero sub-total maps to a 0-value segment (thin sliver / no crash)
    - the displayed total label uses the true totalNetWorth (may be negative) — NOT the clamped cash
  </behavior>
  <action>
    Add a pure builder (e.g. `static func allocationSegments(mf:stock:nps:cash:) -> [DonutSegment]`) in NetWorthCard.swift that maps the four Decimal sub-totals to `[DonutSegment]` with colors MF=Color(.systemBlue), stock=Color(.systemGreen), nps=Color(.systemOrange), cash=Color(.systemTeal), each value `Double(truncating: max(v, 0) as NSDecimalNumber)` (cash clamp is the critical one; clamp all for safety). This builder is what AllocationSegmentTests exercises.
    Create `NetWorthCard` taking the data it needs (allAssets, allAccounts, allExpenses, snapshots — or a precomputed breakdown). Compute the breakdown via `NetWorthCalculator.breakdown(...)`. Layout per Surface 1: HStack with `DonutChart(segments:size:132)` left (center overlay: "NET WORTH" .caption2 + `totalNetWorth.formattedINRWhole()` .headline, true value incl. negative sign), legend VStack right (4 rows: swatch + class label + sub-total .formattedINRWhole()). Below: `NetWorthTrendChart(snapshots:)`. Wrap the whole card in a Button(.plain) / NavigationLink that navigates to `AssetsListView`. Use `cardStyle(cornerRadius: 16, padding: 18)`. DonutChart stays `.accessibilityHidden(true)`; add `.accessibilityValue(totalNetWorth.formattedINR())` on the total.
    Create `NetWorthTrendChart(snapshots: [NetWorthSnapshot])` mirroring SpendOverTimeChart: AreaMark + LineMark over `snap.date` x `Double(truncating: snap.totalNetWorth as NSDecimalNumber)` (convert at the boundary — never pass Decimal to `.value`), `.chartYAxis` compact INR labels, `.chartXAxis` abbreviated months, `.frame(height: 140)`, empty state `Text("No history yet.")` at height 80, `.accessibilityLabel("Net worth trend chart")`.
    Write AllocationSegmentTests: assert negative cash clamps to a 0-value cash segment; assert 4 segments are produced; assert the true total is preserved separately (a negative total is not clamped).
  </action>
  <verify>
    <automated>cd "/Users/reo/My Projects/my-home" && xcodebuild test -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:MyHomeTests/AllocationSegmentTests 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - NetWorthCard contains the cash clamp max( for the cash DonutSegment value (grep confirms)
    - NetWorthCard converts Decimal->Double via NSDecimalNumber (grep confirms; no direct Double(decimal) cast)
    - NetWorthTrendChart never passes a raw Decimal to .value( — conversion happens at the call site; empty state renders "No history yet."
    - AllocationSegmentTests: negative cash -> cash segment value == 0; builder returns 4 segments; true negative total is not clamped
    - DonutChart usage carries .accessibilityHidden(true) and the total carries an accessibility value
    - AllocationSegmentTests green on iPhone 17 simulator
  </acceptance_criteria>
  <done>Allocation segment builder clamps negative cash, NetWorthCard renders total + donut + legend + trend and navigates to holdings, NetWorthTrendChart follows the Decimal->Double boundary — tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Insert NetWorthCard into OverviewView (suppress when no data)</name>
  <files>MyHomeApp/Features/Overview/OverviewView.swift</files>
  <read_first>
    - MyHomeApp/Features/Overview/OverviewView.swift (OverviewMonthContent body; LazyVStack spacing 22; existing @Query decls; sectionHeader(_:action:) helper; the spend hero card position; the NavigationStack present in this view)
    - .planning/phases/11-asset-tracker/11-PATTERNS.md (OverviewView.swift modify section — @Query additions, sectionHeader("Net Worth", action: ("See holdings", ...)), conditional render)
    - .planning/phases/11-asset-tracker/11-UI-SPEC.md (Surface 1 — placement after spend hero/before "Where it's going"; suppression rule when allAssets.isEmpty && cashValue == 0; -8pt section header pull-up; 16pt horizontal padding)
    - MyHomeApp/Features/Assets/NetWorthCard.swift (the init parameters from Task 1)
  </read_first>
  <action>
    In OverviewView (OverviewMonthContent), add `@Query(sort: \Asset.createdAt, order: .reverse) private var allAssets: [Asset]`, `@Query(sort: \NetWorthSnapshot.date, order: .reverse) private var netWorthSnapshots: [NetWorthSnapshot]`, and a `@Query private var allAccounts: [Account]` (plus reuse the existing expenses query). Insert, after the spend hero card and before "Where it's going": a `sectionHeader("Net Worth", action: ("See holdings", { /* navigate to AssetsListView */ }))` with `.padding(.bottom, -8)`, then — ONLY when `!allAssets.isEmpty || cashValue != 0` — the `NetWorthCard(...)` with `.padding(.horizontal, 16)`. Compute `cashValue` for the suppression test via `NetWorthCalculator` (cash sub-total). When suppressed, render neither the header nor the card (no empty card). The "See holdings" action and the card tap both navigate to `AssetsListView` via the existing NavigationStack. Match LazyVStack spacing (22) and card gap conventions.
  </action>
  <verify>
    <automated>cd "/Users/reo/My Projects/my-home" && grep -c 'NetWorthCard' MyHomeApp/Features/Overview/OverviewView.swift ; xcodebuild build -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - OverviewView declares @Query for Asset, NetWorthSnapshot, and Account
    - The NetWorthCard is rendered only when `!allAssets.isEmpty || cashValue != 0` (grep shows the conditional; no empty card on Overview)
    - A "Net Worth" sectionHeader with a "See holdings" action navigates to AssetsListView
    - `grep -c 'NetWorthCard' MyHomeApp/Features/Overview/OverviewView.swift` returns >= 1
    - Project builds on iPhone 17 simulator
  </acceptance_criteria>
  <done>The Overview screen shows the net-worth card after the spend hero, suppressed when there is no asset/cash data, tappable to the holdings list.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The Overview net-worth card: total + 4-class allocation donut + per-class legend + net-worth trend chart, tappable into the holdings list, with negative-cash handled gracefully and the section suppressed when there is no data.</what-built>
  <how-to-verify>
    1. Build & run on iPhone 17 simulator (scheme MyHome). With NO holdings and all account balances 0: open Overview — confirm NO "Net Worth" section/card appears.
    2. Add at least one holding (Settings > Assets) and/or ensure an account has a balance. Return to Overview — the "Net Worth" card now appears after the spend hero, before "Where it's going".
    3. Confirm the donut center shows "NET WORTH" + the total (formatted INR). Confirm the legend lists Mutual Funds (blue), Stocks (green), NPS (orange), Cash (teal) with sub-totals.
    4. Confirm the net-worth trend chart renders below (or "No history yet." if only one day of snapshots — relaunch the app the next IST day, or it shows the single point/empty state).
    5. Tap the card (and separately the "See holdings" header action) — both navigate to the holdings list.
    6. Negative-net case (optional): set a credit-card account with debt larger than savings + holdings. Confirm the center total shows a negative value (e.g. "−₹...") and the donut renders without crashing (cash slice absent/zero).
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues (placement, donut colors/legend, trend chart, navigation, negative-net rendering).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Aggregated net-worth Decimal -> Swift Charts | Decimal sub-totals/totals feed DonutChart + trend chart; type/sign errors crash or mislead |
| Account balances (may be negative) -> donut SectorMark | Negative cash value would crash SectorMark if unclamped |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-12 | DoS | Negative cash value passed to SectorMark | mitigate | Cash segment clamped `max(cashValue, 0)`; true total shown via separate Text; AllocationSegmentTests asserts the clamp |
| T-11-13 | Tampering | Decimal passed to Swift Charts .value (not Plottable) | mitigate | Convert to Double via NSDecimalNumber at the boundary (donut + trend); never pass raw Decimal into the Chart DSL (SpendOverTimeChart Pitfall B) |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs — zero new SPM dependencies (Swift Charts built-in) |
</threat_model>

<verification>
- AllocationSegmentTests green (negative-cash clamp; 4 segments; true total preserved).
- Project builds on iPhone 17 simulator.
- Human-verify checkpoint approved (card placement, donut/legend, trend, navigation, negative-net).
- NetWorthCard reachable; suppressed when no data.
</verification>

<success_criteria>
- Overview shows total net worth = holdings + account balances with a per-class breakdown (ASSET-05).
- Allocation donut splits by MF/stock/NPS/cash, clamping negative cash gracefully (ASSET-07, D-11).
- Net-worth trend chart renders from daily snapshots (ASSET-08).
</success_criteria>

<output>
Create `.planning/phases/11-asset-tracker/11-04-SUMMARY.md` when done.
</output>
