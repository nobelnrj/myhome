---
phase: 11-asset-tracker
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - MyHomeApp/Support/AMFINavService.swift
  - MyHomeApp/Support/NetWorthSnapshotService.swift
  - MyHomeApp/Support/NetWorthCalculator.swift
  - MyHomeApp/RootView.swift
  - MyHomeTests/AMFINavServiceTests.swift
  - MyHomeTests/NetWorthAggregationTests.swift
  - MyHomeTests/NetWorthSnapshotTests.swift
autonomous: true
requirements: [ASSET-03, ASSET-05, ASSET-08]
must_haves:
  truths:
    - "(D-02) The cached scheme list and NAVs both come from a single NAVAll.txt fetch; the picker reads the cached parsed list"
    - "AMFINavService parses NAVAll.txt into AMFIScheme records, skipping section headers and malformed rows"
    - "On scenePhase .active (once per IST day), MF NAVs refresh best-effort and never block the UI"
    - "On fetch failure the app keeps the cached NAV and shows no error popup"
    - "Net worth = sum of holding values + account balances, with CC balances subtracting naturally"
    - "Exactly one NetWorthSnapshot exists per IST day; a same-day re-run overwrites it"
  artifacts:
    - path: "MyHomeApp/Support/AMFINavService.swift"
      provides: "Fetch/parse/cache NAVAll.txt; daily IST-gated refresh + forceRefresh; updates Asset NAVs by amfiSchemeCode"
      contains: "func parseNAVAll"
    - path: "MyHomeApp/Support/NetWorthSnapshotService.swift"
      provides: "Daily upsert of NetWorthSnapshot (total + per-class sub-totals)"
      contains: "func upsertIfNeeded"
    - path: "MyHomeApp/Support/NetWorthCalculator.swift"
      provides: "Pure net-worth aggregation (mf/stock/nps/cash + total) reused by service and UI"
      contains: "static func"
  key_links:
    - from: "MyHomeApp/RootView.swift"
      to: "amfiNavService.refreshIfNeeded() + netWorthSnapshotService.upsertIfNeeded()"
      via: "scenePhase .active onChange"
      pattern: "refreshIfNeeded"
    - from: "MyHomeApp/Support/NetWorthSnapshotService.swift"
      to: "AccountBalance.compute"
      via: "NetWorthCalculator cash aggregation"
      pattern: "AccountBalance.compute"
    - from: "MyHomeApp/Support/AMFINavService.swift"
      to: "Asset.currentNAV / Asset.navAsOfDate"
      via: "match by amfiSchemeCode then context.save()"
      pattern: "asset.currentNAV"
---

<objective>
Build the two app-active services and the shared aggregation logic: `AMFINavService` (fetch/parse/cache AMFI NAVAll.txt, daily IST-gated + pull-to-refresh, silent failure), `NetWorthCalculator` (pure Decimal aggregation of holdings + account balances), and `NetWorthSnapshotService` (daily upsert of total + per-class sub-totals). Wire both services into RootView's scenePhase .active hook and onAppear injection. Per D-02, one NAVAll.txt fetch feeds both the cached scheme list (for the picker) and the per-Asset NAV updates.

Purpose: This is the data/business-logic spine for the Asset Tracker. The UI plans (03, 04) render from `Asset.currentNAV` and `NetWorthSnapshot` rows produced here. Net-worth aggregation must reuse `AccountBalance.compute()` so the POSITIVE-spend / baseline−net sign convention is honored exactly.
Output: AMFINavService, NetWorthSnapshotService, NetWorthCalculator, RootView wiring, and three test suites (parse, aggregation, daily upsert).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-asset-tracker/11-CONTEXT.md
@.planning/phases/11-asset-tracker/11-RESEARCH.md
@.planning/phases/11-asset-tracker/11-PATTERNS.md
@.planning/phases/11-asset-tracker/11-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AMFINavService — fetch/parse/cache NAVAll.txt + daily IST gate</name>
  <files>MyHomeApp/Support/AMFINavService.swift, MyHomeTests/AMFINavServiceTests.swift</files>
  <read_first>
    - MyHomeApp/Features/Notes/RoutineResetService.swift (lines 18-58 — @MainActor @Observable class shape, modelContext injection, IST daily gate with Calendar Asia/Kolkata + startOfDay, UserDefaults date compare, non-fatal catch)
    - MyHomeApp/Features/Gmail/TransferScanService.swift (lines 27-59 — modelContext injection + async work-in-Task + error idiom)
    - .planning/phases/11-asset-tracker/11-PATTERNS.md (AMFINavService.swift section — exact target for class shape, refreshIfNeeded, forceRefresh, performFetch, navDateFormatter, parseNAVAll)
    - .planning/phases/11-asset-tracker/11-RESEARCH.md (AMFI NAVAll.txt — Verified File Format; Fetch Architecture; Caching Strategy)
    - MyHomeApp/Persistence/Models/Asset.swift (the SchemaV7 Asset fields: amfiSchemeCode, currentNAV, navAsOfDate)
  </read_first>
  <behavior>
    - parseNAVAll: given the real 6-column semicolon format (Scheme Code;ISIN;ISIN;Scheme Name;NAV;Date), returns one AMFIScheme per data row with code=col0, name=col3, nav=Decimal(col4), navDate=DD-MMM-yyyy(col5)
    - parseNAVAll: a line with no semicolon (section header) is skipped (not a record, no crash)
    - parseNAVAll: a row with fewer than 6 fields is skipped
    - parseNAVAll: a row with a non-numeric NAV (Decimal(string:) == nil) is skipped (never fatalError)
    - parseNAVAll: NAV is parsed as Decimal, never Double; date parsed with en_US_POSIX, Asia/Kolkata
    - refreshIfNeeded: when last fetch date >= startOfTodayIST, it is a no-op (does not fetch)
    - refreshIfNeeded: when last fetch date < startOfTodayIST, it kicks a fetch
  </behavior>
  <action>
    Create `Support/AMFINavService.swift` as `@MainActor @Observable final class AMFINavService` with `var modelContext: ModelContext?`, `private(set) var cachedSchemes: [String: AMFIScheme]`, an exposed `var schemeList: [AMFIScheme]` (sorted by name for the picker), and `var isFetching: Bool`. Define a top-level `struct AMFIScheme: Identifiable { let code: String; let name: String; let nav: Decimal; let navDate: Date }` (id = code). Per D-02, a single NAVAll.txt fetch populates BOTH the cached scheme list (read by the picker) AND the per-Asset NAV updates — there is no separate scheme-list download.
    Implement: `refreshIfNeeded()` (IST daily gate via Calendar Asia/Kolkata startOfDay, UserDefaults key `"amfiNavLastFetchDate"` read as `as? Date ?? .distantPast`; no-op if lastFetch >= startOfTodayIST; else set isFetching and `Task { await performFetch(...) }`); `forceRefresh()` (bypasses the daily gate — for pull-to-refresh and "Fetch Now"); `private func performFetch(context:todayIST:) async` (URL `https://portal.amfiindia.com/spages/NAVAll.txt` — HTTPS only, named constant; `URLSession.shared.data(from:)`; decode UTF-8; parseNAVAll; rebuild cachedSchemes — this cached parsed list is what the picker reads, D-02; fetch all Assets, for each with a matching `amfiSchemeCode` set currentNAV + navAsOfDate; if any changed `try context.save()`; set UserDefaults lastFetchDate = todayIST; on ANY error per D-07 fail silently — `print` only, keep cached NAV); the `navDateFormatter` (dd-MMM-yyyy, en_US_POSIX, Asia/Kolkata); and `func parseNAVAll(_ text:) -> [AMFIScheme]` with the section-header skip guard (`guard trimmed.contains(";")`), `parts.count >= 6` guard, and `Decimal(string:)` + date guards.
    Security: validate parser inputs defensively (skip malformed rows, never crash); render scheme names later as plain text; HTTPS only.
  </action>
  <verify>
    <automated>cd "/Users/reo/My Projects/my-home" && xcodebuild test -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:MyHomeTests/AMFINavServiceTests 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - AMFINavService.swift declares `@MainActor @Observable final class AMFINavService` with `var modelContext: ModelContext?`
    - `func parseNAVAll` skips section-header lines, skips rows with `<6` fields, and skips rows whose NAV is non-numeric — proven by AMFINavServiceTests with a fixture string containing a header line, a malformed row, and a NaN-NAV row
    - parseNAVAll returns NAV typed `Decimal`; `grep -c 'Double(' MyHomeApp/Support/AMFINavService.swift` does not appear in NAV parsing (no Double conversion of money)
    - The fetch URL string is `https://portal.amfiindia.com/spages/NAVAll.txt` (HTTPS) — `grep -c 'http://' MyHomeApp/Support/AMFINavService.swift` returns 0
    - AMFINavServiceTests asserts refreshIfNeeded is a no-op when lastFetch == today and fetches when lastFetch is .distantPast (gate logic testable without network — extract the gate into a testable function or inject a clock/date)
    - performFetch catch path logs and returns (silent failure, D-07) — no thrown error escapes scenePhase
    - `xcodebuild test ... -only-testing:MyHomeTests/AMFINavServiceTests` is green
  </acceptance_criteria>
  <done>AMFINavService fetches/parses/caches NAVAll.txt, updates Asset NAVs by scheme code, gates once per IST day, and fails silently — parse + gate tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: NetWorthCalculator + NetWorthSnapshotService (daily upsert)</name>
  <files>MyHomeApp/Support/NetWorthCalculator.swift, MyHomeApp/Support/NetWorthSnapshotService.swift, MyHomeTests/NetWorthAggregationTests.swift, MyHomeTests/NetWorthSnapshotTests.swift</files>
  <read_first>
    - MyHomeApp/Support/AccountBalance.swift (the `static func compute(baseline:asOf:expenses:accountID:) -> Decimal` signature and the sign convention: spends stored POSITIVE, balance = baseline − net; CC baseline negative so balance subtracts naturally)
    - MyHomeApp/Features/Notes/RoutineResetService.swift (IST gate + @MainActor @Observable shape for NetWorthSnapshotService)
    - .planning/phases/11-asset-tracker/11-PATTERNS.md (NetWorthSnapshotService.swift section — upsert via fetch-before-insert; net-worth aggregation block with AccountBalance.compute usage)
    - .planning/phases/11-asset-tracker/11-RESEARCH.md (Net-Worth Aggregation — Formula, Sign Convention, Per-Holding Gain/Loss)
    - MyHomeApp/Persistence/Models/Asset.swift, Account.swift, NetWorthSnapshot.swift (the @Model fields)
    - MyHomeTests/AccountBalanceTests.swift (the POSITIVE-spend balance test fixtures to reuse for the CC sign case)
    - MyHomeTests/NetWorthSnapshotTests.swift (the scaffolded stubs from Plan 01 — complete the service-upsert tests here)
  </read_first>
  <behavior>
    NetWorthCalculator (ASSET-05):
    - mfValue = Σ(units × currentNAV) over assets where assetClassRaw == "mutual_fund"; same for stockValue ("stock"), npsValue ("nps"); an asset with nil units or nil currentNAV contributes 0
    - cashValue = Σ AccountBalance.compute(...) over non-archived accounts; a CC account with debt makes cashValue smaller / negative
    - totalNetWorth = mfValue + stockValue + npsValue + cashValue (plain sum; may be negative when CC debt exceeds savings + holdings)
    NetWorthSnapshotService (ASSET-08, D-08):
    - upsertIfNeeded inserts exactly one NetWorthSnapshot keyed to startOfTodayIST when none exists for today
    - a second upsert on the same IST day overwrites the existing row (fetch returns 1 row, not 2)
    - the stored snapshot carries totalNetWorth AND mfValue/stockValue/npsValue/cashValue (D-09)
  </behavior>
  <action>
    Create `Support/NetWorthCalculator.swift` as an enum/struct with pure static functions computing `mfValue/stockValue/npsValue` from `[Asset]`, `cashValue` from `[Account]` + `[Expense]` (reusing `AccountBalance.compute(baseline:asOf:expenses:accountID:)` over non-archived accounts), and a `breakdown(assets:accounts:expenses:)` returning a value type holding the four sub-totals + total. All Decimal; never Double. Do NOT re-implement the balance formula — call AccountBalance.compute (sign convention is non-obvious).
    Create `Support/NetWorthSnapshotService.swift` as `@MainActor @Observable final class NetWorthSnapshotService` with `var modelContext: ModelContext?` and `func upsertIfNeeded()`: compute startOfTodayIST (Calendar Asia/Kolkata), in a `Task` fetch `NetWorthSnapshot` where `date >= todayIST` (fetch-before-insert upsert — NO @Attribute(.unique), CloudKit rule), reuse the first match or insert a new one, set `date = todayIST` and assign total + 4 sub-totals from NetWorthCalculator.breakdown, then `try context.save()`; non-fatal catch (print).
    Complete the NetWorthSnapshotTests service-upsert stubs left by Plan 01.
  </action>
  <verify>
    <automated>cd "/Users/reo/My Projects/my-home" && xcodebuild test -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:MyHomeTests/NetWorthAggregationTests -only-testing:MyHomeTests/NetWorthSnapshotTests 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - NetWorthCalculator.swift contains a `static func` that calls `AccountBalance.compute(` for cash aggregation (grep confirms the call — no re-implemented balance formula)
    - NetWorthAggregationTests: a fixture with a savings account (positive balance) + a CC account (debt) asserts totalNetWorth = holdings + (savings − ccDebt), and that a CC-heavy fixture yields a negative cashValue (sign convention honored)
    - NetWorthAggregationTests: an asset with nil units OR nil currentNAV contributes 0 to its class sub-total
    - NetWorthSnapshotService.upsertIfNeeded uses fetch-before-insert (grep shows `FetchDescriptor<NetWorthSnapshot>`); `grep -c '@Attribute(.unique)' MyHomeApp/Support/NetWorthSnapshotService.swift` returns 0
    - NetWorthSnapshotTests: two upsert calls on the same IST day leave exactly 1 snapshot row; the row carries all 4 sub-totals + total
    - Both test suites green on iPhone 17 simulator
  </acceptance_criteria>
  <done>Net-worth aggregation reuses AccountBalance.compute and honors the CC sign convention; daily snapshot upsert produces exactly one row per IST day with full breakdown — tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Wire AMFINavService + NetWorthSnapshotService into RootView</name>
  <files>MyHomeApp/RootView.swift</files>
  <read_first>
    - MyHomeApp/RootView.swift (lines 41-44 — existing `@State private var routineResetService = RoutineResetService()` pattern; the .onAppear modelContext injections; lines 115-126 — the `.onChange(of: scenePhase)` block where `routineResetService.resetIfNeeded()` is called on `.active`)
    - .planning/phases/11-asset-tracker/11-PATTERNS.md (RootView.swift modify section — exact @State decls, onAppear injection, scenePhase additions)
    - MyHomeApp/Support/AMFINavService.swift, MyHomeApp/Support/NetWorthSnapshotService.swift (the entry points refreshIfNeeded / upsertIfNeeded)
  </read_first>
  <action>
    In RootView: add `@State private var amfiNavService = AMFINavService()` and `@State private var netWorthSnapshotService = NetWorthSnapshotService()` alongside the existing `routineResetService` @State. In the `.onAppear` block, inject `amfiNavService.modelContext = modelContext` and `netWorthSnapshotService.modelContext = modelContext` next to the existing routineResetService injection. In the `.onChange(of: scenePhase)` `if newPhase == .active` branch, after `routineResetService.resetIfNeeded()`, add `amfiNavService.refreshIfNeeded()` (D-06) and `netWorthSnapshotService.upsertIfNeeded()` (D-08). Pass `amfiNavService` down to descendant views via `.environment(amfiNavService)` (or an explicit init parameter) so AssetsListView pull-to-refresh and the AMFI picker in Plan 03 can reach it — match how existing observable services are shared. Do NOT block the main thread: the services already wrap network/compute in Task internally.
  </action>
  <verify>
    <automated>cd "/Users/reo/My Projects/my-home" && grep -c 'refreshIfNeeded\|upsertIfNeeded' MyHomeApp/RootView.swift ; xcodebuild build -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - RootView contains `@State private var amfiNavService = AMFINavService()` and `@State private var netWorthSnapshotService = NetWorthSnapshotService()`
    - The `.onChange(of: scenePhase)` `.active` branch calls both `amfiNavService.refreshIfNeeded()` and `netWorthSnapshotService.upsertIfNeeded()` (grep returns >= 2 matches)
    - `.onAppear` injects modelContext into both new services
    - amfiNavService is made reachable by descendant asset views (environment or parameter) — grep shows `.environment(amfiNavService)` or an equivalent pass-through
    - Project builds on iPhone 17 simulator
  </acceptance_criteria>
  <done>Both services are constructed in RootView, injected with modelContext on appear, fired once per IST day on scenePhase .active, and amfiNavService is reachable by asset views.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AMFI network (portal.amfiindia.com) → app | Untrusted external NAVAll.txt payload parsed into Asset NAVs |
| scenePhase .active → services | App-active triggers background fetch + compute; must never block UI or crash |
| Account/Asset/Expense @Model → aggregation | Local financial data summed into net worth; sign errors corrupt the figure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04 | Tampering | Malformed NAVAll.txt (extra semicolons, NaN NAV, truncated rows) | mitigate | parseNAVAll guards: skip no-semicolon lines, `parts.count >= 6`, `Decimal(string:)` returns nil → skip row; never fatalError — proven by AMFINavServiceTests fixture |
| T-11-05 | Spoofing | HTTPS downgrade / MITM on AMFI fetch | mitigate | Hardcoded `https://portal.amfiindia.com/spages/NAVAll.txt`; ATS blocks plain HTTP by default; grep gate asserts no `http://` |
| T-11-06 | Information Disclosure / DoS | Network failure or AMFI down on scene activation | mitigate | D-07 silent failure — catch logs and returns, keeps cached NAV; staleness badge (Plan 03) signals age; no retry, no alternative endpoint |
| T-11-07 | Tampering | Net-worth sign error (CC debt added instead of subtracted) | mitigate | Reuse `AccountBalance.compute()` (verified Phase 10 sign fix); NetWorthAggregationTests asserts CC debt reduces total; never re-implement the balance formula |
| T-11-08 | DoS | 1.6 MB string parse on main thread | mitigate | parseNAVAll runs inside an async `Task` (off the synchronous scenePhase path); never called synchronously on main |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs — zero new SPM dependencies (URLSession/SwiftData built-ins) |
</threat_model>

<verification>
- AMFINavServiceTests green (parse guards + IST gate).
- NetWorthAggregationTests green (sign convention with CC debt; nil-field assets contribute 0).
- NetWorthSnapshotTests green (one row per IST day, full breakdown).
- RootView builds and fires both services on scenePhase .active.
- No `http://` in AMFINavService; no `@Attribute(.unique)` in NetWorthSnapshotService.
</verification>

<success_criteria>
- MF NAVs auto-refresh best-effort daily and on pull-to-refresh; UI never blocks; failures are silent (ASSET-03).
- Net worth = holdings + account balances with correct sign convention (ASSET-05).
- Exactly one net-worth snapshot per IST day, storing total + per-class breakdown (ASSET-08, D-09).
</success_criteria>

<output>
Create `.planning/phases/11-asset-tracker/11-02-SUMMARY.md` when done.
</output>
