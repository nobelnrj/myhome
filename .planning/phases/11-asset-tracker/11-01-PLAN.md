---
phase: 11-asset-tracker
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - MyHomeApp/Persistence/Schema/SchemaV7.swift
  - MyHomeApp/Persistence/Models/NetWorthSnapshot.swift
  - MyHomeApp/Persistence/Models/Asset.swift
  - MyHomeApp/Persistence/Models/Account.swift
  - MyHomeApp/Persistence/Models/Expense.swift
  - MyHomeApp/Persistence/Models/Category.swift
  - MyHomeApp/Persistence/Models/Note.swift
  - MyHomeApp/Persistence/Models/NoteBlock.swift
  - MyHomeApp/Persistence/Schema/MigrationPlan.swift
  - MyHomeApp/Persistence/ModelContainer+App.swift
  - MyHomeTests/SchemaV7MigrationTests.swift
  - MyHomeTests/AssetCRUDTests.swift
  - MyHomeTests/NetWorthSnapshotTests.swift
autonomous: true
requirements: [ASSET-01, ASSET-08]
must_haves:
  truths:
    - "App launches without crashing on a store migrated from SchemaV6 to SchemaV7"
    - "Existing Asset rows survive the migration with amfiSchemeCode == nil"
    - "A NetWorthSnapshot can be inserted, queried, and persisted"
    - "An Asset can be created, edited, and deleted in SwiftData"
  artifacts:
    - path: "MyHomeApp/Persistence/Schema/SchemaV7.swift"
      provides: "SchemaV7 versioned schema with Asset.amfiSchemeCode + NetWorthSnapshot @Model"
      contains: "enum SchemaV7"
    - path: "MyHomeApp/Persistence/Models/NetWorthSnapshot.swift"
      provides: "NetWorthSnapshot typealias to SchemaV7"
      contains: "typealias NetWorthSnapshot = SchemaV7.NetWorthSnapshot"
    - path: "MyHomeTests/SchemaV7MigrationTests.swift"
      provides: "BLOCKING V6->V7 migration fixture test"
      contains: "SchemaV7"
  key_links:
    - from: "MyHomeApp/Persistence/Schema/MigrationPlan.swift"
      to: "SchemaV7.self"
      via: "schemas + stages arrays append v6ToV7"
      pattern: "v6ToV7"
    - from: "MyHomeApp/Persistence/ModelContainer+App.swift"
      to: "SchemaV7.self"
      via: "Schema(versionedSchema:)"
      pattern: "versionedSchema: SchemaV7"
---

<objective>
Ship the SchemaV7 additive migration as one atomic commit: add `amfiSchemeCode: String?` to `Asset`, add the new `NetWorthSnapshot` @Model, flip ALL @Model typealiases to SchemaV7 together (STAB-08 footgun), append the `v6ToV7` stage, and bump the container. Also scaffold the Wave-0 test files that downstream plans depend on.

Purpose: SchemaV7 is the foundation every other Phase 11 plan builds on. A partial typealias flip is the known STAB-08 crash vector — all flips must land in one commit. This plan unblocks Waves 2 and 3.
Output: SchemaV7 schema, NetWorthSnapshot model + typealias, migration stage, container bump, and three test files (one BLOCKING migration fixture test).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-asset-tracker/11-CONTEXT.md
@.planning/phases/11-asset-tracker/11-RESEARCH.md
@.planning/phases/11-asset-tracker/11-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: SchemaV7 — Asset.amfiSchemeCode + NetWorthSnapshot @Model</name>
  <files>MyHomeApp/Persistence/Schema/SchemaV7.swift</files>
  <read_first>
    - MyHomeApp/Persistence/Schema/SchemaV6.swift (copy the entire enum verbatim; the Asset @Model is at lines ~247-266 — read the whole file to capture every model: Expense, Category, Note, NoteBlock, Account, Asset)
    - MyHomeApp/Persistence/Schema/SchemaV5.swift (to see how a new version enum is structured vs its predecessor)
    - .planning/phases/11-asset-tracker/11-PATTERNS.md (SchemaV7.swift section — exact target code for both @Models and the models array)
    - .planning/phases/11-asset-tracker/11-RESEARCH.md (SchemaV7 Migration Pattern section)
  </read_first>
  <action>
    Create `SchemaV7.swift` as `enum SchemaV7: VersionedSchema` with `versionIdentifier` of `.init(7, 0, 0)`. Copy ALL SchemaV6 @Models verbatim into SchemaV7 (Expense, Category, Note, NoteBlock, Account, Asset) — never reference SchemaV6 types from inside SchemaV7. Make two additive changes only:
    (1) On `SchemaV7.Asset`, append a new stored property `var amfiSchemeCode: String? = nil` AFTER all existing V6 Asset fields (name, assetClassRaw, units, costBasisPerUnit, currentNAV, navAsOfDate, createdAt). Per D-01.
    (2) Add a new `@Model final class NetWorthSnapshot` with these exact fields and defaults: `var id: UUID = UUID()`, `var date: Date = Date()` (UTC; upsert key = startOfTodayIST), `var totalNetWorth: Decimal = Decimal(0)`, `var mfValue: Decimal = Decimal(0)`, `var stockValue: Decimal = Decimal(0)`, `var npsValue: Decimal = Decimal(0)`, `var cashValue: Decimal = Decimal(0)`, `var createdAt: Date = Date()`. Per D-09 (total + per-class breakdown). Init sets `id` and `createdAt`.
    Add `NetWorthSnapshot.self` to the `models` array alongside all V6 models. CloudKit-readiness rules are mandatory: NO `@Attribute(.unique)` on any property; every property defaulted or optional; Decimal (never Double) for money; String raw values (never Swift enums) for stored type discriminators.
  </action>
  <verify>
    <automated>cd "/Users/reo/My Projects/my-home" && xcodebuild build -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - SchemaV7.swift defines `enum SchemaV7: VersionedSchema` with `versionIdentifier` == `.init(7, 0, 0)`
    - `SchemaV7.Asset` contains the line `var amfiSchemeCode: String? = nil` and retains all 7 V6 Asset fields
    - `SchemaV7.NetWorthSnapshot` exists with exactly the 8 fields listed (id, date, totalNetWorth, mfValue, stockValue, npsValue, cashValue, createdAt), all Decimal money fields typed `Decimal`
    - `grep -v '^//' MyHomeApp/Persistence/Schema/SchemaV7.swift | grep -c '@Attribute(.unique)'` returns 0
    - `SchemaV7.models` array includes `SchemaV7.NetWorthSnapshot.self` plus all 6 V6 model types
    - Project builds (build succeeds before typealias flip in Task 2 only if container still references V6 — expect the flip in Task 2; this task may be verified via the Task 2 build)
  </acceptance_criteria>
  <done>SchemaV7.swift exists with additive amfiSchemeCode on Asset and a complete NetWorthSnapshot @Model, CloudKit-ready, no unique constraints.</done>
</task>

<task type="auto">
  <name>Task 2: Atomic typealias flip + migration stage + container bump</name>
  <files>MyHomeApp/Persistence/Models/NetWorthSnapshot.swift, MyHomeApp/Persistence/Models/Asset.swift, MyHomeApp/Persistence/Models/Account.swift, MyHomeApp/Persistence/Models/Expense.swift, MyHomeApp/Persistence/Models/Category.swift, MyHomeApp/Persistence/Models/Note.swift, MyHomeApp/Persistence/Models/NoteBlock.swift, MyHomeApp/Persistence/Schema/MigrationPlan.swift, MyHomeApp/Persistence/ModelContainer+App.swift</files>
  <read_first>
    - MyHomeApp/Persistence/Models/Account.swift (the typealias + STAB-08 rationale comment to mirror for NetWorthSnapshot.swift)
    - MyHomeApp/Persistence/Models/Asset.swift (current state — points to SchemaV6.Asset)
    - MyHomeApp/Persistence/Models/Expense.swift, Category.swift, Note.swift, NoteBlock.swift (current typealias targets)
    - MyHomeApp/Persistence/Schema/MigrationPlan.swift (the schemas array, stages array, the existing v5ToV6 .custom stage to mirror, and the FB13812722 / didMigrate-synchronous comments at ~line 58)
    - MyHomeApp/Persistence/ModelContainer+App.swift (the two Schema(versionedSchema:) sites at ~lines 18, 42)
    - Memory schema-version-mutation-footgun (STAB-08): ALL typealiases flip together in ONE commit
  </read_first>
  <action>
    Make ALL of the following changes so they land in ONE atomic commit (STAB-08: a partial flip crashes on save/query):
    (1) Create `Models/NetWorthSnapshot.swift` with `typealias NetWorthSnapshot = SchemaV7.NetWorthSnapshot` plus the STAB-08 rationale doc-comment (mirror Account.swift's comment block).
    (2) Flip these existing typealiases from `SchemaV6.X` to `SchemaV7.X`: Asset, Account, Expense, Category, Note, NoteBlock. One-line change each.
    (3) In `MigrationPlan.swift`: append `SchemaV7.self` to the `schemas` array (never remove V1-V6); append `v6ToV7` to the `stages` array; add `static let v6ToV7 = MigrationStage.custom(fromVersion: SchemaV6.self, toVersion: SchemaV7.self, willMigrate: nil, didMigrate: nil)`. Use `.custom` (NOT `.lightweight`) per FB13812722 — mandatory for ALL stages. `didMigrate` is nil because the change is purely additive (amfiSchemeCode defaults nil; NetWorthSnapshot is a new entity — no backfill).
    (4) In `ModelContainer+App.swift`: change BOTH `Schema(versionedSchema: SchemaV6.self)` sites to `Schema(versionedSchema: SchemaV7.self)`. No other changes — store URL, App Group fallback, seeding call unchanged.
  </action>
  <verify>
    <automated>cd "/Users/reo/My Projects/my-home" && grep -rl 'SchemaV6\.' MyHomeApp/Persistence/Models/ | grep -c . ; xcodebuild build -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'typealias .* = SchemaV6' MyHomeApp/Persistence/Models/*.swift` returns 0 (no model typealias still points to V6)
    - Models/NetWorthSnapshot.swift contains `typealias NetWorthSnapshot = SchemaV7.NetWorthSnapshot`
    - Models/Asset.swift, Account.swift, Expense.swift, Category.swift, Note.swift, NoteBlock.swift each contain `= SchemaV7.`
    - MigrationPlan.swift `schemas` array ends with `SchemaV7.self`; `stages` array ends with `v6ToV7`; `v6ToV7` uses `.custom` with `willMigrate: nil, didMigrate: nil`
    - `grep -c 'versionedSchema: SchemaV6' MyHomeApp/Persistence/ModelContainer+App.swift` returns 0
    - `grep -c 'versionedSchema: SchemaV7' MyHomeApp/Persistence/ModelContainer+App.swift` returns 2
    - Project builds successfully on iPhone 17 simulator
  </acceptance_criteria>
  <done>All typealiases flipped to SchemaV7 in one commit, v6ToV7 .custom additive stage appended, container references SchemaV7, project builds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wave-0 test scaffolds — SchemaV7 migration (BLOCKING) + Asset CRUD + Snapshot</name>
  <files>MyHomeTests/SchemaV7MigrationTests.swift, MyHomeTests/AssetCRUDTests.swift, MyHomeTests/NetWorthSnapshotTests.swift</files>
  <read_first>
    - MyHomeTests/MigrationTests.swift (existing migration fixture test pattern — how a prior-version store is created and migrated; copy the harness)
    - MyHomeTests/AccountCRUDTests.swift (the in-memory ModelContainer Swift Testing pattern for CRUD — mirror exactly for assets)
    - MyHomeTests/Fixtures (any committed store fixtures used by MigrationTests)
    - MyHomeApp/Persistence/Schema/SchemaV7.swift (the models under test)
  </read_first>
  <behavior>
    SchemaV7MigrationTests (BLOCKING):
    - Test: a store created at SchemaV6 (or migrated up to V6) migrates to V7 without throwing; existing Asset rows have amfiSchemeCode == nil after migration
    - Test: NetWorthSnapshot entity is queryable (FetchDescriptor<NetWorthSnapshot> returns empty array, not a crash) after migration
    AssetCRUDTests (ASSET-01, ASSET-04):
    - Test: insert an Asset (name, assetClassRaw, units, costBasisPerUnit) + save → fetch returns 1 row with the values
    - Test: edit an Asset's currentNAV + save → fetch reflects the new value
    - Test: delete an Asset + save → fetch returns 0 rows
    - Test (ASSET-04 manual NAV preserved): a stock/nps Asset with a manually set currentNAV is not auto-overwritten by any code path in this plan (assert the stored value persists across a fetch)
    NetWorthSnapshotTests (ASSET-08 — partial; upsert logic lands in Plan 02, scaffold the model-level tests here):
    - Test: insert a NetWorthSnapshot with totalNetWorth + the 4 sub-totals + save → fetch returns the stored Decimal values exactly
  </behavior>
  <action>
    Create the three Swift Testing suites (`import Testing`, `@Suite`/`@Test`, in-memory `ModelContainer(for:configurations:)` with `isStoredInMemoryOnly: true` mirroring AccountCRUDTests). SchemaV7MigrationTests MUST mirror the existing MigrationTests fixture harness — if a V6 store fixture does not exist, create the store programmatically by building a ModelContainer at SchemaV6 schema, inserting an Asset, then re-opening with the AppMigrationPlan to force V6->V7. Mark NetWorthSnapshotTests upsert-behavior tests that depend on NetWorthSnapshotService as scaffolded stubs referencing the service that Plan 02 ships (model-level persistence tests run now; service upsert tests are filled in Plan 02). All money assertions use `Decimal`, never Double.
  </action>
  <verify>
    <automated>cd "/Users/reo/My Projects/my-home" && xcodebuild test -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:MyHomeTests/SchemaV7MigrationTests -only-testing:MyHomeTests/AssetCRUDTests 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - SchemaV7MigrationTests.swift exists and its migration test passes (V6 store migrates to V7 without throwing; migrated Asset has amfiSchemeCode == nil)
    - AssetCRUDTests insert/edit/delete tests pass against an in-memory SchemaV7 container
    - AssetCRUDTests includes a test asserting a manually-set stock/nps currentNAV persists unchanged (ASSET-04)
    - NetWorthSnapshotTests model-persistence test passes; service-upsert tests are present as named stubs to be completed in Plan 02
    - `xcodebuild test ... -only-testing:MyHomeTests/SchemaV7MigrationTests` reports the migration suite green
  </acceptance_criteria>
  <done>Three test files exist; the BLOCKING SchemaV7 migration test and Asset CRUD tests are green on iPhone 17 simulator.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SwiftData persistent store → app | Stored Asset/NetWorthSnapshot values are read into UI and aggregation; a corrupt/migrated store must not crash |
| SchemaV6 store → SchemaV7 migration | Existing on-device data crosses the migration boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | SchemaV7 migration of existing store | mitigate | `.custom` stage (FB13812722) with nil closures; additive-only (no field removal); BLOCKING SchemaV7MigrationTests fixture test gates the phase |
| T-11-02 | Denial of Service | Partial typealias flip (STAB-08) | mitigate | All 7 typealiases + container bump in ONE atomic commit; grep gate asserts zero `SchemaV6` typealiases remain |
| T-11-03 | Tampering | @Attribute(.unique) breaking CloudKit-readiness | mitigate | No `.unique` on any SchemaV7 property; NetWorthSnapshot dedup via upsert (Plan 02), not uniqueness; grep gate asserts 0 unique attributes |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this phase — zero new SPM dependencies (iOS SDK built-ins only) per milestone constraint |
</threat_model>

<verification>
- Project builds on iPhone 17 simulator (Xcode 26.5, scheme MyHome).
- SchemaV7MigrationTests (BLOCKING) green: V6→V7 migration does not throw; Asset.amfiSchemeCode nil post-migration; NetWorthSnapshot queryable.
- AssetCRUDTests green.
- No `SchemaV6.` typealias references remain in Models/.
- No `@Attribute(.unique)` in SchemaV7.swift.
</verification>

<success_criteria>
- SchemaV7 ships as one atomic commit (schema + all typealiases + migration stage + container bump).
- Existing Asset data survives migration; amfiSchemeCode defaults nil.
- NetWorthSnapshot @Model is created, queryable, and persists Decimal sub-totals.
- Wave-0 test scaffolds exist; the BLOCKING migration test is green — Waves 2 and 3 are unblocked.
</success_criteria>

<output>
Create `.planning/phases/11-asset-tracker/11-01-SUMMARY.md` when done.
</output>
