---
phase: 21-overview-filtering
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - MyHomeApp/Support/OverviewFilter.swift
  - MyHomeTests/OverviewFilterTests.swift
  - MyHome.xcodeproj/project.pbxproj
autonomous: true
requirements: [OVF-01, OVF-02]

must_haves:
  truths:
    - "A default OverviewFilter (no accounts selected, no custom range) matches EVERY expense, including nil-accountID rows — all-accounts is the default"
    - "Selecting one account or a subset passes only expenses whose accountID is in the set; unassigned rows pass only when includeUnassigned is true"
    - "rangeBoundaries(from:to:) returns inclusive full-day boundaries (00:00:00 start … 23:59:59 end) in the injected calendar, mirroring BudgetCalculator.monthBoundaries"
    - "BudgetCalculator.grossSpend/grossIncome over an account-filtered array still excludes confirmed AND pending transfer pairs (isTransferForCashFlow) — the exclusion survives filtering"
  artifacts:
    - path: "MyHomeApp/Support/OverviewFilter.swift"
      provides: "OverviewFilter value type (accountIDs, includeUnassigned, dateRange, isActive) + OverviewFilterEngine (matchesAccount, apply, rangeBoundaries)"
      contains: "struct OverviewFilter"
    - path: "MyHomeTests/OverviewFilterTests.swift"
      provides: "Swift Testing suite: account subset × date range totals, transfer-exclusion preservation, boundary inclusivity (IST-injected calendar)"
      contains: "struct OverviewFilterTests"
  key_links:
    - from: "MyHomeApp/Support/OverviewFilter.swift"
      to: "BudgetCalculator.isTransferForCashFlow"
      via: "engine doc-comment mandates callers compose apply() with the existing gross helpers — no re-implemented transfer logic"
      pattern: "isTransferForCashFlow"
    - from: "MyHome.xcodeproj/project.pbxproj"
      to: "OverviewFilter.swift"
      via: "4-edit registration (PBXBuildFile, PBXFileReference, PBXGroup children, Sources phase)"
      pattern: "OverviewFilter.swift in Sources"
---

<objective>
Create the filter state model and the pure filtering engine for Phase 21 (OVF-01,
OVF-02): `OverviewFilter` (selected account IDs + optional custom date range,
empty/nil = all-accounts current-month default) and `OverviewFilterEngine`
(account-membership matching, array filtering, and inclusive day-granular range
boundaries). Prove with unit tests that account-subset × date-range aggregation
is correct AND that the load-bearing confirmed-self-transfer exclusion
(`BudgetCalculator.isTransferForCashFlow` via `grossSpend`/`grossIncome`)
survives filtering unchanged.

Purpose: Plans 02 (threading) and 03 (UI) both consume this contract. Getting the
pure logic tested first means the view work is wiring, not math.

Output: `OverviewFilter.swift` implemented + registered in pbxproj;
`OverviewFilterTests` GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@MyHomeApp/Support/BudgetCalculator.swift
@MyHomeApp/Support/OverviewAggregation.swift
@MyHomeTests/OverviewAggregationTests.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define OverviewFilter + OverviewFilterEngine contracts and register both new files in pbxproj</name>
  <files>MyHomeApp/Support/OverviewFilter.swift, MyHomeTests/OverviewFilterTests.swift, MyHome.xcodeproj/project.pbxproj</files>
  <read_first>
    - MyHomeApp/Support/BudgetCalculator.swift — `monthBoundaries` (inclusive [start, end], Calendar.current + TimeZone.current) and `isTransferForCashFlow` (the exclusion contract to preserve)
    - MyHomeApp/Persistence/Schema/SchemaV9.swift (Expense model, ~lines 82–140) — Expense.accountID is a bare `UUID?` (nil = Unassigned), NOT a @Relationship; `sourceAccount: String?` is the Gmail-email dedup key — do NOT confuse the two
    - MyHome.xcodeproj/project.pbxproj — study `F405`/`A405` (OverviewAggregation.swift, app target) and `F402`/`A402` (OverviewAggregationTests.swift, test target) for the 4-edit pattern; Support group children ~line 730, MyHomeTests group ~line 787, app Sources phase ~line 1062, test Sources phase ~line 1172
  </read_first>
  <action>
    Create `MyHomeApp/Support/OverviewFilter.swift` with two types, mirroring the
    pure-helper discipline of OverviewAggregation (no SwiftUI, no @Query, no fetching):

    1. `struct OverviewFilter: Equatable` — value type OverviewView will hold in @State:
       - `var accountIDs: Set<UUID> = []` — selected Account.id values; EMPTY = all accounts (OVF-01 default)
       - `var includeUnassigned: Bool = false` — when a subset is selected, also pass expenses with `accountID == nil` (discretion choice: an "Unassigned" picker row means filtered money can never silently vanish; the flag is irrelevant while accountIDs is empty)
       - `var dateRange: ClosedRange<Date>?` — user-picked day-granular range; nil = default current-month scope (OVF-02)
       - `var accountFilterActive: Bool` — `!accountIDs.isEmpty || includeUnassigned`
       - `var isActive: Bool` — `accountFilterActive || dateRange != nil` (drives the OVF-03 chip in Plan 03)
       - The default `init()` must produce the all-accounts/current-month state so `filter = OverviewFilter()` IS the one-tap clear.

    2. `enum OverviewFilterEngine` — pure static helpers:
       - `static func matchesAccount(_ expense: Expense, filter: OverviewFilter) -> Bool` — true when `!filter.accountFilterActive`; otherwise true iff `expense.accountID` is in `filter.accountIDs`, or (`expense.accountID == nil` and `filter.includeUnassigned`)
       - `static func apply(_ filter: OverviewFilter, to expenses: [Expense]) -> [Expense]` — account-membership filter only; date scoping stays in the view's @Query predicate. Document this division of labour in the doc comment.
       - `static func rangeBoundaries(from: Date, to: Date, calendar: Calendar = .current) -> (start: Date, end: Date)` — `calendar.startOfDay(for: from)` through end-of-day of `to` (start-of-day(to) + 1 day − 1 second), mirroring the inclusive [start, end] convention of `BudgetCalculator.monthBoundaries` so the existing `date >= lo && date <= hi` @Query predicate keeps working verbatim. If `from > to`, swap them (defensive clamp — T-21-01). Calendar is injectable so tests can force Asia/Kolkata (IST) instead of depending on the simulator timezone — same pattern as AnalyticsAggregator.
       - The doc comment MUST state that transfer exclusion is NOT this engine's job: callers compose `apply(_:to:)` with `BudgetCalculator.grossSpend`/`grossIncome`/`monthlySpend`, which already route through `isTransferForCashFlow`. Never re-implement `isTransfer`/`transferPairID` logic here.

    Create `MyHomeTests/OverviewFilterTests.swift` as an empty-but-compiling Swift
    Testing scaffold (`import Testing`, `import SwiftData`, `@testable import MyHome`,
    `@MainActor struct OverviewFilterTests {}`) containing the in-memory ModelContainer
    helper copied from OverviewAggregationTests, extended with `Account.self`. Task 2
    fills in the tests.

    Register BOTH new files in `MyHome.xcodeproj/project.pbxproj`. The project has NO
    synchronized groups, so each file needs 4 manual edits: (1) PBXBuildFile entry,
    (2) PBXFileReference entry, (3) PBXGroup children (Support group for
    OverviewFilter.swift; MyHomeTests group for the test file), (4) the correct Sources
    build phase (app target for OverviewFilter.swift; test target for
    OverviewFilterTests.swift). Pick unused ID pairs following the existing F4xx/A4xx
    convention. Missing any edit means the file silently doesn't compile.
  </action>
  <verify>
    <automated>cd "/Users/reo/My Projects/my-home" && xcodebuild build -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' -quiet 2>&1 | tail -5 && grep -c "OverviewFilter.swift in Sources" MyHome.xcodeproj/project.pbxproj && grep -c "OverviewFilterTests.swift in Sources" MyHome.xcodeproj/project.pbxproj</automated>
  </verify>
  <acceptance_criteria>
    - Build succeeds with both new files compiled (not just present on disk)
    - Both grep counts return exactly `1`
    - `OverviewFilter()` default state: `accountIDs.isEmpty`, `includeUnassigned == false`, `dateRange == nil`, `isActive == false`
    - OverviewFilter.swift imports Foundation/SwiftData only — no SwiftUI
  </acceptance_criteria>
  <done>Contracts exist, compile in both targets, and Plans 02/03 can code against them without exploration.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test-drive OverviewFilterEngine — subset × range totals + transfer-exclusion preservation</name>
  <files>MyHomeTests/OverviewFilterTests.swift, MyHomeApp/Support/OverviewFilter.swift</files>
  <read_first>
    - MyHomeTests/OverviewAggregationTests.swift — container helper style, `private typealias Cat = MyHome.Category` disambiguation, @Test naming convention
    - MyHomeTests/BudgetCalculatorTests.swift — how transfer-pair expenses are constructed in tests (isTransfer / transferPairID permutations)
  </read_first>
  <behavior>
    - defaultFilterMatchesAll: default OverviewFilter passes expenses with accountID set AND accountID == nil (all-accounts default, OVF-01)
    - singleAccountSubset: with accountIDs = {A}, only account-A expenses survive apply(); account-B and unassigned rows are excluded
    - multiAccountSubset: with accountIDs = {A, B}, expenses of A and B survive; C and unassigned excluded
    - includeUnassigned: with accountIDs = {A} and includeUnassigned = true, unassigned rows survive alongside A's
    - transferExclusionPreserved: an account-filtered array containing (a) a confirmed transfer leg (isTransfer == true), (b) a pending pair leg (isTransfer == nil, transferPairID != nil), and (c) a rejected pair (isTransfer == false) — BudgetCalculator.grossSpend/grossIncome over apply(filter, ...) excludes (a) and (b) and includes (c); totals compared against Decimal(N) literals
    - accountTimesDateRange: expenses across two accounts and several dates; apply account filter + rangeBoundaries date window → grossSpend equals the hand-computed Decimal(N) for exactly that account × range cell (OVF-02)
    - rangeBoundariesInclusiveIST: with an injected Asia/Kolkata calendar, an expense at 00:00:00 IST on the start day and one at 23:59:00 IST on the end day both fall inside [start, end]; one at 00:00:00 IST the day after end falls outside
    - rangeBoundariesSwapped: from > to is swapped, not crashed/empty
  </behavior>
  <action>
    Write the behaviors above as @Test functions in OverviewFilterTests (RED), then
    implement/adjust `OverviewFilterEngine` until GREEN. Build fixture expenses via the
    in-memory container: create `Account` models, insert `Expense` rows, set
    `expense.accountID = account.id` (bare UUID link — never touch `sourceAccount`).
    For date tests, construct dates with `DateComponents` through the injected
    Asia/Kolkata calendar — never `Date()` arithmetic against the simulator timezone.

    Decimal footgun (memory: decimal-literal-comparison-footgun): every money
    assertion compares against `Decimal(9500)`-style constructors, NEVER bare
    arithmetic literals like `10_000 - 500`.

    Commit sequence per TDD discipline: `test(21-01): …` (RED) then
    `feat(21-01): …` (GREEN).
  </action>
  <verify>
    <automated>cd "/Users/reo/My Projects/my-home" && xcodebuild test -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:MyHomeTests/OverviewFilterTests -quiet 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - All OverviewFilterTests PASS (≥ 8 tests covering the behaviors above)
    - `grep -c 'Decimal(' MyHomeTests/OverviewFilterTests.swift` ≥ 4 and no bare-arithmetic money comparisons
    - Transfer-exclusion test routes through BudgetCalculator.grossSpend/grossIncome (grep `grossSpend` in the test file ≥ 1) — not a re-implemented filter
    - No modification to BudgetCalculator.swift (its contract is consumed, not changed)
  </acceptance_criteria>
  <done>Account-subset × date-range math is proven correct with the self-transfer exclusion intact, before any view code exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user date-picker input → range math | Arbitrary from/to dates (including from > to) enter boundary computation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | Tampering | OverviewFilterEngine.rangeBoundaries | mitigate | Swap when from > to; boundaries derived via Calendar (no manual epoch math); calendar-injectable and unit-tested at IST day edges |
| T-21-02 | Tampering (integrity) | filtered cash-flow totals | mitigate | Engine never re-implements transfer logic; tests assert grossSpend/grossIncome over filtered arrays still exclude confirmed + pending pairs via isTransferForCashFlow |
| T-21-SC | Tampering | npm/pip/cargo installs | accept | No package installs — first-party Apple frameworks only |
</threat_model>

<verification>
- `xcodebuild test … -only-testing:MyHomeTests/OverviewFilterTests` fully GREEN.
- Full build succeeds; both new files registered with all 4 pbxproj edits each.
- `xcodebuild test … -only-testing:MyHomeTests/BudgetCalculatorTests` still GREEN (contract untouched).
</verification>

<success_criteria>
- OVF-01: empty selection = all accounts (default) proven by test; single/subset selection filters correctly.
- OVF-02: account × custom-date-range totals proven correct with the confirmed-self-transfer exclusion preserved.
</success_criteria>

<output>
Create `.planning/phases/21-overview-filtering/21-01-SUMMARY.md` when done.
</output>
