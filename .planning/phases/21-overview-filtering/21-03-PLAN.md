---
phase: 21-overview-filtering
plan: 03
type: execute
wave: 3
depends_on: ["21-01", "21-02"]
files_modified:
  - MyHomeApp/Features/Overview/OverviewFilterSheet.swift
  - MyHomeApp/Features/Overview/OverviewScopePill.swift
  - MyHomeApp/Features/Overview/OverviewView.swift
  - MyHomeApp/MyHomeApp.swift
  - MyHome.xcodeproj/project.pbxproj
autonomous: true
requirements: [OVF-01, OVF-03]

must_haves:
  truths:
    - "User can open a filter sheet from the Overview header and select a single account, a subset, or Unassigned; All Accounts is the pre-selected default"
    - "User can switch the sheet's date section from This Month (default) to a custom from/to range"
    - "While a filter is active, the header scope pill names the selection (accounts + range) — the filter is never invisible"
    - "One tap on the pill's clear control (xmark replacing the chevron) resets to all-accounts / current-month and every figure returns to the unfiltered value"
    - "Dark mode rendering of existing surfaces is untouched — DesignTokens dark branches unmodified, dark bit-identity tests green"
  artifacts:
    - path: "MyHomeApp/Features/Overview/OverviewFilterSheet.swift"
      provides: "Account multi-select (All Accounts / per-account / Unassigned rows) + This Month vs custom from/to date pickers, neumorphic styling"
      contains: "struct OverviewFilterSheet"
    - path: "MyHomeApp/Features/Overview/OverviewScopePill.swift"
      provides: "Active-filter chip: summary label + one-tap clear button"
      contains: "struct OverviewScopePill"
  key_links:
    - from: "MyHomeApp/Features/Overview/OverviewView.swift"
      to: "OverviewFilterSheet"
      via: "sheet(isPresented:) bound to @State filter"
      pattern: "OverviewFilterSheet"
    - from: "OverviewScopePill clear button"
      to: "OverviewFilter()"
      via: "single-tap reset to the default value"
      pattern: "OverviewFilter\\(\\)"
---

<objective>
Build the visible half of the phase: the filter entry button, the
`OverviewFilterSheet` (account multi-select + custom date range), and the
`OverviewScopePill` active-filter chip with one-tap clear (OVF-01 selection UI,
OVF-03 visibility + clear). Adopts the neumorphic design system (DesignTokens +
NeuSurface) in both light and dark without touching any dark token branch.

Purpose: Plans 01/02 made the math and threading correct; this plan makes the
capability reachable and its state impossible to miss.

Output: Fully wired filter UI; seeded-simulator screenshot evidence of default,
account-filtered, range-filtered, and cleared states; full suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-overview-filtering/21-02-SUMMARY.md
@MyHomeApp/Features/Overview/OverviewView.swift
@MyHomeApp/Support/OverviewFilter.swift
@MyHomeApp/DesignSystem/DesignTokens.swift
@MyHomeApp/DesignSystem/NeuSurface.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: OverviewFilterSheet (account multi-select + date range) + seed accounts for visual verification + pbxproj</name>
  <files>MyHomeApp/Features/Overview/OverviewFilterSheet.swift, MyHomeApp/MyHomeApp.swift, MyHome.xcodeproj/project.pbxproj</files>
  <read_first>
    - .planning/phases/21-overview-filtering/21-UI-REFERENCE.md AND the images it names (21-REF-overview-pill.png, 21-REF-filter-sheet.png) — BINDING visual contract; view the images. Its Decisions section overrides conflicting prose in this plan (no separate chip bar; date section lives in this same sheet; subtitle is "Applies to your Overview").
    - MyHomeApp/Support/OverviewFilter.swift — the state contract this sheet edits
    - MyHomeApp/DesignSystem/NeuSurface.swift + DesignTokens.swift — surface/label/accent tokens; read-only, NEVER edit (dark bit-identity)
    - MyHomeApp/Features/Expenses/AddTransferView.swift or Settings account list — existing account-row rendering conventions (name, symbolName, colorHex, last4)
    - MyHomeApp/MyHomeApp.swift `seedSampleDataIfRequested()` — DEBUG-only seeder to extend
  </read_first>
  <action>
    Create `MyHomeApp/Features/Overview/OverviewFilterSheet.swift`:
    - `struct OverviewFilterSheet: View` taking `@Binding var filter: OverviewFilter`.
      Edits apply LIVE through the binding (figures behind the sheet recompute as
      the user taps); "Done" only dismisses; a "Reset" toolbar/button sets
      `filter = OverviewFilter()`.
    - Accounts section: own `@Query(filter: #Predicate<Account> { !$0.isArchived }, sort: \Account.sortOrder)`.
      Rows: an "All Accounts" row (checked when `!filter.accountFilterActive`;
      tapping it empties `accountIDs` and clears `includeUnassigned`), one row per
      account (toggle its `id` in `filter.accountIDs`; show name + SF symbol +
      colorHex + last4 per existing account-row conventions), and an "Unassigned"
      row toggling `filter.includeUnassigned` (covers manual/legacy expenses with
      `accountID == nil`). Checkmarks reflect membership; multi-select, no
      radio-button behavior.
    - Date section: a "This Month" default state vs custom range. When custom is
      enabled, two compact day-granularity `DatePicker`s (From / To) write
      `filter.dateRange = from...to` (clamp so from ≤ to before assigning — the
      engine also swaps defensively). Disabling custom sets `dateRange = nil`.
    - Style with existing tokens only: `.neuSurface(...)` rows/sections,
      `DesignTokens.label/label2/accentText`, both light and dark come free from
      the token system. Do NOT define new colors and do NOT touch
      DesignTokens/NeuSurface source.
    - Presentation: `.presentationDetents([.medium, .large])`.

    Extend `seedSampleDataIfRequested()` in MyHomeApp.swift (DEBUG-only): create
    two sample accounts ("SAMPLE HDFC", "SAMPLE ICICI Credit" with distinct
    colorHex/typeRaw, explicit sortOrder) and attribute the seeded expenses
    alternately via `expense.accountID = account.id`, leaving the income row
    unassigned — this makes account filtering visually verifiable in the
    simulator. Keep the existing idempotence guard.

    Register OverviewFilterSheet.swift in pbxproj (4 edits: PBXBuildFile,
    PBXFileReference, Overview group `G124` children, app-target Sources phase).
  </action>
  <verify>
    <automated>cd "/Users/reo/My Projects/my-home" && xcodebuild build -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' -quiet 2>&1 | tail -5 && grep -c "OverviewFilterSheet.swift in Sources" MyHome.xcodeproj/project.pbxproj && git diff --name-only | grep -c "DesignSystem" | grep -qx 0 && echo DESIGN_UNTOUCHED</automated>
  </verify>
  <acceptance_criteria>
    - Build succeeds; pbxproj grep returns `1`; DESIGN_UNTOUCHED prints (no DesignSystem edits)
    - Sheet edits the binding live; Reset restores `OverviewFilter()`
    - "All Accounts" row is checked by default; account rows multi-select; "Unassigned" row present
    - Seeder creates 2 accounts and attributes seeded expenses (DEBUG only, idempotent)
  </acceptance_criteria>
  <done>The selection surface exists, styled with the existing neumorphic system, editable live against the Plan 01 contract.</done>
</task>

<task type="auto">
  <name>Task 2: OverviewScopePill (header pill = entry point AND active-state display) + wiring + screenshot self-verification</name>
  <files>MyHomeApp/Features/Overview/OverviewScopePill.swift, MyHomeApp/Features/Overview/OverviewView.swift, MyHome.xcodeproj/project.pbxproj</files>
  <read_first>
    - .planning/phases/21-overview-filtering/21-UI-REFERENCE.md AND the images it names (21-REF-overview-pill.png, 21-REF-filter-sheet.png) — BINDING visual contract; view the images. Its Decisions section overrides conflicting prose in this plan (no separate chip bar; date section lives in this same sheet; subtitle is "Applies to your Overview").
    - MyHomeApp/Features/Overview/OverviewView.swift — the header VStack (eyebrow month + "Overview" title, `.entrance(0)`) where the button and chip slot in; the ReviewBanner struct as the in-file pattern for a tappable neu chip row
    - .claude/projects memory "simulator-screenshot-verify-loop" pattern — build → install → launch `-seedSampleData` → screenshot
  </read_first>
  <action>
    Create `MyHomeApp/Features/Overview/OverviewScopePill.swift`:
    - `struct OverviewScopePill: View` taking the active `filter`, the resolved
      account names for the selection, and an `onClear: () -> Void` closure.
    - Renders ONLY when `filter.isActive` (caller gates it): a compact neu chip
      row summarizing the state, e.g. "HDFC +1 · 5 Jun – 12 Jul" — first selected
      account name plus "+N" overflow ("Unassigned" counts as a name), and the
      range segment only when a custom range is set; omit the account segment
      when only the range is active. Trailing `xmark.circle.fill` button calls
      `onClear` — ONE tap, no confirmation (OVF-03). Give the clear button
      `.accessibilityLabel("Clear filters")` and ≥44pt hit target.

    Wire OverviewView:
    - Header row: add a trailing filter button (`line.3.horizontal.decrease.circle`,
      `DesignTokens.accentText` tint) beside the "Overview" title opening
      `.sheet { OverviewFilterSheet(filter: $filter) }`.
    - Directly under the header, render `OverviewScopePill` when `filter.isActive`
      with `onClear: { filter = OverviewFilter() }`. Resolve selected account
      names via the existing `allAccounts` @Query (pass them down from
      OverviewMonthContent or hoist a lightweight name lookup — do not add a
      second fetch mechanism).
    - Register OverviewScopePill.swift in pbxproj (same 4-edit pattern, Overview
      group G124 + app Sources).

    Self-verify with the screenshot loop (do not ship blind): build, install on
    the iPhone 17 simulator, launch with `-seedSampleData`, then capture:
    (1) default Overview — no chip, unchanged layout; (2) filtered to "SAMPLE
    HDFC" — chip visible, hero/donut totals visibly smaller and consistent with
    each other, Net Worth + Budgets sections gone; (3) custom range active —
    header shows range label, Over Time hidden; (4) after tapping clear — chip
    gone, figures identical to screenshot (1). Repeat screenshot (2) in dark
    appearance to eyeball the chip/sheet using dark tokens correctly.
  </action>
  <verify>
    <automated>cd "/Users/reo/My Projects/my-home" && xcodebuild test -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:MyHomeTests -quiet 2>&1 | tail -8 && grep -c "OverviewScopePill.swift in Sources" MyHome.xcodeproj/project.pbxproj</automated>
    <human-check>End-of-phase: on the seeded simulator, filter to one account, add a custom range, confirm every figure changes together, then clear with one tap and confirm all figures return to the unfiltered values with no leftover chip.</human-check>
  </verify>
  <acceptance_criteria>
    - Full MyHomeTests suite GREEN (incl. DesignTokensTests dark bit-identity, OverviewFilterTests)
    - pbxproj grep returns `1`; no DesignSystem/ file modified this plan (git diff check)
    - All four screenshots captured and consistent per the sequence above; screenshot (4) matches (1)
    - Clear is a single tap from the chip — no menu, no sheet round-trip
  </acceptance_criteria>
  <done>The filter is discoverable, its active state is unmissable, and one tap restores the all-accounts current-month Overview with zero stale figures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user picker input → filter state | Account taps and date pickers mutate the state that reshapes all figures |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-05 | Spoofing (misrepresentation) | OverviewScopePill | mitigate | Chip renders whenever `filter.isActive` — filtered figures can never masquerade as all-account totals (OVF-03); header label swaps to range label under a custom range |
| T-21-06 | Denial of Service | date pickers | mitigate | from ≤ to clamped in the sheet AND swapped in the engine; day-granular pickers, no free-text date input |
| T-21-07 | Tampering | DEBUG seeder extension | mitigate | Account seeding stays inside `#if DEBUG` + `-seedSampleData` guard; idempotence check retained — release builds unaffected |
| T-21-SC | Tampering | npm/pip/cargo installs | accept | No package installs — first-party Apple frameworks only |
</threat_model>

<verification>
- Full MyHomeTests suite GREEN; dark bit-identity intact; no DesignSystem source edits across the phase (`git log --stat` for phase commits shows none).
- Screenshot evidence: default / account-filtered / range-filtered / cleared, plus one dark-mode capture.
- OVF-03 walkthrough: active filter always visible, clears in one tap, figures return exactly to defaults.
</verification>

<success_criteria>
- OVF-01: single account, subset, and Unassigned selectable; All Accounts default pre-selected.
- OVF-03: active filter clearly shown via chip + range-aware header; one-tap clear; no stale figure remains in any state transition.
</success_criteria>

<output>
Create `.planning/phases/21-overview-filtering/21-03-SUMMARY.md` when done.
</output>
