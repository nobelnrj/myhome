---
phase: 21-overview-filtering
plan: 02
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - MyHomeApp/Features/Overview/OverviewView.swift
autonomous: true
requirements: [OVF-01, OVF-02, OVF-03]

must_haves:
  truths:
    - "With the default filter, every Overview figure is byte-for-byte what it was before this plan (all-accounts, current month — zero regression)"
    - "With an account subset selected, the hero income/spend/net, spend donut, by-category bars, and Recent list all recompute from ONLY that subset's expenses"
    - "With a custom date range set, the @Query window becomes that range (inclusive day boundaries) and the header label reflects the range, not the stale month name"
    - "Filtered totals still exclude confirmed + pending self-transfer pairs (grossSpend/grossIncome over the filtered array)"
    - "No unfiltered figure survives on screen while a filter is active: Net Worth card and Budgets sections are suppressed (they are all-account / whole-month concepts), and Over Time hides under a custom range"
  artifacts:
    - path: "MyHomeApp/Features/Overview/OverviewView.swift"
      provides: "@State OverviewFilter threaded into OverviewMonthContent; effective bounds = custom range ?? month; all cash-flow aggregations fed from OverviewFilterEngine.apply output"
      contains: "OverviewFilterEngine.apply"
  key_links:
    - from: "MyHomeApp/Features/Overview/OverviewView.swift"
      to: "OverviewFilterEngine.rangeBoundaries"
      via: "effective (start, end) — custom range overrides BudgetCalculator.monthBoundaries"
      pattern: "rangeBoundaries"
    - from: "OverviewMonthContent body"
      to: "BudgetCalculator.grossSpend / grossIncome / monthlySpend"
      via: "aggregations consume the account-filtered array, not the raw @Query array"
      pattern: "grossSpend\\(for: visibleExpenses"
---

<objective>
Thread `OverviewFilter` through the existing Overview screen so every figure
recomputes for the account × date-range selection (OVF-01, OVF-02) and no stale
unfiltered figure remains visible while a filter is active (OVF-03, data half —
the visible chip/clear UI lands in Plan 03). This EXTENDS the existing
OverviewView/OverviewMonthContent — no rebuild, no new files, no pbxproj edits.

Purpose: The recompute path is the phase's core promise. Doing it before the UI
means Plan 03 only binds controls to already-correct state.

Output: OverviewView.swift threading the filter; default-state behavior
unchanged; full test suite still GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-overview-filtering/21-01-SUMMARY.md
@MyHomeApp/Features/Overview/OverviewView.swift
@MyHomeApp/Support/OverviewFilter.swift
@MyHomeApp/Support/BudgetCalculator.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thread filter state + effective date bounds + account-filtered cash-flow aggregations</name>
  <files>MyHomeApp/Features/Overview/OverviewView.swift</files>
  <read_first>
    - MyHomeApp/Features/Overview/OverviewView.swift — the FULL file: OverviewView owns month bounds and passes start/end into `OverviewMonthContent`, whose init builds the @Query predicate (`expense.date >= lo && expense.date <= hi`); child re-init on changed init params is the established re-query mechanism ("RESEARCH OQ3" comment)
    - MyHomeApp/Support/OverviewFilter.swift — the Plan 01 contract (matchesAccount / apply / rangeBoundaries / isActive)
  </read_first>
  <action>
    In `OverviewView`:
    - Add `@State private var filter = OverviewFilter()`.
    - Compute effective bounds: if `filter.dateRange` is non-nil, use
      `OverviewFilterEngine.rangeBoundaries(from: range.lowerBound, to: range.upperBound)`;
      otherwise keep `BudgetCalculator.monthBoundaries(for: currentMonth)` exactly as today.
    - Pass `filter` (plus the new bounds) into `OverviewMonthContent`. Because
      the child is re-initialized when init params change, the @Query date window
      updates automatically — do NOT invent a new re-query mechanism.
    - Header label: keep `start.formattedAsMonthYear()` for the default; when a
      custom range is active, pass a range label built with a private helper
      using DateFormatter/`Date.FormatStyle` in "5 Jun – 12 Jul 2026" style
      (reuse existing Date+Display conventions; no new files). A stale month
      name over range-filtered figures would violate OVF-03.

    In `OverviewMonthContent`:
    - Accept `let filter: OverviewFilter`.
    - First line of `body`'s aggregation block:
      `let visibleExpenses = OverviewFilterEngine.apply(filter, to: monthExpenses)`.
    - Rewire ALL cash-flow aggregations from `monthExpenses` → `visibleExpenses`:
      `spendByCategory` (BudgetCalculator.monthlySpend), `totalSpend`
      (grossSpend), `totalIncome` (grossIncome), `rankedSpend`/donut/`categoryItems`,
      and `recent`. The transfer exclusion is inherited automatically because
      these helpers route through `isTransferForCashFlow` — do not add any
      transfer logic in the view.
    - `reviewItems` (Gmail triage banner count) stays UNFILTERED — it is a
      triage queue, not a financial figure (document with a comment).
  </action>
  <verify>
    <automated>cd "/Users/reo/My Projects/my-home" && xcodebuild build -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' -quiet 2>&1 | tail -5 && grep -c "OverviewFilterEngine.apply" MyHomeApp/Features/Overview/OverviewView.swift</automated>
  </verify>
  <acceptance_criteria>
    - Build succeeds; `OverviewFilterEngine.apply` appears in OverviewView.swift
    - `grep -n "grossSpend(for: monthExpenses\|grossIncome(for: monthExpenses\|monthlySpend(for: monthExpenses" MyHomeApp/Features/Overview/OverviewView.swift` returns NOTHING (all cash-flow aggregations consume visibleExpenses)
    - Default `OverviewFilter()` path is structurally identical to pre-change behavior (apply returns the input array untouched when inactive)
  </acceptance_criteria>
  <done>Hero, donut, by-category bars, and Recent all derive from the account-filtered, range-scoped expense set; default state unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Suppress unfilterable global sections while a filter is active (no stale figures)</name>
  <files>MyHomeApp/Features/Overview/OverviewView.swift</files>
  <read_first>
    - MyHomeApp/Features/Overview/OverviewView.swift — the `showNetWorth` gate, the "Over Time" section (feeds on allGlobalExpenses), the Budgets glance (`budgeted`), and SpendBudgetCard's `budgetedSpent`/`totalBudget` params (0 budget = strip's existing "no budget set" rendering path)
  </read_first>
  <action>
    OVF-03 bans stale/unfiltered figures on a filtered screen. Apply these
    discretion rules (document each with a short comment naming OVF-03):

    - Net Worth card: render only when `!filter.isActive` (in addition to the
      existing `showNetWorth` gate). Net worth is a balance-sheet total across
      ALL accounts + assets; showing it beside subset cash flow is a stale figure.
    - Budgets glance section AND the hero budget strip: budgets are whole-month,
      all-account concepts. When `filter.isActive`, hide the Budgets section and
      pass `budgetedSpent: 0, totalBudget: 0` to SpendBudgetCard so the strip
      renders its existing no-budget state. When inactive, behavior is unchanged.
    - Over Time chart: when only the ACCOUNT filter is active, feed it
      `OverviewFilterEngine.apply(filter, to: allGlobalExpenses)` so its series
      recomputes for the subset. When a CUSTOM DATE RANGE is active, hide the
      section entirely (its multi-month window contradicts the selected range).

    Do NOT touch DesignTokens, NeuSurface, or any dark-mode token branch —
    DarkBitIdentity tests must stay green (Phase 17 contract).

    Self-verify visually (memory: simulator-screenshot-verify-loop): build,
    install, launch with `-seedSampleData`, screenshot the Overview, and confirm
    the default render is unchanged from pre-phase (no filter UI exists yet, so
    the screen must look identical).
  </action>
  <verify>
    <automated>cd "/Users/reo/My Projects/my-home" && xcodebuild test -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:MyHomeTests -quiet 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - Full MyHomeTests suite GREEN (including DesignTokensTests dark bit-identity and OverviewFilterTests)
    - `grep -c "filter.isActive" MyHomeApp/Features/Overview/OverviewView.swift` ≥ 2 (net-worth + budgets gates)
    - No edits to MyHomeApp/DesignSystem/ files (git diff shows only OverviewView.swift changed)
    - Seeded-simulator screenshot of the default Overview matches pre-change layout
  </acceptance_criteria>
  <done>While any filter is active, every visible figure is filter-consistent; unfilterable sections are suppressed rather than left stale.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filter state → financial figures | User-controlled filter reshapes every money figure on the dashboard |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-03 | Tampering (integrity) | OverviewMonthContent aggregation | mitigate | Single `visibleExpenses` source feeds ALL cash-flow figures — no figure can consume the unfiltered array; grep gate in acceptance criteria enforces it |
| T-21-04 | Information Disclosure | none new | accept | Filtering only narrows already-visible local data; nothing leaves the device |
| T-21-SC | Tampering | npm/pip/cargo installs | accept | No package installs — first-party Apple frameworks only |
</threat_model>

<verification>
- Full MyHomeTests suite GREEN (regression: BudgetCalculatorTests, OverviewAggregationTests, DesignTokensTests dark identity).
- Default-filter Overview screenshot identical to pre-phase render.
- No `monthExpenses` consumer remains among the cash-flow aggregations.
</verification>

<success_criteria>
- OVF-01/OVF-02: hero, donut, and totals recompute for account × date-range selections; all-accounts current-month remains the untouched default.
- OVF-03 (data half): no unfiltered figure is rendered while a filter is active.
</success_criteria>

<output>
Create `.planning/phases/21-overview-filtering/21-02-SUMMARY.md` when done.
</output>
