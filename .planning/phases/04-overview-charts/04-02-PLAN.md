---
phase: 04-overview-charts
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - MyHomeApp/Support/Decimal+INR.swift
  - MyHomeApp/Support/SpendOverTimeAggregator.swift
  - MyHomeApp/Support/OverviewAggregation.swift
autonomous: true
requirements: [OVR-01, OVR-02, OVR-03, EXP-11]
must_haves:
  truths:
    - "SpendOverTimeAggregatorTests, OverviewAggregationTests, DecimalINRTests all pass (RED → GREEN)"
    - "SpendOverTimeAggregator emits all date slots including zero-spend ones; Decimal→Double converts at the aggregation boundary"
    - "OverviewAggregation computes aggregate threshold (0.8/1.0), top-3 ranking with alphabetical tie-break, and OVR-03 pinned/checklist/nil resolution"
    - "formattedINRCompact() returns ₹N / ₹Nk / ₹NL per thresholds"
  artifacts:
    - path: "MyHomeApp/Support/SpendOverTimeAggregator.swift"
      provides: "EXP-11 pure bucketing helper + SpendRange + SpendBucket"
      contains: "enum SpendOverTimeAggregator"
    - path: "MyHomeApp/Support/OverviewAggregation.swift"
      provides: "OVR-01/02/03 pure aggregation helpers"
      contains: "enum OverviewAggregation"
    - path: "MyHomeApp/Support/Decimal+INR.swift"
      provides: "formattedINRCompact() chart-axis formatter"
      contains: "func formattedINRCompact"
  key_links:
    - from: "MyHomeApp/Support/SpendOverTimeAggregator.swift"
      to: "NSDecimalNumber"
      via: "Decimal→Double at aggregation boundary"
      pattern: "NSDecimalNumber\\(decimal:"
    - from: "MyHomeApp/Support/OverviewAggregation.swift"
      to: "NoteListOrganizer.organize"
      via: "OVR-03 pinned resolution (not note.isPinned)"
      pattern: "NoteListOrganizer\\.organize"
---

<objective>
Implement the three pure helpers whose contracts were locked by Plan 04-01's failing tests (GREEN phase): extend `Decimal+INR.swift` with `formattedINRCompact()`, create `SpendOverTimeAggregator` (EXP-11 week/month/year bucketing) and `OverviewAggregation` (OVR-01 aggregate threshold math, OVR-02 top-3 ranking, OVR-03 pinned-note resolution). All three are pure, SwiftUI-free, `Support/` helpers mirroring `CalendarAggregator` / `BudgetCalculator`.

Purpose: These helpers are the unit-testable core of Phase 4. Putting the math here (not in views) keeps the chart/card views dumb and the logic verifiable. This plan turns Plan 04-01's RED suite GREEN.
Output: 1 extended file + 2 new files in `MyHomeApp/Support/`; the SpendOverTime / Overview / DecimalINR test suites pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-overview-charts/04-RESEARCH.md
@.planning/phases/04-overview-charts/04-PATTERNS.md
@.planning/phases/04-overview-charts/04-01-SUMMARY.md
@MyHomeApp/Support/CalendarAggregator.swift
@MyHomeApp/Support/BudgetCalculator.swift
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: formattedINRCompact() on Decimal</name>
  <files>MyHomeApp/Support/Decimal+INR.swift</files>
  <read_first>
    - MyHomeApp/Support/Decimal+INR.swift (existing formattedINR() — add the new method in the same extension block, identical style)
    - .planning/phases/04-overview-charts/04-PATTERNS.md (lines 461-490: exact thresholds and doc-comment)
    - MyHomeTests/DecimalINRTests.swift (the contract this must satisfy — created in 04-01)
  </read_first>
  <behavior>
    - d >= 100_000 → "₹\(Int(d / 100_000))L"
    - d >= 1_000 → "₹\(Int(d / 1_000))k"
    - else → "₹\(Int(d))"
    - Conversion via NSDecimalNumber(decimal: self).doubleValue
  </behavior>
  <action>
    Add `func formattedINRCompact() -> String` to the existing `extension Decimal` in `Decimal+INR.swift`, immediately after `formattedINR()`. Convert `self` to `Double` via `NSDecimalNumber(decimal: self).doubleValue`, then branch on `>= 100_000` ("₹NL"), `>= 1_000` ("₹Nk"), else ("₹N"), using `Int(...)` truncation. Add a doc-comment noting it is for chart axis labels / bar annotations only; `formattedINR()` is used everywhere else. Do NOT introduce `NumberFormatter`.
  </action>
  <verify>
    <automated>xcodebuild test -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:MyHomeTests/DecimalINRTests 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "func formattedINRCompact" MyHomeApp/Support/Decimal+INR.swift` returns 1.
    - `MyHomeTests/DecimalINRTests` passes (all `@Test` green).
    - `grep -c "NumberFormatter" MyHomeApp/Support/Decimal+INR.swift` returns 0.
  </acceptance_criteria>
  <done>`formattedINRCompact()` exists and DecimalINRTests is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SpendOverTimeAggregator + SpendRange + SpendBucket (EXP-11)</name>
  <files>MyHomeApp/Support/SpendOverTimeAggregator.swift</files>
  <read_first>
    - MyHomeApp/Support/CalendarAggregator.swift (exact structural analog: pure `enum`, `startOfDay(_:)` timezone helper at lines 44-49 — copy verbatim, doc-comment header style)
    - MyHomeApp/Support/BudgetCalculator.swift (NSDecimalNumber Decimal→Double pattern; refund/negative-amount convention)
    - .planning/phases/04-overview-charts/04-RESEARCH.md (§ "Pattern 3" contract; § "Code Examples" week-bucketing reference at lines 631-661)
    - .planning/phases/04-overview-charts/04-PATTERNS.md (lines 390-458: SpendBucket struct, SpendRange enum, bucket() signature)
    - MyHomeTests/SpendOverTimeAggregatorTests.swift (the contract — created in 04-01)
  </read_first>
  <behavior>
    - Week → 7 daily buckets (today-6 … today), all slots emitted even at spent 0.0
    - Month → all calendar days of the current month (28-31 buckets), all slots emitted
    - Year → 12 monthly buckets keyed to start-of-month of the current year
    - Each SpendBucket.spent is Double, converted at the aggregation boundary via NSDecimalNumber(decimal:).doubleValue
    - Bucketing uses Calendar.current with TimeZone.current (device timezone) via a copied startOfDay helper
  </behavior>
  <action>
    Create `MyHomeApp/Support/SpendOverTimeAggregator.swift`. Define `enum SpendRange: String, CaseIterable { case week, month, year; var label: String }` (labels "Week"/"Month"/"Year"). Define `struct SpendBucket: Identifiable` with `let id: Date`, `let date: Date`, `let spent: Double`, `var dateLabel: String`. Define `enum SpendOverTimeAggregator` with `static func bucket(expenses: [Expense], range: SpendRange, calendar: Calendar = .current) -> [SpendBucket]` plus private per-range helpers (`weekBuckets`/`monthBuckets`/`yearBuckets`). Copy the `private static func startOfDay(_:)` device-timezone helper verbatim from `CalendarAggregator`. For week/month: generate the full slot list first, accumulate `expense.amount` (Decimal) into a `[Date: Decimal]` keyed by start-of-day, then map every slot (defaulting `.zero`) to a `SpendBucket`, converting to Double via `NSDecimalNumber(decimal:).doubleValue` only at construction. For year: key by start-of-month for all 12 months. Set `dateLabel` via `Date.formatted` (e.g. weekday/day/month) for accessibility. Keep refund/negative-amount handling consistent with `BudgetCalculator` (negatives reduce the bucket). No `@Query`, no SwiftUI, no `import Charts`.
  </action>
  <verify>
    <automated>xcodebuild test -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:MyHomeTests/SpendOverTimeAggregatorTests 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "enum SpendOverTimeAggregator" MyHomeApp/Support/SpendOverTimeAggregator.swift` returns 1.
    - File defines `SpendRange` (week/month/year) and `struct SpendBucket: Identifiable` with a `Double spent`.
    - `grep -c "NSDecimalNumber(decimal:" MyHomeApp/Support/SpendOverTimeAggregator.swift` returns >= 1 (Decimal→Double at boundary — Pitfall B).
    - `grep -c "import Charts" MyHomeApp/Support/SpendOverTimeAggregator.swift` returns 0 (pure helper — no chart import).
    - `MyHomeTests/SpendOverTimeAggregatorTests` passes, including the zero-spend-slot assertion (all buckets present with spent 0.0).
  </acceptance_criteria>
  <done>`SpendOverTimeAggregator` emits all slots incl. zero-spend, converts at the boundary, and the EXP-11 suite is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: OverviewAggregation helper (OVR-01/02/03)</name>
  <files>MyHomeApp/Support/OverviewAggregation.swift</files>
  <read_first>
    - MyHomeApp/Support/BudgetCalculator.swift (BudgetProgressData.colorThreshold 0.8/1.0 logic + BudgetColor enum to reuse; monthlySpend keyed by PersistentIdentifier; uncategorizedSpend)
    - MyHomeApp/Support/NoteListOrganizer.swift (organize(_:).pinned — OVR-03 MUST route through this, never note.isPinned directly, Pitfall E)
    - .planning/phases/04-overview-charts/04-PATTERNS.md (lines 172-186 inline fractionUsed/colorThreshold; lines 219-234 top-3 sort+tie-break; lines 249-259 pinned/fallback resolution)
    - .planning/phases/04-overview-charts/04-RESEARCH.md (§ "Open Questions": totalSpend includes uncategorized; category-free fractionUsed inline)
    - MyHomeTests/OverviewAggregationTests.swift (the contract — created in 04-01)
  </read_first>
  <behavior>
    - aggregateThreshold(totalSpend, totalBudget): totalBudget <= 0 → fractionUsed nil, color .normal; else fractionUsed = Double(truncating: (totalSpend/totalBudget) as NSDecimalNumber); >= 1.0 → .overBudget, >= 0.8 → .warning, else .normal
    - topCategories(spendByCategory, categories, limit 3): include only categories with spend > 0; sort spend descending, tie-break alphabetical by name; prefix(3)
    - resolvePinnedNote(notes): NoteListOrganizer.organize(notes).pinned.first; else first note containing a checkbox NoteBlock; else nil
  </behavior>
  <action>
    Create `MyHomeApp/Support/OverviewAggregation.swift` as a pure `enum OverviewAggregation` (doc-comment header mirroring CalendarAggregator's "pure contract" note). Implement the exact function signatures recorded in `04-01-SUMMARY.md`: an aggregate-threshold function returning `(fractionUsed: Double?, color: BudgetColor)` (reuse the existing `BudgetColor` enum from BudgetCalculator.swift; replicate the 0.8/1.0 thresholds inline since there is no `Category` for the aggregate bar); a top-3 ranking function taking `[PersistentIdentifier: Decimal]` + `[Category]` returning `[(category: Category, spent: Decimal)]` of length <= 3 (filter spend > 0, sort descending with alphabetical `name` tie-break, prefix(3)); and an OVR-03 resolver taking `[Note]` returning `Note?` via `NoteListOrganizer.organize(_:).pinned.first` with the checkbox-block fallback (`blocks?.contains { $0.kindRaw == "checkbox" }`). No SwiftUI, no `@Query`, no `import Charts`. Do NOT filter `note.isPinned` directly.
  </action>
  <verify>
    <automated>xcodebuild test -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:MyHomeTests/OverviewAggregationTests 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "enum OverviewAggregation" MyHomeApp/Support/OverviewAggregation.swift` returns 1.
    - `grep -c "NoteListOrganizer.organize" MyHomeApp/Support/OverviewAggregation.swift` returns >= 1.
    - `grep -vc "note.isPinned" MyHomeApp/Support/OverviewAggregation.swift` confirms zero direct `note.isPinned` filters (Pitfall E): `grep -c "note.isPinned" ...` returns 0.
    - `grep -c "import SwiftUI" MyHomeApp/Support/OverviewAggregation.swift` returns 0 (pure helper).
    - `MyHomeTests/OverviewAggregationTests` passes (all OVR-01/02/03 branches green).
  </acceptance_criteria>
  <done>`OverviewAggregation` implements the three OVR contracts and the OverviewAggregationTests suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure read-only aggregation over already-fetched local SwiftData arrays. No input parsing, no network, no persistence writes, no rendering of user-supplied strings. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02 | Tampering | Decimal→Double conversion in SpendOverTimeAggregator | mitigate | Money math stays `Decimal`; only the final chart-input value is `Double` via `NSDecimalNumber(decimal:).doubleValue` — no float drift in stored/aggregated money (Pitfall B/17) |
| T-04-03 | Information Disclosure | Note content read by OverviewAggregation.resolvePinnedNote | accept | Helper returns the `Note?` reference only; no content logged; rendering is plain `Text()` in a later view plan (T-04-06) |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — zero external packages in this plan; Swift Charts not imported in `Support/` helpers |
</threat_model>

<verification>
- Quick command (`SpendOverTimeAggregatorTests` + `OverviewAggregationTests`) is fully green.
- `DecimalINRTests` is green.
- All three helpers are SwiftUI-free and `import Charts`-free (pure `Support/` tier).
</verification>

<success_criteria>
- Plan 04-01's three RED suites are now GREEN.
- `SpendOverTimeAggregator`, `OverviewAggregation`, and `formattedINRCompact()` exist with the exact signatures the tests assert.
- Decimal→Double conversion happens only at the aggregation boundary; OVR-03 routes through `NoteListOrganizer`.
</success_criteria>

<output>
Create `.planning/phases/04-overview-charts/04-02-SUMMARY.md` when done, recording the final public signatures of `SpendOverTimeAggregator.bucket`, `OverviewAggregation`'s three functions, and confirming the full quick suite is green.
</output>
