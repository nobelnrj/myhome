---
phase: 04-overview-charts
plan: 05
type: execute
wave: 4
depends_on: ["04-02", "04-03", "04-04"]
files_modified:
  - MyHomeApp/Features/Overview/OverviewView.swift
  - MyHomeApp/RootView.swift
autonomous: false
requirements: [OVR-01, OVR-02, OVR-03, OVR-04, EXP-10, EXP-11]
must_haves:
  truths:
    - "App launches into the Overview/Home tab (tag 0); tab order is Home→Expenses→Budgets→Notes"
    - "Overview shows the five cards top-to-bottom: spend-vs-budget, top-3, pinned note, by-category chart, over-time chart"
    - "Quick-add + presents the existing AddExpenseView sheet; Overview auto-refreshes after save"
    - "Notes notification deep-links land on the Notes tab (tag 3), not Budgets"
  artifacts:
    - path: "MyHomeApp/Features/Overview/OverviewView.swift"
      provides: "OVR-01..04 + EXP-10/11 dashboard composition root"
      contains: "struct OverviewView"
    - path: "MyHomeApp/RootView.swift"
      provides: "Tab reorder + deep-link re-tag"
      contains: "OverviewView"
  key_links:
    - from: "MyHomeApp/RootView.swift"
      to: "OverviewView"
      via: "tag 0 default tab"
      pattern: "\\.tag\\(0\\)"
    - from: "MyHomeApp/Features/Overview/OverviewView.swift"
      to: "AddExpenseView"
      via: "quick-add sheet"
      pattern: "AddExpenseView\\(\\)"
    - from: "MyHomeApp/RootView.swift"
      to: "selectedTab"
      via: "Notes deep-link re-tagged to 3"
      pattern: "selectedTab = 3"
---

<objective>
Compose the Overview dashboard and wire it into the app. `OverviewView` owns the `@Query`s (current-month expenses via a child re-init, all categories, all notes), pre-aggregates with the Plan 04-02 helpers, and passes pre-computed data to the five cards (Plans 04-03 / 04-04). `RootView` is reordered so Overview is tag 0 + the default launch tab, the existing tabs shift right, and the single Notes deep-link constant moves from `selectedTab = 2` to `selectedTab = 3`.

Purpose: This is the integration wave — it makes "open the app and immediately see how the household is doing this month" real and re-tags the one deep-link constant the reorder breaks (Pitfall D). A blocking human-verify checkpoint covers the manual-smoke items from `04-VALIDATION.md` (launch tab, tab order, quick-add sheet, deep-link, chart rendering/empty states).
Output: 1 new file (`OverviewView.swift`) + 1 modified file (`RootView.swift`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-overview-charts/04-UI-SPEC.md
@.planning/phases/04-overview-charts/04-PATTERNS.md
@.planning/phases/04-overview-charts/04-02-SUMMARY.md
@.planning/phases/04-overview-charts/04-03-SUMMARY.md
@.planning/phases/04-overview-charts/04-04-SUMMARY.md
@MyHomeApp/RootView.swift
@MyHomeApp/Features/Budgets/BudgetsView.swift
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: OverviewView dashboard composition</name>
  <files>MyHomeApp/Features/Overview/OverviewView.swift</files>
  <read_first>
    - MyHomeApp/Features/Budgets/BudgetsView.swift (lines 12-54 NavigationStack+toolbar+sheet; lines 15-23 current-month DateComponents; lines 36-39 monthBoundaries → child; lines 122-145 BudgetsMonthView @Query init + pre-aggregation in body — mirror exactly for the OverviewMonthContent child)
    - MyHomeApp/Support/BudgetCalculator.swift (monthlySpend, uncategorizedSpend, monthBoundaries)
    - MyHomeApp/Support/OverviewAggregation.swift (from 04-02: aggregate threshold, topCategories, resolvePinnedNote — call these in body)
    - .planning/phases/04-overview-charts/04-02-SUMMARY.md, 04-03-SUMMARY.md, 04-04-SUMMARY.md (exact helper + card + chart init signatures to wire verbatim)
    - MyHomeApp/Features/Expenses/AddExpenseView.swift (no-parameter init; presents via .sheet)
    - .planning/phases/04-overview-charts/04-UI-SPEC.md (§ "Screen: Overview Tab" root layout, card order locked per D4-03, quick-add toolbar, navigation title "Home")
    - .planning/phases/04-overview-charts/04-PATTERNS.md (lines 31-108 OverviewView analog + pre-aggregation snippet)
  </read_first>
  <action>
    Create `MyHomeApp/Features/Overview/OverviewView.swift` (`import SwiftUI`, `import SwiftData`). `struct OverviewView: View` takes `@Binding var selectedTab: Int` and `@Binding var deepLinkNoteID: UUID?` (passed from RootView). Hold `@State private var showAddExpense = false`. Compute current-month `DateComponents` (mirror BudgetsView lines 15-23) and `BudgetCalculator.monthBoundaries(for:)`; pass the (start, end) into a private inner `OverviewMonthContent` child that owns `@Query private var monthExpenses` (date-bounded `#Predicate`, sort `.date` reverse — copy BudgetsMonthView init verbatim), `@Query(sort: \Category.sortOrder) private var categories`, and `@Query private var allNotes` (sort modifiedAt reverse). In `OverviewMonthContent.body`, pre-aggregate OUTSIDE any Chart DSL: `spendByCategory = BudgetCalculator.monthlySpend(...)`; `totalSpend = spendByCategory.values.reduce(.zero,+) + BudgetCalculator.uncategorizedSpend(for: monthExpenses)` (include uncategorized per Open Question #1); `totalBudget = categories.compactMap(\.monthlyBudget).reduce(.zero,+)`; `top3 = OverviewAggregation.topCategories(...)`; resolve the OVR-03 note via `OverviewAggregation.resolvePinnedNote(allNotes)` plus an isFallbackChecklist flag; build `[CategorySpendItem]` (Double, sorted desc) for the by-category chart. Lay out a `ScrollView` → `LazyVStack(alignment: .leading, spacing: 16)` with `.padding(.horizontal, 16)`, `.padding(.top, 8)`, in the locked D4-03 order: `SpendBudgetCard`, `TopCategoriesCard`, `PinnedNoteCard`, `SpendByCategoryChart`, `SpendOverTimeChart(monthExpenses: monthExpenses)`. Wrap in `NavigationStack` with `.navigationTitle("Home")` `.inline`, and a `.toolbar` trailing `Button { showAddExpense = true } label: { Image(systemName: "plus") }` with `.accessibilityLabel("Add Expense")`. `.sheet(isPresented: $showAddExpense) { AddExpenseView() }`. Forward `selectedTab`/`deepLinkNoteID` bindings into the cards that need them. Do NOT pass raw `@Query` arrays into any `Chart {}`.
  </action>
  <verify>
    <automated>xcodebuild build -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' 2>&1 | grep -E "error:|BUILD SUCCEEDED|BUILD FAILED" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "struct OverviewView" MyHomeApp/Features/Overview/OverviewView.swift` returns 1.
    - File references all five card types: SpendBudgetCard, TopCategoriesCard, PinnedNoteCard, SpendByCategoryChart, SpendOverTimeChart.
    - `grep -c "AddExpenseView()" MyHomeApp/Features/Overview/OverviewView.swift` returns >= 1 and `grep -c ".sheet(isPresented:" ...` returns >= 1 (OVR-04 reuses the existing sheet).
    - `grep -c "navigationTitle(\"Home\")" MyHomeApp/Features/Overview/OverviewView.swift` returns 1.
    - `grep -c "uncategorizedSpend" MyHomeApp/Features/Overview/OverviewView.swift` returns >= 1 (totalSpend includes uncategorized).
    - No raw `@Query` array passed into a Chart: `grep -E "Chart\((monthExpenses|categories|allNotes)\)" MyHomeApp/Features/Overview/OverviewView.swift` returns nothing.
    - `import Charts` does NOT appear in OverviewView (charts live in their own card files): `grep -c "import Charts" MyHomeApp/Features/Overview/OverviewView.swift` returns 0.
    - Project builds successfully.
  </acceptance_criteria>
  <done>OverviewView composes the five cards in D4-03 order with the quick-add sheet, pre-aggregating outside the Chart DSL; project builds.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: RootView tab reorder + deep-link re-tag (D4-01)</name>
  <files>MyHomeApp/RootView.swift</files>
  <read_first>
    - MyHomeApp/RootView.swift (current state: ExpenseListView tag 0, BudgetsView tag 1, NotesHomeView tag 2; `selectedTab = 2` at line 49; existing deepLinkNoteID/deepLinkBlockID @State)
    - .planning/phases/04-overview-charts/04-PATTERNS.md (lines 494-537: exact target tab structure + the single deep-link constant change)
    - .planning/phases/04-overview-charts/04-UI-SPEC.md (§ "Tab Bar Reorder (D4-01)" — tags, labels, SF Symbols; house/"Home")
    - .planning/phases/04-overview-charts/04-RESEARCH.md (§ "Pattern 4" — confirms RootView.swift is the ONLY selectedTab reference site)
  </read_first>
  <action>
    Edit `MyHomeApp/RootView.swift`. Insert `OverviewView(selectedTab: $selectedTab, deepLinkNoteID: $deepLinkNoteID)` as the FIRST TabView child with `.tabItem { Label("Home", systemImage: "house") }` and `.tag(0)`. Re-tag the existing children: `ExpenseListView` → `.tag(1)`, `BudgetsView` → `.tag(2)`, `NotesHomeView` → `.tag(3)`. Keep `@State private var selectedTab: Int = 0` unchanged (0 now means Overview). In the `.onReceive(kOpenNoteNotification)` handler, change the single `selectedTab = 2` to `selectedTab = 3` (Pitfall D — the only reference site). Leave `deepLinkNoteID`/`deepLinkBlockID` bindings and the notification mechanism otherwise unchanged. Update the file's doc-comment tab list to reflect the new 4-tab order.
  </action>
  <verify>
    <automated>xcodebuild build -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' 2>&1 | grep -E "error:|BUILD SUCCEEDED|BUILD FAILED" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "selectedTab = 2" MyHomeApp/RootView.swift` returns 0 AND `grep -c "selectedTab = 3" MyHomeApp/RootView.swift` returns 1 (deep-link re-tagged — Pitfall D).
    - `grep -c "OverviewView(" MyHomeApp/RootView.swift` returns 1, with `.tag(0)` and `Label("Home", systemImage: "house")` present.
    - NotesHomeView carries `.tag(3)`, BudgetsView `.tag(2)`, ExpenseListView `.tag(1)`.
    - `@State private var selectedTab: Int = 0` is unchanged.
    - Project builds successfully.
  </acceptance_criteria>
  <done>RootView launches into Overview (tag 0), tabs are Home→Expenses→Budgets→Notes, and the Notes deep-link constant is 3.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual smoke — launch tab, quick-add, deep-link, charts/empty states</name>
  <what-built>OverviewView is wired as the default launch tab; the five cards render from live data; quick-add presents the existing AddExpenseView; Notes notification deep-links land on tag 3; charts populate with data and degrade to D4-07 empty states.</what-built>
  <how-to-verify>
    1. Build + run on the iPhone 17 simulator: `xcodebuild test -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17'` first (full suite must be green), then launch the app.
    2. Confirm the app LANDS on the "Home" tab (leftmost, `house` icon) — not Expenses.
    3. Confirm tab order left→right: Home, Expenses, Budgets, Notes.
    4. On Home, confirm five cards top→bottom: This Month (spend-vs-budget bar), Top Categories, Pinned Note, Spend by Category (bars), Spend Over Time (line). Bar color matches the Budgets tab for the same data (accent/orange/red).
    5. Tap the toolbar `+` → confirm the FULL existing Add Expense sheet appears (custom keypad). Add an expense → dismiss → confirm Overview cards/charts update automatically.
    6. Spend Over Time: switch Week / Month / Year → the line recomputes; Picker stays visible.
    7. Empty states: in a simulator account with no spend this month, confirm "No spend yet this month." (top-3 + by-category) and "No spend yet for this period." (over-time), and "Set a budget" / "Pin a note to see it here." as applicable — no blank/broken charts.
    8. Deep-link: trigger a Notes reminder notification (or the existing Phase-3 banner path) → tap it → confirm it lands on the NOTES tab (now tag 3) and opens the note, NOT the Budgets tab.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (e.g. "deep-link still lands on Budgets", "chart blank instead of empty state").</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Quick-add sheet → SwiftData write | OVR-04 re-presents the existing `AddExpenseView`, the one write path reachable from Overview. It is already validated (amount guard) in Phase 1 — Phase 4 adds no new validation surface. |
| Notes deep-link tab switch | Re-tagged integer assignment; no external/URL input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08 | Elevation / Tampering | Quick-add AddExpenseView sheet | accept | Reuses the existing, already-validated Phase-1 add path verbatim; no new/stripped entry UI; no new input validation surface introduced |
| T-04-09 | Tampering | Notes deep-link re-tag (selectedTab = 3) | mitigate | Single integer constant change at the one verified reference site; manual-smoke step 8 confirms the deep-link lands on Notes, preventing the Pitfall-D wrong-tab regression |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — no external packages; OverviewView imports SwiftUI/SwiftData only; charts (first-party Swift Charts) are confined to the Plan 04-04 card files |
</threat_model>

<verification>
- Full suite green: `xcodebuild test -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17'`.
- RootView: zero `selectedTab = 2`, exactly one `selectedTab = 3`; OverviewView is tag 0.
- Manual-smoke checkpoint approved (launch tab, tab order, quick-add sheet, range control, empty states, Notes deep-link).
</verification>

<success_criteria>
- App launches into Overview/Home (tag 0); order Home→Expenses→Budgets→Notes.
- Overview composes all five cards in D4-03 order; quick-add reuses AddExpenseView; charts render and degrade to empty states.
- Notes notification deep-links land on the Notes tab (tag 3).
- Full `MyHomeTests` suite green.
</success_criteria>

<output>
Create `.planning/phases/04-overview-charts/04-05-SUMMARY.md` when done, recording the final OverviewView/RootView wiring, the full-suite-green result, and the human-verify approval outcome.
</output>
