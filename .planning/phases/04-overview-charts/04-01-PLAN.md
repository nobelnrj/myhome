---
phase: 04-overview-charts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - MyHomeTests/SpendOverTimeAggregatorTests.swift
  - MyHomeTests/OverviewAggregationTests.swift
  - MyHomeTests/DecimalINRTests.swift
autonomous: true
requirements: [OVR-01, OVR-02, OVR-03, EXP-11]
must_haves:
  truths:
    - "Three new failing test files exist and compile against not-yet-written symbols (RED state)"
    - "SpendOverTimeAggregatorTests asserts 7/28-31/12 bucket counts and zero-spend slots for week/month/year"
    - "OverviewAggregationTests asserts top-3 sort+tie-break, aggregate threshold math, and pinned/fallback/empty note resolution"
    - "DecimalINRTests asserts formattedINRCompact() thresholds at <1000 / >=1000 / >=100000"
  artifacts:
    - path: "MyHomeTests/SpendOverTimeAggregatorTests.swift"
      provides: "EXP-11 bucketing test coverage (Nyquist gate)"
      contains: "struct SpendOverTimeAggregatorTests"
    - path: "MyHomeTests/OverviewAggregationTests.swift"
      provides: "OVR-01/02/03 aggregation test coverage (Nyquist gate)"
      contains: "struct OverviewAggregationTests"
    - path: "MyHomeTests/DecimalINRTests.swift"
      provides: "formattedINRCompact() threshold coverage"
      contains: "struct DecimalINRTests"
  key_links:
    - from: "MyHomeTests/SpendOverTimeAggregatorTests.swift"
      to: "SpendOverTimeAggregator.bucket"
      via: "direct call (symbol does not yet exist — RED)"
      pattern: "SpendOverTimeAggregator\\.bucket"
    - from: "MyHomeTests/OverviewAggregationTests.swift"
      to: "OverviewAggregation"
      via: "direct call (symbol does not yet exist — RED)"
      pattern: "OverviewAggregation\\."
---

<objective>
Create the Wave 0 failing-test scaffold for Phase 4 (Nyquist gate). Three new Swift Testing files exercise the three pure helpers that Plan 04-02 will implement: `SpendOverTimeAggregator` (EXP-11 bucketing), `OverviewAggregation` (OVR-01 aggregate threshold math, OVR-02 top-3 ranking, OVR-03 pinned-note resolution), and `Decimal.formattedINRCompact()`.

Purpose: Per the project's Nyquist validation gate (`04-VALIDATION.md`), every pure-logic requirement must have a failing test BEFORE the implementation exists. These files define the executable contracts that Plan 04-02 satisfies (RED → GREEN).
Output: 3 new test files in `MyHomeTests/` that compile-fail or assertion-fail against the not-yet-written production symbols (expected RED state at end of this plan).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-overview-charts/04-RESEARCH.md
@.planning/phases/04-overview-charts/04-PATTERNS.md
@.planning/phases/04-overview-charts/04-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SpendOverTimeAggregatorTests (EXP-11 RED)</name>
  <files>MyHomeTests/SpendOverTimeAggregatorTests.swift</files>
  <read_first>
    - MyHomeTests/CalendarAggregationTests.swift (exact analog: @MainActor struct, in-memory ModelContainer, #expect; mirror its structure)
    - MyHomeTests/BudgetCalculatorTests.swift (lines 1-30: `private typealias Cat = MyHome.Category`, `makeContainer()` with `ModelContainer(for: Expense.self, Cat.self, ...)`)
    - .planning/phases/04-overview-charts/04-RESEARCH.md (§ "Pattern 3: SpendOverTimeAggregator" for the `SpendRange`/`SpendBucket`/`bucket(expenses:range:calendar:)` contract; § "Code Examples" week-bucketing reference)
    - .planning/phases/04-overview-charts/04-PATTERNS.md (lines 541-581: test file shape + key cases to cover)
  </read_first>
  <behavior>
    - Week range → `bucket(...)` returns exactly 7 SpendBucket entries (one per day, last 7 days incl. today)
    - Month range → returns 28-31 entries (all calendar days of the current month)
    - Year range → returns exactly 12 entries (one per month of the current year)
    - Zero-spend scenario (empty expense array) → all buckets present with `spent == 0.0` (no omitted slots — Pitfall C)
    - Decimal→Double boundary: a single expense of `Decimal(1234.56)` on a day produces a bucket with `spent` ≈ 1234.56 (no money rounding error); assert with a tolerance window
    - Refund handling consistent with money math: a negative-amount expense reduces the day's bucket (mirror BudgetCalculator refund convention); clamp negatives per the aggregator contract if specified
  </behavior>
  <action>
    Create `MyHomeTests/SpendOverTimeAggregatorTests.swift` as a `@MainActor struct SpendOverTimeAggregatorTests`. Add `import Testing`, `import Foundation`, `import SwiftData`, `@testable import MyHome`, and `private typealias Cat = MyHome.Category`. Add a `makeContainer()` helper returning `ModelContainer(for: Expense.self, Cat.self, configurations: ModelConfiguration(isStoredInMemoryOnly: true))`. Write `@Test` methods calling `SpendOverTimeAggregator.bucket(expenses:range:calendar:)` with `SpendRange.week`/`.month`/`.year`. Insert `Expense` instances at controlled dates (use `Calendar.current` offsets from `Date()`). Assert `.count` per range, presence of zero-spend slots, and the Decimal→Double conversion tolerance via `#expect`. Use `Issue.record(...)` for any nil-guard branch. These symbols (`SpendOverTimeAggregator`, `SpendRange`, `SpendBucket`) do NOT yet exist — the file is expected to fail to compile/run (RED). Do NOT create production symbols in this plan.
  </action>
  <verify>
    <automated>xcodebuild test -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:MyHomeTests/SpendOverTimeAggregatorTests 2>&1 | grep -E "error:|Compiling|Test Suite" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - File `MyHomeTests/SpendOverTimeAggregatorTests.swift` exists and contains `struct SpendOverTimeAggregatorTests`.
    - File references `SpendOverTimeAggregator.bucket` and all three of `SpendRange.week`, `SpendRange.month`, `SpendRange.year`.
    - `grep -c "@Test" MyHomeTests/SpendOverTimeAggregatorTests.swift` returns >= 5 (week-count, month-count, year-count, zero-spend, decimal-boundary).
    - File asserts zero-spend slot presence (a test that passes an empty `[Expense]` and `#expect`s `buckets.allSatisfy { $0.spent == 0.0 }` AND a non-empty `.count`).
    - The build/test command fails (RED) because `SpendOverTimeAggregator` is undefined — this is the expected outcome of Wave 0.
  </acceptance_criteria>
  <done>The test file exists, compiles against the intended (not-yet-written) API surface, and the only failures are "cannot find SpendOverTimeAggregator/SpendRange/SpendBucket in scope" (RED).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: OverviewAggregationTests (OVR-01/02/03 RED)</name>
  <files>MyHomeTests/OverviewAggregationTests.swift</files>
  <read_first>
    - MyHomeTests/BudgetCalculatorTests.swift (exact analog: `makeContainer()`, `Cat` typealias, `@Test` + `#expect` + `Issue.record` shape; threshold assertions at .normal/.warning/.overBudget)
    - MyHomeApp/Support/BudgetCalculator.swift (BudgetProgressData.colorThreshold 0.8/1.0 boundaries — replicate for aggregate bar; monthlySpend keying by PersistentIdentifier)
    - MyHomeApp/Support/NoteListOrganizer.swift (organize(_:).pinned partition; OVR-03 must use this, NOT note.isPinned directly — Pitfall E)
    - .planning/phases/04-overview-charts/04-PATTERNS.md (lines 584-633: test cases for OVR-01/02/03; lines 172-186 inline fractionUsed/colorThreshold logic the helper must mirror; lines 219-234 top-3 sort+tie-break)
    - .planning/phases/04-overview-charts/04-RESEARCH.md (§ "Open Questions" #1 totalSpend includes uncategorized; #2 category-free fractionUsed)
  </read_first>
  <behavior>
    - OVR-01 aggregate threshold: fractionUsed 0.5 → .normal; 0.85 → .warning; 1.1 → .overBudget; totalBudget == 0 → fractionUsed nil → "no budget" branch
    - OVR-01 totalSpend includes uncategorized spend (categorized sum + uncategorizedSpend) per Open Question #1
    - OVR-02 top-3: 4+ categories with spend → exactly 3 returned, sorted by spend descending; equal-spend pair tie-broken alphabetically by category.name; only categories with spend > 0 included; <3 with spend → fewer rows (no placeholders)
    - OVR-03: pinned note present → returns that note; no pinned but a note with a checkbox block exists → returns the checklist note; neither → returns nil (empty state)
  </behavior>
  <action>
    Create `MyHomeTests/OverviewAggregationTests.swift` as a `@MainActor struct OverviewAggregationTests` mirroring `BudgetCalculatorTests`. Add the same imports + `private typealias Cat = MyHome.Category` + `makeContainer()` (Expense.self, Cat.self, Note.self, NoteBlock.self). Write `@Test` methods that call the not-yet-written `OverviewAggregation` namespace: an aggregate-threshold function returning a `BudgetColor` (or `(fractionUsed: Double?, color: BudgetColor)`) from `(totalSpend: Decimal, totalBudget: Decimal)`; a top-3 ranking function from `(spendByCategory: [PersistentIdentifier: Decimal], categories: [Category])` returning an ordered `[(Category, Decimal)]` of length <= 3; and an OVR-03 resolver from `[Note]` returning `Note?` via `NoteListOrganizer.organize(_:).pinned.first` with checkbox-block fallback. Insert Category/Expense/Note/NoteBlock fixtures and assert with `#expect`/`Issue.record`. The `OverviewAggregation` symbol does NOT yet exist (RED). Do NOT create production code here. Name the exact function signatures in a header comment so Plan 04-02 implements them verbatim.
  </action>
  <verify>
    <automated>xcodebuild test -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:MyHomeTests/OverviewAggregationTests 2>&1 | grep -E "error:|Test Suite" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - File `MyHomeTests/OverviewAggregationTests.swift` exists, contains `struct OverviewAggregationTests`, and references `OverviewAggregation.` and `NoteListOrganizer.organize`.
    - Contains a test asserting `.warning` at fraction 0.85 and `.overBudget` at fraction 1.1 (boundary cases matching BudgetProgressData).
    - Contains a test asserting the "no budget" branch when totalBudget == 0 (fractionUsed nil / color .normal).
    - Contains a top-3 test inserting >= 4 spending categories and `#expect`ing the returned array `.count == 3` in descending-spend order with an alphabetical tie-break assertion.
    - Contains an OVR-03 test for all three branches (pinned / checklist-fallback / nil).
    - `grep -c "note.isPinned" MyHomeTests/OverviewAggregationTests.swift` returns 0 (must route through NoteListOrganizer — Pitfall E).
    - The command fails (RED) because `OverviewAggregation` is undefined.
  </acceptance_criteria>
  <done>Test file exists, encodes OVR-01/02/03 contracts, and fails only on the missing `OverviewAggregation` symbol (RED).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: DecimalINRTests (formattedINRCompact RED)</name>
  <files>MyHomeTests/DecimalINRTests.swift</files>
  <read_first>
    - MyHomeApp/Support/Decimal+INR.swift (existing formattedINR(); the new formattedINRCompact() extends this file)
    - .planning/phases/04-overview-charts/04-PATTERNS.md (lines 461-490: exact formattedINRCompact() thresholds — >=100000 → "₹NL", >=1000 → "₹Nk", else "₹N")
    - MyHomeTests/BudgetCalculatorTests.swift (Swift Testing struct + #expect shape; no container needed for a pure formatting test)
  </read_first>
  <behavior>
    - Decimal(500).formattedINRCompact() == "₹500"
    - Decimal(5000).formattedINRCompact() == "₹5k"
    - Decimal(150000).formattedINRCompact() == "₹1L"
    - Boundary at exactly 1000 → "₹1k"; exactly 100000 → "₹1L"
    - Below 1000 (e.g. 999) → "₹999"
  </behavior>
  <action>
    Create `MyHomeTests/DecimalINRTests.swift` as a `struct DecimalINRTests` (no `@MainActor`/container required — pure formatting). Add `import Testing`, `import Foundation`, `@testable import MyHome`. Write `@Test` methods asserting `Decimal(n).formattedINRCompact()` equals the exact expected string for the < 1000, 1000-99999, and >= 100000 buckets plus the two boundary values. `formattedINRCompact()` does NOT yet exist on `Decimal` (RED). Do NOT add the production method here — that is Plan 04-02.
  </action>
  <verify>
    <automated>xcodebuild test -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:MyHomeTests/DecimalINRTests 2>&1 | grep -E "error:|Test Suite" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - File `MyHomeTests/DecimalINRTests.swift` exists and contains `struct DecimalINRTests`.
    - References `formattedINRCompact()` and asserts the exact strings "₹500", "₹5k", "₹1L".
    - `grep -c "@Test" MyHomeTests/DecimalINRTests.swift` returns >= 5.
    - The command fails (RED) because `formattedINRCompact` is undefined on `Decimal`.
  </acceptance_criteria>
  <done>Test file exists with the three threshold buckets + boundaries and fails only on the missing `formattedINRCompact()` (RED).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Phase 4 is read-only over local on-device SwiftData. This plan adds only test code; no input surface, no network, no persistence writes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | Test fixtures containing note/expense content | accept | In-memory `ModelContainer(isStoredInMemoryOnly: true)`; no content logged; fixtures are synthetic — no real household data |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — zero external packages added in Phase 4 (Swift Charts is first-party; no installs). No `[ASSUMED]`/`[SUS]` packages. |
</threat_model>

<verification>
- All three files exist under `MyHomeTests/`.
- Running the quick command (both aggregation suites) yields a RED result whose only errors are "cannot find ... in scope" for the not-yet-written symbols — confirming the Nyquist gate is correctly failing-first.
- No production source files were modified by this plan.
</verification>

<success_criteria>
- `SpendOverTimeAggregatorTests.swift`, `OverviewAggregationTests.swift`, `DecimalINRTests.swift` exist and encode the EXP-11 / OVR-01/02/03 / formattedINRCompact contracts.
- The suites compile-fail or fail only on missing production symbols (expected RED).
- `04-VALIDATION.md` Wave 0 checklist items are all addressed by file existence.
</success_criteria>

<output>
Create `.planning/phases/04-overview-charts/04-01-SUMMARY.md` when done, recording the exact function signatures the tests expect (so Plan 04-02 implements them verbatim) and confirming RED state.
</output>
