---
phase: 04-overview-charts
plan: 04
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - MyHomeApp/Features/Overview/SpendByCategoryChart.swift
  - MyHomeApp/Features/Overview/SpendOverTimeChart.swift
autonomous: true
requirements: [EXP-10, EXP-11]
must_haves:
  truths:
    - "SpendByCategoryChart renders a horizontal BarMark ranking with compact ₹ annotations, or the 'No spend yet this month.' empty state"
    - "SpendOverTimeChart renders a LineMark+AreaMark with a Week/Month/Year segmented control, or the 'No spend yet for this period.' empty state"
    - "All chart values are Double (pre-converted); no Decimal passed into the Chart DSL"
  artifacts:
    - path: "MyHomeApp/Features/Overview/SpendByCategoryChart.swift"
      provides: "EXP-10 spend-by-category BarMark card + CategorySpendItem"
      contains: "struct SpendByCategoryChart"
    - path: "MyHomeApp/Features/Overview/SpendOverTimeChart.swift"
      provides: "EXP-11 spend-over-time LineMark card + range control"
      contains: "struct SpendOverTimeChart"
  key_links:
    - from: "MyHomeApp/Features/Overview/SpendOverTimeChart.swift"
      to: "SpendOverTimeAggregator.bucket"
      via: "range-driven re-aggregation"
      pattern: "SpendOverTimeAggregator.bucket"
    - from: "MyHomeApp/Features/Overview/SpendByCategoryChart.swift"
      to: "Swift Charts BarMark"
      via: "pre-aggregated CategorySpendItem (Double spent)"
      pattern: "BarMark"
---

<objective>
Build the two Swift Charts cards: `SpendByCategoryChart` (EXP-10, horizontal `BarMark`) and `SpendOverTimeChart` (EXP-11, `LineMark` + `AreaMark` with a Week/Month/Year segmented control). This is the project's first use of `import Charts`. Both consume pre-aggregated `Double`-valued data (never raw `@Query` into the Chart DSL — Pitfall A) and convert `Decimal`→`Double` at the data-prep boundary (Pitfall B).

Purpose: The two charts "sell the app's value" (the phase goal). Keeping them in separate files from the Plan 04-03 cards gives clean file ownership so Waves 04-03 and 04-04 run in parallel.
Output: 2 new files under `MyHomeApp/Features/Overview/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-overview-charts/04-UI-SPEC.md
@.planning/phases/04-overview-charts/04-PATTERNS.md
@.planning/phases/04-overview-charts/04-RESEARCH.md
@.planning/phases/04-overview-charts/04-02-SUMMARY.md
@MyHomeApp/Features/Budgets/BudgetCategoryCard.swift
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: SpendByCategoryChart + CategorySpendItem (EXP-10)</name>
  <files>MyHomeApp/Features/Overview/SpendByCategoryChart.swift</files>
  <read_first>
    - MyHomeApp/Features/Budgets/BudgetCategoryCard.swift (card shell to wrap the chart)
    - MyHomeApp/Support/Decimal+INR.swift (formattedINRCompact() for annotations/axis; formattedINR() for accessibility)
    - .planning/phases/04-overview-charts/04-UI-SPEC.md (§ "Card 4: Spend by Category Chart" — horizontal BarMark, chartXAxis hidden, 220pt height, chartScrollableAxes(.vertical), annotation, empty state, accessibility)
    - .planning/phases/04-overview-charts/04-PATTERNS.md (lines 273-315: BarMark pattern + CategorySpendItem struct)
    - .planning/phases/04-overview-charts/04-RESEARCH.md (§ "Code Examples" BarMark; Pitfall A/B/G)
  </read_first>
  <action>
    Create `MyHomeApp/Features/Overview/SpendByCategoryChart.swift` with `import SwiftUI`, `import Charts`. Define `struct CategorySpendItem: Identifiable { let id: PersistentIdentifier; let name: String; let spent: Double }` (import SwiftData for PersistentIdentifier). Define `struct SpendByCategoryChart: View` taking `categoryItems: [CategorySpendItem]` (pre-built and sorted descending by the parent; spent already Double). Row A title "Spend by Category" (`.title2`). Row B: `Chart(categoryItems) { item in BarMark(x: .value("Amount", item.spent), y: .value("Category", item.name)) ... }` with `.foregroundStyle(Color.accentColor)`, `.annotation(position: .trailing)` showing `Decimal(item.spent).formattedINRCompact()` (`.caption`, `.secondary`), and per-mark `.accessibilityLabel("\(item.name), \(Decimal(item.spent).formattedINR())")`. Apply `.chartXAxis(.hidden)`, a `.chartYAxis` with `.caption` labels, `.frame(height: 220)`, `.chartScrollableAxes(.vertical)`, and a chart-level `.accessibilityLabel("Spend by category chart")`. Empty state — `categoryItems.isEmpty`: replace the Chart with centered "No spend yet this month." (`.subheadline`, `.secondary`, `.frame(height: 80)`). Wrap in the card shell. NEVER pass `Decimal` into `.value(...)` — `item.spent` is `Double`. Add a `#Preview` with populated + empty fixtures.
  </action>
  <verify>
    <automated>xcodebuild build -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' 2>&1 | grep -E "error:|BUILD SUCCEEDED|BUILD FAILED" | head -10</automated>
    <human-check>SpendByCategoryChart preview shows horizontal bars ranked high→low with ₹ annotations, scrollable past 7 categories, and the "No spend yet" state when empty.</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -c "struct SpendByCategoryChart" MyHomeApp/Features/Overview/SpendByCategoryChart.swift` returns 1 and the file defines `struct CategorySpendItem`.
    - `grep -c "import Charts" MyHomeApp/Features/Overview/SpendByCategoryChart.swift` returns 1.
    - `CategorySpendItem.spent` is `Double` (no `Decimal` typed property): `grep -c "spent: Double" ...` returns >= 1.
    - File contains `.frame(height: 220)`, `.chartScrollableAxes(.vertical)`, and "No spend yet this month."
    - No `Decimal` inside `.value(`: `grep -E "\.value\([^)]*Decimal" MyHomeApp/Features/Overview/SpendByCategoryChart.swift` returns nothing.
    - Project builds successfully.
  </acceptance_criteria>
  <done>SpendByCategoryChart renders a ranked horizontal BarMark with Double values + empty state; project builds.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: SpendOverTimeChart + range control (EXP-11)</name>
  <files>MyHomeApp/Features/Overview/SpendOverTimeChart.swift</files>
  <read_first>
    - MyHomeApp/Support/SpendOverTimeAggregator.swift (SpendRange / SpendBucket / bucket() — from Plan 04-02; the chart drives re-aggregation off selectedRange)
    - MyHomeApp/Features/Budgets/BudgetCategoryCard.swift (card shell)
    - MyHomeApp/Support/Decimal+INR.swift (formattedINRCompact() for Y-axis)
    - .planning/phases/04-overview-charts/04-UI-SPEC.md (§ "Card 5: Spend Over Time Chart" — segmented Picker default .month, LineMark+AreaMark, X-axis formats per range, Y-axis compact ₹, 200pt height, zero-spend buckets, empty state, accessibility)
    - .planning/phases/04-overview-charts/04-PATTERNS.md (lines 319-386: LineMark/AreaMark pattern + @State selectedRange + Picker .segmented)
    - .planning/phases/04-overview-charts/04-RESEARCH.md (§ "Code Examples" LineMark + xAxisDateFormat helper)
  </read_first>
  <action>
    Create `MyHomeApp/Features/Overview/SpendOverTimeChart.swift` with `import SwiftUI`, `import Charts`, `import SwiftData`. Define `struct SpendOverTimeChart: View` taking `monthExpenses: [Expense]` (the parent's current-window expense array — note the aggregator computes its own date windows). Hold `@State private var selectedRange: SpendRange = .month`. Compute `let bucketedData = SpendOverTimeAggregator.bucket(expenses: monthExpenses, range: selectedRange)` in `body` (outside the Chart DSL — Pitfall A). Row A title "Spend Over Time" (`.title2`). Row B: `Picker("Range", selection: $selectedRange)` over `SpendRange.allCases` with `.pickerStyle(.segmented)` (labels Week/Month/Year). Row C: `Chart(bucketedData) { point in AreaMark(...).foregroundStyle(Color.accentColor.opacity(0.15)); LineMark(x: .value("Date", point.date), y: .value("Spend", point.spent))... }` with `.symbol(.circle)`, `.symbolSize(30)`, per-point `.accessibilityLabel("\(point.dateLabel), \(Decimal(point.spent).formattedINR())")`. Add `.chartXAxis` using a `xAxisDateFormat(for:)` helper returning `Date.FormatStyle` per range (week→weekday abbreviated, month→day, year→month abbreviated) and `.chartYAxis` rendering `Decimal(d).formattedINRCompact()` for Double axis values. `.frame(height: 200)` + chart-level `.accessibilityLabel("Spend over time chart, \(selectedRange.label) view")`. Empty state — `bucketedData.allSatisfy { $0.spent == 0 }` (no spend in range): replace the Chart with centered "No spend yet for this period." (`.subheadline`, `.secondary`, `.frame(height: 80)`) while keeping the Picker visible. `point.spent` is `Double` — never pass `Decimal` into `.value(`. Wrap in the card shell. Add a `#Preview` with populated + empty fixtures.
  </action>
  <verify>
    <automated>xcodebuild build -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' 2>&1 | grep -E "error:|BUILD SUCCEEDED|BUILD FAILED" | head -10</automated>
    <human-check>SpendOverTimeChart preview shows a line+area trend; switching Week/Month/Year recomputes the curve; the Picker stays visible and "No spend yet for this period." appears when the range has no spend.</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -c "struct SpendOverTimeChart" MyHomeApp/Features/Overview/SpendOverTimeChart.swift` returns 1.
    - `grep -c "SpendOverTimeAggregator.bucket" MyHomeApp/Features/Overview/SpendOverTimeChart.swift` returns >= 1.
    - File contains `.pickerStyle(.segmented)`, `LineMark`, `AreaMark`, `.frame(height: 200)`, and "No spend yet for this period."
    - `@State private var selectedRange: SpendRange = .month` is present (default month).
    - No `Decimal` inside `.value(`: `grep -E "\.value\([^)]*Decimal" MyHomeApp/Features/Overview/SpendOverTimeChart.swift` returns nothing.
    - Project builds successfully.
  </acceptance_criteria>
  <done>SpendOverTimeChart renders the LineMark+AreaMark with a working range control + empty state; project builds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Charts are display-only over pre-aggregated local data. No interactive selection, no input, no network. Axis/annotation labels derive from pure `Decimal` math, never user strings. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Spoofing / Tampering | Chart axis + annotation labels | mitigate | Labels derive from `Decimal.formattedINRCompact()` / `formattedINR()` — pure math; no user-supplied strings rendered as axis labels |
| T-04-05 | Tampering | Decimal→Double at chart boundary | mitigate | `CategorySpendItem.spent` and `SpendBucket.spent` are `Double` converted at the aggregation boundary; no `Decimal` enters the Chart DSL (Pitfall B); money math stays `Decimal` upstream |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — Swift Charts is a first-party Apple framework (`import Charts`); zero SPM/external packages added (RESEARCH § Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- Project builds for iPhone 17 simulator with both chart files present (first `import Charts` in the project compiles cleanly).
- Both charts render populated + empty states in `#Preview`.
- No `Decimal` is passed into any `.value(...)` in either chart.
</verification>

<success_criteria>
- `SpendByCategoryChart` (+ `CategorySpendItem`) and `SpendOverTimeChart` exist under `Features/Overview/` and build.
- EXP-10 ranks categories as a horizontal BarMark; EXP-11 trends spend as a LineMark with a Week/Month/Year control defaulting to Month.
- Both degrade to the D4-07 empty states; all chart values are pre-converted `Double`.
</success_criteria>

<output>
Create `.planning/phases/04-overview-charts/04-04-SUMMARY.md` when done, recording the exact init signatures of both chart views + `CategorySpendItem` (so Plan 04-05's OverviewView wires them verbatim).
</output>
