---
phase: 04-overview-charts
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - MyHomeApp/Features/Overview/SpendBudgetCard.swift
  - MyHomeApp/Features/Overview/TopCategoriesCard.swift
  - MyHomeApp/Features/Overview/PinnedNoteCard.swift
autonomous: true
requirements: [OVR-01, OVR-02, OVR-03]
must_haves:
  truths:
    - "SpendBudgetCard renders the aggregate bar with BudgetColor thresholds and the 'Set a budget' / 'No spend yet' empty states"
    - "TopCategoriesCard renders up to 3 ranked rows or the 'No spend yet this month.' empty state"
    - "PinnedNoteCard renders the pinned/checklist note with an Open-note affordance, or the 'Pin a note to see it here.' empty state"
  artifacts:
    - path: "MyHomeApp/Features/Overview/SpendBudgetCard.swift"
      provides: "OVR-01 aggregate spend-vs-budget bar card"
      contains: "struct SpendBudgetCard"
    - path: "MyHomeApp/Features/Overview/TopCategoriesCard.swift"
      provides: "OVR-02 top-3 categories card"
      contains: "struct TopCategoriesCard"
    - path: "MyHomeApp/Features/Overview/PinnedNoteCard.swift"
      provides: "OVR-03 pinned-note card"
      contains: "struct PinnedNoteCard"
  key_links:
    - from: "MyHomeApp/Features/Overview/SpendBudgetCard.swift"
      to: "BudgetColor"
      via: "bar fill color mapping"
      pattern: "case .overBudget"
    - from: "MyHomeApp/Features/Overview/PinnedNoteCard.swift"
      to: "selectedTab/deepLinkNoteID bindings"
      via: "Open-note deep-link"
      pattern: "selectedTab = 3"
---

<objective>
Build the three non-chart Overview cards as dumb, value-driven SwiftUI components: `SpendBudgetCard` (OVR-01), `TopCategoriesCard` (OVR-02), and `PinnedNoteCard` (OVR-03). Each takes pre-computed data (from `OverviewAggregation`, Plan 04-02) as init parameters — none holds a `@Query`. They render the standard card shell and the D4-07 inline empty states.

Purpose: Isolating the card views from the parent's `@Query` keeps them testable/previewable and prevents chart-style re-layout on data churn. These cards are consumed by `OverviewView` (Plan 04-05).
Output: 3 new files under `MyHomeApp/Features/Overview/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-overview-charts/04-UI-SPEC.md
@.planning/phases/04-overview-charts/04-PATTERNS.md
@.planning/phases/04-overview-charts/04-02-SUMMARY.md
@MyHomeApp/Features/Budgets/BudgetProgressView.swift
@MyHomeApp/Features/Budgets/BudgetCategoryCard.swift
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: SpendBudgetCard (OVR-01)</name>
  <files>MyHomeApp/Features/Overview/SpendBudgetCard.swift</files>
  <read_first>
    - MyHomeApp/Features/Budgets/BudgetProgressView.swift (BudgetColor→Color mapping lines 17-22; GeometryReader+ZStack bar lines 36-55; "No budget set" branch; ₹-remaining / % copy)
    - MyHomeApp/Features/Budgets/BudgetCategoryCard.swift (card shell lines 47-56: padding 16, secondarySystemBackground, cornerRadius 12, shadow, accessibilityElement combine)
    - MyHomeApp/Support/BudgetCalculator.swift (BudgetColor enum + 0.8/1.0 threshold semantics)
    - .planning/phases/04-overview-charts/04-UI-SPEC.md (§ "Card 1: Spend vs. Budget" — Rows A-D, both empty states, accessibility, 16pt bar / 8pt corner radius)
    - .planning/phases/04-overview-charts/04-PATTERNS.md (lines 112-186: SpendBudgetCard analog + inline fractionUsed/colorThreshold)
  </read_first>
  <action>
    Create `struct SpendBudgetCard: View` taking `totalSpend: Decimal`, `totalBudget: Decimal`, and `@Binding var selectedTab: Int` (for the "Set a budget" nudge). Import SwiftUI only (no SwiftData, no Charts). Compute `fractionUsed`/`colorThreshold` inline using the OverviewAggregation result or the inline 0.8/1.0 logic from PATTERNS lines 172-186 (reuse `BudgetColor`). Build Row A title "This Month" (`.title2`); Row B hero `totalSpend.formattedINR()` (`.largeTitle`, bold) + "of \(totalBudget.formattedINR())" (`.subheadline`, `.secondary`); Row C the GeometryReader+ZStack bar at `.frame(height: 16)` cornerRadius 8, fill = BudgetColor mapping (.normal→.accentColor, .warning→.systemOrange, .overBudget→.systemRed), `.animation(.easeInOut(duration: 0.3), value: fractionUsed)`, `.accessibilityElement(children: .ignore)`; Row D "₹X remaining"/"₹X over budget" + "X% used"/"100%+" matching Phase-2 copy exactly. Empty state — totalBudget == 0: replace Rows C/D with "Set a budget to track your spending." + `Button("Set a budget") { selectedTab = 2 }`; if also totalSpend == 0 show "No spend yet this month." Row B still shows real spend. Apply the card shell + `.accessibilityElement(children: .combine)` with the combined VoiceOver label from the UI-SPEC. Render the over-budget/warning ₹ label in the BudgetColor color. Add a `#Preview` with normal/warning/over/no-budget fixtures.
  </action>
  <verify>
    <automated>xcodebuild build -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' 2>&1 | grep -E "error:|BUILD SUCCEEDED|BUILD FAILED" | head -10</automated>
    <human-check>SpendBudgetCard preview shows the bar in accent under 80%, orange at 80-99%, red at >=100%, and the "Set a budget" nudge when budget is 0.</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -c "struct SpendBudgetCard" MyHomeApp/Features/Overview/SpendBudgetCard.swift` returns 1.
    - File contains all three BudgetColor branches (`grep -c "case .overBudget" ...` >= 1) and `.frame(height: 16)`.
    - Contains the exact copy strings "Set a budget to track your spending." and "No spend yet this month."
    - `grep -c "selectedTab = 2" MyHomeApp/Features/Overview/SpendBudgetCard.swift` returns 1 (Budgets-tab nudge).
    - `grep -c "import Charts" MyHomeApp/Features/Overview/SpendBudgetCard.swift` returns 0.
    - Project builds successfully.
  </acceptance_criteria>
  <done>SpendBudgetCard renders the aggregate bar with BudgetColor thresholds and both D4-07 empty states; project builds.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: TopCategoriesCard (OVR-02)</name>
  <files>MyHomeApp/Features/Overview/TopCategoriesCard.swift</files>
  <read_first>
    - MyHomeApp/Features/Budgets/BudgetsView.swift (uncategorized row HStack lines 182-201: icon frame 28, name, Spacer, ₹ amount; card shell)
    - MyHomeApp/Features/Budgets/BudgetCategoryCard.swift (card shell)
    - .planning/phases/04-overview-charts/04-UI-SPEC.md (§ "Card 2: Top 3 Categories" — rank label 20pt, SF Symbol .secondary, name lineLimit(1), ₹ amount, 44pt rows, empty state, no Divider)
    - .planning/phases/04-overview-charts/04-PATTERNS.md (lines 190-234: TopCategoriesCard analog + pre-sorted top3 tuple shape)
  </read_first>
  <action>
    Create `struct TopCategoriesCard: View` taking `top3: [(category: Category, spent: Decimal)]` (pre-sorted by the parent via `OverviewAggregation.topCategories`, length 0-3). Import SwiftUI + SwiftData (Category is a @Model type). Row A title "Top Categories" (`.title2`). For each of `top3`, render an HStack(spacing: 12): rank "#\(index+1)" (`.subheadline`, `.secondary`, fixed width 20pt); SF Symbol `category.symbolName` (`.body`, `.secondary`, 28pt frame, `.accessibilityHidden(true)`); `category.name` (`.body`, `.primary`, lineLimit(1)); Spacer; `spent.formattedINR()` (`.body`, `.primary`). Each row `.frame(minHeight: 44)`. No `Divider()` between rows (VStack spacing only). Empty state — `top3.isEmpty`: single centered "No spend yet this month." (`.subheadline`, `.secondary`). Apply card shell + `.accessibilityElement(children: .combine)`; add `.accessibilityAddTraits(.isStaticText)` per the UI-SPEC. Add a `#Preview` with 3-category, 1-category, and empty fixtures.
  </action>
  <verify>
    <automated>xcodebuild build -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' 2>&1 | grep -E "error:|BUILD SUCCEEDED|BUILD FAILED" | head -10</automated>
    <human-check>TopCategoriesCard preview shows ranked rows #1-#3 with ₹ amounts and the "No spend yet" state when empty.</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -c "struct TopCategoriesCard" MyHomeApp/Features/Overview/TopCategoriesCard.swift` returns 1.
    - File contains "Top Categories", "No spend yet this month.", and `.frame(minHeight: 44)`.
    - `grep -c "Divider()" MyHomeApp/Features/Overview/TopCategoriesCard.swift` returns 0 (no inter-row dividers).
    - `grep -c "formattedINR()" MyHomeApp/Features/Overview/TopCategoriesCard.swift` returns >= 1 (full INR for amounts, not compact).
    - Project builds successfully.
  </acceptance_criteria>
  <done>TopCategoriesCard renders <=3 ranked rows or the empty state; project builds.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: PinnedNoteCard (OVR-03)</name>
  <files>MyHomeApp/Features/Overview/PinnedNoteCard.swift</files>
  <read_first>
    - MyHomeApp/Features/Budgets/BudgetCategoryCard.swift (card shell + accessibilityElement combine)
    - MyHomeApp/Support/NoteListOrganizer.swift (the note partitioning — the parent resolves the note via OverviewAggregation; this card receives the resolved Note? + an isPinned-vs-checklist flag)
    - .planning/phases/04-overview-charts/04-UI-SPEC.md (§ "Card 3: Pinned Note" — Rows A-D, pin/checklist chip, empty + fallback states, Open-note / Go-to-Notes affordances, plain Text() security note)
    - .planning/phases/04-overview-charts/04-PATTERNS.md (lines 238-269: PinnedNoteCard analog + deep-link binding pattern; lines 716-719 plain-Text security rule)
  </read_first>
  <action>
    Create `struct PinnedNoteCard: View` taking `note: Note?`, a `isFallbackChecklist: Bool` flag (parent passes true when the note came from the checklist fallback rather than `pinned`), and `@Binding var selectedTab: Int` + `@Binding var deepLinkNoteID: UUID?`. Import SwiftUI + SwiftData. Row A: HStack title "Pinned Note" (`.title2`) + Spacer + chip: `pin.fill` (`.accentColor`) when pinned, `checklist` (`.secondary`) when `isFallbackChecklist`, both `.accessibilityHidden(true)`. When `note != nil`: Row B `Text(note.title)` (`.body`, `.primary`, lineLimit(1)) rendered as PLAIN `Text(...)` — never `AttributedString(markdown:)` (security T-04-06); Row C first non-empty block text (`.subheadline`, `.secondary`, lineLimit(2)), prefixing a checkbox block with "☐ "/"☑ "; Row D `Button("Open note") { deepLinkNoteID = note.id; selectedTab = 3 }`, trailing-aligned, `.accentColor`, with `.accessibilityLabel("Open \(note.title) in Notes tab")`. Empty state — `note == nil`: "Pin a note to see it here." (`.subheadline`, `.secondary`, centered) + `Button("Go to Notes") { selectedTab = 3 }`. Apply card shell + `.accessibilityElement(children: .combine)`. Add a `#Preview` with pinned, checklist-fallback, and empty fixtures.
  </action>
  <verify>
    <automated>xcodebuild build -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' 2>&1 | grep -E "error:|BUILD SUCCEEDED|BUILD FAILED" | head -10</automated>
    <human-check>PinnedNoteCard preview shows the pinned note with pin chip + Open note, the checklist fallback with checklist chip, and the "Pin a note to see it here." empty state with Go to Notes.</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -c "struct PinnedNoteCard" MyHomeApp/Features/Overview/PinnedNoteCard.swift` returns 1.
    - `grep -c "selectedTab = 3" MyHomeApp/Features/Overview/PinnedNoteCard.swift` returns >= 1 (deep-link to Notes tag 3).
    - File contains "Pin a note to see it here." and "Open note" and "Go to Notes".
    - `grep -c "AttributedString(markdown" MyHomeApp/Features/Overview/PinnedNoteCard.swift` returns 0 (plain Text only — T-04-06).
    - Project builds successfully.
  </acceptance_criteria>
  <done>PinnedNoteCard renders pinned/checklist/empty states with the Notes deep-link affordance; project builds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Note content → on-screen rendering | OVR-03 surfaces user-authored note title + first block text in a card preview. The only place Phase 4 renders user-supplied strings. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | Tampering / Info Disclosure | PinnedNoteCard note title + body preview | mitigate | Render via plain `Text(note.title)` / `Text(blockText)` with `lineLimit(1/2)`; NEVER `Text(AttributedString(markdown:))` — no markdown/link injection (mirrors T-01-06 / T-03-09) |
| T-04-07 | Tampering | Deep-link tab switch from PinnedNoteCard | accept | `selectedTab = 3` is an integer assignment to a local `@State` via `@Binding`; no URL parsing, no external input |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — zero external packages; cards import only SwiftUI/SwiftData |
</threat_model>

<verification>
- Project builds for the iPhone 17 simulator with all three card files present.
- Each card renders its populated and empty/fallback states in `#Preview`.
- No card holds a `@Query`; all data arrives via init parameters / bindings.
</verification>

<success_criteria>
- `SpendBudgetCard`, `TopCategoriesCard`, `PinnedNoteCard` exist under `Features/Overview/` and build.
- Each implements the D4-07 inline empty state with the exact UI-SPEC copy.
- PinnedNoteCard renders note content as plain `Text()` and deep-links to `selectedTab = 3`.
</success_criteria>

<output>
Create `.planning/phases/04-overview-charts/04-03-SUMMARY.md` when done, recording the exact init signatures of all three cards (so Plan 04-05's OverviewView wires them verbatim).
</output>
