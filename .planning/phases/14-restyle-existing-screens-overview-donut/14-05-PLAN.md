---
phase: 14-restyle-existing-screens-overview-donut
plan: 05
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - MyHomeApp/Features/Settings/SettingsView.swift
  - MyHomeApp/Features/Settings/UnlockView.swift
  - MyHomeApp/Features/Settings/MigrationReviewSheet.swift
autonomous: true
requirements: [SKIN-05, SKIN-08, SKIN-09]
must_haves:
  truths:
    - "Settings renders charcoal neumorphic surfaces with the per-row icon tile color map from the UI-SPEC"
    - "The Face ID UnlockView restyle does not weaken or auto-dismiss the lock gate (SKIN-09)"
    - "MigrationReviewSheet toggles are canary; destructive items are negative-red"
    - "No stock system color remains in any Settings file"
  artifacts:
    - path: "MyHomeApp/Features/Settings/SettingsView.swift"
      provides: "Restyled settings list with neumorphic icon tile color map"
      contains: "DesignTokens"
  key_links:
    - from: "MyHomeApp/Features/Settings/SettingsView.swift"
      to: "DesignTokens (icon tile color map)"
      via: "per-row systemX -> token mapping"
      pattern: "neuSurface"
---

<objective>
Restyle the Settings screen group (SKIN-05) plus the Gmail Review MigrationReviewSheet (SKIN-08
continued): migrate the SettingsView cardStyle site, apply the per-row icon tile color map from
the UI-SPEC, and swap all stock system colors. Critically, the UnlockView restyle must NOT alter
the `if lockController.isLocked && lockController.lockEnabled` gate logic (SKIN-09 / security).

Purpose: self-contained screen group, parallel with other Wave-2 plans.
Output: restyled Settings, UnlockView, MigrationReviewSheet.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-restyle-existing-screens-overview-donut/14-PATTERNS.md
@.planning/phases/14-restyle-existing-screens-overview-donut/14-UI-SPEC.md
@.planning/phases/14-restyle-existing-screens-overview-donut/14-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle SettingsView + icon tile color map (SKIN-05)</name>
  <files>MyHomeApp/Features/Settings/SettingsView.swift</files>
  <read_first>
    - MyHomeApp/Features/Settings/SettingsView.swift (self — cardStyle line 341; systemBlue/Purple/Indigo/Red/Orange/Green; accentColor)
    - .planning/phases/14-restyle-existing-screens-overview-donut/14-UI-SPEC.md lines 332-347 (exact Settings icon tile color map table)
    - .planning/phases/14-restyle-existing-screens-overview-donut/14-PATTERNS.md lines 501-521 (SettingsView icon color map + cardStyle migration)
  </read_first>
  <action>
    Migrate cardStyle line 341 -> `.neuSurface(.raised, radius: 20)`. Apply the UI-SPEC icon tile color map exactly: Face ID -> positive; Notifications/Connect Gmail/Sign out -> negative; Sync now -> accent; Reconnect Gmail -> orange; Accounts -> catSubscriptions; Assets -> catHealth; Manage Categories -> catRent; Budget period -> orange; Currency -> positive; About MyHome -> accent. Replace `.accentColor`/`.tint(.accentColor)`->accent, all label tiers, Toggle `.tint(DesignTokens.accent)`, destructive rows (Sign out, Reconnect Gmail) foreground `negative`, list rows `.listRowBackground(DesignTokens.surfaceRaised)`, list/scroll canvas `bgCanvas`, version footer `label3`. No logic/data changes.
  </action>
  <verify>
    <automated>grep -rnE 'Color\(\.(secondary|system|tertiary)|accentColor|\.cardStyle\(' MyHomeApp/Features/Settings/SettingsView.swift | grep -v '//' | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - The grep above returns 0.
    - `grep -c 'DesignTokens.catSubscriptions' MyHomeApp/Features/Settings/SettingsView.swift` returns >= 1 (Accounts row icon tile mapped).
    - `xcodebuild build -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' 2>&1 | tail -5` succeeds.
  </acceptance_criteria>
  <done>SettingsView neumorphic; icon tile color map applied; build green.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle UnlockView + MigrationReviewSheet (SKIN-05, SKIN-08, SKIN-09 security)</name>
  <files>MyHomeApp/Features/Settings/UnlockView.swift, MyHomeApp/Features/Settings/MigrationReviewSheet.swift</files>
  <read_first>
    - MyHomeApp/Features/Settings/UnlockView.swift (self — systemBackground, accentColor; keep the lock gate conditional untouched)
    - MyHomeApp/Features/Settings/MigrationReviewSheet.swift (self — systemRed, accentColor)
    - .planning/phases/14-restyle-existing-screens-overview-donut/14-UI-SPEC.md lines 403-411 (Gmail Review / MigrationReviewSheet contract)
    - .planning/phases/14-restyle-existing-screens-overview-donut/14-RESEARCH.md lines 759-762 (security note: UnlockView restyle must not dismiss the lock overlay)
  </read_first>
  <action>
    UnlockView: replace `Color(.systemBackground)`->bgCanvas, `.accentColor`->accent, label tiers per UI-SPEC; leave the `if lockController.isLocked && lockController.lockEnabled` wrapper and the authenticate() call path completely unchanged (security — do not remove or weaken the gate). MigrationReviewSheet: sheet bg `bgCanvas`, confirm toggles `.tint(DesignTokens.accent)`, destructive items `negative`, section labels `label2`, `systemRed`->negative, `.accentColor`->accent. No logic/data changes.
  </action>
  <verify>
    <automated>grep -rnE 'Color\(\.(secondary|system|tertiary)|accentColor' MyHomeApp/Features/Settings/UnlockView.swift MyHomeApp/Features/Settings/MigrationReviewSheet.swift | grep -v '//' | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - The grep above returns 0.
    - `grep -c 'isLocked' MyHomeApp/Features/Settings/UnlockView.swift` returns the same count as before the edit (gate conditional preserved — confirm against the read_first snapshot).
    - `xcodebuild build -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' 2>&1 | tail -5` succeeds.
  </acceptance_criteria>
  <done>UnlockView + MigrationReviewSheet neumorphic; Face ID gate logic untouched; build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Face ID lock gate (UnlockView ↔ LockController) | Existing security boundary; restyle must preserve it, not introduce a new one. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-08 | Elevation of Privilege | UnlockView lock overlay | mitigate | Restyle changes colors only; the `isLocked && lockEnabled` conditional and authenticate() path are explicitly preserved (acceptance asserts isLocked count unchanged) — the gate cannot be accidentally bypassed. |
| T-14-09 | Tampering | MigrationReviewSheet confirm toggles | accept | Toggle binding/confirm logic unchanged; only tint color changes. |
| T-14-SC | Tampering | npm/pip/cargo installs | accept | No package installs; first-party Swift only. |
</threat_model>

<verification>
- Zero stock system colors in Features/Settings/ (the three files in this plan).
- Face ID gate conditional preserved.
- Build green.
</verification>

<success_criteria>
- Settings group fully neumorphic (SKIN-05); MigrationReviewSheet restyled (SKIN-08); Face ID gate unregressed (SKIN-09).
</success_criteria>

<output>
Create `.planning/phases/14-restyle-existing-screens-overview-donut/14-05-SUMMARY.md` when done
</output>
