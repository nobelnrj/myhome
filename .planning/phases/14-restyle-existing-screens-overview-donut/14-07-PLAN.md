---
phase: 14-restyle-existing-screens-overview-donut
plan: 07
type: execute
wave: 3
depends_on: ["14-02"]
files_modified:
  - MyHomeApp/Features/Expenses/ExpenseListView.swift
  - MyHomeApp/Features/Expenses/ExpenseRow.swift
  - MyHomeApp/Features/Expenses/ReviewInboxRow.swift
  - MyHomeApp/Features/Expenses/AddExpenseView.swift
  - MyHomeApp/Features/Expenses/EditExpenseView.swift
  - MyHomeApp/Features/Expenses/DecimalKeypadView.swift
  - MyHomeApp/Features/Expenses/AccountPickerView.swift
  - MyHomeApp/Features/Expenses/CategoryPickerView.swift
  - MyHomeApp/Features/Expenses/TransferPairRow.swift
autonomous: true
requirements: [SKIN-02, SKIN-08, SKIN-09]
must_haves:
  truths:
    - "Activity list, add/edit expense sheets, pickers, keypad, review inbox, and transfer rows render neumorphic"
    - "Income amounts are positive-green, spend amounts are negative-red with a leading minus"
    - "Review inbox 'Add Expense' CTA is canary; 'Dismiss' is recessed; Inbox Zero empty state is positive"
    - "Transfer pair rows use DesignTokens.positive (no .tint(.green)); confirm/dismiss swipe actions unchanged (SKIN-09)"
    - "No stock system color remains in any Expenses file"
  artifacts:
    - path: "MyHomeApp/Features/Expenses/ExpenseRow.swift"
      provides: "Restyled expense row with category icon tile + income/spend color split"
      contains: "DesignTokens"
  key_links:
    - from: "MyHomeApp/Features/Expenses/TransferPairRow.swift"
      to: "DesignTokens.positive"
      via: "replace .tint(.green)"
      pattern: "DesignTokens\\.positive"
---

<objective>
Restyle the Activity / Expenses screen group (SKIN-02), the Gmail Review inbox rows (SKIN-08),
and the Transfer Inbox rows (SKIN-08) to the neumorphic design system. Swap all stock system
colors, apply the income/spend color split, and replace `.tint(.green)` on transfer rows with
`DesignTokens.positive`. No data/logic changes — expense CRUD, self-transfer confirm swipe, and
Gmail review confirm flows must behave identically (SKIN-09).

This plan is Wave 3 because it edits `ExpenseListView.swift`, which 14-02 already modified to add
the OVR-06 `categoryFilter` binding param — serializing avoids a merge conflict on that file.

Output: fully restyled Activity/Expenses + Transfer + Gmail Review rows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-restyle-existing-screens-overview-donut/14-PATTERNS.md
@.planning/phases/14-restyle-existing-screens-overview-donut/14-UI-SPEC.md
@.planning/phases/14-restyle-existing-screens-overview-donut/14-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle ExpenseListView + rows + transfer rows (SKIN-02, SKIN-08)</name>
  <files>MyHomeApp/Features/Expenses/ExpenseListView.swift, MyHomeApp/Features/Expenses/ExpenseRow.swift, MyHomeApp/Features/Expenses/ReviewInboxRow.swift, MyHomeApp/Features/Expenses/TransferPairRow.swift</files>
  <read_first>
    - MyHomeApp/Features/Expenses/ExpenseListView.swift (self — `.accentColor`; the segmented Picker; do NOT touch the `categoryFilter` binding param added by 14-02 — restyle the body only)
    - MyHomeApp/Features/Expenses/ExpenseRow.swift (self — systemGreen, Color(.label); icon tile via CategoryStyle already recolored by 14-01)
    - MyHomeApp/Features/Overview/OverviewView.swift RecentExpenseRow (analog row from 14-02, already restyled)
    - MyHomeApp/Features/Expenses/TransferPairRow.swift (self — `.tint(.green)` to replace)
    - .planning/phases/14-restyle-existing-screens-overview-donut/14-UI-SPEC.md lines 251-270 (Screen 2 component map + Transfer Inbox rule)
    - .planning/phases/14-restyle-existing-screens-overview-donut/14-PATTERNS.md lines 412-423 (ExpenseRow migration)
  </read_first>
  <action>
    ExpenseListView: replace `.accentColor`->accent; segmented control uses `surfaceElevatedControl` track + `accent` active pill + `label`/`label2` text; day group cards `.neuSurface(.raised, padding: nil)` with `separatorHairline` row dividers; search bar `fillRecessed` bg; canvas bg rule; `.listRowBackground(DesignTokens.surfaceRaised)`. Leave the 14-02 OVR-06 `categoryFilter` binding and its onAppear handling intact (do not remove/rename). ExpenseRow: category icon tile 30x30 radius 9 via `CategoryStyle.color(for:)` (already DesignTokens.cat*); merchant `label`, subtitle `label2`; income amount -> positive `.semibold`, spend -> negative `.semibold`; `Color(.label)`->label, `systemGreen`->positive. ReviewInboxRow: card `.neuSurface(.raised, isInteractive: true)`; sparkle/guesser row `fillRecessed` bg + `accent` icon; Dismiss `fillRecessed` bg + `label2`; Add Expense `accent` bg + `accentOnYellow` text; Inbox Zero empty state `positive`. TransferPairRow: replace `.tint(.green)` -> `DesignTokens.positive`; confirmed badge `positive`, pending badge `accent`; do NOT change the confirm/dismiss swipe-action logic (SKIN-09).
  </action>
  <verify>
    <automated>grep -rnE 'Color\(\.(secondary|system|tertiary|label)|accentColor|\.tint\(\.green\)' MyHomeApp/Features/Expenses/ExpenseListView.swift MyHomeApp/Features/Expenses/ExpenseRow.swift MyHomeApp/Features/Expenses/ReviewInboxRow.swift MyHomeApp/Features/Expenses/TransferPairRow.swift | grep -v '//' | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - The grep above returns 0 (no stock system color, accentColor, or `.tint(.green)`).
    - `grep -c 'DesignTokens.positive' MyHomeApp/Features/Expenses/TransferPairRow.swift` returns >= 1.
    - `grep -c 'categoryFilter' MyHomeApp/Features/Expenses/ExpenseListView.swift` returns >= 1 (OVR-06 binding from 14-02 preserved).
    - `xcodebuild build -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' 2>&1 | tail -5` succeeds.
  </acceptance_criteria>
  <done>Activity list, rows, review inbox, and transfer rows neumorphic; income/spend color split applied; OVR-06 binding intact; build green.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle add/edit expense sheets, pickers, keypad (SKIN-02)</name>
  <files>MyHomeApp/Features/Expenses/AddExpenseView.swift, MyHomeApp/Features/Expenses/EditExpenseView.swift, MyHomeApp/Features/Expenses/DecimalKeypadView.swift, MyHomeApp/Features/Expenses/AccountPickerView.swift, MyHomeApp/Features/Expenses/CategoryPickerView.swift</files>
  <read_first>
    - MyHomeApp/Features/Expenses/AddExpenseView.swift (self — systemRed/Green, secondarySystemBackground, accentColor)
    - MyHomeApp/Features/Expenses/DecimalKeypadView.swift (self — secondarySystemBackground keypad cells)
    - MyHomeApp/Features/Expenses/CategoryPickerView.swift (self — systemRed, Color.accentColor; selection border)
    - .planning/phases/14-restyle-existing-screens-overview-donut/14-UI-SPEC.md lines 268 (Add/Edit sheet + keypad contract), 266 (category picker chips)
  </read_first>
  <action>
    AddExpenseView/EditExpenseView: sheet bg `bgCanvas`; input rows `surfaceRaised`; amount field `label`, error state `negative`, income-negative amounts `positive`; `systemRed`->negative, `systemGreen`->positive, `secondarySystemBackground`->surfaceRaised (or `.neuSurface(.recessed)` for input wells), `.accentColor`->accent. DecimalKeypadView: keypad bg `bgCanvas`, key cells `surfaceElevatedControl`, `secondarySystemBackground`->surfaceElevatedControl. AccountPickerView/CategoryPickerView: chips `fillRecessed` bg, selected chip 1.5pt border in `cat*` color + glow; `systemRed`->negative, `Color.accentColor`->accent. Do NOT change EditExpenseView's `applyTransferMark` logic or the local @State mirror / isDirty detection (SKIN-09). No data/logic changes.
  </action>
  <verify>
    <automated>grep -rnE 'Color\(\.(secondary|system|tertiary)|accentColor' MyHomeApp/Features/Expenses/AddExpenseView.swift MyHomeApp/Features/Expenses/EditExpenseView.swift MyHomeApp/Features/Expenses/DecimalKeypadView.swift MyHomeApp/Features/Expenses/AccountPickerView.swift MyHomeApp/Features/Expenses/CategoryPickerView.swift | grep -v '//' | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - The grep above returns 0.
    - `grep -c 'applyTransferMark' MyHomeApp/Features/Expenses/EditExpenseView.swift` returns the same count as before the edit (transfer-mark logic preserved).
    - `xcodebuild clean build test -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:MyHomeTests/EditExpenseTransferTests 2>&1 | tail -20` green (self-transfer confirm logic unregressed).
  </acceptance_criteria>
  <done>Add/edit sheets, pickers, keypad neumorphic; transfer-mark + isDirty logic untouched; build + EditExpenseTransferTests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gmail-ingested expense → Review inbox → user confirm | Existing boundary (untrusted parsed email data confirmed by user). Restyle must not auto-confirm or bypass review. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-12 | Tampering | Review inbox Add/Dismiss actions | mitigate | Only button colors/surfaces change; the confirm/dismiss bindings are unchanged; SKIN-09 phase-gate smoke test confirms review flow. |
| T-14-13 | Tampering | EditExpenseView self-transfer mark | mitigate | `applyTransferMark` logic preserved (acceptance asserts count unchanged); EditExpenseTransferTests gate confirms no regression. |
| T-14-SC | Tampering | npm/pip/cargo installs | accept | No package installs; first-party Swift only. |
</threat_model>

<verification>
- Zero stock system colors / accentColor / `.tint(.green)` in Features/Expenses/.
- EditExpenseTransferTests green.
- Build green.
</verification>

<success_criteria>
- Activity/Expenses (SKIN-02), Gmail Review + Transfer Inbox (SKIN-08) fully neumorphic; CRUD/self-transfer/review flows unregressed (SKIN-09).
</success_criteria>

<output>
Create `.planning/phases/14-restyle-existing-screens-overview-donut/14-07-SUMMARY.md` when done
</output>
