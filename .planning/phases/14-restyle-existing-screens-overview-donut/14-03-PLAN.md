---
phase: 14-restyle-existing-screens-overview-donut
plan: 03
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - MyHomeApp/Features/Budgets/BudgetsView.swift
  - MyHomeApp/Features/Budgets/BudgetCategoryCard.swift
  - MyHomeApp/Features/Budgets/BudgetProgressView.swift
  - MyHomeApp/Features/Budgets/EditBudgetSheet.swift
  - MyHomeApp/Features/Budgets/ManageCategoriesView.swift
  - MyHomeApp/Features/Budgets/FilteredExpenseListView.swift
  - MyHomeApp/Features/Shared/StackBar.swift
autonomous: true
requirements: [SKIN-03, SKIN-09]
must_haves:
  truths:
    - "Budgets screen renders charcoal neumorphic surfaces with canary-yellow accents"
    - "The budget summary ring is a floating hero card; category budget cards are tappable raised cards"
    - "No stock system color remains in any Budgets file or StackBar"
  artifacts:
    - path: "MyHomeApp/Features/Budgets/BudgetsView.swift"
      provides: "Restyled budgets list with .neuSurface cards"
      contains: "neuSurface"
  key_links:
    - from: "MyHomeApp/Features/Budgets/BudgetCategoryCard.swift"
      to: "DesignTokens / CategoryStyle"
      via: ".neuSurface + category palette progress fill"
      pattern: "neuSurface"
---

<objective>
Restyle the Budgets screen group to the neumorphic design system (SKIN-03): migrate the 4
`.cardStyle()` sites, swap all stock system colors for DesignTokens, and apply the floating
budget-summary-ring hero per the UI-SPEC. No data/logic changes (SKIN-09 no-regression).

Purpose: Budgets is a self-contained screen group with no file overlap with other Wave-2 plans,
so it runs in parallel. The category palette comes from the 14-01 CategoryStyle rewrite.
Output: fully restyled Budgets group + StackBar.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-restyle-existing-screens-overview-donut/14-PATTERNS.md
@.planning/phases/14-restyle-existing-screens-overview-donut/14-UI-SPEC.md
@.planning/phases/14-restyle-existing-screens-overview-donut/14-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle BudgetsView + summary ring hero + cardStyle migration (SKIN-03)</name>
  <files>MyHomeApp/Features/Budgets/BudgetsView.swift, MyHomeApp/Features/Shared/StackBar.swift</files>
  <read_first>
    - MyHomeApp/Features/Budgets/BudgetsView.swift (self — cardStyle sites lines 211/258/304; system colors systemBackground/systemRed/systemOrange/systemGreen/tertiarySystemFill)
    - MyHomeApp/Features/Assets/NetWorthCard.swift (analog for `.neuSurface(.floating)` summary ring hero with DonutChart center)
    - .planning/phases/14-restyle-existing-screens-overview-donut/14-PATTERNS.md lines 525-541 (BudgetsView migration map) + lines 700-713 (progress bar track)
    - .planning/phases/14-restyle-existing-screens-overview-donut/14-UI-SPEC.md lines 274-288 (Screen 3 Budgets component map)
  </read_first>
  <action>
    Migrate cardStyle sites per UI-SPEC: line 211 -> `.neuSurface(.raised, radius: 20)`, line 258 -> `.neuSurface(.floating, padding: 22)` (budget summary ring hero), line 304 -> `.neuSurface(.raised, padding: 20)`. Replace `Color(.systemBackground)` -> `DesignTokens.bgCanvas` (apply `.scrollContentBackground(.hidden).background(DesignTokens.bgCanvas)` on the List/ScrollView), `systemRed`->negative, `systemOrange`->orange, `systemGreen`->positive, `tertiarySystemFill`->fillRecessed2, all `.primary/.secondary/.tertiary`->label/label2/label3, `.accentColor`/`.tint(.accentColor)`->accent. Budget ring color logic: on-budget `positive`, >85% `orange`, over `negative`. In `StackBar.swift` replace `tertiarySystemFill` -> `DesignTokens.fillRecessed`. No logic/data changes.
  </action>
  <verify>
    <automated>grep -rnE 'Color\(\.(secondary|system|tertiary)|\.cardStyle\(' MyHomeApp/Features/Budgets/BudgetsView.swift MyHomeApp/Features/Shared/StackBar.swift | grep -v '//' | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rnE 'Color\(\.(secondary|system|tertiary)|.cardStyle\(' MyHomeApp/Features/Budgets/BudgetsView.swift MyHomeApp/Features/Shared/StackBar.swift | grep -v '//' | wc -l` returns 0.
    - `grep -c 'neuSurface(.floating' MyHomeApp/Features/Budgets/BudgetsView.swift` returns >= 1 (summary ring hero).
    - `xcodebuild build -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' 2>&1 | tail -5` succeeds.
  </acceptance_criteria>
  <done>BudgetsView + StackBar fully neumorphic; summary ring is floating hero; build green.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle remaining Budgets files (SKIN-03)</name>
  <files>MyHomeApp/Features/Budgets/BudgetCategoryCard.swift, MyHomeApp/Features/Budgets/BudgetProgressView.swift, MyHomeApp/Features/Budgets/EditBudgetSheet.swift, MyHomeApp/Features/Budgets/ManageCategoriesView.swift, MyHomeApp/Features/Budgets/FilteredExpenseListView.swift</files>
  <read_first>
    - MyHomeApp/Features/Budgets/BudgetCategoryCard.swift (self — cardStyle line 86; systemRed/systemOrange/tertiarySystemFill/tertiaryLabel/secondaryLabel)
    - MyHomeApp/Features/Assets/NetWorthCard.swift lines 103-117 (legendRow bullet+label+amount analog for BudgetCategoryCard)
    - MyHomeApp/Features/Expenses/ExpenseListView.swift (analog for FilteredExpenseListView list styling — canvas background + listRowBackground)
    - .planning/phases/14-restyle-existing-screens-overview-donut/14-PATTERNS.md lines 427-448 (BudgetCategoryCard migration)
    - .planning/phases/14-restyle-existing-screens-overview-donut/14-UI-SPEC.md lines 284-288 (category budget card, progress bar, edit sheet contracts)
  </read_first>
  <action>
    BudgetCategoryCard: migrate cardStyle line 86 -> `.neuSurface(.raised, radius: 20, padding: 15, isInteractive: true)`; replace systemRed->negative, systemOrange->orange, tertiarySystemFill->fillRecessed2 (progress track), tertiaryLabel->label3, secondaryLabel->label2, `.secondary/.tertiary`->label2/label3; progress fill uses `CategoryStyle.color(for:)` (on-track), orange (>85%), negative (over). BudgetProgressView/EditBudgetSheet/ManageCategoriesView: replace `secondarySystemBackground`->surfaceRaised or `.neuSurface(.recessed)` for input wells, systemRed->negative, `.accentColor`->accent, label tiers per UI-SPEC. FilteredExpenseListView: apply `.scrollContentBackground(.hidden).background(DesignTokens.bgCanvas)` + `.listRowBackground(DesignTokens.surfaceRaised)` (audit per RESEARCH A2 — fix any system colors found). No data/logic changes.
  </action>
  <verify>
    <automated>grep -rnE 'Color\(\.(secondary|system|tertiary)|\.cardStyle\(' MyHomeApp/Features/Budgets/BudgetCategoryCard.swift MyHomeApp/Features/Budgets/BudgetProgressView.swift MyHomeApp/Features/Budgets/EditBudgetSheet.swift MyHomeApp/Features/Budgets/ManageCategoriesView.swift MyHomeApp/Features/Budgets/FilteredExpenseListView.swift | grep -v '//' | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - The grep above returns 0 (no stock system colors or cardStyle in any remaining Budgets file).
    - `grep -c 'neuSurface' MyHomeApp/Features/Budgets/BudgetCategoryCard.swift` returns >= 1.
    - `xcodebuild build -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' 2>&1 | tail -5` succeeds.
  </acceptance_criteria>
  <done>All remaining Budgets files neumorphic; category cards tappable raised cards; build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Presentational restyle only; no new input, network, or storage path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-06 | Tampering | Budget edit sheet (existing input) | accept | EditBudgetSheet's validation/save logic is unchanged; only colors/surfaces change — no new attack surface. |
| T-14-SC | Tampering | npm/pip/cargo installs | accept | No package installs; first-party Swift only. |
</threat_model>

<verification>
- Zero stock system colors / cardStyle in Features/Budgets/ and StackBar.
- Build green.
</verification>

<success_criteria>
- Budgets group fully neumorphic (SKIN-03), no regression in budget edit/manage flows (SKIN-09).
</success_criteria>

<output>
Create `.planning/phases/14-restyle-existing-screens-overview-donut/14-03-SUMMARY.md` when done
</output>
