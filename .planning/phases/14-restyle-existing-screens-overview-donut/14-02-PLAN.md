---
phase: 14-restyle-existing-screens-overview-donut
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - MyHomeApp/Features/Overview/SpendDonutCard.swift
  - MyHomeApp/Features/Overview/OverviewView.swift
  - MyHomeApp/Features/Overview/SpendBudgetCard.swift
  - MyHomeApp/Features/Overview/PinnedNoteCard.swift
  - MyHomeApp/Features/Overview/SpendByCategoryChart.swift
  - MyHomeApp/Features/Overview/SpendOverTimeChart.swift
  - MyHomeApp/Features/Overview/TopCategoriesCard.swift
  - MyHomeApp/RootView.swift
  - MyHomeApp/Features/Expenses/ExpenseListView.swift
  - MyHome.xcodeproj/project.pbxproj
  - MyHomeTests/SpendDonutCardTests.swift
autonomous: true
requirements: [SKIN-01, OVR-05, OVR-06, SKIN-09]
user_setup: []
must_haves:
  truths:
    - "The 'Where it's going' donut appears on Overview showing top-4 categories + Others with a rolling center total"
    - "Tapping a donut legend row navigates to Activity pre-filtered to that category"
    - "The Overview hero card shows a NET CASH FLOW income/spent split (D-04)"
    - "SpendByCategoryChart and SpendOverTimeChart remain on Overview, restyled (D-05)"
    - "No Color(.secondarySystemBackground) or other stock system color remains in any Overview file"
  artifacts:
    - path: "MyHomeApp/Features/Overview/SpendDonutCard.swift"
      provides: "OVR-05/06 donut card composing DonutChart with tappable legend"
      contains: "struct SpendDonutCard"
      min_lines: 60
    - path: "MyHomeTests/SpendDonutCardTests.swift"
      provides: "OVR-06 tap callback returns correct category UUID"
  key_links:
    - from: "MyHomeApp/Features/Overview/SpendDonutCard.swift"
      to: "DonutChart"
      via: "composition with tappable legend Buttons"
      pattern: "DonutChart\\(segments:"
    - from: "MyHomeApp/RootView.swift"
      to: "ExpenseListView"
      via: "Binding<UUID?> activityCategoryFilter (OVR-06)"
      pattern: "activityCategoryFilter"
    - from: "MyHomeApp/Features/Overview/SpendDonutCard.swift"
      to: "SpendDonutAggregation"
      via: "consumes ranked segments computed via the aggregation helper"
      pattern: "onCategoryTap"
---

<objective>
Deliver the Overview restyle plus the "Where it's going" spend donut (OVR-05) with
tap-to-filter into Activity (OVR-06), and replace the Overview hero with the NET CASH FLOW
income/spent split layout (D-04). Restyle every Overview file to neumorphic tokens (SKIN-01),
keeping `SpendByCategoryChart` and `SpendOverTimeChart` on Overview but restyled (D-05).

Purpose: the donut and net-cash-flow hero are the only net-new visible features in Phase 14;
everything else is token substitution. OVR-06 threads a `Binding<UUID?>` through RootView ->
OverviewView -> ExpenseListView, mirroring the existing `deepLinkNoteID` pattern.
Output: SpendDonutCard.swift (new, pbxproj-registered), restyled Overview, OVR-06 wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-restyle-existing-screens-overview-donut/14-RESEARCH.md
@.planning/phases/14-restyle-existing-screens-overview-donut/14-PATTERNS.md
@.planning/phases/14-restyle-existing-screens-overview-donut/14-UI-SPEC.md
@.planning/phases/14-restyle-existing-screens-overview-donut/14-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SpendDonutCard + register in pbxproj + OVR-06 wiring (OVR-05/06)</name>
  <files>MyHomeApp/Features/Overview/SpendDonutCard.swift, MyHome.xcodeproj/project.pbxproj, MyHomeApp/RootView.swift, MyHomeApp/Features/Expenses/ExpenseListView.swift</files>
  <read_first>
    - MyHomeApp/Features/Assets/NetWorthCard.swift (primary analog: DonutChart caller with HStack legend, `toDouble` NSDecimalNumber conversion lines 130-132, `.cardStyle` -> `.neuSurface` target)
    - MyHomeApp/Features/Overview/OverviewView.swift lines 301-351 (existing `WhereItsGoingCard` private struct being extracted into the new file; lines 136-147 for `rankedSpend`/`totalSpend`)
    - MyHomeApp/Features/Shared/DonutChart.swift lines 17-48 (DonutSegment + DonutChart<Center> API; chart is `.accessibilityHidden(true)`)
    - MyHomeApp/RootView.swift lines 34-72, 110-116 (existing `deepLinkNoteID` @State + `ExpenseListView(reviewBadgeCount:)` call + deep-link observer pattern to mirror)
    - MyHomeApp/Features/Expenses/ExpenseListView.swift lines 19-68 (struct, `@State categoryFilter`, `private enum CategoryFilter { case category(UUID) }`, `@Binding reviewBadgeCount`)
    - .planning/phases/14-restyle-existing-screens-overview-donut/14-PATTERNS.md lines 70-202 (SpendDonutCard signature, center 21pt Text pattern, legend rows, OVR-06 tap navigation)
  </read_first>
  <action>
    Create `MyHomeApp/Features/Overview/SpendDonutCard.swift` with `import SwiftUI/Charts/SwiftData` and `struct SpendDonutCard: View` taking `ranked: [(category: Category, spent: Decimal)]`, `total: Decimal`, `onCategoryTap: (UUID?) -> Void`. Compose `DonutChart(segments:size:132)` with a center closure using a 21pt stat Text (`.font(.system(size: 21, weight: .light, design: .rounded)).monospacedDigit().contentTransition(.numericText()).animation(.smooth(duration: 0.78), value: total)`) and a "SPENT" caption in `DesignTokens.label2` — do NOT use RollingMoneyText here (it is locked at 46pt; Pitfall 5). Build segments from `ranked.prefix(4)` colored via `CategoryStyle.color(for:)` (now DesignTokens.cat*) plus an "Others" segment (`DesignTokens.catOther`) when `ranked.count > 4` and the remainder > 0. Each legend row is a `Button { onCategoryTap(item.categoryID) }` (nil = Others) with a glowing 9x9 dot, name in `DesignTokens.label`, amount via `formattedINRWhole()` in `DesignTokens.label2`, and `.accessibilityLabel("\(label): ...")`. Empty state when ranked is empty: `circle.dashed` 40pt `DesignTokens.label3` + "No spend this month". Wrap in `.neuSurface(.raised, padding: 18)` — never add `.clipped()`/`.clipShape()` outside (Pitfall 2).
    Register the new file with the 4 manual pbxproj edits in `MyHome.xcodeproj/project.pbxproj`: PBXFileReference, PBXBuildFile, PBXSourcesBuildPhase entry, and PBXGroup children entry under the Overview group (project has NO synchronized groups — Xcode does not auto-discover; Pitfall 3).
    OVR-06 wiring: in RootView add `@State private var activityCategoryFilter: UUID? = nil` alongside `deepLinkNoteID`, pass it as `categoryFilter: $activityCategoryFilter` into both `OverviewView` and `ExpenseListView`. In ExpenseListView add an optional `@Binding var categoryFilter: UUID?` init param (default behavior preserved when nil) that, when non-nil on appear, sets the internal `CategoryFilter` to `.category(uuid)` and then clears the binding. Do NOT change the access level of the private `CategoryFilter` enum (A1) — translate the incoming UUID to it internally. Keep `kOpenNoteNotification`/`kOpenReconcileNotification` deep-links and Face ID gate untouched (SKIN-09).
  </action>
  <verify>
    <automated>xcodebuild clean build -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `xcodebuild clean build -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17'` succeeds with zero "cannot find type 'SpendDonutCard'" errors (proves pbxproj registration).
    - `grep -c 'SpendDonutCard.swift' MyHome.xcodeproj/project.pbxproj` returns >= 2 (PBXFileReference + PBXBuildFile present).
    - `grep -c 'struct SpendDonutCard' MyHomeApp/Features/Overview/SpendDonutCard.swift` returns 1.
    - `grep -c 'activityCategoryFilter' MyHomeApp/RootView.swift` returns >= 2.
    - `grep -c 'RollingMoneyText' MyHomeApp/Features/Overview/SpendDonutCard.swift` returns 0.
    - `grep -c '.clipped()' MyHomeApp/Features/Overview/SpendDonutCard.swift` returns 0.
  </acceptance_criteria>
  <done>SpendDonutCard exists and builds clean; pbxproj registered; OVR-06 binding threaded through RootView -> Overview/ExpenseListView.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Overview restyle + NET CASH FLOW hero + donut call site (SKIN-01, D-04, D-05, OVR-05/06)</name>
  <files>MyHomeApp/Features/Overview/OverviewView.swift, MyHomeApp/Features/Overview/SpendBudgetCard.swift, MyHomeApp/Features/Overview/PinnedNoteCard.swift, MyHomeApp/Features/Overview/SpendByCategoryChart.swift, MyHomeApp/Features/Overview/SpendOverTimeChart.swift, MyHomeApp/Features/Overview/TopCategoriesCard.swift, MyHomeTests/SpendDonutCardTests.swift</files>
  <read_first>
    - MyHomeApp/Features/Overview/OverviewView.swift (self — `OverviewMonthContent`, `rankedSpend`/`totalSpend`, `WhereItsGoingCard` private struct to remove, `sectionHeader` helper, cardStyle sites lines 209/228/293/349)
    - MyHomeApp/Features/Overview/SpendBudgetCard.swift (self — hero card; cardStyle line 101; layout rewrite to income/spent split)
    - MyHomeApp/Features/Assets/NetWorthCard.swift (analog for `.neuSurface(.floating)` hero with split tiles + RollingMoneyText)
    - MyHomeApp/DesignSystem/RollingMoneyText.swift lines 29-61 (46pt hero API; color param)
    - .planning/phases/14-restyle-existing-screens-overview-donut/14-PATTERNS.md lines 284-410 (Overview migration map, cardStyle table, NET CASH FLOW layout)
    - .planning/phases/14-restyle-existing-screens-overview-donut/14-UI-SPEC.md lines 162-247 (Screen 1 card inventory + hero color rule + donut spec)
  </read_first>
  <behavior>
    - Test (SpendDonutCardTests): tapping a legend row invokes `onCategoryTap` with that segment's category UUID; the "Others" row invokes it with `nil`.
    - Test: the segment list passed to the card preserves top-4 ordering from SpendDonutAggregation.
  </behavior>
  <action>
    Restyle `OverviewView.swift`: migrate the 4 `.cardStyle()` sites per the UI-SPEC CardStyle Migration Map (209 -> `.neuSurface(.raised)`, 228 -> `.neuSurface(.raised, padding: nil)`, 293/ReviewBanner -> `.neuSurface(.raised, radius: 20, padding: 14, isInteractive: true)`); replace `Color(.systemGroupedBackground)` with `.scrollContentBackground(.hidden).background(DesignTokens.bgCanvas)`; replace all `.primary/.secondary/.tertiary` foreground usages with `DesignTokens.label/label2/label3` and `.accentColor`/`Color(.systemRed/Orange)` with `DesignTokens.accent/negative/orange` per the migration map. Remove the `private struct WhereItsGoingCard` and replace its call site (line ~195) with a "Where it's going" 22pt section label + `SpendDonutCard(ranked: Array(rankedSpend.prefix(4)), total: totalSpend, onCategoryTap: { uuid in activityCategoryFilter = uuid; selectedTab = 1 })`. Thread the `categoryFilter: Binding<UUID?>` from OverviewView through `OverviewMonthContent` so the closure can write it (mirror existing `selectedTab` binding threading). Per D-05, RESTYLE and KEEP `SpendByCategoryChart`, `SpendOverTimeChart`, `TopCategoriesCard` (swap `secondarySystemBackground`->surfaceRaised/`.neuSurface`, `.accentColor`->accent) — do NOT remove them.
    Rewrite `SpendBudgetCard.swift` (D-04 NET CASH FLOW): migrate cardStyle line 101 -> `.neuSurface(.floating, padding: 18)`; replace the stacked-bar spend/remaining layout with an income tile + spent tile (each `.neuSurface(.recessed, radius: DesignTokens.radiusInner)`), a `RollingMoneyText` net total (positive color when net>=0, negative + "−" prefix when net<0 per UI-SPEC hero color rule), a "NET CASH FLOW" caption row with status chip, and a flat budget progress strip (track `DesignTokens.fillRecessed2`, fill `positive`/`negative`). This is a structural layout change per D-04, NOT a v1/simplified placeholder.
    Restyle `PinnedNoteCard.swift`: replace the `.padding.background(Color(.secondarySystemBackground)).clipShape.shadow` block with `.neuSurface(.raised, isInteractive: true)`; `Color.accentColor`->accent; label tiers per map.
    Write `SpendDonutCardTests.swift` for the OVR-06 tap callback (RED then GREEN against the card built in Task 1).
  </action>
  <verify>
    <automated>xcodebuild clean build test -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:MyHomeTests/SpendDonutCardTests 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `xcodebuild clean build test ... -only-testing:MyHomeTests/SpendDonutCardTests` green (OVR-06 callback verified).
    - `grep -rnE 'Color\(\.(secondary|system|tertiary)' MyHomeApp/Features/Overview/ | grep -v '//' | wc -l` returns 0 (no stock system color in any Overview file).
    - `grep -rc '.cardStyle(' MyHomeApp/Features/Overview/ | grep -v ':0' | wc -l` returns 0 (all Overview cardStyle sites migrated).
    - `grep -c 'RollingMoneyText' MyHomeApp/Features/Overview/SpendBudgetCard.swift` returns >= 1 (NET CASH FLOW hero uses 46pt rolling total).
    - `grep -c 'WhereItsGoingCard' MyHomeApp/Features/Overview/OverviewView.swift` returns 0 (replaced by SpendDonutCard).
    - `grep -lE 'SpendByCategoryChart|SpendOverTimeChart' MyHomeApp/Features/Overview/OverviewView.swift` is non-empty (charts retained per D-05).
  </acceptance_criteria>
  <done>Overview fully neumorphic; donut wired with tap-to-filter; NET CASH FLOW hero shows income/spent split; both legacy charts restyled and retained; OVR-06 test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | OVR-06 navigation reuses the existing in-process `selectedTab`/`deepLinkNoteID` binding mechanism; no new IPC, URL scheme, or external input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03 | Information Disclosure | Donut center total | mitigate | Total is computed from SpendDonutAggregation (self-transfer-excluded); no raw transfer amounts surfaced. |
| T-14-04 | Spoofing/Deep-link | OVR-06 category binding | accept | `activityCategoryFilter` is in-process @State only (not URL/notification driven); cannot be set by an external party. Cleared after consumption to avoid stale filter. |
| T-14-05 | Tampering | pbxproj registration of new file | mitigate | Clean-build gate fails if the 4 pbxproj edits are missing — registration is verified, not assumed. |
| T-14-SC | Tampering | npm/pip/cargo installs | accept | No package installs; first-party Swift only. |
</threat_model>

<verification>
- Clean build succeeds (pbxproj registration of SpendDonutCard proven).
- SpendDonutCardTests green (OVR-06 tap callback).
- Zero stock system colors in Features/Overview/.
- All Overview cardStyle sites migrated.
</verification>

<success_criteria>
- "Where it's going" donut renders top-4 + Others with rolling center total (OVR-05).
- Tapping a legend row navigates to Activity pre-filtered to that category (OVR-06).
- NET CASH FLOW hero shows income/spent split (D-04).
- Both legacy charts restyled and retained on Overview (D-05).
</success_criteria>

<output>
Create `.planning/phases/14-restyle-existing-screens-overview-donut/14-02-SUMMARY.md` when done
</output>
