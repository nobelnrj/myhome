---
phase: 14-restyle-existing-screens-overview-donut
plan: 08
type: execute
wave: 4
depends_on: ["14-02", "14-03", "14-04", "14-05", "14-06", "14-07"]
files_modified:
  - MyHome.xcodeproj/project.pbxproj
autonomous: false
requirements: [SKIN-09]
must_haves:
  truths:
    - "CardStyle.swift and NeuTabBar.swift are deleted and de-registered from project.pbxproj"
    - "No .cardStyle( call site remains anywhere; no NeuTabBar reference remains anywhere"
    - "A full clean build + test suite passes with zero 'cannot find type' errors"
    - "No stock system color (secondarySystemBackground/systemX/accentColor) remains in any feature view file"
    - "All v1.1 flows verified by human on the iPhone 17 simulator with no regression"
  artifacts:
    - path: "MyHome.xcodeproj/project.pbxproj"
      provides: "pbxproj with CardStyle.swift and NeuTabBar.swift references removed"
  key_links:
    - from: "MyHome.xcodeproj/project.pbxproj"
      to: "(removed) CardStyle.swift / NeuTabBar.swift"
      via: "4 removal edits each (PBXFileReference, PBXBuildFile, PBXSourcesBuildPhase, PBXGroup)"
      pattern: "CardStyle"
---

<objective>
Close Phase 14: delete the now-orphaned `CardStyle.swift` deprecation shim (D-03) and the
orphaned `NeuTabBar.swift` (D-02), each with the 4 manual pbxproj removal edits, then run the
full clean-build + test gate and an app-wide stock-color assertion to prove SKIN-09. Finish with
an end-of-phase human verification walk-through of every v1.1 flow.

Purpose: deletion must come LAST because CardStyle call sites span plans 02/03/05/06/07; removing
the shim before all migrations land would break the build. This plan is the phase build gate.
Output: two files deleted + de-registered; clean build/test green; human-verified no-regression.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-restyle-existing-screens-overview-donut/14-PATTERNS.md
@.planning/phases/14-restyle-existing-screens-overview-donut/14-RESEARCH.md
@.planning/phases/14-restyle-existing-screens-overview-donut/14-02-SUMMARY.md
@.planning/phases/14-restyle-existing-screens-overview-donut/14-03-SUMMARY.md
@.planning/phases/14-restyle-existing-screens-overview-donut/14-04-SUMMARY.md
@.planning/phases/14-restyle-existing-screens-overview-donut/14-05-SUMMARY.md
@.planning/phases/14-restyle-existing-screens-overview-donut/14-06-SUMMARY.md
@.planning/phases/14-restyle-existing-screens-overview-donut/14-07-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete CardStyle.swift + NeuTabBar.swift with pbxproj removal edits (D-02, D-03)</name>
  <files>MyHome.xcodeproj/project.pbxproj</files>
  <read_first>
    - .planning/phases/14-restyle-existing-screens-overview-donut/14-RESEARCH.md lines 480-499 (pbxproj discipline: 4 removal edits per file; project has NO synchronized groups)
    - .planning/phases/14-restyle-existing-screens-overview-donut/14-PATTERNS.md lines 723-734 (files-to-delete table + post-delete grep verification)
    - MyHome.xcodeproj/project.pbxproj (locate the PBXFileReference, PBXBuildFile, PBXSourcesBuildPhase, and PBXGroup entries for CardStyle.swift and NeuTabBar.swift)
  </read_first>
  <action>
    Confirm zero remaining call sites first: `grep -rn "cardStyle\|NeuTabBar" MyHomeApp/ --include="*.swift"` must show no live usage (comments acceptable). Delete the two files `MyHomeApp/Features/Shared/CardStyle.swift` and `MyHomeApp/DesignSystem/NeuTabBar.swift` from disk. For EACH file remove all 4 pbxproj entries in `MyHome.xcodeproj/project.pbxproj`: the PBXFileReference line, the PBXBuildFile line, the file's UUID from the PBXSourcesBuildPhase `files` array, and its UUID from the owning PBXGroup `children` array (8 removal edits total). Do NOT re-introduce a custom/floating tab bar (D-02). If any `.cardStyle(` call site is still found, STOP and report which plan left it un-migrated rather than deleting the shim.
  </action>
  <verify>
    <automated>test ! -f MyHomeApp/Features/Shared/CardStyle.swift && test ! -f MyHomeApp/DesignSystem/NeuTabBar.swift && grep -rn "cardStyle\|NeuTabBar" MyHomeApp/ --include="*.swift" | grep -v '//' | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - Both files are absent from disk (`test ! -f` passes for each).
    - `grep -rn "cardStyle\|NeuTabBar" MyHomeApp/ --include="*.swift" | grep -v '//' | wc -l` returns 0.
    - `grep -c "CardStyle.swift" MyHome.xcodeproj/project.pbxproj` returns 0 and `grep -c "NeuTabBar.swift" MyHome.xcodeproj/project.pbxproj` returns 0.
  </acceptance_criteria>
  <done>CardStyle.swift and NeuTabBar.swift deleted and fully de-registered from pbxproj; zero references remain.</done>
</task>

<task type="auto">
  <name>Task 2: Full clean build + test gate + app-wide stock-color assertion (SKIN-09)</name>
  <files>MyHome.xcodeproj/project.pbxproj</files>
  <read_first>
    - .planning/phases/14-restyle-existing-screens-overview-donut/14-VALIDATION.md (full suite command + sampling rate)
    - .planning/phases/14-restyle-existing-screens-overview-donut/14-RESEARCH.md lines 84-95 (SKIN-09 / build-clean success criteria)
  </read_first>
  <action>
    Run the full clean build + test suite. Then run an app-wide assertion that no stock system color or stray cardStyle remains across the feature views. If the color assertion finds residual occurrences, report the offending files (they indicate an incomplete restyle from an earlier plan) and treat as a gap rather than masking it. This task adds no new source; it is the phase build/quality gate.
  </action>
  <verify>
    <automated>xcodebuild clean build test -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `xcodebuild clean build test -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17'` succeeds with zero "cannot find type" errors and all tests passing.
    - `grep -rnE 'Color\(\.(secondarySystemBackground|systemBackground|systemBlue|systemGreen|systemRed|systemOrange|systemPurple|systemIndigo|systemTeal|systemPink|systemYellow|systemGray)\)|Color\.accentColor|\.cardStyle\(' MyHomeApp/Features/ | grep -v '//' | wc -l` returns 0 (app-wide SKIN-09 color gate).
  </acceptance_criteria>
  <done>Full clean build + tests green; zero stock system colors or cardStyle remain across Features/.</done>
</task>

<task type="checkpoint:human-action">
  <name>Task 3: End-of-phase human verification of no-regression + neumorphic look</name>
  <read_first>
    - .planning/phases/14-restyle-existing-screens-overview-donut/14-VALIDATION.md lines 56-63 (Manual-Only Verifications table)
    - .planning/phases/14-restyle-existing-screens-overview-donut/14-RESEARCH.md lines 680-696 (Navigation/Deep-Link Regression Surface)
  </read_first>
  <action>
    This is an end-of-phase human-verify (config: human_verify_mode=end-of-phase). The executor presents the build for manual verification on the iPhone 17 simulator; no further code is written in this task unless the human reports a defect.
  </action>
  <verify>
    <human-check>
      Launch on iPhone 17 simulator (dark mode) and confirm ALL of the following:
      1. Every screen group (Overview, Activity, Budgets, Notes/Calendar, Settings, Accounts, Assets, Transfer Inbox, Gmail Review) renders charcoal neumorphic surfaces + canary-yellow accent + luminous category icons — no stock blue/grey system look anywhere.
      2. Overview "Where it's going" donut shows top-4 + Others with all segments fully visible inside the card (no clipping); center total renders and animates.
      3. Tapping a donut legend row opens Activity pre-filtered to that category (OVR-06).
      4. NET CASH FLOW hero shows the income/spent split with the rolling total.
      5. No regression: add/edit/delete an expense; add/edit a note; confirm a self-transfer; trigger the Face ID gate (background/foreground) and confirm it still locks; verify a Notes deep-link still lands on the Notes tab; verify the native tab bar's selected tab is yellow.
      Type "approved" if all pass, or describe any defect (which screen, what looks wrong / what regressed).
    </human-check>
  </verify>
  <acceptance_criteria>
    - Human confirms all 5 verification groups pass (or all reported defects are resolved and re-verified).
  </acceptance_criteria>
  <done>Human has verified the full neumorphic restyle, the donut + tap-to-filter, and zero regression in v1.1 flows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Face ID lock gate | Verified end-to-end in the human-check that the gate still locks/unlocks after the restyle. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-14 | Tampering | pbxproj removal correctness | mitigate | Clean build gate fails if a build-phase UUID dangles after file deletion; build success proves the 8 removal edits are consistent. |
| T-14-15 | Elevation of Privilege | Face ID gate after RootView/Unlock changes | mitigate | Human-check explicitly exercises background→foreground lock; any regression blocks phase sign-off. |
| T-14-SC | Tampering | npm/pip/cargo installs | accept | No package installs; first-party Swift only. |
</threat_model>

<verification>
- Both deprecated files deleted + de-registered; zero residual references.
- Full clean build + test suite green.
- App-wide stock-color gate returns 0.
- Human verification of look + no-regression complete.
</verification>

<success_criteria>
- CardStyle + NeuTabBar removed (D-02, D-03); build clean (SKIN-09 success criterion 6).
- Zero stock system colors app-wide (SKIN-01..09 success criterion 1).
- Human-confirmed no regression in all v1.1 flows (SKIN-09).
</success_criteria>

<output>
Create `.planning/phases/14-restyle-existing-screens-overview-donut/14-08-SUMMARY.md` when done
</output>
