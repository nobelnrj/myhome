---
phase: 14-restyle-existing-screens-overview-donut
plan: 04
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - MyHomeApp/Features/Notes/NotesHomeView.swift
  - MyHomeApp/Features/Notes/NotesListView.swift
  - MyHomeApp/Features/Notes/NoteRow.swift
  - MyHomeApp/Features/Notes/AddNoteView.swift
  - MyHomeApp/Features/Notes/EditNoteView.swift
  - MyHomeApp/Features/Notes/CalendarView.swift
  - MyHomeApp/Features/Notes/ReminderEditView.swift
  - MyHomeApp/Features/Notes/RoutineDetailView.swift
autonomous: true
requirements: [SKIN-04, SKIN-09]
must_haves:
  truths:
    - "Notes, calendar, day-agenda, reminder, and routine screens render charcoal neumorphic surfaces with canary-yellow accents"
    - "Pinned note flags are orange; checked checklist/reminder states are canary yellow"
    - "No stock system color remains in any Notes file"
    - "kOpenNoteNotification deep-link to Notes still works (SKIN-09)"
  artifacts:
    - path: "MyHomeApp/Features/Notes/NoteRow.swift"
      provides: "Restyled note card row"
      contains: "DesignTokens"
  key_links:
    - from: "MyHomeApp/Features/Notes/RoutineDetailView.swift"
      to: "DesignTokens / NeuSurface"
      via: ".neuSurface migration of cardStyle line 122"
      pattern: "neuSurface"
---

<objective>
Restyle the Notes / calendar / day-agenda / reminder / routine screen group to the neumorphic
design system (SKIN-04): migrate the RoutineDetailView cardStyle site, swap all stock system
colors for DesignTokens, apply orange pin flags and canary checked-states per UI-SPEC. No
data/logic changes, and the `kOpenNoteNotification` deep-link must keep working (SKIN-09).

Purpose: self-contained screen group, parallel with other Wave-2 plans. Category palette from 14-01.
Output: fully restyled Notes group.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-restyle-existing-screens-overview-donut/14-PATTERNS.md
@.planning/phases/14-restyle-existing-screens-overview-donut/14-UI-SPEC.md
@.planning/phases/14-restyle-existing-screens-overview-donut/14-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle Notes home/list/row + editors (SKIN-04)</name>
  <files>MyHomeApp/Features/Notes/NotesHomeView.swift, MyHomeApp/Features/Notes/NotesListView.swift, MyHomeApp/Features/Notes/NoteRow.swift, MyHomeApp/Features/Notes/AddNoteView.swift, MyHomeApp/Features/Notes/EditNoteView.swift</files>
  <read_first>
    - MyHomeApp/Features/Notes/NotesHomeView.swift (self — systemBackground; segmented control + canvas bg)
    - MyHomeApp/Features/Notes/NoteRow.swift (self — Color.accentColor)
    - MyHomeApp/Features/Expenses/ExpenseRow.swift (analog for NoteRow card row layout — icon tile + label tiers)
    - .planning/phases/14-restyle-existing-screens-overview-donut/14-UI-SPEC.md lines 291-309 (Screen 4 component map: note card, pin flag, checklist/reminder states, editor overlay)
    - .planning/phases/14-restyle-existing-screens-overview-donut/14-PATTERNS.md lines 589-617 (label/accent/semantic replacement rules)
  </read_first>
  <action>
    NotesHomeView: replace `Color(.systemBackground)` -> `DesignTokens.bgCanvas` via canvas-background rule; segmented control uses `surfaceElevatedControl` track + `accent` active pill. NoteRow: note card -> `.neuSurface(.raised, isInteractive: true)`; pin flag -> `DesignTokens.orange` (16pt); title `label`; checklist checkbox `label3` unchecked / `accent` checked; done text `label3` strikethrough; preview body `label2`; date `label3`; `Color.accentColor`->accent. NotesListView/AddNoteView/EditNoteView: `.accentColor`->accent, label tiers per UI-SPEC, editor overlay bg `bgCanvas`, toolbar buttons `fillRecessed` bg + `accent` icon, reminder chip `accent` 12% bg. No logic/data changes; keep delete behavior (no confirmation) per Copywriting Contract.
  </action>
  <verify>
    <automated>grep -rnE 'Color\(\.(secondary|system|tertiary)|accentColor' MyHomeApp/Features/Notes/NotesHomeView.swift MyHomeApp/Features/Notes/NotesListView.swift MyHomeApp/Features/Notes/NoteRow.swift MyHomeApp/Features/Notes/AddNoteView.swift MyHomeApp/Features/Notes/EditNoteView.swift | grep -v '//' | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - The grep above returns 0 (no stock system color / accentColor in these files).
    - `grep -c 'neuSurface' MyHomeApp/Features/Notes/NoteRow.swift` returns >= 1.
    - `xcodebuild build -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' 2>&1 | tail -5` succeeds.
  </acceptance_criteria>
  <done>Notes home/list/row/editors neumorphic; pin flag orange, checked states canary; build green.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle calendar, reminder editor, routine detail (SKIN-04)</name>
  <files>MyHomeApp/Features/Notes/CalendarView.swift, MyHomeApp/Features/Notes/ReminderEditView.swift, MyHomeApp/Features/Notes/RoutineDetailView.swift</files>
  <read_first>
    - MyHomeApp/Features/Notes/RoutineDetailView.swift (self — cardStyle line 122; systemGreen)
    - MyHomeApp/Features/Notes/CalendarView.swift (self — Color.accentColor; selected-day dot)
    - MyHomeApp/Features/Notes/ReminderEditView.swift (self — accentColor, tertiarySystemFill)
    - .planning/phases/14-restyle-existing-screens-overview-donut/14-UI-SPEC.md lines 302-309 (reminder row, calendar card, day agenda, reminder sheet contracts)
    - .planning/phases/14-restyle-existing-screens-overview-donut/14-PATTERNS.md lines 492-497 (RoutineDetailView cardStyle migration)
  </read_first>
  <action>
    RoutineDetailView: migrate cardStyle line 122 -> `.neuSurface(.raised)`; `systemGreen`->positive (or accent for checked routine state per UI-SPEC). CalendarView: calendar card -> `.neuSurface(.raised, padding: nil)`; selected-day dot -> `DesignTokens.accent`; `Color.accentColor`->accent; day-agenda color bars use event `cat*` color or accent. ReminderEditView: reminder rows on `surfaceRaised`; active date value -> `accent`; overdue date -> `negative`; flagged -> `orange`; `tertiarySystemFill`->fillRecessed; Save CTA `accent`; Remove Reminder `negative`. Apply canvas background rule to any List/ScrollView. No logic/data changes.
  </action>
  <verify>
    <automated>grep -rnE 'Color\(\.(secondary|system|tertiary)|accentColor|\.cardStyle\(' MyHomeApp/Features/Notes/CalendarView.swift MyHomeApp/Features/Notes/ReminderEditView.swift MyHomeApp/Features/Notes/RoutineDetailView.swift | grep -v '//' | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - The grep above returns 0.
    - `grep -c 'neuSurface(.raised)' MyHomeApp/Features/Notes/RoutineDetailView.swift` returns >= 1.
    - `xcodebuild build -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' 2>&1 | tail -5` succeeds.
  </acceptance_criteria>
  <done>Calendar/reminder/routine screens neumorphic; cardStyle migrated; build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Presentational restyle only; reminder scheduling/notification logic unchanged. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-07 | Tampering | kOpenNoteNotification deep-link | mitigate | RootView deep-link observer is not edited in this plan; SKIN-09 acceptance confirms no Notes regression; manual deep-link check at phase gate. |
| T-14-SC | Tampering | npm/pip/cargo installs | accept | No package installs; first-party Swift only. |
</threat_model>

<verification>
- Zero stock system colors / cardStyle in Features/Notes/.
- Build green.
</verification>

<success_criteria>
- Notes/calendar/agenda/reminder/routine fully neumorphic (SKIN-04); deep-link + reminder flows unregressed (SKIN-09).
</success_criteria>

<output>
Create `.planning/phases/14-restyle-existing-screens-overview-donut/14-04-SUMMARY.md` when done
</output>
