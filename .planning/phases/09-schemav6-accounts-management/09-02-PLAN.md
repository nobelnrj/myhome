---
phase: 09-schemav6-accounts-management
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - MyHomeApp/Features/Settings/AccountsListView.swift
  - MyHomeApp/Features/Settings/EditAccountView.swift
  - MyHomeApp/Features/Settings/AccountDetailView.swift
  - MyHomeApp/Features/Settings/MigrationReviewSheet.swift
  - MyHomeApp/Features/Settings/SettingsView.swift
  - MyHomeTests/AccountCRUDTests.swift
  - MyHomeTests/AccountBalanceTests.swift
  - MyHomeTests/AccountTypeInferenceTests.swift
autonomous: false
requirements: [ACCT-01, ACCT-02, ACCT-03, ACCT-04, ACCT-05, ACCT-07]
user_setup: []
must_haves:
  truths:
    - "User can create, edit, and delete a bank account with name, type, color/icon, optional last-4 (ACCT-01/02/03)"
    - "User can set an account opening-balance baseline with an as-of date defaulting to today (ACCT-04)"
    - "AccountDetailView shows a live balance = baseline ± attributed transactions since as-of date, updating with no manual refresh (ACCT-05)"
    - "Credit-card balance displays negative (amount owed) in systemRed; positive savings/current in systemGreen (D-09)"
    - "Archiving an account hides it from the active list into a collapsed Archived section while its transactions remain (ACCT-07/D-08)"
    - "Auto-created accounts surface in a first-launch review sheet reachable from a badge on the Settings Accounts row (D-02)"
  artifacts:
    - path: "MyHomeApp/Features/Settings/AccountsListView.swift"
      provides: "Account CRUD list under Settings (active + collapsed archived)"
      contains: "struct AccountsListView"
    - path: "MyHomeApp/Features/Settings/EditAccountView.swift"
      provides: "Create/edit form: name, type, color/icon, balance baseline + as-of date"
      contains: "struct EditAccountView"
    - path: "MyHomeApp/Features/Settings/AccountDetailView.swift"
      provides: "Live computed balance headline + attributed expense list"
      contains: "struct AccountDetailView"
    - path: "MyHomeApp/Features/Settings/MigrationReviewSheet.swift"
      provides: "First-launch review of auto-created accounts (D-02)"
      contains: "struct MigrationReviewSheet"
    - path: "MyHomeTests/AccountBalanceTests.swift"
      provides: "Live-balance formula + credit-card negative sign tests (ACCT-05/D-09)"
      contains: "creditCardIsNegative"
  key_links:
    - from: "MyHomeApp/Features/Settings/SettingsView.swift"
      to: "AccountsListView"
      via: "NavigationLink in Data section"
      pattern: "NavigationLink.*AccountsListView"
    - from: "MyHomeApp/Features/Settings/AccountDetailView.swift"
      to: "Expense.accountID"
      via: "@Query filtered in-memory by accountID + date >= balanceAsOfDate"
      pattern: "accountID == account.id"
---

<objective>
Build the Accounts management surface under Settings (D-06): a CRUD list, a create/edit form, a per-account detail with a live computed balance, and the first-launch migration-review sheet. Covers ACCT-01/02/03/04/05/07 and D-02/D-08/D-09/D-10.

Purpose: This is the primary user-facing deliverable of Phase 9 — users manage their bank accounts, set reconciliation baselines, and see balances that update reactively from attributed transactions.
Output: AccountsListView, EditAccountView, AccountDetailView, MigrationReviewSheet, a SettingsView "Accounts" row with review badge, and unit tests for the balance formula, type inference, and archive behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-schemav6-accounts-management/09-CONTEXT.md
@.planning/phases/09-schemav6-accounts-management/09-PATTERNS.md
@.planning/phases/09-schemav6-accounts-management/09-UI-SPEC.md
@.planning/phases/09-schemav6-accounts-management/09-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AccountBalance + type-inference + archive unit tests, then AccountsListView CRUD</name>
  <files>MyHomeTests/AccountBalanceTests.swift, MyHomeTests/AccountTypeInferenceTests.swift, MyHomeTests/AccountCRUDTests.swift, MyHomeApp/Features/Settings/AccountsListView.swift</files>
  <read_first>
    - MyHomeApp/Features/Budgets/ManageCategoriesView.swift — the direct CRUD analog: @Query + @Environment(\.modelContext), List(.insetGrouped), swipe actions, confirmationDialog delete, `min(existing.sortOrder) - 1` prepend insertion, lookup-before-insert (case-insensitive) uniqueness, explicit `try context.save()` (CR-01)
    - MyHomeTests/CategoryCRUDTests.swift — in-memory ModelContainer test setup; `@Test` / `#expect` patterns to mirror
    - MyHomeApp/Features/Shared/IconTile.swift — IconTile(symbol:color:size:) signature for account row icons
    - MyHomeApp/Persistence/Models/Account.swift — the typealias and SchemaV6.Account field names/initializer (from Plan 01)
    - .planning/phases/09-schemav6-accounts-management/09-PATTERNS.md — the AccountsListView.swift section (imports/@Query/split active-vs-archived; NavigationStack+List; addAccount/deleteAccount; accountRowLabel/IconTile)
    - .planning/phases/09-schemav6-accounts-management/09-UI-SPEC.md — Screen 1 (AccountsListView layout, AccountRowView anatomy spacing 16/0, empty state, archived DisclosureGroup, swipe actions, balance color contract)
    - .planning/phases/09-schemav6-accounts-management/09-RESEARCH.md — Critical Pattern 4 (live balance formula) and Critical Pattern 7 (Account CRUD)
  </read_first>
  <behavior>
    - AccountBalanceTests `liveBalanceEqualsBaselinePlusNet`: an account with balanceBaseline 1000 and balanceAsOfDate D, plus attributed expenses (amount -200 dated after D, amount -50 dated after D, amount -999 dated BEFORE D) yields a computed balance of 1000 + (-200) + (-50) = 750 (the pre-baseline -999 is excluded).
    - AccountBalanceTests `creditCardIsNegative`: a credit_card account with balanceBaseline -5000 (amount owed) and an attributed expense amount -1000 after the as-of date yields -6000 (more negative as you spend, D-09).
    - AccountBalanceTests `nilBaselineYieldsZero`: account with nil balanceBaseline yields balance 0.
    - AccountTypeInferenceTests `inference`: "HDFC CC" → credit_card; "My Credit One" → credit_card; "Platinum Card" → credit_card; "ICICI Savings" → savings; "Salary" → savings (case-insensitive).
    - AccountCRUDTests `archiveHidesFromActive`: creating two accounts then setting one isArchived = true leaves exactly one account in the `!isArchived` filtered set and one in the `isArchived` set; both still exist in the store.
    - AccountCRUDTests `duplicateNameRejected`: inserting an account named "HDFC" then attempting a case-insensitive duplicate "hdfc" via the add helper does not create a second row.
    - The balance formula and type-inference logic must be testable in isolation — extract them into pure free functions or static helpers (e.g. `AccountBalance.compute(baseline:asOf:expenses:accountID:)` and a reusable `inferAccountType`) so tests do not need a SwiftUI view.
  </behavior>
  <action>
    First write the three test files (RED). Put the balance formula in a pure, testable location: create `AccountBalance.compute(baseline: Decimal?, asOf: Date?, expenses: [Expense], accountID: UUID) -> Decimal` returning 0 when baseline or asOf is nil, else `baseline + sum(expense.amount for expense where expense.accountID == accountID and expense.date >= asOf)`. The sign convention lives in the data (D-09), so one formula serves all account types. For type inference, expose a testable helper matching Plan 01's rule ("cc"/"credit"/"card" substring, case-insensitive → "credit_card" else "savings").

    Then create `AccountsListView` mirroring ManageCategoriesView: `@Environment(\.modelContext)`, `@Query(sort: \Account.sortOrder) private var allAccounts`, in-memory split `activeAccounts = allAccounts.filter { !$0.isArchived }` and `archivedAccounts = allAccounts.filter { $0.isArchived }`. List(.insetGrouped) with an Active section (ForEach activeAccounts → NavigationLink to AccountDetailView, `.swipeActions(edge: .trailing)` for Delete (confirmationDialog, role .destructive) and Archive (sets isArchived = true + save, tint orange)) and a collapsed `DisclosureGroup("Archived (\(archivedAccounts.count))")` section (rows `.opacity(0.5)`, swipe Unarchive sets isArchived = false). Empty state: `ContentUnavailableView("No Accounts Yet", systemImage: "building.columns", description: Text("Tap + to add your first bank account."))`. Toolbar `.primaryAction` "+" button (`.accessibilityLabel("Add Account")`) presenting `EditAccountView(account: nil)` as a sheet. Account row: `IconTile(symbol: account.symbolName ?? "creditcard", color: Color(hex: account.colorHex ?? "#636366"), size: 30)` + name (`.body`, plain `Text`, T-02-15) + type label (`.subheadline` `.secondary`) + trailing balance text colored per D-09 (systemRed for CC/negative, systemGreen for positive, .primary for zero) + chevron. HStack spacing 16, name/type VStack spacing 0 (per UI-SPEC revision). `addAccount` uses lookup-before-insert (case-insensitive) and `(all.map(\.sortOrder).min() ?? 0) - 1` prepend; `deleteAccount` uses `context.delete` + `try context.save()`. Pin the migration-review banner Section at top when `UserDefaults.standard.bool(forKey: "accountReviewPending")` is true (Review Now button presents MigrationReviewSheet, built in Task 3).

    Wire the row balance text to `AccountBalance.compute(...)` against a lightweight `@Query` of all expenses, filtered in-memory by accountID.
  </action>
  <verify>
    <automated>xcodebuild test -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:MyHomeTests/AccountBalanceTests -only-testing:MyHomeTests/AccountTypeInferenceTests -only-testing:MyHomeTests/AccountCRUDTests 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `AccountBalanceTests`, `AccountTypeInferenceTests`, `AccountCRUDTests` all exist and pass (TEST SUCCEEDED).
    - `AccountBalance.compute` returns 750 for the baseline-1000 case, -6000 for the credit-card case, and 0 for nil baseline.
    - `AccountsListView` declares `@Query(sort: \Account.sortOrder)` and an `!isArchived` / `isArchived` in-memory split.
    - Delete uses a `confirmationDialog`; Archive sets `isArchived = true` with an explicit `try context.save()`.
    - Account name rendered via plain `Text(account.name ?? "")` (grep: no `AttributedString(markdown` in the file).
    - Empty state uses `ContentUnavailableView` with copy "No Accounts Yet".
  </acceptance_criteria>
  <done>Account CRUD list works with archive/unarchive and prepend insertion order; the balance formula, type inference, and archive behavior are proven by passing unit tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: EditAccountView (create/edit form) + AccountDetailView (live balance + transactions)</name>
  <files>MyHomeApp/Features/Settings/EditAccountView.swift, MyHomeApp/Features/Settings/AccountDetailView.swift</files>
  <read_first>
    - MyHomeApp/Features/Expenses/AddExpenseView.swift — NavigationStack-in-sheet Form structure; Cancel/Save toolbar (`.cancellationAction`/`.confirmationAction`, `.tint(.accentColor)`); the `abs(amount) < 1_000_000_000` validation guard (T-01-03); `formattedINR()` usage
    - MyHomeApp/Features/Expenses/ExpenseListView.swift — @Query expenses, per-day Section grouping with day totals, ExpenseRow usage; the in-memory filter pattern
    - MyHomeApp/Features/Shared/CardStyle.swift — `.cardStyle()` for the balance card; MyHomeApp/Features/Shared/IconTile.swift
    - MyHomeApp/Features/Budgets/ManageCategoriesView.swift — the symbol + color picker rows to mirror for ACCT-03
    - .planning/phases/09-schemav6-accounts-management/09-PATTERNS.md — the EditAccountView.swift and AccountDetailView.swift sections (Form layout, saveAccount, @Query + computed liveBalance, sheet edit)
    - .planning/phases/09-schemav6-accounts-management/09-UI-SPEC.md — Screen 2 (EditAccountView sections, segmented type picker, CC "Amount Owed" vs "Opening Balance" label, as-of DatePicker default today, icon+color picker palette of 12 swatches + 20 SF Symbols, inline `.subheadline` systemRed errors) and Screen 3 (AccountDetailView balance card `.largeTitle.weight(.semibold)`, "as of" subheadline, attributed expense list, empty state, no manual refresh)
  </read_first>
  <behavior>
    - EditAccountView validation: name trimmed-empty is invalid; `abs(balanceBaseline) >= 1_000_000_000` is invalid (error "Balance must be less than ₹1,00,00,00,000."); valid name + in-range balance enables Save.
    - AccountDetailView's `liveBalance` computed property equals `AccountBalance.compute(baseline:asOf:expenses:accountID:)` from Task 1 (reuse, do not re-derive).
  </behavior>
  <action>
    Create `EditAccountView(account: Account?)` (nil = create). NavigationStack-in-sheet with a Form (.insetGrouped). @State mirrors: name, typeRaw ("savings" default), symbolName ("creditcard" default), colorHex ("#636366" default), last4, balanceBaseline (Decimal), balanceAsOfDate (Date() default — D-10), nameError. In edit mode seed @State from the passed account.

    Sections: (1) Identity — tappable icon+color row showing `IconTile(symbol:color:size:44)` + "Icon & Color" label; Name TextField; Last-4 TextField (`.keyboardType(.numberPad)`, optional). (2) Type — `Picker(selection: $typeRaw).pickerStyle(.segmented)` tags "savings"/"current"/"credit_card", labels "Savings"/"Current"/"Credit Card" (ACCT-02). (3) Balance — label `typeRaw == "credit_card" ? "Amount Owed" : "Opening Balance"` (D-09) with a trailing decimalPad TextField bound to balanceBaseline (CC value tinted systemRed); `DatePicker("As of", selection: $balanceAsOfDate, displayedComponents: [.date])` (D-10). (4) Edit-mode-only danger row: "Archive Account" button (systemOrange) setting isArchived = true + dismiss.

    Icon+color picker (ACCT-03, Claude's discretion to inline-section or sub-sheet): a scrollable SF Symbol grid (the 20-symbol set from UI-SPEC: building.columns, creditcard, banknote, wallet.pass, dollarsign.circle, house, car, briefcase, stethoscope, cart, fork.knife, bolt, fuelpump, bag, graduationcap, airplane, gift, heart, tag, star) and a 12-swatch color grid (the systemBlue…systemMint palette from UI-SPEC) storing `colorHex`; selected swatch shows a white checkmark; selections apply on tap.

    Toolbar: Cancel (`.cancellationAction`, dismiss no-save) and "Save Account" (`.confirmationAction`, `.tint(.accentColor)`, disabled until valid). `saveAccount()`: trim name, guard non-empty (error "Account name cannot be empty."), guard `abs(balanceBaseline) < 1_000_000_000` (T-01-03; error "Balance must be less than ₹1,00,00,00,000."), write all fields to `account ?? Account(name:)`, `context.insert` if creating, `try context.save()` (CR-01), dismiss. Inline errors render `.subheadline` in systemRed.

    Create `AccountDetailView(account: Account)`. `@Query(sort: \Expense.date, order: .reverse) private var allExpenses`; `attributedExpenses = allExpenses.filter { $0.accountID == account.id }`; `liveBalance = AccountBalance.compute(baseline: account.balanceBaseline, asOf: account.balanceAsOfDate, expenses: allExpenses, accountID: account.id)` (computed, NEVER stored — reactive via @Query, D-10/ACCT-05). Layout: a `.cardStyle()` balance card with type label (`.subheadline` `.secondary`), `Text(liveBalance.formattedINR()).font(.largeTitle.weight(.semibold))` colored per D-09 (systemRed CC/negative, systemGreen positive, .primary zero), and an "as of \(date)" `.subheadline` `.secondary` row; below it the attributed expenses grouped per day with day totals using ExpenseRow (mirror ExpenseListView). Empty state: `ContentUnavailableView("No Expenses", systemImage: "tray", description: Text("Expenses attributed to this account will appear here."))`. Toolbar trailing "Edit" presents `EditAccountView(account: account)` as a sheet. Do NOT add any refresh/pull-to-refresh affordance (D-10 contract).
  </action>
  <verify>
    <automated>xcodebuild build -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' 2>&1 | tail -10 && grep -q "struct EditAccountView" MyHomeApp/Features/Settings/EditAccountView.swift && grep -q "struct AccountDetailView" MyHomeApp/Features/Settings/AccountDetailView.swift && grep -q "largeTitle" MyHomeApp/Features/Settings/AccountDetailView.swift</automated>
  </verify>
  <acceptance_criteria>
    - Project builds for iPhone 17 simulator (BUILD SUCCEEDED).
    - `EditAccountView` has the segmented type Picker with tags "savings"/"current"/"credit_card", the CC-vs-savings balance label switch, the as-of DatePicker defaulting to today, and the `abs(balanceBaseline) < 1_000_000_000` guard.
    - `saveAccount()` ends with `try context.save()` and dismisses; no `@Attribute(.unique)` reliance for uniqueness (uses lookup-before-insert).
    - `AccountDetailView.liveBalance` calls `AccountBalance.compute(...)` (no stored balance property on the view or model).
    - Balance amount uses `.largeTitle.weight(.semibold)` (not `.bold`); no refresh affordance present.
    - Both views render account name and amounts via plain `Text` (no `AttributedString(markdown`).
  </acceptance_criteria>
  <done>Users can create/edit an account with type, color/icon, and a baseline + as-of date; the detail view shows a reactive computed balance with the correct D-09 sign and attributed transactions.</done>
</task>

<task type="auto">
  <name>Task 3: SettingsView Accounts row (with review badge) + MigrationReviewSheet (D-02)</name>
  <files>MyHomeApp/Features/Settings/SettingsView.swift, MyHomeApp/Features/Settings/MigrationReviewSheet.swift</files>
  <read_first>
    - MyHomeApp/Features/Settings/SettingsView.swift — the existing "Data" Section and its `NavigationLink` rows (e.g. Manage Categories), the `rowLabel(_:symbol:color:)` helper, the existing @State pattern, and the tertiary chevron convention
    - MyHomeApp/Features/Budgets/ManageCategoriesView.swift — the inline-rename TextField pattern and confirmationDialog delete to mirror in the review sheet
    - MyHomeApp/Persistence/Models/Account.swift — Account fields (sourceLabel, name, typeRaw) used in the review list
    - .planning/phases/09-schemav6-accounts-management/09-UI-SPEC.md — Screen 5 (Migration Review Sheet: Settings-row badge dot systemOrange, pinned review Section in AccountsListView, MigrationReviewSheet inline rename + segmented type + swipe delete, "Done" clears the flag, footer copy) and the Copywriting Contract rows
    - .planning/phases/09-schemav6-accounts-management/09-CONTEXT.md — D-02 (editable review of auto-created accounts; idempotent migration)
  </read_first>
  <action>
    Edit `SettingsView`: in the "Data" Section, add a `NavigationLink(destination: AccountsListView())` row labelled "Accounts" with symbol "creditcard" and `Color(.systemBlue)` (reuse the existing `rowLabel` helper). When `accountReviewPending` is true, show an `Image(systemName: "exclamationmark.circle.fill")` in `Color(.systemOrange)` before the chevron (D-02 badge). Add `@State private var accountReviewPending: Bool = UserDefaults.standard.bool(forKey: "accountReviewPending")`. Keep the existing tertiary chevron convention.

    Create `MigrationReviewSheet` (NavigationStack-in-sheet, title "Review Accounts" inline). `@Query private var allAccounts: [Account]`; show only auto-created accounts (those with non-nil `sourceLabel`). List(.insetGrouped) with one row per account: inline-editable name TextField (mirror ManageCategoriesView rename), a segmented type Picker bound to the account's typeRaw ("Savings"/"Current"/"Credit Card"), and swipe-to-delete with a `confirmationDialog` (message "All expenses attributed to this account will become unassigned. This cannot be undone."). Section footer: "Changes here are permanent. Tap Done when finished." Toolbar trailing `Button("Done")` (`.confirmationAction`) that `try? context.save()`, sets `UserDefaults.standard.set(false, forKey: "accountReviewPending")`, and dismisses. Persist edits with explicit `try context.save()` (CR-01).

    Wire the AccountsListView pinned review banner (from Task 1) "Review Now" button to present `MigrationReviewSheet` as a `.sheet`.
  </action>
  <verify>
    <automated>xcodebuild build -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' 2>&1 | tail -10 && grep -q "AccountsListView()" MyHomeApp/Features/Settings/SettingsView.swift && grep -q "accountReviewPending" MyHomeApp/Features/Settings/SettingsView.swift && grep -q "struct MigrationReviewSheet" MyHomeApp/Features/Settings/MigrationReviewSheet.swift</automated>
  </verify>
  <acceptance_criteria>
    - Project builds for iPhone 17 simulator (BUILD SUCCEEDED).
    - `SettingsView` "Data" Section contains `NavigationLink(destination: AccountsListView())` labelled "Accounts".
    - The review badge (`exclamationmark.circle.fill`, systemOrange) shows only when `accountReviewPending` is true.
    - `MigrationReviewSheet` lists only accounts with non-nil `sourceLabel`, supports inline rename + segmented type change + swipe delete with confirmationDialog.
    - "Done" sets `accountReviewPending` UserDefaults flag to false and saves.
  </acceptance_criteria>
  <done>Settings exposes Accounts management with a one-time review badge; auto-created accounts can be renamed/retyped/deleted before being finalized, then the badge clears.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Full Accounts management under Settings: CRUD list with archive, create/edit form with color/icon + baseline, per-account detail with a reactive live balance, and the first-launch migration review sheet. Unit tests for the balance formula, type inference, and archive behavior are green.</what-built>
  <how-to-verify>
    1. Build and run on the iPhone 17 simulator (Xcode 26.5, scheme MyHome). If you have a pre-V6 store, confirm the app launches without crashing (migration ran in Plan 01).
    2. Settings > Accounts. If a migration review badge (orange dot) is present, tap it, rename/retype one auto-created account, tap Done — confirm the badge clears.
    3. Tap "+", create a Savings account "Test Savings" with a color/icon and Opening Balance 1000 (as-of today). Save. Create a Credit Card account "Test CC" with Amount Owed 5000 — confirm the field tints red and the label reads "Amount Owed".
    4. Tap "Test Savings" → confirm the balance card shows ₹1,000 in green. Add an expense attributed to "Test Savings" (via the expense add flow from Plan 03 if available, or note this is verified jointly) and confirm the balance updates with NO refresh button.
    5. Swipe-archive "Test CC" → confirm it moves into the collapsed "Archived" section and leaves the active list. Expand Archived and confirm it is still listed with its transactions intact.
    6. Swipe-delete an account → confirm the confirmationDialog appears with the "become unassigned" message.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any layout/behavior issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User form input → Account model | Account name, last-4, balance baseline are user-entered |
| sourceLabel-derived account name → SwiftUI Text | Auto-created account names rendered in views |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05 | Tampering | Unreasonably large balanceBaseline crashing Decimal arithmetic | mitigate | `abs(balanceBaseline) < 1_000_000_000` guard in EditAccountView.saveAccount (mirrors T-01-03); error surfaced inline |
| T-09-06 | Tampering | Account name rendered with markdown injection | mitigate | All account name display uses plain `Text(account.name ?? "")` (T-02-15); acceptance grep forbids `AttributedString(markdown` |
| T-09-07 | Information Disclosure | last-4 digits over-collection | accept | last-4 is optional, user-entered, stored locally only; single-user local app, no sync/telemetry (v1.1 constraint) |
| T-09-08 | Spoofing | Duplicate account names confusing attribution | mitigate | Case-insensitive lookup-before-insert in addAccount rejects duplicates |
| T-09-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs — first-party Apple frameworks only (RESEARCH Package Legitimacy Audit); no checkpoint needed |
</threat_model>

<verification>
- `xcodebuild test ... -only-testing:MyHomeTests/AccountBalanceTests -only-testing:MyHomeTests/AccountTypeInferenceTests -only-testing:MyHomeTests/AccountCRUDTests` — all green.
- `xcodebuild build -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17'` — BUILD SUCCEEDED.
- Human verify checkpoint passes (CRUD, archive, live balance, review sheet).
</verification>

<success_criteria>
- Account CRUD with archive works under Settings (ACCT-01/02/03/07, D-06/D-08).
- Baseline + as-of date settable; live balance reactive and correctly signed (ACCT-04/05, D-09/D-10).
- First-launch review surfaces and finalizes auto-created accounts (D-02).
</success_criteria>

<output>
Create `.planning/phases/09-schemav6-accounts-management/09-02-SUMMARY.md` when done
</output>
