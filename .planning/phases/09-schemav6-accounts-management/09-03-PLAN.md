---
phase: 09-schemav6-accounts-management
plan: 03
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - MyHomeApp/Features/Expenses/AccountPickerView.swift
  - MyHomeApp/Features/Expenses/AddExpenseView.swift
  - MyHomeApp/Features/Expenses/EditExpenseView.swift
  - MyHomeApp/Features/Expenses/ExpenseListView.swift
  - MyHomeApp/Features/Gmail/GmailSyncController.swift
  - MyHomeTests/GmailAccountAttributionTests.swift
autonomous: false
requirements: [ACCT-06]
user_setup: []
must_haves:
  truths:
    - "Manual expense add/edit has an optional account picker defaulting to the last-used account; blank = Unassigned (D-04)"
    - "The account picker shows only active (non-archived) accounts plus an Unassigned/None option (D-08/Pitfall 6)"
    - "The main expense list has an account filter (All / Unassigned / specific account) that narrows the visible expenses (ACCT-06/D-07)"
    - "Gmail-ingested expenses are auto-attributed to the account whose sourceLabel/name matches; no match → Unassigned (D-05)"
    - "Account attribution lookup in Gmail sync uses pre-loop UUID capture, not @Model refs held across await (STAB-02)"
  artifacts:
    - path: "MyHomeApp/Features/Expenses/AccountPickerView.swift"
      provides: "Optional account picker sheet mirroring CategoryPickerView"
      contains: "struct AccountPickerView"
    - path: "MyHomeTests/GmailAccountAttributionTests.swift"
      provides: "Test that sourceLabel→Account auto-attribution sets accountID (D-05)"
      contains: "func"
  key_links:
    - from: "MyHomeApp/Features/Expenses/AddExpenseView.swift"
      to: "AccountPickerView"
      via: "sheet presenting AccountPickerView bound to selectedAccount"
      pattern: "AccountPickerView"
    - from: "MyHomeApp/Features/Gmail/GmailSyncController.swift"
      to: "Expense.accountID"
      via: "sourceLabel → accountIDsByLabel pre-loop UUID map"
      pattern: "accountID ="
---

<objective>
Wire account attribution into the expense surfaces: an optional account picker in the add/edit expense flow (D-04), an account filter on the main expense list (ACCT-06/D-07), and automatic attribution of newly Gmail-ingested expenses by sourceLabel (D-05).

Purpose: ACCT-06 (per-account spend) is reachable two ways — the account-detail screen (Plan 02) and a filter on the main list (this plan). D-04/D-05 make attribution low-friction: manual quick-add remembers the last account, and Gmail ingestion auto-attributes without user action.
Output: AccountPickerView, account-picker rows in Add/Edit expense, an AccountFilter on ExpenseListView, the Gmail auto-attribution hook, and a test proving sourceLabel→accountID attribution.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-schemav6-accounts-management/09-CONTEXT.md
@.planning/phases/09-schemav6-accounts-management/09-PATTERNS.md
@.planning/phases/09-schemav6-accounts-management/09-UI-SPEC.md
@.planning/phases/09-schemav6-accounts-management/09-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: AccountPickerView + Add/Edit expense account picker rows (D-04)</name>
  <files>MyHomeApp/Features/Expenses/AccountPickerView.swift, MyHomeApp/Features/Expenses/AddExpenseView.swift, MyHomeApp/Features/Expenses/EditExpenseView.swift</files>
  <read_first>
    - MyHomeApp/Features/Expenses/CategoryPickerView.swift — the direct template: @Binding selection, "None" row, ForEach rows with checkmark, Cancel/Clear toolbar (`.cancellationAction` "Cancel" line 83, `.destructiveAction` "Clear" tint systemRed), `.frame(minHeight: 44)`, NavigationStack-in-sheet
    - MyHomeApp/Features/Expenses/AddExpenseView.swift — the optional Category row pattern (selectedCategory @State, the Button row presenting CategoryPickerView, the saveExpense() category assignment); the Section layout for optional fields
    - MyHomeApp/Features/Expenses/EditExpenseView.swift — its category row + isDirty/local @State mirror pattern
    - MyHomeApp/Persistence/Models/Account.swift — Account fields (id, name, symbolName, isArchived)
    - .planning/phases/09-schemav6-accounts-management/09-PATTERNS.md — the AccountPickerView.swift section (full mirror), the AddExpenseView/EditExpenseView account picker @State + last-used UserDefaults + saveExpense accountID assignment
    - .planning/phases/09-schemav6-accounts-management/09-UI-SPEC.md — Screen 4 (AccountPickerView: "Unassigned" top row, active accounts only, checkmark selection, Cancel/Clear toolbar; integration row in Add/Edit "Account" → "Unassigned"); Copywriting Contract
  </read_first>
  <action>
    Create `AccountPickerView` mirroring CategoryPickerView almost verbatim: `@Binding var selectedAccount: Account?`, `@Environment(\.dismiss)`, `@Query(filter: #Predicate<Account> { !$0.isArchived }, sort: \Account.sortOrder) private var activeAccounts` (archived excluded — D-08/Pitfall 6). NavigationStack-in-sheet, List(.insetGrouped). Top "None"/"Unassigned" row (button sets selectedAccount = nil + dismiss, checkmark when selectedAccount == nil). ForEach activeAccounts rows: IconTile/symbol + plain `Text(account.name ?? "")` (T-02-15) + checkmark when `selectedAccount?.persistentModelID == account.persistentModelID`. Title "Account" inline. Toolbar: "Cancel" (`.cancellationAction`) and "Clear" (`.destructiveAction`, `.tint(Color(.systemRed))`, shown only when selectedAccount != nil).

    In `AddExpenseView`: add `@State private var selectedAccount: Account? = nil` and `@State private var showAccountPicker = false`. In the optional fields section, below the Category row, add an "Account" Button row showing `selectedAccount?.name ?? "Unassigned"` (`.subheadline` `.secondary`) + chevron, `.frame(minHeight: 44)`, `.buttonStyle(.plain)`, presenting `AccountPickerView(selectedAccount: $selectedAccount)` as a `.sheet`. On appear, default `selectedAccount` from `UserDefaults.standard.string(forKey: "lastUsedAccountID")` resolved against the account @Query results (D-04); if that account is archived or missing, leave nil. In `saveExpense()`, set `expense.accountID = selectedAccount?.id`; if `selectedAccount?.isArchived == true` set `accountID = nil` (Pitfall 6); after a successful save, if `selectedAccount` non-nil write `UserDefaults.standard.set(selectedAccount.id.uuidString, forKey: "lastUsedAccountID")`.

    In `EditExpenseView`: mirror the same Account row and picker, seeding `selectedAccount` from the existing `expense.accountID` (resolve against the account @Query). Respect the existing local-@State-mirror / isDirty pattern — selecting a different account marks the form dirty; Save writes `expense.accountID`. Do NOT touch `sourceAccount` or `sourceLabel`.
  </action>
  <verify>
    <automated>xcodebuild build -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' 2>&1 | tail -10 && grep -q "struct AccountPickerView" MyHomeApp/Features/Expenses/AccountPickerView.swift && grep -q "lastUsedAccountID" MyHomeApp/Features/Expenses/AddExpenseView.swift && grep -q "AccountPickerView" MyHomeApp/Features/Expenses/EditExpenseView.swift</automated>
  </verify>
  <acceptance_criteria>
    - Project builds for iPhone 17 simulator (BUILD SUCCEEDED).
    - `AccountPickerView` filters with `!$0.isArchived` and offers an Unassigned/None option; Clear toolbar item is conditional on a non-nil selection.
    - AddExpenseView writes `expense.accountID = selectedAccount?.id`, nullifies if archived, and persists `lastUsedAccountID` on save (grep: `lastUsedAccountID`).
    - EditExpenseView seeds `selectedAccount` from `expense.accountID` and writes it back on save without touching `sourceAccount`/`sourceLabel`.
    - Account names rendered via plain `Text` (no `AttributedString(markdown`).
  </acceptance_criteria>
  <done>Manual expense add/edit has a low-friction optional account picker that defaults to the last-used active account and excludes archived accounts.</done>
</task>

<task type="auto">
  <name>Task 2: ExpenseListView account filter (ACCT-06/D-07)</name>
  <files>MyHomeApp/Features/Expenses/ExpenseListView.swift</files>
  <read_first>
    - MyHomeApp/Features/Expenses/ExpenseListView.swift — the existing `CategoryFilter` enum, the `@State private var categoryFilter`, the `filteredExpenses` computed property (where category filtering happens), and the toolbar filter `Menu` with its filled-icon active indicator
    - MyHomeApp/Persistence/Models/Account.swift — Account fields for the filter menu (id, name, isArchived)
    - .planning/phases/09-schemav6-accounts-management/09-PATTERNS.md — the ExpenseListView.swift section (AccountFilter enum mirroring CategoryFilter; @State accountFilter; chaining the account filter after the category filter in filteredExpenses; filterMenu extension)
    - .planning/phases/09-schemav6-accounts-management/09-UI-SPEC.md — Screen 6 (Account filter: single Menu with Category + Account Picker sections, AccountFilter enum cases/labels/icons, filled-icon indicator when any filter active, empty-state label)
  </read_first>
  <action>
    Add `private enum AccountFilter: Hashable { case all; case unassigned; case account(UUID) }` to ExpenseListView (mirror CategoryFilter). Add `@State private var accountFilter: AccountFilter = .all` and a `@Query(filter: #Predicate<Account> { !$0.isArchived }, sort: \Account.sortOrder) private var activeAccounts` for the menu options.

    In `filteredExpenses`, after the existing category filtering, apply the account filter in-memory: `.all` returns the category-filtered set unchanged; `.unassigned` keeps `expense.accountID == nil`; `.account(let id)` keeps `expense.accountID == id`. Do not re-architect the existing category filter — chain after it.

    In the toolbar filter Menu, add an Account picker section (or a second Picker) with rows: "All Accounts" (systemImage "building.columns", tag .all), "Unassigned" (systemImage "questionmark.circle", tag .unassigned), then one row per active account (tag `.account(account.id)`). Make the toolbar filter icon use its `.fill` variant when `accountFilter != .all` OR the category filter is active (same indicator pattern already present). Update the filtered empty-state copy to include the active filter label (e.g. "No expenses for [account]."), reusing the existing empty-state mechanism.
  </action>
  <verify>
    <automated>xcodebuild build -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' 2>&1 | tail -10 && grep -q "enum AccountFilter" MyHomeApp/Features/Expenses/ExpenseListView.swift && grep -q "accountFilter" MyHomeApp/Features/Expenses/ExpenseListView.swift && grep -q "case account(UUID)" MyHomeApp/Features/Expenses/ExpenseListView.swift</automated>
  </verify>
  <acceptance_criteria>
    - Project builds for iPhone 17 simulator (BUILD SUCCEEDED).
    - `AccountFilter` enum has `.all`, `.unassigned`, `.account(UUID)` cases.
    - `filteredExpenses` applies the account filter in addition to (chained after) the existing category filter.
    - The filter Menu lists All Accounts / Unassigned / each active account; archived accounts excluded.
    - The toolbar filter icon reflects an active account filter via the `.fill` variant.
  </acceptance_criteria>
  <done>The main expense list can be filtered to a single account or Unassigned, providing the second ACCT-06 entry point alongside the Plan 02 account detail.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Gmail auto-attribution by sourceLabel (D-05) + test</name>
  <files>MyHomeApp/Features/Gmail/GmailSyncController.swift, MyHomeTests/GmailAccountAttributionTests.swift</files>
  <read_first>
    - MyHomeApp/Features/Gmail/GmailSyncController.swift — the `syncAccount` method; the `categoryIDsByName` pre-loop `[String: PersistentIdentifier]` capture pattern (the STAB-02 fix); the per-message expense-creation site where `sourceLabel`/`sourceAccount` are set; the single batched `ctx.save()` after the loop (STAB-02)
    - MyHomeApp/Persistence/Models/Account.swift — Account fields (id, name, sourceLabel, isArchived)
    - .planning/phases/09-schemav6-accounts-management/09-PATTERNS.md — the GmailSyncController.swift section (pre-loop `accountIDsByLabel: [String: UUID]` capture indexed by sourceLabel AND lowercased name; per-message `expense.accountID = accountIDsByLabel[label] ?? accountIDsByLabel[label.lowercased()]`)
    - .planning/phases/09-schemav6-accounts-management/09-RESEARCH.md — Critical Pattern 6 (auto-attribution) and Pitfall 4 (do not break the category re-fetch pattern; capture account IDs before the loop, never hold @Model refs across await)
  </read_first>
  <behavior>
    - GmailAccountAttributionTests `attributesBySourceLabel`: given an in-memory store with an Account whose sourceLabel == "HDFC CC", a function/helper that maps a parsed sourceLabel to an accountID returns that account's id for input "HDFC CC" and nil for input "Unknown Bank". (Extract the matching logic — building `accountIDsByLabel` and resolving a label — into a testable pure helper so the test does not need the full Gmail network path.)
    - GmailAccountAttributionTests `archivedAccountNotMatched`: an archived account with sourceLabel "Old Bank" is NOT returned by the resolver (excluded like the picker, Pitfall 6).
  </behavior>
  <action>
    Extract the attribution-matching logic into a testable helper: a pure function that takes the fetched `[Account]` and builds `accountIDsByLabel: [String: UUID]` from non-archived accounts (index each by its `sourceLabel` and by `name.lowercased()`), plus a resolver `accountID(forSourceLabel:) -> UUID?` doing `map[label] ?? map[label.lowercased()]`. Write `GmailAccountAttributionTests` against this helper first (RED).

    In `GmailSyncController.syncAccount`: BEFORE the per-message loop, fetch accounts once and build `accountIDsByLabel` via the helper (mirror the existing `categoryIDsByName` pre-loop capture — never hold `@Model` refs across the `await` boundary; store plain `UUID`s, STAB-02/Pitfall 4). After the per-message expense is created and `sourceLabel`/`sourceAccount` are set, add `if let label = parsed.<rawSourceLabel field> { expense.accountID = accountIDsByLabel[label] ?? accountIDsByLabel[label.lowercased()] }` (no match → accountID stays nil = Unassigned, D-05). Use the actual parsed-source-label field name found in the existing code — do NOT introduce a new field. Do NOT add a `ctx.save()` inside the loop (preserve the STAB-02 single batched save after the loop). Do NOT modify `sourceAccount` (Gmail dedup key, ACCT-08).
  </action>
  <verify>
    <automated>xcodebuild test -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:MyHomeTests/GmailAccountAttributionTests 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `GmailAccountAttributionTests` exists and passes (TEST SUCCEEDED), proving sourceLabel→accountID resolution and archived-account exclusion.
    - The attribution helper is a pure, view-independent function building `accountIDsByLabel` from non-archived accounts.
    - `GmailSyncController.syncAccount` builds the account-ID map BEFORE the per-message loop and sets `expense.accountID` inside the loop using plain UUIDs (no `@Model` ref held across `await`).
    - No new `ctx.save()` inside the per-message loop (grep the loop body region; single batched save preserved).
    - `sourceAccount` assignment is unchanged.
  </acceptance_criteria>
  <done>Newly Gmail-ingested expenses are auto-attributed to the matching active account by sourceLabel; non-matches fall back to Unassigned; the STAB-02 batched-save/no-stale-ref invariants are preserved.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Account attribution across the expense surfaces: an optional account picker in add/edit (defaulting to last-used), an account filter on the main expense list, and automatic sourceLabel-based attribution for Gmail-ingested expenses. Auto-attribution logic is unit-tested.</what-built>
  <how-to-verify>
    1. Build and run on the iPhone 17 simulator. Create at least one account (Settings > Accounts) if none exist.
    2. Add a new expense → tap the "Account" row → confirm only active accounts plus "Unassigned" appear; pick one and save. Add a second expense → confirm the picker now defaults to that last-used account (D-04).
    3. On the main expense list, open the filter menu → choose the account you used → confirm only that account's expenses show; choose "Unassigned" → confirm only unattributed expenses show; choose "All Accounts" → confirm all return. The filter icon should look "active" (filled) while a filter is applied.
    4. (If Gmail is configured) trigger a sync and confirm a newly ingested expense whose bank label matches an account name/sourceLabel is auto-attributed (visible in that account's detail and via the filter); a non-matching one stays Unassigned.
    5. Edit an existing expense's account and confirm the change persists and reflects in the account detail balance (Plan 02).
  </how-to-verify>
  <resume-signal>Type "approved" or describe any picker/filter/attribution issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gmail-parsed sourceLabel → account match | Bank-supplied label text drives auto-attribution |
| User picker selection → expense.accountID | User assigns attribution manually |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-09 | Elevation | Archived account selectable in picker / matched by Gmail | mitigate | Picker @Query filters `!$0.isArchived`; AddExpenseView nullifies accountID if selected account archived; attribution helper excludes archived accounts (tested by `archivedAccountNotMatched`) |
| T-09-10 | Denial of Service | Stale @Model account ref held across await in syncAccount crashes sync (STAB-02 regression) | mitigate | Pre-loop UUID capture (`accountIDsByLabel: [String: UUID]`) mirroring `categoryIDsByName`; no @Model refs across await; no per-message save added |
| T-09-11 | Tampering | sourceAccount (dedup key) mutated by attribution code | mitigate | Attribution only writes `accountID`; `sourceAccount` left untouched (ACCT-08) |
| T-09-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs — first-party Apple frameworks only (RESEARCH Package Legitimacy Audit); no checkpoint needed |
</threat_model>

<verification>
- `xcodebuild test ... -only-testing:MyHomeTests/GmailAccountAttributionTests` — green.
- `xcodebuild build -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17'` — BUILD SUCCEEDED.
- Human verify checkpoint passes (picker default, filter, Gmail attribution).
</verification>

<success_criteria>
- Optional account picker in add/edit with last-used default, active accounts only (D-04/D-08).
- Account filter on the main expense list (ACCT-06/D-07).
- Gmail ingestion auto-attributes by sourceLabel without breaking STAB-02 invariants (D-05).
</success_criteria>

<output>
Create `.planning/phases/09-schemav6-accounts-management/09-03-SUMMARY.md` when done
</output>
