---
phase: 09-schemav6-accounts-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - MyHomeApp/Persistence/Schema/SchemaV6.swift
  - MyHomeApp/Persistence/Schema/MigrationPlan.swift
  - MyHomeApp/Persistence/Models/Account.swift
  - MyHomeApp/Persistence/Models/Asset.swift
  - MyHomeApp/Persistence/Models/Expense.swift
  - MyHomeApp/Persistence/Models/Note.swift
  - MyHomeApp/Persistence/Models/NoteBlock.swift
  - MyHomeApp/Persistence/Models/Category.swift
  - MyHomeTests/SchemaV6MigrationTests.swift
autonomous: true
requirements: [ACCT-08]
must_haves:
  truths:
    - "App launches against a real V5 store and migrates to V6 without crashing"
    - "Every existing expense with a non-nil sourceLabel is attributed to an auto-created Account via accountID after migration"
    - "Expenses with nil sourceLabel keep accountID == nil (Unassigned) after migration"
    - "Expense.sourceAccount is retained byte-for-byte unchanged through the migration (Gmail dedup key preserved)"
    - "Re-running the V5→V6 migration creates no duplicate Account rows"
    - "Account type is inferred: sourceLabel containing CC/credit/card → credit_card; else → savings"
  artifacts:
    - path: "MyHomeApp/Persistence/Schema/SchemaV6.swift"
      provides: "SchemaV6 versioned schema with Account + Asset models, additive Expense/Note fields"
      contains: "enum SchemaV6"
    - path: "MyHomeApp/Persistence/Models/Account.swift"
      provides: "typealias Account = SchemaV6.Account"
      contains: "typealias Account = SchemaV6.Account"
    - path: "MyHomeApp/Persistence/Models/Asset.swift"
      provides: "typealias Asset = SchemaV6.Asset"
      contains: "typealias Asset = SchemaV6.Asset"
    - path: "MyHomeTests/SchemaV6MigrationTests.swift"
      provides: "V5→V6 fixture test proving lossless backfill + idempotency"
      contains: "func v5StoreBackfillsAccountID"
  key_links:
    - from: "MyHomeApp/Persistence/Schema/MigrationPlan.swift"
      to: "SchemaV6.Account"
      via: "v5ToV6 didMigrate inserts Account, sets Expense.accountID"
      pattern: "context.insert.*Account"
    - from: "MyHomeApp/Persistence/Models/Expense.swift"
      to: "SchemaV6.Expense"
      via: "typealias flip"
      pattern: "typealias Expense = SchemaV6.Expense"
---

<objective>
Create SchemaV6 — the codebase's first non-nil `didMigrate` migration — as one atomic schema commit, and prove it lossless with a `[BLOCKING]` fixture test before any UI consumes the new schema.

SchemaV6 adds the `Account` model (full shape), the `Asset` model (scaffold for Phase 11, no UI), additive fields on `Expense` (`accountID`, `isTransfer`, `transferPairID`) and `Note` (`isDailyRoutine`, `routineLastResetDate`), and backfills `accountID` on existing expenses by grouping on `sourceLabel` (D-01/D-02/D-03). All six `@Model` typealiases plus `AppMigrationPlan.schemas`/`stages` flip together in this plan (STAB-08 footgun).

Purpose: Every downstream Phase 9 plan (Accounts CRUD, attribution surfaces, routine reset) depends on these schema fields existing. The migration backfill (ACCT-08) is the highest-risk element of the phase — it must be validated against a seeded V5 store before any UI work proceeds.
Output: SchemaV6.swift, the v5ToV6 migration stage with idempotent backfill, six flipped/new model typealias files, and a passing migration fixture test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-schemav6-accounts-management/09-CONTEXT.md
@.planning/phases/09-schemav6-accounts-management/09-RESEARCH.md
@.planning/phases/09-schemav6-accounts-management/09-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the BLOCKING V5→V6 migration fixture test (RED)</name>
  <files>MyHomeTests/SchemaV6MigrationTests.swift</files>
  <read_first>
    - MyHomeTests/MigrationTests.swift — copy the v3StoreMigratesToV4 seed-copy-migrate harness verbatim (lines ~104–173); copy the MigrationTestsPlanV3 trimmed-plan helper pattern (lines ~255–270)
    - MyHomeApp/Persistence/Schema/SchemaV5.swift — confirm SchemaV5.Expense has `sourceLabel` and `sourceAccount` fields and their exact types; confirm the SchemaV5.Expense initializer signature used to seed (`SchemaV5.Expense(amount:)`)
    - MyHomeApp/Persistence/Schema/MigrationPlan.swift — confirm AppMigrationPlan type name and that stages currently end at v4ToV5
    - .planning/phases/09-schemav6-accounts-management/09-PATTERNS.md — the SchemaV6MigrationTests.swift section (the seed-copy-migrate harness and MigrationTestsPlanV5 helper)
  </read_first>
  <behavior>
    - Test `v5StoreBackfillsAccountID`: seed a V5 store with three expenses — e1 amount 100 sourceLabel "HDFC CC" sourceAccount "user@gmail.com"; e2 amount 50 sourceLabel "ICICI Savings" sourceAccount "user@gmail.com"; e3 amount 25 no sourceLabel. After migrating to V6: exactly 2 Account rows exist; e1.accountID == the HDFC account id; e2.accountID == the ICICI account id; e3.accountID == nil; e1.sourceAccount == "user@gmail.com" unchanged; HDFC account typeRaw == "credit_card"; ICICI account typeRaw == "savings".
    - Test `v5MigrationIsIdempotent`: seed the same V5 store, migrate to V6, close the container, re-open the same store URL under AppMigrationPlan again. After the second open: still exactly 2 Account rows (no duplicates); e1.accountID still points at a single HDFC account.
    - Both tests use a temp-file `ModelContainer` (not in-memory) so the seeded store is a real on-disk V5 store; copy seed → migrate URL before opening under V6 (mirror MigrationTests.swift).
  </behavior>
  <action>
    Create `MyHomeTests/SchemaV6MigrationTests.swift` with `import Testing`, `import SwiftData`, `import Foundation`, `@testable import MyHome`, and a `@MainActor struct SchemaV6MigrationTests`.

    Add the `MigrationTestsPlanV5` trimmed-plan helper enum (a `SchemaMigrationPlan` whose `schemas` is `[SchemaV1.self, SchemaV2.self, SchemaV3.self, SchemaV4.self, SchemaV5.self]` and `stages` is `[v1ToV2, v2ToV3, v3ToV4, v4ToV5]`, each a `MigrationStage.custom(fromVersion:toVersion:willMigrate: nil, didMigrate: nil)`) so tests can build a real V5 store that stops at V5. Mirror the existing `MigrationTestsPlanV3` helper in MigrationTests.swift exactly.

    Implement `v5StoreBackfillsAccountID()` and `v5MigrationIsIdempotent()` per the behavior block, using the seed-to-temp-URL → copyItem → re-open-under-`AppMigrationPlan.self`-with-`SchemaV6` harness from MigrationTests.swift. Use `try ctx.save()` twice after seeding to flush the WAL (mirror the existing pattern). Use `defer` to remove both temp store URLs. Assert with `#expect`.

    This task is RED-first: `SchemaV6` and `Account`/`Asset` typealiases do not exist yet, so the file will not compile. That is expected — it defines the contract Task 2/3 must satisfy. Do NOT run the suite green at this task; this is the failing-spec task. Mark the verify `<automated>` as MISSING-until-Task-3 below.
  </action>
  <verify>
    <automated>grep -q "func v5StoreBackfillsAccountID" MyHomeTests/SchemaV6MigrationTests.swift && grep -q "func v5MigrationIsIdempotent" MyHomeTests/SchemaV6MigrationTests.swift && grep -q "enum MigrationTestsPlanV5" MyHomeTests/SchemaV6MigrationTests.swift</automated>
  </verify>
  <acceptance_criteria>
    - `MyHomeTests/SchemaV6MigrationTests.swift` exists and declares `struct SchemaV6MigrationTests`.
    - File contains `func v5StoreBackfillsAccountID`, `func v5MigrationIsIdempotent`, and `enum MigrationTestsPlanV5: SchemaMigrationPlan`.
    - Tests reference `AppMigrationPlan.self` and `Schema(versionedSchema: SchemaV6.self)` for the migrate step (proving they exercise the production plan).
    - Tests assert `sourceAccount` is unchanged and `accountID` is set/nil per the behavior block (grep: `sourceAccount`, `accountID`).
  </acceptance_criteria>
  <done>The fixture test file exists with both test functions and the V5 trimmed-plan helper, encoding the ACCT-08 lossless-backfill + idempotency contract. It will not compile until SchemaV6 lands in Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Define SchemaV6 (Account + Asset models, additive Expense/Note fields)</name>
  <files>MyHomeApp/Persistence/Schema/SchemaV6.swift</files>
  <read_first>
    - MyHomeApp/Persistence/Schema/SchemaV5.swift — copy the ENTIRE file structure verbatim into V6: the file-header CloudKit-readiness rules 1–8 comment block, `enum SchemaV5: VersionedSchema`, `versionIdentifier`, `models` array, and every @Model class (Expense, Category, Note, NoteBlock) field-for-field. Note the exact V5 Expense field list, the V5 Note `blocks: [SchemaV5.NoteBlock]?` relationship, and the V5 `@Relationship(deleteRule:inverse:)` declarations (which side declares inverse).
    - .planning/phases/09-schemav6-accounts-management/09-PATTERNS.md — the SchemaV6.swift section (Account/Asset model definitions; additive field lists for Expense and Note)
    - .planning/phases/09-schemav6-accounts-management/09-RESEARCH.md — Critical Pattern 3 (SchemaV6 Model Definitions) and Pitfall 5 (use bare `accountID: UUID?` on Expense; declare the inverse @Relationship only on `Account.expenses`)
  </read_first>
  <action>
    Create `MyHomeApp/Persistence/Schema/SchemaV6.swift` declaring `enum SchemaV6: VersionedSchema` with `static let versionIdentifier = Schema.Version(6, 0, 0)`.

    Copy the CloudKit-readiness rules 1–8 header comment from SchemaV5 verbatim. Copy `Category` and `NoteBlock` @Model classes verbatim from V5 with NO field changes (NoteBlock gets no new fields this phase — per-block lastCheckedDate is deferred to Phase 12).

    Copy `Expense` from V5 verbatim, then APPEND (never reorder/remove existing fields) exactly these three additive fields with defaults: `var accountID: UUID? = nil`, `var isTransfer: Bool? = nil`, `var transferPairID: UUID? = nil`. Keep `sourceLabel` and `sourceAccount` exactly as V5 — sourceAccount is the Gmail dedup key and must not be touched (ACCT-08).

    Copy `Note` from V5 verbatim, then APPEND exactly: `var isDailyRoutine: Bool = false` (D-11) and `var routineLastResetDate: Date? = nil` (D-12).

    Add the new `Account` @Model with these stored properties and defaults (per CloudKit rules — every property optional or defaulted, no `.unique`, Decimal for money, UUID PK, String raw value for type, hex String for color): `id: UUID = UUID()`, `name: String? = nil`, `typeRaw: String? = nil` (values "savings"/"current"/"credit_card"), `symbolName: String? = nil`, `colorHex: String? = nil`, `last4: String? = nil`, `balanceBaseline: Decimal? = nil`, `balanceAsOfDate: Date? = nil`, `isArchived: Bool = false`, `sortOrder: Int = 0`, `sourceLabel: String? = nil`, `createdAt: Date = Date()`. Declare the one-to-many inverse on THIS side only: `@Relationship(deleteRule: .nullify) var expenses: [SchemaV6.Expense] = []` (rule 7 — Expense uses bare `accountID: UUID?`, NOT a relationship; this avoids the circular-macro error, Pitfall 5). Add `init(name: String, typeRaw: String = "savings", sourceLabel: String? = nil)` setting id, name, typeRaw, sourceLabel, createdAt.

    Add the new `Asset` @Model scaffold (no UI this phase): `id: UUID = UUID()`, `name: String? = nil`, `assetClassRaw: String? = nil`, `units: Decimal? = nil`, `costBasisPerUnit: Decimal? = nil`, `currentNAV: Decimal? = nil`, `navAsOfDate: Date? = nil`, `createdAt: Date = Date()`, with `init()` setting id and createdAt.

    Add `static var models: [any PersistentModel.Type]` listing `SchemaV6.Expense.self, SchemaV6.Category.self, SchemaV6.Note.self, SchemaV6.NoteBlock.self, SchemaV6.Account.self, SchemaV6.Asset.self`.
  </action>
  <verify>
    <automated>grep -q "enum SchemaV6" MyHomeApp/Persistence/Schema/SchemaV6.swift && grep -q "final class Account" MyHomeApp/Persistence/Schema/SchemaV6.swift && grep -q "final class Asset" MyHomeApp/Persistence/Schema/SchemaV6.swift && grep -q "var accountID: UUID? = nil" MyHomeApp/Persistence/Schema/SchemaV6.swift && grep -q "var isDailyRoutine: Bool = false" MyHomeApp/Persistence/Schema/SchemaV6.swift</automated>
  </verify>
  <acceptance_criteria>
    - `Schema.Version(6, 0, 0)` declared.
    - `Account` @Model declares all 12 stored properties listed in the action, `isArchived` and `sortOrder` and `createdAt` with defaults, `balanceBaseline: Decimal?`, `colorHex: String?`, `typeRaw: String?` (no stored enum).
    - `Account.expenses` declares `@Relationship(deleteRule: .nullify)`; `Expense` has bare `accountID: UUID? = nil` with NO `@Relationship` on it.
    - `Asset` @Model present with the 8 scaffold fields.
    - `Expense` has `accountID`, `isTransfer`, `transferPairID` appended; `sourceAccount` and `sourceLabel` unchanged from V5.
    - `Note` has `isDailyRoutine: Bool = false` and `routineLastResetDate: Date? = nil` appended.
    - `models` array lists all six model types including Account and Asset.
    - No `@Attribute(.unique)` anywhere in the file (grep: `.unique` returns nothing).
  </acceptance_criteria>
  <done>SchemaV6 is a strict additive superset of SchemaV5 with the Account and Asset models, satisfying all eight CloudKit-readiness rules.</done>
</task>

<task type="auto">
  <name>Task 3: Flip all six typealiases + add v5ToV6 migration stage with backfill (GREEN — atomic commit)</name>
  <files>MyHomeApp/Persistence/Models/Account.swift, MyHomeApp/Persistence/Models/Asset.swift, MyHomeApp/Persistence/Models/Expense.swift, MyHomeApp/Persistence/Models/Note.swift, MyHomeApp/Persistence/Models/NoteBlock.swift, MyHomeApp/Persistence/Models/Category.swift, MyHomeApp/Persistence/Schema/MigrationPlan.swift</files>
  <read_first>
    - MyHomeApp/Persistence/Models/Note.swift — the existing typealias-flip comment style and the `typealias Note = SchemaV5.Note` line to change
    - MyHomeApp/Persistence/Models/Expense.swift, NoteBlock.swift, Category.swift — confirm each current `typealias X = SchemaV5.X` line
    - MyHomeApp/Persistence/Schema/MigrationPlan.swift — the full file: `AppMigrationPlan` `schemas` array (ends at SchemaV5.self), `stages` array (ends at v4ToV5), and the `.custom(...willMigrate: nil, didMigrate: nil)` FB13812722 pattern + comment for v4ToV5
    - .planning/phases/09-schemav6-accounts-management/09-PATTERNS.md — the MigrationPlan.swift section (schemas/stages extension + the full v5ToV6 didMigrate body with idempotency guards) and the Account.swift/Asset.swift typealias sections
    - .planning/phases/09-schemav6-accounts-management/09-RESEARCH.md — Critical Pattern 1 (idempotent backfill: fetch-existing-before-insert; guard `accountID == nil`; explicit `try context.save()`; set `accountReviewPending` UserDefaults flag when accounts created), Pitfalls 2/3, and Critical Pattern 2 (atomic typealias flip)
    - Memory note schema-version-mutation-footgun (STAB-08) — flip ALL typealiases in one commit
  </read_first>
  <action>
    THIS IS THE STAB-08 ATOMIC COMMIT. Do all of the following as one unit:

    Create `MyHomeApp/Persistence/Models/Account.swift` with `import SwiftData` and `typealias Account = SchemaV6.Account`, plus the STAB-08 flip comment (mirror Note.swift's comment style). Create `MyHomeApp/Persistence/Models/Asset.swift` with `import SwiftData` and `typealias Asset = SchemaV6.Asset`.

    Edit the four existing model files to flip their typealiases to V6: `Expense.swift` → `typealias Expense = SchemaV6.Expense`; `Note.swift` → `typealias Note = SchemaV6.Note`; `NoteBlock.swift` → `typealias NoteBlock = SchemaV6.NoteBlock`; `Category.swift` → `typealias Category = SchemaV6.Category`. Add/update the flip comment in each noting "Flipped SchemaV5.X → SchemaV6.X in Phase 9 (plan 09-01); SchemaV6.X is an additive superset".

    Edit `MyHomeApp/Persistence/Schema/MigrationPlan.swift`:
    - Append `SchemaV6.self` to the `schemas` array (never remove V1–V5).
    - Append `v5ToV6` to the `stages` array.
    - Add `static let v5ToV6 = MigrationStage.custom(fromVersion: SchemaV5.self, toVersion: SchemaV6.self, willMigrate: nil, didMigrate: { context in ... })`. Keep `.custom` (FB13812722 workaround preserved — do NOT use `.lightweight`). Add a comment marking this as the FIRST non-nil didMigrate and that the closure is synchronous (`throws`, NOT `async throws`) — never `await` inside it.
    - didMigrate body (idempotent, in this order): (1) `let expenses = try context.fetch(FetchDescriptor<SchemaV6.Expense>())`; (2) fetch existing accounts and build `var accountByLabel: [String: SchemaV6.Account]` keyed on each account's non-nil `sourceLabel` (idempotency guard — fetch-before-insert, Pitfall 2); (3) for each distinct non-nil `sourceLabel` in expenses, if absent from the map, create `SchemaV6.Account(name: label, typeRaw: inferAccountType(from: label), sourceLabel: label)`, `context.insert(...)`, add to map, and set a `didCreateAny` flag true; (4) for each expense, `guard expense.accountID == nil, let label = expense.sourceLabel else { continue }` then `expense.accountID = accountByLabel[label]?.id`; (5) `try context.save()` (REQUIRED — migration context does not auto-commit, Pitfall 3); (6) if `didCreateAny`, `UserDefaults.standard.set(true, forKey: "accountReviewPending")` (D-02).
    - Add `private static func inferAccountType(from label: String) -> String` (D-03): lowercase the label; if it contains "cc" OR "credit" OR "card" return "credit_card"; otherwise return "savings".

    After this task the migration fixture test from Task 1 must compile and pass. Run the migration test suite to confirm GREEN.
  </action>
  <verify>
    <automated>xcodebuild test -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:MyHomeTests/SchemaV6MigrationTests 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `MyHomeApp/Persistence/Models/Account.swift` contains `typealias Account = SchemaV6.Account`; `Asset.swift` contains `typealias Asset = SchemaV6.Asset`.
    - All four pre-existing model files now read `SchemaV6.X` (grep: `grep -c "SchemaV5\." MyHomeApp/Persistence/Models/*.swift` returns 0 for typealias lines — no model typealias still points at V5).
    - `MigrationPlan.swift` `schemas` array includes `SchemaV6.self`; `stages` array includes `v5ToV6`.
    - `v5ToV6` is `.custom` (not `.lightweight`) with a non-nil `didMigrate`; `inferAccountType` returns "credit_card" for "cc"/"credit"/"card" substrings else "savings".
    - didMigrate ends with `try context.save()` and sets `accountReviewPending` only when accounts were created.
    - `xcodebuild test ... -only-testing:MyHomeTests/SchemaV6MigrationTests` passes both tests (build succeeds, exit shows TEST SUCCEEDED).
  </acceptance_criteria>
  <done>SchemaV6 is the live container schema, all typealiases point at V6 atomically, the v5ToV6 backfill is idempotent, and the BLOCKING migration fixture test is green — unblocking all Wave 2 plans.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| V5 store on disk → V6 migration context | Existing user data crosses into the new schema; data-loss or partial-state risk if didMigrate throws mid-run |
| sourceLabel strings → auto-created Account names | User/bank-supplied label text becomes account display data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | v5ToV6 didMigrate partial-state on throw | mitigate | Idempotent backfill (fetch-existing-before-insert; guard `accountID == nil`) so a re-run after a failed init repairs rather than duplicates (Pitfall 2); explicit single `try context.save()` at end (Pitfall 3); idempotency proven by `v5MigrationIsIdempotent` test |
| T-09-02 | Tampering | sourceAccount (Gmail dedup key) accidentally mutated in migration | mitigate | Migration only writes `accountID` (additive); `sourceAccount` untouched; `v5StoreBackfillsAccountID` asserts `sourceAccount == "user@gmail.com"` unchanged |
| T-09-03 | Denial of Service | typealias mismatch (STAB-08) causing save/query crash app-wide | mitigate | All six typealiases flipped in one atomic task with `schemas` array update; acceptance grep confirms no model typealias still points at V5 |
| T-09-04 | Tampering | Account name rendered from untrusted sourceLabel | accept | Account names are displayed via plain `Text()` only (enforced in Plan 02/03); no markdown interpretation; local single-user app, label source is the user's own bank emails |
| T-09-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this phase — all first-party Apple frameworks (RESEARCH Package Legitimacy Audit: no external packages); no checkpoint needed |
</threat_model>

<verification>
- `xcodebuild test -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:MyHomeTests/SchemaV6MigrationTests` — both migration tests green (BLOCKING gate for the phase).
- `grep -rn "SchemaV5\." MyHomeApp/Persistence/Models/` — no model typealias still references V5.
- Full suite green before phase verification: `xcodebuild test -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17'`.
</verification>

<success_criteria>
- SchemaV6 is the container schema; all six typealiases point at V6 (atomic commit).
- V5→V6 migration backfills `accountID` from `sourceLabel`, retains `sourceAccount`, is idempotent, and infers account type (D-01/D-02/D-03/ACCT-08).
- The BLOCKING migration fixture test passes before any UI plan runs.
</success_criteria>

<output>
Create `.planning/phases/09-schemav6-accounts-management/09-01-SUMMARY.md` when done
</output>
