---
phase: 09-schemav6-accounts-management
plan: 04
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - MyHomeApp/Features/Notes/RoutineResetService.swift
  - MyHomeApp/RootView.swift
  - MyHomeTests/RoutineResetServiceTests.swift
autonomous: false
requirements: [STAB-04, NOTE-02]
user_setup: []
must_haves:
  truths:
    - "On scenePhase .active, RoutineResetService unchecks all checklist blocks of every isDailyRoutine note whose routineLastResetDate < startOfToday in IST, then stamps the new reset date (STAB-04/NOTE-02/D-12)"
    - "Notes NOT flagged isDailyRoutine are never auto-unchecked (D-11)"
    - "Repeated .active events on the same IST day are a no-op (idempotent reset)"
    - "RoutineResetService receives its ModelContext from RootView so the reset reads/writes the live store"
  artifacts:
    - path: "MyHomeApp/Features/Notes/RoutineResetService.swift"
      provides: "Filled reset body using isDailyRoutine + note-level routineLastResetDate (IST)"
      contains: "isDailyRoutine"
    - path: "MyHomeTests/RoutineResetServiceTests.swift"
      provides: "Reset / idempotency / non-routine-untouched tests (STAB-04)"
      contains: "func"
  key_links:
    - from: "MyHomeApp/RootView.swift"
      to: "RoutineResetService.modelContext"
      via: ".onAppear injects modelContext (mirrors gmailSyncController.setContext)"
      pattern: "routineResetService.modelContext"
    - from: "MyHomeApp/Features/Notes/RoutineResetService.swift"
      to: "Note.routineLastResetDate"
      via: "fetch isDailyRoutine notes; compare/stamp routineLastResetDate"
      pattern: "routineLastResetDate"
---

<objective>
Fill the Phase 8 `RoutineResetService` stub body with the real per-day reset (STAB-04, NOTE-02, D-11/D-12), and inject its `ModelContext` from `RootView`. On `scenePhase .active`, every note flagged `isDailyRoutine` whose note-level `routineLastResetDate` is before IST start-of-today has all its checklist blocks unchecked and the reset date stamped; ordinary notes are untouched; same-day reactivations are no-ops.

Purpose: This is the observable behavior that satisfies STAB-04 (a daily routine's checked state resets each morning). The Phase 8 scaffold already wired the call site in RootView; SchemaV6 (Plan 01) added the `isDailyRoutine` and `routineLastResetDate` fields this body consumes.
Output: a completed RoutineResetService.resetIfNeeded(), a ModelContext injection in RootView, and unit tests for reset, idempotency, and the non-routine-untouched guarantee.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-schemav6-accounts-management/09-CONTEXT.md
@.planning/phases/09-schemav6-accounts-management/09-PATTERNS.md
@.planning/phases/09-schemav6-accounts-management/09-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RoutineResetServiceTests (reset, idempotency, non-routine untouched) — RED</name>
  <files>MyHomeTests/RoutineResetServiceTests.swift</files>
  <read_first>
    - MyHomeApp/Features/Notes/RoutineResetService.swift — the Phase 8 stub: `@MainActor @Observable final class RoutineResetService`, the IST `Calendar`/`startOfDay` already sketched, and the `resetIfNeeded()` signature (currently a logged no-op with no modelContext property)
    - MyHomeTests/CategoryCRUDTests.swift — in-memory `ModelContainer` test setup; `@Test`/`#expect` patterns; how a context is built for tests
    - MyHomeApp/Persistence/Schema/SchemaV5.swift — Note `blocks: [NoteBlock]?` relationship and NoteBlock `kindRaw` (== "checkbox") + `isChecked` fields (V6 copies these verbatim); confirm the NoteBlock initializer signature for seeding
    - MyHomeApp/Persistence/Models/Note.swift, NoteBlock.swift — the V6 typealiases (Plan 01) and the new `isDailyRoutine` / `routineLastResetDate` fields on Note
    - .planning/phases/09-schemav6-accounts-management/09-PATTERNS.md — the RoutineResetService.swift section (full reset body) and the RootView injection note
    - .planning/phases/09-schemav6-accounts-management/09-RESEARCH.md — Critical Pattern 5 (RoutineResetService body)
  </read_first>
  <behavior>
    - `resetsRoutineNoteCrossingMidnight`: a Note with isDailyRoutine = true, routineLastResetDate = yesterday (IST), and two checkbox blocks both isChecked = true. After resetIfNeeded(): both blocks isChecked == false; note.routineLastResetDate == IST start-of-today.
    - `nonRoutineNoteUntouched`: a Note with isDailyRoutine = false and a checked checkbox block. After resetIfNeeded(): the block stays isChecked == true; routineLastResetDate stays nil (D-11 — ordinary checklists never auto-reset).
    - `idempotentSameDay`: a routine note already reset today (routineLastResetDate == IST start-of-today) with a block the user re-checked. After resetIfNeeded(): the block stays checked (same-day = no-op; the user's intra-day checks are preserved).
    - `onlyChecklistBlocksAffected`: a routine note with a text block and a checked checkbox block resets only the checkbox block (text block untouched).
    - Tests construct a RoutineResetService, assign its `modelContext` to an in-memory test context, seed notes/blocks, call `resetIfNeeded()`, and assert on the fetched models. To control "today vs yesterday", set the seeded `routineLastResetDate` relative to an IST `startOfDay(for: Date())` the test computes the same way the service does.
  </behavior>
  <action>
    Create `MyHomeTests/RoutineResetServiceTests.swift` (`import Testing`, `import SwiftData`, `import Foundation`, `@testable import MyHome`, `@MainActor struct RoutineResetServiceTests`). Build an in-memory `ModelContainer` for the test (mirror CategoryCRUDTests setup). For each test, instantiate `RoutineResetService()`, set `service.modelContext = container.mainContext`, seed Notes and NoteBlocks per the behavior block, call `service.resetIfNeeded()`, then fetch and `#expect` on results.

    Compute the IST reference date in the test exactly as the service will (`Calendar(identifier: .gregorian)` with `timeZone = TimeZone(identifier: "Asia/Kolkata")!`, `startOfDay(for: Date())`) so "yesterday"/"today" comparisons are deterministic. This is RED-first: `resetIfNeeded()` is still the Phase 8 no-op (and has no modelContext property yet) until Task 2, so these tests fail/don't-compile now — that is the intended contract. Add the modelContext property reference the tests need (Task 2 implements it).
  </action>
  <verify>
    <automated>grep -q "struct RoutineResetServiceTests" MyHomeTests/RoutineResetServiceTests.swift && grep -q "resetsRoutineNoteCrossingMidnight" MyHomeTests/RoutineResetServiceTests.swift && grep -q "nonRoutineNoteUntouched" MyHomeTests/RoutineResetServiceTests.swift && grep -q "idempotentSameDay" MyHomeTests/RoutineResetServiceTests.swift</automated>
  </verify>
  <acceptance_criteria>
    - `MyHomeTests/RoutineResetServiceTests.swift` exists with `struct RoutineResetServiceTests` and the four test functions named in the behavior block.
    - Tests assign `service.modelContext` to an in-memory context and call `service.resetIfNeeded()`.
    - Tests compute the IST start-of-today the same way the service does (grep: `Asia/Kolkata`).
    - Assertions cover: routine blocks unchecked + date stamped; non-routine untouched; same-day no-op; text blocks unaffected.
  </acceptance_criteria>
  <done>The STAB-04 reset contract is encoded as failing tests, ready for the Task 2 implementation to satisfy.</done>
</task>

<task type="auto">
  <name>Task 2: Fill RoutineResetService body + inject ModelContext in RootView (GREEN)</name>
  <files>MyHomeApp/Features/Notes/RoutineResetService.swift, MyHomeApp/RootView.swift</files>
  <read_first>
    - MyHomeApp/Features/Notes/RoutineResetService.swift — the full Phase 8 stub (the @MainActor @Observable class, the IST calendar lines, the no-op print) — replace the body, keep the class markers
    - MyHomeApp/RootView.swift — the `.onAppear` block where `gmailSyncController.setContext(modelContext)` is called (the injection-point analog), the `@State private var routineResetService = RoutineResetService()`, and the `.onChange(of: scenePhase)` `.active` branch already calling `routineResetService.resetIfNeeded()`
    - MyHomeApp/Persistence/Models/Note.swift, NoteBlock.swift — V6 Note `isDailyRoutine`/`routineLastResetDate`/`blocks`; NoteBlock `kindRaw`/`isChecked`
    - .planning/phases/09-schemav6-accounts-management/09-PATTERNS.md — the RoutineResetService.swift full-body section and the RootView injection one-liner
  </read_first>
  <action>
    Add `var modelContext: ModelContext?` to `RoutineResetService` (keep `@MainActor @Observable final class`; add `import SwiftData`). Replace the no-op `resetIfNeeded()` body:
    - `guard let context = modelContext else { return }`.
    - Build the IST calendar (`Calendar(identifier: .gregorian)`, `timeZone = TimeZone(identifier: "Asia/Kolkata")!`) and `let startOfTodayIST = cal.startOfDay(for: Date())` (keep the Phase 8 IST line).
    - In a `do`: fetch `try context.fetch(FetchDescriptor<Note>(predicate: #Predicate { $0.isDailyRoutine == true }))` (D-11 — only routine notes). For each note: `let lastReset = note.routineLastResetDate ?? .distantPast`; `guard lastReset < startOfTodayIST else { continue }` (D-12 idempotent same-day no-op). For each block in `note.blocks ?? []` where `block.kindRaw == "checkbox" && block.isChecked`, set `block.isChecked = false`. Set `note.routineLastResetDate = startOfTodayIST`. Track a `didChange` flag and `if didChange { try context.save() }` (CR-01).
    - `catch { print("[RoutineResetService] reset failed: \(error)") }` (non-fatal; do not crash the app on scene activation).

    Edit `RootView.onAppear`: after `gmailSyncController.setContext(modelContext)`, add `routineResetService.modelContext = modelContext` (same injection pattern). Do NOT change the existing `.onChange(of: scenePhase) .active` call site — it already invokes `routineResetService.resetIfNeeded()` synchronously.

    After this task, the Task 1 tests must pass.
  </action>
  <verify>
    <automated>xcodebuild test -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:MyHomeTests/RoutineResetServiceTests 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `RoutineResetServiceTests` all pass (TEST SUCCEEDED).
    - `RoutineResetService` has a `var modelContext: ModelContext?` and a `resetIfNeeded()` that fetches only `isDailyRoutine == true` notes, compares note-level `routineLastResetDate` against IST start-of-today, unchecks only `kindRaw == "checkbox"` blocks, stamps the date, and saves only when changed.
    - The body is wrapped in do/catch and does not crash on error.
    - `RootView.onAppear` assigns `routineResetService.modelContext = modelContext` (grep: `routineResetService.modelContext`).
    - The existing scenePhase `.active` call to `resetIfNeeded()` is preserved.
  </acceptance_criteria>
  <done>The daily-routine reset is fully operational: routine checklists clear once per IST day on app activation, ordinary checklists are never auto-reset, and same-day reactivations are no-ops.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The RoutineResetService body and its RootView ModelContext injection. Routine-flagged notes have their checklist items unchecked once per IST day on scene activation; non-routine notes are untouched. Reset/idempotency/non-routine behavior is unit-tested green.</what-built>
  <how-to-verify>
    1. Build and run on the iPhone 17 simulator. The "mark note as routine" UI toggle ships in Phase 12, so for this check set a note's `isDailyRoutine` flag manually (debugger, a temporary debug toggle, or seeded data) on a note that has checked checklist items.
    2. Check several checklist items on that routine note. Background the app, advance the simulator's date by one day (Settings > General > Date & Time, or relaunch after changing the system date), then foreground the app.
    3. Confirm the routine note's checklist items are now all unchecked (reset fired on `.active`).
    4. Re-check some items, background+foreground the app on the SAME day → confirm your checks are preserved (same-day no-op, not double-reset).
    5. On an ordinary (non-routine) note with checked items, repeat the day-advance → confirm those items stay checked (D-11).
  </how-to-verify>
  <resume-signal>Type "approved" or describe any reset-timing or scope issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scenePhase .active event → store mutation | A lifecycle event triggers a batch write to checklist state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-12 | Tampering | Over-broad reset unchecking non-routine or non-checkbox blocks (data loss of user's checked state) | mitigate | Fetch predicate restricts to `isDailyRoutine == true`; inner guard restricts to `kindRaw == "checkbox"`; covered by `nonRoutineNoteUntouched` and `onlyChecklistBlocksAffected` tests |
| T-09-13 | Tampering | Repeated .active events double-resetting and wiping intra-day user checks | mitigate | Note-level `routineLastResetDate < startOfTodayIST` guard makes same-day reactivation a no-op; covered by `idempotentSameDay` test |
| T-09-14 | Denial of Service | reset error crashing the app on every activation | mitigate | resetIfNeeded body wrapped in do/catch that logs and returns; nil modelContext guarded |
| T-09-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs — first-party Apple frameworks only (RESEARCH Package Legitimacy Audit); no checkpoint needed |
</threat_model>

<verification>
- `xcodebuild test ... -only-testing:MyHomeTests/RoutineResetServiceTests` — green.
- `xcodebuild build -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17'` — BUILD SUCCEEDED.
- Human verify checkpoint passes (day-advance reset, same-day no-op, non-routine untouched).
</verification>

<success_criteria>
- Routine checklists reset once per IST day on .active; non-routine notes untouched (STAB-04/NOTE-02/D-11).
- Same-day reactivation is idempotent (D-12).
- RoutineResetService reads/writes the live store via RootView-injected ModelContext.
</success_criteria>

<output>
Create `.planning/phases/09-schemav6-accounts-management/09-04-SUMMARY.md` when done
</output>
