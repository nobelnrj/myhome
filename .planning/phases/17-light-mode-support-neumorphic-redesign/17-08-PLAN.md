---
phase: 17-light-mode-support-neumorphic-redesign
plan: 08
type: execute
wave: 4
depends_on: [17-03]
files_modified:
  - MyHomeApp/DesignSystem/RollingMoneyText.swift
  - MyHomeApp/Features/Settings/SettingsView.swift
  - MyHomeApp/Features/Settings/AccountsListView.swift
  - MyHomeApp/Features/Settings/UnlockView.swift
  - MyHomeApp/Features/Settings/EditAccountView.swift
  - MyHomeApp/Features/Settings/MigrationReviewSheet.swift
  - MyHomeApp/Features/Expenses/AddTransferView.swift
  - MyHomeApp/Features/Expenses/EditExpenseView.swift
  - MyHomeApp/Features/Expenses/ExpenseListView.swift
  - MyHomeApp/Features/Expenses/AddExpenseView.swift
  - MyHomeApp/Features/Expenses/AccountPickerView.swift
  - MyHomeApp/Features/Expenses/CategoryPickerView.swift
autonomous: true
requirements: [D-08]
must_haves:
  truths:
    - "In the design system (RollingMoneyText), Settings, and Expenses feature areas, accent-colored TEXT and ICONS use accentText (dark amber in light, canary in dark) while fills — Toggle tracks, swipe-action backgrounds — keep canary accent (D-08)"
    - "Dark rendering is unchanged by every swap (accentText dark branch == #FFD60A) — screenshots diff clean vs Plan 01 baselines (D-06)"
    - "Every accent call site in these areas has a recorded role classification — no site is silently skipped"
  artifacts:
    - path: "MyHomeApp/DesignSystem/RollingMoneyText.swift"
      provides: "Hero numeral tint on accentText"
      contains: "accentText"
  key_links:
    - from: "audited feature files"
      to: "MyHomeApp/DesignSystem/DesignTokens.swift"
      via: "text/icon-role sites reference DesignTokens.accentText"
      pattern: "DesignTokens\\.accentText"
---

<objective>
Execute the D-08 accent role-split call-site audit, part 1 of 2: RollingMoneyText
(design system) + the Settings and Expenses feature areas. Text/icon-role sites
move to accentText; fill-role sites keep canary accent; every site gets an
explicit per-file disposition so partial passes are detectable.

Purpose: canary text on the light canvas is ~1.4:1 (RESEARCH) — illegible. The
role split fixes light legibility with zero dark impact. Part 2 (Notes, Budgets,
Overview, Assets, Analytics) is Plan 17-09.
Output: 12 files audited and swapped where text-role; classification table in
SUMMARY; dark bit-identical.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-light-mode-support-neumorphic-redesign/17-CONTEXT.md
@.planning/phases/17-light-mode-support-neumorphic-redesign/17-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Role-split audit — RollingMoneyText + Settings area (6 files)</name>
  <files>MyHomeApp/DesignSystem/RollingMoneyText.swift, MyHomeApp/Features/Settings/SettingsView.swift, MyHomeApp/Features/Settings/AccountsListView.swift, MyHomeApp/Features/Settings/UnlockView.swift, MyHomeApp/Features/Settings/EditAccountView.swift, MyHomeApp/Features/Settings/MigrationReviewSheet.swift</files>
  <read_first>
    - .planning/phases/17-light-mode-support-neumorphic-redesign/17-RESEARCH.md §Pitfall 6 (role classification rules)
    - Each file listed below at the cited lines (confirm the surrounding control before classifying)
  </read_first>
  <action>
    Apply the D-08 role rules. TEXT/ICON role (accent renders as glyphs/labels) → `DesignTokens.accentText`. FILL role (accent renders as a surface: Toggle ON track, swipe-action button background, shape fills, gradient stops, shadow/glow tints) → KEEP `DesignTokens.accent`. Sites tinting OTHER tokens (negative/orange/catSubscriptions) → leave untouched (already adaptive from Plan 02). accentText dark branch == #FFD60A, so misclassification is invisible in dark and caught in light review.

    Per-file checklist (line numbers as of planning; re-locate by pattern if drifted):
    | File:line | Current | Expected disposition |
    |-----------|---------|---------------------|
    | RollingMoneyText.swift:111 | .foregroundStyle(accent) hero numeral | → accentText |
    | SettingsView.swift:64 | .tint(accent) on Face ID Toggle | KEEP accent (Toggle track = fill) |
    | SettingsView.swift:153 | .foregroundStyle(accent) | → accentText |
    | AccountsListView.swift:76 | .tint(accent) | classify: swipe-action/Toggle = KEEP; button/link text = accentText — record verdict |
    | AccountsListView.swift:101 | .tint(orange) | leave (adaptive token) |
    | AccountsListView.swift:124 | .tint(catSubscriptions) | leave (adaptive token) |
    | UnlockView.swift:45 | .tint(accent) | classify per control; ProgressView/Button label = accentText |
    | UnlockView.swift:73 | .foregroundStyle(accent) | → accentText |
    | EditAccountView.swift:209 | .tint(accent) | classify per control |
    | MigrationReviewSheet.swift:54 | .foregroundStyle(accent) | → accentText |
    | MigrationReviewSheet.swift:90, :130 | .tint(accent) | classify per control |
    After edits: `grep -n "DesignTokens.accent" <each file>` and confirm every remaining bare `accent` reference is a documented fill-role. Record the full table (file:line → role → action) in the SUMMARY.
  </action>
  <verify>
    <automated>xcodebuild build -scheme MyHome -destination "id=2F09365E-5099-490E-9484-B8788C53C816" -quiet && grep -rn 'foregroundStyle(DesignTokens.accent)' MyHomeApp/DesignSystem/RollingMoneyText.swift MyHomeApp/Features/Settings --include="*.swift" | wc -l # expect 0</automated>
  </verify>
  <acceptance_criteria>
    - Zero `.foregroundStyle(DesignTokens.accent)` in RollingMoneyText.swift and Features/Settings/
    - RollingMoneyText.swift:111 reads accentText; SettingsView.swift:64 Toggle still reads accent (fill-role preserved)
    - Per-file table above fully dispositioned in SUMMARY — one row per listed site, none marked "skipped"
    - Build green
  </acceptance_criteria>
  <done>Settings area + hero numerals role-split; every site classified on record.</done>
</task>

<task type="auto">
  <name>Task 2: Role-split audit — Expenses area (6 files) + dark identity check</name>
  <files>MyHomeApp/Features/Expenses/AddTransferView.swift, MyHomeApp/Features/Expenses/EditExpenseView.swift, MyHomeApp/Features/Expenses/ExpenseListView.swift, MyHomeApp/Features/Expenses/AddExpenseView.swift, MyHomeApp/Features/Expenses/AccountPickerView.swift, MyHomeApp/Features/Expenses/CategoryPickerView.swift</files>
  <read_first>
    - .planning/phases/17-light-mode-support-neumorphic-redesign/17-RESEARCH.md §Pitfall 6
    - Each file at the cited lines below
  </read_first>
  <action>
    Same rules as Task 1. Per-file checklist:
    | File:line | Current | Expected disposition |
    |-----------|---------|---------------------|
    | AddTransferView.swift:82 | .tint(accent) | classify per control; button/nav text = accentText |
    | EditExpenseView.swift:101 | .tint(accent) | classify per control |
    | EditExpenseView.swift:310 | .tint(negative) | leave (adaptive token) |
    | ExpenseListView.swift:217 | .tint(accent) | classify per control |
    | AddExpenseView.swift:93 | .tint(accent) | classify per control |
    | AccountPickerView.swift:45, :73 | .foregroundStyle(accent) | → accentText |
    | AccountPickerView.swift:96 | .tint(negative) | leave (adaptive token) |
    | CategoryPickerView.swift:42, :70 | .foregroundStyle(accent) | → accentText |
    | CategoryPickerView.swift:93 | .tint(negative) | leave (adaptive token) |
    After edits, run the dark identity gate for this whole plan: rebuild, dark appearance, no stored theme key, capture all 6 screens per the Plan 01 loop, `python3 diff_dark.py baselines <after>` — must exit 0 (the swaps are invisible in dark by construction).
  </action>
  <verify>
    <automated>xcodebuild build -scheme MyHome -destination "id=2F09365E-5099-490E-9484-B8788C53C816" -quiet && grep -rn 'foregroundStyle(DesignTokens.accent)' MyHomeApp/Features/Expenses --include="*.swift" | wc -l # expect 0</automated>
  </verify>
  <acceptance_criteria>
    - Zero `.foregroundStyle(DesignTokens.accent)` in Features/Expenses/
    - All 9 listed sites dispositioned in the SUMMARY table (none skipped); the three .tint(negative) sites unmodified
    - diff_dark.py vs Plan 01 baselines exits 0 (all 6 screens)
  </acceptance_criteria>
  <done>Expenses area role-split; dark render proven unchanged for the whole plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| none new | Mechanical color-token substitution; no data flows, storage, or input surfaces. No new attack surface. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-09 | Tampering (regression) | Silent partial pass — a site skipped or misclassified | mitigate | Per-file checklist with mandatory disposition rows in SUMMARY + zero-count greps scoped per directory + dark diff gate |
</threat_model>

<verification>
- Build green; per-directory foregroundStyle(accent) greps return 0
- diff_dark.py vs Plan 01 baselines exits 0
- SUMMARY contains a disposition row for every listed site
</verification>

<success_criteria>
- D-08 role split complete for design-system numerals + Settings + Expenses (12 files), fills preserved as canary
- Dark bit-identity maintained (D-06)
</success_criteria>

<output>
Create `.planning/phases/17-light-mode-support-neumorphic-redesign/17-08-SUMMARY.md` when done. Include the full classification table.
</output>
