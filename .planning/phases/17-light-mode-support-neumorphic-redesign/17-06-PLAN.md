---
phase: 17-light-mode-support-neumorphic-redesign
plan: 06
type: execute
wave: 6
depends_on: [17-05]
files_modified:
  - MyHomeApp/Features/Overview/SpendOverTimeChart.swift
  - MyHomeApp/Features/Analytics/AnalyticsTrendChart.swift
  - MyHomeApp/Features/Assets/NetWorthTrendChart.swift
  - MyHomeApp/Features/Analytics/AnalyticsView.swift
  - MyHomeApp/Features/Analytics/AIInsightCard.swift
  - MyHomeApp/Features/Shared/IconTile.swift
  - MyHomeApp/Features/Settings/EditAccountView.swift
  - MyHomeApp/DesignSystem/DesignTokens.swift
  - MyHomeTests/DesignTokensTests.swift
autonomous: true
requirements: [D-09, D-11, D-12, D-13, D-15]
must_haves:
  truths:
    - "All three trend charts (spend-over-time, analytics trend, net-worth trend) render inside slate instrument windows with their luminous amber/neon palette intact in light mode — the user's 'ALL charts in deep dishes' intent (D-12 boundary decision resolved YES)"
    - "AI Insight card in light: deepened violet text/sparkles passing contrast, subtle deep-violet tinted edge shadow, breathing orb keeps violet fill with reduced bloom; violet stays AI-only (D-15)"
    - "IconTile glyphs stay legible on deepened light category fills (D-09)"
    - "Analytics range-picker pill carries light-tuned rims/shadows; account color picker palette audited for light legibility without touching stored user data"
    - "Dark rendering remains pixel-identical to Plan 01 baselines"
  artifacts:
    - path: "MyHomeApp/Features/Overview/SpendOverTimeChart.swift"
      provides: "Recessed slate plot inset + force-dark chart content"
      contains: "dishSlate"
    - path: "MyHomeApp/Features/Analytics/AIInsightCard.swift"
      provides: "D-15 violet treatment via aiVioletText + scheme-adaptive tokens"
      contains: "aiVioletText"
    - path: "MyHomeApp/Features/Shared/IconTile.swift"
      provides: "Adaptive glyph color token instead of inline #16161C"
  key_links:
    - from: "trend chart plot insets"
      to: "MyHomeApp/DesignSystem/DesignTokens.swift"
      via: "dishSlate fill + .environment(\\.colorScheme, .dark) on chart content"
      pattern: "dishSlate"
---

<objective>
Close out the feature-file light audit: put all three trend charts in slate
instrument windows (resolving RESEARCH Open Question 1 as YES per the user's
"ALL charts in deep dishes" intent), apply the D-15 violet treatment to the AI
Insight card, adapt the IconTile glyph, the Analytics range picker, and audit
the account color picker for light.

Purpose: after this plan every hardcoded feature-level color has a light-aware
path; only phase-level verification remains.
Output: fully adaptive feature layer, dark bit-identical.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-light-mode-support-neumorphic-redesign/17-CONTEXT.md
@.planning/phases/17-light-mode-support-neumorphic-redesign/17-RESEARCH.md
@.planning/phases/17-light-mode-support-neumorphic-redesign/17-PATTERNS.md
@MyHomeApp/Features/Analytics/AIInsightCard.swift
@MyHomeApp/Features/Overview/SpendOverTimeChart.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trend charts → slate instrument windows (D-12 boundary decision: ALL charts)</name>
  <files>MyHomeApp/Features/Overview/SpendOverTimeChart.swift, MyHomeApp/Features/Analytics/AnalyticsTrendChart.swift, MyHomeApp/Features/Assets/NetWorthTrendChart.swift, MyHomeApp/DesignSystem/DesignTokens.swift</files>
  <read_first>
    - MyHomeApp/Features/Analytics/AnalyticsTrendChart.swift (existing recessed plot inset ~:56-70: RoundedRectangle 18pt fillRecessed + hairline b.45/w.03 + point shadow b.5 + gridlines w.045 + #FFB43C line/glow — the reference recipe the other two adopt)
    - MyHomeApp/Features/Overview/SpendOverTimeChart.swift (chart on a .raised card, NO inset today; gridlines w.045; #FFB43C gradient at :81 + shadow at :118)
    - MyHomeApp/Features/Assets/NetWorthTrendChart.swift (compact 140pt chart, gridlines w.045; sits inside NetWorthCard near its donut dish)
    - .planning/phases/17-light-mode-support-neumorphic-redesign/17-RESEARCH.md §Pattern 3 boundary decision + Open Question 1
  </read_first>
  <action>
    PLANNER DECISION (recorded per RESEARCH Open Q1, within Claude's discretion): all three trend charts get instrument-window treatment — CONTEXT §specifics says the user chose "ALL charts in deep dishes".
    1. Promote #FFB43C to `DesignTokens.chartAmber` = Color.adaptive(light: "#FFB43C", dark: "#FFB43C") — same value both schemes (it only ever renders inside force-dark windows; the named token exists for auditability). Swap the 4 inline uses (SpendOverTimeChart :81/:118, AnalyticsTrendChart :92/:154). Add to DarkBitIdentityTests.
    2. AnalyticsTrendChart: plot-inset fill fillRecessed → dishSlate; inset hairline colors → the dish* tokens (Plan 05); wrap the `chart` content (not the inset background) in `.environment(\.colorScheme, .dark)` so gridlines w.045, point shadows, and the amber glow render as designed on slate.
    3. SpendOverTimeChart: ADD a recessed plot inset matching AnalyticsTrendChart's recipe exactly (RoundedRectangle cornerRadius 18 continuous, dishSlate fill, dish hairline stroke pair, 12pt padding) around the Chart; wrap the Chart in the same force-dark env. The card itself stays .raised (unchanged).
    4. NetWorthTrendChart: same treatment at its compact 140pt height — dishSlate inset + force-dark chart content; keep the empty-state text OUTSIDE the window (it is label text on the card).
    In dark, dishSlate == fillRecessed3 (#15151B) but AnalyticsTrendChart's inset is fillRecessed (#16161C) today — CAUTION: that swap would shift dark by 1 hex step and FAIL the diff. Resolution: add `dishSlateInset` = Color.adaptive(light: <same slate as dishSlate>, dark: "#16161C") for the trend insets (dark verbatim #16161C), and use dishSlateInset for AnalyticsTrendChart. For SpendOverTimeChart and NetWorthTrendChart the inset is NET-NEW chrome — new pixels in dark are a D-06 VIOLATION (dark must stay pixel-identical). Therefore the added inset on those two charts must render ONLY in light: gate the inset background/hairline on the resolved scheme (this is the third legitimate @Environment(\.colorScheme) read site, RESEARCH Anti-Patterns allowance "optionally NeuSurface if light geometry must diverge" — apply it in these two chart files with a comment citing D-06/D-12). In dark the charts render exactly as today; in light they gain the slate window.
  </action>
  <verify>
    <automated>xcodebuild test -scheme MyHome -destination "id=2F09365E-5099-490E-9484-B8788C53C816" -only-testing:MyHomeTests/DesignTokensTests -quiet && grep -rn '"#FFB43C"' MyHomeApp/Features --include="*.swift" | wc -l # expect 0</automated>
  </verify>
  <acceptance_criteria>
    - Zero inline "#FFB43C" in Features/ (all through DesignTokens.chartAmber)
    - Dark screenshot diff vs Plan 01 baselines exits 0 for tabs 0/3 and Analytics (the three charts' screens) — proves the light-only inset gating works
    - Light screenshots: all three trend charts show luminous amber lines inside slate windows
    - AnalyticsTrendChart uses dishSlateInset (dark #16161C verbatim); identity test covers it
  </acceptance_criteria>
  <done>All charts live in instrument windows in light; dark render untouched including the two net-new insets (light-only).</done>
</task>

<task type="auto">
  <name>Task 2: IconTile glyph + Analytics range picker + account color palette audit</name>
  <files>MyHomeApp/Features/Shared/IconTile.swift, MyHomeApp/Features/Analytics/AnalyticsView.swift, MyHomeApp/Features/Settings/EditAccountView.swift, MyHomeApp/DesignSystem/DesignTokens.swift</files>
  <read_first>
    - MyHomeApp/Features/Shared/IconTile.swift (:35 glyph Color(hex:"#16161C").opacity(0.85) on category fill — assumption A5: dark glyph may fail on the deepest light twins)
    - MyHomeApp/Features/Analytics/AnalyticsView.swift (range-picker pill rims/shadows w.06/b.30/b.45/b.45-w.03)
    - MyHomeApp/Features/Settings/EditAccountView.swift (:182 picker palette `Color(hex: item.hex)` — user-chosen ACCOUNT colors; :78 usage)
  </read_first>
  <action>
    1. IconTile: promote the glyph color to `DesignTokens.iconTileGlyph` adaptive token. Dark branch: #16161C alpha 0.85 verbatim. Light branch: the tile FILL is a deepened category twin (Plan 02), so a dark glyph loses contrast — start with #FFFFFF alpha 0.92 (light glyph on deep fill) and verify per-tile on device across all 11 categories (screenshot the category picker / Budgets manage list in light); if a white glyph fails on the lightest twin (catOther #475569 → white = 7.5:1, fine), tune. Add to identity tests.
    2. AnalyticsView range-picker pill: swap its inline white/black rim+shadow opacities to the Plan 04 neu* tokens (it is a standard on-surface control, NOT a dish — same recipe as NeuSecondaryButtonStyle rims). No geometry change.
    3. EditAccountView color picker: do NOT adapt stored `account.colorHex` data (user data, PATTERNS rule). Audit the PICKER PALETTE list (item.hex values) in light: screenshot the picker in light mode; if any palette swatch/selected state is illegible on the light surface, adjust only presentation chrome (selection ring/border colors → tokens), never the stored hex values. Record findings in SUMMARY.
  </action>
  <verify>
    <automated>xcodebuild test -scheme MyHome -destination "id=2F09365E-5099-490E-9484-B8788C53C816" -only-testing:MyHomeTests/DesignTokensTests -quiet && grep -v '^\s*//' MyHomeApp/Features/Shared/IconTile.swift | grep -c 'Color(hex:' # expect 0</automated>
  </verify>
  <acceptance_criteria>
    - IconTile has zero inline hex; iconTileGlyph in identity tests (dark = #16161C a0.85)
    - Dark diff vs baselines exits 0 (tabs 1/2/4 cover tiles + picker + range pill)
    - Light screenshots: 11 category tiles legible; range picker reads neumorphic on light; account picker audit recorded
  </acceptance_criteria>
  <done>Shared tiles and remaining on-surface controls adapt; user account colors untouched.</done>
</task>

<task type="auto">
  <name>Task 3: AI Insight card D-15 violet treatment</name>
  <files>MyHomeApp/Features/Analytics/AIInsightCard.swift</files>
  <read_first>
    - MyHomeApp/Features/Analytics/AIInsightCard.swift (aiVioletTop text :93/:97, edge gradient :134, neonGlow(aiVioletGlow, radius: 8) :150, stroke aiVioletGlow.opacity(0.5) :164, sparkle gradient white .9 → aiVioletGlow :172)
    - .planning/phases/17-light-mode-support-neumorphic-redesign/17-CONTEXT.md D-15 + 16-CONTEXT.md reference (violet is AI-only, Phase 16 D-04)
  </read_first>
  <action>
    Most of D-15 is already delivered by Plan 02's adaptive aiViolet* tokens + scheme-aware neonGlow. Finish the card:
    1. Text/label/sparkle sites using aiVioletTop (:93/:97) → `DesignTokens.aiVioletText` (dark branch = #C4A6FF verbatim → dark unchanged; light = #5B21B6, 7.19:1).
    2. Sparkle gradient white .9 → aiVioletGlow: keep in dark; in light the white leg on a light card is invisible — since the card surface is LIGHT (not a dish), express the gradient with aiVioletText/aiVioletGlow adaptive values so sparkles read dark-violet on light. If a scheme-conditional is unavoidable here, prefer token-level adaptation over an @Environment read (no new read sites).
    3. Edge glow (:150 neonGlow) and stroke (:164): neonGlow already yields the D-14 tinted drop-shadow in light with the deepened aiVioletGlow (#6D28D9) — verify the "subtle deep-violet tinted shadow" reads as edge-glow whisper, not smudge; tune the light branch of aiVioletGlow if needed.
    4. Breathing orb: keeps violet fill; reduced bloom in light comes from the neonGlow light branch automatically — verify visually via -openAnalytics light screenshots.
    5. Confirm violet remains AI-only: `grep -rn "aiViolet" MyHomeApp --include="*.swift"` must list only DesignTokens.swift + AIInsightCard.swift.
  </action>
  <verify>
    <automated>xcodebuild test -scheme MyHome -destination "id=2F09365E-5099-490E-9484-B8788C53C816" -only-testing:MyHomeTests/DesignTokensTests -quiet && grep -rln "aiViolet" MyHomeApp --include="*.swift" | wc -l # expect 2</automated>
  </verify>
  <acceptance_criteria>
    - aiViolet* referenced only by DesignTokens.swift and AIInsightCard.swift (violet stays AI-only)
    - Dark diff vs baselines exits 0 for dark-analytics.png
    - Light -openAnalytics screenshot: violet label/sparkles clearly legible (deep violet), edge glow a subtle tinted shadow, orb breathing with reduced bloom
  </acceptance_criteria>
  <done>AI card keeps its violet signature in light with contrast-passing deepened tones; dark untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| none new | Visual theming of feature views; account colorHex remains read-only user data. No new attack surface. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-07 | Tampering (regression) | Net-new light-only chart insets leaking into dark render (D-06) | mitigate | Scheme-gated inset rendering + dark screenshot diff on tabs 0/3 + Analytics in Task 1 acceptance |
</threat_model>

<verification>
- DesignTokensTests target green; diff_dark.py vs Plan 01 baselines exits 0 (all 6 screens)
- Zero inline "#FFB43C" and zero inline hex in IconTile
- aiViolet scope check returns exactly 2 files
</verification>

<success_criteria>
- All charts in instrument windows (D-12 "ALL charts" intent), dark bit-identical incl. net-new insets (D-06)
- IconTile/range picker/account palette adapted or audited (D-09)
- AI Insight card completes D-15 with violet staying AI-only
</success_criteria>

<output>
Create `.planning/phases/17-light-mode-support-neumorphic-redesign/17-06-SUMMARY.md` when done. Record the Open-Question-1 resolution (all charts windowed) and the account-palette audit outcome.
</output>
