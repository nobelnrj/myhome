---
phase: 17-light-mode-support-neumorphic-redesign
plan: 09
type: execute
wave: 5
depends_on: [17-08]
files_modified:
  - MyHomeApp/Features/Notes/AddNoteView.swift
  - MyHomeApp/Features/Notes/CalendarView.swift
  - MyHomeApp/Features/Notes/NotesListView.swift
  - MyHomeApp/Features/Notes/ReminderEditView.swift
  - MyHomeApp/Features/Notes/NoteRow.swift
  - MyHomeApp/Features/Budgets/EditBudgetSheet.swift
  - MyHomeApp/Features/Budgets/ManageCategoriesView.swift
  - MyHomeApp/Features/Budgets/BudgetsView.swift
  - MyHomeApp/Features/Overview/PinnedNoteCard.swift
  - MyHomeApp/Features/Overview/OverviewView.swift
  - MyHomeApp/Features/Assets/ReconcileView.swift
  - MyHomeApp/Features/Assets/AMFISchemePickerView.swift
  - MyHomeApp/Features/Assets/NPSSchemePickerView.swift
  - MyHomeApp/Features/Analytics/DeltaDrillDownSheet.swift
autonomous: true
requirements: [D-08]
must_haves:
  truths:
    - "In Notes, Budgets, Overview, Assets, and Analytics feature areas, accent-colored TEXT and ICONS use accentText while fill-role sites keep canary accent (D-08)"
    - "App-wide D-08 completion: zero .foregroundStyle(DesignTokens.accent) remains anywhere outside NeuSurface.swift (whose :288 swap belongs to Plan 04)"
    - "Dark rendering is unchanged by every swap — screenshots diff clean vs Plan 01 baselines (D-06)"
    - "Every accent call site in these areas has a recorded role classification — no site is silently skipped"
  artifacts:
    - path: "MyHomeApp/Features/Overview/OverviewView.swift"
      provides: "Overview accent text/icons on accentText"
      contains: "accentText"
  key_links:
    - from: "audited feature files"
      to: "MyHomeApp/DesignSystem/DesignTokens.swift"
      via: "text/icon-role sites reference DesignTokens.accentText"
      pattern: "DesignTokens\\.accentText"
---

<objective>
Execute the D-08 accent role-split call-site audit, part 2 of 2: Notes, Budgets,
Overview, Assets, and Analytics feature areas — then close the audit with the
app-wide gate (zero text-role canary outside the design system).

Purpose: completes D-08 across the app. After this plan the only remaining
accent-as-text site is NeuSurface.swift:288 (NeuSecondaryButtonStyle), swapped
in Plan 04 alongside that file's shadow-token work.
Output: 14 files audited; app-wide grep gate green; dark bit-identical.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-light-mode-support-neumorphic-redesign/17-CONTEXT.md
@.planning/phases/17-light-mode-support-neumorphic-redesign/17-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Role-split audit — Notes + Budgets areas (8 files)</name>
  <files>MyHomeApp/Features/Notes/AddNoteView.swift, MyHomeApp/Features/Notes/CalendarView.swift, MyHomeApp/Features/Notes/NotesListView.swift, MyHomeApp/Features/Notes/ReminderEditView.swift, MyHomeApp/Features/Notes/NoteRow.swift, MyHomeApp/Features/Budgets/EditBudgetSheet.swift, MyHomeApp/Features/Budgets/ManageCategoriesView.swift, MyHomeApp/Features/Budgets/BudgetsView.swift</files>
  <read_first>
    - .planning/phases/17-light-mode-support-neumorphic-redesign/17-RESEARCH.md §Pitfall 6 (role classification rules)
    - .planning/phases/17-light-mode-support-neumorphic-redesign/17-08-SUMMARY.md (part-1 classification precedents — apply the same verdicts to identical control types)
    - Each file at the cited lines below
  </read_first>
  <action>
    Same D-08 rules as Plan 17-08: text/icon role → `DesignTokens.accentText`; fill role (Toggle tracks, swipe backgrounds, shape fills, gradient stops, shadow/glow tints incl. `neonGlow(DesignTokens.accent...)`) → KEEP accent; other-token tints (negative etc.) → leave. Per-file checklist:
    | File:line | Current | Expected disposition |
    |-----------|---------|---------------------|
    | AddNoteView.swift:47 | .tint(accent) | classify per control; button/nav/picker text = accentText |
    | CalendarView.swift:378 | .tint(accent) | classify per control |
    | NotesListView.swift:73 | .tint(accent) | classify per control |
    | ReminderEditView.swift:228 | .tint(accent) | classify per control (Toggle = KEEP) |
    | ReminderEditView.swift:291, :400 | .foregroundStyle(accent) | → accentText |
    | NoteRow.swift:64 | .foregroundStyle(accent) | → accentText |
    | EditBudgetSheet.swift:87 | .tint(accent) | classify per control |
    | EditBudgetSheet.swift:146 | .tint(negative) | leave (adaptive token) |
    | ManageCategoriesView.swift:52, :139 | .tint(accent) | classify per control |
    | BudgetsView.swift:81, :260 | .foregroundStyle(accent) | → accentText |
    | BudgetsView.swift:263 | neonGlow(accent-family) | KEEP accent (glow tint = fill role, D-14 handles light) |
    Record every disposition in the SUMMARY table.
  </action>
  <verify>
    <automated>xcodebuild build -scheme MyHome -destination "id=2F09365E-5099-490E-9484-B8788C53C816" -quiet && grep -rn 'foregroundStyle(DesignTokens.accent)' MyHomeApp/Features/Notes MyHomeApp/Features/Budgets --include="*.swift" | wc -l # expect 0</automated>
  </verify>
  <acceptance_criteria>
    - Zero `.foregroundStyle(DesignTokens.accent)` in Features/Notes/ and Features/Budgets/
    - All 12 listed sites dispositioned in SUMMARY (none skipped); EditBudgetSheet:146 unmodified; BudgetsView:263 glow keeps accent
    - Build green
  </acceptance_criteria>
  <done>Notes + Budgets role-split with recorded classifications.</done>
</task>

<task type="auto">
  <name>Task 2: Role-split audit — Overview + Assets + Analytics (6 files) + app-wide gate</name>
  <files>MyHomeApp/Features/Overview/PinnedNoteCard.swift, MyHomeApp/Features/Overview/OverviewView.swift, MyHomeApp/Features/Assets/ReconcileView.swift, MyHomeApp/Features/Assets/AMFISchemePickerView.swift, MyHomeApp/Features/Assets/NPSSchemePickerView.swift, MyHomeApp/Features/Analytics/DeltaDrillDownSheet.swift</files>
  <read_first>
    - .planning/phases/17-light-mode-support-neumorphic-redesign/17-RESEARCH.md §Pitfall 6
    - Each file at the cited lines below
  </read_first>
  <action>
    Per-file checklist:
    | File:line | Current | Expected disposition |
    |-----------|---------|---------------------|
    | PinnedNoteCard.swift:91, :107 | .tint(accent) | classify per control |
    | PinnedNoteCard.swift:63 | .foregroundStyle(accent) | → accentText |
    | OverviewView.swift:329 | .tint(accent) | classify per control |
    | OverviewView.swift:328 | .foregroundStyle(accent) | → accentText |
    | OverviewView.swift:354 | neonGlow(...) | KEEP accent-family (glow tint = fill role) |
    | ReconcileView.swift:95 | .tint(accent) | classify per control |
    | AMFISchemePickerView.swift:80 | .tint(accent) | classify per control |
    | AMFISchemePickerView.swift:109 | .foregroundStyle(accent) | → accentText |
    | NPSSchemePickerView.swift:86 | .tint(accent) | classify per control |
    | NPSSchemePickerView.swift:124 | .foregroundStyle(accent) | → accentText |
    | DeltaDrillDownSheet.swift:80 | .foregroundStyle(accent) | → accentText |
    Then close the app-wide D-08 audit:
    1. Gate A: `grep -rn 'foregroundStyle(DesignTokens.accent)' MyHomeApp --include="*.swift" | grep -v NeuSurface.swift` must return 0 lines (NeuSurface:288 is Plan 04's swap, done with that file's shadow-token work).
    2. Gate B: `grep -rn '\.tint(DesignTokens.accent)' MyHomeApp --include="*.swift"` — every remaining line must correspond to a documented fill-role verdict in the 17-08 or 17-09 SUMMARY tables (union check; list the remaining sites + their verdicts in this SUMMARY).
    3. Dark identity gate for the whole plan: rebuild, dark appearance, no stored key, capture all 6 screens, diff_dark.py vs Plan 01 baselines — exit 0.
  </action>
  <verify>
    <automated>xcodebuild build -scheme MyHome -destination "id=2F09365E-5099-490E-9484-B8788C53C816" -quiet && grep -rn 'foregroundStyle(DesignTokens.accent)' MyHomeApp --include="*.swift" | grep -v NeuSurface.swift | wc -l # expect 0</automated>
  </verify>
  <acceptance_criteria>
    - Gate A returns 0; Gate B remainder fully cross-referenced to fill-role verdicts (table in SUMMARY)
    - All 11 listed sites dispositioned (none skipped); both neonGlow sites keep accent
    - diff_dark.py vs Plan 01 baselines exits 0 (all 6 screens)
  </acceptance_criteria>
  <done>D-08 audit complete app-wide (NeuSurface:288 explicitly delegated to Plan 04); dark unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| none new | Mechanical color-token substitution; no data flows, storage, or input surfaces. No new attack surface. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-10 | Tampering (regression) | Silent partial pass — a site skipped or misclassified | mitigate | Per-file checklist rows + app-wide Gate A/B cross-reference + dark diff gate |
</threat_model>

<verification>
- Build green; Gate A grep returns 0; Gate B remainder documented
- diff_dark.py vs Plan 01 baselines exits 0
- SUMMARY contains disposition rows for every listed site + the Gate B cross-reference table
</verification>

<success_criteria>
- D-08 role split complete for Notes/Budgets/Overview/Assets/Analytics (14 files) and closed app-wide
- Dark bit-identity maintained (D-06)
</success_criteria>

<output>
Create `.planning/phases/17-light-mode-support-neumorphic-redesign/17-09-SUMMARY.md` when done. Include the classification table and the Gate B cross-reference.
</output>
