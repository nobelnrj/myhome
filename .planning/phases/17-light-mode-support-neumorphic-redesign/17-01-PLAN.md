---
phase: 17-light-mode-support-neumorphic-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - MyHomeApp/Support/Color+Hex.swift
  - MyHomeTests/DesignTokensTests.swift
  - .planning/phases/17-light-mode-support-neumorphic-redesign/baselines/
  - .planning/phases/17-light-mode-support-neumorphic-redesign/diff_dark.py
autonomous: true
requirements: [D-06]
must_haves:
  truths:
    - "Baseline dark screenshots of all 5 tabs + Analytics exist BEFORE any token/code change touches rendering (D-06 ground truth)"
    - "A Color.adaptive(light:dark:) factory exists whose dark branch parses hex with the exact same sRGB math as the existing Color(hex:)"
    - "DesignTokensTests no longer uses hexString (which resolves against UITraitCollection.current and flaps by machine appearance)"
    - "A dark-bit-identity test table pins every current DesignTokens color to its pre-refactor hex"
  artifacts:
    - path: ".planning/phases/17-light-mode-support-neumorphic-redesign/baselines/dark-tab0.png"
      provides: "Pre-refactor dark Overview ground truth (orb region to be masked in diffs)"
    - path: ".planning/phases/17-light-mode-support-neumorphic-redesign/diff_dark.py"
      provides: "PIL masked-pixel diff script for D-06 verification"
    - path: "MyHomeApp/Support/Color+Hex.swift"
      provides: "UIColor(hex:alpha:) + Color.adaptive(light:lightAlpha:dark:darkAlpha:) factory"
      contains: "static func adaptive"
    - path: "MyHomeTests/DesignTokensTests.swift"
      provides: "DarkBitIdentityTests with full legacy hex table + adaptive factory resolution test"
      contains: "DarkBitIdentityTests"
  key_links:
    - from: "MyHomeTests/DesignTokensTests.swift"
      to: "MyHomeApp/Support/Color+Hex.swift"
      via: "Color.adaptive resolved with Color.resolve(in:) under explicit colorScheme environments"
      pattern: "resolve\\(in:"
---

<objective>
Establish the D-06 ground truth and the core adaptive-color mechanism BEFORE any
token is converted: capture baseline dark screenshots of every screen, add the
`Color.adaptive` dynamic-provider factory, and rewrite/extend DesignTokensTests
so dark bit-identity is machine-checkable from Plan 02 onward.

Purpose: D-06 (dark stays pixel-identical) is unverifiable without a pre-refactor
baseline and a deterministic resolve-based test harness. This plan is Wave 0 of
the phase — nothing that renders may change here.
Output: baselines/ PNG set, diff_dark.py, extended Color+Hex.swift, rewritten
DesignTokensTests.swift (all green against the UNCHANGED tokens).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-light-mode-support-neumorphic-redesign/17-CONTEXT.md
@.planning/phases/17-light-mode-support-neumorphic-redesign/17-RESEARCH.md
@MyHomeApp/Support/Color+Hex.swift
@MyHomeApp/DesignSystem/DesignTokens.swift
@MyHomeTests/DesignTokensTests.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture baseline dark screenshots of all screens (pre-refactor ground truth)</name>
  <files>.planning/phases/17-light-mode-support-neumorphic-redesign/baselines/, .planning/phases/17-light-mode-support-neumorphic-redesign/diff_dark.py</files>
  <read_first>
    - .planning/phases/17-light-mode-support-neumorphic-redesign/17-RESEARCH.md §"Simulator verification loop" and §Pitfall 3 (masking rationale)
  </read_first>
  <action>
    This MUST run against the current unmodified main tree — do it before Task 2/3 edits (those don't affect rendering, but ordering removes all doubt).

    Use iPhone 17 simulator UDID 2F09365E-5099-490E-9484-B8788C53C816, scheme MyHome, bundle id com.reojacob.myhome (per memory: iPhone 17, NOT iPhone 16). Steps:
    1. Boot simulator if needed; pin determinism: `xcrun simctl status_bar <UDID> override --time "9:41" --batteryState charged --batteryLevel 100` and `xcrun simctl ui <UDID> appearance dark`.
    2. Build once: `xcodebuild -scheme MyHome -destination "id=<UDID>" -derivedDataPath build/DerivedData build`; install the produced MyHome.app via `xcrun simctl install`.
    3. For N in 0 1 2 3 4 (tabs: Home/Overview, Expenses, Budgets, Notes, Settings): terminate app, `xcrun simctl launch <UDID> com.reojacob.myhome -seedSampleData -startTab <N>`, `sleep 3` (entrance cascade settles ~1s; 3s margin), screenshot to `.planning/phases/17-light-mode-support-neumorphic-redesign/baselines/dark-tab<N>.png`.
    4. Analytics: terminate, launch with `-seedSampleData -openAnalytics`, sleep 3, screenshot `baselines/dark-analytics.png`.
    5. Write `diff_dark.py` in the phase directory: PIL-based (PIL 11.3.0 installed; ImageMagick absent). Signature: `python3 diff_dark.py <before_dir> <after_dir>`. For each matching PNG pair: convert to RGB, compute ImageChops.difference, report PASS if getbbox() is None. For `dark-tab0.png` ONLY, first mask the hero-orb dish region with a black ellipse on both images (the dish is the 300pt NeuCircularWell in SpendBudgetCard, horizontally centered; determine the pixel bbox by inspecting the baseline image once and hardcode it as ORB_BBOX with a ~10px margin — the GlowParticleRing is TimelineView-driven and never byte-stable). Exit code 0 only if every screen passes.
    6. Sanity-check the script: run it against baselines vs baselines (identical dirs must PASS for tabs 1–4/analytics; tab0 passes trivially since same file).
  </action>
  <verify>
    <automated>ls .planning/phases/17-light-mode-support-neumorphic-redesign/baselines/ | grep -c "dark-" # expect 6, then: python3 .planning/phases/17-light-mode-support-neumorphic-redesign/diff_dark.py .planning/phases/17-light-mode-support-neumorphic-redesign/baselines .planning/phases/17-light-mode-support-neumorphic-redesign/baselines; echo "exit=$?" # expect exit=0</automated>
  </verify>
  <acceptance_criteria>
    - baselines/ contains exactly: dark-tab0.png, dark-tab1.png, dark-tab2.png, dark-tab3.png, dark-tab4.png, dark-analytics.png — each non-empty and visibly dark-theme (spot-open one)
    - Screenshots were captured from a build of the UNMODIFIED working tree (`git status` shows no MyHomeApp/ changes at capture time)
    - diff_dark.py self-diff (baselines vs baselines) exits 0
    - Status bar in every PNG reads 9:41 (pinned)
  </acceptance_criteria>
  <done>Six pinned-status-bar dark baselines + a working masked diff script exist in the phase directory, captured before any source change.</done>
</task>

<task type="auto">
  <name>Task 2: Add UIColor(hex:alpha:) + Color.adaptive factory to Color+Hex.swift</name>
  <files>MyHomeApp/Support/Color+Hex.swift</files>
  <read_first>
    - MyHomeApp/Support/Color+Hex.swift (the existing 6-digit sRGB parser at init(hex:) and hexString — both must remain byte-for-byte unchanged; hexString is still used by Account.colorHex user data)
    - .planning/phases/17-light-mode-support-neumorphic-redesign/17-RESEARCH.md §Pattern 1
  </read_first>
  <action>
    Extend (do NOT create a new file — pbxproj has no synchronized groups) with two additions per D-06's mechanism (RESEARCH Pattern 1, empirically verified):
    1. `extension UIColor { convenience init(hex: String, alpha: CGFloat = 1) }` — parse with the IDENTICAL 6-digit contract as the existing `Color.init(hex:)`: trim whitespace, strip "#", require count == 6 and UInt64(radix: 16), components = ((value >> 16) & 0xFF)/255 etc., failure fallback = systemGray equivalent of the existing `.gray` fallback (use UIColor(red:green:blue:alpha:) with the same component math; on parse failure use `.gray`'s components — match Color.gray by constructing UIColor(Color.gray)). Import UIKit in this file if not already imported.
    2. `extension Color { static func adaptive(light: String, lightAlpha: CGFloat = 1, dark: String, darkAlpha: CGFloat = 1) -> Color }` — returns `Color(uiColor: UIColor { trait in trait.userInterfaceStyle == .dark ? UIColor(hex: dark, alpha: darkAlpha) : UIColor(hex: light, alpha: lightAlpha) })`.
    The dark branch of every future token will pass today's hex verbatim; component-identity with the legacy `Color(hex:)` path is enforced by Task 3's tests (RESEARCH Pitfall 2: same extended-sRGB init, but the test is the authority, not the reasoning).
  </action>
  <verify>
    <automated>xcodebuild -scheme MyHome -destination "id=2F09365E-5099-490E-9484-B8788C53C816" build -quiet && grep -v '^\s*//' MyHomeApp/Support/Color+Hex.swift | grep -c "static func adaptive"</automated>
  </verify>
  <acceptance_criteria>
    - Build succeeds; no new .swift file created (git status shows only modifications)
    - Existing `Color.init(hex:)` and `hexString` implementations are textually unchanged (git diff shows only additions to the file)
    - `Color.adaptive(light:"#FFFFFF", dark:"#000000")` compiles and returns a Color (used by Task 3's test)
  </acceptance_criteria>
  <done>Adaptive factory exists in Color+Hex.swift with the same parsing contract as Color(hex:), verified by build + Task 3 tests.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite DesignTokensTests — resolve-based assertions + full dark-bit-identity table</name>
  <files>MyHomeTests/DesignTokensTests.swift</files>
  <read_first>
    - MyHomeTests/DesignTokensTests.swift (current accentColorMatchesSpec uses hexString — RESEARCH Pitfall 1: resolves against UITraitCollection.current, flaps by machine appearance under dynamic colors)
    - .planning/phases/17-light-mode-support-neumorphic-redesign/17-RESEARCH.md §Code Examples (D-06 bit-identity test + contrastRatio helper)
    - MyHomeApp/DesignSystem/DesignTokens.swift (source of the legacy hex table below — confirm values match before pinning)
  </read_first>
  <action>
    Extend the existing file (no new test file — avoids pbxproj edits). Swift Testing (`import Testing`), iOS 17 `Color.resolve(in:)` API:

    1. REWRITE `accentColorMatchesSpec`: replace the hexString assertion with `DesignTokens.accent.resolve(in: darkEnv) == Color(hex: "#FFD60A").resolve(in: darkEnv)` where `darkEnv` is `EnvironmentValues()` with `colorScheme = .dark`. Keep shadowRaisedSpec/radiusCard/tabBarClearance tests unchanged.
    2. ADD `@MainActor struct DarkBitIdentityTests` with a parameterized `@Test(arguments:)` over the FULL legacy table — every color token in DesignTokens.swift today, expected = the legacy expression resolved in the same dark env. Pin these exact pairs (verbatim from current source):
       bgCanvas #1C1C23; surfaceRaised #1F1F27; surfaceRaisedStrong #22222C; surfaceRaisedTop #24242D; surfaceRaisedBottom #1B1B21; surfaceRaisedStrongTop #282833; surfaceRaisedStrongBottom #1E1E26; surfaceElevatedControl #262630; fillRecessed #16161C; fillRecessed2 #191920; fillRecessed3 #15151B; accent #FFD60A; accentOnYellow #1A1404; positive #34E29B; negative #FF6B6B; orange #FFB020; aiVioletTop #C4A6FF; aiVioletBottom #7C5CFF; aiVioletGlow #8B5CF6; label #ECEDF4; catGroceries #2DD4BF; catDining #FB923C; catFuel #F472B6; catUtilities #7DD3FC; catRent #818CF8; catAuto #38BDF8; catShopping #E879F9; catHealth #A78BFA; catSubscriptions #22D3EE; catEntertainment #C084FC; catOther #94A3B8.
       Alpha-carrying tokens compare against the legacy composite expression resolved in dark env: accentSoft vs Color(hex:"#FFD60A").opacity(0.16); label2 vs Color(hex:"#DCDFEE").opacity(0.56); label3 vs same @0.32; label4 vs same @0.16; separatorHairline vs Color.white.opacity(0.05); separatorEdge vs Color.black.opacity(0.30); glassBorder vs Color.white.opacity(0.025).
       These tests pass trivially NOW (tokens are still the legacy expressions) and become the D-06 gate the moment Plan 02 converts them.
    3. ADD `AdaptiveFactoryTests`: `Color.adaptive(light: "#FFFFFF", dark: "#000000")` resolves to white components under a light env and black components under a dark env (proves assumption A1 on iOS — environment-driven resolution). Also assert `Color.adaptive(light: "#E3E6EE", dark: "#1C1C23").resolve(in: darkEnv) == Color(hex: "#1C1C23").resolve(in: darkEnv)` (factory dark branch == legacy parser output, RESEARCH Pitfall 2 / assumption A3).
    4. ADD the `contrastRatio(_:_:)` WCAG relative-luminance helper (exact formula from RESEARCH §Code Examples) as a file-internal function — consumed by Plan 02's ContrastTests. Add one self-test: white-on-black ratio == 21.0 ±0.01.
  </action>
  <verify>
    <automated>xcodebuild test -scheme MyHome -destination "id=2F09365E-5099-490E-9484-B8788C53C816" -only-testing:MyHomeTests/DesignTokensTests -quiet</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "hexString" MyHomeTests/DesignTokensTests.swift` returns 0
    - DarkBitIdentityTests covers all 38 color tokens (31 plain + 7 alpha-composite) — count the arguments entries
    - All DesignTokensTests pass on the unmodified tokens (green pre-conversion is the point)
    - AdaptiveFactoryTests proves light/dark env-driven resolution on iOS (A1) and factory-vs-legacy dark equality (A3)
  </acceptance_criteria>
  <done>Deterministic resolve-based test harness green against unchanged tokens; D-06 gate armed for Plan 02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| none new | Pure test/tooling scaffolding + additive color factory; no data flows, network, or input surfaces added |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Tampering (trivial) | UIColor(hex:alpha:) parse of malformed hex | mitigate | Same guard-and-fallback contract as existing Color(hex:) — malformed input degrades to gray, never crashes |
</threat_model>

<verification>
- Full quick run green: `xcodebuild test -scheme MyHome -destination "id=2F09365E-5099-490E-9484-B8788C53C816" -only-testing:MyHomeTests/DesignTokensTests -quiet`
- baselines/ has 6 dark PNGs captured pre-change; diff_dark.py self-diff exits 0
- No rendering-path source file modified (only Color+Hex.swift additions + test file)
</verification>

<success_criteria>
- D-06 ground truth (screenshots + pinned legacy hex table) exists and is committed
- Color.adaptive factory proven environment-driven on iOS via unit test (closes assumption A1)
- Zero hexString-based token assertions remain
</success_criteria>

<output>
Create `.planning/phases/17-light-mode-support-neumorphic-redesign/17-01-SUMMARY.md` when done. Record the ORB_BBOX pixel values chosen for the mask.
</output>
