---
phase: 17-light-mode-support-neumorphic-redesign
plan: 02
type: execute
wave: 2
depends_on: [17-01]
files_modified:
  - MyHomeApp/DesignSystem/DesignTokens.swift
  - MyHomeTests/DesignTokensTests.swift
autonomous: true
requirements: [D-04, D-06, D-08, D-09, D-10, D-13, D-14, D-15]
must_haves:
  truths:
    - "Every DesignTokens color is an adaptive light/dark pair whose dark branch is the pre-refactor hex verbatim (D-06)"
    - "DarkBitIdentityTests from Plan 01 stay green after conversion — dark resolution is component-identical (D-06)"
    - "Role-split accent exists: accent stays canary in both schemes (fills), new accentText is dark amber in light / canary in dark (D-08)"
    - "All 11 category colors and the semantic trio have deepened light twins with preserved hue identity (D-09, D-10)"
    - "A dishSlate token exists: light #3E4250-family deep slate, dark #15151B verbatim (D-13)"
    - "neonGlow renders the exact current two-layer bloom in dark and a single faint tinted drop-shadow in light (D-14)"
    - "Deepened AI-violet light twins exist including an aiVioletText pair passing 4.5:1 (D-15)"
  artifacts:
    - path: "MyHomeApp/DesignSystem/DesignTokens.swift"
      provides: "All-adaptive token palette + accentText + dishSlate + NeonGlowModifier"
      contains: "Color.adaptive"
    - path: "MyHomeTests/DesignTokensTests.swift"
      provides: "ContrastTests enforcing WCAG floors on locked light pairs"
      contains: "ContrastTests"
  key_links:
    - from: "MyHomeApp/DesignSystem/DesignTokens.swift"
      to: "MyHomeApp/Support/Color+Hex.swift"
      via: "every token built through Color.adaptive"
      pattern: "Color\\.adaptive\\(light:"
    - from: "MyHomeTests/DesignTokensTests.swift"
      to: "MyHomeApp/DesignSystem/DesignTokens.swift"
      via: "DarkBitIdentityTests resolve every token in dark env"
      pattern: "resolve\\(in:"
---

<objective>
Convert every DesignTokens color to an adaptive light/dark pair (dark = current
hex verbatim), add the net-new role-split and dish tokens (accentText,
aiVioletText, dishSlate), make neonGlow scheme-aware, and lock the light palette
floors with WCAG contrast tests.

Purpose: this single file is consumed by 62 files — converting it retrofits most
of the app in one move (D-04/08/09/10/13/15 at token level, D-14 glow language).
Output: fully adaptive DesignTokens.swift with dark bit-identity proven by the
Plan 01 gate + masked screenshot diff.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-light-mode-support-neumorphic-redesign/17-CONTEXT.md
@.planning/phases/17-light-mode-support-neumorphic-redesign/17-RESEARCH.md
@.planning/phases/17-light-mode-support-neumorphic-redesign/17-PATTERNS.md
@MyHomeApp/DesignSystem/DesignTokens.swift
@MyHomeApp/Support/Color+Hex.swift
@MyHomeTests/DesignTokensTests.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert all color tokens to adaptive pairs + add accentText/aiVioletText/dishSlate</name>
  <files>MyHomeApp/DesignSystem/DesignTokens.swift</files>
  <read_first>
    - MyHomeApp/DesignSystem/DesignTokens.swift (lines 12-67 — the exact static lets being converted; dark branches must be these values verbatim)
    - .planning/phases/17-light-mode-support-neumorphic-redesign/17-RESEARCH.md §WCAG Contrast Table (light-value floors) and §Pattern 1
    - MyHomeApp/Support/Color+Hex.swift (the adaptive factory signature from Plan 01)
  </read_first>
  <action>
    Replace each `static let X = Color(hex: "#...")` with `static let X = Color.adaptive(light: "<light>", dark: "<current hex verbatim>")`. Call sites in 62 consumer files are untouched. Light values below are DIRECTIONAL starting points (CONTEXT: light hexes are Claude's discretion, tuned on device in Plans 04-07); dark values are LOCKED verbatim:

    | Token | light (start) | dark (verbatim, LOCKED) |
    |-------|---------------|-------------------------|
    | bgCanvas | #E3E6EE | #1C1C23 |
    | surfaceRaised | #E9ECF3 | #1F1F27 |
    | surfaceRaisedStrong | #EDF0F6 | #22222C |
    | surfaceRaisedTop | #EFF2F8 | #24242D |
    | surfaceRaisedBottom | #E2E5ED | #1B1B21 |
    | surfaceRaisedStrongTop | #F3F5FA | #282833 |
    | surfaceRaisedStrongBottom | #E6E9F1 | #1E1E26 |
    | surfaceElevatedControl | #EDEFF6 | #262630 |
    | fillRecessed | #D7DBE6 | #16161C |
    | fillRecessed2 | #DADEE8 | #191920 |
    | fillRecessed3 | #D4D8E3 | #15151B |
    | accent | #FFD60A (fills keep canary, D-08) | #FFD60A |
    | accentOnYellow | #1A1404 | #1A1404 |
    | positive | #047857 | #34E29B |
    | negative | #B91C1C | #FF6B6B |
    | orange | #B45309 | #FFB020 |
    | aiVioletTop | #6D28D9 | #C4A6FF |
    | aiVioletBottom | #4C1D95 | #7C5CFF |
    | aiVioletGlow | #6D28D9 | #8B5CF6 |
    | label | #23252E | #ECEDF4 |
    | catGroceries | #0F766E | #2DD4BF |
    | catDining | #C2410C | #FB923C |
    | catFuel | #BE185D | #F472B6 |
    | catUtilities | #0369A1 | #7DD3FC |
    | catRent | #4338CA | #818CF8 |
    | catAuto | #0284C7 | #38BDF8 |
    | catShopping | #A21CAF | #E879F9 |
    | catHealth | #6D28D9 | #A78BFA |
    | catSubscriptions | #0E7490 | #22D3EE |
    | catEntertainment | #7E22CE | #C084FC |
    | catOther | #475569 | #94A3B8 |

    Alpha-carrying tokens use the baked-alpha factory form (per-scheme alpha, RESEARCH Pattern 1):
    - accentSoft: light #FFD60A alpha 0.22 (handoff tokens.jsx light recipe) / dark #FFD60A alpha 0.16
    - label2: light #23252E alpha 0.62 / dark #DCDFEE alpha 0.56
    - label3: light #23252E alpha 0.40 / dark #DCDFEE alpha 0.32
    - label4: light #23252E alpha 0.22 / dark #DCDFEE alpha 0.16
    - separatorHairline: light #000000 alpha 0.07 / dark #FFFFFF alpha 0.05
    - separatorEdge: light #000000 alpha 0.12 / dark #000000 alpha 0.30
    - glassBorder: light #000000 alpha 0.04 / dark #FFFFFF alpha 0.025

    ADD net-new tokens (with doc comments naming the decision):
    - `accentText` = Color.adaptive(light: "#755C00", dark: "#FFD60A") — D-08 role split; text/icon accent; 5.12:1 on #E3E6EE per RESEARCH table. Dark branch identical to accent so dark rendering cannot shift when call sites switch.
    - `aiVioletText` = Color.adaptive(light: "#5B21B6", dark: "#C4A6FF") — D-15 text/sparkle violet; 7.19:1 on light canvas. Dark branch = current aiVioletTop value (text uses aiVioletTop today).
    - `dishSlate` = Color.adaptive(light: "#3E4250", dark: "#15151B") — D-13 instrument-window dish interior; dark branch = fillRecessed3 verbatim (dish fill today). Consumed by Plans 05/06.

    Do NOT touch ShadowSpec, radii, spacing, springs, Haptics, EntranceModifier, or geometry of any kind (RESEARCH Pattern 4: geometry frozen). D-09/D-11 note in doc comments: category light twins apply on light SURFACES; inside force-dark instrument windows the same tokens auto-resolve to the original luminous dark values.
  </action>
  <verify>
    <automated>xcodebuild test -scheme MyHome -destination "id=2F09365E-5099-490E-9484-B8788C53C816" -only-testing:MyHomeTests/DesignTokensTests -quiet && grep -v '^\s*//' MyHomeApp/DesignSystem/DesignTokens.swift | grep -c 'Color(hex:' # expect 0</automated>
  </verify>
  <acceptance_criteria>
    - DarkBitIdentityTests (all 38 tokens) pass unchanged — the D-06 unit gate
    - Non-comment `Color(hex:` count in DesignTokens.swift is 0 (all tokens go through Color.adaptive)
    - accentText, aiVioletText, dishSlate exist with the exact dark branches above (#FFD60A, #C4A6FF, #15151B)
    - No change to any non-color declaration in the file (git diff touches only color token lines + new tokens)
  </acceptance_criteria>
  <done>All DesignTokens colors adaptive; dark resolution bit-identical per unit gate; new role-split/dish tokens available for downstream plans.</done>
</task>

<task type="auto">
  <name>Task 2: neonGlow → scheme-aware NeonGlowModifier (dark bloom verbatim, light drop-shadow)</name>
  <files>MyHomeApp/DesignSystem/DesignTokens.swift</files>
  <read_first>
    - MyHomeApp/DesignSystem/DesignTokens.swift lines 136-144 (current neonGlow free function — the exact two-shadow bloom that becomes the .dark branch verbatim) and lines 150-169 (EntranceModifier — the ViewModifier + @Environment shape to copy)
    - .planning/phases/17-light-mode-support-neumorphic-redesign/17-RESEARCH.md §Pattern 5
  </read_first>
  <action>
    Convert `func neonGlow(_ color: Color, radius: CGFloat = 8, intensity: Double = 1)` into a private `NeonGlowModifier: ViewModifier` with `@Environment(\.colorScheme) private var scheme`, keeping the PUBLIC extension signature `.neonGlow(_:radius:intensity:)` byte-compatible so zero call sites change (verified call sites: AIInsightCard:150, BudgetProgressView:50, BudgetsView:263, OverviewView:354).
    - scheme == .dark branch: EXACTLY the current two shadows — color.opacity(0.55 * intensity) radius radius*0.55, then color.opacity(0.32 * intensity) radius radius*1.5 (D-06: dark path unchanged).
    - light branch (D-14): single tinted drop-shadow — starting values color.opacity(0.28 * intensity), radius radius*0.7, y offset 2. Directional; tuned on device in Plans 04-07. Doc comment: "D-14 — whisper of the neon language on light; never a rendering smudge".
    Synergy note in doc comment: inside force-dark dish subtrees (Plan 05) the environment reads .dark, so full bloom is preserved automatically (D-11/D-12).
    This is one of only THREE legitimate @Environment(\.colorScheme) read sites in the app (RESEARCH Anti-Patterns) — do not add reads elsewhere.
  </action>
  <verify>
    <automated>xcodebuild build -scheme MyHome -destination "id=2F09365E-5099-490E-9484-B8788C53C816" -quiet && grep -rn "neonGlow(" MyHomeApp --include="*.swift" | grep -v "DesignTokens.swift" | wc -l # expect 4 unchanged call sites</automated>
  </verify>
  <acceptance_criteria>
    - Extension signature `func neonGlow(_ color: Color, radius: CGFloat = 8, intensity: Double = 1)` unchanged (grep exact string in DesignTokens.swift)
    - Dark branch shadow constants are exactly 0.55/0.55x and 0.32/1.5x (source assertion via git diff)
    - Zero call-site files modified by this task
    - App builds clean
  </acceptance_criteria>
  <done>neonGlow adapts per scheme with a verbatim dark bloom; call sites untouched.</done>
</task>

<task type="auto">
  <name>Task 3: ContrastTests + full dark-identity screenshot diff (D-06 render gate #1)</name>
  <files>MyHomeTests/DesignTokensTests.swift</files>
  <read_first>
    - MyHomeTests/DesignTokensTests.swift (contrastRatio helper + DarkBitIdentityTests from Plan 01)
    - .planning/phases/17-light-mode-support-neumorphic-redesign/17-RESEARCH.md §WCAG Contrast Table (floors) and §"Simulator verification loop"
  </read_first>
  <action>
    1. ADD `ContrastTests` (Swift Testing) resolving pairs in an explicit LIGHT EnvironmentValues and asserting WCAG floors with the contrastRatio helper:
       - accentText vs bgCanvas ≥ 4.5 (small text, D-08)
       - positive, negative, orange vs bgCanvas ≥ 4.5 each (small delta text, D-10)
       - aiVioletText vs bgCanvas ≥ 4.5 (D-15)
       - label vs bgCanvas ≥ 7.0 (primary text, currently 12.23)
       - each of the 11 cat* tokens vs bgCanvas ≥ 3.0 (icons/tiles/fills, D-09; note: where category color renders SMALL TEXT on light, later plans may deepen further)
       - accentOnYellow vs accent ≥ 4.5 in light env (CTA text on canary, D-08 fill rule)
       - label vs dishSlate ≥ 4.5 in light env (dish readouts on slate, D-13)
       - accent vs dishSlate ≥ 4.5 in light env (canary inside dishes, D-12)
       These tests LOCK the palette floors: future device tuning may change hexes but must keep floors green.
    2. Rebuild + reinstall the app, then re-run the Plan 01 screenshot loop in DARK appearance (same UDID 2F09365E-5099-490E-9484-B8788C53C816, pinned status bar, -seedSampleData, tabs 0-4 + -openAnalytics) into a temp dir, and run `python3 .planning/phases/17-light-mode-support-neumorphic-redesign/diff_dark.py <baselines> <temp>` — every screen must PASS (orb masked on tab0). If any pixel drifts: the resolve tests passed but rendering changed → investigate opacity-composite tokens first (RESEARCH Pitfall 2), fix, re-diff. Do NOT proceed with a failing diff.
  </action>
  <verify>
    <automated>xcodebuild test -scheme MyHome -destination "id=2F09365E-5099-490E-9484-B8788C53C816" -only-testing:MyHomeTests/DesignTokensTests -quiet && python3 .planning/phases/17-light-mode-support-neumorphic-redesign/diff_dark.py .planning/phases/17-light-mode-support-neumorphic-redesign/baselines /tmp/17-02-dark-after 2>/dev/null; echo "diff exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - ContrastTests green with the floors listed (≥4.5 text / ≥3.0 non-text)
    - diff_dark.py exits 0 across all 6 screens against Plan 01 baselines (dark render bit-identical, orb masked)
    - Full DesignTokensTests target green (identity + contrast + factory + legacy specs)
  </acceptance_criteria>
  <done>Light palette floors are executable regression tests; dark rendering proven pixel-identical at both unit and render level after the token conversion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| none new | Pure visual token refactor; no new inputs, storage, or network. No new attack surface. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-02 | DoS (trivial) | Dynamic-provider closures executed per trait resolution | accept | Pure value computation, no I/O; identical pattern to OS asset-catalog colors |
</threat_model>

<verification>
- `xcodebuild test ... -only-testing:MyHomeTests/DesignTokensTests -quiet` green
- Masked dark screenshot diff vs Plan 01 baselines exits 0 (all 6 screens)
- Non-comment `Color(hex:` in DesignTokens.swift == 0
</verification>

<success_criteria>
- 38 tokens adaptive + 3 net-new tokens (accentText D-08, aiVioletText D-15, dishSlate D-13); dark bit-identity double-gated (D-06)
- neonGlow scheme-aware with verbatim dark bloom (D-14)
- WCAG floors locked as tests for D-08/09/10/15 light values
</success_criteria>

<output>
Create `.planning/phases/17-light-mode-support-neumorphic-redesign/17-02-SUMMARY.md` when done.
</output>
