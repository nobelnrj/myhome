---
phase: 17-light-mode-support-neumorphic-redesign
plan: 07
type: execute
wave: 9
depends_on: [17-06]
files_modified:
  - MyHomeApp/DesignSystem/DesignTokens.swift
  - .planning/phases/17-light-mode-support-neumorphic-redesign/baselines/
  - .planning/phases/17-light-mode-support-neumorphic-redesign/17-VALIDATION.md
autonomous: true
requirements: [D-01, D-02, D-05, D-06, D-12, D-14]
must_haves:
  truths:
    - "Full test suite green; every dark screen (5 tabs + Analytics) is pixel-identical to the pre-refactor baselines with the orb region masked (D-06 final gate)"
    - "Live theme flip via the Settings row re-renders every surface immediately — including the drawingGroup-rasterized dish chrome (assumption A7)"
    - "Sheets, keyboard, Face ID lock overlay, and launch experience follow the resolved scheme in pinned-Light + system-dark (assumption A2, D-01)"
    - "A final light-mode review set of all screens exists for the user's end-of-phase sign-off"
  artifacts:
    - path: ".planning/phases/17-light-mode-support-neumorphic-redesign/baselines/light-tab0.png"
      provides: "Final light review set (6 screens) alongside the dark identity evidence"
    - path: ".planning/phases/17-light-mode-support-neumorphic-redesign/17-VALIDATION.md"
      provides: "Validation contract updated with final green statuses"
  key_links:
    - from: ".planning/phases/17-light-mode-support-neumorphic-redesign/diff_dark.py"
      to: "baselines/"
      via: "final masked diff run — the D-06 render verdict"
      pattern: "diff_dark"
---

<objective>
Run the phase gate: full-suite tests, the definitive dark bit-identity screenshot
sweep, and the light-mode integration smoke (live flip, sheets, lock overlay,
Reduce Motion) — then assemble the light review set for the user's end-of-phase
visual sign-off (config: human_verify_mode = end-of-phase).

Purpose: D-06 is the phase's hard gate and D-01/02 plumbing has net-new
combinations (pinned scheme vs system) that only an integration pass can prove.
Output: green suite, passing diffs, light review set, updated VALIDATION.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-light-mode-support-neumorphic-redesign/17-CONTEXT.md
@.planning/phases/17-light-mode-support-neumorphic-redesign/17-RESEARCH.md
@.planning/phases/17-light-mode-support-neumorphic-redesign/17-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Final dark bit-identity sweep + full test suite (D-06 phase gate)</name>
  <files>.planning/phases/17-light-mode-support-neumorphic-redesign/baselines/</files>
  <read_first>
    - .planning/phases/17-light-mode-support-neumorphic-redesign/diff_dark.py (mask + pass criteria)
    - Prior plan SUMMARYs 17-02 … 17-06, 17-08, 17-09 (any recorded drift investigations or tuned values)
  </read_first>
  <action>
    1. Full suite: `xcodebuild test -scheme MyHome -destination "id=2F09365E-5099-490E-9484-B8788C53C816" -quiet` — must be fully green (76+ test files; LockSettings/LockState are @Suite(.serialized), do not parallelize around them).
    2. Definitive dark sweep: simulator appearance dark, NO stored theme key (delete the app or `xcrun simctl spawn <UDID> defaults delete com.reojacob.myhome appearanceTheme` before launch — proves D-02 default-System path resolves dark under a dark system), pinned status bar, capture all 6 screens exactly as Plan 01 did, run diff_dark.py vs baselines. Every screen must PASS.
    3. Repeat the sweep once with the theme PINNED to Dark via `defaults write com.reojacob.myhome appearanceTheme dark` — diff must also PASS (pinned-dark == system-dark == baseline, D-01).
    4. Save the after-shots and diff output log into baselines/ as evidence (dark-final-*.png, diff-final.log).
    Any failure: fix in the owning file, re-run identity unit tests, re-sweep. Do not mark done with a failing screen.
  </action>
  <verify>
    <automated>xcodebuild test -scheme MyHome -destination "id=2F09365E-5099-490E-9484-B8788C53C816" -quiet && python3 .planning/phases/17-light-mode-support-neumorphic-redesign/diff_dark.py .planning/phases/17-light-mode-support-neumorphic-redesign/baselines /tmp/17-07-dark-final; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - Full MyHomeTests suite green (not just DesignTokensTests)
    - diff_dark.py exits 0 for BOTH the system-dark run and the pinned-dark run (12 comparisons total)
    - Evidence files committed: dark-final-*.png + diff-final.log
  </acceptance_criteria>
  <done>Dark mode proven pixel-identical end-to-end in both resolution paths; suite green.</done>
</task>

<task type="auto">
  <name>Task 2: Light integration smoke + final light review set (A2/A7, D-01, D-14)</name>
  <files>.planning/phases/17-light-mode-support-neumorphic-redesign/baselines/, MyHomeApp/DesignSystem/DesignTokens.swift</files>
  <read_first>
    - .planning/phases/17-light-mode-support-neumorphic-redesign/17-RESEARCH.md §Pitfall 5 (drawingGroup + live flips) and §Pitfall 7 (sheet/cover propagation)
    - .planning/phases/17-light-mode-support-neumorphic-redesign/17-CONTEXT.md §Claude's Discretion (crossfade only if jarring; must honor Reduce Motion)
  </read_first>
  <action>
    1. Light review set: simulator appearance light, no stored key (System default → light, D-02), pinned status bar, -seedSampleData; capture light-tab0..4.png + light-analytics.png into baselines/. Self-review each against the phase bar: cool-gray sculpted canvas (D-04/05), slate instrument windows with luminous content (D-12/13), amber accent text (D-08), deepened category/semantic colors (D-09/10), drop-shadow glow whispers (D-14). Fix-and-recapture any obvious misses (final light hex tweaks allowed; dark branches untouchable — re-run Task 1's diff if DesignTokens.swift changes).
    2. Live-flip check (A7): launch to Settings (-startTab 4), screenshot; since simctl cannot tap, flip the theme by relaunching with the OTHER appearance AND separately by toggling `defaults write com.reojacob.myhome appearanceTheme light` then `dark` between launches — confirm dish chrome (drawingGroup) renders the correct scheme on every combination (tab 0 screenshots). Record that true in-app tap-flip is covered by the human check below.
    3. Sheet/cover propagation (A2): with theme PINNED light and simulator appearance dark, screenshot one sheet flow if reachable via launch args; otherwise mark as human-check item.
    4. Crossfade discretion: leave the default environment-change transition unless evidence of jarring flips emerged; record the decision.
    5. Update 17-VALIDATION.md: set statuses green, wave_0_complete: true, nyquist_compliant: true, approval date.
  </action>
  <verify>
    <automated>ls .planning/phases/17-light-mode-support-neumorphic-redesign/baselines/ | grep -c "light-" # expect >=6</automated>
    <human-check>
      End-of-phase visual sign-off on device (both themes, all 5 tabs + Analytics):
      1. Set iPhone to Light; open MyHome (Appearance=System) — whole app renders the light neumorphic theme; walk all 5 tabs + Analytics.
      2. Settings → Appearance: tap Light/Dark/System — app re-themes instantly, dishes update, no stale rasters (A7 tap path).
      3. In Light: hero orb / donut / rings / pill gauges sit in deep-slate windows with glowing content (not black holes, D-13); accent text is dark amber, CTAs stay canary (D-08); AI card violet legible (D-15).
      4. Open any Add/Edit sheet in both pinned themes — sheet matches the app theme (A2); keyboard appearance follows; Face ID lock overlay renders correctly in light.
      5. Pin Dark: confirm the app looks exactly as it did before this phase (D-06).
      Reply "approved" or describe issues.
    </human-check>
  </verify>
  <acceptance_criteria>
    - baselines/ contains ≥6 light-*.png review screenshots
    - Scheme-combination matrix recorded in SUMMARY: {system-light, system-dark, pinned-light-on-dark-system, pinned-dark-on-light-system} × tab0 all render the correct palette
    - 17-VALIDATION.md frontmatter shows wave_0_complete: true and nyquist_compliant: true
    - Crossfade decision + any deferred human-check items explicitly listed in SUMMARY
  </acceptance_criteria>
  <done>Light mode integration-proven and review set assembled; user sign-off requested at phase end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| none new | Verification-only plan; theme preference in UserDefaults is non-sensitive. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-08 | Information disclosure | App-switcher privacy blur in light mode | mitigate | Included in the smoke matrix: verify scenePhase blur renders over light surfaces (existing mechanism is scheme-independent) |
</threat_model>

<verification>
- Full suite green; dark diffs exit 0 in both resolution paths
- Light review set complete; VALIDATION.md finalized
- Human sign-off (end-of-phase) requested via /gsd-verify-work
</verification>

<success_criteria>
- D-06 conclusively proven (unit + render, system-dark and pinned-dark)
- D-01/D-02 combinations behave correctly incl. sheets/lock/keyboard
- User has a complete both-theme review set; phase ready for /gsd-verify-work
</success_criteria>

<output>
Create `.planning/phases/17-light-mode-support-neumorphic-redesign/17-07-SUMMARY.md` when done.
</output>
