---
phase: 17-light-mode-support-neumorphic-redesign
plan: 03
type: execute
wave: 3
depends_on: [17-02]
files_modified:
  - MyHomeApp/MyHomeApp.swift
  - MyHomeApp/DesignSystem/DesignTokens.swift
  - MyHomeApp/RootView.swift
  - MyHomeApp/Features/Settings/SettingsView.swift
  - MyHomeTests/DesignTokensTests.swift
autonomous: true
requirements: [D-01, D-02, D-03, D-07, D-08]
must_haves:
  truths:
    - "The hard-coded .preferredColorScheme(.dark) at MyHomeApp.swift:34 is gone; an AppStorage-backed System/Light/Dark setting drives the root scheme (D-01)"
    - "Missing/garbage UserDefaults value resolves to System — first launch after update follows the phone appearance with no opt-in gate (D-02)"
    - "Settings has one neumorphic segmented Appearance row (System/Light/Dark) near the top; flipping it re-themes the app live (D-03)"
    - "Native tab bar, nav bars, sheets, and keyboard adapt to the resolved scheme with no forced-dark remnant (D-07)"
    - "The selected-tab tint uses accentText — dark amber iconography on the light bar, canary in dark (D-08, chrome slice only; the app-wide call-site audit is Plans 17-08/17-09)"
    - "With the theme pinned to Dark, every screen renders pixel-identical to the Plan 01 baselines (D-06)"
  artifacts:
    - path: "MyHomeApp/DesignSystem/DesignTokens.swift"
      provides: "AppearanceTheme enum (system/light/dark → ColorScheme?)"
      contains: "enum AppearanceTheme"
    - path: "MyHomeApp/MyHomeApp.swift"
      provides: "@AppStorage-driven preferredColorScheme at root"
      contains: "appearanceTheme"
    - path: "MyHomeApp/Features/Settings/SettingsView.swift"
      provides: "Appearance segmented pill row bound to the same AppStorage key"
      contains: "Appearance"
  key_links:
    - from: "MyHomeApp/Features/Settings/SettingsView.swift"
      to: "MyHomeApp/MyHomeApp.swift"
      via: "shared @AppStorage(\"appearanceTheme\") key — row change re-resolves root scheme live"
      pattern: "AppStorage\\(\"appearanceTheme\"\\)"
    - from: "MyHomeApp/RootView.swift"
      to: "MyHomeApp/DesignSystem/DesignTokens.swift"
      via: ".tint(DesignTokens.accentText) selected-tab tint"
      pattern: "tint\\(DesignTokens\\.accentText\\)"
---

<objective>
Unlock light mode: replace the forced dark scheme with the AppStorage-backed
System/Light/Dark setting (D-01/D-02), add the neumorphic Appearance segmented
row in Settings (D-03), and verify native chrome adapts (D-07) with the
selected-tab tint moved to accentText (the chrome slice of D-08).

Purpose: after this plan, light mode is user-reachable and structurally correct.
The app-wide D-08 accent call-site audit is deliberately split out into Plans
17-08 (Settings + Expenses) and 17-09 (Notes/Budgets/Overview/Assets/Analytics)
to keep executor context bounded. This plan supersedes requirement DS-05
(dark-only) by user decision (roadmap promotion 2026-06-27).
Output: theme plumbing + settings row + adapted chrome, dark still bit-identical.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-light-mode-support-neumorphic-redesign/17-CONTEXT.md
@.planning/phases/17-light-mode-support-neumorphic-redesign/17-RESEARCH.md
@.planning/phases/17-light-mode-support-neumorphic-redesign/17-PATTERNS.md
@MyHomeApp/MyHomeApp.swift
@MyHomeApp/RootView.swift
@MyHomeApp/Features/Settings/SettingsView.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: AppearanceTheme enum + AppStorage root scheme (replace MyHomeApp.swift:34)</name>
  <files>MyHomeApp/DesignSystem/DesignTokens.swift, MyHomeApp/MyHomeApp.swift, MyHomeTests/DesignTokensTests.swift</files>
  <read_first>
    - MyHomeApp/MyHomeApp.swift (line 34 `.preferredColorScheme(.dark)` on RootView inside WindowGroup — the single modifier to replace; note the .onAppear chain must stay intact)
    - .planning/phases/17-light-mode-support-neumorphic-redesign/17-RESEARCH.md §Pattern 2
  </read_first>
  <action>
    1. In DesignTokens.swift add `enum AppearanceTheme: String, CaseIterable` with cases system/light/dark, a computed `var colorScheme: ColorScheme?` (system → nil, light → .light, dark → .dark), and a `var label: String` ("System"/"Light"/"Dark") for the Settings row. Doc comment cites D-01/D-02.
    2. In MyHomeApp.swift add `@AppStorage("appearanceTheme") private var appearanceThemeRaw = AppearanceTheme.system.rawValue` and replace line 34's `.preferredColorScheme(.dark)` with `.preferredColorScheme((AppearanceTheme(rawValue: appearanceThemeRaw) ?? .system).colorScheme)`. Plain UserDefaults.standard — NO App Group suite (no widget/extension reads the theme; the App-Group store is a SwiftData concern only). Missing key → system default = D-02 with zero migration. Remove the "DS-05" comment; replace with a D-01 comment.
    3. In DesignTokensTests.swift add `AppearanceThemeTests`: system.colorScheme == nil, light == .light, dark == .dark; `AppearanceTheme(rawValue: "garbage") ?? .system` == .system and `AppearanceTheme(rawValue: String())` is nil (V5 input-validation: malformed persisted value degrades safely — threat T-17-03).
  </action>
  <verify>
    <automated>xcodebuild test -scheme MyHome -destination "id=2F09365E-5099-490E-9484-B8788C53C816" -only-testing:MyHomeTests/DesignTokensTests -quiet && grep -rn "preferredColorScheme(.dark)" MyHomeApp/MyHomeApp.swift | wc -l # expect 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'preferredColorScheme(.dark)' MyHomeApp/MyHomeApp.swift` returns 0; `grep -c 'AppStorage("appearanceTheme")' MyHomeApp/MyHomeApp.swift` returns 1
    - AppearanceThemeTests green including garbage-value fallback
    - With simulator appearance dark and no stored key, all screens render dark (spot screenshot of tab 0 diffs clean vs baseline via diff_dark.py)
  </acceptance_criteria>
  <done>Root scheme is AppStorage-driven with system default; forced dark removed; mapping unit-tested.</done>
</task>

<task type="auto">
  <name>Task 2: Neumorphic Appearance segmented row in Settings (D-03)</name>
  <files>MyHomeApp/Features/Settings/SettingsView.swift</files>
  <read_first>
    - MyHomeApp/Features/Settings/SettingsView.swift (Section + .listRowBackground(DesignTokens.surfaceRaised) idiom around lines 49-66; rowLabel helper ~line 373)
    - MyHomeApp/DesignSystem/NeuSurface.swift lines 341-386 (EmbossedBar recessed-track recipe: fillRecessed3 + top shade band + hairline stroke) and lines 285-325 (NeuSecondaryButtonStyle raised gradient pill — the active-segment look)
  </read_first>
  <action>
    Add a private `AppearanceSegmentedRow: View` struct INSIDE SettingsView.swift (no new file — pbxproj tax) and host it in a new `Section("Appearance")` placed ABOVE the Security section, with `.listRowBackground(DesignTokens.surfaceRaised)`.
    - Control: custom neumorphic 3-segment pill, NOT Picker(.segmented) (D-03/PATTERNS). Recessed track: Capsule filled with DesignTokens.fillRecessed3 + top shade band (black-opacity gradient stop like EmbossedBar track) + hairline stroke. Active segment: raised Capsule with the surfaceRaisedStrongTop→surfaceRaisedStrongBottom diagonal gradient + rim stroke (white/black opacity pair like NeuSecondaryButtonStyle), sliding between segments with DesignTokens.springBouncy via matchedGeometryEffect.
    - Segments iterate AppearanceTheme.allCases using .label; selected segment text = DesignTokens.label, unselected = DesignTokens.label2. Binding: `@AppStorage("appearanceTheme") private var appearanceThemeRaw = AppearanceTheme.system.rawValue` — the SAME key as the root, so the flip re-themes live. Call Haptics.selection() on change. Honor Reduce Motion (skip the slide animation when reduceMotion, matching EntranceModifier's guard style).
    - Theme-flip transition (Claude's discretion, CONTEXT): default to SwiftUI's environment change — do NOT add a crossfade in this plan; Plan 07 evaluates jarring-ness on device.
    - Do NOT touch the existing `.tint(DesignTokens.accent)` at SettingsView:64 (Face ID Toggle) or `.foregroundStyle(DesignTokens.accent)` at :153 — those belong to the Plan 17-08 audit.
  </action>
  <verify>
    <automated>xcodebuild build -scheme MyHome -destination "id=2F09365E-5099-490E-9484-B8788C53C816" -quiet && grep -c 'Section("Appearance")' MyHomeApp/Features/Settings/SettingsView.swift # expect 1</automated>
  </verify>
  <acceptance_criteria>
    - Section("Appearance") appears before Section("Security") in source order
    - Zero occurrences of `pickerStyle(.segmented)` in SettingsView.swift
    - Both the row and MyHomeApp.swift reference the identical key string "appearanceTheme"
    - Screenshot check: launch with -startTab 4 in dark → row renders in the neumorphic language; tap simulation not required (live-flip verified in Plan 07 + end-of-phase human check)
  </acceptance_criteria>
  <done>One segmented Appearance row near the top of Settings, bound to the shared AppStorage key, styled as a neumorphic pill control.</done>
</task>

<task type="auto">
  <name>Task 3: Chrome verification (D-07) + selected-tab tint → accentText</name>
  <files>MyHomeApp/RootView.swift</files>
  <read_first>
    - MyHomeApp/RootView.swift line 102 (.tint(DesignTokens.accent) — selected-tab iconography on the bar → accentText per RESEARCH D-07 note 2)
    - .planning/phases/17-light-mode-support-neumorphic-redesign/17-RESEARCH.md §"System chrome (D-07)" and assumption A4
  </read_first>
  <action>
    1. RootView.swift:102: change `.tint(DesignTokens.accent)` to `.tint(DesignTokens.accentText)` — selected-tab icon/label is accent ICONOGRAPHY on a (light) bar, a D-08 text/icon role. accentText's dark branch == #FFD60A, so dark rendering cannot change. Update the trailing comment to cite D-07/D-08.
    2. D-07 chrome verification (no further code expected): rebuild, set simulator appearance LIGHT, launch each tab with -seedSampleData -startTab N, screenshot to a scratch dir, and confirm native tab bar/nav bars/sheet chrome render the system light material (no black bars, no forced-dark remnant). RESEARCH verified zero UITabBarAppearance/UINavigationBarAppearance code exists; native bars auto-adapt. ONLY IF the default light bar material visibly clashes with the #E3E6EE canvas (e.g. stark white slab against cool gray), add UITabBarAppearance/UINavigationBarAppearance objects built with UIColor(dynamicProvider:) canvas-family colors, set once in an init — this is the bounded fallback (assumption A4), not the default path. Light-mode NEUMORPHIC quality is NOT judged here (surfaces are tuned in later plans); only chrome adaptation is.
    3. Dark identity: re-run the dark screenshot loop (appearance dark, no stored theme key) and diff_dark.py vs Plan 01 baselines — the plumbing + tint swap must be invisible in dark.
  </action>
  <verify>
    <automated>xcodebuild build -scheme MyHome -destination "id=2F09365E-5099-490E-9484-B8788C53C816" -quiet && grep -c 'tint(DesignTokens.accentText)' MyHomeApp/RootView.swift # expect 1</automated>
  </verify>
  <acceptance_criteria>
    - RootView.swift:102 reads `.tint(DesignTokens.accentText)`; zero `.tint(DesignTokens.accent)` remains in RootView.swift
    - Light chrome screenshots captured for all 5 tabs; A4 outcome recorded in SUMMARY (auto-adapt OK vs appearance-code fallback added)
    - diff_dark.py vs Plan 01 baselines exits 0 (all 6 screens)
  </acceptance_criteria>
  <done>Chrome adapts in light with accentText tab tint; dark still bit-identical.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| UserDefaults → root scheme | Persisted string crosses into scheme resolution at app root |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-03 | Tampering/DoS (trivial) | appearanceTheme UserDefaults value | mitigate | `AppearanceTheme(rawValue:) ?? .system` optional-init fallback; unit-tested with garbage input (Task 1) |
| T-17-04 | Information disclosure | App-switcher privacy blur under scheme change | accept | Existing scenePhase blur is scheme-independent (RESEARCH verified); re-checked in Plan 07 smoke |
</threat_model>

<verification>
- DesignTokensTests target green (incl. AppearanceThemeTests)
- `grep -rn "preferredColorScheme" MyHomeApp --include="*.swift" | grep -v "#Preview" | grep -v Tests` shows only the AppStorage-mapped root modifier (preview pins are Plan 04's cleanup)
- Dark screenshot diff vs baselines exits 0
</verification>

<success_criteria>
- Light mode reachable via System/Light/Dark setting defaulting to System (D-01, D-02)
- Neumorphic Appearance row in Settings (D-03); chrome adapts with accentText tab tint (D-07, D-08 chrome slice)
- Dark bit-identity preserved (D-06); app-wide accent audit handed to Plans 17-08/17-09
</success_criteria>

<output>
Create `.planning/phases/17-light-mode-support-neumorphic-redesign/17-03-SUMMARY.md` when done. Include the A4 chrome outcome.
</output>
