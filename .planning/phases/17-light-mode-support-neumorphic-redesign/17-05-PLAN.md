---
phase: 17-light-mode-support-neumorphic-redesign
plan: 05
type: execute
wave: 7
depends_on: [17-04]
files_modified:
  - MyHomeApp/DesignSystem/NeuSurface.swift
  - MyHomeApp/DesignSystem/DesignTokens.swift
  - MyHomeApp/Features/Shared/DonutChart.swift
  - MyHomeApp/Features/Budgets/BudgetsView.swift
  - MyHomeTests/DesignTokensTests.swift
autonomous: true
requirements: [D-05, D-11, D-12, D-13, D-14]
must_haves:
  truths:
    - "In light mode every chart dish — hero orb dish, donut dish, net-worth dish, budget-ring dish, and vertical pill-gauge wells — is a deep slate instrument window; in dark mode they are pixel-identical to today (D-12, D-13)"
    - "Dish CONTENT (orb particles, donut segments, ring fills, gauge fills) renders the ORIGINAL luminous dark palette with full neonGlow bloom in both themes (D-11)"
    - "Dish chrome is #3E4250-family slate in light — harmonized with the light palette, never verbatim #15151B charcoal (D-13, the 'no hole punched into dark mode' rule)"
    - "EmbossedBar is NOT a dish: its track is a light-tuned recessed well and its fills use deepened light colors with the D-14 drop-shadow glow language"
    - "Dark rendering remains pixel-identical to Plan 01 baselines"
  artifacts:
    - path: "MyHomeApp/DesignSystem/NeuSurface.swift"
      provides: "NeuCircularWell + VerticalPillGauge as instrument windows (dishSlate chrome + force-dark content)"
      contains: "environment(\\.colorScheme, .dark)"
    - path: "MyHomeApp/Features/Shared/DonutChart.swift"
      provides: "Dish gradients harmonized to slate in light (adaptive dish tokens)"
  key_links:
    - from: "MyHomeApp/DesignSystem/NeuSurface.swift"
      to: "MyHomeApp/DesignSystem/DesignTokens.swift"
      via: "NeuCircularWell/VerticalPillGauge fills use DesignTokens.dishSlate"
      pattern: "DesignTokens\\.dishSlate"
    - from: "MyHomeApp/DesignSystem/NeuSurface.swift"
      to: "dish content subtree"
      via: "content() wrapped in .environment(\\.colorScheme, .dark) — content ONLY, never chrome"
      pattern: "\\.environment\\(\\\\?\\.colorScheme, \\.dark\\)"
---

<objective>
Implement the instrument-window treatment: dish chrome becomes adaptive deep
slate in light (D-13) while dish content is forced to the dark environment so
luminous chart colors and full glow render as designed (D-11/D-12); pill-gauge
wells get the same treatment; EmbossedBar (not a dish) gets its light-well +
deepened-fill + drop-shadow treatment (D-14).

Purpose: the dishes are the signature of the design — this plan is what makes
light mode keep the WHOOP-grade luminous data-viz without punching holes into
dark mode.
Output: instrument windows across orb/donut/net-worth/budget-ring/pill gauges,
dark bit-identical.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-light-mode-support-neumorphic-redesign/17-CONTEXT.md
@.planning/phases/17-light-mode-support-neumorphic-redesign/17-RESEARCH.md
@.planning/phases/17-light-mode-support-neumorphic-redesign/17-PATTERNS.md
@MyHomeApp/DesignSystem/NeuSurface.swift
@MyHomeApp/Features/Shared/DonutChart.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: NeuCircularWell → instrument window (slate chrome + force-dark content)</name>
  <files>MyHomeApp/DesignSystem/NeuSurface.swift, MyHomeApp/Features/Shared/DonutChart.swift, MyHomeApp/Features/Budgets/BudgetsView.swift, MyHomeApp/DesignSystem/DesignTokens.swift</files>
  <read_first>
    - MyHomeApp/DesignSystem/NeuSurface.swift :459-508 (NeuCircularWell — dish chrome ZStack with drawingGroup at :502, content() at :504; call sites: SpendBudgetCard:95 orb 300, SpendDonutCard:46 donut 236, NetWorthCard:53 donut 236, BudgetsView:278 ring 136)
    - MyHomeApp/Features/Shared/DonutChart.swift (dish gradients + GlowParticleRing inline black/white opacities around :405-488)
    - .planning/phases/17-light-mode-support-neumorphic-redesign/17-RESEARCH.md §Pattern 3 incl. the ordering caveat (Pitfall 4)
  </read_first>
  <action>
    1. NeuCircularWell: change the dish Circle fill from `DesignTokens.fillRecessed3` to `DesignTokens.dishSlate` (Plan 02 token: light #3E4250-family / dark #15151B verbatim — dark output identical). Wrap `content()` — and ONLY content(), never the chrome ZStack — in `.environment(\.colorScheme, .dark)` with a doc comment citing D-11/D-12/D-13 and the Pitfall-4 ordering rule. The chrome's inner-shadow arcs (black .55 / white .06 / hairline .50-.04) become dish-specific adaptive tokens (dishInnerShade, dishInnerRise, dishHairlineDark, dishHairlineLight — dark verbatim; light starts harmonized for slate: deeper navy-slate arc ~#23262F a0.6, rise #565B6C a0.5, tune in Task 3). Add these to DesignTokens.swift + DarkBitIdentityTests.
    2. DonutChart.swift: the dish-layer gradients (b.45/.30/.88/.62/.32, w.07 around :405-488) are DISH CHROME rendered inside the well content in some layers — classify each: layers that draw the dish/bevel = chrome → use the dish* adaptive tokens (slate-harmonized in light); layers that draw data/particles (GlowParticleRing particles, segment strokes) = content → leave on original values/tokens (they live inside the force-dark subtree and resolve luminous automatically). Record the per-layer classification in the SUMMARY.
    3. BudgetsView.swift :~278 ring track `black .18` (inside the 136 dish) → inside the force-dark subtree it resolves dark automatically IF expressed via adaptive token; since Color.black.opacity(0.18) is scheme-invariant anyway, leave it unchanged (document why).
    4. This is the 2nd of the 3 legitimate colorScheme-related sites (the env WRITE; no @Environment read needed here).
  </action>
  <verify>
    <automated>xcodebuild test -scheme MyHome -destination "id=2F09365E-5099-490E-9484-B8788C53C816" -only-testing:MyHomeTests/DesignTokensTests -quiet && grep -c 'environment(\\.colorScheme, .dark)' MyHomeApp/DesignSystem/NeuSurface.swift # expect >=1</automated>
  </verify>
  <acceptance_criteria>
    - NeuCircularWell dish fill reads `DesignTokens.dishSlate`; `.environment(\.colorScheme, .dark)` is attached to content() only (source assertion: the modifier is NOT on the chrome ZStack or the outer ZStack)
    - Dark screenshot diff vs baselines exits 0 (dishSlate dark branch = fillRecessed3 verbatim)
    - Light screenshot of tab 0: orb dish + donut dish interiors are slate (visibly lighter than #15151B, visibly darker than canvas) with luminous chart colors inside
    - DarkBitIdentityTests extended with dish* tokens, green
  </acceptance_criteria>
  <done>All four NeuCircularWell dishes are slate instrument windows in light with luminous content, bit-identical in dark.</done>
</task>

<task type="auto">
  <name>Task 2: VerticalPillGauge instrument wells + EmbossedBar light treatment (D-12/D-14)</name>
  <files>MyHomeApp/DesignSystem/NeuSurface.swift, MyHomeApp/DesignSystem/DesignTokens.swift</files>
  <read_first>
    - MyHomeApp/DesignSystem/NeuSurface.swift :327-386 (EmbossedBar: track fillRecessed3 + black .35 shade band + hairline; fill emboss white .28/black .28) and :388-457 (VerticalPillGauge: well fillRecessed3 + black .35/.55/white .04; fill sheen white .35; glow shadow color .45 radius 8)
    - .planning/phases/17-light-mode-support-neumorphic-redesign/17-CONTEXT.md D-12 (pill-gauge wells ARE dishes) vs D-14 (EmbossedBar fills are glow-on-light-surface elements — NOT dishes)
  </read_first>
  <action>
    VerticalPillGauge (IS an instrument window, D-12):
    - Well Capsule fill fillRecessed3 → dishSlate; well shade-band/hairline colors → the dish* tokens from Task 1.
    - Wrap the FILL PILL subtree (the `if fraction > 0` Capsule + sheen + glow shadow) in `.environment(\.colorScheme, .dark)` so the caller-passed category `color` (adaptive since Plan 02) resolves to the luminous dark variant and the `.shadow(color: color.opacity(0.45), radius: 8)` glow renders at full strength inside the well (D-11). The well chrome itself stays OUTSIDE the override.
    EmbossedBar (NOT a dish, D-14):
    - Track keeps `fillRecessed3` (adaptive: light-gray well in light) — no dishSlate, no env override.
    - Track shade band black .35 + hairline pair → reuse neu*/dish-agnostic adaptive tokens (neuInnerShade-family from Plan 04 or a trackShade token if the light value must differ — executor's call, dark verbatim either way).
    - Fill emboss overlay white .28 / black .28 → adaptive pair (embossTop/embossBottom; light starts: white a0.45 / #8E97AD a0.30).
    - Fills carry caller-passed colors which resolve to DEEPENED light variants on the light track automatically (D-9/D-10 tokens) — no call-site change. Where an EmbossedBar fill glows via neonGlow at call sites (BudgetProgressView:50), the Plan 02 scheme-aware modifier already gives the D-14 drop-shadow — verify visually.
  </action>
  <verify>
    <automated>xcodebuild test -scheme MyHome -destination "id=2F09365E-5099-490E-9484-B8788C53C816" -only-testing:MyHomeTests/DesignTokensTests -quiet && grep -c 'environment(\\.colorScheme, .dark)' MyHomeApp/DesignSystem/NeuSurface.swift # expect 2 (well content + gauge fill)</automated>
  </verify>
  <acceptance_criteria>
    - VerticalPillGauge well fill is dishSlate; EmbossedBar track fill is still fillRecessed3 (grep both)
    - Exactly 2 `.environment(\.colorScheme, .dark)` sites in NeuSurface.swift (NeuCircularWell content, VerticalPillGauge fill)
    - Dark screenshot diff vs baselines exits 0
    - Light screenshots: Analytics/Overview category pill gauges show slate wells + luminous glowing fills; Budgets progress bars show light wells + deepened fills with subtle drop-shadow (not two-layer bloom)
  </acceptance_criteria>
  <done>Pill gauges are instrument windows; EmbossedBar speaks the light glow language; dark untouched.</done>
</task>

<task type="auto">
  <name>Task 3: On-device tuning pass for dishes and wells</name>
  <files>MyHomeApp/DesignSystem/DesignTokens.swift</files>
  <read_first>
    - .planning/phases/17-light-mode-support-neumorphic-redesign/17-CONTEXT.md D-13 (user's explicit correction: slate must belong to the light palette — dark enough for glow, soft enough to harmonize)
  </read_first>
  <action>
    Screenshot-loop tuning (light appearance) of ONLY light branches: dishSlate tone (start #3E4250; user accepted family, exact value is discretion), dish* arc/hairline light values, embossTop/embossBottom, gauge sheen. Screens: tab 0 (orb dish + donut + budgets glance gauges), tab 2 (budget rings + progress bars), Analytics (-openAnalytics: category pill gauges). Judge against: glow reads clearly inside dishes (RESEARCH contrast floor: luminous #2DD4BF/#34E29B ≥5:1 on slate — keep the ContrastTests accent/label-on-dishSlate floors green); dish does NOT read as a hole into dark mode (compare side-by-side with a dark screenshot). Re-run dark diff after tuning. Dark branches untouchable.
  </action>
  <verify>
    <automated>xcodebuild test -scheme MyHome -destination "id=2F09365E-5099-490E-9484-B8788C53C816" -only-testing:MyHomeTests/DesignTokensTests -quiet</automated>
  </verify>
  <acceptance_criteria>
    - Light screenshots (light-dishes-*.png) in phase dir covering orb/donut/rings/gauges
    - accent-vs-dishSlate and label-vs-dishSlate ContrastTests still green with the final slate value
    - diff_dark.py vs Plan 01 baselines exits 0
    - git diff touches only light-branch strings in DesignTokens.swift
  </acceptance_criteria>
  <done>Slate dishes tuned to harmonize with the light palette while keeping luminous content legible; dark bit-identical.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| none new | Visual dish/gauge theming; no data flows or inputs. No new attack surface. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-06 | Tampering (regression) | Force-dark override leaking to dish chrome ("hole into dark mode", D-13) | mitigate | Override placed on content()/fill subtrees only; source assertion in acceptance criteria + light screenshot review |
</threat_model>

<verification>
- DesignTokensTests target green; diff_dark.py vs baselines exits 0
- Exactly 2 env-override sites in NeuSurface.swift, both on content/fill subtrees
- Light screenshots show slate windows with luminous content
</verification>

<success_criteria>
- All D-12 dish surfaces (orb, donut ×2, budget ring, pill-gauge wells) are slate instrument windows in light (D-12, D-13)
- Dish content renders original luminous palette + full bloom via env override (D-11)
- EmbossedBar carries the light-well + deepened-fill + drop-shadow language (D-14)
- Dark bit-identity maintained (D-06)
</success_criteria>

<output>
Create `.planning/phases/17-light-mode-support-neumorphic-redesign/17-05-SUMMARY.md` when done. Include the DonutChart layer classification (chrome vs content) and final dish token values.
</output>
