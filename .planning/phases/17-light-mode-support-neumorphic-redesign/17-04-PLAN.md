---
phase: 17-light-mode-support-neumorphic-redesign
plan: 04
type: execute
wave: 4
depends_on: [17-03]
files_modified:
  - MyHomeApp/DesignSystem/NeuSurface.swift
  - MyHomeApp/DesignSystem/DesignTokens.swift
  - MyHomeApp/DesignSystem/RollingMoneyText.swift
  - MyHomeApp/Features/Overview/SpendBudgetCard.swift
  - MyHomeTests/DesignTokensTests.swift
autonomous: true
requirements: [D-05, D-06, D-08]
must_haves:
  truths:
    - "Raised cards, recessed wells, both CTA button styles, and the circular puck carry light-tuned neumorphic depth — gray-blue shadows + bright white highlights with the same sculptural drama as dark (D-05)"
    - "Every promoted inline shadow/rim color has a named adaptive token whose dark branch is the current white/black opacity verbatim (D-06)"
    - "Shadow geometry (radii, offsets, blur, lineWidth) is unchanged in both schemes — only colors adapt (D-05 pass-1 rule)"
    - "NeuSecondaryButtonStyle label uses accentText (D-08); CTA canary gradient fills are unchanged"
    - "Dark rendering remains pixel-identical to Plan 01 baselines"
  artifacts:
    - path: "MyHomeApp/DesignSystem/DesignTokens.swift"
      provides: "Named neumorphic shadow/rim adaptive tokens (neuOuterHighlight, neuOuterShade, neuRim*, neuInner*, neuHairline*, emboss*)"
      contains: "neuOuterHighlight"
    - path: "MyHomeApp/DesignSystem/NeuSurface.swift"
      provides: "Surface/button/puck recipes rewired to named tokens; zero inline .white.opacity/.black.opacity in the converted components"
    - path: "MyHomeApp/DesignSystem/NeuSurface.swift"
      provides: "Paired light+dark previews"
      contains: "#Preview"
  key_links:
    - from: "MyHomeApp/DesignSystem/NeuSurface.swift"
      to: "MyHomeApp/DesignSystem/DesignTokens.swift"
      via: "shadow/rim colors resolved through DesignTokens.neu* adaptive tokens"
      pattern: "DesignTokens\\.neu"
---

<objective>
Give the core neumorphic components their light twins: promote every inline
white/black shadow, rim, and hairline color in NeuSurface.swift (surfaces,
both button styles, circular puck) plus the ShadowSpec colors to named adaptive
tokens (dark = verbatim), then tune the light values on the simulator until the
light theme reads as "the same physical object under different lighting" (D-05).

Purpose: this is the depth pass — without it, light mode has washed-out
dark-calibrated shadows. Dishes/gauges/bars are Plan 05; this plan owns the
raised/recessed/button/puck vocabulary.
Output: token-driven NeuSurface with light-tuned depth, dark bit-identical.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-light-mode-support-neumorphic-redesign/17-CONTEXT.md
@.planning/phases/17-light-mode-support-neumorphic-redesign/17-RESEARCH.md
@.planning/phases/17-light-mode-support-neumorphic-redesign/17-PATTERNS.md
@MyHomeApp/DesignSystem/NeuSurface.swift
@MyHomeApp/DesignSystem/DesignTokens.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Promote inline surface/button/puck shadow colors to named adaptive tokens</name>
  <files>MyHomeApp/DesignSystem/DesignTokens.swift, MyHomeApp/DesignSystem/NeuSurface.swift</files>
  <read_first>
    - MyHomeApp/DesignSystem/NeuSurface.swift (FULL file — RimOverlayModifier :119-145, RecessedOverlayModifier :154-188, NeuPrimaryButtonStyle :234-273, NeuSecondaryButtonStyle :277-325, NeuCircularPuck :512-538; these are the exact inline values to promote. EmbossedBar/VerticalPillGauge/NeuCircularWell are Plan 05 — do NOT convert them here)
    - MyHomeApp/DesignSystem/DesignTokens.swift (ShadowSpec + shadowRaised/shadowFloat at :101-125; PATTERNS warning: ShadowSpec's light/dark fields mean highlight-vs-depth, NOT color scheme — do not overload)
    - .planning/phases/17-light-mode-support-neumorphic-redesign/17-RESEARCH.md §Pattern 4
  </read_first>
  <action>
    Add a "MARK: - Neumorphic shadow colors (adaptive)" block to DesignTokens.swift with named adaptive tokens. Dark branch = current value VERBATIM; light starts are directional (Task 2 tunes). Mapping (current inline → token name → dark verbatim → light start):

    | Current inline | Token | dark (LOCKED) | light (start) |
    |----------------|-------|---------------|---------------|
    | shadowRaised.lightColor white .05 | neuOuterHighlight | #FFFFFF a0.05 | #FFFFFF a0.80 |
    | shadowRaised.darkColor black .55 | neuOuterShade | #000000 a0.55 | #8E97AD a0.50 |
    | shadowFloat.lightColor white .055 | neuOuterHighlightFloat | #FFFFFF a0.055 | #FFFFFF a0.90 |
    | shadowFloat.darkColor black .62 | neuOuterShadeFloat | #000000 a0.62 | #8E97AD a0.60 |
    | rim gradient white .07 (Rim + SecondaryButton + Puck rim) | neuRimTop | #FFFFFF a0.07 | #FFFFFF a0.90 |
    | rim gradient black .35 | neuRimBottom | #000000 a0.35 | #9BA3B8 a0.55 |
    | recessed dark arc black .55 (Recessed + Puck shadow) | neuInnerShade | #000000 a0.55 | #99A1B5 a0.70 |
    | recessed light rim white .05 | neuInnerRise | #FFFFFF a0.05 | #FFFFFF a0.85 |
    | hairline black .45 | neuHairlineDark | #000000 a0.45 | #9BA3B8 a0.50 |
    | hairline white .04 | neuHairlineLight | #FFFFFF a0.04 | #FFFFFF a0.70 |
    | button press shadow white .04 (both styles) | neuButtonHighlight | #FFFFFF a0.04 | #FFFFFF a0.80 |
    | button press shadow black .62 | neuButtonShade | #000000 a0.62 | #8E97AD a0.55 |

    Rewire in NeuSurface.swift, keeping ALL geometry (radius/offset/blur/lineWidth/press-state numerics) untouched:
    - shadowRaised/shadowFloat ShadowSpec initializers in DesignTokens.swift: swap `.white.opacity(0.05)` → `neuOuterHighlight` etc. (ShadowSpec fields are Color-typed; adaptive Colors slot in).
    - RimOverlayModifier gradient colors → neuRimTop/neuRimBottom.
    - RecessedOverlayModifier: black .55 → neuInnerShade, white .05 → neuInnerRise, hairline pair → neuHairlineDark/neuHairlineLight.
    - NeuPrimaryButtonStyle: KEEP the CTA hexes #231B00/#E8C918/#D9B000/#FFE04A/#F2C500 inline and unchanged (scheme-invariant canary fills per D-08 — canary + dark-text CTA identical in both themes); top rim white .45/.15 KEEP inline (it sits on canary, scheme-invariant); float shadows white .04 → neuButtonHighlight, black .62/.25/.20 → neuButtonShade with pressed-state opacity multipliers preserved via .opacity() on the token ONLY IF that keeps dark output identical — otherwise add explicit pressed variants (neuButtonShadePressed = dark #000000 a0.25 / light #8E97AD a0.25). Accent halo `.shadow(color: DesignTokens.accent.opacity(...))` unchanged (canary glow both schemes).
    - NeuSecondaryButtonStyle: same shadow/rim token swaps (pressed variant black .20 → follow the same pressed-token approach); label `.foregroundStyle(DesignTokens.accent)` at :288 → `DesignTokens.accentText` (D-08 text role — dark branch identical).
    - NeuCircularPuck: white .05/black .55 outer pair → neuOuterHighlight/neuOuterShade; rim pair → neuRimTop/neuRimBottom.
    CAUTION on composed opacity: `adaptiveToken.opacity(x)` multiplies BOTH branches — dark output changes unless x==1. Wherever the current code has state-dependent opacity (e.g. `.white.opacity(isPressed ? 0 : 0.04)`), use `isPressed ? Color.clear : token` or dedicated pressed tokens; never multiply an adaptive token's alpha.
    Extend DarkBitIdentityTests with every new neu* token (dark branch vs the verbatim legacy expression, e.g. neuOuterHighlight vs Color.white.opacity(0.05)).
  </action>
  <verify>
    <automated>xcodebuild test -scheme MyHome -destination "id=2F09365E-5099-490E-9484-B8788C53C816" -only-testing:MyHomeTests/DesignTokensTests -quiet && sed -n '119,188p;234,325p;512,538p' MyHomeApp/DesignSystem/NeuSurface.swift | grep -v '^\s*//' | grep -cE '\.(white|black)\.opacity' # expect 1 (the CTA rim white .45/.15 kept by design) or 0 if expressed differently — record exact count + justification</automated>
  </verify>
  <acceptance_criteria>
    - DarkBitIdentityTests green including all new neu* tokens
    - In the converted components (Rim/Recessed/both ButtonStyles/Puck) the only remaining inline white/black opacity is the CTA top rim on canary (documented as scheme-invariant by design)
    - CTA hexes #FFE04A/#F2C500/#E8C918/#D9B000/#231B00 unchanged (grep each, count ≥1)
    - Geometry diff-check: git diff of NeuSurface.swift shows no numeric changes to radius/offset/blur/lineWidth/padding values
    - Dark screenshot diff vs Plan 01 baselines exits 0
  </acceptance_criteria>
  <done>Surface/button/puck shadow colors are named adaptive tokens; dark output bit-identical at unit + render level.</done>
</task>

<task type="auto">
  <name>Task 2: Tune light-mode depth on device via screenshot loop (D-05)</name>
  <files>MyHomeApp/DesignSystem/DesignTokens.swift</files>
  <read_first>
    - .planning/phases/17-light-mode-support-neumorphic-redesign/17-CONTEXT.md §specifics ("same object under different lighting"; WHOOP is the data-viz quality bar; hero numerals stay .semibold)
    - .planning/phases/17-light-mode-support-neumorphic-redesign/17-RESEARCH.md §"Simulator verification loop"
  </read_first>
  <action>
    Iterate ONLY the light branches of the neu* tokens and the Plan 02 surface/canvas light values (bgCanvas/surfaceRaised*/fillRecessed* family) using the loop: `xcrun simctl ui <UDID> appearance light` → build → install → launch `-seedSampleData -startTab N` → screenshot → inspect → adjust light hexes/alphas → repeat. Focus screens: tab 0 (Overview cards + hero card frame; ignore dish interiors — Plan 05), tab 4 (Settings rows + Appearance pill), tab 1 (Expenses day cards + recessed search/input wells), a CTA-bearing screen (e.g. Budgets edit sheet or Add flows) for both button styles pressed/unpressed.
    Acceptance bar (self-judged against D-04/D-05): canvas reads cool-gray #E3E6EE-family; raised cards read as plump pillows lit from top-left (bright white highlight, gray-blue shade — NOT muddy gray-on-gray); recessed wells read clearly carved BELOW the canvas (darker than canvas, D-04); the drama level subjectively matches the dark theme's sculpting at a glance. Dark branches MUST NOT be touched in this task (git diff of dark-branch strings empty; identity tests enforce).
    Record final tuned values in the SUMMARY as a table (token → final light value) — Plans 05/06 reuse the same shading vocabulary.
  </action>
  <verify>
    <automated>xcodebuild test -scheme MyHome -destination "id=2F09365E-5099-490E-9484-B8788C53C816" -only-testing:MyHomeTests/DesignTokensTests -quiet</automated>
  </verify>
  <acceptance_criteria>
    - Light screenshots of tabs 0/1/4 + one CTA screen saved to the phase dir (light-tuning-*.png) showing carved wells + pillow cards
    - Identity + contrast tests still green (tuning cannot break floors or dark)
    - git diff since Task 1 touches ONLY light-branch hex/alpha strings in DesignTokens.swift
  </acceptance_criteria>
  <done>Light neumorphic depth tuned on-device to match dark's sculptural drama; all gates green.</done>
</task>

<task type="auto">
  <name>Task 3: Un-pin dark previews — add paired light/dark previews</name>
  <files>MyHomeApp/DesignSystem/NeuSurface.swift, MyHomeApp/DesignSystem/RollingMoneyText.swift, MyHomeApp/Features/Overview/SpendBudgetCard.swift</files>
  <read_first>
    - MyHomeApp/DesignSystem/NeuSurface.swift :555-595 (preview pinning .dark), MyHomeApp/DesignSystem/RollingMoneyText.swift :74, MyHomeApp/Features/Overview/SpendBudgetCard.swift :268+284 (RESEARCH Pitfall 8 — pinned previews hide light regressions)
  </read_first>
  <action>
    For each pinned preview: keep the existing dark preview (rename `#Preview("Dark")` where unnamed, retain `.preferredColorScheme(.dark)`) and ADD a sibling `#Preview("Light")` with identical content pinned `.preferredColorScheme(.light)`. Do not change preview content beyond the duplication; SpendBudgetCard's two previews each get a light twin (4 previews total there).
  </action>
  <verify>
    <automated>xcodebuild build -scheme MyHome -destination "id=2F09365E-5099-490E-9484-B8788C53C816" -quiet && grep -rn 'preferredColorScheme(.light)' MyHomeApp/DesignSystem/NeuSurface.swift MyHomeApp/DesignSystem/RollingMoneyText.swift MyHomeApp/Features/Overview/SpendBudgetCard.swift | wc -l # expect 4</automated>
  </verify>
  <acceptance_criteria>
    - Each of the 3 files contains at least one `.preferredColorScheme(.light)` preview alongside the retained dark one (SpendBudgetCard has 2)
    - Build (which type-checks previews) succeeds
  </acceptance_criteria>
  <done>Design-system previews show both themes; light regressions are visible during future work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| none new | Visual shadow-token refactor + previews; no data flows, storage, or input surfaces. No new attack surface. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-05 | Tampering (regression) | Dark rendering drift via composed .opacity() on adaptive tokens | mitigate | Pressed-state variants use dedicated tokens/Color.clear, never alpha multiplication; DarkBitIdentityTests + masked screenshot diff gate every task |
</threat_model>

<verification>
- DesignTokensTests target green (identity incl. neu* tokens, contrast, theme mapping)
- diff_dark.py vs Plan 01 baselines exits 0 after all three tasks
- No geometry numerals changed in NeuSurface.swift (git diff review)
</verification>

<success_criteria>
- All inline shadow/rim colors in surfaces/buttons/puck promoted to named adaptive tokens with verbatim dark branches (D-05 + D-06)
- Light depth tuned on device to the "same object, different lighting" bar (D-05)
- NeuSecondaryButtonStyle text on accentText (D-08); previews paired light/dark
</success_criteria>

<output>
Create `.planning/phases/17-light-mode-support-neumorphic-redesign/17-04-SUMMARY.md` when done. Include the final light-value table for all neu* tokens.
</output>
