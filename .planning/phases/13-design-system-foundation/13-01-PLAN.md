---
phase: 13-design-system-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - MyHomeApp/DesignSystem/DesignTokens.swift
  - MyHomeTests/DesignTokensTests.swift
  - MyHome.xcodeproj/project.pbxproj
autonomous: true
requirements: [DS-01, DS-05]
must_haves:
  truths:
    - "DesignTokens.swift compiles and is reachable from the MyHome target (no 'cannot find type' error)"
    - "Every neumorphic color, radius, spacing, shadow, and spring animation value used in v1.2 resolves through DesignTokens — no hardcoded hex or pixel-literal shadow anywhere else"
    - "DesignTokens contains zero stock system colors (no systemBackground / secondarySystemBackground)"
  artifacts:
    - path: "MyHomeApp/DesignSystem/DesignTokens.swift"
      provides: "Caseless enum of all neumorphic visual tokens (colors, radii, spacing, ShadowSpec, springs)"
      contains: "enum DesignTokens"
      min_lines: 80
    - path: "MyHomeTests/DesignTokensTests.swift"
      provides: "Token-value assertions for DS-01 (accent hex, shadow offsets, radiusCard, tabBarClearance)"
      contains: "struct DesignTokensTests"
  key_links:
    - from: "MyHomeApp/DesignSystem/DesignTokens.swift"
      to: "MyHomeApp/Support/Color+Hex.swift"
      via: "Color(hex:) initializer for every color constant"
      pattern: "Color\\(hex:"
    - from: "MyHome.xcodeproj/project.pbxproj"
      to: "MyHomeApp/DesignSystem/DesignTokens.swift"
      via: "G_DS group + PBXBuildFile/PBXFileReference/PBXSourcesBuildPhase registration"
      pattern: "DesignTokens.swift in Sources"
---

<objective>
Scaffold the `DesignSystem/` directory and its pbxproj group (`G_DS`), then create `DesignTokens.swift` — the single source-of-truth enum for every neumorphic color, radius, spacing, shadow spec, and spring animation in v1.2 (DS-01). Enforce dark-mode-only by using only fixed token colors, never stock system colors (DS-05). Create the Wave-0 test scaffolds (`DesignTokensTests.swift`, `RollingMoneyTextTests.swift`) so all later plans have somewhere to assert.

Purpose: Every subsequent v1.2 surface (NeuSurface, NeuTabBar, RollingMoneyText, and all Phase 14–16 screens) consumes these tokens. This plan unblocks all of them and resolves the #1 execution risk (pbxproj registration) first.
Output: `DesignSystem/` directory + `G_DS` group registered in pbxproj; `DesignTokens.swift`; two test files (`DesignTokensTests.swift` populated, `RollingMoneyTextTests.swift` scaffolded for Plan 02).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-design-system-foundation/13-UI-SPEC.md
@.planning/phases/13-design-system-foundation/13-PATTERNS.md
@.planning/phases/13-design-system-foundation/13-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold DesignSystem/ directory and register the G_DS group + all six Phase-13 files in project.pbxproj</name>
  <files>MyHomeApp/DesignSystem/ (new directory), MyHome.xcodeproj/project.pbxproj</files>
  <read_first>
    - MyHome.xcodeproj/project.pbxproj — read the PBXBuildFile section (find the `A803SH /* CardStyle.swift in Sources */` line), the PBXFileReference section (find `F803SH /* CardStyle.swift */`), the `G100 /* MyHomeApp */` group children list, the `G200 /* MyHomeTests */` group children list, the `P001 /* Sources */` build phase, and the `P003 /* Sources */` build phase. These are the exact insertion sites.
    - .planning/phases/13-design-system-foundation/13-PATTERNS.md § "pbxproj Registration Pattern (4 edits per file)" — the authoritative edit-by-edit recipe with exact ID assignments and verbatim line templates.
    - .planning/phases/13-design-system-foundation/13-RESEARCH.md § "Pattern 6: pbxproj registration for DesignSystem/ files" and § "Pitfall 1".
  </read_first>
  <action>
    First create the `MyHomeApp/DesignSystem/` directory on disk (Xcode does not create directories from pbxproj group references; the filesystem dir must exist first).
    Then make the four pbxproj edits for ALL six Phase-13 files at once, using the exact IDs from 13-PATTERNS.md: production files DesignTokens.swift (A13DS1/F13DS1), NeuSurface.swift (A13NS/F13NS), NeuTabBar.swift (A13NTB/F13NTB), RollingMoneyText.swift (A13RMT/F13RMT); test files DesignTokensTests.swift (A13DST/F13DST), RollingMoneyTextTests.swift (A13RMTT/F13RMTT).
    Edit 1 (PBXBuildFile): add one PBXBuildFile line per file following the `A803SH` template.
    Edit 2 (PBXFileReference): add one PBXFileReference line per file following the `F803SH` template (path = bare filename, sourceTree "<group>").
    Edit 3 (PBXGroup): create the new `G_DS /* DesignSystem */` group with the four production-file children and `path = DesignSystem`; insert `G_DS /* DesignSystem */,` into the `G100 /* MyHomeApp */` children list directly after `F105 /* Info.plist */,`; add the two test-file references into the `G200 /* MyHomeTests */` children list.
    Edit 4 (PBXSourcesBuildPhase): add the four production-file `in Sources` entries to `P001 /* Sources */`; add the two test-file `in Sources` entries to `P003 /* Sources */`.
    Register all six files now even though five do not exist yet on disk — pbxproj references to not-yet-written files are harmless until build; this front-loads the #1 risk so later plans only write source, never touch pbxproj structure. Each later plan's task that writes its file relies on this registration already existing.
  </action>
  <verify>
    <automated>cd "/Users/reo/My Projects/my-home" && test -d MyHomeApp/DesignSystem && grep -c "DesignTokens.swift in Sources" MyHome.xcodeproj/project.pbxproj | grep -qx 1 && grep -q "G_DS /\* DesignSystem \*/ = {" MyHome.xcodeproj/project.pbxproj && grep -q "G_DS /\* DesignSystem \*/," MyHome.xcodeproj/project.pbxproj && grep -c "RollingMoneyTextTests.swift in Sources" MyHome.xcodeproj/project.pbxproj | grep -qx 1 && echo PBXPROJ_OK</automated>
  </verify>
  <acceptance_criteria>
    - `MyHomeApp/DesignSystem/` directory exists on disk.
    - The `G_DS /* DesignSystem */` group block exists AND is referenced once in the `G100` children list.
    - All six files have a PBXBuildFile entry, a PBXFileReference entry, and a PBXSourcesBuildPhase entry (production → P001, tests → P003).
    - Both test files are referenced in the `G200 /* MyHomeTests */` children list.
    - `grep -c "DesignTokens.swift in Sources"` returns exactly 1 (PBXBuildFile entry; the P001 entry shares the same comment so the count check is against the PBXBuildFile-style line — confirm both the build-file declaration and the build-phase reference are present by eye if the count differs).
  </acceptance_criteria>
  <done>The DesignSystem/ directory and all six Phase-13 pbxproj registrations are in place; the verify grep gate passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create DesignTokens.swift and its value-assertion tests</name>
  <files>MyHomeApp/DesignSystem/DesignTokens.swift, MyHomeTests/DesignTokensTests.swift, MyHomeTests/RollingMoneyTextTests.swift</files>
  <read_first>
    - .planning/phases/13-design-system-foundation/13-UI-SPEC.md § "DesignTokens.swift File Structure" — the authoritative, complete enum listing (every hex, radius, spacing, ShadowSpec, spring). This is the spec; transcribe it exactly.
    - .planning/phases/13-design-system-foundation/13-PATTERNS.md § "MyHomeApp/DesignSystem/DesignTokens.swift" — color/struct/animation/header patterns and the @ScaledMetric footgun note.
    - MyHomeApp/Support/Color+Hex.swift — the existing `Color(hex:)` init and `hexString` round-trip property the tests rely on.
    - MyHomeTests/AccountBalanceTests.swift — the Swift Testing struct + @Test + #expect pattern to mirror.
  </read_first>
  <behavior>
    - Test: `DesignTokens.accent.hexString.uppercased()` equals `"#FFD60A"`.
    - Test: `DesignTokens.shadowRaised.lightX == -6` and `DesignTokens.shadowRaised.darkX == 7`.
    - Test: `DesignTokens.radiusCard == 26`.
    - Test: `DesignTokens.tabBarClearance == 100`.
  </behavior>
  <action>
    Write `DesignTokens.swift` as a caseless `enum DesignTokens` with `import SwiftUI`, transcribing every constant from the UI-SPEC DesignTokens listing verbatim: surface/canvas colors (bgCanvas, surfaceRaised, surfaceRaisedStrong, surfaceElevatedControl, fillRecessed/2/3), accent/semantic (accent #FFD60A, accentSoft as `.opacity(0.16)`, accentOnYellow, positive, negative, orange), labels (label, label2/3/4 from #DCDFEE with opacities), separators/borders (separatorHairline, separatorEdge, glassBorder), the 11-color category palette, corner radii (radiusCard 26, radiusInner 20, radiusPill 999, radiusTabBar 34, radiusSheet 20), on-grid spacing (4/8/16/24/32/48) and the three handoff exceptions (spacing2/12/22), tab-bar geometry (tabBarHeight 62, tabBarBottomOffset 24, tabBarClearance 100, tabItemWidth 58), the nested `ShadowSpec` struct plus `shadowRaised` and `shadowFloat` instances, and the `springBouncy`/`springSoft` Animation constants. Every color uses `Color(hex:)` or `Color.white/.black.opacity()` — never `Color(.systemBackground)` or any stock system color (DS-05). Do NOT declare any `@ScaledMetric` here (it cannot be a static stored property); record Group-B font base sizes only as comments per the PATTERNS footgun note.
    Then populate `DesignTokensTests.swift` with the four @Test assertions from the behavior block, mirroring the AccountBalanceTests header (`import Testing` + `import Foundation` + `@testable import MyHome`, `@MainActor struct DesignTokensTests`).
    Also create `RollingMoneyTextTests.swift` as a minimal compilable scaffold (same header, `@MainActor struct RollingMoneyTextTests {}` with one placeholder `@Test` that asserts `true`) so the file Plan 01 registered in pbxproj compiles; Plan 02 fills in the real assertions.
  </action>
  <verify>
    <automated>cd "/Users/reo/My Projects/my-home" && xcodebuild test -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:MyHomeTests/DesignTokensTests 2>&1 | tail -20 | grep -qiE "TEST SUCCEEDED|with 0 failures" && echo TOKENS_TESTS_OK</automated>
  </verify>
  <acceptance_criteria>
    - `DesignTokens.swift` defines `enum DesignTokens` with all tokens from the UI-SPEC listing.
    - `grep -c "Color(.systemBackground" MyHomeApp/DesignSystem/DesignTokens.swift` returns 0 and `grep -c "secondarySystemBackground" MyHomeApp/DesignSystem/DesignTokens.swift` returns 0 (DS-05).
    - `grep -cE '\.system\(size: [0-9]' MyHomeApp/DesignSystem/DesignTokens.swift` returns 0 (no hardcoded font pixel sizes; DS-06).
    - `DesignTokensTests` runs and passes all four assertions under `xcodebuild test`.
    - `RollingMoneyTextTests.swift` compiles (placeholder test green).
  </acceptance_criteria>
  <done>DesignTokens.swift exists with every spec token, contains no stock system colors and no hardcoded font sizes, and DesignTokensTests passes green; RollingMoneyTextTests scaffold compiles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Phase 13 is a pure presentation-layer infrastructure phase. No user data, no network, no auth, no persistence, no input crosses any boundary in this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | (n/a) | DesignTokens.swift compile-time constants | accept | No applicable STRIDE threat: tokens are static `let` constants with no user input, no data flow, no runtime computation. Per 13-RESEARCH.md § Security Domain, ASVS L1 has no blocking requirements for this phase. |

No package installs in this plan (zero new dependencies — all first-party SwiftUI). No `T-13-SC` supply-chain checkpoint required.
</threat_model>

<verification>
- `xcodebuild build -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17'` succeeds (DesignTokens + test scaffolds compile; confirms pbxproj registration of DesignTokens.swift).
- `xcodebuild test … -only-testing:MyHomeTests/DesignTokensTests` is green.
- `grep -rE "systemBackground" MyHomeApp/DesignSystem/` returns nothing (DS-05).
</verification>

<success_criteria>
- `DesignSystem/` directory + `G_DS` group registered; all six Phase-13 files have pbxproj entries.
- `DesignTokens.swift` defines every neumorphic token per UI-SPEC, with no stock system colors and no hardcoded font sizes.
- `DesignTokensTests` passes (accent hex, shadow offsets, radiusCard, tabBarClearance).
- `RollingMoneyTextTests.swift` scaffold compiles, ready for Plan 02.
</success_criteria>

<output>
Create `.planning/phases/13-design-system-foundation/13-01-SUMMARY.md` when done.
</output>
