---
phase: 13-design-system-foundation
plan: 03
type: execute
wave: 3
depends_on: ["13-01", "13-02"]
files_modified:
  - MyHomeApp/DesignSystem/NeuTabBar.swift
  - MyHomeApp/RootView.swift
  - MyHomeApp/MyHomeApp.swift
autonomous: false
requirements: [DS-03, DS-05, DS-06]
must_haves:
  truths:
    - "The floating capsule NeuTabBar replaces the native SwiftUI tab bar chrome in RootView"
    - "All five existing tabs remain reachable at their current deep-link indices (Home 0, Activity 1, Budgets 2, Notes 3, Settings 4); kOpenNoteNotification still selects Notes (3)"
    - "The capsule floats above the home indicator and scrolled content is never occluded (100pt bottom clearance contract)"
    - "The active tab pill slides with a spring on tab change, and jumps instantly when Reduce Motion is on"
    - "The entire app is forced dark-mode-only via a single .preferredColorScheme(.dark) at the app root"
  artifacts:
    - path: "MyHomeApp/DesignSystem/NeuTabBar.swift"
      provides: "Floating capsule tab bar reading/writing a Binding<Int> selectedTab"
      contains: "struct NeuTabBar"
      min_lines: 50
  key_links:
    - from: "MyHomeApp/RootView.swift"
      to: "MyHomeApp/DesignSystem/NeuTabBar.swift"
      via: ".overlay(alignment: .bottom) { NeuTabBar(selectedTab: $selectedTab) } after .toolbar(.hidden, for: .tabBar)"
      pattern: "NeuTabBar\\(selectedTab:"
    - from: "MyHomeApp/MyHomeApp.swift"
      to: "RootView"
      via: ".preferredColorScheme(.dark)"
      pattern: "preferredColorScheme\\(\\.dark\\)"
    - from: "MyHomeApp/DesignSystem/NeuTabBar.swift"
      to: "MyHomeApp/DesignSystem/DesignTokens.swift"
      via: "accent / surfaceRaisedStrong / shadowFloat / springBouncy / accentSoft tokens"
      pattern: "DesignTokens\\."
---

<objective>
Build `NeuTabBar` (DS-03) — the floating capsule tab bar — and wire it into `RootView` as a drop-in visual replacement for the native tab chrome while preserving the existing `@State selectedTab: Int` binding and all five deep-link indices. Force the app to dark-mode-only with `.preferredColorScheme(.dark)` at the root (DS-05). Close out DS-06 with the Accessibility Inspector pass on `NeuSurface`, `NeuTabBar`, and `RollingMoneyText` previews.

This is the integration plan and the phase exit gate. It is NOT autonomous: deep-link/tab behavior and Accessibility Inspector contrast verification require a human on-device/simulator pass (per `human_verify_mode: end-of-phase`).

Purpose: Completes the design system so every Phase 14 screen can host the new tab bar and adopt the tokens.
Output: `NeuTabBar.swift`; `RootView.swift` overlaying NeuTabBar with native chrome hidden; `MyHomeApp.swift` forcing dark mode.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-design-system-foundation/13-UI-SPEC.md
@.planning/phases/13-design-system-foundation/13-PATTERNS.md
@.planning/phases/13-design-system-foundation/13-RESEARCH.md
@MyHomeApp/DesignSystem/DesignTokens.swift
@MyHomeApp/RootView.swift
@MyHomeApp/MyHomeApp.swift
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create NeuTabBar.swift (floating capsule, sliding pill, safe-area aware)</name>
  <files>MyHomeApp/DesignSystem/NeuTabBar.swift</files>
  <read_first>
    - .planning/phases/13-design-system-foundation/13-UI-SPEC.md § "NeuTabBar Component Contract (DS-03)" — structural layout (62pt height, 34pt radius, 24pt offset, 58pt item width), the 5-tab definition table with indices/labels/SF Symbols, the active-pill spec (58×50, radius 26, accentSoft fill, accent .35 border, accent .22 glow), the safe-area inset contract, and the VoiceOver label/hint requirements.
    - .planning/phases/13-design-system-foundation/13-PATTERNS.md § "MyHomeApp/DesignSystem/NeuTabBar.swift" — the tabs tuple array (verbatim), the @Binding/@Environment/@Namespace declarations, the matchedGeometryEffect active-pill pattern, and the GeometryReader safe-area bottom-padding pattern.
    - .planning/phases/13-design-system-foundation/13-RESEARCH.md § "Pattern 3" and § "Pitfall 5" (read safeAreaInsets.bottom so the capsule clears the home indicator).
    - MyHomeApp/DesignSystem/DesignTokens.swift — tokens consumed (accent, accentSoft, label2, surfaceRaisedStrong, shadowFloat, radiusTabBar, tabBarHeight, tabBarBottomOffset, tabItemWidth, springBouncy, glassBorder).
  </read_first>
  <behavior>
    - Tapping the button at index N sets `selectedTab = N` (manual simulator gate — binding not unit-testable).
    - The active pill renders only behind the selected index; active icon+label use `DesignTokens.accent`, inactive use `DesignTokens.label2`.
    - With Reduce Motion on, the pill jumps to the new index with no slide.
  </behavior>
  <action>
    Create `NeuTabBar.swift` with `import SwiftUI` and `struct NeuTabBar: View` exposing `@Binding var selectedTab: Int`, plus `@Environment(\.accessibilityReduceMotion) private var reduceMotion` and `@Namespace private var pillNamespace`. Declare the `tabs` tuple array verbatim from PATTERNS: (0,"Home","house","house.fill"), (1,"Activity","creditcard","creditcard.fill"), (2,"Budgets","chart.pie","chart.pie.fill"), (3,"Notes","note.text","note.text"), (4,"Settings","gear","gear"). Render an `HStack` of per-tab buttons inside a capsule: each button is `tabItemWidth` (58pt) wide with a `VStack` of the SF Symbol (24pt, `.fill` variant when active) above the 10pt label, gap `spacing2`/3pt; active uses `accent`, inactive uses `label2`. Behind the selected tab draw the sliding pill — a `RoundedRectangle(cornerRadius: 26, style: .continuous)` filled `accentSoft`, with a `Color(hex:"#FFD60A").opacity(0.35)` 0.5pt stroke and `.shadow(color: accent.opacity(0.22), radius: 8)` glow — attached via `.matchedGeometryEffect(id: "activePill", in: pillNamespace)` and `.animation(reduceMotion ? nil : DesignTokens.springBouncy, value: selectedTab)`. Wrap the HStack capsule body in fill `surfaceRaisedStrong`, `.clipShape(Capsule())` or `RoundedRectangle(cornerRadius: radiusTabBar)`, the `shadowFloat` dual outer shadow (light first, dark second), a `glassBorder` 0.5pt stroke (DS-06 boundary affordance), height `tabBarHeight` (62pt). Position with a `GeometryReader`-driven `.padding(.bottom, max(DesignTokens.tabBarBottomOffset, geometry.safeAreaInsets.bottom + 8))` so it floats above the home indicator (Pitfall 5). Each tab button taps `selectedTab = tab.id`. Add `.accessibilityLabel(tab.label)` and `.accessibilityHint("Tab \(index+1) of 5")` per the UI-SPEC VoiceOver contract.
    Add a `#Preview` hosting NeuTabBar over a `DesignTokens.bgCanvas` background with a `@State` selectedTab so the pill slide and the Accessibility Inspector pass have a target.
    Do NOT modify RootView in this task (Task 2 owns RootView).
  </action>
  <verify>
    <automated>cd "/Users/reo/My Projects/my-home" && xcodebuild build -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' 2>&1 | tail -15 | grep -qiE "BUILD SUCCEEDED" && grep -q "matchedGeometryEffect(id: \"activePill\"" MyHomeApp/DesignSystem/NeuTabBar.swift && grep -q "accessibilityHint" MyHomeApp/DesignSystem/NeuTabBar.swift && echo NEUTABBAR_OK</automated>
  </verify>
  <acceptance_criteria>
    - `NeuTabBar.swift` defines `struct NeuTabBar: View` with `@Binding var selectedTab: Int` and the five-tab tuple array at the exact indices.
    - The active pill uses `matchedGeometryEffect(id: "activePill", ...)` gated by `reduceMotion ? nil : DesignTokens.springBouncy`.
    - Bottom padding reads `geometry.safeAreaInsets.bottom` (Pitfall 5 — not a bare 24pt literal).
    - `grep -c "accessibilityHint" MyHomeApp/DesignSystem/NeuTabBar.swift` ≥ 1 and `grep -c "accessibilityLabel" MyHomeApp/DesignSystem/NeuTabBar.swift` ≥ 1 (DS-06 VoiceOver).
    - `grep -cE "Color\(\.(secondary)?systemBackground" MyHomeApp/DesignSystem/NeuTabBar.swift` returns 0 (DS-05).
    - `xcodebuild build` succeeds (NeuTabBar.swift's pre-registered pbxproj entry compiles).
  </acceptance_criteria>
  <done>NeuTabBar renders the floating capsule with sliding accent pill, safe-area-aware positioning, and VoiceOver labels, reading only DesignTokens; build is green.</done>
</task>

<task type="auto">
  <name>Task 2: Wire NeuTabBar into RootView and force dark mode in MyHomeApp</name>
  <files>MyHomeApp/RootView.swift, MyHomeApp/MyHomeApp.swift</files>
  <read_first>
    - MyHomeApp/RootView.swift — the `TabView(selection: $selectedTab)` block (lines ~66–97), the existing `.tabItem`/`.tag()` modifiers (must stay — TabView requires .tabItem to track tabs), the deep-link `.onReceive(kOpenNoteNotification)` observer that sets `selectedTab = 3` (must keep working), and the full trailing modifier chain (.onAppear/.sheet/.blur/.overlay/.onChange/.environment).
    - MyHomeApp/MyHomeApp.swift — the `WindowGroup { RootView(...).onAppear { setupNotifications() } }` block (lines ~32–37) where `.preferredColorScheme(.dark)` is inserted.
    - .planning/phases/13-design-system-foundation/13-PATTERNS.md § "MyHomeApp/RootView.swift (modify ...)" and § "MyHomeApp/MyHomeApp.swift (modify ...)" — the exact surgical edits.
    - .planning/phases/13-design-system-foundation/13-RESEARCH.md § "Pattern 3" (RootView integration) and § "Pitfall 3" (restore NavigationStack toolbars if hidden by `.toolbar(.hidden, for: .tabBar)`) and § "Pattern 5" (DS-05 root placement).
  </read_first>
  <action>
    In `RootView.swift`: keep the `TabView(selection: $selectedTab)` and ALL existing `.tabItem`/`.tag()`/`.badge()` modifiers untouched (TabView needs `.tabItem` to track tabs; the icons just stop displaying). Immediately after the `TabView { … }` closing brace, add `.toolbar(.hidden, for: .tabBar)` to suppress the native chrome, then add `.overlay(alignment: .bottom) { NeuTabBar(selectedTab: $selectedTab) }`. Append these two modifiers to the existing chain without restructuring the `.onAppear`/`.onReceive`/`.sheet`/`.blur`/`.overlay(UnlockView)`/`.onChange`/`.environment` modifiers — they all remain. Each tab's content scroll view should already (or in Phase 14) apply the 100pt bottom clearance; this plan does not restyle screens, but verify no regression in tab switching. If any tab's NavigationStack title/back button disappears after hiding the tab bar (Pitfall 3), restore it on that view with `.toolbar(.visible, for: .navigationBar)`.
    In `MyHomeApp.swift`: insert `.preferredColorScheme(.dark)` between `RootView(gmailSyncController: gmailSyncController)` and `.onAppear { setupNotifications() }` (DS-05, Pattern 5 — applied once at the root, never per-view).
    Make no other behavioral changes. Deep-link continuity is preserved because `NeuTabBar` reads and writes the same `$selectedTab` the `kOpenNoteNotification` observer mutates.
  </action>
  <verify>
    <automated>cd "/Users/reo/My Projects/my-home" && grep -q "NeuTabBar(selectedTab: \$selectedTab)" MyHomeApp/RootView.swift && grep -q "toolbar(.hidden, for: .tabBar)" MyHomeApp/RootView.swift && grep -q "preferredColorScheme(.dark)" MyHomeApp/MyHomeApp.swift && xcodebuild build -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' 2>&1 | tail -15 | grep -qiE "BUILD SUCCEEDED" && echo INTEGRATION_OK</automated>
  </verify>
  <acceptance_criteria>
    - RootView keeps the `TabView(selection: $selectedTab)` with all five `.tabItem`/`.tag()` intact; adds `.toolbar(.hidden, for: .tabBar)` and `.overlay(alignment: .bottom) { NeuTabBar(selectedTab: $selectedTab) }`.
    - The `kOpenNoteNotification` observer (`selectedTab = 3`) is unchanged.
    - `MyHomeApp.swift` has exactly one `.preferredColorScheme(.dark)` on `RootView`.
    - `xcodebuild build` succeeds.
  </acceptance_criteria>
  <done>NeuTabBar overlays the native-chrome-hidden TabView; deep-link indices preserved; app forced dark mode; build green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The complete design system: DesignTokens, NeuSurface (3 states), RollingMoneyText (animated INR), and NeuTabBar (floating capsule) overlaid in RootView with native chrome hidden and the app forced to dark mode. This checkpoint closes DS-03 (tab behavior + deep-links) and DS-06 (Accessibility Inspector contrast) — the manual-only gates from 13-VALIDATION.md.
  </what-built>
  <how-to-verify>
    1. Run the app on the iPhone 17 simulator (Xcode 26.5, scheme MyHome). Confirm the floating capsule tab bar appears, does not overlap the home indicator, and the active accent pill slides between tabs.
    2. Tap each of the 5 tabs (Home, Activity, Budgets, Notes, Settings). Confirm content switches correctly and each tab's NavigationStack title/back buttons are intact (Pitfall 3 regression check).
    3. Deep-link: trigger a note reminder notification (or post kOpenNoteNotification) and confirm the Notes tab (index 3) activates and the note opens — deep-link index stability (DS-03).
    4. Settings > Accessibility > Reduce Motion ON: confirm the tab pill jumps with no slide and any RollingMoneyText value snaps instantly with zero intermediate frames (DS-06).
    5. Open the NeuSurface, NeuTabBar, and RollingMoneyText Xcode Previews and run the Accessibility Inspector (Xcode > Open Developer Tool > Accessibility Inspector). Confirm ZERO non-text contrast warnings (WCAG 1.4.11 / DS-06) — canary #FFD60A active states and the glassBorder affordance must carry the boundary, never shadow depth alone.
    6. Confirm `NeuSurface(.recessed)` overlay-gradient inset shadow looks acceptable at spec values (Wave-0 open question resolution).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (tab regression, deep-link break, contrast warning, recessed look wrong).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | NeuTabBar reads/writes an in-process `Binding<Int>`; the RootView/MyHomeApp edits are presentation wiring. No network, no persistence change, no auth, no untrusted input crosses any boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-03 | (n/a) | NeuTabBar / RootView integration / preferredColorScheme | accept | No applicable STRIDE threat: the tab bar mutates an in-memory selection index and the app-root edits are pure presentation. No data flow, no boundary crossing. Per 13-RESEARCH.md § Security Domain, ASVS L1 has no blocking requirements for this phase. |

No package installs (zero new dependencies — first-party SwiftUI only). No `T-13-SC` supply-chain checkpoint required.
</threat_model>

<verification>
- `xcodebuild build -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17'` succeeds.
- Full suite green: `xcodebuild test -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17'`.
- `grep -r "systemBackground" MyHomeApp/DesignSystem/` returns nothing (DS-05 across all four files).
- `grep -rE '\.font\(\.system\(size: [0-9]+' MyHomeApp/DesignSystem/` returns nothing except `@ScaledMetric`-anchored uses (DS-06).
- Human-verify checkpoint: tab switching, deep-link index stability, Reduce Motion, and Accessibility Inspector zero-contrast-warning pass all confirmed.
</verification>

<success_criteria>
- Floating capsule NeuTabBar replaces native chrome in RootView; five tabs reachable at indices 0–4; kOpenNoteNotification still selects Notes (3).
- Capsule clears the home indicator; active pill slides (spring) / jumps (Reduce Motion).
- App forced dark-mode-only via one root `.preferredColorScheme(.dark)`.
- Accessibility Inspector shows zero non-text contrast warnings on all three component previews (DS-06).
- Full build + test suite green.
</success_criteria>

<output>
Create `.planning/phases/13-design-system-foundation/13-03-SUMMARY.md` when done.
</output>
