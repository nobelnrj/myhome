---
phase: 13-design-system-foundation
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - MyHomeApp/DesignSystem/NeuSurface.swift
  - MyHomeApp/DesignSystem/RollingMoneyText.swift
  - MyHomeApp/Features/Shared/CardStyle.swift
  - MyHomeTests/RollingMoneyTextTests.swift
autonomous: true
requirements: [DS-02, DS-04, DS-06]
must_haves:
  truths:
    - "Any view adopts the raised, floating, or recessed neumorphic look with a single .neuSurface(_:) modifier — no duplicated shadow code"
    - "CardStyle is marked deprecated (renamed: neuSurface) and still compiles"
    - "RollingMoneyText animates a Decimal rupee figure via .contentTransition(.numericText) over ~780ms, and snaps instantly with zero intermediate frames when Reduce Motion is on"
    - "RollingMoneyText formats INR with lakh grouping (en_IN) and exposes the final value to VoiceOver, never the mid-animation interpolation"
  artifacts:
    - path: "MyHomeApp/DesignSystem/NeuSurface.swift"
      provides: "NeuSurface ViewModifier + NeuSurfaceState enum + View.neuSurface() extension (raised/floating/recessed)"
      contains: "func neuSurface"
      min_lines: 40
    - path: "MyHomeApp/DesignSystem/RollingMoneyText.swift"
      provides: "Animated INR money readout honoring Reduce Motion"
      contains: "struct RollingMoneyText"
      min_lines: 25
    - path: "MyHomeTests/RollingMoneyTextTests.swift"
      provides: "INR lakh-grouping formatting assertion for DS-04"
      contains: "struct RollingMoneyTextTests"
  key_links:
    - from: "MyHomeApp/DesignSystem/NeuSurface.swift"
      to: "MyHomeApp/DesignSystem/DesignTokens.swift"
      via: "fill/shadow/radius tokens (surfaceRaised, shadowRaised, radiusCard)"
      pattern: "DesignTokens\\.(surfaceRaised|shadowRaised|shadowFloat|fillRecessed3|radiusCard|glassBorder)"
    - from: "MyHomeApp/DesignSystem/RollingMoneyText.swift"
      to: "accessibilityReduceMotion"
      via: "@Environment-gated contentTransition + animation"
      pattern: "accessibilityReduceMotion"
---

<objective>
Build the two token-consuming components that have no dependency on each other: `NeuSurface` (DS-02) — the ViewModifier family that renders raised / floating / recessed neumorphic surfaces via the dual-shadow system and replaces `CardStyle` — and `RollingMoneyText` (DS-04) — the animated INR money readout that honors Reduce Motion. Both consume `DesignTokens` from Plan 01. Both are accessible by construction (DS-06): NeuSurface adds the `glassBorder` boundary affordance on interactive surfaces; RollingMoneyText degrades to instant on Reduce Motion and exposes the final value to VoiceOver.

Purpose: These are the reusable surface and money primitives every Phase 14–16 screen will use.
Output: `NeuSurface.swift`, `RollingMoneyText.swift`, `CardStyle.swift` marked deprecated, `RollingMoneyTextTests.swift` filled in.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-design-system-foundation/13-UI-SPEC.md
@.planning/phases/13-design-system-foundation/13-PATTERNS.md
@.planning/phases/13-design-system-foundation/13-RESEARCH.md
@MyHomeApp/DesignSystem/DesignTokens.swift
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create NeuSurface.swift (raised/floating/recessed) and deprecate CardStyle</name>
  <files>MyHomeApp/DesignSystem/NeuSurface.swift, MyHomeApp/Features/Shared/CardStyle.swift</files>
  <read_first>
    - MyHomeApp/Features/Shared/CardStyle.swift — the exact ViewModifier + View-extension structure to mirror (optional-padding Group block, .frame(maxWidth:.infinity), .clipShape(RoundedRectangle(style:.continuous))) and the file to deprecate.
    - .planning/phases/13-design-system-foundation/13-UI-SPEC.md § "NeuSurface ViewModifier Contract (DS-02)" — the state→fill/shadow/rim table, the modifier signature, the ROADMAP term mapping (.raised/.floating/.recessed), and the DS-06 interactive-border requirement.
    - .planning/phases/13-design-system-foundation/13-PATTERNS.md § "MyHomeApp/DesignSystem/NeuSurface.swift" — the 9-step replacement recipe, dual-shadow application order, rim-overlay gradient, and the recessed overlay-gradient recommendation.
    - .planning/phases/13-design-system-foundation/13-RESEARCH.md § "Pattern 2" and § "Pitfall 2" (shadow order: light first, dark second) and § "Open Question 1" (recessed overlay-gradient is the default; confirm visually in preview).
    - MyHomeApp/DesignSystem/DesignTokens.swift — the token names consumed (surfaceRaised, surfaceRaisedStrong, fillRecessed3, shadowRaised, shadowFloat, radiusCard, glassBorder).
  </read_first>
  <action>
    Create `NeuSurface.swift` with `import SwiftUI`, a `NeuSurfaceState` enum (`.raised`, `.floating`, `.recessed`), a `NeuSurface: ViewModifier` struct with `state`, `radius: CGFloat = DesignTokens.radiusCard`, `padding: CGFloat? = 16`, and an `isInteractive: Bool = false` parameter. In `body`, reuse CardStyle's optional-padding `Group { if let padding … }` block and `.frame(maxWidth: .infinity, alignment: .leading)`, set `.background(fill)` where `fill` switches on state (raised→surfaceRaised, floating→surfaceRaisedStrong, recessed→fillRecessed3), `.clipShape(RoundedRectangle(cornerRadius: radius, style: .continuous))`. For raised/floating, apply the dual outer shadow — light shadow FIRST then dark shadow (Pitfall 2) — reading offsets/radii from `DesignTokens.shadowRaised` (raised) or `DesignTokens.shadowFloat` (floating), and add the rim `.overlay` (RoundedRectangle strokeBorder LinearGradient white .045 → black .30, topLeading→bottomTrailing, lineWidth 1). For recessed, apply the overlay-gradient inset approximation per the PATTERNS recommendation (no outer shadow, no rim) — the fillRecessed3 color being lighter than bgCanvas carries most of the sunken look. When `isInteractive` is true, add a 0.5pt `DesignTokens.glassBorder` stroke overlay as the WCAG 1.4.11 boundary affordance (DS-06 — shadow depth is never the sole affordance). Add the `extension View { func neuSurface(_ state:radius:padding:) }` wrapper mirroring CardStyle's extension.
    In `CardStyle.swift`, mark the `cardStyle` extension function `@available(*, deprecated, renamed: "neuSurface")` — do NOT delete it (Phase 14 removes usages). Keep it compiling.
    Add a `#Preview` to NeuSurface.swift showing all three states stacked on a `DesignTokens.bgCanvas` background so the recessed overlay-gradient can be visually confirmed (Wave-0 open question) and the Accessibility Inspector pass in Plan 03 has a target.
  </action>
  <verify>
    <automated>cd "/Users/reo/My Projects/my-home" && xcodebuild build -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' 2>&1 | tail -15 | grep -qiE "BUILD SUCCEEDED" && grep -q "deprecated, renamed: \"neuSurface\"" MyHomeApp/Features/Shared/CardStyle.swift && echo NEUSURFACE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `NeuSurface.swift` defines `enum NeuSurfaceState` with `.raised/.floating/.recessed`, a `NeuSurface: ViewModifier`, and `extension View { func neuSurface(...) }`.
    - Dual shadow uses `DesignTokens.shadowRaised`/`shadowFloat`; light `.shadow` precedes dark `.shadow` in source order.
    - `grep -c "glassBorder" MyHomeApp/DesignSystem/NeuSurface.swift` ≥ 1 (interactive boundary affordance present, DS-06).
    - `grep -cE "Color\(\.(secondary)?systemBackground" MyHomeApp/DesignSystem/NeuSurface.swift` returns 0 (DS-05).
    - CardStyle's `cardStyle` extension carries `@available(*, deprecated, renamed: "neuSurface")` and still compiles.
    - `xcodebuild build` succeeds (confirms NeuSurface.swift's pre-registered pbxproj entry compiles).
  </acceptance_criteria>
  <done>NeuSurface renders all three states from a single modifier reading only DesignTokens; CardStyle is deprecated but compiling; build is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create RollingMoneyText.swift and its INR-formatting test</name>
  <files>MyHomeApp/DesignSystem/RollingMoneyText.swift, MyHomeTests/RollingMoneyTextTests.swift</files>
  <read_first>
    - .planning/phases/13-design-system-foundation/13-UI-SPEC.md § "RollingMoneyText Component Contract (DS-04)" — the API signature, behavior contract table, heroMoney typography, and the Reduce Motion implementation snippet.
    - .planning/phases/13-design-system-foundation/13-PATTERNS.md § "MyHomeApp/DesignSystem/RollingMoneyText.swift" — the @Environment + @ScaledMetric instance-property pattern and the full implementation skeleton.
    - .planning/phases/13-design-system-foundation/13-RESEARCH.md § "Pattern 4" and § "Pitfall 6" (contentTransition MUST be paired with .animation(_:value:)) and § "Pitfall 7" (VoiceOver reads final value via accessibilityLabel).
    - MyHomeTests/RollingMoneyTextTests.swift — the scaffold created in Plan 01 to fill in.
    - MyHomeTests/AccountBalanceTests.swift — the Swift Testing pattern.
  </read_first>
  <behavior>
    - Test: `Decimal(123456).formatted(.currency(code: "INR").locale(Locale(identifier: "en_IN")))` contains `"1,23,456"` (lakh grouping).
    - Behavior (manual gate, documented): with `accessibilityReduceMotion = true`, the view uses `.identity` transition and `nil` animation — zero intermediate frames.
  </behavior>
  <action>
    Create `RollingMoneyText.swift` with `import SwiftUI` and a `struct RollingMoneyText: View` exposing `amount: Decimal`, `currencyCode: String = "INR"`, `locale: Locale = Locale(identifier: "en_IN")`, and `animationDuration: Double = 0.78`. Declare `@Environment(\.accessibilityReduceMotion) private var reduceMotion` and an instance `@ScaledMetric(relativeTo: .largeTitle) private var baseSize: CGFloat = 46` (NOT static). Compute `formatted` via `amount.formatted(.currency(code: currencyCode).locale(locale))`. The body renders `Text(formatted)` with `.font(.system(size: baseSize, weight: .ultraLight, design: .rounded))`, `.monospacedDigit()`, then the PAIRED `.contentTransition(reduceMotion ? .identity : .numericText())` and `.animation(reduceMotion ? nil : .smooth(duration: animationDuration), value: amount)` (Pitfall 6 — both required), and `.accessibilityLabel("₹\(formatted)")` reading the final value (Pitfall 7). No `withAnimation` on mount (the value/animation pairing only fires on change, satisfying the "no animation on appear" contract).
    Replace the placeholder test in `RollingMoneyTextTests.swift` with the INR lakh-grouping @Test from the behavior block. Add a code comment documenting that the Reduce Motion behavior is a manual simulator gate (Accessibility > Reduce Motion ON) since @Environment state cannot be unit-tested without a host view.
    Add a `#Preview` that toggles `amount` via a button so the digit-roll can be seen and the Reduce-Motion path can be exercised.
  </action>
  <verify>
    <automated>cd "/Users/reo/My Projects/my-home" && xcodebuild test -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:MyHomeTests/RollingMoneyTextTests 2>&1 | tail -20 | grep -qiE "TEST SUCCEEDED|with 0 failures" && echo ROLLINGMONEY_TESTS_OK</automated>
  </verify>
  <acceptance_criteria>
    - `RollingMoneyText.swift` defines `struct RollingMoneyText: View` with the DS-04 API signature and an instance `@ScaledMetric` (not static).
    - `.contentTransition(...)` and `.animation(..., value: amount)` both appear (Pitfall 6).
    - `grep -c "accessibilityReduceMotion" MyHomeApp/DesignSystem/RollingMoneyText.swift` ≥ 1 (DS-06 Reduce Motion gate).
    - `grep -c "accessibilityLabel" MyHomeApp/DesignSystem/RollingMoneyText.swift` ≥ 1 (Pitfall 7).
    - `RollingMoneyTextTests` runs and the INR lakh-grouping assertion passes under `xcodebuild test`.
  </acceptance_criteria>
  <done>RollingMoneyText animates via numericText with paired animation, degrades to instant on Reduce Motion, formats INR lakh grouping, exposes the final value to VoiceOver; its formatting test is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Pure presentation layer. NeuSurface composes shadows; RollingMoneyText formats a caller-supplied Decimal. No network, no persistence, no auth, no untrusted input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-02 | (n/a) | NeuSurface / RollingMoneyText | accept | No applicable STRIDE threat: both are stateless view components rendering caller-provided data with no boundary crossing. Per 13-RESEARCH.md § Security Domain, ASVS L1 has no blocking requirements here. |

No package installs (zero new dependencies). No `T-13-SC` supply-chain checkpoint required.
</threat_model>

<verification>
- `xcodebuild build -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17'` succeeds (NeuSurface + RollingMoneyText compile against their pre-registered pbxproj entries).
- `xcodebuild test … -only-testing:MyHomeTests/RollingMoneyTextTests` is green.
- `grep -rE "systemBackground" MyHomeApp/DesignSystem/` returns nothing (DS-05).
- `grep -rE '\.system\(size: [0-9]' MyHomeApp/DesignSystem/RollingMoneyText.swift` — the only `.system(size:)` use is `@ScaledMetric`-anchored `baseSize`, not a literal; manual confirm it is the scaled var, not a hardcoded literal (DS-06).
</verification>

<success_criteria>
- One `.neuSurface(.raised/.floating/.recessed)` modifier renders each state; no duplicated shadow code; CardStyle deprecated and compiling.
- RollingMoneyText rolls digits via numericText, snaps instantly on Reduce Motion, formats INR lakh grouping, VoiceOver reads the final value.
- DS-06 affordances present (glassBorder on interactive NeuSurface; Reduce Motion + accessibilityLabel on RollingMoneyText).
- Both component tests/builds green.
</success_criteria>

<output>
Create `.planning/phases/13-design-system-foundation/13-02-SUMMARY.md` when done.
</output>
