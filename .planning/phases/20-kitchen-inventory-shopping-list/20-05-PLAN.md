---
phase: 20-kitchen-inventory-shopping-list
plan: 05
type: execute
wave: 5
depends_on: ["20-02", "20-03", "20-04"]
files_modified:
  - .planning/phases/20-kitchen-inventory-shopping-list/
autonomous: true
requirements: [KTCH-01, KTCH-02, KTCH-03, KTCH-04]

must_haves:
  truths:
    - "Full test suite green on iPhone 17 simulator — schema migration, kitchen logic, shopping, kitchen sync, Phase-18 sync suites, and DarkBitIdentityTests all pass together"
    - "The complete kitchen loop is machine-verified end-to-end where automatable, and the remaining experiential checks (feel, two-phone sync) are handed to the user as ONE end-of-phase human check (config: human_verify_mode = end-of-phase)"
    - "A both-theme review set of the kitchen surface exists in the phase directory for sign-off"
    - "Sync invariants hold on the final build: kitchen round-trip idempotent, v10 snapshot refused at (found: 10, expected: 11), tombstones honored"
  artifacts:
    - path: ".planning/phases/20-kitchen-inventory-shopping-list/20-05-light-pantry.png"
      provides: "Final review set (pantry + shopping, light + dark — 4 screenshots minimum)"
    - path: ".planning/phases/20-kitchen-inventory-shopping-list/20-05-gate.log"
      provides: "Full-suite test output evidence for the phase gate"
  key_links:
    - from: "20-05 gate run"
      to: "MyHomeTests (full suite)"
      via: "xcodebuild test with no -only-testing filter"
      pattern: "Test Suite.*passed"
---

<objective>
Run the Phase 20 gate: full-suite regression on the final integrated build,
grep-gate re-checks of the phase's structural invariants, a both-theme
screenshot review set of the finished kitchen surface, and the single
end-of-phase human verification (config: human_verify_mode = end-of-phase) —
covering the KTCH-01..04 user loop on device and the two-phone sync check that
no simulator can prove.

Purpose: plans 01–04 each verified their slice; this plan proves the slices
compose — the pantry drives the shopping list, check-off drives the pantry,
and the whole kitchen state crosses to the other phone through the version-11
snapshot.

Output: green gate evidence committed, review set assembled, user sign-off
requested.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/20-kitchen-inventory-shopping-list/20-02-SUMMARY.md
@.planning/phases/20-kitchen-inventory-shopping-list/20-03-SUMMARY.md
@.planning/phases/20-kitchen-inventory-shopping-list/20-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full-suite gate + invariant grep gates + evidence log</name>
  <read_first>
    - Prior plan SUMMARYs 20-01 … 20-04 (any recorded deviations, skipped sites, or tuned values to re-check)
  </read_first>
  <action>
    1. Full suite: `xcodebuild test -scheme MyHome -destination 'platform=iOS
       Simulator,name=iPhone 17' -quiet 2>&1 | tee
       .planning/phases/20-kitchen-inventory-shopping-list/20-05-gate.log` —
       must be fully green (LockSettings/LockState are @Suite(.serialized);
       do not parallelize around them). This includes SchemaV11MigrationTests,
       KitchenLogicTests, ShoppingListTests, KitchenSyncTests, the Phase-18
       sync suites at version 11, and DarkBitIdentityTests.
    2. Structural invariant re-checks on the integrated tree (each must hold):
       - zero `typealias X = SchemaV10.` declaration lines under
         MyHomeApp/Persistence/Models/ (STAB-08 still atomic after 4 plans)
       - `grep -rn 'context.delete(' MyHomeApp --include='*.swift' | grep -v
         deleteRule | grep -v DeletionTracker.swift` returns nothing (18-04
         gate survived the kitchen UI work)
       - `grep -c 'currentSchemaVersion = 11' MyHomeApp/Sync/SyncSnapshot.swift`
         returns 1
       - `git diff --stat MyHome.xcodeproj/project.pbxproj` clean in the sense
         that every Kitchen/tests file appears in `in Sources` entries (list
         them in the log)
       - DesignTokens.swift has no uncommitted or phase-introduced dark-branch
         changes (`git log --oneline -1 -- MyHomeApp/DesignSystem/DesignTokens.swift`
         predates Phase 20; if 20-03/20-04 touched it at all, the gate FAILS)
    3. Any failure: fix in the owning file, re-run the affected suite, then
       re-run the FULL suite. Do not mark done with a failing check.
  </action>
  <verify>
    <automated>cd "/Users/reo/My Projects/my-home" && [ "$(grep -rE '^typealias [A-Za-z]+ = SchemaV10\.' MyHomeApp/Persistence/Models/ | wc -l | tr -d ' ')" = "0" ] && [ "$(grep -rn 'context.delete(' MyHomeApp --include='*.swift' | grep -v deleteRule | grep -v 'DeletionTracker.swift' | wc -l | tr -d ' ')" = "0" ] && xcodebuild test -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' -quiet 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - full suite green; 20-05-gate.log committed as evidence
    - all five structural invariants hold on the integrated tree
  </acceptance_criteria>
  <done>Phase-wide regression and invariants proven on the final build.</done>
</task>

<task type="auto">
  <name>Task 2: Both-theme review set + simulator sync loop + end-of-phase human check</name>
  <read_first>
    - .planning/phases/17-light-mode-support-neumorphic-redesign/17-07-PLAN.md Task 2 (review-set + human-check precedent for end-of-phase mode)
  </read_first>
  <action>
    1. Review set (iPhone 17 simulator, `-seedSampleData`): capture
       20-05-light-pantry.png (`-openKitchen`), 20-05-light-shopping.png
       (`-openKitchen -kitchenTab 1`), and the same two in dark appearance
       (20-05-dark-pantry.png, 20-05-dark-shopping.png) into the phase
       directory. Self-review against the v1.2 bar: neumorphic raised cards on
       bgCanvas, legible LOW/OUT badges in both schemes, dark keeps the neon
       look. Also capture 20-05-light-overview-entry.png showing the Kitchen
       entry card on Overview.
    2. Simulator sync loop (self-contained KTCH-04 smoke on the REAL UI build,
       mirroring the 18-05 Files loop): launch with seeded kitchen data →
       Settings → Data → "Export Sync Snapshot" → Save to Files → "Import
       Snapshot…" → pick it → confirm sheet shows pantry/shopping counts →
       Merge → stats show 0 inserted (idempotent self-merge visible in the UI
       with kitchen data aboard). Note the result in the SUMMARY.
    3. Request the end-of-phase sign-off below. Do not create any
       checkpoint:human-verify task mid-flow — this single human-check IS the
       phase's human verification (human_verify_mode = end-of-phase).
  </action>
  <verify>
    <automated>cd "/Users/reo/My Projects/my-home" && [ "$(ls .planning/phases/20-kitchen-inventory-shopping-list/ | grep -c '20-05-.*\.png')" -ge 5 ] && echo PASS</automated>
    <human-check>
      End-of-phase verification on device (the full KTCH loop + two-phone sync):
      1. Open MyHome → Home tab → Kitchen card → pantry opens. Add "Milk",
         quantity 3, unit L, low-stock threshold 2, restock quantity 3.
      2. Tap − (used) twice → quantity hits 1 → the row shows the LOW flag
         (at/below threshold). Tap − again → 0 → OUT flag.
      3. Switch to the Shopping segment → Milk appears in the Restock section
         automatically (with the "+3 L" restock hint).
      4. Add a manual extra ("Sponges") → it appears under Extras; check it
         off → struck through; "Clear checked" removes it.
      5. Check Milk off in Restock → back on the Pantry segment, Milk's
         quantity reads 3 and the flag is gone (check-off restocked the
         pantry).
      6. Toggle light/dark appearance → the kitchen surface matches the v1.2
         neumorphic system in both; dark looks exactly like the rest of the
         dark app.
      7. Two-phone sync (KTCH-04): Settings → Data → Export Sync Snapshot →
         AirDrop to the other phone (both phones on the Phase-20 build) →
         Merge → the other phone shows the same pantry items, flags, and
         manual extras. Delete a pantry item on phone B, export B → merge on
         A → it stays deleted on A.
      8. If one phone is still on the previous (v10) build: importing its
         snapshot must show the incompatible-version message and change
         nothing (optional check — skip if both phones already updated).
      Reply "approved" or describe issues.
    </human-check>
  </verify>
  <acceptance_criteria>
    - ≥5 review screenshots committed (pantry + shopping in both themes + Overview entry)
    - simulator export→import self-merge shows 0 inserted with kitchen data in the snapshot
    - human sign-off requested with the full 8-step loop; outcome recorded in SUMMARY
  </acceptance_criteria>
  <done>Review set assembled, sync loop smoke-passed, user sign-off requested at phase end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Verification-only plan; exercises boundaries established and mitigated in 20-02/20-03/20-04. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-10 | Tampering | regression drift across the 4 implementation plans | mitigate | Full-suite gate + structural grep invariants re-run on the integrated tree; evidence log committed. |
| T-20-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs anywhere in Phase 20. |
</threat_model>

<verification>
- Full suite green (20-05-gate.log); all structural invariants hold.
- Both-theme kitchen review set committed; simulator sync self-merge idempotent.
- End-of-phase human check answered "approved" (or issues routed to gap closure).
</verification>

<success_criteria>
- Phase 20 success criteria 1–4 demonstrably true: pantry CRUD + used/restocked (KTCH-01), threshold flags (KTCH-02), auto-populating list with restock-on-check-off + manual extras (KTCH-03), neumorphic light+dark surface whose data syncs phone-to-phone at snapshot version 11 (KTCH-04).
- Phase ready for /gsd-verify-work.
</success_criteria>

<output>
Create `.planning/phases/20-kitchen-inventory-shopping-list/20-05-SUMMARY.md` when done.
</output>
