---
phase: 12-notes-daily-routine-enhancement
plan: 04
type: execute
wave: 3
depends_on: ["12-01", "12-02"]
files_modified:
  - MyHomeApp/Features/Notes/CalendarView.swift
  - MyHomeTests/RoutineCalendarTests.swift
  - MyHomeApp/MyHome.xcodeproj/project.pbxproj
autonomous: true
requirements: [NOTE-01, NOTE-05]
must_haves:
  truths:
    - "A routine note surfaces in a 'Daily Routines' section on EVERY day in DayAgendaView, driven purely off isDailyRoutine (D-01/D-03)"
    - "Day cells do NOT gain a dot badge just because a routine repeats (D-02)"
    - "Tapping a checklist routine's indicator checks all blocks AND records the completion at check-time (D-06)"
    - "A text-only routine can be completed from the agenda via a Done today affordance (D-06)"
    - "Deleting a routine note while the agenda is open does not crash (STAB-01 tombstone guard)"
  artifacts:
    - path: "MyHomeApp/Features/Notes/CalendarView.swift"
      provides: "DayAgendaView Daily Routines section + RoutineAgendaRow + tombstone-guarded routineNotes"
      contains: "Daily Routines"
    - path: "MyHomeTests/RoutineCalendarTests.swift"
      provides: "routineNotes filtering + dot-badge-unchanged tests (NOTE-01, D-02)"
      contains: "RoutineCalendarTests"
  key_links:
    - from: "DayAgendaView.routineNotes"
      to: "Note.isDailyRoutine"
      via: "filter with tombstone guard"
      pattern: "isDailyRoutine"
    - from: "RoutineAgendaRow.toggleAllBlocks"
      to: "RoutineCompletion"
      via: "fetch-before-insert at check-time + context.save"
      pattern: "RoutineCompletion\\(noteID:"
---

<objective>
Surface routine notes in the calendar. Add a new "Daily Routines" Section at the TOP of DayAgendaView's List (above the progress header and reminders), populated by a tombstone-guarded `routineNotes` filter on `isDailyRoutine` (D-01/D-03), with a new `RoutineAgendaRow` sub-view that shows completion state, a tap-to-edit row action, and check-time completion recording. Leave CalendarAggregator/dot-badge logic untouched so routines never inflate the dot badge (D-02). Update the empty-state gate to account for routine-only days.

Purpose: This is the NOTE-01 calendar-surfacing requirement — a routine must appear every day without configuring a recurring reminder. It is file-isolated from plan 12-03 (only CalendarView.swift) so it runs in parallel in Wave 3.
Output: modified CalendarView.swift, new RoutineCalendarTests.swift, pbxproj registration; tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/12-notes-daily-routine-enhancement/12-CONTEXT.md
@.planning/phases/12-notes-daily-routine-enhancement/12-RESEARCH.md
@.planning/phases/12-notes-daily-routine-enhancement/12-PATTERNS.md
@.planning/phases/12-notes-daily-routine-enhancement/12-UI-SPEC.md
@.planning/phases/12-notes-daily-routine-enhancement/12-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: DayAgendaView "Daily Routines" section + RoutineAgendaRow + completion recording (D-01/D-02/D-03/D-06)</name>
  <files>MyHomeApp/Features/Notes/CalendarView.swift</files>
  <read_first>
    - MyHomeApp/Features/Notes/CalendarView.swift (DayAgendaView from line 282; remindersOnDay + tombstone guard line 304; body empty-state gate line 331; toggleCompletion line 414-451 with try? context.save() line 451; AgendaReminderItem row lines 358-386; dayCell dot-badge logic; @Environment modelContext)
    - MyHomeApp/Support/CalendarAggregator.swift (perDayCounts lines 68-94 — confirm routines are NOT added here; leave UNCHANGED)
    - MyHomeApp/Features/Notes/RoutineResetService.swift (lines 26-29 — IST day-key for the completion write)
    - MyHomeApp/Persistence/Models/RoutineCompletion.swift (field names from 12-01)
    - .planning/phases/12-notes-daily-routine-enhancement/12-PATTERNS.md (CalendarView/DayAgendaView section — routineNotes, body restructure, RoutineAgendaRow, toggleAllBlocks with fetch-before-insert, .sheet editing pattern, STAB-01 guard)
    - .planning/phases/12-notes-daily-routine-enhancement/12-UI-SPEC.md (Surface 3 — section placement/ordering, RoutineAgendaRow layout/copy/icons, empty-state copy "Nothing Scheduled", Done today inline button, dot-badge rule)
    - .planning/phases/12-notes-daily-routine-enhancement/12-02-SUMMARY.md (no service needed here, but confirms identifier scheme if a completion path references it)
  </read_first>
  <action>
In DayAgendaView add a `routineNotes` computed property filtering `notes` to `isDailyRoutine == true` WITH the STAB-01 tombstone guard (`guard note.modelContext != nil else { return false }`), sorted by title (D-01/D-03). Add `@State private var editingRoutineNote: Note? = nil`. Restructure the body: change the empty-state gate from `remindersOnDay.isEmpty` to `remindersOnDay.isEmpty && routineNotes.isEmpty`, and update the ContentUnavailableView copy to "Nothing Scheduled" / `systemImage: "calendar"` / "No routines or reminders for this day." (per UI-SPEC). In the populated List, insert a new `Section("Daily Routines")` FIRST (above the existing progress header and reminders sections) rendering `ForEach(routineNotes) { RoutineAgendaRow(note: $0, onTap: { editingRoutineNote = $0 }) }`. Add `.sheet(item: $editingRoutineNote) { EditNoteView(note: $0) }`. Do NOT touch the progress header or reminders sections, and do NOT modify CalendarAggregator (D-02 — routines must not produce a dot badge).

Add a new `RoutineAgendaRow` view (within CalendarView.swift, mirroring AgendaReminderItem rows): a leading completion-indicator Button (`circle` in accentColor when incomplete / `checkmark.circle.fill` in `.secondary` when complete, `.frame(minWidth: 44, minHeight: 44)`, accessibility label per UI-SPEC); the note title with `.strikethrough(isComplete)` and secondary foreground when complete; a caption status subtitle ("Daily at {time}" when a reminder time is set, else "Daily routine"); a row tap-gesture calling `onTap()`. For text-only routines (no checkbox blocks) show an inline `Button("Done today")` (`.buttonStyle(.bordered)`, `.font(.caption)`) when not yet complete. `isComplete` for checklist routines = all checkbox blocks isChecked; for text-only = a RoutineCompletion record exists for today's IST dayKey. Implement `toggleAllBlocks()`: when completing a checklist routine, set all checkbox blocks isChecked, and if `note.isDailyRoutine` write the completion via the IST-dayKey fetch-before-insert pattern (update completedAt if present, else insert RoutineCompletion), then `try? context.save()` (mirrors toggleCompletion line 451). The text-only "Done today" button does the same completion write + save. Render all strings via plain `Text(...)` (security T-12-10).
  </action>
  <verify>
    <automated>cd "/Users/reo/My Projects/my-home" && xcodebuild build -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - CalendarView.swift contains a `routineNotes` computed property with `guard note.modelContext != nil` (STAB-01 guard) filtering `isDailyRoutine`, and a `Section("Daily Routines")` placed before the reminders section.
    - The empty-state gate text contains `routineNotes.isEmpty` and the ContentUnavailableView title is "Nothing Scheduled".
    - A `RoutineAgendaRow` view exists with a 44pt completion indicator, title strikethrough on complete, caption status subtitle, and an inline "Done today" button for text-only routines.
    - `toggleAllBlocks` (and the Done today path) use fetch-before-insert (`FetchDescriptor<RoutineCompletion>` + update-or-insert) and call `try? context.save()`.
    - CalendarAggregator.swift is unchanged (no diff) — routines do not affect the dot badge.
    - Routine strings use plain `Text(...)`; grep finds no `AttributedString` in the new code.
    - `xcodebuild build -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17'` succeeds.
  </acceptance_criteria>
  <done>DayAgendaView shows a tombstone-safe "Daily Routines" section on every day, with rows that complete checklist or text-only routines and record completions at check-time, without affecting the dot badge; project builds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RoutineCalendarTests — routineNotes filtering + dot-badge-unchanged (NOTE-01, D-02)</name>
  <files>MyHomeTests/RoutineCalendarTests.swift, MyHomeApp/MyHome.xcodeproj/project.pbxproj</files>
  <read_first>
    - MyHomeTests/RoutineResetServiceTests.swift (lines 18-21 makeContainer in-memory pattern; Note/NoteBlock seeding)
    - MyHomeTests/CalendarAggregationTests.swift (existing perDayCounts test pattern — extend the dot-badge assertion that a routine without a reminderEnabled reminder produces no count)
    - MyHomeApp/Support/CalendarAggregator.swift (perDayCounts signature for the dot-badge-unchanged test)
    - MyHomeApp/Persistence/Models/RoutineCompletion.swift (for completion-record assertions)
    - .planning/phases/12-notes-daily-routine-enhancement/12-RESEARCH.md RQ-5 (routineNotes filter logic; CalendarAggregator exclusion of routines)
    - .planning/phases/12-notes-daily-routine-enhancement/12-PATTERNS.md (Test File Patterns — makeContainer including RoutineCompletion.self)
  </read_first>
  <behavior>
    - A note with isDailyRoutine == true is included in the routine filter; a normal note is excluded. (Test the same boolean filter logic DayAgendaView.routineNotes uses — factor the filter into a testable static/free function or assert the predicate over a seeded container.)
    - CalendarAggregator.perDayCounts does NOT count a routine note that has no reminderEnabled reminder (D-02 — dot badge unaffected by routines).
    - A routine note that ALSO has a reminderEnabled reminder on a day still produces a dot for that day (correct existing behavior preserved).
    - Completing a routine writes exactly one RoutineCompletion for today's IST dayKey; a second completion on the same day upserts (count stays 1) — idempotency (D-06/D-08).
  </behavior>
  <action>
Create RoutineCalendarTests.swift with `makeContainer()` including `Note.self, NoteBlock.self, RoutineCompletion.self`. Add: `routineNoteIncludedInFilter()` / `normalNoteExcludedFromFilter()` asserting the `isDailyRoutine` filter logic used by DayAgendaView.routineNotes (if the filter is inlined in the view, extract it into a small testable helper on the view type or a free function and reference that). Add `routineWithoutReminderDoesNotProduceDotCount()` seeding a routine note with no reminderEnabled reminder and asserting `CalendarAggregator.perDayCounts(...)` yields no count for its day (D-02). Add `routineWithReminderStillProducesDot()` confirming a routine that also has a reminderEnabled reminder still counts (existing behavior). Add `completionIsIdempotentPerDay()` performing two fetch-before-insert completion writes for the same `(noteID, IST dayKey)` and asserting exactly one RoutineCompletion row exists. Register RoutineCalendarTests.swift in pbxproj (4 edits, MyHomeTests group + target).
  </action>
  <verify>
    <automated>cd "/Users/reo/My Projects/my-home" && xcodebuild test -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:MyHomeTests/RoutineCalendarTests 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - RoutineCalendarTests.swift referenced in project.pbxproj and compiled into MyHomeTests target.
    - `xcodebuild test ... -only-testing:MyHomeTests/RoutineCalendarTests` passes all cases: routine filter inclusion/exclusion, dot-badge-unchanged for reminderless routines (D-02), dot preserved for routine-with-reminder, and per-day completion idempotency (D-06/D-08).
    - The dot-badge test exercises the real `CalendarAggregator.perDayCounts` (proves D-02 with no aggregator change).
  </acceptance_criteria>
  <done>RoutineCalendarTests proves routine filtering, dot-badge invariance under routines, and per-day completion idempotency; all green; file registered in pbxproj.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deleted Note tombstone -> DayAgendaView property access | a note may be deleted elsewhere while the agenda sheet is open |
| user-authored routine title -> SwiftUI Text in agenda row | user content rendered in the calendar |
| package-manager installs | None this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-09 | Denial of Service | tombstone crash on routine row | mitigate | `routineNotes` applies the established `guard note.modelContext != nil` STAB-01 idiom before any property access; deleting a routine note while the agenda is open does not fault |
| T-12-07 | Tampering | completion lost to midnight reset | mitigate | RoutineAgendaRow writes RoutineCompletion at check-time (toggleAllBlocks / Done today) with fetch-before-insert idempotency before RoutineResetService wipes isChecked |
| T-12-10 | Information Disclosure | markdown/AttributedString injection in agenda strings | mitigate | Routine title and status strings rendered via plain `Text(...)`; acceptance grep confirms no `AttributedString` in the new CalendarView code (T-114-03 / T-11-10) |
| T-12-SC | Tampering | package installs | accept | No package installs this phase |
</threat_model>

<verification>
- `xcodebuild build -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17'` succeeds.
- `xcodebuild test ... -only-testing:MyHomeTests/RoutineCalendarTests` green.
- CalendarAggregator.swift unchanged (dot badge invariant).
- No `AttributedString` in new CalendarView code.
</verification>

<success_criteria>
Routine notes surface in a tombstone-safe "Daily Routines" section on every day in DayAgendaView, complete via tap (checklist) or Done today (text-only) with idempotent check-time completion recording, and never inflate the dot badge (D-02 proven by an unchanged CalendarAggregator); tests green.
</success_criteria>

<output>
Create `.planning/phases/12-notes-daily-routine-enhancement/12-04-SUMMARY.md` when done. Record where the routineNotes filter was made testable and confirm CalendarAggregator was left unchanged.
</output>
