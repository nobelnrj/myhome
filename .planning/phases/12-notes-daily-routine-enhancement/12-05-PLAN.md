---
phase: 12-notes-daily-routine-enhancement
plan: 05
type: execute
wave: 4
depends_on: ["12-01", "12-02", "12-03", "12-04"]
files_modified:
  - MyHomeApp/MyHome.xcodeproj/project.pbxproj
autonomous: false
requirements: [NOTE-01, NOTE-03, NOTE-04, NOTE-05]
must_haves:
  truths:
    - "The full app builds for the iPhone 17 simulator with every new file compiled in (no silent pbxproj omission)"
    - "The full MyHomeTests suite is green"
    - "The four manual-only behaviors (notification fire, reorder persistence, every-day surfacing, cross-midnight streak) are human-verified"
  artifacts:
    - path: ".planning/phases/12-notes-daily-routine-enhancement/12-HUMAN-UAT.md"
      provides: "Recorded human verification outcomes for the four manual-only behaviors"
      contains: "UAT"
  key_links:
    - from: "all new .swift files"
      to: "MyHome / MyHomeTests targets"
      via: "project.pbxproj membership"
      pattern: "RoutineCompletion|SchemaV9|RoutineNotificationService|StreakCalculator|RoutineDetailView"
---

<objective>
Phase exit gate. Run a clean full build for iPhone 17 to prove every new `.swift` file (SchemaV9, RoutineCompletion, RoutineNotificationService, StreakCalculator, RoutineDetailView, and all four test files) is actually compiled into its target — the explicit-file-refs landmine surfaces here if any pbxproj edit was missed. Run the full test suite green. Then a human checkpoint verifies the four manual-only behaviors that automated tests cannot cover (notification delivery, the drag gesture, every-day calendar surfacing, and cross-midnight streak retention).

Purpose: This catches the two systemic risks — silently-uncompiled new files and behaviors only observable at runtime on a device/simulator — before the phase is marked complete.
Output: a green full build + suite, plus 12-HUMAN-UAT.md recording the manual verification outcomes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/12-notes-daily-routine-enhancement/12-VALIDATION.md
@.planning/phases/12-notes-daily-routine-enhancement/12-01-SUMMARY.md
@.planning/phases/12-notes-daily-routine-enhancement/12-02-SUMMARY.md
@.planning/phases/12-notes-daily-routine-enhancement/12-03-SUMMARY.md
@.planning/phases/12-notes-daily-routine-enhancement/12-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Post-merge build gate + full test suite</name>
  <files>MyHomeApp/MyHome.xcodeproj/project.pbxproj</files>
  <read_first>
    - .planning/phases/12-notes-daily-routine-enhancement/12-01-SUMMARY.md (pbxproj edits recorded for SchemaV9 + RoutineCompletion + test files)
    - .planning/phases/12-notes-daily-routine-enhancement/12-02-SUMMARY.md (pbxproj edits for StreakCalculator + RoutineNotificationService)
    - .planning/phases/12-notes-daily-routine-enhancement/12-03-SUMMARY.md (pbxproj edits for RoutineDetailView)
    - .planning/phases/12-notes-daily-routine-enhancement/12-04-SUMMARY.md (pbxproj edits for RoutineCalendarTests)
    - MyHomeApp/MyHome.xcodeproj/project.pbxproj (confirm all nine new files have build-file + file-reference + group + sources-phase entries)
  </read_first>
  <action>
Confirm every new file from this phase is registered in project.pbxproj: SchemaV9.swift, RoutineCompletion.swift, RoutineNotificationService.swift, StreakCalculator.swift, RoutineDetailView.swift (MyHome target); SchemaV9MigrationTests.swift, StreakCalculatorTests.swift, RoutineNotificationServiceTests.swift, NoteReorderTests.swift, RoutineCalendarTests.swift (MyHomeTests target). If any file is missing any of its 4 entries (PBXBuildFile, PBXFileReference, PBXGroup, PBXSourcesBuildPhase), add the missing entries. Run a clean full build and the complete test suite. If any new model/service/view is silently absent (the explicit-file-refs failure mode), the build or a corresponding test will fail — fix the pbxproj membership and re-run until green.
  </action>
  <verify>
    <automated>cd "/Users/reo/My Projects/my-home" && xcodebuild test -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - grep of project.pbxproj finds all nine new filenames (SchemaV9, RoutineCompletion, RoutineNotificationService, StreakCalculator, RoutineDetailView, SchemaV9MigrationTests, StreakCalculatorTests, RoutineNotificationServiceTests, NoteReorderTests, RoutineCalendarTests).
    - `xcodebuild test -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17'` completes with the FULL MyHomeTests suite passing (no failures, no compile errors).
    - No SchemaV8-only typealias remains (re-confirm grep from 12-01 is still clean post-merge).
  </acceptance_criteria>
  <done>Every new file is compiled into its target and the full test suite is green on the iPhone 17 simulator.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human UAT — manual-only routine behaviors</name>
  <action>Human-verify the four runtime/lifecycle behaviors that automated tests cannot cover (per 12-VALIDATION.md Manual-Only Verifications). Build and run on the iPhone 17 simulator, walk the four scenarios in <how-to-verify>, and record each outcome in 12-HUMAN-UAT.md. No code is written in this task; failures are routed to a gap-closure plan by the orchestrator.</action>
  <what-built>The full Phase 12 routine feature set: EditNoteView Routine section (toggle, optional daily reminder time, Done today, compact streak + history link), drag-to-reorder, RoutineDetailView 30-day history, DayAgendaView "Daily Routines" section, daily notification scheduling, and check-time completion recording. All automated tests are green; these four behaviors are runtime/lifecycle events that automated tests cannot cover (per 12-VALIDATION.md Manual-Only Verifications).</what-built>
  <how-to-verify>
1. Run the app on the iPhone 17 simulator (Xcode 26.5, scheme MyHome).
2. NOTE-03 — notification fires once: Open a note, toggle "Daily Routine" ON, enable "Daily Reminder", set the time to ~2 minutes from now. Leave the editor, background the app. Confirm EXACTLY ONE banner arrives at that time. Then change the time and re-save; confirm you do NOT get a second/duplicate banner at the old time (D-05 single-pending).
3. NOTE-04 — reorder persists: In a checklist note, tap the reorder toolbar button (arrow.up.arrow.down), drag items into a new order, tap Done to dismiss, reopen the note. Confirm the new order holds.
4. NOTE-01 — every-day surfacing + no dot inflation: Mark a note as a Daily Routine. Open the calendar; tap several different days (past, today, future). Confirm the routine appears in a "Daily Routines" section on every day. Confirm day cells do NOT show a new dot badge solely because of the routine (D-02).
5. NOTE-05 — cross-midnight streak: Complete a routine today (tick all boxes or tap Done today). Advance the simulator's date by one day (Settings or device date). Reopen the app. Confirm the checkboxes reset for the new day BUT the streak count and the RoutineDetailView history still show yesterday's completion (the completion record survived the reset).
  </how-to-verify>
  <done>All four manual-only behaviors verified and recorded in 12-HUMAN-UAT.md; any failures described for gap-closure routing.</done>
  <resume-signal>Type "approved" if all four behaviors pass, or describe any that failed (the orchestrator will route failures to a gap-closure plan).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build system -> target membership | a new file missing from pbxproj compiles to nothing (silent feature loss) |
| app -> iOS notification delivery | runtime notification scheduling verified only on device/simulator |
| package-manager installs | None this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03 | Denial of Service | new file silently absent from build | mitigate | Phase build gate greps all nine new filenames in pbxproj and runs the full suite; a missing file fails the build or its test, forcing the fix before phase exit |
| T-12-11 | Tampering | notification not actually delivered / duplicated at runtime | mitigate | Human UAT confirms exactly one banner fires at the set time and no duplicate after re-schedule (D-05) — the only verification possible for OS-delivered notifications |
| T-12-07 | Tampering | streak record lost across real midnight | mitigate | Human UAT advances the device date and confirms the streak/history survive the scenePhase .active reset while checkboxes reset |
| T-12-SC | Tampering | package installs | accept | No package installs this phase |
</threat_model>

<verification>
- Full `xcodebuild test -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17'` green.
- All nine new filenames present in project.pbxproj.
- Human UAT recorded in 12-HUMAN-UAT.md with all four behaviors approved.
</verification>

<success_criteria>
The full app builds with every new file compiled in and the complete test suite green; a human has verified notification single-delivery, reorder persistence, every-day routine surfacing without dot inflation, and cross-midnight streak retention.
</success_criteria>

<output>
Create `.planning/phases/12-notes-daily-routine-enhancement/12-HUMAN-UAT.md` recording the four manual-verification outcomes, and `.planning/phases/12-notes-daily-routine-enhancement/12-05-SUMMARY.md` when done.
</output>
