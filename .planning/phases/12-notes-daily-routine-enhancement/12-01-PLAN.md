---
phase: 12-notes-daily-routine-enhancement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - MyHomeApp/Persistence/Schema/SchemaV9.swift
  - MyHomeApp/Persistence/Schema/MigrationPlan.swift
  - MyHomeApp/Persistence/Models/RoutineCompletion.swift
  - MyHomeApp/Persistence/Models/Note.swift
  - MyHomeApp/Persistence/Models/NoteBlock.swift
  - MyHomeApp/Persistence/Models/Category.swift
  - MyHomeApp/Persistence/Models/Account.swift
  - MyHomeApp/Persistence/Models/Asset.swift
  - MyHomeApp/Persistence/Models/Expense.swift
  - MyHomeApp/Persistence/Models/NetWorthSnapshot.swift
  - MyHomeApp/Persistence/Models/SIPAmountChange.swift
  - MyHomeApp/Persistence/Models/SIP.swift
  - MyHomeApp/Persistence/Models/Contribution.swift
  - MyHomeApp/Persistence/ModelContainer+App.swift
  - MyHomeApp/MyHome.xcodeproj/project.pbxproj
  - MyHomeTests/SchemaV9MigrationTests.swift
  - MyHomeTests/StreakCalculatorTests.swift
  - MyHomeTests/RoutineNotificationServiceTests.swift
  - MyHomeTests/NoteReorderTests.swift
autonomous: true
requirements: [NOTE-05]
must_haves:
  truths:
    - "App boots against an existing SchemaV8 store and migrates additively to SchemaV9 with no data loss"
    - "RoutineCompletion entity is queryable after migration (empty table, not a crash)"
    - "Existing Note rows survive migration with routineDailyReminderTime == nil"
    - "All four new Wave-0 test files compile and are registered in the MyHomeTests target"
  artifacts:
    - path: "MyHomeApp/Persistence/Schema/SchemaV9.swift"
      provides: "SchemaV9 versioned schema — 10 copied V8 models + new routineDailyReminderTime field on Note + RoutineCompletion model"
      contains: "enum SchemaV9"
    - path: "MyHomeApp/Persistence/Models/RoutineCompletion.swift"
      provides: "RoutineCompletion typealias to SchemaV9.RoutineCompletion"
      contains: "typealias RoutineCompletion = SchemaV9.RoutineCompletion"
    - path: "MyHomeTests/SchemaV9MigrationTests.swift"
      provides: "Additive V8->V9 migration fixture test (BLOCKING)"
      contains: "SchemaV9MigrationTests"
  key_links:
    - from: "MyHomeApp/Persistence/ModelContainer+App.swift"
      to: "SchemaV9"
      via: "Schema(versionedSchema: SchemaV9.self)"
      pattern: "versionedSchema: SchemaV9"
    - from: "MyHomeApp/Persistence/Schema/MigrationPlan.swift"
      to: "v8ToV9 stage"
      via: "stages array"
      pattern: "v8ToV9"
---

<objective>
Introduce SchemaV9 as a purely additive migration: add `routineDailyReminderTime: Date?` to Note, introduce the new `RoutineCompletion` @Model, flip ALL 11 model typealiases atomically (STAB-08 footgun), append the `v8ToV9` nil-closure migration stage, register every new `.swift` file in `project.pbxproj`, and scaffold the four Wave-0 test files so downstream feature plans have failing-but-compiling test targets to fill in.

Purpose: This is the [BLOCKING] schema foundation. No feature plan can read or write `RoutineCompletion` until this lands. The atomic typealias flip and pbxproj registration are the two known landmines (STAB-08, explicit file refs) that silently break the build if done partially.
Output: SchemaV9.swift, RoutineCompletion.swift, modified MigrationPlan/ModelContainer/10 typealias files, pbxproj edits, and 4 Wave-0 test scaffolds — all compiling, with the BLOCKING migration fixture test green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-notes-daily-routine-enhancement/12-CONTEXT.md
@.planning/phases/12-notes-daily-routine-enhancement/12-RESEARCH.md
@.planning/phases/12-notes-daily-routine-enhancement/12-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SchemaV9 + RoutineCompletion model + atomic typealias flip + migration stage [BLOCKING]</name>
  <files>MyHomeApp/Persistence/Schema/SchemaV9.swift, MyHomeApp/Persistence/Schema/MigrationPlan.swift, MyHomeApp/Persistence/ModelContainer+App.swift, MyHomeApp/Persistence/Models/RoutineCompletion.swift, MyHomeApp/Persistence/Models/Note.swift, MyHomeApp/Persistence/Models/NoteBlock.swift, MyHomeApp/Persistence/Models/Category.swift, MyHomeApp/Persistence/Models/Account.swift, MyHomeApp/Persistence/Models/Asset.swift, MyHomeApp/Persistence/Models/Expense.swift, MyHomeApp/Persistence/Models/NetWorthSnapshot.swift, MyHomeApp/Persistence/Models/SIPAmountChange.swift, MyHomeApp/Persistence/Models/SIP.swift, MyHomeApp/Persistence/Models/Contribution.swift, MyHomeApp/MyHome.xcodeproj/project.pbxproj</files>
  <read_first>
    - MyHomeApp/Persistence/Schema/SchemaV8.swift (copy ALL 10 models verbatim; preserve the 8-rule CloudKit-readiness header; Note fields incl. isDailyRoutine/routineLastResetDate are the additive seam)
    - MyHomeApp/Persistence/Schema/MigrationPlan.swift (lines 11, 15, 123-132 — schemas array, stages array, v7ToV8 .custom nil-closure stage = analog for v8ToV9; FB13812722 workaround rationale)
    - MyHomeApp/Persistence/ModelContainer+App.swift (line 18 versionedSchema construction; container init derives models from SchemaV9.models automatically)
    - MyHomeApp/Persistence/Models/Note.swift (typealias flip comment structure to mirror across all 10 files)
    - MyHomeApp/Persistence/Models/Contribution.swift, MyHomeApp/Persistence/Models/SIP.swift (typealias one-liner analog for new RoutineCompletion.swift)
    - .planning/phases/12-notes-daily-routine-enhancement/12-PATTERNS.md (SchemaV9 section + Typealias section + RoutineCompletion @Model shape with exact fields)
    - .planning/phases/12-notes-daily-routine-enhancement/12-RESEARCH.md RQ-1 (full RoutineCompletion shape, dayKey IST semantics, bare-UUID-not-@Relationship rationale)
  </read_first>
  <action>
Create SchemaV9.swift mirroring SchemaV8: declare `enum SchemaV9: VersionedSchema` with `versionIdentifier = Schema.Version(9, 0, 0)`. Copy all 10 existing models verbatim (Expense, Category, Note, NoteBlock, Account, Asset, NetWorthSnapshot, SIP, SIPAmountChange, Contribution), replacing every `SchemaV8.` prefix with `SchemaV9.`. On `SchemaV9.Note`, APPEND ONE new field after the existing routineLastResetDate field: `var routineDailyReminderTime: Date? = nil` (D-04 — additive only, never reorder/remove). Add a new 11th @Model `RoutineCompletion` with fields exactly per 12-PATTERNS.md: `id: UUID = UUID()`, `noteID: UUID = UUID()` (bare UUID back-ref, NOT @Relationship — Pitfall 5/CloudKit rule 7), `dayKey: Date = Date()` (UTC representing IST start-of-day), `completedAt: Date = Date()`, `createdAt: Date = Date()`, and `init(noteID:dayKey:completedAt:)`. No `@Attribute(.unique)` anywhere (CloudKit rule 2). Add `SchemaV9.RoutineCompletion.self` to the `SchemaV9.models` array.

In MigrationPlan.swift: append `SchemaV9.self` to the `schemas` array (never remove V1-V8); append `v8ToV9` to the `stages` array; add the `v8ToV9` static stage as `MigrationStage.custom(fromVersion: SchemaV8.self, toVersion: SchemaV9.self, willMigrate: nil, didMigrate: nil)` mirroring the v7ToV8 stage (purely additive — no backfill; preserve the FB13812722 `.custom`-over-`.lightweight` rationale comment).

In ModelContainer+App.swift: change `Schema(versionedSchema: SchemaV8.self)` to `Schema(versionedSchema: SchemaV9.self)`. No other change — RoutineCompletion is auto-included via SchemaV9.models.

ATOMIC TYPEALIAS FLIP (STAB-08 — must be in THIS task/commit): flip all 10 existing typealias files from `SchemaV8.X` to `SchemaV9.X`: Category, Contribution, Account, Asset, Note, NoteBlock, Expense, NetWorthSnapshot, SIPAmountChange, SIP. Preserve each file's flip-history comment and add a Phase 12 / SchemaV9 line noting the atomic flip. Create the NEW Models/RoutineCompletion.swift with `typealias RoutineCompletion = SchemaV9.RoutineCompletion` mirroring Contribution.swift's comment structure.

pbxproj registration (Xcode explicit file refs — NO synchronized groups): add the 4 required entries (PBXBuildFile, PBXFileReference, PBXGroup membership, PBXSourcesBuildPhase membership) for the 2 NEW app files: SchemaV9.swift (group Persistence/Schema, target MyHome) and RoutineCompletion.swift (group Persistence/Models, target MyHome). The 10 flipped typealias files already exist in the project — no pbxproj change for them.
  </action>
  <verify>
    <automated>cd "/Users/reo/My Projects/my-home" && grep -L "SchemaV9" MyHomeApp/Persistence/Models/Note.swift MyHomeApp/Persistence/Models/NoteBlock.swift MyHomeApp/Persistence/Models/Category.swift MyHomeApp/Persistence/Models/Account.swift MyHomeApp/Persistence/Models/Asset.swift MyHomeApp/Persistence/Models/Expense.swift MyHomeApp/Persistence/Models/NetWorthSnapshot.swift MyHomeApp/Persistence/Models/SIPAmountChange.swift MyHomeApp/Persistence/Models/SIP.swift MyHomeApp/Persistence/Models/Contribution.swift MyHomeApp/Persistence/Models/RoutineCompletion.swift; test $(grep -L "SchemaV9" MyHomeApp/Persistence/Models/Note.swift MyHomeApp/Persistence/Models/NoteBlock.swift MyHomeApp/Persistence/Models/Category.swift MyHomeApp/Persistence/Models/Account.swift MyHomeApp/Persistence/Models/Asset.swift MyHomeApp/Persistence/Models/Expense.swift MyHomeApp/Persistence/Models/NetWorthSnapshot.swift MyHomeApp/Persistence/Models/SIPAmountChange.swift MyHomeApp/Persistence/Models/SIP.swift MyHomeApp/Persistence/Models/Contribution.swift MyHomeApp/Persistence/Models/RoutineCompletion.swift | wc -l) -eq 0 && xcodebuild build -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' 2>&1 | tail -5 | grep -q 'BUILD SUCCEEDED'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "SchemaV8\." MyHomeApp/Persistence/Models/*.swift` returns 0 for all typealias files (no file left pointing at SchemaV8) — i.e. ALL 11 typealiases flipped (10 existing + new RoutineCompletion).
    - SchemaV9.swift contains `enum SchemaV9`, `final class RoutineCompletion`, `var routineDailyReminderTime: Date?`, and lists `SchemaV9.RoutineCompletion.self` in its `models` array; contains no `@Attribute(.unique)`.
    - MigrationPlan.swift `stages` array text contains `v8ToV9`; `v8ToV9` is a `.custom` stage with `willMigrate: nil, didMigrate: nil`.
    - ModelContainer+App.swift contains `versionedSchema: SchemaV9.self` and no longer references `versionedSchema: SchemaV8.self`.
    - project.pbxproj references both `SchemaV9.swift` and `RoutineCompletion.swift` (grep finds each filename) with build-file + file-reference + group + sources-phase entries; `xcodebuild build -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17'` compiles (both new files compiled into the target).
    - RoutineCompletion @Model uses `var noteID: UUID` (bare UUID), NOT `@Relationship`.
  </acceptance_criteria>
  <done>SchemaV9 exists with the new field + RoutineCompletion model; all 11 typealiases point at SchemaV9; v8ToV9 nil-closure stage appended; container builds on SchemaV9; both new app files registered in pbxproj and the target compiles.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wave-0 test scaffolds — SchemaV9 migration fixture (BLOCKING) + StreakCalculator/Notification/Reorder stubs</name>
  <files>MyHomeTests/SchemaV9MigrationTests.swift, MyHomeTests/StreakCalculatorTests.swift, MyHomeTests/RoutineNotificationServiceTests.swift, MyHomeTests/NoteReorderTests.swift, MyHomeApp/MyHome.xcodeproj/project.pbxproj</files>
  <read_first>
    - MyHomeTests/SchemaV8MigrationTests.swift (the MigrationTestsPlanV7 trimmed-plan pattern + genuine-store-build + copy-seed-to-migrate-URL + open-under-new-schema assertion structure)
    - MyHomeTests/RoutineResetServiceTests.swift (lines 18-21 makeContainer in-memory pattern; IST day-key helpers; NoteBlock seed pattern)
    - MyHomeTests/Support/SpyCenter.swift (addedRequests / addedIdentifiers / removedIdentifierSets API — note: add appends, removePending removes from addedRequests so addedRequests == current pending set)
    - .planning/phases/12-notes-daily-routine-enhancement/12-PATTERNS.md (Test File Patterns section — exact test names per file)
    - .planning/phases/12-notes-daily-routine-enhancement/12-RESEARCH.md RQ-1 (UUID-predicate Open Question — write the fetch-by-noteID predicate test to confirm `$0.noteID == noteID` compiles; if it fails to compile, fall back to `$0.noteID.uuidString == noteID.uuidString` and note in the SUMMARY)
  </read_first>
  <behavior>
    - SchemaV9MigrationTests: building a genuine V8 store (via a MigrationTestsPlanV8 trimmed plan stopping at SchemaV8) with one Note, then opening it under SchemaV9 + AppMigrationPlan, yields notes.count == 1 with routineDailyReminderTime == nil; fetching RoutineCompletion returns [] without crashing.
    - StreakCalculatorTests: stub test cases (named per 12-PATTERNS.md) compile against `RoutineCompletion` and an in-memory container including RoutineCompletion.self. Cases assert the FUTURE StreakCalculator API surface — leave bodies as failing/`Issue.record("pending Wave 2")` or `#expect(Bool(false))` so they compile now and get filled in plan 12-02. Do NOT yet implement StreakCalculator.
    - RoutineNotificationServiceTests: stub cases verifying single-pending-after-reschedule using SpyCenter; compile-only, pending Wave 2 (RoutineNotificationService does not exist yet — reference it behind a `// pending 12-02` comment, do NOT call it; keep the file compiling by testing SpyCenter directly as a placeholder).
    - NoteReorderTests: the reorder-persists test is fully implementable now (it only needs NoteBlock + order + context.save) — implement it green per 12-PATTERNS.md NoteReorderTests example (move index 2 to 0, expect order ["C","A","B"]).
    - The UUID-predicate confirmation test (fetch RoutineCompletion by noteID) is implementable now and MUST pass — it resolves Open Question #1.
  </behavior>
  <action>
Create the four test files. Register all four in project.pbxproj (4 edits each: PBXBuildFile, PBXFileReference, PBXGroup MyHomeTests, PBXSourcesBuildPhase MyHomeTests target).

SchemaV9MigrationTests.swift (BLOCKING): mirror SchemaV8MigrationTests — add a `MigrationTestsPlanV8` trimmed SchemaMigrationPlan whose `schemas` stops at SchemaV8 and `stages` stops at v7ToV8. Write `v8StoreNoteSurvivesWithNilReminderTime()` (build V8 store with one Note, copy seed to migrate URL, open under `Schema(versionedSchema: SchemaV9.self)` + `AppMigrationPlan`, assert notes.count == 1 and note.routineDailyReminderTime == nil) and `routineCompletionQueryableAfterMigration()` (assert `context.fetch(FetchDescriptor<RoutineCompletion>())` returns [] post-migration without crashing). Both must be GREEN.

StreakCalculatorTests.swift: define `makeContainer()` returning `ModelContainer(for: Note.self, NoteBlock.self, RoutineCompletion.self, ...)` and IST calendar helpers. Add the test cases named in 12-PATTERNS.md (streakIsZeroWithNoCompletions, incompleteToday_doesNotBreakStreak, streakBreaksOnMissedDay, completingTodayExtendsStreak, idempotentCompletion). Leave each body as a compiling pending stub (e.g. record an Issue or `#expect(Bool(false), "pending 12-02")`) — these go green in plan 12-02 when StreakCalculator exists. Also add `uuidPredicateFetchCompiles()` that inserts a RoutineCompletion and fetches via `#Predicate<RoutineCompletion> { $0.noteID == noteID }` — this MUST pass (resolves Open Question #1; fall back to `.uuidString` comparison if the macro rejects it, and record the chosen form in the SUMMARY).

RoutineNotificationServiceTests.swift: define `@MainActor struct RoutineNotificationServiceTests` using SpyCenter. Add pending stub cases (rescheduleIsAtomicCancelThenAdd, exactlyOnePendingRequest, cancelRemovesPendingRequest) that compile but are marked pending 12-02 (RoutineNotificationService is created in 12-02).

NoteReorderTests.swift: implement `reorderPersists()` fully per 12-PATTERNS.md (seed 3 NoteBlocks order 0/1/2, move index 2 to 0, re-index, save, refetch sorted by order, expect ["C","A","B"]). This MUST be GREEN now.
  </action>
  <verify>
    <automated>cd "/Users/reo/My Projects/my-home" && xcodebuild test -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:MyHomeTests/SchemaV9MigrationTests -only-testing:MyHomeTests/NoteReorderTests 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - All four test files referenced in project.pbxproj (grep finds each filename) and compiled into MyHomeTests target.
    - `xcodebuild test ... -only-testing:MyHomeTests/SchemaV9MigrationTests` passes (BLOCKING migration fixture green — V8 store migrates additively, RoutineCompletion queryable).
    - `xcodebuild test ... -only-testing:MyHomeTests/NoteReorderTests` passes (reorderPersists green).
    - StreakCalculatorTests and RoutineNotificationServiceTests COMPILE (full suite builds) even though their feature-dependent cases are pending stubs.
    - The UUID-predicate fetch test passes; the SUMMARY states whether `$0.noteID == noteID` or the `.uuidString` fallback was used (Open Question #1 resolved).
  </acceptance_criteria>
  <done>BLOCKING SchemaV9 migration fixture test is green; NoteReorder test green; the other two suites compile as pending stubs ready for plan 12-02; Open Question #1 (UUID predicate) resolved and recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| existing on-disk SwiftData store -> SchemaV9 migration | A real V8 store (production data) is opened under the new schema; data-integrity boundary |
| package-manager installs | None this phase — no npm/pip/cargo/SPM additions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | V8->V9 SwiftData migration | mitigate | Purely additive `.custom` stage with nil closures (no field removal/rename); BLOCKING fixture test `SchemaV9MigrationTests` proves an existing V8 store survives with zero data loss before any feature plan runs |
| T-12-02 | Tampering | Schema typealias partial flip (STAB-08) | mitigate | All 11 typealiases flipped atomically in Task 1; acceptance gate greps that no Models/*.swift still references `SchemaV8.`; partial flip = save/query crash, so this is enforced, not advisory |
| T-12-03 | Denial of Service | new .swift files not in pbxproj | mitigate | Each new file gets the 4 pbxproj edits in-task; build gate confirms target compiles with new files included (silent non-compile = missing functionality) |
| T-12-SC | Tampering | package installs | accept | No package installs this phase; RESEARCH Package Legitimacy Audit confirms zero external packages |
</threat_model>

<verification>
- `xcodebuild build -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17'` compiles with SchemaV9 + RoutineCompletion included.
- BLOCKING: `xcodebuild test ... -only-testing:MyHomeTests/SchemaV9MigrationTests` green.
- No `Models/*.swift` references `SchemaV8.` (grep clean).
</verification>

<success_criteria>
SchemaV9 is the active container schema; RoutineCompletion is a queryable empty table after additive migration from a real V8 store; all 11 typealiases atomically flipped; both new app files + four new test files registered in pbxproj; the BLOCKING migration fixture and the reorder test are green; the UUID-predicate form is resolved.
</success_criteria>

<output>
Create `.planning/phases/12-notes-daily-routine-enhancement/12-01-SUMMARY.md` when done. Record: the resolved UUID-predicate form (Open Question #1), confirmation all 11 typealiases flipped, and the pbxproj edits made.
</output>
