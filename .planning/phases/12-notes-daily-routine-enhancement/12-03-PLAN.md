---
phase: 12-notes-daily-routine-enhancement
plan: 03
type: execute
wave: 3
depends_on: ["12-01", "12-02"]
files_modified:
  - MyHomeApp/Features/Notes/EditNoteView.swift
  - MyHomeApp/Features/Notes/RoutineDetailView.swift
  - MyHomeApp/MyHome.xcodeproj/project.pbxproj
autonomous: true
requirements: [NOTE-01, NOTE-03, NOTE-04, NOTE-05]
must_haves:
  truths:
    - "A user can toggle any note into a daily routine from the editor's Routine section (D-10)"
    - "A user can set an optional daily reminder time that schedules exactly one notification (D-04/D-05)"
    - "A user can drag-reorder checklist items in any checklist note and the new order persists after dismiss (D-11)"
    - "Completing all checkboxes (or tapping Done today for text routines) records a completion at check-time, surviving the midnight reset (D-06)"
    - "A compact streak shows in the editor and a full 30-day history opens in RoutineDetailView (D-09)"
  artifacts:
    - path: "MyHomeApp/Features/Notes/RoutineDetailView.swift"
      provides: "Full 30-day streak/history screen mirroring AssetDetailView header-card + insetGrouped list"
      contains: "struct RoutineDetailView"
    - path: "MyHomeApp/Features/Notes/EditNoteView.swift"
      provides: "Routine section (toggle + reminder time + Done today + compact streak), drag-reorder, check-time completion recording"
      contains: "routineSection"
  key_links:
    - from: "EditNoteView.toggleCheck / Done today"
      to: "RoutineCompletion"
      via: "recordTodayCompletion fetch-before-insert at check-time"
      pattern: "RoutineCompletion\\(noteID:"
    - from: "EditNoteView routineSection time picker"
      to: "RoutineNotificationService.schedule"
      via: "cancel-then-add on time change / toggle"
      pattern: "RoutineNotificationService\\(\\)"
    - from: "EditNoteView reorderBlocks"
      to: "NoteBlock.order"
      via: "onMove re-index + markDirty"
      pattern: "block.order = idx"
---

<objective>
Wire the routine experience into EditNoteView and build the RoutineDetailView screen. EditNoteView gains: a "Routine" GroupBox section (toggle D-10, optional daily reminder time D-04 wired to RoutineNotificationService D-05, "Done today" for text-only routines D-06, compact streak + "Routine History" link D-09); drag-to-reorder for ALL checklist notes via an EditMode List with onMove (D-11); and check-time completion recording at the last-checkbox tick (D-06). RoutineDetailView is a new screen showing the current streak as a hero card plus a scrollable 30-day history, mirroring AssetDetailView.

Purpose: This is the primary user-facing surface for marking routines, scheduling reminders, reordering, and viewing streaks. Completion MUST be written at check-time (before RoutineResetService wipes isChecked overnight) or the streak record is lost — the central architectural constraint of this phase.
Output: modified EditNoteView.swift, new RoutineDetailView.swift, pbxproj registration; full suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/12-notes-daily-routine-enhancement/12-CONTEXT.md
@.planning/phases/12-notes-daily-routine-enhancement/12-RESEARCH.md
@.planning/phases/12-notes-daily-routine-enhancement/12-PATTERNS.md
@.planning/phases/12-notes-daily-routine-enhancement/12-UI-SPEC.md
@.planning/phases/12-notes-daily-routine-enhancement/12-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: EditNoteView — Routine section + check-time completion recording + reminder wiring (D-04/D-06/D-09/D-10)</name>
  <files>MyHomeApp/Features/Notes/EditNoteView.swift</files>
  <read_first>
    - MyHomeApp/Features/Notes/EditNoteView.swift (body VStack, titleField, blockList, addBlockButtons, toggleCheck lines 375-382, markDirty/debouncer lines 310-342, deleteNote lines 404-414, saveError alert line 140, toolbar lines 100-127, init signature)
    - MyHomeApp/Features/Notes/RoutineResetService.swift (lines 26-29 — IST day-key for recordTodayCompletion)
    - MyHomeApp/Features/Notes/RoutineNotificationService.swift (schedule/cancel signatures from 12-02)
    - MyHomeApp/Features/Notes/StreakCalculator.swift (compute signature from 12-02)
    - MyHomeApp/Persistence/Models/RoutineCompletion.swift (field names)
    - .planning/phases/12-notes-daily-routine-enhancement/12-PATTERNS.md (EditNoteView section — routineSection, recordTodayCompletion, checkAndRecordCompletion, currentStreak, modified toggleCheck, modified deleteNote, @Query-in-init for completions)
    - .planning/phases/12-notes-daily-routine-enhancement/12-UI-SPEC.md (Surface 1 — exact layout, copy, interactions; Error States; Security note)
    - .planning/phases/12-notes-daily-routine-enhancement/12-02-SUMMARY.md (service signatures)
  </read_first>
  <action>
Add to EditNoteView: a `@Query private var completions: [RoutineCompletion]` captured in `init` via `#Predicate<RoutineCompletion> { $0.noteID == noteID }` (Pitfall 4 — capture note.id into a local before the predicate; use the predicate form resolved in 12-01). Append a `routineSection` `@ViewBuilder` to the body VStack after addBlockButtons, rendered as `GroupBox("Routine")` per UI-SPEC Surface 1: `Toggle("Daily Routine", isOn: $note.isDailyRoutine)`; when on, a "Daily Reminder" enable toggle (bound to `routineDailyReminderTime != nil`, default 07:00 when enabled), a `DatePicker("Time", ..., displayedComponents: .hourAndMinute)` when a time is set, a `Button("Done today")` (`.buttonStyle(.bordered)`, `.frame(minHeight: 44)`) shown ONLY when the note has no checkbox blocks (D-06), and a compact `HStack` with `Text("🔥 \(currentStreak) day streak").font(.subheadline)` + `NavigationLink("Routine History") { RoutineDetailView(note: note) }`. GroupBox bottom padding 16pt. Render all strings via plain `Text(...)` — no AttributedString (security T-12-10).

Add `currentStreak` computed property calling `StreakCalculator.compute(for: note.id, completions: completions, today: Date(), calendar: <IST gregorian>)`.currentStreak.

Add `recordTodayCompletion()`: build IST dayKey (RoutineResetService pattern), fetch-before-insert a RoutineCompletion for `(noteID, dayKey)` — if found update completedAt, else insert; let markDirty()'s debounced save persist it (idempotent — D-06/D-08). Add `checkAndRecordCompletion()`: guard the note has checkbox blocks and all are checked, then call recordTodayCompletion. Modify `toggleCheck` to call `checkAndRecordCompletion()` when `note.isDailyRoutine && block.isChecked` after the existing toggle logic, before markDirty().

Wire reminder scheduling: on the Daily Routine toggle onChange, always `RoutineNotificationService().cancel(noteID: note.id)` first, then if on and a time is set, `Task { await RoutineNotificationService().schedule(noteID: note.id, title: note.title, time: time) }`; clear `routineDailyReminderTime = nil` when toggled off. On the time picker set and the Daily Reminder enable toggle, call schedule/cancel accordingly (cancel-then-add — D-05). Add a toolbar `Image(systemName: "arrow.up.arrow.down")` button with `.accessibilityLabel("Reorder items")` (the reorder toggle wiring lands in Task 2; place the button here). Modify `deleteNote()` to call `RoutineNotificationService().cancel(noteID: note.id)` before `context.delete(note)` (orphaned-notification mitigation — T-12-06). Add the notification-permission-denied `.alert` copy per UI-SPEC Error States, reusing the saveError alert style.
  </action>
  <verify>
    <automated>cd "/Users/reo/My Projects/my-home" && xcodebuild build -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - EditNoteView.swift contains `routineSection`, `recordTodayCompletion`, `checkAndRecordCompletion`, and `currentStreak`; toggleCheck calls `checkAndRecordCompletion()` guarded by `note.isDailyRoutine`.
    - recordTodayCompletion uses fetch-before-insert (grep finds `FetchDescriptor<RoutineCompletion>` and both an `existing.completedAt =` update branch and a `context.insert(RoutineCompletion(` branch) — no `.unique`, no duplicate insert.
    - The Daily Routine/reminder paths call `RoutineNotificationService().cancel(` and `RoutineNotificationService().schedule(` with cancel ordered before schedule; deleteNote calls cancel before `context.delete(note)`.
    - Routine strings rendered via plain `Text(...)`; grep finds no `AttributedString` in EditNoteView.swift.
    - `xcodebuild build -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17'` succeeds.
  </acceptance_criteria>
  <done>EditNoteView has a working Routine section (toggle, optional daily reminder, Done today, compact streak + history link), records completions at check-time idempotently, schedules a single daily notification, and cancels it on delete; project builds.</done>
</task>

<task type="auto">
  <name>Task 2: EditNoteView drag-to-reorder (D-11) + RoutineDetailView screen (D-09)</name>
  <files>MyHomeApp/Features/Notes/EditNoteView.swift, MyHomeApp/Features/Notes/RoutineDetailView.swift, MyHomeApp/MyHome.xcodeproj/project.pbxproj</files>
  <read_first>
    - MyHomeApp/Features/Notes/EditNoteView.swift (blockList lines 173-185, sortedBlocks lines 300-306 — the open-above-checked split; markDirty)
    - MyHomeApp/Features/Assets/AssetDetailView.swift (lines 1-17 struct + @Query-in-init; 67-74 header-card list-row treatment; 119-151 headerCard contents; 49-53 gain color pattern; cardStyle usage)
    - MyHomeApp/Features/Notes/StreakCalculator.swift (compute returns StreakResult.history of 30 DayStatus; from 12-02)
    - .planning/phases/12-notes-daily-routine-enhancement/12-PATTERNS.md (EditNoteView blocksForDisplay/blockList-with-onMove/reorderBlocks; RoutineDetailView section)
    - .planning/phases/12-notes-daily-routine-enhancement/12-RESEARCH.md RQ-6 + Open Question #2 (List-only-in-edit-mode to avoid full ScrollView restructure; verify onMove works in the nested List, fall back to wrapping all checklist blocks in a List if needed) + Assumption A1
    - .planning/phases/12-notes-daily-routine-enhancement/12-UI-SPEC.md (Surface 2 drag-to-reorder; Surface 4 RoutineDetailView exact layout, copy, streak status lines, empty state, history row treatment)
  </read_first>
  <action>
Drag-to-reorder (D-11, ALL checklist notes): add `@State private var editMode: EditMode = .inactive` and a `blocksForDisplay` computed property that returns raw `order`-sorted blocks when `editMode.isEditing` else the existing `sortedBlocks` (open-above-checked). Modify `blockList` so that when `editMode.isEditing` it renders the blocks inside a `List { ForEach(blocksForDisplay) { blockRow($0) }.onMove { reorderBlocks(from:to:) } }.environment(\.editMode, $editMode)` (nested List only in edit mode per Open Question #2 — keep the existing ScrollView+VStack+ForEach path when not editing). Wire the toolbar reorder button (added in Task 1) to toggle `editMode` between `.active`/`.inactive` with `withAnimation`. Add `reorderBlocks(from:to:)`: sort `note.blocks` by order, `move(fromOffsets:toOffset:)`, re-index each block's `order` to its new index (0,1,2,...), then `markDirty()`. VERIFY onMove fires in the nested List on the iPhone 17 simulator; if it does not, fall back to wrapping the full checklist in a List regardless of edit mode and record the deviation in the SUMMARY.

RoutineDetailView (D-09, new file): create `struct RoutineDetailView: View` taking `var note: Note` with `@Query private var completions: [RoutineCompletion]` captured in `init(note:)` via the resolved UUID predicate, sorted by dayKey reverse. Body is a `List` with `.listStyle(.insetGrouped)`, `.navigationTitle(note.title)` (plain string), `.navigationBarTitleDisplayMode(.inline)`. Section 1: a header card (cardStyle, clear row background, hidden separator, EdgeInsets per AssetDetailView) showing "Daily Routine" subheadline, `Text("🔥 \(currentStreak)")` as `.largeTitle.weight(.semibold)`, the "day streak"/"days streak" unit label, and the streak status line ("Today's streak is active" / "Complete today to extend your streak" / "Start your streak today" per UI-SPEC). Section 2 "Last 30 Days": ForEach over `StreakCalculator.compute(...).history` rendering each DayStatus as an HStack — `checkmark.circle.fill` in systemGreen when completed else `circle` in `.secondary`, the formatted weekday/day/month (today shown as "Today"), and a trailing "Done"/"—" label; `.frame(minHeight: 44)`. Empty-state row when there are zero completions per UI-SPEC copy. Compute currentStreak / history via StreakCalculator with the IST calendar and `today: Date()`. Plain `Text(...)` only (security). Register RoutineDetailView.swift in pbxproj (4 edits, group Features/Notes, target MyHome).
  </action>
  <verify>
    <automated>cd "/Users/reo/My Projects/my-home" && xcodebuild test -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:MyHomeTests/NoteReorderTests 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - EditNoteView.swift contains `editMode`, `blocksForDisplay`, `reorderBlocks`, and an `.onMove` modifier; reorderBlocks re-indexes via `block.order = idx` then `markDirty()`.
    - RoutineDetailView.swift contains `struct RoutineDetailView`, a `@Query` captured in `init(note:)`, a cardStyle header card with the streak hero number, and a "Last 30 Days" section over StreakCalculator history; uses `Color(.systemGreen)` for completed days; plain `Text(...)` only (no AttributedString).
    - RoutineDetailView.swift referenced in project.pbxproj and compiled into MyHome target.
    - `xcodebuild test ... -only-testing:MyHomeTests/NoteReorderTests` passes (order persistence regression stays green).
    - Full `xcodebuild build -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17'` succeeds.
    - SUMMARY records whether onMove worked in the nested-List-in-edit-mode form or required the always-List fallback (Open Question #2 resolved).
  </acceptance_criteria>
  <done>Checklist items in any checklist note can be dragged to reorder with persisted order; RoutineDetailView shows the streak hero card and a 30-day history; both registered/compiled; reorder regression test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-authored note title/streak strings -> SwiftUI Text rendering | user content rendered in editor, detail view, and notification |
| EditNoteView -> RoutineNotificationService -> iOS scheduling | notification scheduling crosses to the OS |
| package-manager installs | None this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-06 | Information Disclosure | orphaned notification after note delete | mitigate | `deleteNote()` calls `RoutineNotificationService().cancel(noteID:)` before `context.delete(note)` — no pending notification references a deleted note |
| T-12-07 | Tampering | completion record lost to midnight reset | mitigate | recordTodayCompletion writes at check-time (last checkbox / Done today) with fetch-before-insert idempotency, before RoutineResetService can wipe isChecked; the record survives the reset (RoutineCompletion is independent of isChecked) |
| T-12-10 | Information Disclosure | markdown/AttributedString injection in routine/streak strings | mitigate | All routine, streak, and history strings rendered via plain `Text(...)`; acceptance greps confirm no `AttributedString` in EditNoteView.swift or RoutineDetailView.swift (T-114-03 / T-11-10 rule) |
| T-12-08 | Input Validation | reminder time / title input | accept | Time is a DatePicker (bounded), title already trimmed by existing EditNoteView logic; SwiftData #Predicate is compiled, not string-interpolated — no injection surface |
| T-12-SC | Tampering | package installs | accept | No package installs this phase |
</threat_model>

<verification>
- `xcodebuild build -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17'` succeeds.
- `xcodebuild test ... -only-testing:MyHomeTests/NoteReorderTests` green.
- No `AttributedString` in EditNoteView.swift or RoutineDetailView.swift.
- RoutineDetailView.swift registered in pbxproj.
</verification>

<success_criteria>
EditNoteView exposes the full Routine section (toggle, optional single daily reminder, Done today, compact streak + history link), records completions at check-time idempotently, and supports drag-reorder of checklist items with persisted order; RoutineDetailView shows the streak hero and 30-day history; all rendered via plain Text; project builds and the reorder regression stays green.
</success_criteria>

<output>
Create `.planning/phases/12-notes-daily-routine-enhancement/12-03-SUMMARY.md` when done. Record the onMove form chosen (Open Question #2) and the recordTodayCompletion call sites added.
</output>
