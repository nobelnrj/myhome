---
phase: 12-notes-daily-routine-enhancement
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - MyHomeApp/Features/Notes/StreakCalculator.swift
  - MyHomeApp/Features/Notes/RoutineNotificationService.swift
  - MyHomeApp/MyHome.xcodeproj/project.pbxproj
  - MyHomeTests/StreakCalculatorTests.swift
  - MyHomeTests/RoutineNotificationServiceTests.swift
autonomous: true
requirements: [NOTE-03, NOTE-05]
must_haves:
  truths:
    - "Current streak = consecutive completed days; an incomplete TODAY does not break the streak (D-07)"
    - "Completing today extends the streak by 1; a fully-missed PAST day breaks it (D-07)"
    - "Exactly one pending daily notification exists per routine note after any number of re-schedules (D-05)"
    - "Re-scheduling cancels the prior request synchronously before adding the new one (D-05)"
  artifacts:
    - path: "MyHomeApp/Features/Notes/StreakCalculator.swift"
      provides: "Pure streak/history algorithm over [RoutineCompletion] with injected IST calendar + today"
      contains: "enum StreakCalculator"
    - path: "MyHomeApp/Features/Notes/RoutineNotificationService.swift"
      provides: "Daily time-only repeating notification with stable per-note identifier, cancel-then-add"
      contains: "struct RoutineNotificationService"
  key_links:
    - from: "RoutineNotificationService.schedule"
      to: "NotificationCenterPort"
      via: "cancel (sync) then center.add (async)"
      pattern: "removePendingNotificationRequests"
    - from: "StreakCalculator.compute"
      to: "RoutineCompletion records"
      via: "filter by noteID + IST startOfDay set membership"
      pattern: "completedDays.contains"
---

<objective>
Build the two pure/back-end pieces that the UI plans consume: `StreakCalculator` (a pure, injectable-calendar function over `[RoutineCompletion]` implementing the forgiving D-07 streak rule) and `RoutineNotificationService` (a daily time-only `UNCalendarNotificationTrigger` with a stable per-note identifier and the cancel-then-add single-pending guarantee, D-05). Turn the pending Wave-0 stubs from plan 12-01 green.

Purpose: Both are pure/service layers with no SwiftUI dependency, so they are unit-testable without a view and can be built in parallel with nothing else competing for their files. Getting the streak semantics (D-07) and the single-pending guarantee (D-05) provably correct here de-risks the UI integration in Wave 3.
Output: StreakCalculator.swift, RoutineNotificationService.swift, both registered in pbxproj, with StreakCalculatorTests and RoutineNotificationServiceTests fully green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-notes-daily-routine-enhancement/12-CONTEXT.md
@.planning/phases/12-notes-daily-routine-enhancement/12-RESEARCH.md
@.planning/phases/12-notes-daily-routine-enhancement/12-PATTERNS.md
@.planning/phases/12-notes-daily-routine-enhancement/12-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: StreakCalculator pure algorithm (D-07 forgiving streak) — turn StreakCalculatorTests green</name>
  <files>MyHomeApp/Features/Notes/StreakCalculator.swift, MyHomeTests/StreakCalculatorTests.swift, MyHomeApp/MyHome.xcodeproj/project.pbxproj</files>
  <read_first>
    - MyHomeApp/Features/Notes/RoutineResetService.swift (lines 25-29 — exact IST Gregorian calendar construction to mirror for day-boundary math)
    - MyHomeApp/Persistence/Models/RoutineCompletion.swift (the typealias + field names noteID/dayKey/completedAt, created in 12-01)
    - MyHomeTests/StreakCalculatorTests.swift (the pending stubs from 12-01 — fill their bodies)
    - .planning/phases/12-notes-daily-routine-enhancement/12-PATTERNS.md (StreakCalculator section — DayStatus/StreakResult/compute signature and the streak-walk loop)
    - .planning/phases/12-notes-daily-routine-enhancement/12-RESEARCH.md RQ-2 (D-07 semantics: startOffset = todayCompleted ? 0 : 1; first miss ends streak; 30-day cap)
    - .planning/phases/12-notes-daily-routine-enhancement/12-01-SUMMARY.md (resolved UUID-predicate form, if relevant to filtering)
  </read_first>
  <behavior>
    - compute(noteID:completions:today:calendar:) returns currentStreak == 0 when no completions exist for the note.
    - Given completions for yesterday and the day before (but NOT today), currentStreak reflects that 2-day run — today-incomplete does NOT reset to 0 (D-07).
    - Given completions for yesterday + today, currentStreak == prior run + 1 (today extends).
    - A gap on a past (pre-today) day stops the count at the gap (first miss ends streak).
    - history is exactly 30 DayStatus entries, newest first, each flagged isCompleted by IST-startOfDay set membership.
    - Streak count is capped at the 30-day window.
    - completions for OTHER noteIDs are ignored.
  </behavior>
  <action>
Create StreakCalculator.swift in Features/Notes. Define `struct DayStatus { let dayKey: Date; let isCompleted: Bool }`, `struct StreakResult { let currentStreak: Int; let history: [DayStatus] }`, and `enum StreakCalculator` with a single `static func compute(for noteID: UUID, completions: [RoutineCompletion], today: Date, calendar: Calendar) -> StreakResult`. Implement exactly per 12-PATTERNS.md/RQ-2: build a `completedDays` Set of `calendar.startOfDay(for: $0.dayKey)` filtered to noteID; build a 30-entry `history` window newest-first ending at today; compute the streak by walking backward from `today` with `startOffset = completedDays.contains(todayKey) ? 0 : 1`, incrementing while days are present and breaking on first miss, capped at 30. Do NOT call `Date()` or build the calendar internally — both `today` and `calendar` are injected for testability. Register StreakCalculator.swift in pbxproj (4 edits, group Features/Notes, target MyHome).

Fill the pending StreakCalculatorTests stubs from 12-01 with concrete assertions matching the behavior block above, passing a fixed `today` and the IST calendar so the tests are deterministic. Seed RoutineCompletion rows directly (in-memory container) with `dayKey` = IST startOfDay offset by N days. Use the UUID-predicate form resolved in 12-01.
  </action>
  <verify>
    <automated>cd "/Users/reo/My Projects/my-home" && xcodebuild test -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:MyHomeTests/StreakCalculatorTests 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - StreakCalculator.swift contains `enum StreakCalculator` and a `static func compute(for:completions:today:calendar:)`; it does not call `Date()` internally (grep shows `today` is a parameter, not a `Date()` call inside compute).
    - StreakCalculator.swift referenced in project.pbxproj and compiles into MyHome target.
    - `xcodebuild test ... -only-testing:MyHomeTests/StreakCalculatorTests` passes ALL cases (streakIsZeroWithNoCompletions, incompleteToday_doesNotBreakStreak, streakBreaksOnMissedDay, completingTodayExtendsStreak, idempotentCompletion, plus the UUID-predicate test from 12-01).
    - history array count == 30 in tests; cross-note completions are excluded (verified by a case seeding a second noteID).
  </acceptance_criteria>
  <done>StreakCalculator implements the D-07 forgiving rule as a pure injectable function; all StreakCalculatorTests cases green; file registered in pbxproj and compiling.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RoutineNotificationService daily notification (D-05 single-pending) — turn RoutineNotificationServiceTests green</name>
  <files>MyHomeApp/Features/Notes/RoutineNotificationService.swift, MyHomeTests/RoutineNotificationServiceTests.swift, MyHomeApp/MyHome.xcodeproj/project.pbxproj</files>
  <read_first>
    - MyHomeApp/Support/SIPAccrualService.swift (lines 483-512 — direct UNCalendarNotificationTrigger time-only repeating pattern; cancel-then-add; the analog this service mirrors. Do NOT use NotificationScheduler.buildRequests)
    - MyHomeApp/Support/NotificationCenterPort.swift (the port protocol; SystemNotificationCenter production conformer; default-arg injection point)
    - MyHomeApp/Support/NotificationActions.swift (line 9 — kReminderCategoryID constant so the Complete action appears)
    - MyHomeTests/Support/SpyCenter.swift (addedRequests / addedIdentifiers / removedIdentifierSets — add appends, removePending removes from addedRequests, so addedRequests == current pending set)
    - MyHomeTests/RoutineNotificationServiceTests.swift (pending stubs from 12-01 — fill bodies)
    - .planning/phases/12-notes-daily-routine-enhancement/12-PATTERNS.md (RoutineNotificationService section — full schedule/cancel pattern + stable identifier)
    - .planning/phases/12-notes-daily-routine-enhancement/12-RESEARCH.md RQ-4 + Pitfall 3 (cancel synchronous BEFORE async add; userInfo carries ONLY noteID + isRoutineReminder)
  </read_first>
  <behavior>
    - identifier(for:) returns the stable string "routine-daily-{uuidString}" — distinct from the NotificationScheduler reminder identifier scheme.
    - schedule(noteID:title:time:) cancels any existing request for that noteID, then adds exactly one request with a time-only UNCalendarNotificationTrigger(repeats: true).
    - After scheduling the same noteID three times with different times, exactly one pending request exists for that noteID (D-05 — no stacking).
    - cancel(noteID:) removes the pending request for that noteID.
    - The added request's userInfo contains only "noteID" and "isRoutineReminder" keys — no note body text (security T-12-05).
    - content.categoryIdentifier == kReminderCategoryID (Complete action available).
  </behavior>
  <action>
Create RoutineNotificationService.swift in Features/Notes mirroring SIPAccrualService's direct-trigger approach (NOT NotificationScheduler.buildRequests). Define `struct RoutineNotificationService` with `private let center: any NotificationCenterPort` and `init(center: any NotificationCenterPort = SystemNotificationCenter())`. Add `static func identifier(for noteID: UUID) -> String` returning `"routine-daily-\(noteID.uuidString)"`. Implement `func schedule(noteID:title:time:) async`: FIRST call `cancel(noteID:)` (synchronous removePending), THEN build time-only DateComponents `[.hour, .minute]` from `time` (device-timezone Calendar.current), construct `UNCalendarNotificationTrigger(dateMatching:repeats:true)`, build `UNMutableNotificationContent` with `title = title`, generic `body`, `sound = .default`, `categoryIdentifier = kReminderCategoryID`, and `userInfo = ["noteID": noteID.uuidString, "isRoutineReminder": "true"]` (NO note body text — security). Add the request via `try? await center.add(request)` using `Self.identifier(for: noteID)`. Implement `func cancel(noteID:)` calling `center.removePendingNotificationRequests(withIdentifiers: [Self.identifier(for: noteID)])`. Register in pbxproj (4 edits, group Features/Notes, target MyHome).

Fill the pending RoutineNotificationServiceTests stubs using SpyCenter injected into the service: assert that after three `schedule` calls the spy's `addedRequests` filtered to this note's identifier has count 1 (single-pending); assert `removedIdentifierSets` contains the note identifier before each add (cancel-then-add order); assert `cancel` leaves zero pending for that identifier; assert the added request's userInfo keys are exactly noteID + isRoutineReminder.
  </action>
  <verify>
    <automated>cd "/Users/reo/My Projects/my-home" && xcodebuild test -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:MyHomeTests/RoutineNotificationServiceTests 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - RoutineNotificationService.swift contains `struct RoutineNotificationService`, `static func identifier(for:)` returning the `"routine-daily-"` prefix, and uses `UNCalendarNotificationTrigger(dateMatching:` with `repeats: true`; does NOT reference `NotificationScheduler.buildRequests`.
    - userInfo dictionary literal in the file contains exactly the keys `noteID` and `isRoutineReminder` and no note body/title beyond `content.title` (grep confirms no `note.body`/block text in userInfo).
    - File referenced in project.pbxproj and compiles into MyHome target.
    - `xcodebuild test ... -only-testing:MyHomeTests/RoutineNotificationServiceTests` passes ALL cases including exactlyOnePendingRequest (D-05) and cancel-then-add ordering.
  </acceptance_criteria>
  <done>RoutineNotificationService schedules a single daily time-only notification with the stable identifier and cancel-then-add guarantee; all notification tests green; file registered in pbxproj.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app -> iOS UserNotifications scheduling | notification content (title, userInfo) crosses to the OS and may surface on the lock screen |
| package-manager installs | None this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04 | Denial of Service | duplicate daily notifications (D-05) | mitigate | Cancel-then-add with a stable per-note identifier; test `exactlyOnePendingRequest` asserts exactly one pending request after repeated schedules (no stacking) |
| T-12-05 | Information Disclosure | notification userInfo payload | mitigate | userInfo carries ONLY noteID (UUID) + isRoutineReminder flag — no note body text; only the title (its display purpose) is in content.title; mirrors T-03-16. Note title on a routine notification is user-authored and expected; no separate note body leaks |
| T-12-10 | Information Disclosure | markdown-injection in displayed strings | mitigate | StreakCalculator/notification strings rendered downstream via plain `Text(...)` only; no `AttributedString(markdown:)` (consistent with T-114-03 / T-11-10) — enforced in the Wave-3 UI plans that render these values |
| T-12-SC | Tampering | package installs | accept | No package installs this phase |
</threat_model>

<verification>
- `xcodebuild test ... -only-testing:MyHomeTests/StreakCalculatorTests` green (D-07 streak).
- `xcodebuild test ... -only-testing:MyHomeTests/RoutineNotificationServiceTests` green (D-05 single-pending).
- Both new files compile into the MyHome target (pbxproj registered).
</verification>

<success_criteria>
StreakCalculator computes the forgiving D-07 streak as a pure injectable function with all cases green; RoutineNotificationService guarantees exactly one pending daily notification per routine via cancel-then-add with a stable identifier and leak-free userInfo, with all notification tests green; both files registered in pbxproj.
</success_criteria>

<output>
Create `.planning/phases/12-notes-daily-routine-enhancement/12-02-SUMMARY.md` when done. Record the StreakCalculator and RoutineNotificationService public signatures so the Wave-3 UI plans wire to them without re-derivation.
</output>
