---
phase: 07-bank-parsers-ingestion-pipeline
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - MyHomeApp/Persistence/Models/Expense.swift
  - MyHomeApp/Persistence/Models/Category.swift
  - MyHomeApp/Persistence/Models/Note.swift
  - MyHomeApp/Persistence/Models/NoteBlock.swift
  - MyHomeApp/Persistence/ModelContainer+App.swift
  - MyHomeTests/MigrationTests.swift
autonomous: true
requirements: [ING-10, ING-11, ING-12, ING-15]

must_haves:
  truths:
    - "The app's active schema is SchemaV4 — typealiases and ModelContainer point at V4"
    - "A bundled V3 store migrates to V4 successfully (additive fields default to nil)"
    - "Existing manual expenses keep ingestion fields nil after migration"
    - "Full existing test suite stays green after the schema flip"
  artifacts:
    - path: "MyHomeApp/Persistence/Models/Expense.swift"
      provides: "typealias Expense = SchemaV4.Expense"
      contains: "SchemaV4.Expense"
    - path: "MyHomeApp/Persistence/ModelContainer+App.swift"
      provides: "Container built on SchemaV4 with AppMigrationPlan"
      contains: "SchemaV4"
  key_links:
    - from: "ModelContainer+App.swift"
      to: "SchemaV4"
      via: "Schema(versionedSchema:)"
      pattern: "Schema\\(versionedSchema: SchemaV4"
---

<objective>
Flip the app onto SchemaV4: update the four typealiases and the ModelContainer factory to point at SchemaV4, and prove a bundled V3 store migrates to V4 with a migration test. This is the GREEN companion to plan 01's schema definition — it activates the new ingestion fields (rawEmailBody, parserID, parserVersion, sourceLabel, gmailMessageID, ingestionStateRaw, parseConfidence) on the live `Expense` model.

Purpose: Make the SchemaV4 ingestion fields the active model so plans 03/05/06 can write to them, while guaranteeing existing data and tests survive the migration.
Output: Updated typealiases + container factory + a passing V3→V4 migration test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-bank-parsers-ingestion-pipeline/07-PATTERNS.md
@.planning/phases/07-bank-parsers-ingestion-pipeline/07-01-SUMMARY.md
@MyHomeApp/Persistence/Schema/SchemaV4.swift
@MyHomeApp/Persistence/Schema/MigrationPlan.swift
@MyHomeApp/Persistence/ModelContainer+App.swift
@MyHomeTests/MigrationTests.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip typealiases + ModelContainer to SchemaV4</name>
  <files>MyHomeApp/Persistence/Models/Expense.swift, MyHomeApp/Persistence/Models/Category.swift, MyHomeApp/Persistence/Models/Note.swift, MyHomeApp/Persistence/Models/NoteBlock.swift, MyHomeApp/Persistence/ModelContainer+App.swift</files>
  <read_first>
    - MyHomeApp/Persistence/Models/Expense.swift (current `typealias Expense = SchemaV3.Expense`)
    - MyHomeApp/Persistence/Models/Category.swift, Note.swift, NoteBlock.swift (current V3 typealiases)
    - MyHomeApp/Persistence/ModelContainer+App.swift (current `Schema(versionedSchema: SchemaV3.self)` at line ~18 and `migrationPlan: AppMigrationPlan.self` at line ~43)
    - MyHomeApp/Persistence/Schema/SchemaV4.swift (created in plan 01 — confirm the four model types exist)
  </read_first>
  <action>
    Update the four typealias files to point at V4: `typealias Expense = SchemaV4.Expense` (Expense.swift), `typealias Category = SchemaV4.Category` (Category.swift), `typealias Note = SchemaV4.Note` (Note.swift), `typealias NoteBlock = SchemaV4.NoteBlock` (NoteBlock.swift). Update each file's header comment to note the V3→V4 flip in Phase 7 (mirror the existing "Flipped from SchemaV1→SchemaV2 in Phase 2" comment style). In `ModelContainer+App.swift`, change `Schema(versionedSchema: SchemaV3.self)` to `Schema(versionedSchema: SchemaV4.self)`; leave `migrationPlan: AppMigrationPlan.self` unchanged (the plan already chains v3ToV4 from plan 01). Do not change the App Group store URL or fallback logic. All views/tests using bare `Expense`/`Category`/`Note`/`NoteBlock` continue to compile unchanged because the typealias absorbs the version bump.
  </action>
  <verify>
    <automated>xcodebuild build -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' 2>&1 | tail -5 | grep -q "BUILD SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - `Expense.swift` contains `typealias Expense = SchemaV4.Expense`
    - `Category.swift` / `Note.swift` / `NoteBlock.swift` each contain their `SchemaV4.` typealias
    - `ModelContainer+App.swift` contains `Schema(versionedSchema: SchemaV4.self)` and still references `AppMigrationPlan.self`
    - `xcodebuild build` reports BUILD SUCCEEDED
  </acceptance_criteria>
  <done>Live model is SchemaV4; existing surfaces compile unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: V3→V4 migration test green</name>
  <files>MyHomeTests/MigrationTests.swift</files>
  <read_first>
    - MyHomeTests/MigrationTests.swift (existing V1→V2 / V2→V3 migration test structure — how a bundled prior-version store is loaded and asserted, the makeContainer/seed-store loading pattern)
    - MyHomeApp/Persistence/Schema/SchemaV4.swift (the new fields that must default to nil post-migration)
    - .planning/phases/07-bank-parsers-ingestion-pipeline/07-RESEARCH.md (§SchemaV4 Migration — additive-only, .custom(nil,nil) migrates optional fields automatically)
  </read_first>
  <action>
    Add a `@Test` to MigrationTests.swift that opens a ModelContainer using `AppMigrationPlan` against a store created at the V3 schema (reuse the existing bundled-seed-store loading approach already used for V1/V2/V3 tests; if the existing tests build an in-memory V3 store and re-open under the plan, follow that exact pattern). Assert: (1) the container opens without throwing (migration succeeds); (2) any pre-existing Expense rows have `rawEmailBody == nil`, `parserID == nil`, `parserVersion == nil`, `sourceLabel == nil`, `gmailMessageID == nil`, `ingestionStateRaw == nil`, `parseConfidence == nil` after migration (additive defaults). If the existing MigrationTests rely on a bundled `.store` resource, add or reuse a V3 seed store; if they construct stores in-memory at the prior `VersionedSchema`, replicate that. Keep all existing migration tests intact.
  </action>
  <verify>
    <automated>xcodebuild test -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:MyHomeTests/MigrationTests 2>&1 | tail -8 | grep -qE "Test Suite.*passed|TEST SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - MigrationTests.swift contains a new V3→V4 `@Test` asserting the container opens and ingestion fields default to nil
    - `xcodebuild test -only-testing:MyHomeTests/MigrationTests` passes
    - Existing V1→V2 and V2→V3 migration tests still pass
  </acceptance_criteria>
  <done>V3→V4 migration verified green; legacy migration tests intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Persisted V3 store → V4 schema | Existing user data must survive the schema bump |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | Tampering / Data Loss | V3→V4 live migration on existing store | mitigate | Additive-only optional fields; `.custom(nil,nil)`; migration test asserts existing rows survive with nil ingestion fields |
| T-07-02 | Information Disclosure | rawEmailBody field now active on live Expense | accept | Local-only store, Face ID gated (D7-10); not yet populated (population in plan 06) |
| T-07-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs this phase (Apple frameworks only) |
</threat_model>

<verification>
- App builds on SchemaV4
- V3→V4 migration test passes; existing migration tests unaffected
- No data-loss path on the schema flip
</verification>

<success_criteria>
- Live model points at SchemaV4; all existing surfaces compile
- Bundled/constructed V3 store migrates to V4 with ingestion fields defaulting nil
- Full migration test file green
</success_criteria>

<output>
Create `.planning/phases/07-bank-parsers-ingestion-pipeline/07-02-SUMMARY.md` when done
</output>
