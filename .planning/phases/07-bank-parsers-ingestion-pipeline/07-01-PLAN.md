---
phase: 07-bank-parsers-ingestion-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - MyHomeApp/Persistence/Schema/SchemaV4.swift
  - MyHomeApp/Persistence/Schema/MigrationPlan.swift
  - MyHomeApp/Gmail/GmailFetchPort.swift
  - MyHomeApp/Features/Ingestion/BankEmailParser.swift
  - MyHomeTests/Support/SpyGmailFetch.swift
  - MyHomeTests/HDFCParserTests.swift
  - MyHomeTests/ICICIParserTests.swift
  - MyHomeTests/ConfidenceScorerTests.swift
  - MyHomeTests/DedupCheckerTests.swift
  - MyHomeTests/MerchantNormalizerTests.swift
  - MyHomeTests/IngestionPipelineTests.swift
  - MyHomeTests/Fixtures/README.md
  - MyHome.xcodeproj/project.pbxproj
autonomous: true
requirements: [ING-06, ING-07, ING-08, ING-09, ING-10, ING-11, ING-12, ING-13, ING-14, ING-15]

must_haves:
  truths:
    - "SchemaV4 compiles and the project builds with a V3→V4 migration stage appended (no behavior change yet)"
    - "GmailFetchPort protocol + GmailProfile value type + ParsedExpense value type + BankEmailParser protocol exist and compile"
    - "SpyGmailFetch test double conforms to GmailFetchPort"
    - "Six new test files exist with failing @Test stubs (RED) covering every Phase 7 requirement"
    - "Full test suite runs and the new stub tests fail for the expected reason (unimplemented), not compile errors"
  artifacts:
    - path: "MyHomeApp/Persistence/Schema/SchemaV4.swift"
      provides: "VersionedSchema v4 — additive ingestion fields on Expense"
      contains: "enum SchemaV4"
    - path: "MyHomeApp/Gmail/GmailFetchPort.swift"
      provides: "Email retrieval port + GmailProfile"
      contains: "protocol GmailFetchPort"
    - path: "MyHomeApp/Features/Ingestion/BankEmailParser.swift"
      provides: "Parser protocol + ParsedExpense value type"
      contains: "struct ParsedExpense"
    - path: "MyHomeTests/Support/SpyGmailFetch.swift"
      provides: "GmailFetchPort test double"
      contains: "class SpyGmailFetch"
  key_links:
    - from: "MigrationPlan.swift"
      to: "SchemaV4.self"
      via: "stages array append v3ToV4"
      pattern: "v3ToV4"
---

<objective>
Wave 0 scaffold for Phase 7 (Nyquist gate). Create the SchemaV4 schema + migration stage, the GmailFetchPort seam, the parser protocol + ParsedExpense value type, the SpyGmailFetch test double, and all six failing test files (RED) covering every Phase 7 requirement. Register every new file in the Xcode project so the suite compiles and the stubs fail for the right reason (unimplemented) — not for missing symbols.

Purpose: Establish the testable contracts and the failing-test baseline before any GREEN implementation, so later waves implement against fixed interfaces rather than discovering them.
Output: SchemaV4.swift, MigrationPlan.swift (updated), GmailFetchPort.swift, BankEmailParser.swift, SpyGmailFetch.swift, six test files, Fixtures/README.md, pbxproj registration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-bank-parsers-ingestion-pipeline/07-RESEARCH.md
@.planning/phases/07-bank-parsers-ingestion-pipeline/07-PATTERNS.md
@.planning/phases/07-bank-parsers-ingestion-pipeline/07-CONTEXT.md
@MyHomeApp/Persistence/Schema/SchemaV3.swift
@MyHomeApp/Persistence/Schema/MigrationPlan.swift
@MyHomeApp/Gmail/GmailAuthPort.swift
@MyHomeTests/Support/SpyGmailAuth.swift
@MyHomeTests/BudgetCalculatorTests.swift
@MyHomeTests/GmailSyncControllerTests.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: SchemaV4 + V3→V4 migration stage</name>
  <files>MyHomeApp/Persistence/Schema/SchemaV4.swift, MyHomeApp/Persistence/Schema/MigrationPlan.swift</files>
  <read_first>
    - MyHomeApp/Persistence/Schema/SchemaV3.swift (copy the CloudKit-readiness rules header verbatim; copy Expense, Category, Note, NoteBlock @Models verbatim; note the `Schema.Version(3,0,0)` and four-model `models` array)
    - MyHomeApp/Persistence/Schema/MigrationPlan.swift (the existing `schemas` array, `stages` array, and the `.custom(willMigrate: nil, didMigrate: nil)` pattern with the FB13812722 comment)
    - .planning/phases/07-bank-parsers-ingestion-pipeline/07-PATTERNS.md (§SchemaV4.swift and §MigrationPlan.swift — exact copy targets)
  </read_first>
  <action>
    Create `SchemaV4.swift` as an `enum SchemaV4: VersionedSchema` with `static let versionIdentifier = Schema.Version(4, 0, 0)` and a `models` array listing `SchemaV4.Expense.self, SchemaV4.Category.self, SchemaV4.Note.self, SchemaV4.NoteBlock.self`. Copy the CloudKit-readiness 8-rule header comment verbatim from SchemaV3. Copy `Category`, `Note`, `NoteBlock` @Models verbatim from SchemaV3 (only changing the nested type prefix to `SchemaV4` where relationships reference sibling types). Copy `Expense` verbatim, updating its relationship to `inverse: \SchemaV4.Category.expenses`, then append these new optional/defaulted fields per D7-16 (all `var name: Type? = nil`, no `@Attribute(.unique)`, no stored enums): `rawEmailBody: String? = nil` (ING-10, D7-10), `parserID: String? = nil` (ING-11, D7-11), `parserVersion: String? = nil` (ING-11, D7-11), `sourceLabel: String? = nil` (D7-15), `gmailMessageID: String? = nil` (ING-14, D7-07), `ingestionStateRaw: String? = nil` (ING-12/13/14 — String not enum; values "autoSaved"|"needsReview"|"possibleDuplicate"), `parseConfidence: Double? = nil` (ING-12 — Double for a 0.0–1.0 ratio, NOT Decimal). Add a header note that SchemaV3 is IMMUTABLE and V4 is an additive superset. In `MigrationPlan.swift`: append `SchemaV4.self` to the `schemas` array (never remove V1/V2/V3); append `v3ToV4` to the `stages` array; add `static let v3ToV4 = MigrationStage.custom(fromVersion: SchemaV3.self, toVersion: SchemaV4.self, willMigrate: nil, didMigrate: nil)` with the same FB13812722 comment rationale (use .custom not .lightweight). Do NOT flip the `typealias Expense`/`Category`/`Note`/`NoteBlock` or the `Schema(versionedSchema:)` in ModelContainer+App.swift yet — that is plan 02. Do NOT add an `init` mismatch: copy SchemaV4.Expense's `init` from SchemaV3's, keeping existing params; new fields default to nil so the initializer signature need not change.
  </action>
  <verify>
    <automated>xcodebuild build -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' 2>&1 | tail -5 | grep -q "BUILD SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - `SchemaV4.swift` contains `enum SchemaV4: VersionedSchema` and `Schema.Version(4, 0, 0)`
    - `SchemaV4.swift` Expense contains `var rawEmailBody: String? = nil`, `var parserID: String? = nil`, `var parserVersion: String? = nil`, `var sourceLabel: String? = nil`, `var gmailMessageID: String? = nil`, `var ingestionStateRaw: String? = nil`, `var parseConfidence: Double? = nil`
    - `SchemaV4.swift` contains NO `@Attribute(.unique)` and NO stored `enum` property
    - `MigrationPlan.swift` `schemas` array contains `SchemaV4.self`; `stages` array contains `v3ToV4`; `v3ToV4` uses `.custom(` with `willMigrate: nil, didMigrate: nil`
    - `xcodebuild build` reports BUILD SUCCEEDED
  </acceptance_criteria>
  <done>SchemaV4 + V3→V4 stage exist and the app builds; typealias flip deferred to plan 02.</done>
</task>

<task type="auto">
  <name>Task 2: GmailFetchPort seam + BankEmailParser protocol + ParsedExpense + SpyGmailFetch</name>
  <files>MyHomeApp/Gmail/GmailFetchPort.swift, MyHomeApp/Features/Ingestion/BankEmailParser.swift, MyHomeTests/Support/SpyGmailFetch.swift</files>
  <read_first>
    - MyHomeApp/Gmail/GmailAuthPort.swift (exact structure to mirror: public protocol + Sendable, value type as Decodable+Sendable, SystemX final class @unchecked Sendable conformer)
    - MyHomeTests/Support/SpyGmailAuth.swift (exact spy structure to mirror: settable result stubs, shouldThrow per method, recorded-call arrays, reset())
    - .planning/phases/07-bank-parsers-ingestion-pipeline/07-PATTERNS.md (§GmailFetchPort.swift, §BankEmailParser.swift, §SpyGmailFetch.swift — the exact signatures and SpyGmailFetch shape)
  </read_first>
  <action>
    Create `GmailFetchPort.swift` in `MyHomeApp/Gmail/` (`import Foundation` only): `public protocol GmailFetchPort: Sendable` with three async throws methods — `func getProfile(accessToken: String) async throws -> GmailProfile`, `func listMessageIDs(accessToken: String, q: String, maxResults: Int) async throws -> [String]`, `func getRawMessage(accessToken: String, messageID: String) async throws -> String`. Add `public struct GmailProfile: Decodable, Sendable { public let emailAddress: String }`. Define a `public enum GmailFetchError: Error` with cases `networkError(Error)`, `decodingError`, `httpError(Int)` mirroring GmailAuthError's wrapping style. In this same file, add a `public final class SystemGmailFetch: GmailFetchPort, @unchecked Sendable` with `public init() {}` and the three methods STUBBED to `throw GmailFetchError.networkError(...)` or `fatalError("unimplemented — plan 05")` — the production network calls are written in plan 05. (Stub, not full implementation; plan 05 fills these against the real Gmail REST endpoints.)
    Create `BankEmailParser.swift` in `MyHomeApp/Features/Ingestion/`: `public struct ParsedExpense: Sendable` with `let amount: Decimal` (never Double), `let rawMerchant: String`, `let normalizedMerchant: String`, `let categoryHint: String?`, `let date: Date`, `let rawSourceLabel: String`, `let isReversal: Bool`, `let fingerprintScore: Double`, `let extractionScore: Double`, plus a public memberwise init. Add `public protocol BankEmailParser: Sendable { var parserID: String { get }; var parserVersion: String { get }; func canHandle(sender: String, subject: String) -> Bool; func parse(rawEmail: String) -> ParsedExpense? }`.
    Create `SpyGmailFetch.swift` in `MyHomeTests/Support/` (`import Testing`, `@testable import MyHome`): `public final class SpyGmailFetch: GmailFetchPort, @unchecked Sendable` mirroring SpyGmailAuth exactly — settable stubs `profileResult: GmailProfile = GmailProfile(emailAddress: "test@gmail.com")`, `messageIDsResult: [String] = []`, `rawMessageResult: String = ""` (and an optional `rawMessagesByID: [String: String] = [:]` lookup used if non-empty), `shouldThrowOnGetProfile/ListMessages/GetRaw: Error? = nil`, recorded-call arrays `getProfileCalls`, `listMessageIDsCalls`, `getRawMessageCalls`, a `public init() {}`, the three conforming methods that record the call then throw-or-return, and a `reset()`.
  </action>
  <verify>
    <automated>xcodebuild build -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' 2>&1 | tail -5 | grep -q "BUILD SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - `GmailFetchPort.swift` defines `public protocol GmailFetchPort` with `func getProfile(`, `func listMessageIDs(`, `func getRawMessage(`
    - `GmailFetchPort.swift` defines `public struct GmailProfile` with `emailAddress` and `public final class SystemGmailFetch: GmailFetchPort`
    - `BankEmailParser.swift` defines `public struct ParsedExpense` with `let amount: Decimal` and `public protocol BankEmailParser`
    - `SpyGmailFetch.swift` defines `class SpyGmailFetch` conforming to `GmailFetchPort` with `getProfileCalls`, `listMessageIDsCalls`, `getRawMessageCalls`, and `reset()`
    - `xcodebuild build` reports BUILD SUCCEEDED
  </acceptance_criteria>
  <done>Fetch port, parser protocol, ParsedExpense value type, and the spy all compile; production fetch methods stubbed for plan 05.</done>
</task>

<task type="auto">
  <name>Task 3: Six failing test files (RED) + Fixtures dir + pbxproj registration</name>
  <files>MyHomeTests/HDFCParserTests.swift, MyHomeTests/ICICIParserTests.swift, MyHomeTests/ConfidenceScorerTests.swift, MyHomeTests/DedupCheckerTests.swift, MyHomeTests/MerchantNormalizerTests.swift, MyHomeTests/IngestionPipelineTests.swift, MyHomeTests/Fixtures/README.md, MyHome.xcodeproj/project.pbxproj</files>
  <read_first>
    - MyHomeTests/BudgetCalculatorTests.swift (the `@MainActor struct` + `makeContainer()` in-memory ModelContainer pattern, and `@Test(... arguments:)` table-driven pattern)
    - MyHomeTests/GmailSyncControllerTests.swift (resetDefaults()/defer test-isolation pattern and controller construction)
    - .planning/phases/07-bank-parsers-ingestion-pipeline/07-RESEARCH.md (§Validation Architecture → Phase Requirements → Test Map for the exact behavior each test must assert; Code Examples 1-4)
    - .planning/phases/07-bank-parsers-ingestion-pipeline/07-PATTERNS.md (§Test files — analog A pure-logic and analog B SwiftData)
  </read_first>
  <action>
    Create six Swift Testing files, each a `@MainActor struct`, `import Testing` + `import Foundation` + `@testable import MyHome`. Write REAL `@Test` stubs that reference the not-yet-implemented types so they fail at runtime (return wrong value / `Issue.record`), NOT compile errors — where a type does not exist yet (HDFCParser, ICICIParser, ConfidenceScorer, DedupChecker, MerchantNormalizer), create a one-line placeholder of that type INSIDE the same test target is NOT allowed; instead the production types are created GREEN in plans 02/03/05, so these test files must compile by referencing only types that exist now (ParsedExpense, Expense, SpyGmailFetch) and mark requirement-specific assertions with `Issue.record("unimplemented — plan NN")` inside each `@Test` so the suite is RED but compiles. Specifically:
    `MerchantNormalizerTests.swift` — table `@Test` asserting "AMAZON IN BLR"→"Amazon"/Shopping, "ZOMATO ONL BANGAL"→"Zomato"/Dining, longest-key-wins ("AMAZON IN" before "AMAZON"), and unknown→raw string + nil hint (ING-15, D7-09/12). Stub each with `Issue.record("MerchantNormalizer unimplemented — plan 03")`.
    `ConfidenceScorerTests.swift` — boundary table: a ParsedExpense with full fingerprint+all fields scores ≥0.85 (autoSaved side); one missing amount scores <0.85 (needsReview side) (ING-12). Stub `Issue.record("ConfidenceScorer unimplemented — plan 03")`.
    `DedupCheckerTests.swift` — uses `makeContainer()` to insert an existing Expense, then asserts a ParsedExpense with same amount + overlapping merchant substring + date within ±1 day returns the existing match, and outside ±1 day or different amount returns nil (ING-14, D7-13). Stub `Issue.record("DedupChecker unimplemented — plan 03")`.
    `HDFCParserTests.swift` and `ICICIParserTests.swift` — `canHandle` rejects OTP/promo/verification/statement subjects and non-bank senders (ING-08); `parse` returns nil on fingerprint failure (ING-07); a `arguments:` fixture table mapping `.eml` filename → expected (amount, normalizedMerchant, isReversal) including a reversal fixture producing a negative amount (ING-06/09). Include a `loadFixture(_:)` helper that loads from the test bundle. Stub each `@Test` body with `Issue.record("HDFCParser/ICICIParser + fixtures unimplemented — plan 05 (corpus-gated)")`.
    `IngestionPipelineTests.swift` — uses `makeContainer()` (in-memory, model types `Expense.self, Category.self, Note.self, NoteBlock.self`); asserts after a simulated ingestion an Expense persists with `rawEmailBody`, `parserID`, `parserVersion` non-nil (ING-10/11), and that confidence ≥0.85 yields `ingestionStateRaw == "autoSaved"` while <0.85 yields `"needsReview"` (ING-12), plus a connectedEmail-population assertion using SpyGmailFetch for UAT-6-05. Stub each `@Test` with `Issue.record("Ingestion pipeline unimplemented — plan 06")`.
    Create `MyHomeTests/Fixtures/README.md` documenting that anonymized `.eml` corpus files (50+ per bank, HDFC + ICICI) are added by the human corpus-collection checkpoint (plan 04) before parser calibration in plan 05; list expected naming: `hdfc_cc_spend_N.eml`, `hdfc_upi_debit_N.eml`, `hdfc_refund_N.eml`, `hdfc_otp_N.eml` (rejection fixture), and `icici_*` equivalents.
    Register ALL new source files (SchemaV4.swift, GmailFetchPort.swift, BankEmailParser.swift) in the `MyHome` app target and all new test files (SpyGmailFetch.swift + six test files) in the `MyHomeTests` target inside `MyHome.xcodeproj/project.pbxproj`, plus add the `Fixtures` folder as a bundle resource of the test target. Mirror how existing files in the same directories are listed (PBXBuildFile + PBXFileReference + group membership + target Sources/Resources phase).
  </action>
  <verify>
    <automated>xcodebuild test -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' 2>&1 | tail -25 | grep -qE "MerchantNormalizer|ConfidenceScorer|DedupChecker|HDFCParser|ICICIParser|Ingestion"</automated>
  </verify>
  <acceptance_criteria>
    - All six test files exist and the project compiles (no missing-symbol errors): `xcodebuild test` reaches test execution rather than failing at build
    - The new stub tests RUN and FAIL via `Issue.record(...)` (RED), referencing their plan number
    - `MyHomeTests/Fixtures/README.md` documents the `.eml` corpus naming and plan-04 corpus gate
    - `project.pbxproj` references SchemaV4.swift, GmailFetchPort.swift, BankEmailParser.swift in the MyHome target and SpyGmailFetch.swift + 6 test files in MyHomeTests
  </acceptance_criteria>
  <done>Suite compiles; new requirement-mapped tests are RED for the right reason; fixtures dir + pbxproj registration in place.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gmail API → app | Untrusted email content crosses here (parsed in later plans) |
| SwiftData store at rest | rawEmailBody (PII) will be persisted locally (this plan only defines the schema field) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | SchemaV4 migration (V3→V4) | mitigate | Use `.custom(willMigrate: nil, didMigrate: nil)` to sidestep FB13812722 (iOS 17.0–17.3 lightweight-migration crash); additive-only fields, no destructive transforms |
| T-07-02 | Information Disclosure | `rawEmailBody: String?` field defined on Expense | accept | Local-only store (D7-10); no CloudKit in v1; protected by existing Face ID gate (LockController). Field is only defined here, not populated |
| T-07-03 | Tampering | New @Model fields breaking CloudKit-readiness | mitigate | All new fields optional/defaulted, no `@Attribute(.unique)`, no stored enum (`ingestionStateRaw: String?`), Double only for the 0–1 ratio — asserted in acceptance criteria |
| T-07-SC | Tampering | npm/pip/cargo installs | mitigate | No third-party packages this phase (Apple frameworks only — RESEARCH §Package Legitimacy Audit); no install tasks |
</threat_model>

<verification>
- `xcodebuild build` SUCCEEDS with SchemaV4 + V3→V4 stage present
- `xcodebuild test` compiles the full suite and the six new test files run RED (Issue.record)
- No new third-party dependency introduced
- SchemaV4 obeys all 8 CloudKit-readiness rules
</verification>

<success_criteria>
- SchemaV4, migration stage, GmailFetchPort, BankEmailParser/ParsedExpense, SpyGmailFetch all compile
- Six requirement-mapped test files are RED and reference their implementing plan
- Fixtures directory + README document the corpus gate
- All new files registered in project.pbxproj
</success_criteria>

<output>
Create `.planning/phases/07-bank-parsers-ingestion-pipeline/07-01-SUMMARY.md` when done
</output>
