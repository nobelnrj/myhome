---
phase: 07-bank-parsers-ingestion-pipeline
plan: 03
type: tdd
wave: 2
depends_on: ["07-01"]
files_modified:
  - MyHomeApp/Features/Ingestion/MerchantNormalizer.swift
  - MyHomeApp/Features/Ingestion/ConfidenceScorer.swift
  - MyHomeApp/Features/Ingestion/DedupChecker.swift
  - MyHomeApp/Features/Ingestion/DismissedMessageStore.swift
  - MyHomeTests/MerchantNormalizerTests.swift
  - MyHomeTests/ConfidenceScorerTests.swift
  - MyHomeTests/DedupCheckerTests.swift
  - MyHome.xcodeproj/project.pbxproj
autonomous: true
requirements: [ING-12, ING-14, ING-15]

must_haves:
  truths:
    - "MerchantNormalizer maps ~20-30 raw Indian merchant substrings to normalized names + category hints, longest-key-wins, falling back to raw+nil"
    - "ConfidenceScorer computes a 0.0-1.0 score where ≥0.85 means full fingerprint + amount/date/merchant extracted"
    - "DedupChecker finds an existing Expense matching (amount + merchant-substring + date ±1 day), else nil"
    - "DismissedMessageStore persists and reads dismissed Gmail message IDs in App Group UserDefaults"
    - "MerchantNormalizerTests, ConfidenceScorerTests, DedupCheckerTests are GREEN"
  artifacts:
    - path: "MyHomeApp/Features/Ingestion/MerchantNormalizer.swift"
      provides: "Static merchant seed table + normalize()"
      contains: "struct MerchantNormalizer"
    - path: "MyHomeApp/Features/Ingestion/ConfidenceScorer.swift"
      provides: "Pure confidence scoring"
      contains: "struct ConfidenceScorer"
    - path: "MyHomeApp/Features/Ingestion/DedupChecker.swift"
      provides: "Pure dedup match"
      contains: "struct DedupChecker"
    - path: "MyHomeApp/Features/Ingestion/DismissedMessageStore.swift"
      provides: "Dismissed message-ID persistence"
      contains: "struct DismissedMessageStore"
  key_links:
    - from: "DismissedMessageStore"
      to: "App Group UserDefaults"
      via: "suiteName group.com.reojacob.myhome"
      pattern: "group.com.reojacob.myhome"
---

<objective>
GREEN the four pure-logic ingestion helpers via TDD against the stubs created in plan 01: MerchantNormalizer (seed table, ING-15/D7-09/D7-12), ConfidenceScorer (0.85 triage math, ING-12), DedupChecker (amount+merchant-substring+date±1day, ING-14/D7-13), and DismissedMessageStore (swipe-to-discard memory, D7-07). All are deterministic and table-testable with no network and minimal SwiftData.

Purpose: Land the calibratable, side-effect-free core of the pipeline so the parsers (plan 05) and the controller wiring (plan 06) can compose them.
Output: Four implementation files + three GREEN test files (DismissedMessageStore is exercised in plan 06's controller tests).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-bank-parsers-ingestion-pipeline/07-RESEARCH.md
@.planning/phases/07-bank-parsers-ingestion-pipeline/07-PATTERNS.md
@.planning/phases/07-bank-parsers-ingestion-pipeline/07-CONTEXT.md
@.planning/phases/07-bank-parsers-ingestion-pipeline/07-01-SUMMARY.md
@MyHomeApp/Support/BudgetCalculator.swift
@MyHomeApp/Features/Ingestion/BankEmailParser.swift
@MyHomeApp/Features/Gmail/GmailSyncController.swift
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MerchantNormalizer + ConfidenceScorer (GREEN)</name>
  <files>MyHomeApp/Features/Ingestion/MerchantNormalizer.swift, MyHomeApp/Features/Ingestion/ConfidenceScorer.swift, MyHomeTests/MerchantNormalizerTests.swift, MyHomeTests/ConfidenceScorerTests.swift, MyHome.xcodeproj/project.pbxproj</files>
  <read_first>
    - MyHomeApp/Support/BudgetCalculator.swift (the pure `struct` + `static func` + `static let` constant-table pattern to mirror)
    - MyHomeApp/Features/Ingestion/BankEmailParser.swift (the `ParsedExpense` value type fields ConfidenceScorer consumes)
    - .planning/phases/07-bank-parsers-ingestion-pipeline/07-RESEARCH.md (§Merchant Normalization for the ~29-entry seed table and longest-key-wins rule; §Confidence Scoring Mechanics for the 0.5/0.5 split and field weights amount 0.40 / date 0.25 / merchant 0.20 / card 0.15)
    - .planning/phases/07-bank-parsers-ingestion-pipeline/07-PATTERNS.md (§MerchantNormalizer.swift, §ConfidenceScorer.swift)
    - MyHomeTests/MerchantNormalizerTests.swift, MyHomeTests/ConfidenceScorerTests.swift (the RED stubs from plan 01 — replace Issue.record with real assertions)
  </read_first>
  <behavior>
    - MerchantNormalizer: "AMAZON IN BLR" → ("Amazon", "Shopping"); "ZOMATO ONL BANGAL" → ("Zomato", "Dining"); longest key wins ("AMAZON IN" matched before "AMAZON"); unknown "RANDOM SHOP 99" → ("RANDOM SHOP 99", nil)
    - ConfidenceScorer: full fingerprint (1.0) + amount+date+merchant+card extracted → 1.0 (≥0.85, autoSaves); full fingerprint + missing card only → 0.925 (≥0.85); fingerprint 1.0 + amount missing → 0.5*1.0 + 0.5*(0.25+0.20+0.15)=0.80 (<0.85, needsReview)
  </behavior>
  <action>
    RED→GREEN→REFACTOR. First update `MerchantNormalizerTests.swift` and `ConfidenceScorerTests.swift` to replace the `Issue.record` stubs with the behavior assertions above (table-driven `@Test(... arguments:)`), confirm RED, then implement.
    `MerchantNormalizer.swift`: `public struct MerchantSeedEntry { public let normalizedName: String; public let categoryHint: String? }` and `public struct MerchantNormalizer` with `static let seed: [String: MerchantSeedEntry]` containing the ~29 entries from RESEARCH §Merchant Normalization (Amazon/Flipkart/Zomato/Swiggy/Uber/Ola/Rapido/HPCL/BPCL/Indian Oil/Nykaa/Myntra/BigBasket/Blinkit/Zepto/Instamart/Netflix/Spotify/Hotstar/JioCinema/IRCTC/MakeMyTrip/Goibibo/Apollo/MedPlus/PhonePe/GPay/Paytm, keys UPPERCASE, category hints matching the existing 14-category India seed names e.g. "Shopping"/"Dining"/"Fuel"/"Groceries"/"Auto/Cab"/"Entertainment"/"Health/Pharmacy"/"UPI to Person"; "Travel" entries map to "Misc" if no Travel category exists — verify against the seeded category list and pick the closest existing category). `public static func normalize(_ rawMerchant: String) -> MerchantSeedEntry` uppercases input, sorts seed keys by length descending (longest-key-wins, D7-12), returns the first `contains` hit, else `MerchantSeedEntry(normalizedName: rawMerchant, categoryHint: nil)` (Uncategorized fallback, D7-09).
    `ConfidenceScorer.swift`: `public struct ConfidenceScorer` with `public static func score(_ result: ParsedExpense) -> Double` returning `result.fingerprintScore * 0.5 + computeExtractionScore(result) * 0.5`, and `static func computeExtractionScore(_ result: ParsedExpense) -> Double` summing amount 0.40 (if `result.amount != 0`), date 0.25 (always — parser falls back to message date), merchant 0.20 (if `!result.rawMerchant.isEmpty`), card 0.15 (if `!result.rawSourceLabel.isEmpty`). Document the 0.85 threshold and that the 0.5/0.5 split + weights are initial estimates pending real-corpus calibration (D7-03). Register both new source files + ensure the two test files are in the test target in project.pbxproj.
  </action>
  <verify>
    <automated>xcodebuild test -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:MyHomeTests/MerchantNormalizerTests -only-testing:MyHomeTests/ConfidenceScorerTests 2>&1 | tail -8 | grep -qE "passed|TEST SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - `MerchantNormalizer.swift` contains `struct MerchantNormalizer`, a `seed` dictionary with ≥20 entries, and a `normalize(` returning longest-match
    - `ConfidenceScorer.swift` contains `struct ConfidenceScorer` with `score(` using the `* 0.5` split and per-field weights `0.40/0.25/0.20/0.15`
    - `MerchantNormalizerTests` and `ConfidenceScorerTests` pass (GREEN)
  </acceptance_criteria>
  <done>Normalizer + scorer implemented and GREEN; threshold math matches ING-12/ING-15.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DedupChecker + DismissedMessageStore (GREEN)</name>
  <files>MyHomeApp/Features/Ingestion/DedupChecker.swift, MyHomeApp/Features/Ingestion/DismissedMessageStore.swift, MyHomeTests/DedupCheckerTests.swift, MyHome.xcodeproj/project.pbxproj</files>
  <read_first>
    - MyHomeApp/Support/BudgetCalculator.swift (pure-struct, caller-supplies-array pattern — DedupChecker must NOT fetch from SwiftData itself)
    - MyHomeApp/Features/Gmail/GmailSyncController.swift (lines ~107-109 — the App Group `UserDefaults(suiteName: "group.com.reojacob.myhome")` accessor pattern to mirror)
    - .planning/phases/07-bank-parsers-ingestion-pipeline/07-RESEARCH.md (§Dedup Logic for the match algorithm; §Dismissed Message-ID Tracking for the Set<String> round-trip)
    - .planning/phases/07-bank-parsers-ingestion-pipeline/07-PATTERNS.md (§DedupChecker.swift, §DismissedMessageStore.swift)
    - MyHomeTests/DedupCheckerTests.swift (RED stub from plan 01 — has the makeContainer helper)
  </read_first>
  <behavior>
    - DedupChecker.findDuplicate: existing Expense amount ₹500, note "Zomato Order", date today; candidate amount ₹500, normalizedMerchant "Zomato", date today → returns that Expense; candidate date today+2days → nil; candidate amount ₹501 → nil; candidate merchant "Uber" → nil
    - DismissedMessageStore: dismiss("msg-1") then isDismissed("msg-1")==true, isDismissed("msg-2")==false; survives a fresh read (round-trips through UserDefaults)
  </behavior>
  <action>
    RED→GREEN. Update `DedupCheckerTests.swift` to replace `Issue.record` with the assertions above using the in-memory `makeContainer()` helper to insert an existing Expense, then implement.
    `DedupChecker.swift`: `public struct DedupChecker` with `public static func findDuplicate(of candidate: ParsedExpense, in existingExpenses: [Expense]) -> Expense?`. Match rule (ING-14, D7-13): `existing.amount == candidate.amount` AND case-insensitive merchant-substring overlap between `existing.note ?? ""` and `candidate.normalizedMerchant` (each contains the other, guard against empty candidate merchant) AND `abs(existing.date.timeIntervalSince(candidate.date)) <= 86400` (±1 day). Caller supplies the array (no internal fetch).
    `DismissedMessageStore.swift`: `public struct DismissedMessageStore` with `private static let key = "gmail_dismissed_message_ids"`, a `private static var defaults: UserDefaults { UserDefaults(suiteName: "group.com.reojacob.myhome") ?? .standard }`, `public static func isDismissed(_ messageID: String) -> Bool`, `public static func dismiss(_ messageID: String)` (insert into the set, write back as `Array`), and `static func dismissed() -> Set<String>` reading `stringArray(forKey:)`. Use the same suiteName string as GmailSyncController. Register both source files + the test file in project.pbxproj.
  </action>
  <verify>
    <automated>xcodebuild test -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:MyHomeTests/DedupCheckerTests 2>&1 | tail -8 | grep -qE "passed|TEST SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - `DedupChecker.swift` contains `struct DedupChecker` with `findDuplicate(of:in:)` matching amount + merchant-substring + `86400` date window
    - `DismissedMessageStore.swift` contains `struct DismissedMessageStore` using `suiteName: "group.com.reojacob.myhome"` and key `"gmail_dismissed_message_ids"`
    - `DedupCheckerTests` passes (GREEN)
  </acceptance_criteria>
  <done>Dedup + dismissed-ID store implemented; dedup tests GREEN; dismissed store exercised in plan 06.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dismissed-ID set in UserDefaults | Non-security state token store |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 | Tampering | DismissedMessageStore UserDefaults set | accept | Dismissed IDs are non-security state; worst case a dismissed email re-shows in Review Inbox — no data/security impact (RESEARCH §Security Domain) |
| T-07-06 | Information Disclosure | DedupChecker reads existing expense notes/amounts | accept | Operates only on in-process app data already behind the Face ID gate; no egress |
| T-07-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs this phase (Apple frameworks only) |
</threat_model>

<verification>
- MerchantNormalizer, ConfidenceScorer, DedupChecker tests GREEN
- All four helpers are pure / side-effect-isolated (only DismissedMessageStore touches UserDefaults)
- No network dependency in any helper
</verification>

<success_criteria>
- Four ingestion helpers implemented matching ING-12/14/15 + D7-07/09/12/13
- Three target test files GREEN
- All new files registered in project.pbxproj
</success_criteria>

<output>
Create `.planning/phases/07-bank-parsers-ingestion-pipeline/07-03-SUMMARY.md` when done
</output>
