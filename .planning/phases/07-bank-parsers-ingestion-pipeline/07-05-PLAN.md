---
phase: 07-bank-parsers-ingestion-pipeline
plan: 05
type: tdd
wave: 3
depends_on: ["07-01", "07-03", "07-04"]
files_modified:
  - MyHomeApp/Features/Ingestion/HDFCParser.swift
  - MyHomeApp/Features/Ingestion/ICICIParser.swift
  - MyHomeApp/Gmail/GmailFetchPort.swift
  - MyHomeTests/HDFCParserTests.swift
  - MyHomeTests/ICICIParserTests.swift
  - MyHome.xcodeproj/project.pbxproj
autonomous: true
requirements: [ING-06, ING-07, ING-08, ING-09]

must_haves:
  truths:
    - "HDFCParser and ICICIParser parse real-corpus fixtures into ParsedExpense with correct amount, merchant, date, source label"
    - "Pre-filter rejects OTP/promotional/verification/statement emails and non-bank senders (canHandle=false)"
    - "Fingerprint matching is separate from value extraction — a missing required template literal returns nil (no extraction attempted)"
    - "Refund/reversal emails produce a negative-amount ParsedExpense"
    - "SystemGmailFetch production conformer performs real Gmail REST calls (getProfile, listMessageIDs with pagination, getRawMessage with base64url decode)"
    - "HDFCParserTests and ICICIParserTests are GREEN against the real corpus"
  artifacts:
    - path: "MyHomeApp/Features/Ingestion/HDFCParser.swift"
      provides: "HDFC two-stage parser"
      contains: "struct HDFCParser"
    - path: "MyHomeApp/Features/Ingestion/ICICIParser.swift"
      provides: "ICICI two-stage parser"
      contains: "struct ICICIParser"
    - path: "MyHomeApp/Gmail/GmailFetchPort.swift"
      provides: "SystemGmailFetch real network implementation"
      contains: "gmail.googleapis.com"
  key_links:
    - from: "HDFCParser.parse"
      to: "MerchantNormalizer.normalize"
      via: "merchant normalization at parse time"
      pattern: "MerchantNormalizer.normalize"
---

<objective>
GREEN the two bank parsers against the real corpus collected in plan 04, and implement the SystemGmailFetch production network conformer. Each parser uses the two-stage pattern (ING-07): a sender+subject pre-filter (ING-08) → template fingerprint match → value extraction → merchant normalization → reversal detection (ING-09). Fingerprint failure returns nil (fails loudly, never mis-parses). SystemGmailFetch fills the network stubs from plan 01 with real Gmail REST calls.

Purpose: Turn real bank emails into structured ParsedExpense values with calibrated confidence, and connect the app to live Gmail email retrieval.
Output: HDFCParser.swift, ICICIParser.swift, SystemGmailFetch (filled), GREEN parser tests against corpus fixtures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-bank-parsers-ingestion-pipeline/07-RESEARCH.md
@.planning/phases/07-bank-parsers-ingestion-pipeline/07-PATTERNS.md
@.planning/phases/07-bank-parsers-ingestion-pipeline/07-CONTEXT.md
@.planning/phases/07-bank-parsers-ingestion-pipeline/07-04-SUMMARY.md
@MyHomeTests/Fixtures/README.md
@MyHomeApp/Features/Ingestion/BankEmailParser.swift
@MyHomeApp/Features/Ingestion/MerchantNormalizer.swift
@MyHomeApp/Support/BudgetCalculator.swift
@MyHomeApp/Gmail/GmailAuthPort.swift
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: HDFCParser + ICICIParser GREEN against real corpus</name>
  <files>MyHomeApp/Features/Ingestion/HDFCParser.swift, MyHomeApp/Features/Ingestion/ICICIParser.swift, MyHomeTests/HDFCParserTests.swift, MyHomeTests/ICICIParserTests.swift, MyHome.xcodeproj/project.pbxproj</files>
  <read_first>
    - MyHomeTests/Fixtures/README.md (CONFIRMED senders, subjects, and the fixture-inventory table from plan 04 — use these real values, NOT the [ASSUMED] ones in RESEARCH)
    - The actual .eml fixtures in MyHomeTests/Fixtures/ (read several per bank/type to derive required template literals, amount/merchant/date/account regex patterns)
    - MyHomeApp/Features/Ingestion/BankEmailParser.swift (the BankEmailParser protocol + ParsedExpense fields to populate)
    - MyHomeApp/Features/Ingestion/MerchantNormalizer.swift (normalize() to call at parse time, ING-15)
    - MyHomeApp/Support/BudgetCalculator.swift (pure-struct guard-and-return-nil pattern)
    - .planning/phases/07-bank-parsers-ingestion-pipeline/07-RESEARCH.md (§Parser Architecture: two-stage, pre-filter, reversal keywords, fingerprint; §Confidence Scoring for fingerprintScore/extractionScore the parser must set)
    - MyHomeTests/HDFCParserTests.swift, MyHomeTests/ICICIParserTests.swift (RED stubs from plan 01 — fill the arguments table from the fixture inventory)
  </read_first>
  <behavior>
    - canHandle: confirmed HDFC/ICICI sender + transaction subject → true; OTP/"verification code"/"promotional"/"offer"/"statement" subject → false; unknown sender → false (ING-08)
    - parse: valid cc-spend fixture → ParsedExpense(amount, normalizedMerchant, date, rawSourceLabel non-empty, isReversal:false); refund fixture → amount negative, isReversal:true (ING-09); an email missing a required fingerprint literal → nil (ING-07)
  </behavior>
  <action>
    RED→GREEN→REFACTOR. First fill the `arguments:` tables in HDFCParserTests.swift and ICICIParserTests.swift from the plan-04 fixture inventory (filename → expected amount/merchant/isReversal), plus canHandle rejection cases and a fingerprint-fail nil case; confirm RED, then implement.
    `HDFCParser.swift`: `public struct HDFCParser: BankEmailParser` with `let parserID = "hdfc-v1"`, `let parserVersion = "1.0"`. Use CONFIRMED senders from Fixtures/README in `private static let allowedSenders: Set<String>` and a `blockedSubjectKeywords` list ("otp", "one time password", "verification code", "promotional", "offer", "statement"). `canHandle(sender:subject:)`: lowercased sender in allowedSenders AND no blocked keyword in lowercased subject. `parse(rawEmail:)` two-stage (ING-07): (1) extract a plain-text body from the raw RFC 2822 string by locating the `Content-Type: text/plain` part (hand-rolled targeted search per RESEARCH §Don't Hand-Roll — do NOT add a MIME library); (2) FINGERPRINT — check all required template literals (derived from the real fixtures) are present; if any is missing, `return nil` immediately (no extraction). (3) EXTRACT amount (Decimal via NSRegularExpression on the confirmed amount format, never Double — Pitfall 17), merchant string, date (fall back to message date if absent), and card/account source label (e.g. "HDFC ••1234"). (4) Call `MerchantNormalizer.normalize(rawMerchant)` for normalizedMerchant + categoryHint (ING-15). (5) REVERSAL — set `isReversal` true if body contains any of "reversed","reversal of","refund","credited back","refund credited" (ING-09); when true the parser reports the amount as `-abs(amount)`. (6) compute `fingerprintScore` (1.0 if all required literals present, plus optional-literal boost) and `extractionScore` (fraction of fields extracted) and return a populated `ParsedExpense`. Mirror the whole structure in `ICICIParser.swift` with `parserID = "icici-v1"` using ICICI's confirmed senders/templates. Register both source files in the MyHome target in project.pbxproj.
  </action>
  <verify>
    <automated>xcodebuild test -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:MyHomeTests/HDFCParserTests -only-testing:MyHomeTests/ICICIParserTests 2>&1 | tail -8 | grep -qE "passed|TEST SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - `HDFCParser.swift` defines `struct HDFCParser: BankEmailParser` with `parserID = "hdfc-v1"`, a `canHandle(` pre-filter, and a `parse(` that returns nil on fingerprint failure
    - `ICICIParser.swift` defines `struct ICICIParser: BankEmailParser` with `parserID = "icici-v1"`
    - `parse` calls `MerchantNormalizer.normalize` and produces a negative amount when a reversal keyword is present
    - HDFCParserTests + ICICIParserTests pass against the real `.eml` fixtures (GREEN)
  </acceptance_criteria>
  <done>Both parsers GREEN against the real corpus; fingerprint/extraction separated; reversals negative; pre-filter rejects non-transaction mail.</done>
</task>

<task type="auto">
  <name>Task 2: SystemGmailFetch real Gmail REST implementation</name>
  <files>MyHomeApp/Gmail/GmailFetchPort.swift, MyHome.xcodeproj/project.pbxproj</files>
  <read_first>
    - MyHomeApp/Gmail/GmailAuthPort.swift (lines ~200-235 — the URLSession request/error-wrapping pattern; adapt POST→GET with `Authorization: Bearer` header)
    - MyHomeApp/Gmail/GmailFetchPort.swift (the protocol + SystemGmailFetch stub from plan 01 to fill in)
    - .planning/phases/07-bank-parsers-ingestion-pipeline/07-RESEARCH.md (§GmailFetchPort — the three exact REST endpoints, base64url decode, pagination via nextPageToken; Pitfall 7 pagination)
    - .planning/phases/07-bank-parsers-ingestion-pipeline/07-PATTERNS.md (§URLSession GET with Bearer Token)
  </read_first>
  <action>
    Fill the three `SystemGmailFetch` methods with real network calls (this is exempt from TDD — it is network glue verified at runtime and via UAT, not unit-testable without the live API). `getProfile`: GET `https://gmail.googleapis.com/gmail/v1/users/me/profile` with `Authorization: Bearer \(accessToken)`, decode `GmailProfile { emailAddress }`. `listMessageIDs`: GET `https://gmail.googleapis.com/gmail/v1/users/me/messages?q=<urlencoded q>&maxResults=<min(maxResults,500)>`, decode `{ messages: [{id}], nextPageToken }`, and LOOP following `nextPageToken` until nil or a 3-page cap (Pitfall 7), collecting all message IDs. `getRawMessage`: GET `https://gmail.googleapis.com/gmail/v1/users/me/messages/<id>?format=RAW`, decode `{ raw: String }` (the field is `raw`, lowercase — RESEARCH A3), base64url-decode it (substitute `-`→`+`, `_`→`/`, pad to multiple of 4, then `Data(base64Encoded:)`, decode UTF-8). Wrap network failures in `GmailFetchError.networkError`, non-2xx in `.httpError(statusCode)`, decode failures in `.decodingError` (mirror GmailAuthError style). All over HTTPS via `URLSession.shared` with `timeoutInterval = 60`. Ensure GmailFetchPort.swift remains registered in project.pbxproj.
  </action>
  <verify>
    <automated>xcodebuild build -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' 2>&1 | tail -5 | grep -q "BUILD SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - `GmailFetchPort.swift` SystemGmailFetch contains the three real endpoints under `gmail.googleapis.com` with `Authorization: Bearer`
    - `listMessageIDs` loops on `nextPageToken` (pagination — not a single call)
    - `getRawMessage` decodes the base64url `raw` field (substitutes `-`/`_`, pads, `Data(base64Encoded:)`)
    - `xcodebuild build` reports BUILD SUCCEEDED
  </acceptance_criteria>
  <done>SystemGmailFetch performs live Gmail retrieval with pagination + base64url decode; verified live via plan 06 UAT.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gmail API → app | Untrusted email body content enters the app and is parsed |
| Access token → Gmail REST | Bearer token sent on every fetch call |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-08 | Tampering | Malicious email body content parsed by HDFC/ICICIParser | mitigate | Parser treats body as a plain `String` — string-matching and NSRegularExpression only; no `eval`, no HTML rendering, no script execution (RESEARCH §Security Domain) |
| T-07-09 | Information Disclosure | Over-broad Gmail `q` returning non-bank email | mitigate | `q` filters to bank senders + newer_than window (plan 06); `canHandle` pre-filter rejects non-bank senders before any body retention |
| T-07-10 | Information Disclosure | Access token over the wire | mitigate | All Gmail calls over HTTPS via URLSession (automatic TLS); token never logged; in-memory only (D6-07, unchanged from Phase 6) |
| T-07-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs this phase; MIME extraction hand-rolled (no MimeParser dependency) — Apple frameworks only |
</threat_model>

<verification>
- Parser tests GREEN against real fixtures
- Fingerprint-fail returns nil (no mis-parse)
- SystemGmailFetch builds; live behavior verified in plan 06 UAT
- No third-party MIME/network dependency added
</verification>

<success_criteria>
- HDFC + ICICI parsers calibrated to the real corpus (ING-06/07/08/09)
- SystemGmailFetch performs real Gmail retrieval with pagination + base64url decode
- Parser test files GREEN; all new files registered
</success_criteria>

<output>
Create `.planning/phases/07-bank-parsers-ingestion-pipeline/07-05-SUMMARY.md` when done
</output>
