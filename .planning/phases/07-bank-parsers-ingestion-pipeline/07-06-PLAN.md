---
phase: 07-bank-parsers-ingestion-pipeline
plan: 06
type: execute
wave: 4
depends_on: ["07-02", "07-03", "07-05"]
files_modified:
  - MyHomeApp/Features/Gmail/GmailSyncController.swift
  - MyHomeApp/Features/Expenses/ExpenseListView.swift
  - MyHomeApp/Features/Expenses/ReviewInboxRow.swift
  - MyHomeApp/Features/Expenses/ExpenseRow.swift
  - MyHomeApp/RootView.swift
  - MyHomeApp/MyHomeApp.swift
  - MyHomeApp/Info.plist
  - MyHomeTests/IngestionPipelineTests.swift
  - MyHomeTests/GmailSyncControllerTests.swift
  - MyHome.xcodeproj/project.pbxproj
autonomous: false
requirements: [ING-04, ING-12, ING-13, ING-14]

must_haves:
  truths:
    - "GmailSyncController.sync() runs the full pipeline: getProfile→connectedEmail, list→skip-dismissed→getRaw→parse→score→dedup→triage→persist"
    - "Confidence ≥0.85 AND not duplicate → Expense saved as autoSaved; below threshold or duplicate → needsReview/possibleDuplicate"
    - "Settings shows 'Connected as: <email>' after sync (UAT-6-05)"
    - "Expenses tab shows a Review-count badge and a 'Needs Review' section at the top of the list"
    - "Each review row shows parsed fields with one-tap-accept, tap-to-edit, swipe-to-discard; discard records the message ID as dismissed"
    - "Possible-duplicate rows show 'Possible duplicate of <existing>' side-by-side"
    - "Auto-saved expenses show a subtle envelope 'auto' marker and a source label"
    - "BGAppRefreshTask is registered (Info.plist + handler) and triggers ingestion via the LLDB simulate path"
  artifacts:
    - path: "MyHomeApp/Features/Expenses/ReviewInboxRow.swift"
      provides: "Review Inbox triage row"
      contains: "struct ReviewInboxRow"
    - path: "MyHomeApp/Info.plist"
      provides: "BGTask permitted identifier + fetch background mode"
      contains: "BGTaskSchedulerPermittedIdentifiers"
  key_links:
    - from: "GmailSyncController.sync"
      to: "HDFCParser/ICICIParser + ConfidenceScorer + DedupChecker"
      via: "pipeline composition"
      pattern: "ConfidenceScorer.score"
    - from: "ExpenseListView"
      to: "ingestionStateRaw needsReview/possibleDuplicate"
      via: "Needs Review section query"
      pattern: "ingestionStateRaw"
    - from: "MyHomeApp"
      to: "BGAppRefreshTask"
      via: ".backgroundTask(.appRefresh)"
      pattern: "appRefresh"
---

<objective>
Wire the full ingestion pipeline and its UI. Extend GmailSyncController.sync() to run getProfile→list→skip-dismissed→getRaw→parse→score→dedup→triage→persist using a ModelContext, populate connectedEmail (UAT-6-05), build the Review Inbox surface on the Expenses tab (badge + "Needs Review" section + triage row, D7-04/05/06/14), add the "auto" marker + source label to ExpenseRow (D7-08/15), and register the best-effort BGAppRefreshTask (ING-04). No UI-SPEC was produced for this phase (planned with --skip-ui): use the existing Expenses-tab / ExpenseListView / ExpenseRow visual baseline from Phases 1-2.

Purpose: Deliver the zero-touch ingestion experience — auto-save the certain, triage the rest, flag duplicates, keep "Sync now" the reliable path with background fetch as a bonus.
Output: Pipeline-complete GmailSyncController, Review Inbox UI, auto marker, BGTask registration, GREEN pipeline tests, and a device-verification checkpoint for ING-04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-bank-parsers-ingestion-pipeline/07-RESEARCH.md
@.planning/phases/07-bank-parsers-ingestion-pipeline/07-PATTERNS.md
@.planning/phases/07-bank-parsers-ingestion-pipeline/07-CONTEXT.md
@.planning/phases/06-gmail-sign-in-client/06-SECURITY.md
@.planning/phases/07-bank-parsers-ingestion-pipeline/07-02-SUMMARY.md
@.planning/phases/07-bank-parsers-ingestion-pipeline/07-03-SUMMARY.md
@.planning/phases/07-bank-parsers-ingestion-pipeline/07-05-SUMMARY.md
@MyHomeApp/Features/Gmail/GmailSyncController.swift
@MyHomeApp/Features/Expenses/ExpenseListView.swift
@MyHomeApp/Features/Expenses/ExpenseRow.swift
@MyHomeApp/RootView.swift
@MyHomeApp/MyHomeApp.swift
@MyHomeTests/GmailSyncControllerTests.swift
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GmailSyncController pipeline + connectedEmail (UAT-6-05)</name>
  <files>MyHomeApp/Features/Gmail/GmailSyncController.swift, MyHomeApp/RootView.swift, MyHomeTests/IngestionPipelineTests.swift, MyHomeTests/GmailSyncControllerTests.swift</files>
  <read_first>
    - MyHomeApp/Features/Gmail/GmailSyncController.swift (the init at lines ~118-128 to add `fetch:` param; the sync() stub at lines ~231-275 — keep the proactive-refresh block and the .syncing/.done transitions and the D6-10 query computation; the `connectedEmail` UserDefaults property at lines ~59-62)
    - MyHomeApp/RootView.swift (line 35 `@State private var gmailSyncController` ownership; how modelContext is available — sync needs a ModelContext)
    - MyHomeApp/Features/Ingestion/BankEmailParser.swift, HDFCParser.swift, ICICIParser.swift, ConfidenceScorer.swift, DedupChecker.swift, MerchantNormalizer.swift, DismissedMessageStore.swift (the pipeline components to compose)
    - .planning/phases/07-bank-parsers-ingestion-pipeline/07-RESEARCH.md (§Architecture Diagram for exact pipeline order; §UAT-6-05; Code Examples 1-2 for the triage insert + review query; Open Questions 1+3 for fetch injection + ModelContext)
    - MyHomeTests/GmailSyncControllerTests.swift (resetDefaults/defer pattern; how the controller is constructed with spies — add SpyGmailFetch as 3rd param)
    - MyHomeTests/IngestionPipelineTests.swift (RED stubs from plan 01 — replace Issue.record with real pipeline assertions)
  </read_first>
  <behavior>
    - After sync with SpyGmailFetch returning a profile, connectedEmail == that emailAddress (UAT-6-05)
    - SpyGmailFetch returns one raw HDFC cc-spend email → an Expense persists with rawEmailBody/parserID/parserVersion/sourceLabel/gmailMessageID set and ingestionStateRaw == "autoSaved" (confidence ≥0.85, not dup)
    - A low-confidence parse → ingestionStateRaw == "needsReview"; a dedup hit → "possibleDuplicate" (not autoSaved)
    - A message ID already in DismissedMessageStore is skipped (no Expense created)
  </behavior>
  <action>
    RED→GREEN. Update IngestionPipelineTests.swift + GmailSyncControllerTests.swift (replace plan-01 Issue.record stubs) to assert the behaviors above using `makeContainer()` (in-memory) + SpyGmailFetch, confirm RED, then implement.
    Add `fetch: any GmailFetchPort = SystemGmailFetch()` as the THIRD init parameter of GmailSyncController (after auth, keychain; before now), store as `private let fetch`. Give the controller access to a ModelContext for ingestion: add a `modelContext: ModelContext?` settable property (or a `func setContext(_:)`) injected by RootView via `.environment`/`onAppear` — pick the approach matching how RootView exposes the container (per Open Question 3, the BGTask path in Task 3 will create its OWN container/context; the foreground sync uses the app's context). Replace the sync() stub body AFTER the existing proactive-refresh block and AFTER `syncStatus = .syncing`: (1) `let profile = try await fetch.getProfile(accessToken: accessToken!); connectedEmail = profile.emailAddress` (UAT-6-05). (2) build `q` from the existing D6-10 query plus the bank sender filter (use confirmed senders from the parsers), call `fetch.listMessageIDs(...)`. (3) for each messageID: `if DismissedMessageStore.isDismissed(id) { continue }` (D7-07); `let raw = try await fetch.getRawMessage(...)`; extract sender+subject from raw headers; pick the parser whose `canHandle` returns true; `guard let parsed = parser.parse(rawEmail: raw) else { continue }` (fingerprint fail → skip). (4) `let confidence = ConfidenceScorer.score(parsed)`; fetch existing expenses from the context and `let dup = DedupChecker.findDuplicate(of: parsed, in: existing)`. (5) build an `Expense` (SchemaV4) per RESEARCH Code Example 1: amount (negative if isReversal), date, note=normalizedMerchant, parserID/parserVersion, rawEmailBody=raw, sourceLabel, gmailMessageID, parseConfidence, categories from categoryHint if present else leave for Uncategorized; set `ingestionStateRaw`: `"possibleDuplicate"` if dup != nil, else `"autoSaved"` if confidence >= 0.85, else `"needsReview"`. Insert into the context. When possibleDuplicate, persist a reference to the matched existing expense for side-by-side display (store the matched expense's merchant+date+amount in a derived form the row can read). (6) keep `lastSyncedAt = now(); syncStatus = .done`. Guard all `try await` in a do/catch that sets `syncStatus = .error` / `.tokenExpired` like the existing refresh block; do not regress Phase 6 token mitigations (06-SECURITY.md). Keep the 0.85 threshold as a named constant.
  </action>
  <verify>
    <automated>xcodebuild test -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:MyHomeTests/IngestionPipelineTests -only-testing:MyHomeTests/GmailSyncControllerTests 2>&1 | tail -8 | grep -qE "passed|TEST SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - GmailSyncController init has `fetch: any GmailFetchPort` as the third parameter
    - sync() calls `fetch.getProfile` and sets `connectedEmail` (UAT-6-05), and composes `ConfidenceScorer.score`, `DedupChecker.findDuplicate`, `DismissedMessageStore.isDismissed`, and a parser's `parse`
    - Persisted Expense sets `ingestionStateRaw` to autoSaved/needsReview/possibleDuplicate per triage and stores rawEmailBody/parserID/parserVersion/sourceLabel/gmailMessageID (ING-10/11/12/14)
    - IngestionPipelineTests + GmailSyncControllerTests pass (GREEN)
  </acceptance_criteria>
  <done>Full foreground pipeline + connectedEmail wired and GREEN; Phase 6 token mitigations intact.</done>
</task>

<task type="auto">
  <name>Task 2: Review Inbox UI + badge + auto marker + source label</name>
  <files>MyHomeApp/Features/Expenses/ExpenseListView.swift, MyHomeApp/Features/Expenses/ReviewInboxRow.swift, MyHomeApp/Features/Expenses/ExpenseRow.swift, MyHomeApp/RootView.swift, MyHome.xcodeproj/project.pbxproj</files>
  <read_first>
    - MyHomeApp/Features/Expenses/ExpenseListView.swift (the @Query + List + ForEach + onDelete + onTapGesture→editingExpense structure to extend with a Section)
    - MyHomeApp/Features/Expenses/ExpenseRow.swift (the HStack row layout to clone into ReviewInboxRow and to extend with the auto marker)
    - MyHomeApp/RootView.swift (the TabView tabItem for Expenses at tag 1 — where the `.badge(...)` attaches)
    - .planning/phases/07-bank-parsers-ingestion-pipeline/07-PATTERNS.md (§ExpenseListView, §ReviewInboxRow, §ExpenseRow auto marker — exact insertion points)
    - .planning/phases/07-bank-parsers-ingestion-pipeline/07-CONTEXT.md (D7-04/05/06/08/14/15 surface decisions)
  </read_first>
  <action>
    No UI-SPEC exists for this phase (planned with --skip-ui) — note this in the SUMMARY and follow the existing Expenses-tab visual baseline. In `ExpenseListView.swift`: add a computed/`@Query` `reviewItems` for expenses where `ingestionStateRaw != nil && ingestionStateRaw != "autoSaved"` sorted by createdAt desc (RESEARCH Code Example 2); render a `Section("Needs Review")` ABOVE the main expense ForEach when `!reviewItems.isEmpty`, each row a `ReviewInboxRow` (D7-04). The main list continues to show all expenses (autoSaved ones included, with the auto marker). Expose `reviewItems.count` upward so RootView can attach `.badge(count)` to the Expenses tabItem (D7-04). In `RootView.swift`, bind the badge count (e.g. via a small @Observable or a shared query) to the Expenses `.tabItem`. Create `ReviewInboxRow.swift` cloning the ExpenseRow HStack: show amount, normalized merchant (note), suggested category, date, and `sourceLabel` (caption, secondary) — parsed fields only, no raw-email snippet (D7-05). Add `.swipeActions(edge: .trailing)`: a destructive "Discard" that calls `DismissedMessageStore.dismiss(expense.gmailMessageID)` then deletes the expense from the context (D7-06/07), and a non-destructive "Accept" that clears `ingestionStateRaw = nil` (promotes to a normal expense). Tap on the row opens the existing EditExpenseView (tap-to-edit, D7-06). When `ingestionStateRaw == "possibleDuplicate"`, render the "Possible duplicate of <existing summary>" line and show the candidate + existing side-by-side (D7-14) using the duplicate summary persisted in Task 1. In `ExpenseRow.swift`: when `expense.ingestionStateRaw == "autoSaved"` show a subtle `Image(systemName: "envelope")` (caption2, secondary) as the auto marker (D7-08), and show `sourceLabel` on the row/detail when present (D7-15). Register ReviewInboxRow.swift in the MyHome target in project.pbxproj.
  </action>
  <verify>
    <automated>xcodebuild build -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' 2>&1 | tail -5 | grep -q "BUILD SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - `ReviewInboxRow.swift` defines `struct ReviewInboxRow` showing parsed fields + sourceLabel + swipe accept/discard, and a possible-duplicate line
    - `ExpenseListView.swift` renders a `Section("Needs Review")` for `ingestionStateRaw` needsReview/possibleDuplicate items and exposes a review count
    - `RootView.swift` attaches a `.badge(` to the Expenses tabItem driven by the review count
    - `ExpenseRow.swift` shows an `envelope` marker when `ingestionStateRaw == "autoSaved"` and shows `sourceLabel` when present
    - `xcodebuild build` reports BUILD SUCCEEDED
  </acceptance_criteria>
  <done>Review Inbox surface, badge, auto marker, and source label implemented on the Expenses tab baseline.</done>
</task>

<task type="auto">
  <name>Task 3: BGAppRefreshTask registration + Info.plist</name>
  <files>MyHomeApp/MyHomeApp.swift, MyHomeApp/RootView.swift, MyHomeApp/Info.plist, MyHome.xcodeproj/project.pbxproj</files>
  <read_first>
    - MyHomeApp/MyHomeApp.swift (the Scene body + `.modelContainer(container)` chain + setupNotifications onAppear — where the `.backgroundTask` modifier and `scenePhase` scheduling attach)
    - MyHomeApp/RootView.swift (current `@State private var gmailSyncController` ownership — must be reachable from the BGTask handler; per RESEARCH Open Question 2, move/share it so MyHomeApp can capture it)
    - MyHomeApp/Info.plist (current keys — add the two new arrays)
    - MyHomeApp/Persistence/ModelContainer+App.swift (the container factory to reuse for a FRESH container/context inside the background handler — Open Question 3)
    - .planning/phases/07-bank-parsers-ingestion-pipeline/07-RESEARCH.md (§BGAppRefreshTask — registration, scheduleBackgroundRefresh, Info.plist keys, MainActor hop, reschedule-inside-handler, 30s limit, LLDB trigger; Pitfalls 5+6)
  </read_first>
  <action>
    Register the best-effort BGAppRefreshTask (ING-04) — this is a BONUS path; "Sync now" stays the reliable path (do not imply reliability in any UI text). In `Info.plist` add `BGTaskSchedulerPermittedIdentifiers` = array containing `com.reojacob.myhome.emailrefresh`, and `UIBackgroundModes` = array containing `fetch`. In `MyHomeApp.swift`: add `@Environment(\.scenePhase) private var phase`; share the `gmailSyncController` so the handler can reach it (per Open Question 2 — move the `@State` controller to MyHomeApp and pass it into RootView, OR hold a shared reference; keep RootView's existing usage working). Add `.onChange(of: phase)` that calls `scheduleBackgroundRefresh()` when `newPhase == .background` (submit a `BGAppRefreshTaskRequest(identifier: "com.reojacob.myhome.emailrefresh")` with `earliestBeginDate = Date(timeIntervalSinceNow: 3600)`). Add `.backgroundTask(.appRefresh("com.reojacob.myhome.emailrefresh"))` chained after `.modelContainer(container)`; inside the handler: FIRST reschedule the next refresh (reschedule-inside-handler, otherwise never fires again), then `await MainActor.run { ... }` hop (GmailSyncController is @MainActor, Pitfall 5), create a FRESH ModelContainer+ModelContext via the Phase 1 factory (Open Question 3 — the background process cannot reuse the app's container), inject that context into the controller, and `await gmailSyncController.sync()`. Keep the handler fast (30s budget — list→skip-dismissed→getRaw→parse→triage, no retries). The `.backgroundTask` scene modifier registers the handler before end of launch (Info.plist id must have a handler or the app crashes — ensure both exist). Reflect Info.plist + edited files in project.pbxproj if needed.
  </action>
  <verify>
    <automated>xcodebuild build -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' 2>&1 | tail -5 | grep -q "BUILD SUCCEEDED" && plutil -p MyHomeApp/Info.plist | grep -q "com.reojacob.myhome.emailrefresh"</automated>
  </verify>
  <acceptance_criteria>
    - `Info.plist` contains `BGTaskSchedulerPermittedIdentifiers` with `com.reojacob.myhome.emailrefresh` and `UIBackgroundModes` with `fetch` (verified via `plutil -p`)
    - `MyHomeApp.swift` contains `.backgroundTask(.appRefresh("com.reojacob.myhome.emailrefresh"))` with a `MainActor.run` hop, an inside-handler reschedule, and a fresh ModelContainer/context
    - `scheduleBackgroundRefresh()` submits a `BGAppRefreshTaskRequest` on background scene phase
    - `xcodebuild build` reports BUILD SUCCEEDED
  </acceptance_criteria>
  <done>BGAppRefreshTask registered, scheduled, and handler wired with a fresh context + MainActor hop; device verification in Task 4.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Device + simulator verification of background fetch and Review Inbox</name>
  <files>MyHomeApp/MyHomeApp.swift, MyHomeApp/Features/Expenses/ExpenseListView.swift</files>
  <action>Human-verify checkpoint — pause after Tasks 1-3 and have Reo run the simulator + LLDB + real-device verification steps in how-to-verify, confirming connectedEmail, Review Inbox triage, possible-duplicate side-by-side, the auto marker, and the BGAppRefreshTask LLDB smoke test (plus the best-effort overnight-device run for ING-04). No code changes here — this verifies the prior tasks against the live Google account.</action>
  <what-built>
    Tasks 1-3 wired the full ingestion pipeline, the Review Inbox UI (badge + Needs Review section + triage), the auto marker + source label, connectedEmail (UAT-6-05), and the best-effort BGAppRefreshTask. The simulator NEVER fires BGAppRefreshTask autonomously (RESEARCH §BGAppRefreshTask Reliability) — so ING-04 needs both an LLDB simulate-trigger smoke test AND a real-device overnight run (carried blocker from STATE.md).
  </what-built>
  <how-to-verify>
    Build and run on the iPhone 17 simulator first (Settings → connect/Sync now with the live Google account):
    1. UAT-6-05: After Sync now, Settings shows "Connected as: <your-email>".
    2. Ingestion: real bank emails appear — high-confidence ones in the main list with the envelope "auto" marker + source label; low-confidence/duplicate ones in the "Needs Review" section with a count badge on the Expenses tab.
    3. Triage: one-tap-accept promotes a row out of review; tap-to-edit opens EditExpenseView; swipe-to-discard removes it AND a subsequent Sync now does NOT re-surface that email (dismissed-ID memory, D7-07).
    4. Possible-duplicate: create a manual expense that matches an incoming email (same amount + merchant + date) → it lands in Review marked "Possible duplicate of …" side-by-side, never silent-merged (D7-14).
    5. BGTask smoke test (simulator): background the app, then in the LLDB console run:
       `e -l objc -- (void)[[BGTaskScheduler sharedScheduler] _simulateLaunchForTaskWithIdentifier:@"com.reojacob.myhome.emailrefresh"]`
       Confirm the handler runs sync() without crashing and ingests.
    6. BGTask device verification (ING-04, the real gate): install on a real iPhone, sign in, leave it UNPLUGGED OVERNIGHT, and next morning confirm new bank emails were ingested without opening the app (or confirm best-effort: iOS may not have scheduled it — acceptable since "Sync now" is the reliable path; the requirement is that it is registered and CAN run, not that it always runs).
  </how-to-verify>
  <verify><human-check>Reo confirms pipeline ingestion, Review Inbox triage, connected-email, possible-duplicate, auto marker, and the LLDB BGTask smoke test all work; overnight-device result noted.</human-check></verify>
  <done>Foreground pipeline + Review Inbox + UAT-6-05 verified live; BGTask runs via LLDB simulate; overnight-device result recorded (or marked pending real-device availability).</done>
  <resume-signal>Type "verified" if the pipeline, Review Inbox triage, connected-email, and the LLDB BGTask smoke test all work (and note the overnight-device result, or that it is pending real-device availability). Otherwise describe the failures.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gmail API → app (foreground + background) | Untrusted email content parsed and persisted |
| Background process → SwiftData store | Fresh container/context writes ingested expenses |
| rawEmailBody at rest | Full PII email body persisted locally |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02 | Information Disclosure | rawEmailBody persisted on ingested Expense | accept | Local-only App Group store, no CloudKit in v1 (D7-10); protected by existing Face ID gate (LockController) — same prompt that gates expense amounts |
| T-07-11 | Authentication Bypass | Access token reused in background task after expiry | mitigate | sync() runs the existing proactive-refresh block before any fetch (D6-06, 06-SECURITY) — preserved unchanged in Task 1; background path calls the same sync() |
| T-07-12 | Denial of Service | BGTask exceeds 30s budget and is killed | mitigate | Handler kept fast (list→skip-dismissed→getRaw→parse→triage, no retries); "Sync now" remains the reliable path so a killed BG task never blocks ingestion |
| T-07-13 | Tampering | Background handler missing for registered identifier → crash | mitigate | `.backgroundTask` scene modifier registers the handler before end of launch; Info.plist id + handler added together (Task 3) |
| T-07-09 | Information Disclosure | Over-broad Gmail query persisting non-bank email bodies | mitigate | `q` restricted to confirmed bank senders + newer_than window; `canHandle` pre-filter + fingerprint gate before any Expense (with rawEmailBody) is created |
| T-07-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs this phase (Apple frameworks only) |
</threat_model>

<verification>
- IngestionPipelineTests + GmailSyncControllerTests GREEN
- Full suite green: `xcodebuild test -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17'`
- Info.plist BGTask keys present (plutil)
- connectedEmail, Review Inbox triage, possible-duplicate, auto marker verified manually
- BGTask LLDB smoke test + real-device overnight run (best-effort)
- Phase 6 OAuth/token mitigations intact (06-SECURITY.md)
</verification>

<success_criteria>
- Full pipeline wired (ING-12/13/14), connectedEmail populated (UAT-6-05)
- Review Inbox surface + badge + triage + possible-duplicate + auto marker + source label
- BGAppRefreshTask registered and best-effort runs (ING-04), verified via LLDB + device
- All Phase 7 tests GREEN; no regression
</success_criteria>

<output>
Create `.planning/phases/07-bank-parsers-ingestion-pipeline/07-06-SUMMARY.md` when done
</output>
