---
phase: 07-bank-parsers-ingestion-pipeline
plan: 04
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - MyHomeTests/Fixtures/README.md
autonomous: false
requirements: [ING-06, ING-07, ING-08, ING-09]

must_haves:
  truths:
    - "At least 50 anonymized real HDFC and 50 anonymized real ICICI transaction emails exist as .eml fixtures"
    - "The fixture set includes credit-card spend, UPI/debit, refund/reversal, and at least one OTP/promotional rejection sample per bank"
    - "Real sender addresses and subject patterns are recorded so the parser pre-filters (plan 05) use confirmed values, not [ASSUMED] ones"
  artifacts:
    - path: "MyHomeTests/Fixtures/README.md"
      provides: "Confirmed sender/subject patterns + fixture inventory from real corpus"
      contains: "Confirmed senders"
  key_links:
    - from: "MyHomeTests/Fixtures/*.eml"
      to: "plan 05 parsers"
      via: "loadFixture test helper"
      pattern: "\\.eml"
---

<objective>
Human corpus-collection gate (D7-03, carried blocker from STATE.md). Parser implementation and confidence-threshold calibration (plan 05) CANNOT proceed against invented email formats — they must be written against a real corpus. This plan collects ≥50 anonymized real HDFC and ≥50 anonymized real ICICI transaction emails as `.eml` fixtures and records the confirmed sender addresses + subject patterns that the parser pre-filters depend on.

Purpose: Replace the [ASSUMED] sender/subject/merchant assumptions (RESEARCH A1/A2/A5) with confirmed real-world values so parsers are calibrated, not guessed.
Output: Populated `MyHomeTests/Fixtures/` with anonymized `.eml` files + an updated README recording confirmed senders, subjects, and the fixture inventory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-bank-parsers-ingestion-pipeline/07-CONTEXT.md
@.planning/phases/07-bank-parsers-ingestion-pipeline/07-RESEARCH.md
@.planning/phases/07-bank-parsers-ingestion-pipeline/07-01-SUMMARY.md
@MyHomeTests/Fixtures/README.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Collect + anonymize the real HDFC/ICICI email corpus</name>
  <files>MyHomeTests/Fixtures/ (.eml files)</files>
  <action>Human-action checkpoint — no automation can synthesize a real bank-email corpus (D7-03). Pause execution and have Reo collect, anonymize, and place the `.eml` fixtures per the steps in how-to-verify, then resume. Do NOT invent emails: the parser fingerprints + 0.85 calibration depend on real templates. Target ≥50 HDFC + ≥50 ICICI including cc-spend, upi/debit, refund/reversal, and ≥1 OTP/promotional rejection sample per bank.</action>
  <what-built>
    Plan 01 created `MyHomeTests/Fixtures/README.md` documenting the expected `.eml` naming and the corpus gate. The parser test files (HDFCParserTests / ICICIParserTests) are RED stubs waiting on these fixtures. This checkpoint is the human-only step: real bank transaction emails cannot be synthesized — they must be collected from a real inbox and anonymized.
  </what-built>
  <how-to-verify>
    This is a human-action checkpoint — no automation can produce a real bank email corpus. Reo must:
    1. Ensure HDFC and ICICI transaction email alerts are enabled on the real accounts and that ~50+ transactions per bank have accumulated (credit-card spends, UPI/debit, and at least one refund/reversal each).
    2. Export the raw emails as `.eml` files (Gmail → Show original → Download Original, or equivalent).
    3. Anonymize each file: replace card/account tails with placeholders that PRESERVE format (e.g. real "HDFC ••4821" → "HDFC ••1234"; "a/c XX9087" → "a/c XX5678"), replace personal names/addresses, but KEEP the structural template intact (headers, sender, subject, amount formatting, merchant strings, date format) — the parser fingerprints depend on that structure.
    4. Place the files in `MyHomeTests/Fixtures/` using the naming from README: `hdfc_cc_spend_N.eml`, `hdfc_upi_debit_N.eml`, `hdfc_refund_N.eml`, `hdfc_otp_N.eml`, and `icici_*` equivalents. Include ≥1 OTP/promotional/verification sample per bank (these become rejection fixtures for ING-08).
    5. From the real emails, note the actual From: sender address(es) and representative Subject: lines for each transaction type per bank.
    Target: ≥50 HDFC + ≥50 ICICI. A smaller real set is far better than invented emails, but the 0.85 threshold calibration assumes a meaningful sample.
  </how-to-verify>
  <verify><human-check>Reo confirms the anonymized `.eml` corpus is present in MyHomeTests/Fixtures/ with the expected naming.</human-check></verify>
  <done>≥50 HDFC + ≥50 ICICI anonymized `.eml` fixtures (cc-spend, upi/debit, refund, otp/rejection) present in MyHomeTests/Fixtures/.</done>
  <resume-signal>Type "corpus ready" once the .eml files are in MyHomeTests/Fixtures/, or describe what was collected (counts per bank/type) and any blockers (e.g. not enough refund samples yet).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Record confirmed senders/subjects + fixture inventory in README</name>
  <files>MyHomeTests/Fixtures/README.md</files>
  <read_first>
    - MyHomeTests/Fixtures/README.md (the plan-01 scaffold to extend)
    - The collected .eml files in MyHomeTests/Fixtures/ (read a representative few per bank/type to extract real From: and Subject: values)
    - .planning/phases/07-bank-parsers-ingestion-pipeline/07-RESEARCH.md (Assumptions A1/A2/A5 that this confirms)
  </read_first>
  <action>
    Read a representative sample of the collected `.eml` files. Update `MyHomeTests/Fixtures/README.md` with a "## Confirmed senders" section listing the actual From: addresses observed for HDFC and ICICI transaction alerts, a "## Confirmed subjects" section with representative Subject: patterns per transaction type (cc-spend, upi/debit, refund/reversal, otp/promo), and a "## Fixture inventory" table listing each `.eml` filename, its bank, type, and the expected parse outcome (amount, merchant, isReversal) so plan 05's table-driven parser tests can be filled in directly. Flag any [ASSUMED] value from RESEARCH (A1/A2/A5) that the real corpus CONTRADICTS so plan 05 uses the confirmed value. Do NOT write parser code here — only document. If the corpus is short of 50 per bank, record the actual counts and note the calibration caveat.
  </action>
  <verify>
    <automated>test -f MyHomeTests/Fixtures/README.md && grep -q "Confirmed senders" MyHomeTests/Fixtures/README.md && ls MyHomeTests/Fixtures/*.eml >/dev/null 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `MyHomeTests/Fixtures/` contains `.eml` files (`ls MyHomeTests/Fixtures/*.eml` succeeds)
    - `README.md` contains a "Confirmed senders" section with real From: addresses for HDFC + ICICI
    - `README.md` contains a fixture-inventory table mapping each `.eml` to bank/type/expected outcome
    - Any RESEARCH [ASSUMED] sender/subject contradicted by the corpus is flagged for plan 05
  </acceptance_criteria>
  <done>Real corpus is in place and the confirmed sender/subject patterns + fixture inventory are documented for plan 05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Real personal emails → repo fixtures | PII must be stripped before committing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-07 | Information Disclosure | Real bank emails committed as test fixtures | mitigate | Anonymization step (Task 1 step 3) strips card tails, names, addresses while preserving template structure; fixtures contain placeholder account numbers only |
| T-07-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs this phase |
</threat_model>

<verification>
- `.eml` fixtures present in MyHomeTests/Fixtures/
- Confirmed sender/subject patterns recorded
- PII anonymized in committed fixtures
</verification>

<success_criteria>
- Real HDFC + ICICI corpus collected and anonymized
- Confirmed senders/subjects + fixture inventory documented for plan 05
- Parser calibration gate satisfied
</success_criteria>

<output>
Create `.planning/phases/07-bank-parsers-ingestion-pipeline/07-04-SUMMARY.md` when done
</output>
