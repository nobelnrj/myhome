---
phase: 03-notes-checklists
plan: 05
type: execute
wave: 4
depends_on: ["03-02", "03-04"]
files_modified:
  - MyHomeApp/Features/Notes/NotesHomeView.swift
  - MyHomeApp/Features/Notes/NotesListView.swift
  - MyHomeApp/Features/Notes/NoteRow.swift
  - MyHomeApp/Features/Notes/AddNoteView.swift
  - MyHomeApp/Features/Notes/EditNoteView.swift
  - MyHomeApp/Support/Date+Display.swift
  - MyHomeApp/RootView.swift
  - MyHome.xcodeproj/project.pbxproj
autonomous: false
requirements: [NOT-01, NOT-02, NOT-03, NOT-04, NOT-05, NOT-06]
must_haves:
  truths:
    - "A Notes tab appears in the TabView; its root hosts a List | Calendar segmented control (Calendar content arrives in plan 03-06)"
    - "The notes list renders Daily Routine → Pinned → Other sections with checklist rows; checked rows sink, struck-through + dimmed"
    - "User can create a note (required title) and edit title + interleaved text/checkbox blocks; edits auto-save ~500ms with no save button"
    - "An untitled note is discarded on dismiss"
    - "User can pin/unpin a note and search title + block text via .searchable"
  artifacts:
    - path: "MyHomeApp/Features/Notes/NotesHomeView.swift"
      provides: "Notes tab root: List|Calendar segmented host (calendar branch wired in 03-06)"
      contains: "segmented"
    - path: "MyHomeApp/Features/Notes/NotesListView.swift"
      provides: "Sectioned notes list + .searchable + toolbar +, using NoteListOrganizer/NoteSearchFilter"
      contains: "searchable"
    - path: "MyHomeApp/Features/Notes/EditNoteView.swift"
      provides: "Block editor + debounced auto-save + discard-on-empty-title"
      contains: "task"
    - path: "MyHomeApp/RootView.swift"
      provides: "Notes tab added to TabView"
      contains: "Notes"
  key_links:
    - from: "MyHomeApp/Features/Notes/NotesListView.swift"
      to: "MyHomeApp/Support/NoteListOrganizer.swift"
      via: "computed sections from @Query results"
      pattern: "NoteListOrganizer"
    - from: "MyHomeApp/Features/Notes/EditNoteView.swift"
      to: "modelContext.save()"
      via: "debounced ~500ms auto-save"
      pattern: "save\\(\\)"
    - from: "MyHomeApp/RootView.swift"
      to: "MyHomeApp/Features/Notes/NotesHomeView.swift"
      via: ".tabItem Notes"
      pattern: "NotesHomeView"
---

<objective>
Build the Notes note-keeper UI and wire the Notes tab: the segmented List|Calendar host (`NotesHomeView`, calendar branch filled in 03-06), the sectioned searchable list (`NotesListView` + `NoteRow`), the add/edit sheets with block editor + debounced auto-save + discard-on-empty-title, the reminder date/calendar display helpers in `Date+Display.swift`, and the `RootView` tab slot. Turns NOT-01..06 user-observable.

Purpose: NOT-01..06 — the note keeper itself. The list consumes NoteListOrganizer/NoteSearchFilter (03-04); auto-save uses the debounce verified in AutoSaveTests. Reminder editing/calendar/notifications are deferred to 03-06 (this plan leaves clean hooks).
Output: 5 new view files + Date+Display extension + RootView tab + auto-save (AutoSaveTests green). Ends with a human-verify checkpoint (auto-save + sectioning are L3 UAT).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-notes-checklists/03-PATTERNS.md
@.planning/phases/03-notes-checklists/03-UI-SPEC.md
@.planning/phases/03-notes-checklists/03-RESEARCH.md
@MyHomeApp/Features/Expenses/ExpenseListView.swift
@MyHomeApp/Features/Expenses/EditExpenseView.swift
@MyHomeApp/RootView.swift
@MyHomeApp/Support/Date+Display.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: NotesHomeView (segmented host) + NotesListView + NoteRow + RootView tab + Date+Display</name>
  <read_first>
    - MyHomeApp/Features/Expenses/ExpenseListView.swift (WHOLE file: @Query + @Environment(\.modelContext) read/write no-repository at 18-22, empty-state/List/toolbar-+/edit-.sheet at 24-64, explicit-save delete at 69-81)
    - MyHomeApp/Features/Budgets/BudgetsView.swift (NavigationStack + control-header-above-content shape at 30-54 — template for the List|Calendar segmented host)
    - MyHomeApp/RootView.swift (TabView at 13-23 — add the Notes tab beside Expenses/Budgets, lean 3-tab bar D3-17)
    - MyHomeApp/Support/Date+Display.swift (formattedForExpenseList 8-15, formattedAsMonthYear 42-49 — extend for reminder date/time + calendar labels; store UTC display .current D-02)
    - MyHomeApp/Support/NoteListOrganizer.swift + NoteSearchFilter.swift (the pure helpers from 03-04 the list consumes)
    - .planning/phases/03-notes-checklists/03-PATTERNS.md § NotesListView, § NoteRow, § RootView, § Date+Display, § "Segmented List|Calendar toggle"
    - .planning/phases/03-notes-checklists/03-UI-SPEC.md §1-6 (spacing 8pt, typography, colors, empty-state copy "No Notes Yet"/"Tap + to capture…", checked-row strikethrough+60% opacity, ≥44pt targets, VoiceOver labels)
  </read_first>
  <action>
    Create `NotesHomeView` as the Notes tab root: a `NavigationStack` whose top hosts a `Picker(...).pickerStyle(.segmented)` (List | Calendar) bound to `@State`, switching between `NotesListView` and a Calendar placeholder (a `ContentUnavailableView`/"No Reminders" stub — the real CalendarView lands in 03-06; leave a clearly-marked hook). Create `NotesListView`: `@Query(sort: \Note.modifiedAt, order: .reverse)` + `@Environment(\.modelContext)`; compute sections via `NoteListOrganizer` in a computed property; render Daily Routine → Pinned → Other as `Section`s (24pt section spacing); empty state `ContentUnavailableView("No Notes Yet", systemImage:"note.text", description:"Tap + to capture your first note or checklist.")`; toolbar `+` opens AddNoteView `.sheet`; tap-row opens EditNoteView `.sheet(item:)`; attach `.searchable` and filter via `NoteSearchFilter` (in-memory, per A2/Open Q3); explicit-save delete (mirror ExpenseListView:69-81). Create `NoteRow`: title (headline 20pt), pin icon (accent when pinned, tap toggles isPinned + explicit save), reminder-count badge; checked checklist rows shown `.strikethrough().opacity(0.6)`, sunk below open items per the organizer. Add the Notes tab to `RootView` (`NotesHomeView().tabItem { Label("Notes", systemImage: "note.text") }`). Extend `Date+Display.swift` with reminder date/time + calendar day/month label formatters (display in `.current`; UTC stored). Add all new files to the `MyHome` app target in project.pbxproj.
  </action>
  <verify>
    <automated>xcodebuild build -project MyHome.xcodeproj -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' 2>&1 | tail -5</automated>
  </verify>
  <done>App builds; Notes tab present with List|Calendar segmented control; list renders three sections + search + empty state; pin toggle + delete work with explicit save. (Sectioning/pin visual confirmation is the Task 3 checkpoint.)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AddNoteView + EditNoteView — block editor, debounced auto-save, discard-on-empty-title</name>
  <behavior>
    - AutoSaveTests/debounceCommitsAfterQuiet: an edit followed by ~500ms of quiet triggers exactly one context.save(); rapid edits coalesce into a single trailing save.
    - Empty-title-on-dismiss: a note left with empty title is deleted (context.delete) on dismiss, not persisted.
    - Block edit: adding/typing text and checkbox blocks updates the Note.blocks order; checking a row sets isChecked and re-sorts (open above checked).
  </behavior>
  <read_first>
    - MyHomeApp/Features/Expenses/EditExpenseView.swift (@Bindable to @Model 16-18, NavigationStack-in-sheet + Cancel toolbar 62-93, isDirty change-tracking 39-50, TextField row 226, plain-Text discipline comments 196/221, confirmationDialog destructive 94-105/241-252, saveExpense do/catch/assertionFailure 295-304, dismiss teardown 305-306)
    - MyHomeApp/Features/Expenses/AddExpenseView.swift (the add-sheet shell analog)
    - MyHomeTests/AutoSaveTests.swift (the failing debounceCommitsAfterQuiet stub from 03-01 — turn green; isolate the debounce so it is unit-testable without the view)
    - .planning/phases/03-notes-checklists/03-PATTERNS.md § EditNoteView/AddNoteView ("Auto-save override", "Required-title / discard-on-dismiss", isDirty gating, plain-text-only)
    - .planning/phases/03-notes-checklists/03-UI-SPEC.md §4-5 (no save button, untitled discarded, error copy "Couldn't save note…", block spacing 16pt)
  </read_first>
  <action>
    Create `AddNoteView` (sheet to create a Note: required title TextField, then dismisses into edit, or inserts and presents the block editor) and `EditNoteView` (`@Bindable var note: Note`): a block editor of interleaved `TextField` text blocks + checkbox rows (toolbar/menu to append a text block or checkbox block; reorder support optional), title `TextField` (required). Replace the explicit Save toolbar item with a DEBOUNCED ~500ms auto-save: isolate the debounce into a small testable unit (e.g. a `Debouncer`/async-`.task`-driven helper) so AutoSaveTests/debounceCommitsAfterQuiet passes; gate saves on `isDirty` (mirror EditExpenseView:39-50); use the `do { try context.save() } catch { assertionFailure(...) }` discipline. On `.dismiss`, if title is empty, `context.delete(note)` (discard-on-empty-title, D3-03/D3-19). Checking a checkbox row sets isChecked, cancels nothing yet (reminder cancellation arrives in 03-06 — leave a hook), re-sorts open-above-checked, struck-through+dimmed per UI-SPEC. Plain `TextField` only — never `TextEditor`/AttributedString (Pitfall 1). Leave a clearly-marked hook where a per-note / per-block "Set Reminder" entry point will attach (filled by 03-06). Add both files to the `MyHome` app target in project.pbxproj.
  </action>
  <verify>
    <automated>xcodebuild test -project MyHome.xcodeproj -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:MyHomeTests/AutoSaveTests 2>&1 | tail -10</automated>
  </verify>
  <done>AutoSaveTests/debounceCommitsAfterQuiet GREEN; EditNoteView/AddNoteView build; no save button; empty-title note discarded on dismiss; block editor edits interleaved text/checkbox blocks with checked-sinks-to-bottom; plain-text only.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual UAT — note keeper (auto-save, sectioning, pin, search, checked-to-bottom)</name>
  <files>(verification only — no files modified in this task)</files>
  <action>Pause for human verification. Build and run the app on the iPhone 17 simulator, then walk the how-to-verify steps below. This task modifies no files; it gates the plan on a person confirming the note-keeper behaviors (auto-save, sectioning, pin, search, checked-to-bottom) that cannot be asserted headlessly.</action>
  <what-built>The Notes tab with a sectioned searchable list, add/edit block editor, debounced auto-save (no save button), pin/unpin, and checked-row sinking. Calendar segment is still a placeholder (filled in 03-06).</what-built>
  <how-to-verify>
    On the iPhone 17 simulator (Xcode 26.5, scheme MyHome), per 03-VALIDATION.md Manual-Only Verifications:
    1. Launch, open the Notes tab — confirm List|Calendar segmented control and empty state "No Notes Yet".
    2. Tap + → create a note with a title and a few interleaved text + checkbox blocks. Confirm NO save button anywhere.
    3. Edit text, wait ~1s, force-quit and reopen the app — confirm edits persisted (auto-save).
    4. Create a second note, leave its title EMPTY, dismiss — confirm it was discarded (not in the list).
    5. Check a checklist row — confirm it sinks below open items, struck-through + dimmed.
    6. Pin a note — confirm it moves into the Pinned section. Create a note with a daily reminder later (03-06) to verify Daily Routine; for now confirm Pinned vs Other ordering (most-recent-first).
    7. Type in the search field — confirm the list filters by title AND block text.
    8. VoiceOver: confirm checkbox + pin controls have descriptive labels; Dynamic Type scales text.
  </how-to-verify>
  <verify><human-check>Human confirms steps 1-8 on the iPhone 17 simulator and records pass/fail in VERIFICATION.md.</human-check></verify>
  <done>Human has confirmed: Notes tab + segmented control render; create/edit with no save button; auto-save persists across force-quit; empty-title discarded; checked rows sink struck-through+dimmed; pin moves to Pinned section; search filters title+block text; VoiceOver/Dynamic Type OK.</done>
  <resume-signal>Type "approved" or describe issues (e.g. auto-save not firing, sections misordered, save button present).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user text → SwiftData | Title/block text persisted via SwiftData (parameterized; no query-string building) |
| user search query → in-memory filter | Free-form query used for type-safe Swift contains |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-10 | Input Validation | required-title rule (D3-03) | mitigate | Empty-title note discarded on dismiss; not persisted as a valid note |
| T-03-11 | Error Handling | save failure surfaced to user | mitigate | UI error copy "Couldn't save note. Please try again." (UI-SPEC §4); assertionFailure in debug, no body content logged |
| T-03-12 | Information Disclosure | note body in logs | mitigate | Save-error strings omit title/block text (zero-telemetry charter) |
| T-03-SC | Tampering | dependency installs | accept | No packages added — first-party SwiftUI/SwiftData only |
</threat_model>

<verification>
- `-only-testing:MyHomeTests/AutoSaveTests` GREEN (NOT-05 debounce).
- App builds; Notes tab + sectioned searchable list + block editor render.
- Manual UAT checkpoint (Task 3) recorded for auto-save / sectioning / pin / search / checked-to-bottom.
- Full suite green.
</verification>

<success_criteria>
- Notes tab with List|Calendar segmented host (calendar placeholder).
- Sectioned (Daily Routine → Pinned → Other) searchable list; pin/unpin; checked-row sink.
- Add/Edit block editor; debounced auto-save (no save button); discard-on-empty-title.
- NOT-01..06 observable; AutoSaveTests green; UAT recorded.
</success_criteria>

<output>
Create `.planning/phases/03-notes-checklists/03-05-SUMMARY.md` when done.
</output>
