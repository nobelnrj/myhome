---
phase: 03-notes-checklists
plan: 02
type: execute
wave: 1
depends_on: ["03-01"]
files_modified:
  - MyHomeApp/Persistence/Schema/SchemaV3.swift
  - MyHomeApp/Persistence/Models/Note.swift
  - MyHomeApp/Persistence/Models/NoteBlock.swift
  - MyHomeApp/Persistence/Models/ReminderValueTypes.swift
  - MyHomeApp/Persistence/Schema/MigrationPlan.swift
  - MyHomeApp/Persistence/ModelContainer+App.swift
  - MyHome.xcodeproj/project.pbxproj
autonomous: true
requirements: [NOT-01, NOT-02]
must_haves:
  truths:
    - "A Note with a title and an interleaved list of text + checkbox blocks persists and refetches with title and block order intact"
    - "Note and NoteBlock both carry reminder fields (all-day/timed, recurrence, end rule, lead-time)"
    - "A V2 store opens cleanly under AppMigrationPlan targeting SchemaV3 and pre-existing Expense rows survive"
    - "Note and NoteBlock pass the CloudKit-readiness reflection assertions (all properties optional/defaulted, no uniqueness constraints)"
  artifacts:
    - path: "MyHomeApp/Persistence/Schema/SchemaV3.swift"
      provides: "VersionedSchema v3.0.0 with Expense, Category copied verbatim + new Note, NoteBlock"
      contains: "Schema.Version(3, 0, 0)"
    - path: "MyHomeApp/Persistence/Models/Note.swift"
      provides: "typealias Note = SchemaV3.Note"
      contains: "typealias Note"
    - path: "MyHomeApp/Persistence/Models/ReminderValueTypes.swift"
      provides: "RecurrenceType/ReminderRecurrence/EndRuleType/ReminderEndRule Codable value types"
      contains: "ReminderRecurrence"
    - path: "MyHomeApp/Persistence/Schema/MigrationPlan.swift"
      provides: "v2ToV3 custom stage appended; schemas list includes SchemaV3"
      contains: "v2ToV3"
  key_links:
    - from: "MyHomeApp/Persistence/ModelContainer+App.swift"
      to: "SchemaV3"
      via: "Schema(versionedSchema: SchemaV3.self)"
      pattern: "versionedSchema: SchemaV3"
    - from: "SchemaV3.Note.blocks"
      to: "SchemaV3.NoteBlock.note"
      via: "@Relationship(deleteRule: .cascade, inverse: \\NoteBlock.note)"
      pattern: "inverse: \\\\NoteBlock\\.note"
---

<objective>
Add the Phase 3 data layer: `SchemaV3` (copies V2's Expense + Category verbatim, adds `Note` + `NoteBlock` with embedded reminder fields), the Codable reminder value types, the `v2ToV3` additive migration stage, the `Note`/`NoteBlock` typealiases, and the container flip to V3. This turns NoteModelTests and MigrationTests green.

Purpose: NOT-01 (note with title + body) and NOT-02 (inline checklist blocks) require a persistent block-list model; everything downstream (scheduler, list, calendar) reads these types. Schema work is the foundation of the phase.
Output: SchemaV3 + 2 @Models + reminder value types + migration stage + typealiases + container flip.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-notes-checklists/03-RESEARCH.md
@.planning/phases/03-notes-checklists/03-PATTERNS.md
@MyHomeApp/Persistence/Schema/SchemaV2.swift
@MyHomeApp/Persistence/Schema/MigrationPlan.swift
@MyHomeApp/Persistence/ModelContainer+App.swift
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create reminder value types + SchemaV3 with Note and NoteBlock @Models</name>
  <behavior>
    - NoteModelTests/noteWithTitlePersists: insert Note(title:"X"), save, refetch, title == "X"
    - NoteModelTests/blockListPreservesOrder: insert Note with interleaved [text(order:0), checkbox(order:1)], refetch sorted by order, kindRaw[1] == "checkbox", count == 2
    - NoteModelTests CloudKit-readiness assertion: every Note/NoteBlock attribute is optional or has a default; entity.uniquenessConstraints.isEmpty
  </behavior>
  <read_first>
    - MyHomeApp/Persistence/Schema/SchemaV2.swift (the WHOLE file: the 8-rule header comment 12-20, models list 24-26, the @Relationship inverse-on-ONE-side-only caveat at 42-48 + 75, defaulted/optional field discipline 34-40 & 64-70)
    - .planning/phases/03-notes-checklists/03-RESEARCH.md § "Data Model Design" (recommended Note/NoteBlock field sets + ReminderRecurrence/ReminderEndRule value types + the "Rationale (refined for the 8 rules)")
    - .planning/phases/03-notes-checklists/03-PATTERNS.md § SchemaV3.swift (structure to copy, models declaration, the Note.blocks cascade+inverse snippet, no-stored-enum rule)
    - MyHomeApp/Support/BudgetCalculator.swift lines 8-57 (the value-type + computed-property style template for the Codable reminder value types)
  </read_first>
  <action>
    Create `MyHomeApp/Persistence/Models/ReminderValueTypes.swift` with the Codable value types per D3-10/D3-11 and RESEARCH § Data Model Design: `enum RecurrenceType: String, Codable { none, daily, weekly, monthly, yearly }`, `struct ReminderRecurrence: Codable { type, weekdays: [Int]? }` (weekdays 1=Sun..7=Sat, only for .weekly), `enum EndRuleType: String, Codable { never, onDate, afterCount }`, `struct ReminderEndRule: Codable { type, endDate: Date?, occurrenceCount: Int? }`. These are NOT SwiftData @Models — they are serialized to `Data?` on the models (no stored enums, Pitfall 6 / rule 5).
  </action>
  <action>
    Create `MyHomeApp/Persistence/Schema/SchemaV3.swift` as `enum SchemaV3: VersionedSchema` with `static let versionIdentifier = Schema.Version(3, 0, 0)`. Copy V2's `Expense` and `Category` @Models VERBATIM into SchemaV3 (additive superset; never reference SchemaV2's types from V3). Add `@Model final class Note` and `@Model final class NoteBlock` with the field sets from RESEARCH § Data Model Design: Note { id UUID defaulted (NO @Attribute(.unique)), title String = "", blocks [NoteBlock]? = [] with `@Relationship(deleteRule: .cascade, inverse: \NoteBlock.note)`, isPinned Bool=false, createdAt/modifiedAt Date()=UTC, reminderEnabled Bool=false, reminderDate Date?, reminderIsAllDay Bool=false, reminderRecurrenceData Data?, reminderEndRuleData Data?, reminderLeadMinutes Int=0 }. NoteBlock { id UUID, kindRaw String="text", text String="", isChecked Bool=false, order Int=0, note Note? (inverse inferred — do NOT declare inverse on this side), plus the same reminder field set }. Declare `inverse:` ONLY on `Note.blocks` (the circular-macro caveat from SchemaV2:42-48/75). Reproduce the 8-rule header comment. Set `static var models` to `[SchemaV3.Expense.self, SchemaV3.Category.self, SchemaV3.Note.self, SchemaV3.NoteBlock.self]`. Add both new source files to the `MyHome` app target in project.pbxproj.
  </action>
  <verify>
    <automated>xcodebuild test -project MyHome.xcodeproj -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:MyHomeTests/NoteModelTests 2>&1 | tail -12</automated>
  </verify>
  <done>NoteModelTests is GREEN: noteWithTitlePersists, blockListPreservesOrder, and the CloudKit-readiness reflection assertion all pass. No @Attribute(.unique); reminder data fields present on both models; inverse declared on exactly one side (no circular-macro build error).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Append v2ToV3 migration stage, add typealiases, flip container to SchemaV3</name>
  <behavior>
    - MigrationTests/v1StoreMigratesCleanly stays green (no regression)
    - MigrationTests/v2StoreMigratesToV3: bundled MyHomeV2Seed.store opens under AppMigrationPlan with Schema(versionedSchema: SchemaV3.self); pre-existing Expense rows survive
  </behavior>
  <read_first>
    - MyHomeApp/Persistence/Schema/MigrationPlan.swift (the WHOLE file: schemas list 8-10, stages 12-14, the v1ToV2 .custom stage 19-24 + the FB13812722 rationale 16-18)
    - MyHomeApp/Persistence/Models/Expense.swift (the typealias version-flip pattern at line 11)
    - MyHomeApp/Persistence/ModelContainer+App.swift (line 18 schema flip point; the rest of the container/seed wiring 20-52 stays identical)
    - MyHomeTests/MigrationTests.swift (the v2StoreMigratesToV3 stub created in plan 03-01 — fill in its real assertions)
    - .planning/phases/03-notes-checklists/03-PATTERNS.md § MigrationPlan.swift + § Typealias Flip + § ModelContainer+App.swift
  </read_first>
  <action>
    In `MigrationPlan.swift`: append `SchemaV3.self` to the `schemas` list (never remove/reorder V1/V2), add `v2ToV3` to the `stages` array, and declare `static let v2ToV3 = MigrationStage.custom(fromVersion: SchemaV2.self, toVersion: SchemaV3.self, willMigrate: nil, didMigrate: nil)` mirroring the v1ToV2 stage (`.custom` over `.lightweight` per FB13812722; V3 adds only new models so both hooks are nil).
  </action>
  <action>
    Create `MyHomeApp/Persistence/Models/Note.swift` with `typealias Note = SchemaV3.Note` and `MyHomeApp/Persistence/Models/NoteBlock.swift` with `typealias NoteBlock = SchemaV3.NoteBlock` (mirror Expense.swift:11; one bare-name typealias per @Model pointed at the top version). In `ModelContainer+App.swift` flip line 18 `Schema(versionedSchema: SchemaV2.self)` → `SchemaV3.self`; leave the App-Group-URL fallback, `cloudKitDatabase: .none`, `ModelContainer(for:migrationPlan:configurations:)`, and `seedCategoriesIfNeeded` call untouched. Add the two new model files to the `MyHome` app target in project.pbxproj. Remove the `Issue.record("SchemaV3 pending plan 03-02")` gate in MigrationTests.v2StoreMigratesToV3 and let the real assertions run.
  </action>
  <verify>
    <automated>xcodebuild test -project MyHome.xcodeproj -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:MyHomeTests/MigrationTests 2>&1 | tail -12</automated>
  </verify>
  <done>MigrationTests fully GREEN: v1StoreMigratesCleanly unchanged, v2StoreMigratesToV3 opens the bundled V2 store under SchemaV3 and confirms Expense survival. Container builds on SchemaV3; bare `Note`/`NoteBlock` names resolve app-wide.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration → existing user store | Additive V2→V3 stage must not destroy/mutate existing Expense/Category data |
| free-form text → SwiftData | Title/block text persisted via SwiftData parameterized storage (no query-string building) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02 | Tampering / Data loss | v2ToV3 migration stage | mitigate | `.custom(willMigrate:nil, didMigrate:nil)` additive-only stage; never edit V1/V2; v2StoreMigratesToV3 asserts Expense survival (L2) |
| T-03-03 | Tampering | CloudKit-readiness violation (`.unique`, stored enum) | mitigate | Defaulted UUID PK, no `@Attribute(.unique)`, kindRaw String + recurrence/end-rule as Codable Data?; NoteModelTests reflection assertion enforces it |
| T-03-04 | Information Disclosure | note body text in logs | mitigate | No logging of title/block text (zero-telemetry charter); only `assertionFailure`/save-error strings without body content |
</threat_model>

<verification>
- `-only-testing:MyHomeTests/NoteModelTests` GREEN (NOT-01, NOT-02).
- `-only-testing:MyHomeTests/MigrationTests` GREEN (V1 + V2->V3).
- App + test target build on SchemaV3; no circular-macro relationship error.
- Phase 1/2 suites remain green (full suite).
</verification>

<success_criteria>
- SchemaV3 is an additive superset of V2 (Expense + Category verbatim + Note + NoteBlock).
- Reminder fields embedded on BOTH Note and NoteBlock (D3-02); reminder value types Codable, serialized to Data?.
- v2ToV3 additive stage appended; container + typealiases flipped to V3.
- NoteModelTests + MigrationTests green.
</success_criteria>

<output>
Create `.planning/phases/03-notes-checklists/03-02-SUMMARY.md` when done.
</output>
