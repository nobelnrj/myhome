---
phase: 03-notes-checklists
plan: 03
type: tdd
wave: 2
depends_on: ["03-02"]
files_modified:
  - MyHomeApp/Support/NotificationScheduler.swift
  - MyHomeApp/Support/NotificationCenterPort.swift
  - MyHome.xcodeproj/project.pbxproj
autonomous: true
requirements: [NOT-01, NOT-02]
must_haves:
  truths:
    - "A timed reminder with N advance lead-times builds N+1 notification requests"
    - "A weekly reminder with multiple weekdays builds one repeating calendar trigger per selected weekday"
    - "An 'after N occurrences' end rule stops scheduling at the Nth occurrence; an end-on-date rule stops past the date"
    - "buildRequests is pure (no OS I/O); only schedule/cancel touch the injected NotificationCenterPort"
    - "Pending request count stays at or under the iOS 64 cap under multi-weekday-weekly + after-N load"
  artifacts:
    - path: "MyHomeApp/Support/NotificationScheduler.swift"
      provides: "Pure buildRequests + schedule/cancel/pendingCount over NotificationCenterPort"
      contains: "buildRequests"
    - path: "MyHomeApp/Support/NotificationCenterPort.swift"
      provides: "NotificationCenterPort protocol + production UNUserNotificationCenter conformer"
      contains: "UNUserNotificationCenter"
  key_links:
    - from: "MyHomeApp/Support/NotificationScheduler.swift"
      to: "MyHomeApp/Support/NotificationCenterPort.swift"
      via: "let center: NotificationCenterPort"
      pattern: "NotificationCenterPort"
    - from: "NotificationScheduler.buildRequests"
      to: "UNCalendarNotificationTrigger(repeats: true)"
      via: "one repeating trigger per recurrence/weekday"
      pattern: "UNCalendarNotificationTrigger"
---

<objective>
Build the isolated, unit-testable notification scheduling core: the production `NotificationCenterPort` (wrapping `UNUserNotificationCenter.current()`) and the `NotificationScheduler` value type with a PURE `buildRequests(for:)` (main fire + each lead-time alert + recurrence trigger), plus `schedule`/`cancel`/`pendingCount` that touch only the injected port. Implements recurrence (daily/weekly-with-weekdays/monthly/yearly), app-side "after N" + end-on-date stop logic, and 64-cap budgeting. This turns NotificationSchedulerTests and RecurrenceTests green.

Purpose: D3-10/D3-11/D3-14/D3-15 — recurrence and the 64-cap require real logic that must be headlessly testable without the OS notification center. Keeping buildRequests pure (per RESEARCH seam) is what makes SC-R2 verifiable in CI.
Output: NotificationScheduler + NotificationCenterPort (production conformer); the SpyCenter test double from plan 03-01 drives the tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-notes-checklists/03-RESEARCH.md
@.planning/phases/03-notes-checklists/03-PATTERNS.md
@MyHomeApp/Support/BudgetCalculator.swift
</context>

<feature>
  <name>NotificationScheduler + NotificationCenterPort</name>
  <files>MyHomeApp/Support/NotificationScheduler.swift, MyHomeApp/Support/NotificationCenterPort.swift, MyHomeTests/NotificationSchedulerTests.swift, MyHomeTests/RecurrenceTests.swift, MyHomeTests/Support/SpyCenter.swift</files>
  <behavior>
    RED→GREEN per the VALIDATION.md tests (already stubbed in plan 03-01):
    - buildRequestsLeadAlerts: a timed reminder with leadMinutes [60, 1440] → buildRequests returns 3 requests (main + 2 advance), distinct identifiers.
    - weeklyMultiWeekday: a weekly reminder with weekdays [Mon, Wed, Fri] → 3 UNCalendarNotificationTrigger with repeats:true (one per weekday).
    - afterNStops: an "after N" end rule with N occurrences → the scheduler stops producing/rescheduling after the Nth occurrence (app-side occurrence counter).
    - endOnDateStops: an end-on-date rule → no requests for fire dates past endDate.
    - pendingCountUnderCap: under multi-weekday-weekly + after-N load, pendingCount() stays ≤ 64; budgeting trims/prioritizes if approached.
    - all-day vs timed: all-day builds DateComponents [.year,.month,.day]; timed adds [.hour,.minute].
  </behavior>
  <read_first>
    - .planning/phases/03-notes-checklists/03-RESEARCH.md § "Notification Service Architecture" (the full seam: NotificationCenterPort 4 members, NotificationScheduler with pure buildRequests + schedule/cancel/pendingCount) AND § "Common Pitfalls" 4 (64-cap) + 5 (timezone: build DateComponents in device tz from UTC) + Open Questions 2 (cancel-by-identifier-set then rebuild) & 4 (monthly/yearly clamp to last-valid-day)
    - MyHomeApp/Support/BudgetCalculator.swift lines 59-115 (pure static array-in/value-out discipline; monthBoundaries timezone handling at 105-115) — the shape NotificationScheduler.buildRequests copies
    - MyHomeTests/Support/SpyCenter.swift (the NotificationCenterPort protocol + ReminderInfo placeholder created in 03-01 — finalize ReminderInfo's real shape here and update the spy if needed)
    - MyHomeApp/Persistence/Models/ReminderValueTypes.swift (RecurrenceType/ReminderRecurrence/EndRuleType/ReminderEndRule — the scheduler consumes these)
    - MyHomeTests/NotificationSchedulerTests.swift + MyHomeTests/RecurrenceTests.swift (the failing stubs to turn green)
  </read_first>
  <implementation>
    Create `NotificationCenterPort.swift`: the `NotificationCenterPort` protocol (move it here from SpyCenter if cleaner, keeping SpyCenter conforming) + a production `final class SystemNotificationCenter: NotificationCenterPort` wrapping `UNUserNotificationCenter.current()` for all four members. Create `NotificationScheduler.swift` as a `struct` holding `let center: NotificationCenterPort`. Define the `ReminderInfo` input value type (reminderID, date UTC, isAllDay, recurrence: ReminderRecurrence, endRule: ReminderEndRule, leadMinutes: [Int]) — pure, no SwiftData. Implement `buildRequests(for:) throws -> [UNNotificationRequest]` PURELY: build the main fire request + one per lead-time, expand weekly multi-weekday to one `UNCalendarNotificationTrigger(repeats:true)` per weekday, clamp monthly/yearly to last-valid-day (Feb-29 / day-31), and honor end rules (afterCount via app-side occurrence count, onDate via date comparison). Build DateComponents by converting the stored UTC date to the device timezone (Pitfall 5). Implement `schedule(_:) async throws` (center.add per request), `cancel(reminderID:leadCount:)` (removePending by deterministic identifier set), and `pendingCount() async`. Enforce the 64-cap budget in scheduling (D3-15). Add both source files to the `MyHome` app target in project.pbxproj. Update SpyCenter/ReminderInfo in the test target as needed so tests compile against the finalized shape.
  </implementation>
</feature>

<tasks>

<task type="tdd">
  <name>Task 1: GREEN NotificationScheduler.buildRequests + recurrence/end-rule/64-cap (port + pure logic)</name>
  <read_first>
    - (see &lt;feature&gt;.read_first above — same files)
  </read_first>
  <action>
    Implement per &lt;feature&gt;.implementation. RED first: run the stubbed suites and confirm they fail; then implement NotificationCenterPort production conformer + NotificationScheduler until GREEN. Commit RED (test) → GREEN (feat) → REFACTOR if needed. Keep buildRequests free of any `UNUserNotificationCenter`/async I/O so it is asserted directly. Identifier scheme must be deterministic (reminderID + lead index + weekday) so cancel-by-identifier is exact.
  </action>
  <verify>
    <automated>xcodebuild test -project MyHome.xcodeproj -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:MyHomeTests/NotificationSchedulerTests -only-testing:MyHomeTests/RecurrenceTests 2>&1 | tail -15</automated>
  </verify>
  <done>NotificationSchedulerTests (buildRequestsLeadAlerts, weeklyMultiWeekday, pendingCountUnderCap) and RecurrenceTests (afterNStops, endOnDateStops) all GREEN. buildRequests is pure; production port wraps UNUserNotificationCenter; 64-cap budget enforced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app → OS notification center | Scheduler requests cross into UNUserNotificationCenter; bounded by the 64-pending cap |
| user integer input → scheduler | lead-time minutes + "after N" counts are user-controlled integers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Tampering / DoS (cap exhaustion) | unbounded lead-minutes / "after N" / multi-weekday weekly | mitigate | Clamp/validate lead-minutes ≥ 0 and occurrenceCount ≥ 1; enforce 64-pending budget in schedule(); pendingCountUnderCap test asserts ≤ 64 (D3-15) |
| T-03-06 | Repudiation / lost reminders | orphaned pending requests on cancel | mitigate | Deterministic identifier scheme enables exact cancel-by-identifier (cancel-then-rebuild on edit) — pendingCount tracks enabled-reminder count |
| T-03-07 | Information Disclosure | note body text in notification logs/console | mitigate | Scheduler logs identifiers/counts only, never note title/body content (zero-telemetry charter) |
</threat_model>

<verification>
- `-only-testing:MyHomeTests/NotificationSchedulerTests -only-testing:MyHomeTests/RecurrenceTests` GREEN.
- buildRequests has no async/OS dependency (assertable synchronously).
- Full suite remains green.
</verification>

<success_criteria>
- buildRequests pure; schedule/cancel/pendingCount the only port-touching members.
- Recurrence (daily/weekly-weekdays/monthly/yearly), lead-time alerts, after-N + on-date end rules, monthly/yearly clamp, 64-cap budget all implemented and tested.
- Production NotificationCenterPort conformer wraps UNUserNotificationCenter.
</success_criteria>

<output>
Create `.planning/phases/03-notes-checklists/03-03-SUMMARY.md` when done.
</output>
