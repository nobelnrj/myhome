---
phase: 03-notes-checklists
plan: 04
type: tdd
wave: 3
depends_on: ["03-02", "03-03"]
files_modified:
  - MyHomeApp/Support/NoteListOrganizer.swift
  - MyHomeApp/Support/NoteSearchFilter.swift
  - MyHomeApp/Support/CalendarAggregator.swift
  - MyHome.xcodeproj/project.pbxproj
autonomous: true
requirements: [NOT-03, NOT-04, NOT-06]
must_haves:
  truths:
    - "Given a set of notes, organizing them yields three ordered sections: Daily Routine (daily-recurring) → Pinned (manual) → Other (most-recent-first)"
    - "Toggling a note's isPinned moves it from Other into the Pinned section"
    - "A daily-recurring reminder places its note in Daily Routine; weekly/monthly/yearly do not"
    - "A search query matches a note when the query appears in its title OR any block's text; non-matches are excluded"
    - "Per-day reminder counts and a day's completion progress (done/total) are computed correctly from reminder records"
  artifacts:
    - path: "MyHomeApp/Support/NoteListOrganizer.swift"
      provides: "Pure sectioning/sort (Daily Routine → Pinned → Other) over a [Note] array"
      contains: "dailyRoutine"
    - path: "MyHomeApp/Support/NoteSearchFilter.swift"
      provides: "Pure title+block-text search predicate/filter over [Note]"
      contains: "matches"
    - path: "MyHomeApp/Support/CalendarAggregator.swift"
      provides: "Pure per-day reminder counts + day completion progress"
      contains: "perDay"
  key_links:
    - from: "MyHomeApp/Support/NoteListOrganizer.swift"
      to: "ReminderRecurrence (.daily)"
      via: "Daily Routine filter decodes reminderRecurrenceData"
      pattern: "daily"
    - from: "MyHomeApp/Support/CalendarAggregator.swift"
      to: "Note/NoteBlock reminder fields"
      via: "derived live; stores nothing extra"
      pattern: "reminderDate"
---

<objective>
Build the three PURE, SwiftData-free helper types that the Notes UI will consume: `NoteListOrganizer` (sectioning + sort: Daily Routine → Pinned → Other, NOT-03/04/SC-R5), `NoteSearchFilter` (title + all-block-text matching, NOT-06/D3-18), and `CalendarAggregator` (per-day reminder counts + day completion progress, SC-R4 math). These turn NoteListOrderingTests, NoteSearchTests, and CalendarAggregationTests green. Runs after 03-03 (serialized to avoid the shared project.pbxproj write).

Purpose: D3-08 list layout and D3-16 calendar are derived-live with no extra stored data; isolating the logic in pure helpers (BudgetCalculator discipline) makes the ordering, search, and aggregation headlessly testable independent of SwiftUI.
Output: 3 pure helper types in Support/, all consuming the bare `Note`/`NoteBlock` types + reminder value types.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-notes-checklists/03-RESEARCH.md
@.planning/phases/03-notes-checklists/03-PATTERNS.md
@MyHomeApp/Support/BudgetCalculator.swift
</context>

<feature>
  <name>NoteListOrganizer + NoteSearchFilter + CalendarAggregator</name>
  <files>MyHomeApp/Support/NoteListOrganizer.swift, MyHomeApp/Support/NoteSearchFilter.swift, MyHomeApp/Support/CalendarAggregator.swift, MyHomeTests/NoteListOrderingTests.swift, MyHomeTests/NoteSearchTests.swift, MyHomeTests/CalendarAggregationTests.swift</files>
  <behavior>
    - sectionOrdering: [pinned note, unpinned note, daily-recurring note] → sections == [DailyRoutine:[daily], Pinned:[pinned], Other:[unpinned]] with Other most-recent-first.
    - pinMovesToPinnedSection: toggling isPinned=true on an Other note re-sections it into Pinned.
    - dailyRoutineFilter: a note whose reminderRecurrenceData decodes to .daily lands in Daily Routine; a .weekly/.monthly/.yearly note does NOT.
    - matchesTitleAndBlockText: query "milk" matches a note titled "Groceries" with a checkbox block "buy milk"; a note with neither title nor any block containing "milk" is excluded; case-insensitive.
    - perDayCountsAndProgress: given reminders across days, per-day count map is correct; for a tapped day, completion progress = (checked rows due that day) / (total reminders due that day).
  </behavior>
  <read_first>
    - MyHomeApp/Support/BudgetCalculator.swift (lines 59-95 static array-in/value-out pure functions; 105-115 monthBoundaries timezone-correct month-edge math — directly reusable for calendar month edges)
    - .planning/phases/03-notes-checklists/03-PATTERNS.md § NotesListView (the Daily-Routine/Pinned/Other partition must live in a computed/pure helper, tested in NoteListOrderingTests — @Query can't express it) + § CalendarView (per-day counts + completion derived live; aggregation in a pure helper; reuse BudgetCalculator.monthBoundaries) + § "Pure-Logic-Helper Seam"
    - .planning/phases/03-notes-checklists/03-RESEARCH.md § "UI Architecture" (List sections + Calendar derived data) + Open Question 3 / Assumption A2 (search may need in-memory filter if #Predicate can't reach block text — this helper IS that in-memory filter)
    - MyHomeApp/Persistence/Models/ReminderValueTypes.swift (decode reminderRecurrenceData → ReminderRecurrence to detect .daily; decode for calendar fire dates)
    - MyHomeTests/NoteListOrderingTests.swift, NoteSearchTests.swift, CalendarAggregationTests.swift (the failing stubs from plan 03-01)
  </read_first>
  <implementation>
    `NoteListOrganizer`: a struct/enum with a static pure function taking `[Note]` (already fetched most-recent-first) → an ordered sections value (dailyRoutine, pinned, other). Daily Routine = notes whose decoded note-level reminder recurrence is `.daily`; Pinned = `isPinned == true` and not in Daily Routine; Other = the rest, preserving modifiedAt-descending order. `NoteSearchFilter`: a static pure `matches(_ note: Note, query: String) -> Bool` (case-insensitive contains over title + every block.text) and/or `filter(_ notes:[Note], query:)`. `CalendarAggregator`: static pure functions for per-day reminder counts over a month (reuse `BudgetCalculator.monthBoundaries` for edges, device-tz date bucketing per Pitfall 5) and a day's completion progress (done/total). All three are SwiftData-free (operate on already-fetched arrays), no SwiftUI, no @Query. Add all three source files to the `MyHome` app target in project.pbxproj.
  </implementation>
</feature>

<tasks>

<task type="tdd">
  <name>Task 1: GREEN NoteListOrganizer + NoteSearchFilter (sectioning, pin, daily-routine, search)</name>
  <read_first>
    - (see &lt;feature&gt;.read_first — NoteListOrderingTests, NoteSearchTests, NotesListView pattern, ReminderValueTypes, BudgetCalculator)
  </read_first>
  <action>
    Implement NoteListOrganizer + NoteSearchFilter per &lt;feature&gt;.implementation. RED: confirm NoteListOrderingTests + NoteSearchTests fail; GREEN: implement until sectionOrdering, pinMovesToPinnedSection, dailyRoutineFilter, matchesTitleAndBlockText pass. Decode reminderRecurrenceData with the value types from 03-02; keep both helpers pure and SwiftData-fetch-free.
  </action>
  <verify>
    <automated>xcodebuild test -project MyHome.xcodeproj -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:MyHomeTests/NoteListOrderingTests -only-testing:MyHomeTests/NoteSearchTests 2>&1 | tail -12</automated>
  </verify>
  <done>NoteListOrderingTests (sectionOrdering, pinMovesToPinnedSection, dailyRoutineFilter) and NoteSearchTests (matchesTitleAndBlockText) GREEN.</done>
</task>

<task type="tdd">
  <name>Task 2: GREEN CalendarAggregator (per-day counts + completion progress)</name>
  <read_first>
    - MyHomeApp/Support/BudgetCalculator.swift lines 105-115 (monthBoundaries — reuse for month edges, TimeZone.current)
    - MyHomeTests/CalendarAggregationTests.swift (the failing perDayCountsAndProgress stub)
    - .planning/phases/03-notes-checklists/03-PATTERNS.md § CalendarView (derived-live, stores nothing) + RESEARCH § UI Architecture Calendar
    - MyHomeApp/Persistence/Models/ReminderValueTypes.swift
  </read_first>
  <action>
    Implement CalendarAggregator per &lt;feature&gt;.implementation: per-day reminder count map for a month and per-day completion progress (done/total). Bucket reminder fire dates by device-timezone day (Pitfall 5), reuse BudgetCalculator.monthBoundaries for month edges. RED→GREEN on perDayCountsAndProgress. Keep pure (arrays in, values out).
  </action>
  <verify>
    <automated>xcodebuild test -project MyHome.xcodeproj -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:MyHomeTests/CalendarAggregationTests 2>&1 | tail -10</automated>
  </verify>
  <done>CalendarAggregationTests/perDayCountsAndProgress GREEN; aggregation is pure and timezone-correct.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user search text → filter | Free-form query string used for in-memory case-insensitive contains (no query-string injection — type-safe Swift, not SQL) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08 | Tampering | search predicate injection | mitigate | In-memory Swift `localizedCaseInsensitiveContains` over fetched arrays; no raw SwiftData query strings (type-safe) |
| T-03-09 | Information Disclosure | logging note/block text during filtering/aggregation | mitigate | Helpers return values only; no logging of title/block content (zero-telemetry charter) |
</threat_model>

<verification>
- NoteListOrderingTests + NoteSearchTests + CalendarAggregationTests GREEN.
- All three helpers are pure (no @Query / SwiftUI / fetch inside).
- Full suite remains green.
</verification>

<success_criteria>
- NoteListOrganizer yields Daily Routine → Pinned → Other (most-recent-first).
- NoteSearchFilter matches title + all block text, case-insensitive (NOT-06/D3-18).
- CalendarAggregator computes per-day counts + completion progress, derived live.
</success_criteria>

<output>
Create `.planning/phases/03-notes-checklists/03-04-SUMMARY.md` when done.
</output>
