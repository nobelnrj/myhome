---
phase: 03-notes-checklists
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - MyHomeTests/NoteModelTests.swift
  - MyHomeTests/NoteListOrderingTests.swift
  - MyHomeTests/NoteSearchTests.swift
  - MyHomeTests/AutoSaveTests.swift
  - MyHomeTests/NotificationSchedulerTests.swift
  - MyHomeTests/RecurrenceTests.swift
  - MyHomeTests/CalendarAggregationTests.swift
  - MyHomeTests/MigrationTests.swift
  - MyHomeTests/Support/SpyCenter.swift
  - MyHomeTests/Resources/MyHomeV2Seed.store
  - build/GenerateV2SeedStore.swift
  - MyHome.xcodeproj/project.pbxproj
autonomous: true
requirements: [NOT-01, NOT-02, NOT-03, NOT-04, NOT-05, NOT-06]
must_haves:
  truths:
    - "Every Wave 0 test file from VALIDATION.md exists and compiles (as skipped/failing stubs)"
    - "A SpyCenter conforming to NotificationCenterPort exists for headless scheduler tests"
    - "A bundled MyHomeV2Seed.store exists in the MyHomeTests target's Copy Bundle Resources"
    - "The full test suite builds and runs (stubs may be marked failing, not erroring to compile)"
  artifacts:
    - path: "MyHomeTests/NotificationSchedulerTests.swift"
      provides: "Failing-stub suite for scheduler + 64-cap (NotificationCenterPort/SpyCenter seam)"
      contains: "SpyCenter"
    - path: "MyHomeTests/Support/SpyCenter.swift"
      provides: "In-memory NotificationCenterPort test double"
      contains: "NotificationCenterPort"
    - path: "MyHomeTests/Resources/MyHomeV2Seed.store"
      provides: "On-disk V2 fixture for the V2->V3 migration-load test"
    - path: "MyHomeTests/MigrationTests.swift"
      provides: "Extended with v2StoreMigratesToV3 stub"
      contains: "v2StoreMigratesToV3"
  key_links:
    - from: "MyHomeTests/NotificationSchedulerTests.swift"
      to: "MyHomeTests/Support/SpyCenter.swift"
      via: "NotificationScheduler(center: SpyCenter())"
      pattern: "SpyCenter\\(\\)"
    - from: "MyHomeTests/Resources/MyHomeV2Seed.store"
      to: "MyHomeTests Copy Bundle Resources"
      via: "pbxproj build phase membership"
      pattern: "MyHomeV2Seed"
---

<objective>
Create the Nyquist Wave 0 test scaffold for Phase 3: every test file listed in 03-VALIDATION.md "Wave 0 Requirements", the shared `NotificationCenterPort` + `SpyCenter` test seam, and the bundled `MyHomeV2Seed.store` migration fixture (plus its generator script and Xcode bundle-membership wiring). Tests are created as compiling stubs that FAIL (not error) so later implementation waves turn them green.

Purpose: Establish the failing-test baseline that gates every downstream plan. Without this wave, implementation tasks have no automated proof-of-correctness to reference (VALIDATION.md Dimension 8).
Output: 8 test files (1 modified), 1 shared spy helper, 1 seed store + generator, pbxproj target membership for all new test files and the resource.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-notes-checklists/03-VALIDATION.md
@.planning/phases/03-notes-checklists/03-RESEARCH.md
@.planning/phases/03-notes-checklists/03-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the NotificationCenterPort seam + SpyCenter test double</name>
  <read_first>
    - .planning/phases/03-notes-checklists/03-RESEARCH.md § "Recommended seam (for testability)" (the NotificationCenterPort protocol + NotificationScheduler shape)
    - .planning/phases/03-notes-checklists/03-PATTERNS.md § "No Analog Found" (both port files have no codebase analog; spec is RESEARCH)
    - MyHomeTests/BudgetCalculatorTests.swift (Swift Testing @MainActor struct + #expect/Issue.record convention, and the `private typealias Cat = MyHome.Category` disambiguation trick at line 6)
  </read_first>
  <action>
    Create `MyHomeTests/Support/SpyCenter.swift`. Declare the `NotificationCenterPort` protocol there for now (production conformer is created in plan 03-06) with the four members from RESEARCH: `requestAuthorization(options:) async throws -> Bool`, `add(_:) async throws`, `removePendingNotificationRequests(withIdentifiers:)`, `pendingNotificationRequests() async -> [UNNotificationRequest]`. Implement `final class SpyCenter: NotificationCenterPort` as an in-memory double that records every added `UNNotificationRequest`, every removed identifier set, and a settable `authorizationResult: Bool`. Expose inspection properties: `addedRequests: [UNNotificationRequest]`, `removedIdentifiers: [String]`, `addedIdentifiers: [String]`. Add reminder fixture builder helpers (e.g. `timedReminder(leadMinutes:)`, `weeklyReminder(weekdays:)`) returning whatever input type the scheduler will consume (a `ReminderInfo`-shaped value — name it `ReminderInfo` per RESEARCH and define a placeholder struct here that plan 03-03 will move/replace). Mark the file with `import Testing`, `import UserNotifications`, `@testable import MyHome`. Add the new file to the `MyHomeTests` target in `project.pbxproj` (the project does NOT use file-system-synchronized groups — explicit pbxproj membership is required).
  </action>
  <verify>
    <automated>xcodebuild build-for-testing -project MyHome.xcodeproj -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' 2>&1 | tail -5</automated>
  </verify>
  <done>SpyCenter.swift compiles into the MyHomeTests target; NotificationCenterPort and SpyCenter resolve; build-for-testing succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Create all Wave 0 test-file stubs (failing) for notes, ordering, search, auto-save, scheduler, recurrence, calendar</name>
  <read_first>
    - .planning/phases/03-notes-checklists/03-VALIDATION.md § "Per-Task Verification Map" + "Wave 0 Requirements" (exact suite + test names: noteWithTitlePersists, blockListPreservesOrder, sectionOrdering, pinMovesToPinnedSection, dailyRoutineFilter, matchesTitleAndBlockText, debounceCommitsAfterQuiet, buildRequestsLeadAlerts, weeklyMultiWeekday, afterNStops, endOnDateStops, pendingCountUnderCap, perDayCountsAndProgress)
    - MyHomeTests/ExpenseModelTests.swift (Swift Testing imports, `makeContainer()` in-memory pattern at lines 1-14, CloudKit-readiness schema-metadata assertion at lines 78-129)
    - MyHomeTests/BudgetCalculatorTests.swift (pure-logic @Test/#expect style for scheduler/recurrence/calendar/ordering)
    - .planning/phases/03-notes-checklists/03-RESEARCH.md § "Testing Strategy" (sample @Test bodies for noteWithBlocksCreation, buildRequestsLeadAlerts, weeklyMultiWeekday, completeCancels, v2StoreMigratesToV3, afterNStops)
  </read_first>
  <action>
    Create these files, each an `@MainActor struct` Swift Testing suite, named to match VALIDATION.md exactly:
    `NoteModelTests` (noteWithTitlePersists, blockListPreservesOrder — NOT-01/02; plus a CloudKit-readiness metadata assertion mirroring ExpenseModelTests:78-129 for Note + NoteBlock),
    `NoteListOrderingTests` (sectionOrdering, pinMovesToPinnedSection, dailyRoutineFilter — NOT-03/04, SC-R5),
    `NoteSearchTests` (matchesTitleAndBlockText — NOT-06),
    `AutoSaveTests` (debounceCommitsAfterQuiet — NOT-05),
    `NotificationSchedulerTests` (buildRequestsLeadAlerts, weeklyMultiWeekday, pendingCountUnderCap; constructs `NotificationScheduler(center: SpyCenter())` — SC-R1, SC-R3(a), 64-cap),
    `RecurrenceTests` (afterNStops, endOnDateStops — SC-R2),
    `CalendarAggregationTests` (perDayCountsAndProgress — SC-R4(a,b)).
    Each test body must reference types that do not exist yet (Note, NoteBlock, NotificationScheduler, calendar aggregation helper). To keep the target COMPILING while these types are absent, gate each unimplemented `@Test` body so it fails rather than fails-to-compile: use `Issue.record("not yet implemented")` as the body where the production type is missing, OR add minimal local placeholder type declarations inside the test file that the implementing plan deletes. Choose the approach that compiles today (prefer `Issue.record` so there are no throwaway types to remove). Do NOT add the production types here. Add every new test file to the `MyHomeTests` target in `project.pbxproj`.
  </action>
  <action>
    Annotate each suite's header comment with the requirement ID(s) it proves (NOT-01..06, SC-R1..R5) and the VALIDATION.md command, so the executor of later waves can map test → requirement. Use `grep -v '^/'` style counting if any acceptance grep is needed (avoid counting comment lines).
  </action>
  <verify>
    <automated>xcodebuild test -project MyHome.xcodeproj -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:MyHomeTests/NoteModelTests -only-testing:MyHomeTests/NotificationSchedulerTests 2>&1 | tail -15</automated>
  </verify>
  <done>All seven suites compile and run; the targeted command executes (tests present and FAILING via recorded issues, proving the baseline). Suite/test names match VALIDATION.md verbatim.</done>
</task>

<task type="auto">
  <name>Task 3: Generate MyHomeV2Seed.store fixture, extend MigrationTests, wire bundle membership</name>
  <read_first>
    - build/GenerateSeedStore.swift (the existing V1 generator: schema-mirror approach, ModelContainer(for:migrationPlan:configurations:) usage, output-dir CLI arg)
    - MyHomeApp/Persistence/Schema/SchemaV2.swift (the exact V2 Expense + Category @Model definitions the V2 seed store must mirror so entity names match)
    - MyHomeTests/MigrationTests.swift (v1StoreMigratesCleanly at lines 16-56: temp-copy of bundled store at 29-33, open under AppMigrationPlan at 38-44, survival assertions at 47-55, MigrationTestsClass bundle accessor at 63-66, Bundle(for:).url(forResource:withExtension:"store") at 21-26)
    - .planning/phases/03-notes-checklists/03-PATTERNS.md § MigrationTests "Build-config action" note
  </read_first>
  <action>
    Create `build/GenerateV2SeedStore.swift` mirroring `build/GenerateSeedStore.swift` but mirroring `SchemaV2` (Expense + Category with the V2 relationship) verbatim, and an `AppMigrationPlan` mirror whose top schema is `SchemaV2`. It must write `MyHomeV2Seed.store` (+ `-shm`/`-wal`) and insert at least one Expense row (e.g. amount 100, note "Seed") and one Category so survival is assertable. Run it via `xcrun -sdk macosx swift build/GenerateV2SeedStore.swift MyHomeTests/Resources` to produce the store under `MyHomeTests/Resources/`.
  </action>
  <action>
    Extend `MyHomeTests/MigrationTests.swift` with `v2StoreMigratesToV3`: copy the bundled `MyHomeV2Seed.store` to temp (mirror lines 29-33), open `Schema(versionedSchema: SchemaV3.self)` under `AppMigrationPlan` (mirror 38-44), assert pre-existing Expense rows survive (mirror 47-55). Since `SchemaV3` does not exist until plan 03-02, gate the assertion body with `Issue.record("SchemaV3 pending plan 03-02")` so the file compiles and the test FAILS today. Add `MyHomeV2Seed.store` (and `-shm`/`-wal`) to the `MyHomeTests` target's Copy Bundle Resources build phase in `project.pbxproj` (mirror the existing `MyHomeV1Seed.store` membership).
  </action>
  <verify>
    <automated>ls -la MyHomeTests/Resources/MyHomeV2Seed.store && xcodebuild test -project MyHome.xcodeproj -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:MyHomeTests/MigrationTests 2>&1 | tail -10</automated>
  </verify>
  <done>MyHomeV2Seed.store exists in MyHomeTests/Resources and is in Copy Bundle Resources; MigrationTests compiles and runs; v1 migration test stays green; v2StoreMigratesToV3 present and failing (pending V3).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixture → on-disk store | Bundled `.store` is first-party, generated by our own script; no untrusted input |
| (none) | No network, no user input, no auth surface in this wave |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | SchemaV3/SchemaV2 seed-store entity-name mismatch | mitigate | Generator mirrors SchemaV2 verbatim so SQLite entity names match the app schema (same discipline as GenerateSeedStore.swift) |
| T-03-SC | Tampering | package/dependency installs | accept | No packages installed this phase — first-party Apple frameworks only (RESEARCH Package Legitimacy Audit = N/A) |
</threat_model>

<verification>
- `xcodebuild build-for-testing` succeeds (whole test target compiles with new stubs + SpyCenter).
- Targeted suites run and FAIL by design (recorded issues), establishing the Nyquist baseline.
- `MyHomeV2Seed.store` present in Resources and bundled into MyHomeTests.
- Existing Phase 1/2 suites remain green (no regression).
</verification>

<success_criteria>
- All 7 new test files + extended MigrationTests + SpyCenter compile in MyHomeTests.
- Suite/test names match 03-VALIDATION.md verbatim.
- MyHomeV2Seed.store generated and bundle-wired.
- Full `xcodebuild test` runs to completion (Phase 3 stubs failing, Phase 1/2 green).
</success_criteria>

<output>
Create `.planning/phases/03-notes-checklists/03-01-SUMMARY.md` when done.
</output>
