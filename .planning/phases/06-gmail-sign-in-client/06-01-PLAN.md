---
phase: 06-gmail-sign-in-client
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - MyHomeApp/Gmail/GmailAuthPort.swift
  - MyHomeApp/Gmail/KeychainPort.swift
  - MyHomeApp/Gmail/PKCE.swift
  - MyHomeApp/Features/Gmail/GmailSyncController.swift
  - MyHomeTests/Support/SpyGmailAuth.swift
  - MyHomeTests/Support/SpyKeychainStore.swift
  - MyHomeTests/PKCETests.swift
  - MyHomeTests/GmailAuthURLTests.swift
  - MyHomeTests/KeychainPortTests.swift
  - MyHomeTests/GmailSyncControllerTests.swift
  - MyHomeTests/RelativeTimestampTests.swift
  - MyHome.xcodeproj/project.pbxproj
autonomous: true
requirements: [ING-01, ING-02, ING-03, ING-05, ING-16, SEC-03, SET-04, SET-05]
user_setup:
  - service: google-cloud-console
    why: "OAuth client_id + redirect URI registration is a manual prerequisite for any real OAuth path (research §Google Cloud Console Setup). Wave 0 code compiles and unit-tests pass WITHOUT it; only manual/UAT paths in plan 04 need it."
    env_vars:
      - name: GMAIL_OAUTH_CLIENT_ID
        source: "Google Cloud Console -> APIs & Services -> Credentials -> OAuth client (iOS type). Stored later in GmailOAuthConfig.swift, not an env var; surfaced in plan 04."
    dashboard_config:
      - task: "Create iOS OAuth client (Bundle ID com.reojacob.myhome), enable Gmail API, set consent screen scope to gmail.readonly, add Reo as test user, register redirect URI"
        location: "console.cloud.google.com -> APIs & Services"

must_haves:
  truths:
    - "All five Phase 6 test files compile and FAIL (RED) because production logic is stubbed"
    - "GmailAuthPort and KeychainPort protocols exist as public Sendable seams"
    - "GmailSyncController exists as @MainActor @Observable with stubbed signIn/sync/signOut/refresh"
    - "SpyGmailAuth and SpyKeychainStore exist and conform to their ports"
  artifacts:
    - path: "MyHomeApp/Gmail/GmailAuthPort.swift"
      provides: "GmailAuthPort protocol + TokenResponse + RefreshResponse + GmailAuthError value types"
      contains: "public protocol GmailAuthPort"
    - path: "MyHomeApp/Gmail/KeychainPort.swift"
      provides: "KeychainPort protocol + KeychainError"
      contains: "public protocol KeychainPort"
    - path: "MyHomeApp/Gmail/PKCE.swift"
      provides: "PKCE struct stub (verifier/challenge) + generate() throwing"
      contains: "struct PKCE"
    - path: "MyHomeApp/Features/Gmail/GmailSyncController.swift"
      provides: "@Observable @MainActor controller stub + SyncStatus enum + buildAuthorizationURL stub"
      contains: "@Observable"
    - path: "MyHomeTests/Support/SpyGmailAuth.swift"
      provides: "in-memory GmailAuthPort test double"
      contains: "SpyGmailAuth"
    - path: "MyHomeTests/Support/SpyKeychainStore.swift"
      provides: "in-memory KeychainPort test double"
      contains: "SpyKeychainStore"
    - path: "MyHomeTests/GmailSyncControllerTests.swift"
      provides: "failing state-machine tests (ING-02/03/05/16, SET-04/05, SEC-03 spy)"
      contains: "GmailSyncControllerTests"
  key_links:
    - from: "MyHomeTests/Support/SpyGmailAuth.swift"
      to: "MyHomeApp/Gmail/GmailAuthPort.swift"
      via: "@testable import MyHome + protocol conformance"
      pattern: "GmailAuthPort"
    - from: "MyHome.xcodeproj/project.pbxproj"
      to: "all new .swift files"
      via: "PBXBuildFile + PBXFileReference + Sources build phase entries"
      pattern: "GmailAuthPort.swift in Sources"
---

<objective>
Wave 0 Nyquist gate: create the failing-test scaffold for all of Phase 6, plus the
minimum production type surface (port protocols, value types, PKCE stub, controller stub)
those tests reference so the whole suite COMPILES and runs RED.

This is the failing-test scaffold that every later plan turns GREEN. No real OAuth, no real
Keychain, no real network — only types, protocols, spies, and intentionally-failing test bodies.

Purpose: Establish the testable seam (mirrors Phase 5 BiometricAuthPort/LockController) so
plans 02–03 can implement against a fixed contract with no scavenger hunt, and so the verifier
can sample failing→passing transitions per 06-VALIDATION.md.

Output: 11 new files + pbxproj registration; full suite compiles, GmailSyncControllerTests /
PKCETests / GmailAuthURLTests / KeychainPortTests / RelativeTimestampTests all RED.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-gmail-sign-in-client/06-RESEARCH.md
@.planning/phases/06-gmail-sign-in-client/06-PATTERNS.md
@.planning/phases/06-gmail-sign-in-client/06-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port protocols + value types + PKCE/controller stubs (compile surface)</name>
  <files>MyHomeApp/Gmail/GmailAuthPort.swift, MyHomeApp/Gmail/KeychainPort.swift, MyHomeApp/Gmail/PKCE.swift, MyHomeApp/Features/Gmail/GmailSyncController.swift</files>
  <read_first>
    - MyHomeApp/Security/BiometricAuthPort.swift (THE analog for both port protocol shape AND the same-file production-conformer convention — note: this project keeps protocol + System* conformer in ONE file, contrary to RESEARCH's separate-file split; follow the codebase, not RESEARCH, for file layout)
    - MyHomeApp/Support/NotificationCenterPort.swift (second port analog: mixed sync/async throwing methods)
    - MyHomeApp/Security/LockController.swift (THE analog for GmailSyncController: @MainActor @Observable final class, App Group `defaults` computed prop with suite "group.com.reojacob.myhome", injected ports with `= SystemXxx()` defaults, injectable `now: () -> Date`, scenePhaseChanged signature, private mapError)
    - .planning/phases/06-gmail-sign-in-client/06-RESEARCH.md (Patterns 1, 6, 7; value-type definitions for TokenResponse/RefreshResponse/GmailAuthError/KeychainError/SyncStatus)
  </read_first>
  <action>
    Create the production compile surface that tests reference. Keep bodies STUBBED so tests fail RED.

    GmailAuthPort.swift: declare `public protocol GmailAuthPort: Sendable` with three async throwing methods — `authorize(authURL:callbackScheme:) async throws -> String`, `exchangeCode(_:verifier:clientID:redirectURI:) async throws -> TokenResponse`, `refreshToken(_:clientID:) async throws -> RefreshResponse` (signatures per PATTERNS analog lines 51–55). In the SAME file declare the `public` Decodable value types `TokenResponse` (access_token, expires_in:Int, refresh_token:String?, token_type, scope) and `RefreshResponse` (access_token, expires_in:Int, token_type) per RESEARCH Patterns 4–5, and `public enum GmailAuthError: Error` with cases userCancelled, callbackURLInvalid, noAuthCode, networkError(Error), oauthError(String) per RESEARCH Pattern 2. Do NOT add the SystemGmailAuth conformer here — that is plan 04. `import Foundation` / `import AuthenticationServices`.

    KeychainPort.swift: declare `public protocol KeychainPort: Sendable` with `save(_:forKey:) throws`, `load(forKey:) throws -> String?`, `delete(forKey:) throws` (synchronous-throwing, per RESEARCH Pattern 6 lines 455–459). Add `public enum KeychainError: Error` with cases itemNotFound, duplicateItem, unexpectedData, unexpectedStatus(OSStatus). Do NOT add SystemKeychainStore — that is plan 04. `import Foundation`.

    PKCE.swift: declare `struct PKCE` with `let verifier: String`, `let challenge: String`, and `static func generate() throws -> PKCE` whose body is `fatalError("unimplemented")` (stub). Add `enum PKCEError: Error { case failedToGenerateRandomBytes }`. `import Foundation`.

    GmailSyncController.swift: declare `@MainActor @Observable final class GmailSyncController`. Mirror LockController structure exactly. Persistent computed props backed by App Group `defaults` (suite "group.com.reojacob.myhome"): `lastSyncedAt: Date?` (key "gmail_last_synced_at"), `accessTokenExpiry: Date?` (key "gmail_access_token_expiry"), `connectedEmail: String?` (key "gmail_connected_email"). In-memory vars: `accessToken: String? = nil`, `syncStatus: SyncStatus = .idle`, `authError: GmailAuthError? = nil`. Derived props: `isConnected` (via `try? keychain.load(forKey: "refresh_token") != nil`), `isTokenExpired` (expiry < now()), `needsProactiveRefresh` (expiry.timeIntervalSince(now()) < 300, treat nil expiry as true). Injected deps: `private let auth: any GmailAuthPort`, `private let keychain: any KeychainPort`, `private let now: () -> Date`; `init(auth: any GmailAuthPort = SystemGmailAuth(), keychain: any KeychainPort = SystemKeychainStore(), now: @escaping () -> Date = Date.init)` — NOTE the default args reference SystemGmailAuth()/SystemKeychainStore() which don't exist until plan 04, so for Wave 0 make the defaults reference temporary inline stub structs `_StubGmailAuth`/`_StubKeychain` defined privately in THIS file (plan 04 swaps them for the real System* defaults). Add the methods `func scenePhaseChanged(_ phase: ScenePhase)`, `func signIn() async`, `func sync() async`, `func signOut()`, and a pure `nonisolated func buildAuthorizationURL(clientID:redirectURI:pkce:state:) -> URL?` — all with STUB bodies (`syncStatus = .idle` / empty / `return nil`) so the corresponding tests fail. Declare `enum SyncStatus: Equatable { case idle, authorizing, syncing, done, tokenExpired, error(String) }` in this file. `import Foundation` / `import SwiftUI` / `import AuthenticationServices`.
  </action>
  <verify>
    <automated>cd "/Users/reo/My Projects/my-home" && grep -q "public protocol GmailAuthPort: Sendable" MyHomeApp/Gmail/GmailAuthPort.swift && grep -q "public protocol KeychainPort: Sendable" MyHomeApp/Gmail/KeychainPort.swift && grep -q "@Observable" MyHomeApp/Features/Gmail/GmailSyncController.swift && grep -q "enum SyncStatus" MyHomeApp/Features/Gmail/GmailSyncController.swift && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `MyHomeApp/Gmail/GmailAuthPort.swift` contains exact string `public protocol GmailAuthPort: Sendable` and declares TokenResponse, RefreshResponse, GmailAuthError
    - `MyHomeApp/Gmail/KeychainPort.swift` contains `public protocol KeychainPort: Sendable` and `enum KeychainError`
    - `MyHomeApp/Gmail/PKCE.swift` contains `static func generate() throws -> PKCE` whose body is a stub (fatalError / not-yet-implemented)
    - `GmailSyncController.swift` contains `@MainActor`, `@Observable`, `enum SyncStatus`, computed `defaults` using suite `"group.com.reojacob.myhome"`, and stubbed signIn/sync/signOut/scenePhaseChanged/buildAuthorizationURL
    - No file contains a `SystemGmailAuth` or `SystemKeychainStore` struct definition (those are plan 04)
  </acceptance_criteria>
  <done>Four production stub files exist; protocols are public+Sendable; controller mirrors LockController; bodies are stubs that will fail tests.</done>
</task>

<task type="auto">
  <name>Task 2: Test doubles + five failing test files (RED) + pbxproj registration</name>
  <files>MyHomeTests/Support/SpyGmailAuth.swift, MyHomeTests/Support/SpyKeychainStore.swift, MyHomeTests/PKCETests.swift, MyHomeTests/GmailAuthURLTests.swift, MyHomeTests/KeychainPortTests.swift, MyHomeTests/GmailSyncControllerTests.swift, MyHomeTests/RelativeTimestampTests.swift, MyHome.xcodeproj/project.pbxproj</files>
  <read_first>
    - MyHomeTests/Support/SpyBiometricAuth.swift (THE analog for SpyGmailAuth: public final class, @unchecked Sendable, settable `<method>Result` stubs, recorded `<method>Calls` arrays, reset())
    - MyHomeTests/Support/SpyCenter.swift (THE analog for SpyKeychainStore: in-memory store + reset())
    - MyHomeTests/LockStateTests.swift (THE analog for GmailSyncControllerTests: @MainActor struct, import Testing, @testable import MyHome, App Group suite reset + defer cleanup, Arrange/Act/Assert with #expect and message strings)
    - MyHomeTests/LockSettingsTests.swift (analog for PKCETests/GmailAuthURLTests/KeychainPortTests/RelativeTimestampTests: non-async @Test naming `"<name>: <behavior> — <reqID>"`, UAT Verification Log MARK block)
    - MyHome.xcodeproj/project.pbxproj lines 72–75, 164–167, 330–331, 391, 572 (the exact A50x/F50x BuildFile + FileReference + group-children + Sources-phase pattern to copy for BiometricAuthPort/SpyBiometricAuth/LockController)
    - .planning/phases/06-gmail-sign-in-client/06-RESEARCH.md (Validation Architecture test map; SpyKeychainStore code lines 544–560; UAT-6-01..10 list)
    - .planning/phases/06-gmail-sign-in-client/06-VALIDATION.md (Per-Task Verification Map → which test file covers which requirement)
  </read_first>
  <action>
    SpyGmailAuth.swift: `public final class SpyGmailAuth: GmailAuthPort, @unchecked Sendable`. Settable stubs: `authorizeResult: String = "stub_code"`, `exchangeResult: TokenResponse` (default with non-nil refresh_token), `refreshResult: RefreshResponse`, plus throw stubs `shouldThrowOnAuthorize/Exchange/Refresh: Error? = nil`. Recorded calls arrays. `reset()`. `import Testing` + `import AuthenticationServices` + `@testable import MyHome`.

    SpyKeychainStore.swift: `public final class SpyKeychainStore: KeychainPort, @unchecked Sendable` with `private var store: [String:String]=[:]`, `shouldThrowOnSave/Load: Error? = nil`, save/load/delete operating on the dict, and `reset()` clearing dict + error stubs (RESEARCH lines 544–560).

    Write FIVE test files with real `@Test` cases that assert correct behavior and therefore FAIL against the stubs. Every `@Test` description ends with the requirement ID. Map exactly to 06-VALIDATION.md:
      - PKCETests.swift (ING-01): verifier length 43–128; challenge non-empty and != verifier; generate() twice yields different verifiers.
      - GmailAuthURLTests.swift (ING-01): buildAuthorizationURL output contains query items client_id, redirect_uri, response_type=code, scope=https://www.googleapis.com/auth/gmail.readonly, code_challenge, code_challenge_method=S256, access_type=offline, state, prompt=consent.
      - KeychainPortTests.swift (SEC-03): SpyKeychainStore save→load round-trip returns the value; delete→load returns nil; overwrite (save twice) returns latest value.
      - RelativeTimestampTests.swift (ING-05, SET-05): a `Date.relativeToNow` helper (to be implemented in plan 02) for a date 2 hours in the past produces a string containing "hour"; assert this — RED until plan 02 adds the extension.
      - GmailSyncControllerTests.swift (ING-02/03/05/16, SET-04/05, SEC-03): inject SpyGmailAuth + SpyKeychainStore + fixed `now`. Cover: signIn drives syncStatus idle→done and writes lastSyncedAt and stores refresh token in spy keychain and calls sync (ING-02/05); sync transitions idle→syncing→done (ING-03); needsProactiveRefresh true when expiry < 5 min from now and false when > 5 min (ING-16); isTokenExpired true → scenePhaseChanged(.active) sets syncStatus == .tokenExpired (ING-16); signOut() calls keychain.delete for "refresh_token" and clears connectedEmail/lastSyncedAt/accessTokenExpiry (SET-04); reconnect overwrites refresh token in spy keychain with new value (SEC-03/SET-04). Reset the App Group suite keys at top of each mutating test with defer cleanup. Append a `// MARK: - UAT Verification Log` block listing UAT-6-01..10 from RESEARCH.

    Register ALL 11 new files (4 from Task 1 + 2 spies + 5 tests) in MyHome.xcodeproj/project.pbxproj. For EACH file add: (a) a PBXBuildFile entry, (b) a PBXFileReference entry, (c) a child entry under the correct PBXGroup (app files under the MyHomeApp group / create Gmail + Features/Gmail subgroups mirroring Security/Support; test files under MyHomeTests, spies under MyHomeTests/Support group), (d) an entry in the correct target's Sources build phase (app files → MyHome target Sources; test files → MyHomeTests target Sources). Use new unique stable IDs continuing the A50x/F50x scheme (e.g. A510+/F510+). Mirror exactly the BiometricAuthPort.swift (app) and SpyBiometricAuth.swift (test) entries.
  </action>
  <verify>
    <automated>cd "/Users/reo/My Projects/my-home" && for f in GmailAuthPort KeychainPort PKCE GmailSyncController SpyGmailAuth SpyKeychainStore PKCETests GmailAuthURLTests KeychainPortTests GmailSyncControllerTests RelativeTimestampTests; do grep -q "$f.swift" MyHome.xcodeproj/project.pbxproj || { echo "MISSING $f in pbxproj"; exit 1; }; done && xcodebuild test -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:MyHomeTests/PKCETests -only-testing:MyHomeTests/GmailSyncControllerTests 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - All 11 new `.swift` filenames appear in `MyHome.xcodeproj/project.pbxproj` (grep loop in verify exits 0)
    - `xcodebuild test ... -only-testing:MyHomeTests/PKCETests -only-testing:MyHomeTests/GmailSyncControllerTests` BUILDS SUCCEEDED and reports test FAILURES (RED) — compilation must succeed; tests must fail because logic is stubbed
    - `SpyGmailAuth` conforms to `GmailAuthPort`; `SpyKeychainStore` conforms to `KeychainPort`; both `@unchecked Sendable` with `reset()`
    - GmailSyncControllerTests injects both spies and a fixed `now` closure; every `@Test` description string ends with a requirement ID (ING-/SEC-/SET-)
    - A `// MARK: - UAT Verification Log` block listing UAT-6-01 through UAT-6-10 exists in GmailSyncControllerTests.swift
  </acceptance_criteria>
  <done>Spies + 5 test files exist; suite compiles; targeted tests fail RED; all new files registered in pbxproj so the project builds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test bundle → app types | Tests use `@testable import MyHome`; spies replace OS-touching ports. No real Keychain/OAuth crosses this boundary in Wave 0. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-W0-01 | Tampering | pbxproj edit introduces malformed build phase | mitigate | Verify task runs `xcodebuild test` — a broken pbxproj fails the build, gating the commit |
| T-06-W0-02 | Information Disclosure | stub controller accidentally persists a token | mitigate | Wave 0 has no real tokens; `accessToken` stays in-memory var, `refresh_token` only ever passes through SpyKeychainStore (in-memory dict) |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | Zero external packages this phase (RESEARCH §Package Legitimacy Audit: all first-party Apple frameworks); no install tasks |
</threat_model>

<verification>
- `xcodebuild test -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17'` BUILDS SUCCEEDED (whole project compiles with new files)
- Targeted Phase 6 suites run RED (intentional — Nyquist gate)
- All Phase 1–5 tests remain GREEN (no regression from pbxproj changes)
</verification>

<success_criteria>
- 11 new files created and registered in pbxproj
- Full project compiles on iPhone 17 simulator
- PKCETests / GmailAuthURLTests / KeychainPortTests / GmailSyncControllerTests / RelativeTimestampTests all FAIL RED against stubs
- No SystemGmailAuth / SystemKeychainStore implementations yet (deferred to plan 04)
</success_criteria>

<output>
Create `.planning/phases/06-gmail-sign-in-client/06-01-SUMMARY.md` when done.
</output>
