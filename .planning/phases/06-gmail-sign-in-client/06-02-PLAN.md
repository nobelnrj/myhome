---
phase: 06-gmail-sign-in-client
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - MyHomeApp/Gmail/PKCE.swift
  - MyHomeApp/Features/Gmail/GmailSyncController.swift
  - MyHomeApp/Gmail/RelativeTimestamp.swift
  - MyHome.xcodeproj/project.pbxproj
autonomous: true
requirements: [ING-01, ING-05, SET-05]
must_haves:
  truths:
    - "PKCE.generate() produces a 43–128 char base64url verifier and a SHA256-derived base64url challenge"
    - "buildAuthorizationURL emits every required Google OAuth query parameter, correctly encoded"
    - "A Date.relativeToNow helper renders 'X hours ago'-style strings"
  artifacts:
    - path: "MyHomeApp/Gmail/PKCE.swift"
      provides: "real CryptoKit + SecRandomCopyBytes PKCE implementation"
      contains: "SHA256.hash"
    - path: "MyHomeApp/Gmail/RelativeTimestamp.swift"
      provides: "Date.relativeToNow extension using RelativeDateTimeFormatter"
      contains: "RelativeDateTimeFormatter"
  key_links:
    - from: "MyHomeApp/Features/Gmail/GmailSyncController.swift"
      to: "MyHomeApp/Gmail/PKCE.swift"
      via: "buildAuthorizationURL consumes PKCE.challenge"
      pattern: "code_challenge"
---

<objective>
Wave 2 (pure logic GREEN): implement the three fully-unit-testable pieces of Phase 6 with no
OS/network/Keychain dependency — PKCE math (ING-01), the Google authorization-URL builder
(ING-01), and the relative-timestamp display helper (ING-05/SET-05).

Purpose: Turn PKCETests, GmailAuthURLTests, and RelativeTimestampTests from RED to GREEN.
These are pure transforms — the highest-confidence, highest-leverage logic to lock down before
the controller state machine (plan 03) and the production conformers (plan 04) build on them.

Output: PKCE.generate() implemented, buildAuthorizationURL implemented, Date.relativeToNow added.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-gmail-sign-in-client/06-RESEARCH.md
@.planning/phases/06-gmail-sign-in-client/06-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement PKCE.generate() (CryptoKit + SecRandomCopyBytes)</name>
  <files>MyHomeApp/Gmail/PKCE.swift</files>
  <read_first>
    - MyHomeApp/Gmail/PKCE.swift (current stub from plan 01)
    - MyHomeTests/PKCETests.swift (the failing tests this must satisfy)
    - .planning/phases/06-gmail-sign-in-client/06-RESEARCH.md (Pattern 1, lines 211–252 — full ready-to-copy implementation)
  </read_first>
  <behavior>
    - generate() returns verifier with 43 <= count <= 128
    - verifier contains only base64url chars (A-Z a-z 0-9 - _), no '=' padding, no '+' or '/'
    - challenge = base64url(SHA256(ascii bytes of verifier)), non-empty, != verifier
    - two successive generate() calls return different verifiers (random)
  </behavior>
  <action>
    Replace the stubbed `static func generate() throws -> PKCE` body with the RESEARCH Pattern 1
    implementation: allocate 32 bytes, fill via `SecRandomCopyBytes(kSecRandomDefault, 32, &bytes)`,
    throw `PKCEError.failedToGenerateRandomBytes` on non-`errSecSuccess`. Base64url-encode the bytes
    (`.base64EncodedString()` then replace "=" with "", "+" with "-", "/" with "_") to form `verifier`.
    Hash the ASCII bytes of the verifier with `CryptoKit.SHA256.hash(data:)`, base64url-encode the
    digest the same way to form `challenge`. `import CryptoKit` + `import Security`. Keep the struct's
    stored props `let verifier`/`let challenge` and `enum PKCEError`.
  </action>
  <verify>
    <automated>cd "/Users/reo/My Projects/my-home" && xcodebuild test -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:MyHomeTests/PKCETests 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `MyHomeApp/Gmail/PKCE.swift` calls `SecRandomCopyBytes` and `SHA256.hash`
    - PKCE.swift contains no `fatalError` / unimplemented stub
    - `xcodebuild test ... -only-testing:MyHomeTests/PKCETests` reports TEST SUCCEEDED (all PKCE tests green)
  </acceptance_criteria>
  <done>PKCETests passes; verifier/challenge satisfy RFC 7636 length + base64url shape.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement buildAuthorizationURL + Date.relativeToNow</name>
  <files>MyHomeApp/Features/Gmail/GmailSyncController.swift, MyHomeApp/Gmail/RelativeTimestamp.swift, MyHome.xcodeproj/project.pbxproj</files>
  <read_first>
    - MyHomeApp/Features/Gmail/GmailSyncController.swift (current stub buildAuthorizationURL from plan 01)
    - MyHomeTests/GmailAuthURLTests.swift and MyHomeTests/RelativeTimestampTests.swift (failing tests to satisfy)
    - .planning/phases/06-gmail-sign-in-client/06-RESEARCH.md (Pattern 3 lines 342–361 auth URL; Pattern 9 lines 681–688 RelativeDateTimeFormatter)
    - MyHome.xcodeproj/project.pbxproj (BiometricAuthPort entry pattern for registering the new RelativeTimestamp.swift)
  </read_first>
  <behavior>
    - buildAuthorizationURL returns a URL whose query contains: client_id, redirect_uri, response_type=code, scope=https://www.googleapis.com/auth/gmail.readonly, code_challenge (== pkce.challenge), code_challenge_method=S256, access_type=offline, state (== passed state), prompt=consent
    - Date.relativeToNow for a date 2 hours in the past returns a string containing "hour"
  </behavior>
  <action>
    In GmailSyncController.swift replace the stub `buildAuthorizationURL(clientID:redirectURI:pkce:state:)`
    with the RESEARCH Pattern 3 implementation: base URL `https://accounts.google.com/o/oauth2/v2/auth`,
    queryItems for client_id, redirect_uri, response_type=code, scope
    `https://www.googleapis.com/auth/gmail.readonly`, code_challenge=pkce.challenge,
    code_challenge_method=S256, access_type=offline, state, prompt=consent. Return `components?.url`.
    Keep it `nonisolated` so it is callable from tests without main-actor hop.

    Create MyHomeApp/Gmail/RelativeTimestamp.swift: `extension Date { var relativeToNow: String { ... } }`
    using `RelativeDateTimeFormatter` with `.unitsStyle = .full` and `localizedString(for: self, relativeTo: Date())`
    (RESEARCH Pattern 9). `import Foundation`. Register RelativeTimestamp.swift in pbxproj (PBXBuildFile +
    PBXFileReference + MyHomeApp Gmail group child + MyHome target Sources phase), mirroring the
    BiometricAuthPort.swift entries with a new A/F stable ID.
  </action>
  <verify>
    <automated>cd "/Users/reo/My Projects/my-home" && grep -q "RelativeTimestamp.swift" MyHome.xcodeproj/project.pbxproj && xcodebuild test -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:MyHomeTests/GmailAuthURLTests -only-testing:MyHomeTests/RelativeTimestampTests 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `GmailSyncController.swift` buildAuthorizationURL emits query items including `access_type=offline`, `code_challenge_method=S256`, `prompt=consent`, and scope `gmail.readonly`
    - `RelativeTimestamp.swift` exists, contains `RelativeDateTimeFormatter`, and is registered in pbxproj
    - `xcodebuild test ... -only-testing:MyHomeTests/GmailAuthURLTests -only-testing:MyHomeTests/RelativeTimestampTests` reports TEST SUCCEEDED
  </acceptance_criteria>
  <done>GmailAuthURLTests and RelativeTimestampTests pass green; RelativeTimestamp.swift registered in project.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app → Google authorization endpoint | buildAuthorizationURL constructs the URL the system browser will open; PKCE challenge + state are the only secrets-adjacent values placed in it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-PKCE | Spoofing/Elevation | PKCE.generate() | mitigate | Use SecRandomCopyBytes (CSPRNG) + CryptoKit.SHA256 per RFC 7636; never arc4random/drand48 (RESEARCH Don't Hand-Roll); verifier length asserted 43–128 by PKCETests |
| T-06-CSRF | Tampering | buildAuthorizationURL | mitigate | `state` query param included so the callback can be validated against the request (validated in plan 03/04) |
| T-06-SCOPE | Elevation of Privilege | scope param | mitigate | Hardcode scope to `gmail.readonly` only (D6-03); GmailAuthURLTests asserts exact scope string |
| T-06-SC | Tampering | package installs | accept | Zero external packages; CryptoKit/Security are first-party (RESEARCH §Package Legitimacy Audit) |
</threat_model>

<verification>
- PKCETests, GmailAuthURLTests, RelativeTimestampTests all GREEN
- Full suite still BUILDS SUCCEEDED on iPhone 17 simulator
- GmailSyncControllerTests / KeychainPortTests may remain RED (handled by plan 03)
</verification>

<success_criteria>
- PKCE math, auth-URL builder, and relative-timestamp helper implemented and unit-test green
- Scope locked to gmail.readonly; access_type=offline + prompt=consent + S256 present
- RelativeTimestamp.swift registered in pbxproj
</success_criteria>

<output>
Create `.planning/phases/06-gmail-sign-in-client/06-02-SUMMARY.md` when done.
</output>
