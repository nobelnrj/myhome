---
phase: 06-gmail-sign-in-client
plan: 03
type: execute
wave: 3
depends_on: ["06-01", "06-02"]
files_modified:
  - MyHomeApp/Features/Gmail/GmailSyncController.swift
autonomous: true
requirements: [ING-02, ING-03, ING-05, ING-16, SEC-03, SET-04, SET-05]
must_haves:
  truths:
    - "signIn() authorizes, exchanges the code, stores the refresh token in Keychain, and immediately syncs newer_than:30d"
    - "sync() proactively refreshes the access token when within 5 minutes of expiry, then transitions idle→syncing→done and writes lastSyncedAt"
    - "An expired refresh token (invalid_grant) sets syncStatus = .tokenExpired"
    - "scenePhaseChanged(.active) surfaces .tokenExpired when the token is already expired"
    - "signOut() deletes the Keychain refresh token and clears all sync UserDefaults"
    - "Reconnect overwrites the old refresh token with the new one"
  artifacts:
    - path: "MyHomeApp/Features/Gmail/GmailSyncController.swift"
      provides: "full signIn/sync/signOut/refresh/expiry state machine driving SyncStatus"
      contains: "kSecAttrAccessibleAfterFirstUnlockThisDeviceOnly"
  key_links:
    - from: "GmailSyncController.signIn"
      to: "keychain.save(forKey: \"refresh_token\")"
      via: "store refresh token after exchangeCode"
      pattern: "keychain.save"
    - from: "GmailSyncController.sync"
      to: "auth.refreshToken"
      via: "proactive refresh when needsProactiveRefresh"
      pattern: "refreshToken"
---

<objective>
Wave 3 (controller state machine GREEN): implement the GmailSyncController sign-in / sync /
sign-out / proactive-refresh / expiry logic against the injected GmailAuthPort + KeychainPort,
turning GmailSyncControllerTests (and the SEC-03 portions of KeychainPortTests usage) GREEN.

Purpose: This is the heart of the phase — the @Observable state machine the UI binds to. It is
fully unit-testable through the spies (no real OAuth/Keychain/network), so it lands here with high
confidence before the production conformers + UI wiring in plan 04.

Output: GmailSyncController.signIn/sync/signOut/scenePhaseChanged fully implemented; all
GmailSyncControllerTests green. Covers D6-06/07/08/09/10/11/12/13/17 and ING-02/03/05/16, SET-04/05, SEC-03 (spy path).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-gmail-sign-in-client/06-CONTEXT.md
@.planning/phases/06-gmail-sign-in-client/06-RESEARCH.md
@.planning/phases/06-gmail-sign-in-client/06-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: signIn / first-sync / sign-out / reconnect</name>
  <files>MyHomeApp/Features/Gmail/GmailSyncController.swift</files>
  <read_first>
    - MyHomeApp/Features/Gmail/GmailSyncController.swift (current state after plans 01–02)
    - MyHomeApp/Security/LockController.swift (analog: authenticate() async flow, App Group defaults writes, mapError shape)
    - MyHomeTests/GmailSyncControllerTests.swift (the failing tests defining exact expected behavior)
    - .planning/phases/06-gmail-sign-in-client/06-CONTEXT.md (D6-08 first-sync, D6-12 reconnect overwrite, D6-17 sign-out, D6-19 raw error)
    - .planning/phases/06-gmail-sign-in-client/06-RESEARCH.md (Pattern 7 controller; Pattern 3 prompt=consent on reconnect; Open Question #4 connected-email via userinfo)
  </read_first>
  <behavior>
    - signIn(): set syncStatus=.authorizing; call auth.authorize(authURL:callbackScheme:) with URL from buildAuthorizationURL (clientID/redirectURI from GmailOAuthConfig — see action); on code, call auth.exchangeCode(...); if TokenResponse.refresh_token is non-nil, keychain.save(it, forKey:"refresh_token") and set accessToken + accessTokenExpiry (now + expires_in); store connectedEmail; then immediately call sync() (D6-08). On thrown GmailAuthError set authError + syncStatus=.error(rawMessage) (D6-19). Reconnect path: a second signIn overwrites the stored refresh token.
    - signOut(): keychain.delete(forKey:"refresh_token"); clear accessToken (nil), accessTokenExpiry, lastSyncedAt, connectedEmail; syncStatus=.idle (D6-17).
  </behavior>
  <action>
    Implement `signIn() async` and `signOut()` per the behavior block. For clientID/redirectURI,
    reference a `GmailOAuthConfig` type with static `clientID`/`redirectURI`/`callbackScheme` —
    declare a placeholder `enum GmailOAuthConfig` inside GmailSyncController.swift now with
    TODO sentinel values (e.g. clientID = "REPLACE_IN_PLAN_04"); plan 04 replaces it with the real
    committed config file. Tests inject SpyGmailAuth so the real values are never exercised here.
    On exchangeCode, guard `refresh_token != nil` else set syncStatus=.error("no refresh token —
    missing access_type=offline") (RESEARCH Pitfall 2). Map thrown GmailAuthError.oauthError(msg)
    straight into syncStatus=.error(msg) (D6-19 raw display). signOut clears every gmail_* UserDefaults key.
    Do NOT call real Keychain/network — everything goes through the injected `auth`/`keychain` ports.
  </action>
  <verify>
    <automated>cd "/Users/reo/My Projects/my-home" && xcodebuild test -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:MyHomeTests/GmailSyncControllerTests 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `GmailSyncController.signIn` calls `auth.authorize`, `auth.exchangeCode`, and `keychain.save(..., forKey: "refresh_token")`
    - signIn guards non-nil refresh_token and sets `syncStatus = .error` with the raw message on GmailAuthError.oauthError (D6-19)
    - `signOut` calls `keychain.delete(forKey: "refresh_token")` and clears gmail_last_synced_at / gmail_access_token_expiry / gmail_connected_email
    - sign-out + sign-in (reconnect) tests in GmailSyncControllerTests pass (new refresh token overwrites old in SpyKeychainStore)
  </acceptance_criteria>
  <done>sign-in / first-sync trigger / sign-out / reconnect-overwrite tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: sync(), proactive refresh, and token-expiry CTA</name>
  <files>MyHomeApp/Features/Gmail/GmailSyncController.swift</files>
  <read_first>
    - MyHomeApp/Features/Gmail/GmailSyncController.swift (after Task 1)
    - MyHomeTests/GmailSyncControllerTests.swift (sync / needsProactiveRefresh / isTokenExpired / scenePhase tests)
    - .planning/phases/06-gmail-sign-in-client/06-CONTEXT.md (D6-06 5-min window, D6-10 newer_than:30d/last_synced_at, D6-11 foreground expiry check, D6-13 sync-time expiry)
    - .planning/phases/06-gmail-sign-in-client/06-RESEARCH.md (Pattern 5 refresh; invalid_grant → tokenExpired line 446; Pattern 8 listMessages query)
  </read_first>
  <behavior>
    - needsProactiveRefresh: true when accessTokenExpiry is nil OR within 300s of now(); false when > 300s away
    - sync(): if needsProactiveRefresh, load refresh_token from keychain; if nil → syncStatus=.tokenExpired; else auth.refreshToken(...) → update accessToken + accessTokenExpiry; on invalid_grant/expired refresh, syncStatus=.tokenExpired (ING-16). Then set syncStatus=.syncing, perform the fetch query (newer_than:30d on first sync when lastSyncedAt nil, else newer_than:<since last sync>), write lastSyncedAt=now(), syncStatus=.done (ING-02/03/05).
    - scenePhaseChanged(.active): if isTokenExpired, syncStatus=.tokenExpired (D6-11)
  </behavior>
  <action>
    Implement `sync() async` and finalize `needsProactiveRefresh` / `isTokenExpired` / `scenePhaseChanged`.
    Use the injectable `now` closure everywhere (never `Date()` directly) so tests control time.
    Detect refresh-token expiry by mapping a thrown auth error representing invalid_grant (the spy
    throws via shouldThrowOnRefresh) to `syncStatus = .tokenExpired` (RESEARCH line 446, D6-13).
    Compute the query string: `lastSyncedAt == nil ? "newer_than:30d" : "newer_than:\(daysSince)d"`
    (D6-10) — the actual message fetch in Phase 6 only needs the query + a listMessages call routed
    through the port; full message-body parsing is Phase 7. Write `lastSyncedAt = now()` only on success.
    scenePhaseChanged checks `phase == .active && isTokenExpired` → `.tokenExpired`, with `@unknown default: break`.
  </action>
  <verify>
    <automated>cd "/Users/reo/My Projects/my-home" && xcodebuild test -scheme MyHome -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:MyHomeTests/GmailSyncControllerTests -only-testing:MyHomeTests/KeychainPortTests 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `needsProactiveRefresh` returns true for expiry within 300s and for nil expiry; false for expiry > 300s (assert via injected `now`)
    - `sync` transitions syncStatus idle→syncing→done and writes `lastSyncedAt` on success
    - A thrown refresh error (SpyGmailAuth.shouldThrowOnRefresh) sets `syncStatus == .tokenExpired` (ING-16)
    - `scenePhaseChanged(.active)` with an expired token sets `syncStatus == .tokenExpired`
    - `xcodebuild test ... -only-testing:MyHomeTests/GmailSyncControllerTests -only-testing:MyHomeTests/KeychainPortTests` reports TEST SUCCEEDED
  </acceptance_criteria>
  <done>All GmailSyncControllerTests and KeychainPortTests green; sync/refresh/expiry state machine complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| controller → KeychainPort | refresh token crosses into secure storage; controller must never write it to UserDefaults |
| controller → GmailAuthPort | access token / refresh token returned from Google flow through here; access token stays in-memory only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-TOKEN | Information Disclosure | refresh/access token handling | mitigate | refresh_token → keychain.save only (never UserDefaults); accessToken is an in-memory `var` (D6-07); only accessTokenExpiry timestamp persisted to UserDefaults; signOut deletes keychain item |
| T-06-EXPIRE | Denial of Service | stale token blocking sync | mitigate | proactive 5-min refresh (D6-06) + invalid_grant → .tokenExpired reconnect CTA (ING-16) so the user is never silently stuck |
| T-06-RAWERR | Information Disclosure | D6-19 raw Google error display | accept | user-chosen debug-friendly raw error display; single-user private app, errors shown only in-app to the owner |
| T-06-SC | Tampering | package installs | accept | zero external packages this phase |
</threat_model>

<verification>
- GmailSyncControllerTests + KeychainPortTests GREEN
- PKCETests / GmailAuthURLTests / RelativeTimestampTests still GREEN (no regression)
- Full suite BUILDS SUCCEEDED on iPhone 17 simulator
</verification>

<success_criteria>
- Full sign-in → store-token → first-sync → sign-out → reconnect → proactive-refresh → expiry-CTA state machine implemented and unit-test green via spies
- refresh_token never persisted outside Keychain; access_token in-memory only
- newer_than:30d first-sync query; invalid_grant → .tokenExpired
</success_criteria>

<output>
Create `.planning/phases/06-gmail-sign-in-client/06-03-SUMMARY.md` when done.
</output>
